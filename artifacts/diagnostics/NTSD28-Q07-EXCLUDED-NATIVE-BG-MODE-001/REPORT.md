# Excluded native background and mode inputs

User confirmed on 2026-09-24 that original NTSD background DAT and both mode DAT families are outside the Unity project's content scope. The archive is a verified copy of 135 files, with each entry and SHA-256 in archive-manifest.csv. It contains the 50 background/background-mode DATs introduced in this turn, their 76 metas, the unused new parser/test plus metas, and copies of the two previously staged production mode DATs plus three metas.

Initial archive state: the first archive-and-remove command was rejected by automatic policy review with only `blocked by policy`, so no deletion was attempted. The user then authorized moving the excluded files into a unified place for their own deletion. A reversible PowerShell `Move-Item` operation, with resolved workspace/quarantine path checks and no `Remove-Item`, moved the 50 newly staged background/background-mode DATs, 76 metas and unused parser/test pair plus metas (130 files) out of `Assets`; after the independent project mode Asset was connected and tested, it also moved the two previously staged native mode DATs and three metas. The verified 135-entry zip was moved into the same folder. The sole cleanup target is now `for-user-deletion/` (136 files including the zip). The original DAT/script paths are absent from `Assets`; no original resource file was deleted. The user may delete this folder after reviewing it.

The project-owned `ProjectBattleModeConfig.asset` now supplies immutable battle mode values to configured production prewarm; the old DAT parser remains only as a historical explicit diagnostic route. Focused original-Editor tests, standard SelfCheck, isolated Play without a native mode DAT and formal-content Play after the project Asset switch are recorded in `NTSD28-Q07-PROJECT-MODE-PUBLICATION-001` Change Record. This replaces the preliminary statement below that production still reads the old mode DATs.

Q07 next bounded task: refresh current content inventory after exclusion and continue remaining in-scope battle content/behavior; do not reintroduce the original background or mode DATs. Old history in the alignment doc is superseded by its current top correction and Q07 Next action.
