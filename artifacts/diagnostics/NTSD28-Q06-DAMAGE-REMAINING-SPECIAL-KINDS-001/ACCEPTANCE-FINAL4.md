# Final four-case throwing-tail return and package acceptance

This supersedes this package's earlier `ACCEPTANCE.md` for state1002 missing-frame coverage. Its earlier 17-kind source/Unity evidence remains valid; the only later production change was the type3 throwing-tail selected-action binder.

Formal EXE SHA-256 remained `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; playable source manifest remained `07CD47A0623F23D2C439E0E85EABF2ED10F8EAE8FC7D70DDB8396C704B3D778F`. Four-case throwing-tail source double run was byte-identical SHA-256 `9AAB24E8D3253F3DF6A4FC72E08FCD1F093BFCC804ECE4D8FAF53C2BD4A35C6C`. Case3 declares only random action0; seed42 selects action3, and source immediately uses its implicit frame descriptor `wait=0`. Unity RED job `97b37a1d` matched initial and other observed fields but used `wait=1` immediately and after the following full tick. After only this call changed to `DirectWriteNativeRawFramePreserveWaitCounter`, focused job `393b7878` passed all four cases: initial/impact/following raw50, descriptors, pending impulse, RNG and calls. `throw-unity-pass4.json` records four rows and zero differences. `throw-unity-red.json` is a generated historical filename that now contains the latest pass output; the RED job is preserved in the Change Record.

After the final production edit, Unity compilation showed zero C# errors and the stable `BattleRuntimeSelfCheck` returned PASS at 2026-09-21 15:12:22 UTC (`selfcheck-final4.result`). Real NTSD_Battle Play at 15:13:27 UTC returned PASS for 18 case-profile invocations: special-kind six plus state1002 three (including missing-frame) in Authority400/DataOriented/actual Renderer and MobileExtended/Legacy/logic. Scene checksum unchanged, Renderer borrowers 2→2 (`play-final18.json`). Its Q05 snapshot restore and ordered shutdown returned PASS: objects 4→4, final World/slots/logic and render borrowers all 0, Stopped after two frames (`ordered-close-final18.json`). Unity exited Play. Scene file SHA-256 remains `BCD1047BF912C6A4A8BC9F3A76EAF3FA954211AD064E0402B1C01BF3BA0E9FB6`.

No Scene, resource, importer, nonbattle, Unity/GAS framework, or approved exception changed. This package is verified for declared synthetic-content battle behavior and real Renderer/close paths. Q06 batch exit must still reconcile its other producer/consumer packages and map Q07/Q08/Q09/Q12 dependent returns; it does not prove natural-skill or audiovisual parity on the future Logan DAT/images.
