# NTSD28-Q09-KILL-ICON-PUBLICATION-001

Status: PLANNED before script modification. Parent BATCH-05/Q09, R17. This package owns only the seven-slot, three-image selected knockout icon Sprite publication; screen consumption, Canvas placement and native pixel comparison are downstream Q09 work.

Authority: formal root NTSD2.8-Logan.exe SHA B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033; playable `render_snapshot.cpp:2019-2034` selects type 1..6 or fallback 0 and keeps path availability; `d3d11_renderer.cpp:2076-2093` draws the whole selected PNG at native size. Selected `data/mode/ntsd.dat` maps seven pic_type slots to three unique files: c.png, sk1.png and sk2.png. Their exact staged bytes and seven-slot candidate SHA/presence freshness are already covered by Q09 content and identity packages.

Declared code ownership:
- `Assets/NTSD/Scripts/Animation/Runtime/BattleSpriteCatalog.cs`: immutable optional seven-slot icon Sprite table on the existing battle catalog, preserved through central-binding remap, with a read-only type lookup; no entity key or ordinary UI changes.
- `Assets/NTSD/Scripts/Animation/Manager/CharacterAnimtorManager.cs`: verified PNG decode, deduplicated three-image staging, main-thread full-image Texture2D/Sprite creation, existing prewarm ownership/cancellation sets, and attachment to the final catalog immediately before the existing no-await atomic commit.
- New `Assets/NTSD/Scripts/Test/Editor/NTSD28Q09KillIconPublicationEditorTests.cs` plus meta: one scoped production prewarm fixture with formal selected mode and three formal PNGs; assert seven aliases, three unique Sprite/Texture objects, image dimensions/bytes, catalog preservation and old-generation retirement behavior reachable in the fixture.

No Scene, Prefab, GameConfig, regular HUD/menu, World/tick/input/stats, audio, old resource deletion, third-party/generated or other project paths. Existing dirty files and user-confirmed HUDBg x=30 remain protected.

Contract: missing selected icon path remains absent; an invalid present PNG fails before publication and retains the previous generation. Exact selected SHA is checked by BMPLoader during decode and again by existing candidate AssertInputsCurrent before commit. Type aliases share the same Sprite only when their selected absolute image path and SHA match. The catalog is immutable after commit. The selected Sprite is owned by the same publication and old generation retires only after frozen-frame/renderer catalog leases release. Icons are not added to the world-camera atlas because the formal order is after mode HUD; a later battle-only screen consumer must read the bound catalog generation, not the manager's mutable current generation.

Validation: original project compile; one focused original-Editor production prewarm test and failure/retirement checks; `git diff --check`; `Tools/Validate-ChangeLedger.ps1`. Actual Scene draw coordinates, glyphs, exit/re-enter, formal EXE pixel comparison remain Q09/R17 acceptance. Rollback is reviewed reversal of this ID's exact changes only, preserving all other dirty work.

2026-09-22 pre-edit focused-test completion: the first test revision checks aliases and lease retirement but has not yet exercised the declared malformed-present-PNG failure. Within the already declared new test file only, extend that same production prewarm fixture: after a valid publication, replace its own Temp fixture c.png with invalid bytes, capture a new candidate, expect the production load to fail before commit, and assert the old catalog and icon remain live. Then perform the existing lease-retirement assertions. Do not alter production code or any staged/formal source file for this test case; only the test's own Temp fixture may be mutated. Original Editor is currently importing and bridge commands time out; do not launch a second Editor or run concurrent tests.
