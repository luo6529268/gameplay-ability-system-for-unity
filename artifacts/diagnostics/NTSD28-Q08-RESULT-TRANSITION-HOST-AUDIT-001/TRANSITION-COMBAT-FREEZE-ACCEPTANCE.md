# Q08 native result transition combat-core freeze

Status: `FOCUSED_TEST_PASS / ISOLATED_SELFCHECK_PASS / HOST_PENDING` (2026-09-22). Task/Change: `NTSD28-Q08-NATIVE-TRANSITION-COMBAT-FREEZE-001`.

The formal root EXE SHA-256 remains `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; formal `game_session.cpp` and `battle_flow.cpp` hashes remain `9CCB6E12...65E4189` and `7BED51E3...5676AF`. Prior exact-source complete-session fixtures double-ran mode4 349→350→next and ordinary 2→1/standalone rematch branches. The original isolated Unity mode4 RED measured FrameSequence349→350 on the transition tick. Two new [prechange RED cases](UNITY-ADDITIONAL-FREEZE-RED.xml) compiled and failed 0/2 at ordinary FrameSequence expected0/actual1 and mode4 old Results `PendingHostAction` expected0/actual1.

`NTSDBattleTickSystem.RunTick` now runs the native result classifier before Unity combat passes. If it emits or retains a nonzero transition, the method returns successful host-step completion before combat-flow header, spark, human/character input, frame, physics, collision, world clock, old Results writer, render dispatch and combat ECS shadow refresh. Ordinary command2 may become1 on the following call; mode-specific 28/128/202 stay distinct. The three targeted [postchange transition cases](UNITY-TRANSITION-CORE-POST.xml) passed 3/3, including mode4 349→350→next, ordinary 2→1 with unchanged old clock, and denial of post350 old Results writes. The combined [Q08 and Results input classes](UNITY-TRANSITION-CORE-ADJACENT.xml) passed 17/17. The isolated [full SelfCheck log](UNITY-TRANSITION-CORE-SELFCHECK.log) records both battle runtime PASS and editor completion.

This closes only the combat-core stop. `SimulationTickDriver.StepOneTickInternal` currently increments its host tick, applies `FrameInputSet`, computes checksum and publishes sounds around this call; AppManager has no production consumer for native transition2/28/128/202. The next host integration must stop or route automatic ticks without violating Manual/Lockstep, use existing Unity selection UI for ordinary mode while preserving the nonbattle UI logic, and handle the distinct mode commands and battle-only rematch. Same-seed snapshot/replay and representative original Editor/Player battle enter/exit/re-enter remain unverified. The Unity result-page visual exception remains. No computer-use, Scene, DAT, image, deletion or nonbattle edit occurred; the original Scene SHA-256 remains `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`.

Governance: [Change Ledger validation](TRANSITION-CORE-LEDGER.log) passed 665 records/six governed code diff files; `git diff --check` exited 0. These checks cover the code-change record and whitespace, not the pending real-session behavior.

2026-09-25 original Editor evidence correction: focused job `0a164411360a4854a799026c81256e40` ran the three existing core freeze/transition tests in the original project, with total3/passed3/failed0/skipped0. The saved `original-editor-transition-core-three-tests-20260925.json` has SHA-256 `E1059BAD07B014A4659432D152234D0B710AE7DE0E6084804B24FD4978DDA134`; current Battle and Menu Scene disk SHA-256 remained `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A` and `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`. This supersedes the earlier absence of original-project focused evidence for the combat-core stop. Later `NTSD28-Q08-TRANSITION-HOST-TICK-ADMISSION-001` already guards old-world host tick admission; the older sentence saying the driver *currently* applies another input/tick is a historical pre-host snapshot. Command routing and natural Play remain separate open Q08 gates.
