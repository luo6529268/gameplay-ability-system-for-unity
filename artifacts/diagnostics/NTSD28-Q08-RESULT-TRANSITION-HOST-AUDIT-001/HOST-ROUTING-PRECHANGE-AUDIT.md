# Q08 native result transition host routing prechange audit

Status: `READ_ONLY_ROUTE_OWNERS_IDENTIFIED / IMPLEMENTATION_PENDING` (2026-09-22). The core combat and driver old-world admission guards are separately recorded in `TRANSITION-COMBAT-FREEZE-ACCEPTANCE.md` and `HOST-TICK-ADMISSION-ACCEPTANCE.md`; neither consumes a transition command.

Formal `GameSession28::step()` emits ordinary command2 at timer350, skips combat, then `BattleFlow28::step()` changes 2→1 on the following upper-state call. The complete-session ordinary fixture observed entry to selection with the same native World. Under explicit `battle_scene_only_loop`, that following call instead recreates the battle from the unchanged configuration; mode4 command202 is distinct. The source `battle_flow.cpp` leaves commands28/128/202 to mode-specific native helpers. These are source/fixture facts, not authority to map all commands to one Unity menu destination.

Current Unity owners: `SimulationTickDriver.Update` can see the native transition after a synchronous tick or after `ConsumeDedicatedSimulationWorkerPublication`; it currently has no Results transition dispatch. `AppManager.UnloadBattle()` performs ordered shutdown and additive Battle Scene unload, then unconditionally sets `MenuMain` and calls `MenuUIController.ShowMainMenu()`. `MenuUIController` already exposes `ShowSelectCharacter()`; `CharacterSelectionController.ResetAll()` resets its own selection state. The existing selection confirm path calls `AppManager.SetMatchConfig()` then `LoadBattleAdditive()`. These are sufficient entrypoints for a battle-specific ordinary return-to-Unity-selection request without changing nonbattle UI logic, but no such request exists yet. The production `PendingHostAction` field in the old Results writer has no discovered AppManager reader and must not be mistaken for this native command.

Deployment boundary: `ProjectSettings/EditorBuildSettings.asset` currently has `m_Scenes: []`; the earlier Q07 Menu→Battle callback Play case failed at additive Battle Scene loading. A result-to-selection Editor request can be tested in a loaded Menu+Battle scene fixture, but a complete packaged Player return-to-selection/rematch requires an explicit build scene/deployment decision. Do not silently add Scenes or derive formal `battle_scene_only_loop` solely from Menu Scene presence. The current Q07 battle-only Player probes are not proof of general menu routing.

Next governed host task should declare `SimulationTickDriver` and battle-specific `AppManager` methods before script edits. It must dispatch ordinary command2 exactly once on the main thread after the tick/worker publication boundary and after ordered battle shutdown, keep existing Unity selection UI, preserve pretransition `MatchConfig`, and specify an explicit battle-only loop owner before implementing rematch. Commands28/128/202 need separate authority-backed routes or a clearly exposed pending state. Focused tests should cover no repeated dispatch, no extra old-world frame, shutdown ordering, selected menu state, battle-only fresh World, and Scene hash; then real Menu+Battle and standalone Player validation. This is a route contract, not a completed implementation or permission to change nonbattle selection logic.
