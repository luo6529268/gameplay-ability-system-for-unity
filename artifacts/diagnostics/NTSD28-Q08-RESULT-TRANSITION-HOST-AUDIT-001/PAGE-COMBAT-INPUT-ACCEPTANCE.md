# Q08 Unity result-page combat input isolation

Status: `FOCUSED_TEST_PASS / RUNTIME_PENDING` (2026-09-22). Task/Change: `NTSD28-Q08-RESULT-PAGE-COMBAT-INPUT-ISOLATION-001`.

Formal playable `GameSession28::step()` continues combat with participant input before timer350. P-19/G-08 allow Unity's own result-page visuals; they do not permit its visibility flag to suppress battle input. In `NTSDBattleTickSystem.RunTick`, the old `Results.IsActive` flag gated human polling, CharacterInput and battle-entry clear. The same-tick active/inactive-page [prechange Unity RED](UNITY-PAGE-INPUT-RED.xml) compiled and failed 0/1 at the exact active-page human poll: expected true, actual false. The inactive-page control was true.

Only the three combat gates were decoupled from the page flag. `resultsActiveAtTickStart` remains in the post-world Results UI path, preserving existing P1/P2 page input. The old test that expected null human polling was corrected to test the combat-owned input clear followed by the next full world tail. Isolated Unity [focused class](UNITY-PAGE-INPUT-POST.xml) compiled and passed 4/4; both adjacent [Q08 held-continue cases](UNITY-PAGE-INPUT-ADJACENT.xml) passed 2/2.

The first full [SelfCheck log](UNITY-PAGE-INPUT-SELFCHECK.log) failed at the historical `BATTLE-AUDIT7-F6` assertion: it expected no human poll and an unconsumed battle-entry clear under active Results. The exact `CheckAudit7ResultsActiveGate` fixture was corrected to start after the clear gate and require human polling; the separate focused Editor test verifies that the clear gate itself still consumes the request and returns before the full tail. The fresh [second SelfCheck log](UNITY-PAGE-INPUT-SELFCHECK-2.log) contains both `[BattleRuntimeSelfCheck] 战斗运行时自检通过。` and `[BattleRuntimeSelfCheckEditor] 自检完成。` The [Change Ledger run](PAGE-INPUT-LEDGER.log) passed 664 records/six governed code diff files, `git diff --check` exited 0, and the original Battle Scene SHA-256 remained `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`.

This is a focused shared-input seam result, not a real natural battle or whole-Q08 exit. The separate archived mode4 transition350 full-tick RED remains: combat still advances on the transition tick. Host transition, following frozen tick, next-match configuration and original Editor/Player acceptance remain pending. No computer-use, Scene, DAT, image, deletion or nonbattle edit occurred.
