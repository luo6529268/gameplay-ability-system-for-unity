# Q08 direct Battle Scene result-host Play probe

Status (2026-09-22): `VERIFIED / DIAGNOSTIC_SCOPE_ONLY`. Test/Change ID `NTSD28-Q08-DIRECT-BATTLE-RESULT-HOST-PROBE-001`. The original and isolated `NTSD_Battle.unity`, `AppManager.cs`, `SimulationTickDriver.cs` and `BattleTestBootstrap.cs` had equal SHA-256 values before testing. The original Battle Scene remained SHA-256 `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0` afterward.

The new Editor test opened the real copied Battle Scene in the isolated Unity 2022.3.62f3 project, entered Play and waited for `BattleTestBootstrap` to publish the formal content, create its direct-scene AppManager, register characters and resume a nonempty World. It then seeded native results phase3/timer350/transition2 and observed five Play frames. `UNITY-DIRECT-BATTLE-RESULT-PROBE-GPU.xml` passed 1/1: the AppManager was named `AppManager [TestBootstrap]` and stayed `MenuMain`, no `NTSD_Menu` scene was loaded, `NTSD_Battle` stayed loaded, `NativeTransitionState` stayed 2, and the old driver tick did not advance. This proves the current direct-scene result command remains pending/frozen without a battle-only host owner. It is a deliberately injected result boundary, not a natural KO or rematch test.

Two earlier attempts are not battle failures. The first `-nographics` run exhausted 900 rendered frames while official DAT conversion was still in `Preparing`; its assertions did not reach the result seam. The bounded real-time retry reached sprite publication but NullGfxDevice reported `MaxTextureSize=4096` and refused seven official oversized image sheets; this is a graphics-device limitation. The passing run omitted `-nographics`, used Direct3D 11.0 feature level 11.1, completed `BattleTestBootstrap`, and produced no `error CS` or compilation failure in its log. It exited Play and the isolated Editor process terminated. No computer-use was used.

This probe does not implement the first direct rematch, the second result selection path, native RNG/input-phase continuity, formal EXE-visible frontend parity, or the original Menu-to-Battle Player route. Q08 remains open. No production script, Scene, GameConfig, DAT/image, nonbattle UI, GAS, J: formal source, Build Settings, commit or push was changed for this probe.
