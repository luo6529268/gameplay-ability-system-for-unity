# Q08 ordinary result selection host, scoped acceptance

Status (2026-09-22): `RUNTIME_PENDING`. Formal ordinary result command2 returns to selection state1; this task connects that command to the existing Unity selection host when both Menu and Battle scenes are loaded. `SimulationTickDriver` reads it on the main thread after worker publication and after synchronous tick processing. `AppManager` reuses ordered battle shutdown/unload and shows its existing character selection view after unload. The normal `UnloadBattle()` destination remains MenuMain. No route for battle-only rematch or commands28/128/202 is claimed.

The first attempted PlayMode-platform test discovered zero tests because the class is in an Editor assembly. The first EditMode invocation with `-quit` produced no result XML. `UNITY-ORDINARY-SELECT-HOST-RED-EDIT2.xml` and `GREEN1.xml` failed because the synthetic driver remained `Preparing`; they are **invalid behavioral RED evidence** for the missing transition consumer. Corrected synthetic Play enters `Running` before marking command2. `UNITY-ORDINARY-SELECT-HOST-GREEN2.xml` passed 1/1. `UNITY-ORDINARY-SELECT-HOST-THREE.xml` passed 3/3: command2 with Menu returns to selection and destroys the old Battle driver, command202 remains pending, and command2 without Menu remains pending. `UNITY-ORDINARY-SELECT-ADJACENT.xml` passed 28/28 across the three existing host/Q08/Results classes plus these three Play cases.

`UNITY-ORDINARY-SELECT-SELFCHECK.log` records `[BattleRuntimeSelfCheck] 战斗运行时自检通过` and editor completion. `ORDINARY-SELECT-LEDGER.log` records validator PASS. `git diff --check` exited 0. Original `Assets/NTSD/Scene/NTSD_Battle.unity` SHA-256 remained `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`.

The Play scene was temporary and synthetic. The original Menu-to-Battle route, Windows Player, same-seed formal EXE/Unity full-state replay, battle-only rematch, commands28/128/202, logical result record at101, and Q08 full exit remain unverified. `ProjectSettings/EditorBuildSettings.asset` still has an empty scene list; this task did not change it, any original Scene, DAT/images, nonbattle UI code, GAS, or approved exceptions. No computer-use was used.

2026-09-25 current-state correction: the old empty-scene-list observation is no longer current. `ProjectSettings/EditorBuildSettings.asset` now contains the user-confirmed enabled Menu→Battle order, and its two GUIDs match the current Scene `.meta` files; SHA-256 `8D621A077642B5154BA305861CA57DFB549FF4FDE5BC58F3E284536A5982F01E`, with no Git diff. The original Menu→Battle result route can now be exercised without editing Build Settings. No original-route result test was run in this correction; the remaining acceptance limits above still apply.

The existing Q07 original-Scene callback report `NTSD28-Q07-MENU-SCENE-CALLBACK-PLAY-001/q07-menu-scene-closure-2.json` (SHA-256 `7B7E4BB16E9585CCD36A685A335BFF8EE281A9F1DBDB34957ED4DCEA7063968A`) previously reached a loaded additive Battle Scene, `AppFlowState.BattleRunning`, a nonempty World and ordered manual return to Menu. This proves the real Menu/Battle/AppManager preconditions were attainable at that earlier content snapshot; its source key contains the historical V2 fingerprint and it invoked `UnloadBattle()`, **not** a native result command. Current `AppManager.TryReturnToCharacterSelectionFromBattleResult()` requires BattleRunning/Paused plus loaded Menu and Battle, then performs ordered shutdown/unload before showing the existing selection view. The next original-project Q08 test must inject or naturally reach result phase3/command2 while these current preconditions hold, assert one dispatch to `MenuSelectCharacter`, no extra old-world tick, ordered shutdown/zero borrowers and unchanged Scene files. The old Q07 report cannot substitute for that result-path test.
