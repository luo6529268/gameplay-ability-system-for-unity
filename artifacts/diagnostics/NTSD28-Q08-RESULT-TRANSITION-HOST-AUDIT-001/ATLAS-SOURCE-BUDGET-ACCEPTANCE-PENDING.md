# Q08 formal-content source binding: targeted runtime acceptance

Status: `NTSD28-Q08-ATLAS-BUDGET-SOURCE-BINDING-001 / RUNTIME_PENDING` (2026-09-22). BATCH-03/Q06 remains `DELIVERED_SCOPED / Q06_LOCAL_EXIT`; Q08 and the master alignment goal remain open. No computer-use was used.

The formal Logan image plan requires 145 atlas pages, 2,432,696,320 bytes, above the existing 512 MiB desktop atlas budget. Auto formerly rejected the texture array but then built unbounded ordered pages; the previous real Scene ended at page 119 with native out-of-memory. The change applies the budget to Auto publication in both character-only and unified manager paths. Over budget, it retains already-staged `SourceTexture2D` central bindings and allocates no atlas. Explicit ordered/array modes and in-budget Auto retain their existing branches.

Focused isolated Unity EditMode validation: the first run was 3/4 because the unified publication path was still missing the condition. After correcting that path, `ATLAS-SOURCE-BUDGET-FOCUSED-AFTER.xml` passed 4/4, process exit 0, SHA-256 `A07582DA72A525982E8A07E84553852B23634497ACF13D454A556C8DE0FA3622`. The test checks the unified common-source bindings, no owned atlas resource, effective policy/diagnostic, and explicit OrderedPages behavior.

The isolated D3D11 formal-content Battle Scene test `SecondResultEntersOrdinaryUpperSelectionInSameWorld` passed 1/1, process exit 0; `UNITY-SECOND-BATTLE-SOURCE-BUDGET.xml` SHA-256 `E70E58EAA8D64150B314F843E8320F97D3CAD5CB3F5317A9A552B692D7692B77`. `UNITY-SECOND-BATTLE-SOURCE-BUDGET-MEMORY.csv` has 52 two-second samples and maximum private bytes 13,242,007,552. The opt-in atlas allocation trace produced no page-allocation line in this run. This is consistent with the source path and confirms that the previous atlas OOM did not block this target test; the focused policy test is the direct mode-selection evidence.

Still pending: representative formal-source pixel/UV and command-order acceptance in the actual Scene, formal EXE visible comparison, natural KO and the other Q08 result branches. Audio load exceptions appear in the long Unity log and require separate scope assessment; they did not fail this targeted result test. Do not report Q08 or full visual fidelity complete from this result.

Follow-up focused source-order validation: the same four-test EditMode class now also checks that Auto fallback retains the exact source binding objects (therefore their sprite, source rect and UV descriptors) and that an interleaved shadow/spark/word/shadow frame resolves into four ordered segments with the expected source textures and first-command indexes. An initial 3/4 RED exposed an incorrect test expectation that retained source bindings would be cloned; after correcting that expectation, `ATLAS-SOURCE-BUDGET-ORDER-FOCUSED-AFTER.xml` passed 4/4, process exit 0, SHA-256 `3B99B46D43B8ED3E91F64D6F57CA6D8C4A056F1CF42448122A78F13B37DB7FDA`. Two earlier `-quit` invocations exited 0 without XML and are not counted as test passes. The production Scene SHA-256 remained `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`. This is structural order/descriptor evidence, not actual Scene pixel parity.
