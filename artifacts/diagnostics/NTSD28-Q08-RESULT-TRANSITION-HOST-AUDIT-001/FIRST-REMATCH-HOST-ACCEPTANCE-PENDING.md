# Q08 first battle-only rematch host — scoped acceptance pending

`NTSD28-Q08-BATTLE-ONLY-FIRST-REMATCH-HOST-001 / RUNTIME_PENDING`. The pre-change direct Battle Scene Play RED is `BATTLE-ONLY-REMATCH-UNITY-RED.md`: formal content bootstrapped, result command 2 held the same World after 15 seconds. Formal root EXE SHA-256 remains `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The source-model first and second cycles plus RNG/input-phase transaction remain in `SECOND-CYCLE-SOURCE-WITNESS.md` and `RNG-PHASE-SOURCE-WITNESS.md`.

Production change: `SimulationTickDriver` now accepts a registered battle-only result host before its existing ordinary AppManager selection route. `BattleTestBootstrap` registers only after direct-scene startup, unregisters on destruction, and owns at most one direct rematch. On the first result command 2 it captures prior native/local RNG and input phase, uses AppManager's eleven-stage ordered shutdown with actual `BattleBootstrap.DisablePresentation` map cleanup, recreates a preparing World, reopens direct battle services, rebuilds the same scene's roster from existing override IDs/spawn points, restores streams/phase, makes the default BGM synchronized draw at `0x004021E0`, seals allocation, enables presentation and resumes. Failed shutdown prevents recreation. Existing Menu+Battle owner remains the fallback. The old pending-command diagnostic Play test now checks the direct owner and absence of Menu Scene; its prior XML remains historical RED.

Isolated Unity 2022.3.62f3 D3D11 real Battle Scene/formal-content tests: `UNITY-BATTLE-ONLY-REMATCH-RNG-AFTER.xml` 2/2 PASS. One case verifies automatic command2 dispatch recreates a nonempty World/roster and resumes ticks. The other invokes the same owner synchronously at the frozen boundary and verifies old World object count0, new World/roster nonempty, input phase1 preserved, Unity local RNG state preserved, native CRT state/calls unchanged, synchronized table unchanged, synchronized calls +1 and last site `0x004021E0`. `UNITY-BATTLE-ONLY-DIAGNOSTIC-AFTER.xml` 1/1 PASS verifies the direct owner works without Menu Scene. Adjacent ordinary-selection class `UNITY-ORDINARY-SELECT-ADJACENT-AFTER.xml` 3/3 PASS, including ordinary menu return, mode4 nonrouting, and no-menu ordinary pending. A single full batch `BattleRuntimeSelfCheck` run logged `战斗运行时自检通过` in `UNITY-BATTLE-ONLY-REMATCH-SELFCHECK.log`; no `error CS` was found in the focused logs. Original and isolated `NTSD_Battle.unity` hashes remained `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`. The isolated Editor processes exited after each run.

This establishes the first direct-rematch host seam under a synthetic source-matched result boundary. It does not verify natural KO, formal EXE visible frontend parity, every roster/configuration, or the second result's ordinary upper-selection state when no Menu Scene exists. That second result must not be mislabeled complete because `firstDirectRematchCompleted` only prevents another direct recreation; the current no-Menu AppManager route still cannot create selection. The Q08 master exit stays open. No Scene, DAT/image, nonbattle UI, GAS, J: source, Build Settings, commit, push or computer-use action occurred. The task's four declared script paths are the only script edits for this scoped change.
