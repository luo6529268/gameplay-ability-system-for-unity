# Q08 result transition and combat-stop host audit

Scope correction (2026-09-22, supersedes any contrary acceptance wording below): alignment §1.2, §4 P-19 and G-08 explicitly exclude **full native result-page presentation and full native selection UI**. The formal timer101 result-record **logic**, timer350 transition command, combat stop, and selected battle parameters remain required. Unity's own result/selection visuals need not activate at the native visual frame or imitate the native frontend. The phase11 old-page activation is an observed coupling risk because `IsActive` affects input/Results execution, not by itself a visual acceptance failure. An integrated host must isolate native battle truth from this presentation path and route the completed transition into the existing Unity-owned UI without modifying nonbattle selection behavior. Below references to “101 result-page activation” or making old UI the formal 101/144/350 presentation are superseded by this distinction; preserve the approved exception and verify the logical result-record event instead.

The exact old-UI-to-combat coupling, affected historical test oracle and implementation exit contract are in [P19-EXCEPTION-LOGIC-COUPLING.md](P19-EXCEPTION-LOGIC-COUPLING.md). This supplement is read-only; it preserves the P-19/G-08 exceptions while identifying non-exempt battle-state writes.

Status: `SOURCE_AND_UNITY_CALLER_AUDITED / INTEGRATION_PENDING` (2026-09-22). Read-only audit of the current `81cacc3e` worktree plus the separately recorded Q08 held-input changes; no production script was edited under this audit.

Source-witness addendum (same date): `NTSD28-Q08-TRANSITION-FREEZE-SOURCE-WITNESS-001` built an isolated diagnostic against exact-hash-matching formal `game_session.cpp` (`9CCB6E12...65E4189`), `battle_flow.cpp` (`7BED51E3...5676AF`) and `README_SOURCE.md`; root formal EXE SHA remains `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The [fixture](native_transition_freeze_fixture.cpp) SHA-256 is `857DD3D63489BBCED690D5D471F22E6D106AF5B42968CC1F0407118B2D227B30`; isolated executable SHA-256 is `39CA6943E5F87B203827D73B56287E30E46F247A129D448E96ED56BAB76D9C11`. Two complete-session [runs](NATIVE-FREEZE-RUN-1.txt) / [repeat](NATIVE-FREEZE-RUN-2.txt) exited 0 and had identical SHA-256 `E194AC7976FCB01FE49905A9420E578C76CAF3FC3F31F52D33DC5CB59C3DBC83`. Mode4 timer349→350→one following step retained every occupied entity slot's sampled identity, frame action/counter, integer/precise position, motion, HP/MP, plus CRT and synchronized RNG state/call counts; both transition and following step exposed null `last_tick`. This is a source-model witness for the sampled fields and mode4 command202, not a full-world byte comparison, ordinary-state2/battle-only proof, formal EXE pixel proof or Unity acceptance.

Unity first-difference addendum: `NTSD28-Q08-TRANSITION-FREEZE-UNITY-RED-001` added only one Q08 Editor fixture after its Task/Change Record. In the independent Unity clone, [target XML](UNITY-MODE4-FREEZE-RED.xml) compiled and ran 1/1 target test; it reached native output timer350 and transition202, then failed exactly at the combat-world clock: expected previous `NativeWorldClock.FrameSequence=349`, actual `350`. No C# compile error was found in that run. This is a measured same-branch first difference for mode4 349→350; the following tick assertion was not reached because the target test stopped at the first failure. It is not evidence that every field differs or that the generic freeze/host integration is already implemented. No production script changed in the RED probe.

Ordinary/loop branch addendum: `NTSD28-Q08-ORDINARY-REMATCH-SOURCE-WITNESS-001` added a separate [complete-session fixture](native_ordinary_rematch_fixture.cpp) to the isolated native test tree. Formal and isolated `game_session.cpp`, `battle_flow.cpp` and `README_SOURCE.md` hashes still match. Focused `build.ps1 -Target game_session_tests` exited0; the fixture SHA-256 is `3CE1551E13ECF2D08615F1C7F8C4B9C9A4DDBC79EAB4E558A8938EDD50F87E64`, isolated test executable SHA-256 `E33C41A53A0549E1BE6D16236567CCAE52BC8E753AE2AA372EB88AF19DE3C66D`. Two [runs](NATIVE-ORDINARY-REMATCH-RUN-1.txt) / [repeat](NATIVE-ORDINARY-REMATCH-RUN-2.txt) exited0 with identical output SHA-256 `63A82770A51AB12C08C142C327D4E21DA6B2AA7BC37E61AE12D59DDF39AC8921`. For ordinary mode0, timer350 emits transition2, keeps the sampled combat projection unchanged and `last_tick=null`; the next `GameSession28::step` enters selection with transition1 and retains the same battle-world pointer. With the same setup and `battle_scene_only_loop=true`, timer350 has the same combat-free result boundary, but the next call explicitly recreates a battle and resets the previously dead combatant to HP500. These are distinct source host branches; neither is the mode4 command202 behavior, and neither alone proves Unity or formal EXE frontend pixels. The next integrated Unity Task must preserve this mode-specific routing while fixing the measured combat-world tick and the 101 result-page owner.

## Formal playable boundary

The matching `source/ntsd28_playable/src/game_session.cpp` `GameSession28::step()` calls `BattleFlow28::step` before the combat driver. At timer 350, `battle_flow.cpp` sets `result_complete`, emits the mode-specific transition command and stores timer zero. In the same `GameSession28::step` call, lines 3228-3237 check the nonzero transition state, clear configured combatants' pending input, reset the last combat tick and return **before** `tick_driver_.step`. On later calls, the transition/selection owner runs and the battle world stays frozen. The ordinary state-2 path initializes the upper selection flow; `battle_scene_only_loop` uses explicit `start_selected_battle` for a fresh rematch. Mode-specific commands 28/128/202 are separate, not equivalent to a generic rematch. The source timing/transition cases were double-run in `NTSD28-Q08-RESULT-GROUP-CARRIER-AUDIT-001/NATIVE-FULL-DRIVER-6.txt`; formal EXE-visible frontend parity is still pending.

## Current Unity first difference and coupled consumers

`NTSDBattleTickSystem.RunTick` lines 313-369 calls `AdvanceBattleFlowTick`, then `AdvanceNativeBattleResultsBeforeCombat`, but does not branch on the resulting `NativeTransitionState`/phase. It continues through spark advance, human/character input, frame motion, physics, collision, late entity update, world clock, stage, Results and render dispatch. Therefore the Unity world can continue changing on the same tick on which its native carrier outputs 350, whereas the formal host skips the combat driver. This is a static caller-path finding; a same-state full-tick frozen-world RED and replay are still required before implementation.

`BattleResultsOutcomeHostWriter.UpdateSummaryActivation` independently increments `BattleEndPhase` after combat and calls `ActivateSummary` at phase 11, while the formal result record appears at timer 101. `BattleResultsWriter.RunActiveTick` reads the old page's P1/P2 pressed input after combat. Existing mode-4 reserve transaction sits in this old path. `BattleResultsWriter.RunSettingsPhase` sets `PendingHostAction` for rematch/direct bootstrap, but current source search finds its writes, snapshot/checksum and tests, with no production reader. These are coupled host ownership gaps: a bare early return after the native carrier would freeze combat but leave the old result page and host transition unresolved. Likewise moving only page activation from 11 to 101 without specifying reserve and transition ownership would not finish the formal session behavior.

## Next exact validation and implementation order

1. Build a same-config formal playable full-session case for timer 349 -> 350 and one following upper-state tick, including world frame/position/HP/RNG and transition state; double-run it. Distinguish ordinary state2, battle-scene-only rematch, and mode4 command202 rather than projecting one branch onto all modes.
2. In isolated Unity, write a focused full-tick RED asserting no combat entity/world-clock mutation on the transition tick and following tick, while preserving snapshot/checksum and ordered shutdown. Test the 101 result-page activation separately and record the old reserve path's exact precondition. Create a precise Task/Change Record before any test or production script edit.
3. Integrate the host boundary only after defining which owner consumes each transition and how the old Results UI either becomes the formal 101/144/350 presentation or remains a justified exception. Keep mode4 reserve until its authority question is resolved. Verify focused EditMode, full SelfCheck, representative real battle enter/exit/re-enter, same-seed trace and Scene hash; do not rerun every character or the closed Q06 matrix.

No Q08 phase3/host completion is claimed. Q06 scoped exit remains closed, and this audit neither authorizes Scene/resource deletion nor touches nonbattle UI.
