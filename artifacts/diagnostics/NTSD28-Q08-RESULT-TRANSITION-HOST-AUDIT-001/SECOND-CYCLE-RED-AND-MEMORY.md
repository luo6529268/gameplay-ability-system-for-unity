# Q08 second battle-only result: proxy RED and real-Scene resource block

`NTSD28-Q08-SECOND-BATTLE-ONLY-RESULT-UNITY-RED-001` has a valid **temporary-host proxy RED**, not a real Battle Scene RED. The formal source-model witness `SECOND-CYCLE-SOURCE-WITNESS.md` measured first battle-only rematch once, then second timer350 transition2 and following upper-state selection transition1 in the same World. The first Unity rematch host is already scoped Play-accepted in `FIRST-REMATCH-HOST-ACCEPTANCE-PENDING.md`.

The new test file is `Assets/NTSD/Scripts/Test/Editor/NTSD28Q08SecondBattleOnlyResultSelectionRedPlayModeTests.cs`. Two isolated Unity D3D11 attempts to run its real Battle Scene case exited before `BattleTestBootstrap` completed formal content loading; both logs (`UNITY-SECOND-BATTLE-RESULT-RED.log`, `UNITY-SECOND-BATTLE-RESULT-RED-RETRY.log`) end with `Fatal Error! Could not allocate memory: System out of memory!` after `Loading content: broken.png`. Neither produced XML. They are environment/resource-loading failures and supply no second-result runtime verdict. No other Unity processes were touched.

The isolated Unity `-nographics` temporary Battle Scene host case avoids image publication. It creates a driver and explicit direct owner already past its one-rematch latch, seeds result phase3/timer350/transition2, and lets the production `SimulationTickDriver.Update` dispatch for three frames. `UNITY-SECOND-BATTLE-PROXY-RED.xml` reports total1/passed0/failed1 at the target line: expected transition1, observed2. World identity and frozen tick assertions passed before that line. This proves the missing logical 2→1 consumer at that host seam, while remaining weaker than a formal-content Battle Scene/physical KO test. No C# compile or NullGfx failure was observed in the proxy run.

Next production Task may add only the direct owner's second-result logical state transition, preserving the same World and existing ordinary Menu+Battle routing. It must not claim an implemented character-selection UI/roster frontend from a scalar state change. After implementation rerun this narrow proxy plus adjacent ordinary tests; leave real formal-content Play pending until memory pressure permits a valid run. Q08/master goal remain open.
