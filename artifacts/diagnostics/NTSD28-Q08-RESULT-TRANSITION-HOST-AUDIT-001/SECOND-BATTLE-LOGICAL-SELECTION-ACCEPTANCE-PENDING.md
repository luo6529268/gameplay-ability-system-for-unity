# Q08 second battle-only result logical selection — scoped proxy acceptance

`NTSD28-Q08-SECOND-BATTLE-ONLY-LOGICAL-SELECTION-001 / RUNTIME_PENDING`. Formal root EXE SHA-256 remains `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. Source witness `SECOND-CYCLE-SOURCE-WITNESS.md` measures one direct rematch, then the second result timer350/transition2 and the next upper-state selection transition1 in the same World. The prior direct-rematch host has real Battle Scene first-cycle Play acceptance in `FIRST-REMATCH-HOST-ACCEPTANCE-PENDING.md`.

Before this change, the no-image temporary Battle Scene host test compiled and ran through production `SimulationTickDriver.Update`; `UNITY-SECOND-BATTLE-PROXY-RED.xml` (SHA-256 `E5229E23BF7F24D9353FBEDF41D8A90F566AEEF0A0AA7AB2191F3777A5641E38`) reported 1/1 target failure: same World and frozen tick passed, but transition stayed2 instead of1. Two attempts at the full formal-content real Battle Scene test both crashed during image loading near `broken.png` with `Fatal Error! Could not allocate memory`, before bootstrap or second-result logic; neither produced XML. They are environmental failures, not a logical RED/PASS.

`SimulationTickDriver` now offers its registered direct owner a separate second-result dispatch before falling back to ordinary AppManager. `BattleTestBootstrap` accepts that branch only after a successful first direct rematch and when phase3/timer>=350/transition2 are present, writes logical transition1, and keeps the same World. Existing transition-state tick admission keeps combat frozen. It does not synthesize a selection UI, roster frontend or another World. Ordinary Menu+Battle routing remains separate.

After the change, the same no-image temporary-host Play test `UNITY-SECOND-BATTLE-PROXY-AFTER.xml` passed 1/1 (SHA-256 `06A49E832B6304334FDC9AAE5972B76D7DD74532446B7B33619CDF0B8656C6D6`). Adjacent ordinary selection class `UNITY-SECOND-BATTLE-ORDINARY-ADJACENT.xml` passed 3/3 (SHA-256 `3402AAA1AC02F6151180EEBA71A83848C1CA52A94C460C70770F0E18788ED459`). The isolated Editor exited, and both original and isolated `NTSD_Battle.unity` remain SHA-256 `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`. The original and isolated changed production scripts have matching SHA-256. A full SelfCheck was run and passed after the preceding first-rematch host change; it was not repeated for this two-branch scalar addition. No real formal-content second-cycle Play, natural KO, selection UI behavior, or EXE visible parity is established here.

Q08 remains open. Further work must verify the real full-content second-cycle once the resource-loading OOM is resolved and implement any missing upper-selection state/roster semantics beyond this scalar handoff, while preserving the user's selection UI exception and nonbattle modules. No Scene, DAT/image, UI, GAS, J: source, Build Settings, commit, push or computer-use action occurred in this change.

Post-record log triage: the second crashed run's final managed stack reaches `BattleAtlasResourceBuilder.TryBuild` at `Assets/NTSD/Scripts/Animation/Runtime/BattleAtlasResources.cs:246`, through `CharacterAnimtorManager.TryBuildUnifiedCentralAtlasPublication` at line 2490, while constructing `UnityEngine.Texture2D`. Unity reports failure to allocate 26,776 bytes. `broken.png` appeared earlier in the log and is not established as the failing image or cause. The crash remains before the second-result test's bootstrap verdict. This identifies the allocation boundary for the next isolated resource-load investigation; it does not prove whether the root is atlas size, accumulated memory, Unity allocation limits or another factor. The current OS free-memory snapshot was about 24 GiB after the crash, which is not a measurement of the crashed process at failure time.

Read-only follow-up: both `UNITY-SECOND-BATTLE-RESULT-RED.log` and `UNITY-SECOND-BATTLE-RESULT-RED-RETRY.log` independently report `Could not allocate memory` with the same managed call boundary, `BattleAtlasResources.cs:246` (`new Texture2D` in the ordered-page loop), reached from `CharacterAnimtorManager.cs:2490`. The second report's final native message requests only 26,776 bytes. `TryBuild` first assembles 2048×2048 RGBA32 CPU pages, evaluates whether a `Texture2DArray` is allowed, then allocates ordered `Texture2D` pages if the array is disallowed or fails. These logs do not yet disclose the planned page count, whether the array was attempted, process peak commitment, or which resource exhausted the allocator. A previous formal-content first-rematch Scene test completed successfully in the same isolated project, so the two crashes are not proof of an invariably invalid DAT/image. The next evidence gate is per-process allocation/atlas-plan diagnostics during a single isolated load; avoid a third identical full Scene retry without a changed diagnostic setup.

Later correction (2026-09-22): the atlas-budget source-binding change resolved the OOM for this targeted test. The same formal-content Battle Scene `SecondResultEntersOrdinaryUpperSelectionInSameWorld` passed 1/1, exit 0, in `UNITY-SECOND-BATTLE-SOURCE-BUDGET.xml` (SHA-256 `E70E58EAA8D64150B314F843E8320F97D3CAD5CB3F5317A9A552B692D7692B77`). The test manually seeds both result transitions, proving the host branch in a populated full-content Scene without proving natural KO or complete formal EXE/Unity result parity. No fresh original-project Editor pass is claimed.
