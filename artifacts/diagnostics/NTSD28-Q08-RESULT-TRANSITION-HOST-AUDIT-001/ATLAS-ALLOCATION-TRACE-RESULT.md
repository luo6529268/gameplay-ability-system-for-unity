# Q08 formal-content Scene atlas allocation trace

Task/Change: `NTSD28-Q08-ATLAS-ALLOCATION-TRACE-001`. This diagnoses a prerequisite for Q08 second-result Scene validation; it is not battle-rule or visual-parity acceptance. The isolated Unity 2022.3.62f3 Editor ran only `NTSD28Q08SecondBattleOnlyResultSelectionRedPlayModeTests.SecondResultEntersOrdinaryUpperSelectionInSameWorld` with `-ntsdAtlasAllocationTrace`, real D3D11 graphics and formal staged Logan content. The original project Editor and both Battle Scenes were not used for this run.

`UNITY-SECOND-BATTLE-ATLAS-TRACE.log` reports a plan of 145 pages at 2048×2048 RGBA32. The planned page payload is `2,432,696,320` bytes (`145 × 2048 × 2048 × 4`). The capability policy rejected `Texture2DArray` because that exceeds its `536,870,912`-byte array budget; ordered pages were selected, without an array allocation attempt. Before CPU assembly `GC.GetTotalMemory(false)` was `3,816,284,160` bytes; after assembly it was `6,151,811,072` bytes. Ordered page 87 completed and page 88 began, then Unity reported `Could not allocate memory` at `BattleAtlasResources.cs` `new Texture2D`. There was no test XML, bootstrap completion or second-result assertion. PowerShell reported process exit value `1073741845`; this is an infrastructure crash, not a test failure verdict.

The external two-second sample file `UNITY-SECOND-BATTLE-ATLAS-TRACE-MEMORY.csv` has 59 samples. Maximum observed private bytes were `18,015,555,584` (about 16.78 GiB), working set `13,369,778,176` (about 12.45 GiB), and virtual bytes `43,542,347,776` (about 40.55 GiB) at the private-memory maximum. Sampling does not prove the exact peak at failure, total system commit, graphics-driver memory or why this particular small native allocation failed. Earlier two crashes reached the same ordered-page constructor without opt-in diagnostics. `broken.png` was only an earlier loaded asset and has no proven causal role.

Original and isolated `Assets/NTSD/Scene/NTSD_Battle.unity` both remain SHA-256 `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`. The one-file diagnostic script SHA-256 matches across original and isolated copies: `B20FA3F9035F06F11BB4866DAE4FAC4EEE9038ABC4A91F5DA6ECCA896741B046`. The diagnostic is opt-in and does not change layout, allocation, upload or fallback.

Next action requires a separate battle-render resource Task/Change or a measured environment remedy. Review 145-page publication and source/CPU/page texture lifetimes with per-process and system commit evidence; do not increase the array budget blindly or bypass formal images to claim Q08 Scene acceptance. The second-result `RUNTIME_PENDING` status and Q06 local exit remain unchanged.

Diagnostic-script regression: an initial EditMode invocation with `-quit` exited 0 before writing XML, so it is not a test verdict. The subsequent isolated `BattleCommonAtlasBindingEditorTests` invocation omitted `-quit`, produced `ATLAS-ALLOCATION-FOCUSED-ACTUAL.xml` (SHA-256 `46786EB2AF5A12B078C7684E2DF1695814523A7A6068E6F4565C990C30FA4366`) with 2/2 passed, and exited 0. The run did not pass the trace flag and the log contains no `[Q08 AtlasTrace]` lines. This checks the small atlas binding fixture/default switch path, not 145-page full-content completion.
