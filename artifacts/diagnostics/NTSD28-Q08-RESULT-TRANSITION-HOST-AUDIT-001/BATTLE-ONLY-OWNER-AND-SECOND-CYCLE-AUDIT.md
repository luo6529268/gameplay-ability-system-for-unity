# Q08 battle-only result host: owner and second-cycle audit

Update: the second-cycle source-model question below has been answered by `SECOND-CYCLE-SOURCE-WITNESS.md`: a focused exact-hash build and two byte-identical runs confirmed one direct rematch, then selection on the second result. The earlier “untested” statements are historical pre-witness status; Unity and formal EXE-visible corroboration remain pending.

Date: 2026-09-22. Status: `READ_ONLY / IMPLEMENTATION_NOT_READY`. Formal root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; current formal `game_session.cpp` SHA-256 `9CCB6E12A97FCB9FF95DF2FE95BC33F24C0AC32E9F956E41D23E4C14165E4189`; formal `main.cpp` SHA-256 `E0B8789CA38A882DD2616E8FD5D1B598D291D20B4EF53259742C14B0286E6C09`. These are fresh file hashes, not a new EXE runtime run.

The release host sets `BattleConfig28::battle_scene_only_loop` in `main.cpp:849-850` from explicit playback/selection-loading options. `GameSession28::step()` at `game_session.cpp:2750-2764` branches on that bit after result `upper_scene_entered`: the battle-only branch calls `start_selected_battle(config_)`, whereas the ordinary branch initializes the selection flow. The prior complete-session native witness double-ran only the **first** transition and established timer350 command2/no combat, then first battle-only recreation with the former victim at HP500. Scene presence was not its condition.

There is an untested second-cycle question. In the current `start_selected_battle` body, `game_session.cpp:2384-2391` sets `selected_config.battle_scene_only_loop=false` before `initialize`, then explicitly leaves `config_.battle_scene_only_loop=false` on success. Since `initialize` assigns `config_=config` at line1165, static source predicts that the next result boundary would take the ordinary selection branch. The existing fixture stops after the first recreated battle and does **not** prove the second transition. Run an isolated full-session two-rematch witness against exact current source before implementing an assumed perpetual loop. If source behavior conflicts with formal EXE observation, keep it unresolved pending release behavior evidence; do not silently fix the formal source.

Unity ownership is materially different from the ordinary Menu+Battle seam. `MatchConfig.cs` has no explicit battle-scene-only bit. The normal menu `CharacterSelectionController` creates `MatchConfig` and calls `AppManager.LoadBattleAdditive`. The current `BattleSinglePlayerRuntimeValidationBuild` and Q07 Windows probe package only `NTSD_Battle.unity`; serialized `AppManager` GUID `78a91810154a34642985ee3475cb5b0a` is in `NTSD_Menu.unity`, not `NTSD_Battle.unity`. Direct Battle Scene startup instead uses `BattleTestBootstrap.Start()` to create a transient AppManager, prepare direct test characters, and call `SimulationTickDriver.SetPaused(false)`. Its current source has no `AppManager.SetBattlePaused(false)` call, so the transient AppManager remains at its default MenuMain state even while the driver runs. This is a source/scene-call-path observation, not a fresh Player rematch run.

The newly added ordinary command2 receiver requires loaded Menu and Battle scenes plus AppManager BattleRunning/Paused. Therefore it deliberately does not handle the direct Battle Scene bootstrap. `SimulationTickDriver.RecreateWorld()` exists, but its current path performs ordered shutdown and creates an empty production World; it does not by itself re-register the existing roster or preserve native RNG/input phase. `ApplyMatchConfig()` resets runtime and seeds both RNG streams from `MatchConfig.seed`. Calling either method alone would not establish parity with the formal `start_selected_battle` boundary. Do not route by `Menu Scene absent`, call `UnloadBattle()` for this branch, or use `RecreateWorld()` as an unverified substitute.

Next action: (1) exact-hash source second-cycle executable witness and a fresh direct Battle Scene Unity result-boundary trace; (2) define an explicit Unity battle-only launch/config owner separate from ordinary selection, with unchanged roster/stage and RNG/input-phase continuity; (3) only then create a code-path-specific Task/Change for rematch, test old World stop/new World birth, second result behavior, ordered shutdown/re-entry, and Player. Commands28/128/202 and the original Menu-to-Battle route remain separate Q08 dependencies. No production code, Scene, content, build settings, or nonbattle UI was modified for this audit; no computer-use was used.
