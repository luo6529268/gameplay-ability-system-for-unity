# R15 Unity B0 v2 diagnostic producer

Status: `FOCUSED_TEST_PASS_UNITY_EDITOR_DIAGNOSTIC_ONLY`. The package edits only the Unity Editor raw-capture producer and its focused test class. The default v1 format, existing requests, main/B2 output, Unity-only `aiAcceptedTrace`, battle runtime, Scenes and formal Logan files remain outside this change.

The optional `domainVersion` request/test argument accepts exact `v1` or `v2` only with a domain output. An omitted version retains v1. V2 writes raw schema `ntsd28-logan-b0-domain-raw-v2` and derives a six-field `object-id-change` event for a same-slot, same-positive-epoch object-ID mutation. V1 still rejects it; birth/death/reuse and epoch-monotonicity checks remain.

The first two offline Editor MSBuild attempts failed before test compilation because the generated project referenced missing `Temp/bin/Debug` assemblies (`Temp/r15-unity-v2-red.log`, `Temp/r15-unity-v2-green2.log`). They are not a valid TDD RED. A Temp-only targets override pointed at existing original-project `Library/ScriptAssemblies` and compiled exit0 (`Temp/r15-unity-v2-green3.log`). The original Editor PID173216 then refreshed scripts and completed domain reload. A focused EditMode TestRunner run, job `1da70265be714720a9316334ce93c7e7`, passed 7/7: exact v2 event, v1 rejection, four invalid request cases, v2 neutral capture and existing default-v1 input capture. The generated `Temp/NTSD28UnityTrace/FocusedTests/v2-domain.raw.jsonl` passed the independent B0 validator with `valid=true`, producer `unity-diagnostic`, 3 ticks. Validation JSON is `Temp/diagnostics/NTSD28-R15-B0-UNITY-V2-PRODUCER-001-domain-validation.json`. The two-file diff check passed.

This proves diagnostic capture format behavior. The neutral scenario has no kind transform, and the synthetic event test is not an actual Unity kind hit. A formal type3/action initial-state Unity scenario, same-seed main/B2/B0 cross-producer comparison, and formal EXE observable verification remain. `certificateEligible=false`; Q07/R15 and the total goal remain open.
