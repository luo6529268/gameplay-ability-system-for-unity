# Q08 real-Menu physical J to authored punch KO

Status: `VERIFIED_SCOPED`. In the original Unity Editor, real Menu callbacks selected two Naruto participants on opposing teams and loaded the formal-content additive Battle Scene. The diagnostic paused LocalFreeRun and sent a synthetic P1 `<Keyboard>/j` InputSystem event through the existing `CharacterInputModule` and local `FrameInputSet`. It controlled only the defender's Play-time position and HP10; it did not assign the attacker's action/frame, change formal DAT/ITR, inject damage/KO/result state or use a synthetic attacker.

The first unique request, `q08-physical-attack-real-menu-ko-20260925-1.json` (SHA-256 `2E2E99F24B4D523DF44D3D81C481BCDC014A228DEFCD4181F2A75A37C304C21D`), passed the input→attack→KO→result→return sequence. It did not serialize KO source attribution, so the outcome was kept as bounded sequence evidence. The first compile of this diagnostic failed with CS0246 because `NTSD.Game` was missing; after adding the exact namespace the original Editor compiled and ran the request. Both facts remain in the Change Record.

The attribution-strengthened second unique request, `q08-physical-attack-real-menu-ko-20260925-2.json` (SHA-256 `FFB5C8F50FD26CB1C92CB066A8748994A860BCBD61C4967D8D30991111204F2B`), returned `PASS`. P1 physical J appeared in applied frame input at tick89. The actual attacker visited an authored punch frame and was on formal Naruto frame513 at the KO tick96; `c/nar/nar.dat` frame513 carries a kind-0 ITR with injury20. The defender HP was -10. The native KO event identifies victim slot1, source slot0, credit slot0, four-owner slot0 and source type0, so the recorded KO is attributed to the real P1 character. Result timer was 0 on tick96, 1 on the next tick, output milestones 80/101/350 and ordinary transition2 at 350. The existing production host unloaded Battle and returned to active character selection; old Driver destroyed, pool borrowers0. The request was consumed and its restore flag cleared.

After the second Play, the original Editor restored `NTSD_Battle.unity` and reported `isDirty=false`. Disk SHA-256 remained Menu `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`, Battle `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`, and GameConfig `0527D737A1FA38FC56B51D00DC6E96A421D3C67222546368B147C2D074CB8EA7`. `git diff --check` exited 0.

This is a synthetic physical device event against a controlled nearby low-HP opponent, stepped one full Driver tick at a time. It is not human keyboard wall-clock play, an uncontrolled match, a formal-EXE same-input trace or visible/audio parity. It closes only the scoped Q08 P1 authored hit→ordinary result host witness. Q07, Q08 and BATCH-04 remain open.
