# Q06 L-02–L-04 source-first audit (2026-09-21)

Status: `SOURCE_CHAIN_LOCATED / JOINT_RUNTIME_PENDING`. This is a read-only call-chain finding, not a parity pass or a production change.

Authority: formal NTSD 2.8-Logan EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033` and its playable source. `SimulationTickDriver28::step` walks live slots in ascending order and calls `BattleWorld28::step_frame_slot` before `materialize_native_frame_zero_entry`. The latter requires a live entity, no lifecycle pending, and `frame.frame_counter == 0`; it writes `opoint_action_latch` as provenance, not a one-shot suppression. `FrameMachine28::step` increments the counter and resets it when `counter > wait`; `wait:0,next:0` therefore returns to zero on each ordinary frame step. `BattleWorld28::spawn_transient` scans the first free transient slot from 50 upward. These together establish the intended repeated emission and high/unscanned versus low/already-scanned birth timing. Evidence: `simulation_tick_driver.cpp:351-373,931-1075`, `frame_machine.cpp:20-44`, `battle_world.cpp:1501-1510,8543-8600`.

Unity currently has the same broad ascending live-slot pattern in `BattleLateEntityLifecycleModule.Run`: it reads the current occupant for each slot, runs the frame tick, processes the OPoint segment, then flushes queued tasks. `BattleLogicObjectPointRuntime.ProcessOneLateOpoint` requests `FindFirstFreeFrameLogicRuntimeSlot` and creates immediately. `SimulationRegistryModule.RegisterCore` claims the slot immediately; `_ticking` principally defers unregister/destroy. These static facts make high-slot same-tick visibility plausible, but they do not prove it for a full tick. Evidence: `BattleLateEntityLifecycleModule.cs:68-76,158-257,334-336`, `BattleLogicObjectPointRuntime.cs:240-302`, `SimulationWorld.cs:1984-1989,2030-2037`, `SimulationRegistryModule.cs:458-477,758-940,995-1020`.

Potential first difference requiring an actual matched case: both `BattleLogicObjectPointRuntime.cs:166-174` and `LF2ObjectPointFactory.cs:246-253` reject an OPoint when `AttackingCounter != 0`, then separately reject a character with `FrameDelay != 0`. The formal source OPoint tail gates on frame counter and lifecycle only. The frame transaction skips while ordinary motion hold survives, which may leave the counter at zero. Therefore a character with nonzero surviving delay, zero counter and an OPoint frame is a candidate for native emission versus Unity suppression. It is not established by static reading alone because earlier pass effects and the exact Unity frame/hold carrier need a source–Unity full-tick witness. Do not remove the delay gate on this inference.

Next bounded witness: (1) same zero-frame `wait:0,next:0` over two ticks, recording per-tick spawn count and counter; (2) parent in slot 50 with next free slot above it, recording newborn's same-tick frame/tail; (3) parent in a higher slot and recycled free slot 50, recording no newborn tail until next tick; (4) the delay/counter candidate above as a control. Compare source and Unity with the same initial DAT, slot occupancy, tick and input, including immediate/following state, birth slot, action/counter and structural event order. Build source witness with formal identity check; use actual Unity full driver, not a direct materializer call. Only measured first differences authorize a minimal production edit. Reuse prior OPoint materializer/owner/value evidence; package-end compile, focused tests, stable SelfCheck, representative Play/ordered close and ledger validation only after any code changes.

Q06 remains `HOLD / L-02-L-04`; Q07 content migration has not started. No files under production scripts, Scene, resources or nonbattle scope were changed by this audit.
