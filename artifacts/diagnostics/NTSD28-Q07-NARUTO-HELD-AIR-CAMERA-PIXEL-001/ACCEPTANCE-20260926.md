# Q07 Naruto held-air pic97 original-Editor camera pixel witness (2026-09-26)

Status: `VERIFIED_SCOPED_UNITY_CAMERA_PIXEL`. The original Unity 2022.3.62f3 Editor PID11944, saved `NTSD_Battle` Scene and formal staged content root were used. Only the existing opt-in Editor diagnostic script changed; production combat, DAT, PNG, Scene, Prefab, project settings, mode Asset and nonbattle files did not change in this package.

Authority: formal root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable `SpriteFrameResolver28::resolve`/renderer, formal `c/nar/nar.dat` action30/pic97 and `vfs/c/nar/nar.png`. The prior original-Editor full-Driver witness already established the actual held attack action and one current CentralOnly command. Formal/staged `nar.png` SHA-256 is `391E7A17A04A4DB5E103656571AA5EA2CE9DADB91E377F1FA917DF9920B57FB5`.

The original Editor refreshed and rebuilt `Assembly-CSharp-Editor.dll` after each diagnostic edit; each pre-Play Console read returned zero current C# errors. Two uniquely named focused Play results are preserved. `naruto-held-air-camera-20260926-01` passed and wrote a 960×540 PNG, but reported ROI dimensions without origin. The follow-up `-02` added ROI x/y within the declared diagnostic scope and passed with the same input route: OID2 held OID120, action30/state15/pic97 at tick20; production catalog rect `(560,801,79,79)` and valid central binding; one exact stable ID102/OID2/pic97 79×79 Entity command in the current 10-command CentralOnly frame; submission acquired.

For `-02`, the target command projected to bottom-origin ROI `x=66..121`, `y=276..332`, width56×height57 on the 960×540 world-camera readback. The ROI contained 486 nonblack pixels. Independent Pillow analysis used the top-origin equivalent PNG crop `(66,207,122,264)` and the formal/staged source pic97 crop `(560,720,639,799)` from the 799×1600 PNG. Of the ROI's 486 nonblack pixels, 474 had exact RGB values present in the source cell; all 19 distinct nonblack source-cell RGB values appeared in the ROI. This is a color-set/ROI attribution check, not a full per-pixel image-equality claim. Visual inspection of the saved PNG places the airborne Naruto in that left ROI; other scene characters and health/shadow overlays remain outside or adjacent to the target.

The camera capture used a temporary black clear/culling mask to isolate central drawing and saved/restored target texture, culling mask, clear flags, background color, HDR/MSAA and active RenderTexture in `finally`; the result reports `cameraRestored=true`. After `-02`, World objects4→4, claimed slots2→2, active object pool2→2, temporary fixture unregistered and roster restored; request false. The same original Editor returned idle/non-Play after a transient null MCP state on Play exit. Battle/Menu saved Scene SHA-256 remained `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A` / `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`. Two MCP disposed-client Console errors appeared during exit; these are tooling errors, not C# compile errors.

Final result JSON SHA-256: `5205FC1CE04D193ADF8AB8F3D2A8A8F841789A4128989322DD939FE9C553B3B4`. Final camera PNG SHA-256: `A8F9FF8D9B0AFF3AC1BDDAFB6B9461C3799CE1FD5BCADAC3505195B64ED5DB82`. Files are in the preceding action package with the `naruto-held-air-camera-20260926-02` stem. The first `-01` diagnostic is retained and not silently superseded.

This establishes target-frame **Unity camera pixels** under the existing fixed-camera/background exception. It does not establish formal root EXE same-world pixel parity, a natural collision pickup, physical-keyboard mapping, or full Q07/R17 closure. The next alignment dependency is a formally comparable release capture or a documented authority-surface limit; avoid interpreting the diagnostic black background or project-specific HUD/shadow as a formal background comparison.
