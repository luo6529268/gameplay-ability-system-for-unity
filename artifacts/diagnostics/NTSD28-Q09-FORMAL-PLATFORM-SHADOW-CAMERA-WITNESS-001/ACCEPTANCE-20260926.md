# NTSD28-Q09-FORMAL-PLATFORM-SHADOW-CAMERA-WITNESS-001

Outcome: `PARTIAL_PIXEL_OWNERSHIP_UNPROVEN`. The original Editor compiled the optional probe without observed C# errors and completed two uniquely named opt-in Battle Scene Play requests. The first raw result `platform-shadow-camera-01.json` remains unchanged; it used top-level `PASS` for the already-proved logic/command while its camera field said `PIXEL_OWNERSHIP_UNPROVEN`. The corrected `platform-shadow-camera-02.json` uses top-level `PARTIAL_PIXEL_OWNERSHIP_UNPROVEN` so the pixel result cannot be mistaken for a pass. Raw second JSON SHA-256: `58869EDC5D547DD18FFE6AA1D7F99F4319F54568509ADFBFF223E63102D7BED9`.

Final-file confirmation: a post-review correction now accumulates the camera-restoration check across both captures. The original project's Editor assembly was newer than that final source file, and a third unique request `platform-shadow-camera-03.json` reproduced `PARTIAL_PIXEL_OWNERSHIP_UNPROVEN`, `cameraStateRestored=true`, tick6 carrier/command values, zero exclusive pixels and exact cleanup baselines. The final JSON SHA-256 is `5F35E9ED01A4ADF423BF9E3723D9EAFA577FD2E8A760F54A35D059E9ADAA9DC3`; its baseline/post PNG SHA-256 values are `93623E3C7B3135AD92426398A2AF5280719621F43DD4077BAE147D8071E86620` / `E9D8028EB78D33D63A71EA232FB7D94CA3A987524792404C25BCA83841D107E5`. The original Editor instance was explicitly selected for final state/Console inspection: Battle Scene, non-Play, zero Console errors. UnityMCP's default connection briefly reported another already-open Editor during final refresh; no Play request or Scene/content write was sent to that Editor. The final Play request was consumed by the original project's own compiled probe.

The second request reproduced the previous controlled formal case: OID56/frame130 current and snapshot with different-group OID2; production Driver tick6 once; target platform slot50, collision reference-5 and `RenderShadowOffset10C=-5`; one CentralOnly target Shadow command with ZInt287 and Y≈-4.87000084, matching the Z+10C projection. This is a forced initial frame/position diagnostic, not natural input or root EXE graphics.

Camera capture used the active original `ScenesCamera` and a temporary 1920×1080 white-clear RenderTexture. The pre-fixture baseline PNG is white; the post-tick PNG visibly includes the Hidan/target group and their shadows. Target Shadow command projection is `(169,528,53,11)`. After excluding the inflated projected bounds of every other command, `exclusiveWhiteBaselinePixels=0` and `exclusiveChangedPixels=0`. The target shadow is covered by other command bounds in this case, so no pixel can be uniquely assigned to it under this conservative method. The post-tick image and the command carrier are separate positive observations; they do not prove the target shadow's GPU pixels or formal EXE parity. The exact raw captures are `platform-shadow-camera-02-baseline.png` SHA `17A82C6DC967E635E27856F0DC3DE4CAE458E7BA91F831AF874FABBC2F8CE15E` and `platform-shadow-camera-02-post-tick.png` SHA `545E71D6A288604AE370366A57799125E30F07A3F1A9234E4D4B8FDF91690A85`.

The probe reports `cameraStateRestored=true`; World objects4→4, claimed slots2→2, Renderer borrowers2→2, and both fixtures unregistered. Original Editor returned idle/non-Play. Battle Scene SHA stayed `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`; Menu Scene SHA stayed `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`. After the second run the Console error query returned zero entries. No DAT, image, production script, Scene or nonbattle owner changed in this Task.

Final governance: `Tools/Validate-ChangeLedger.ps1 -RepositoryRoot <current project>` exited 0; `git diff --check` exited 0 with only existing line-ending notices; the three live progress documents each contain zero NUL bytes. The validator log is `Temp/NTSD28-Q09-FORMAL-PLATFORM-SHADOW-CAMERA-WITNESS-001-validator.log`. No broad suite or SelfCheck was run for this Editor-only diagnostic.

Next Q09 pixel step needs a separately governed, valid discriminating capture that isolates the target Shadow draw while preserving its full-Driver carrier, or an equivalent source-pixel ownership proof. It must not mutate 10C after the tick and rebuild a cached plan as though it were the same frame. Natural input, Legacy Play and root formal EXE same-condition graphics remain independent exits. Parent Q09 and the total battle-alignment goal remain open.
