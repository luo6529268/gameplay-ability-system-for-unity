# NTSD28-Q08-Q09-KILLTEXT-RUNTIME-LIFETIME-001

Latest status (2026-09-22): `CODE_WRITTEN / OFFLINE_COMPILE_PASS / ORIGINAL_EDITOR_RUNTIME_PENDING`. The PLANNED sentence below is the preserved pre-edit contract; see Change Record and `ACCEPTANCE-PENDING.md` for current evidence.

Status `PLANNED` before script edits, 2026-09-22. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001`; BATCH-04/Q08 consumes BATCH-05/Q09 input. The user-authorized battle-only boundary, formal EXE SHA `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, and `NTSD28-Q08-Q09-KILLTEXT-LIFETIME-HANDOFF-AUDIT-001/REPORT.md` are authority.

The formal Session prunes newest-tail knockout records after core only when `knockout_feed` config exists, using its `lifetime_ticks`; `bound` and battle mode do not gate pruning. Missing config does not prune, and negative lifetime is a no-op. Current Unity always prunes with literal 70. Q09's `LoganModeComboInput.KnockoutFeed` now projects the full formal record from the same identity-protected mode child bytes, but is not published to the match World.

Implementation: add match-owned scalar `NativeKnockoutFeed` runtime state with `RecordPresent` and `LifetimeTicks` (default absent/0), reset and snapshot restore. During the existing published Logan catalog preparation, after `ApplyMatchConfig`/before seal and first tick, set this state from the captured optional feed. Keep present `bound:0` present. The Q08 post-results/pre-render expiry reads this state and skips only absent config. Add scalar snapshot, restore, lockstep checksum and extended/lockstep parity fields with explicit schema advances; frozen Authority400 v3 parity stays unchanged. Existing mode double-DAT raw identity covers the input bytes. Full visual layout/icon/sound remains Q09/Q10 separately.

Exact code-path ownership before edit: `Assets/NTSD/Scripts/Simulation/Runtime/BattleRuntimeState.cs`, `Assets/NTSD/Scripts/Simulation/Host/SimulationTickDriver.cs`, `Assets/NTSD/Scripts/Simulation/Core/NTSDBattleTickSystem.cs`, `Assets/NTSD/Scripts/Simulation/Lockstep/Snapshot/BattleWorldCoreScalarSnapshot.cs`, `Assets/NTSD/Scripts/Simulation/Lockstep/Snapshot/BattleStateSnapshot.cs`, `Assets/NTSD/Scripts/Simulation/Lockstep/Snapshot/BattleStateSnapshotRestore.cs`, `Assets/NTSD/Scripts/Simulation/Lockstep/Checksum/BattleLockstepChecksumModule.cs`, `Assets/NTSD/Scripts/Simulation/Lockstep/Checksum/BattleParitySnapshot.cs`, `Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs`, `Assets/NTSD/Scripts/Test/Editor/BattleWorldCoreScalarSnapshotEditorTests.cs`, `Assets/NTSD/Scripts/Test/Editor/BattleStateSnapshotRestoreEditorTests.cs`, and `Assets/NTSD/Scripts/Test/Editor/NTSD28Q08NativeKnockoutEventStateEditorTests.cs`. Amend the Change Record before touching another script.

Risks: any config loss on reset/restore changes record retention and replay; blindly retaining 70 or conditioning on bound/mode changes formal behavior; changing frozen v3 parity would break historical authority evidence. Validation: targeted reset/prune/negative/present-bound0/snapshot/checksum tests, original-project compile, stable SelfCheck, same-seed formal trace and Battle Scene Play/exit-reentry when Editor permits. Old Q09 WORDS request has no terminal result, so no competing TestRunner or second Editor. `git diff --check`, Change Ledger validator and Temp-only original-project offline compile are intermediate gates, not runtime acceptance. Rollback only declared diffs after review; preserve other dirty work and user's Battle Scene change. No Scene/Prefab/resource/nonbattle path.

Pre-edit test-path amendment: add `Assets/NTSD/Scripts/Test/Editor/BattleStateSnapshotRestoreEditorTests.cs` to the Record before editing; it owns the existing aggregate restore fixture needed to verify the new scalar is restored with checksum.
