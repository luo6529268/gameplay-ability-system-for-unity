# Conditional knockout-feed lifetime: current evidence

2026-09-22 status `CODE_WRITTEN / OFFLINE_COMPILE_PASS / ORIGINAL_EDITOR_RUNTIME_PENDING`.

The original project's battle preparation now publishes `RecordPresent/LifetimeTicks` from the captured and identity-matched formal mode child before first tick. Q08 post-results/post-core expiry uses that optional lifetime; absent config no longer expires, while bound0 with a present config still expires. Negative lifetime continues to be a no-op in the existing newest-tail writer. World reset and aggregate snapshot/restore/checksum cover these scalars; Unity extended and lockstep parity schemas advance to v3, frozen Authority400 v3 remains unchanged. No Scene, resource, nonbattle, renderer or audio file was changed.

Checks run: original-project Temp-only absolute-targets offline runtime+Editor `dotnet msbuild` exit 0; twelve-path `git diff --check` exit 0; focused fixtures written for scalar capture, aggregate restore/checksum and reset/checksum; targeted source search found no remaining old extended/lockstep v2 schema literal in Assets/NTSD/Scripts or Tools/NTSD28Parity. These fixtures have **not** run in Unity.

Pending: original Editor compile and focused NUnit, stable SelfCheck, actual published-catalog tick behavior for missing/present/bound0/negative configurations, same-seed formal full-tick trace, Battle Scene Play and exit/re-entry. Q09 icon byte identity, row rendering and EXE pixel comparison, Q10 sounds, and R15 schema/trace fixtures remain open. The prior Q09 WORDS TestRunner request has no terminal result; no competing request or second Unity Editor was started.
