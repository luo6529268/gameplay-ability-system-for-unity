# Lee J→L non-sound child: scoped formal source/release witness

Change: `NTSD28-Q07-LEE-NATURAL-NONSOUND-CHILD-001`. This is a controlled, ordinary-input `GameSession28` run and a root formal-EXE LFR replay, not a physical-keyboard or Unity Play result. No frame, OPoint or DAT value was injected or changed.

The root `NTSD2.8-Logan.exe` was rehashed to `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The diagnostic compiled successfully from the paired playable/core source and ran with the formal decoded DAT and complete VFS runtime root. Its bounded scan found the first successful schedule with Lee OID7 at `(500,0,650)`, Naruto OID2 at `(1200,0,650)`, HP/MP 500, opposing teams, seed `682973786`, mode 0, background 23, BGM selection 2: attack/J on tick 2, defend/L on ticks 3–4, then release. The full 45-tick source record is `run2/lee-jl-schedule-scan.csv` (SHA-256 `D42120AD1BD00C73321635CF71A7D39D67BE7325C9787A8AE05C2343B300F7B0`); successful LFR is `run2/lee-natural-child.lfr` (SHA-256 `9753F93C3C8267285A33DF5A140AD46CD556D90FA920E33A3861C2DF48D95903`).

Source tick 4 reaches action 145, MP 300; tick 5 reaches 146; tick 6 reaches 164 and creates five owned OID204 children. The root formal EXE, run with explicit `--resource-root` and `--complete-vfs-root` pointing to the formal runtime root, exited 0. `run4/formal-release-report.json` records `passed=true`, `failureCode=0`, `declaredTicks=45`, `completedTicks=46` (including manager final step), and `nativeParityClaim=false` by the playback tool's own definition. Its report SHA-256 is `E4A287B16A2E91B7848C9A8D8D1C71709F773B3F55A3EAD6949408BCE232FFED`; trace SHA-256 is `33FE38881ADB397F0D4667EE76D8FBA1C449FB231B285601BD5160915D1B5A8F`.

An independent per-tick comparison of the 45 source rows with the release trace's ticks 1–45 found zero differences across four fields per tick: input update phase, Lee action, Lee MP, and number of owned OID204 children (180 comparisons). At tick 6 the formal release trace contains five children in slots 51–55, all OID204/action20/owner0/team1 at `(437,-35,651)`; the source records child count five. This selected-field check is narrower than complete state or render parity. Source scan binary SHA-256 is `8A933BF70A999155771CB5CF29389BB6A3EDF2815C6F0FF0F047390165B8BA44`.

Negative setup attempts remain intact: the first source run used `runtime/vfs` where a complete VFS root was required; the first EXE replay lacked explicit complete-root/BGM selection and produced `run2/formal-release-report.json` with failureCode45; a later unquoted path launch with spaces exited 10. The successful `run4` has explicit roots and quoted paths. An earlier compile wrapper returned 1 solely while reading a nonexistent empty compiler log; the g++ compile itself returned 0 and yielded the runnable binary. No failed artifact was overwritten.

Scope exit: formal source/release reachability and selected-field parity **PASS**. Original Unity Editor same-input full-Driver run, physical keys, source coordinate child placement in Unity, pixels, lifetime/cleanup in Unity, and broad D-024/Q07/BATCH-04 closure remain **PENDING**. The previous natural Lee AI 600-tick run still only reached sound-only OID814; this controlled player-input route does not change that observation.
