# Q07 unindexed legacy `effect/weapon4.dat` reachability audit

Decision: `STATIC_NO_KNOWN_READER / KEEP_PENDING_EXACT_RETIREMENT_APPROVAL`. This audit narrows the 521-row retirement worksheet's only old DAT absent from `data.txt`. It does not prove global runtime unreachability and does not authorize deletion.

The existing file is `Assets/NTSD/Config/effect/weapon4.dat`, 16,948 bytes, SHA-256 `780EBCB15E699878EF12396D657AD2FB6B6221754C9362D0B6C5C14A57A50D7D`, meta GUID `6966cf050fcbc1c46abae7609d3c3d2d`. It differs from the indexed `Assets/NTSD/Config/chars/weapon4.dat`, SHA-256 `D17E952C7EF13437611636A38B85EF4FB74C039ADD9101E20A94D224EB55CECE`, which `data.txt:48` maps to OID120. A name-only `weapon4.dat` match therefore cannot assign the effect file to OID120.

Observed current readers: the formal default battle branch in `BattleTestBootstrap.LoadCharacterDataAsync` calls `PrewarmConfiguredLoganContentAsync` and returns; the legacy branch calls `ParseCharacterFrameConfigs`. That parser iterates `GameDataManager.GetAllObjects()` from old `data.txt`, resolves each object's explicit `file`, and decrypts it. No directory enumeration of `Assets/NTSD/Config/effect` is present in this parser. The manager's manual `RefreshAllData` reaches the same old-index parser. `LoganObjectCatalog` does enumerate `.dat` files, but only inside each folder named by the *formal* catalog and source root, not the old effect directory. `NTSDResourceLoader`'s nearby file enumeration filters audio files. The Editor `FormalContentClosureResourcePatcher` constructs individual `effect/<name>` patch paths, but its patch list contains no `weapon4.dat` entry.

Targeted literal search across current NTSD scripts, text config, Scene, prefabs and assets found no reference to `effect/weapon4.dat`; a scan for its meta GUID found no serialized NTSD owner. This is a static search bound, not proof against arbitrary external path input, reflection, historical tools or future authoring. The file and meta remain untouched, and its retirement worksheet row stays `REVIEW_UNINDEXED_DAT`, `deleteAuthorized=false`. Before any deletion request, recheck the complete current owner graph and obtain the repository-required explicit approval for this exact path and meta.

No Unity run, script edit, Scene edit, resource edit, deletion, computer-use, commit or push was performed for this audit. Q07 remains `IN_PROGRESS`; Q06 stays `DELIVERED_SCOPED / Q06_LOCAL_EXIT`.
