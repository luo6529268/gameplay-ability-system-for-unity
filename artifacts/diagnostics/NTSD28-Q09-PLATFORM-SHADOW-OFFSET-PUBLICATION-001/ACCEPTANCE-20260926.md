# Q09 platform shadow offset publication: focused acceptance

Status: `FOCUSED_TEST_PASS / RUNTIME_PENDING`. The paired playable `render_snapshot.cpp` places each shadow at `position.z + render_shadow_offset_10c` while retaining Z for depth order; this report covers the corresponding Unity display consumer only. The native nameplate's separate offset use and all other Q09 presentation items remain outside this package.

The original Unity Editor first compiled the new central command test before production changes and reported exactly the expected missing-snapshot-field errors (CS1061 twice and CS1739 once). After the `BattlePresentationEntitySnapshot` capture/copy and Legacy/central shadow display changes, the Editor recompiled with zero current Console errors. The first central test run failed on an incorrect diagnostic assumption that the frame would contain only one command; the existing overlay path emitted seven commands. The test was corrected to compare a zero-offset baseline against nonzero-offset output command by command. Its next run passed 1/1. The final focused run added the actual Legacy `LF2Character.UpdateShadow` test and passed 2/2: `f920d5a5ffd04565b54c23b561535668`.

The central test verifies a 23-pixel offset moves exactly one Shadow command to the transform of (X120,Z203) from baseline Z180. It verifies unchanged command Z/depth order and all non-shadow command positions, and that `WithResolvedSprite` and `WithPresentationBaseOrder` preserve the offset. The Legacy test uses a real `LF2Character`, frame state0 and `SpriteRenderer`, verifies the renderer's snapped center at (120,203), resets offset0 and verifies (120,180), with logic Z still180. The production diff leaves the central `stableGroundPosition` used by the project's foot-marker and health anchors untouched; these anchors were not rendered in a Play test here.

An adjacent seven-test `BattlePresentationCommandWriterEditorTests` class run completed six tests and failed `CentralOnly_GenericComUsesRelationSheetComposite_LegacyKeepsThreeGlyphs` at `Assert.That(CatalogWithComLabelsConstructor, Is.Not.Null)` before building commands. Current source's reflection search requests a six-parameter com-label constructor, while `BattleCommonVisualCatalog` provides the com-label overload with seven parameters including an optional `bool`; neither signature was changed by this package. This is a separate existing test-fixture mismatch, not a shadow-command assertion. No fix is bundled into this shadow package.

After the focused run, the original Editor was idle and non-Play on `NTSD_Battle`; Menu Scene SHA-256 remained `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`, Battle Scene SHA-256 remained `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`, and neither Scene had a Git diff. No DAT, PNG, shadow prefab, Scene, gameplay state, mode, input, or nonbattle file was modified by this package. No battle Play camera pixel, formal EXE same-scene GPU, aggregate SelfCheck or whole suite was run. Q09 and the parent goal remain open.

`Tools/Validate-ChangeLedger.ps1` exited 0 with 842 Records and one then-current governed code file in diff; `git diff --check` exited 0; the three live progress documents were NUL-free. During this package the shared checkout advanced by one commit that included previously dirty changes; this report does not assign that commit to this diagnostic or use it as validation. Two untracked B11 JSONL artifacts in the final status were not touched.
