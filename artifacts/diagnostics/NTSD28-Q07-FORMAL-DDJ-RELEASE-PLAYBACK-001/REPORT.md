# Q07 formal Naruto DDJ controlled release playback

Status: `VERIFIED_SCOPED / NOT_INDEPENDENT_NATIVE_CAPTURE / Q07_OPEN` (2026-09-25). Task/Change: `NTSD28-Q07-FORMAL-DDJ-RELEASE-PLAYBACK-001`.

The root formal `NTSD2.8-Logan.exe` SHA-256 was `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033` both before and after. The diagnostic harness was compiled from the current playable/core C++ closure into this directory, without editing formal source or Unity production code. It initialized OID2 Naruto at action110, HP/MP500, seed `0x28A55A5A`, an inert distant OID7 opponent, and injected per-tick defend/down/jump at ticks2/4/6, releasing between presses. The first invocation with `resources/runtime/vfs` as the complete-VFS root failed because that root has no `decoded_dat`; the second with `decoded_dat` as extracted root failed because that root has no `catalog.csv`. Using `resources/runtime` for both constructor roots succeeded and produced 26 source ticks and a 10,006-byte LFR. These two path failures did not edit resources or create an LFR.

The source trace entered action495 with `input_last_action_144=272` at tick6, MP500→150, then emitted OID518 at tick16. The unchanged formal release EXE accepted the generated LFR via `--headless-playback-lfr`, completed with `passed:true`, `failureCode:0`, and emitted report/trace. An initial playback without action override also passed, but LFR does not store the initial action: its tick0 action was 0 while the source harness began at 110. Therefore the second, separate playback used the release-supported `--lfr-slot0-action 110`; its tick0 action is 110 and it passed. Both playback results are retained.

`comparison.json` compares exact numeric values, rather than only milestones: source ticks1–26 versus the action110 release trace have **0 first differences in 26 rows** for action, `inputLastAction144`, HP, MP and OID518 count. Those source rows versus original Unity Battle Scene physical L/S/K result `formal-ddj-play-3.json` have **0 first differences in 22 overlapping rows** for action/frame, last action, MP/PP and OID518 count. Both paths resolve action272 and full-tick frame495 at tick6, retain source cost350, and first observe OID518 at tick16. The release report emits 28 trace rows including initial tick0 and one additional completion tick; the comparison covers the 26 recorded input ticks and does not assert whole-trace equality.

Evidence SHA-256: harness source `D11577882C4690804945053F44804D0D6C510F603189F314BD4F26F7BE285991`; binary `682FA2F6CAB53353697AEAB01C1E49CEE583DC9F8FDEF6F0FEA8830B6A9010BA`; LFR `8CF4FC780BA6C570BE77ACD6F678DFD85D747554A6F3DA76ACECA12DB02D5349`; source ticks CSV `3480EB33FFB326FA71EB12CE850E84C728C5AF774595665B64989DAC4E1085BD`; action110 release report `07FAD6A89EA3E970DF8493AA8BF4E7D12B6F44C0DC726B1BCB025364B6C7AC37`; action110 release trace `769D3A734C72EE5FD165054383D6A237691F5401D286590CDF53472AC267F189`; comparison JSON `7A943CA2B195D19B0D31359970805E258B64F1832EC980B04B54788C0BB719C9`.

The C++ compile input remains preserved here; an exact same-SHA governed source copy is at `Tools/NTSD28Q07Diagnostics/ddj_lfr_probe.cpp` so the repository's script ledger can cover this diagnostic. The first ledger metadata attempt with an artifact `code-path` failed as non-governed; `NONE` was also rejected because this is not a governance-only Change. After declaring the true governed source path and checking both source copies are byte-identical, `Tools/Validate-ChangeLedger.ps1` exited 0 (`Change ledger validation PASSED`, 801 Records, 7 current governed code files covered). This correction did not change the compiled diagnostic bytes, LFR or playback results.

This is a source-generated LFR replayed by the formal release, so the release report itself declares `nativeParityClaim:false`: it verifies independent playback and manager checks, not independent native recording or native same-phase equivalence. Unity's other participants, positions, seed, stage/mode inputs and full world/RNG state were not established as identical to this controlled source scene. No visual, sound, other skill or Q07 aggregate parity follows. Old DAT/image deletion authorization remains zero.
