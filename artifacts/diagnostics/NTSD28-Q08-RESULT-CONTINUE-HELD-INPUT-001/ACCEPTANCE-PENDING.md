# Q08 result-continue held input — focused acceptance

Scope correction (2026-09-22): P-19/G-08 exclude the full native result/selection visuals. The old UI's phase11 appearance is not alone a mandatory visual change; its effects on input and battle state still require isolation. Timer101 logical result record and timer350 combat stop/transition remain pending. This supersedes any “old UI101” visual acceptance wording below.

Status: `FOCUSED_TEST_PASS / ISOLATED_SELFCHECK_PASS / REAL_BATTLE_PENDING / Q08_PARENT_OPEN` (2026-09-22).

Formal playable `GameSession28::step()` scans configured effective-combatant slots for held Attack/Jump immediately before `BattleFlow28::step`; the latter increments first and at timer >=144 sets 350, result-complete and mode transition in the same step. The prior complete-session source fixture double-ran the 144 Attack shortcut (`NTSD28-Q08-RESULT-GROUP-CARRIER-AUDIT-001/NATIVE-FULL-DRIVER-6.txt`). This change maps the **current tick** Unity frame's roster `PlayerSlot` to active configured participant entries, without equating it to runtime physical slot, and reads held `Buttons`. A stale frame, inactive roster slot or pressed-only edge cannot request the shortcut. The existing native timer/phase/transition fields handle the result; no snapshot/checksum schema changes were needed. Old two-side result UI and mode-4 reserve were not edited.

In the independent Unity 2022.3.62f3 clone, the two new cases compiled and produced expected RED [XML](UNITY-RED.xml): 0/2 PASS, both expected 350 but observed 144. After the source-matched route, the full [Q08 focused class](UNITY-FOCUSED.xml) passed 10/10, including the two new cases; the adjacent [result scene input seam](UNITY-ADJACENT-SCENE.xml) passed 3/3. Both test runs compiled without C# error. The clone's full [BattleRuntimeSelfCheck log](UNITY-SELFCHECK.log) contains `[BattleRuntimeSelfCheck] 战斗运行时自检通过。` and `[BattleRuntimeSelfCheckEditor] 自检完成。`; no claim is based on a transient Temp result file. Original Battle Scene SHA-256 remained `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0` before/after this change.

Limits: this proves the independent carrier's held-input shortcut and adjacent input seam in the isolated Unity test environment. It does not prove natural combat result-to-continue in the original Editor/Player, formal EXE-visible result UI parity, or Q08 parent completion. In particular the old UI still has its separate early phase11 activation, and the Unity battle tick still needs explicit phase3/transition combat-skip integration; formal GameSession skips combat after the transition. Q07 formal content and mode4 reserve authority remain separate. No computer-use, Scene, DAT, image, deletion or nonbattle edit occurred.
