# Q07 serialized production root current-state correction (2026-09-22)

Status: `VERIFIED_SERIALIZED_ROOT_AND_INDEXED_DAT_IDENTITY_ONLY`. Read-only; no asset, Scene, code or resource edit.

The current `Assets/NTSD/Config/GameConfig/GameConfig.asset` sets `BattleContentRuntimeRoot: Assets/NTSD/Content/LoganRuntime` (line 15). Its GUID `513170c32731baa449d4d0e9abf8c827` is referenced by both `NTSD_Battle.unity` (lines 3073 and 4851) and `NTSD_Menu.unity` (line 11173). `AppManager.InitializeGameConfig` assigns this serialized asset to `GameConfig.Instance`. The empty initializer in `GameConfig.cs` applies to a newly created unsaved object; it does not describe the current serialized production asset. `git show HEAD` has the same root value, so this was not introduced by this audit.

The staged and formal `catalog.csv` files have identical SHA-256 `0AAB4A0FFEE70F17D31DDF952254C798DFEF185FEDD55CACEC28C7A66181E6FE`. All 330 catalog `object` DAT paths exist in the staged `decoded_dat` and each matches the corresponding formal DAT by SHA-256 (0 missing, 0 different). The staged tree contains 331 DAT total; its only non-indexed `.dat` is `data/fusion.dat`. Fresh file counts: formal `resources/runtime` has 405 DAT, 1,255 PNG and 981 WAV; staged LoganRuntime has 331 DAT, 1,010 PNG and 0 WAV. These are raw counts, not a claim that all omitted formal files are needed for the current battle roster.

The original Editor PID 33236 previously produced a focused 1/1 PASS for `FormalSourceBindingProducesProductionCameraPixels`, with formal content fingerprint, `SourceTexture2D`, four central commands at tick 33 and a rendered 960x540 PNG; see `artifacts/diagnostics/NTSD28-Q08-FORMAL-SOURCE-PIXEL-WITNESS-001/original-editor-focused-20260922-1247.xml/.json/.png`. Its assembly predates later v2 test edits. This proves that formal content can be consumed in that focused original-Editor scene path, not that Menu-to-Battle Player deployment, all skills, images, references or audible presentation are complete.

Consequently, older Q07 notes saying the **serialized production GameConfig root** is still empty are stale and must not drive a duplicate root-setting change. `GameConfig.cs` still deliberately initializes new objects with an empty fallback. Q07 remains open for Scene/Player closure, dynamic references, full content behavior and approved old-resource retirement. Q10 must not assume the staged LoganRuntime already includes audio: it contains no WAV files, and D-023 did not authorize a blanket WAV migration.

## Formal DAT files not yet staged

`formal-dat-not-staged.csv` (SHA-256 `D095B1874B73BACABD96B2606BF57E6BE796A6DDA285E83F438376D177298643`) records every formal `decoded_dat` file absent from the current staged root, with relative path, byte size, formal SHA-256 and catalog-background membership. There are **74**, split into 24 catalog background DAT (`b/*`), 25 `data/bg/*` background data DAT, 18 other `data/*` DAT (including `mode.dat`, `resource.dat`, `sound.dat`, `system.dat`, `kind.dat` and the deliberately deferred default `stage.dat`), and 7 `s/*` story/stage DAT. The 330 catalog object DAT and staged `data/fusion.dat` account for the 331 present DAT. `LoganObjectCatalog` currently filters `catalog.csv` to `registry_section == "object"`, so the 24 background catalog rows are not consumed by that object loader merely because their paths exist in the CSV.

These 74 are a **remaining content/reachability inventory**, not a blanket copy list. The default `data/stage.dat` deployment remains explicitly paused; other files require a caller and resource-dependency check before staged import, particularly because background images and audio are governed separately. Q07 cannot claim all formal DAT are staged or that background/system behavior is aligned from the 330 object matches alone.

## Pre-copy consumer and scope gate

The proposed bulk copy of the other 73 DAT was stopped before any file operation. The list includes `data/menu.dat` and `data/minibar*` (nonbattle presentation), `data/sound.dat`/`data/bgm.dat` (Q10 audio), 49 background files with additional image dependencies, and seven story/stage files adjacent to the deferred default `stage.dat`. These categories need distinct owners; treating all 73 as one migration would cross the user's protected nonbattle and approved exception boundaries.

In current Unity production C# (excluding tests and Editor code), a scoped literal-basename search found `BattleStageCampaignLoader` reading `stage.dat` from `Application.streamingAssetsPath/NTSD/data`, **not** from the staged LoganRuntime. It found no explicit `.dat` basename reader for the other 17 `data/other` names. This negative search does not exclude dynamically constructed paths or compiled equivalents. `LoganObjectCatalog`'s object-only filter also does not by itself consume the 24 background catalog entries. Formal playable source has separate readers/uses for `mode.dat`, `bg_mode.dat`, `resource.dat`, `sound.dat`, `stage.dat` and other tables; their Unity rule/presentation counterparts must be traced before moving content. No missing DAT was copied, no old resource was deleted, and no task was labeled complete from this static gate.

## Subsequent scoped correction (2026-09-22)

The 74-file CSV and 331-DAT/1,010-PNG count above describe the pre-copy snapshot. A later, exact Q07 battle-presentation content package `NTSD28-Q07-GLOBAL-SPARK-CONTENT-ENTRY-001` staged only formal `data/resource.dat`, `data/system.dat` and global `vfs/sprite/UI/SPARK.png` with matching SHA-256. Current staged counts are 333 DAT and 1,011 PNG; 72 formal DAT remain unstaged, including the deferred `stage.dat`. This did not activate a loader or copy any other file from the 73-file mixed-scope set. See that package's `TASK-CONTRACT.md` for hashes and scope.

## Subsequent mode/combo content entry (2026-09-22)

`NTSD28-Q07-MODE-COMBO-CONTENT-ENTRY-001` then staged exactly formal `data/mode.dat`, its selected `data/mode/ntsd.dat` child, and the child `<combo>` image `vfs/sprite/combo_hits.png`, with each hash matching. Current staged counts are **335 DAT / 1,012 PNG / 0 WAV**, leaving **70 formal DAT** unstaged, including default `stage.dat`. The selected child also contains KO/selection/menu records. Staging its bytes does not connect those records or change Unity's protected menu/result-page behavior. Q09 combat-combo publication remains a separate code/Play task; no runtime reader was changed here.
