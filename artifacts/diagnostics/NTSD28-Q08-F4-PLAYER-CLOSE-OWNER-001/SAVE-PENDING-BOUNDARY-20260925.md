# Q08 F4 recording-save boundary — source and Unity owner audit

Status: `READ_ONLY_CONDITIONAL_GAP / NO_PRODUCTION_EDIT / Q08_OPEN`. This follows the scoped Player F4 exit in `ACCEPTANCE.md`; it does not revoke that accepted ordinary close path.

The formally identified playable `src/main.cpp` initializes `recording_enabled=false` (line 115) and enables recording only with `--recording` (lines 637–638). It rejects recording in automated gates (lines 945–948). Thus the normal nonrecording F4 Player test exercised the default branch; it did not exercise a pending encoded recording.

When enabled, `RecordingApplication28::finish_before_result_tick` requires result timer 100 and encodes the recording into memory before the timer-101 step (`recording_application.cpp:557–576`). A failed commit leaves state `save_pending` and retains pending bytes (`:581–615`). `main.cpp:2344–2371` makes two immediate bounded retries and pauses the world on continued failure. `main.cpp:2637–2643` keeps the message loop alive for F9 retry/F10 explicit discard while no world tick advances. The nonrepeat battle F4 host command sends `WM_CLOSE` (`main.cpp:1930–1978`); `WM_CLOSE` retries a pending save and vetoes window destruction on failure (`:1900–1910`). Esc/title-bar/OS graceful close share that guard. `RecordingApplication28::discard_pending` requires `save_pending` and explicitly clears pending bytes only on F10 (`recording_application.cpp:621–637`).

The current Unity self-written `Assets/NTSD/Scripts/Simulation`, `App` and `Load` trees have no `RecordingApplication`/`save_pending` owner. `AppManager.TryCloseBattleApplicationFromNativeFunctionKey` correctly performs ordered battle shutdown and Player `Application.Quit(0)` for the accepted ordinary F4 path, and does not currently have a recording state to query. Therefore it would be wrong to add a fake unconditional F4 veto or to claim the recording-enabled branch is verified. The missing branch is conditional on introducing an actual encoded-recording production owner. It must be tracked with Q08/Q11 recording ownership, not hidden in Q12 as a mere test gap.

Future exit contract when recording is in scope: one production owner must retain encoded bytes across commit failure; pause further simulation at the timer-100 boundary; route F4, Esc, title-bar and graceful OS close through the same retry/veto; allow F9 retry and F10 explicit discard; then verify successful commit, failed commit, repeated close refusal, explicit discard, ordered shutdown and zero residuals in a recording-enabled Player. Editor-only synthetic state is insufficient for the Player/persistence claim. Whether the user authorizes implementing optional LFR recording in this battle-alignment campaign remains a separate scope decision; no claim of an approved exception is made here.

This audit read current playable source and Unity caller paths only. It did not run a native EXE recording session, rebuild the native source, invoke Unity, or change scripts, assets, DAT, Scene or ProjectSettings.
