# Q08 physical F4 Player close — scoped acceptance (2026-09-22)

Status: `VERIFIED_PLAYER_F4_CLOSE_SCOPE`. Q08 aggregate remains open. Formal rule: a fresh battle F4 routes to `leave_battle`, then the playable host sends guarded `WM_CLOSE` and ends the application. Source/call graph and authority identity: `NTSD28-Q08-F4-CLOSE-OWNER-AUDIT-001/REPORT.md`.

The Unity Player driver now consumes the accepted leave request before automatic tick advancement. `AppManager` calls the existing ordered shutdown and only then `Application.Quit(0)`. The Editor build keeps the previous diagnostic handoff and never automatically quits the Editor. `UnloadBattle()` was not used or changed, so F4 does not return to Menu.

The fresh Windows x64 Mono Development Player build `q08-f4-build-1` succeeded with 0 errors and reported 130,686,213 bytes (`artifacts/diagnostics/NTSD28-Q07-CONTENT-MIGRATION-READINESS-001/q08-f4-build-1-build.json`). Two independent launches of that build with the focused physical F4 probe both returned PASS (`player-run-1.json`, `player-run-2.json`). In each, F4 was queued after battle reached Running, tick was **3 before F4 and 3 at quit**, lifecycle at quit was `Stopped`, active pool borrowers were **0**, and the pool was quiesced. The second process was launched with `Start-Process -WindowStyle Hidden -Wait -PassThru` and returned exit code **0**. The first run's process exit code was not captured.

The existing real Editor Play physical function-key probe returned PASS (`editor-physical-key-play.json`): F4 handoff and F6/F7/F8/F9/F10/F11/F12/Ctrl+F10 controls passed. Focused EditMode router 8/8 after adding exact repeated-F4 and outside-battle-F4 rejection assertions (`ec64772419bc41a18bfaf7db73690999`); production integration 11/11 before that Editor-only test addition (`b62705d83fe543eb93fff71b6bf62780`). After the final test edit Unity compiled and the 8/8 router class reran. Editor exited Play, active battle Scene was `isDirty=false`, rootCount14; battle/Menu Scene hashes stayed `BCD1047BF912C6A4A8BC9F3A76EAF3FA954211AD064E0402B1` and `6CF124A17F692325CED5774C3C10C3324AA047439BC6C3EB08856188A78AB4B1`. Change Ledger validator passed 654 records/19 governed diff files; `git diff --check` exited 0 (line-ending warnings only).

Limits: the Unity runtime has no corresponding encoded recording save-pending owner, so the formal recording persistence/veto branch remains unimplemented and unverified. We did not force an ordered-shutdown failure or test application-quit veto behavior. The Player log reports legacy missing audio directories already assigned to Q10. Q08 result/stage-count behavior, Q07 Menu→Battle Scene closure and the full alignment goal remain open. No computer-use, Scene/Build Settings change, old-resource deletion or nonbattle production edit occurred in this package.
