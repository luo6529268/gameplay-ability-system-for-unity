# Q07 Naruto clone central command and camera-pixel witness — 2026-09-22

Decision: `VERIFIED_REPRESENTATIVE_CENTRAL_PIXEL_ROUTE_ONLY`. In real `NTSD_Battle` Play under the serialized formal Logan root, the existing physical L/D/J probe and a new independent central-pixel observer both passed in the same third run. This closes a representative OID33 command-to-camera-pixel link, not original EXE pixel parity, all-character rendering, sorting, shadow or full skill behavior.

Formal content: OID33 `Naruto(Clone)` authored frame240/241 pic999 and frame242 pic1 from `ncl.dat`; `ncl.png` SHA-256 `F8673833CCC0FC18695EBD956911704C8981B65179E3EEE343AA31F9597135B0`. The production GameConfig root and semantic fingerprint were checked by the Play observer. The concurrent unchanged physical-input result `../NTSD28-Q07-NARUTO-CLONE-SPRITE-BINDING-001/q07-naruto-clone-5.json` is PASS: L/D/J at ticks2/4/6, frame285, OID33 hidden-to-visible formal entry.

The focused `q07-clone-pixel-3.json` is PASS. At visible tick12 and central plan simulation tick12, it found an `Entity` command with stableId103, runtimeSlot51, visualDataId33 and effectivePic1. After releasing the observer's central submission lease, a temporary RenderTexture capture using the bound production world camera produced 2,583 non-clear pixels; 1,087 were within the projected command bounds (3,192 sampled pixels). The PNG is `q07-clone-pixel-3.png`, SHA-256 `6920BBC528956DE904793B561CC45A578F8CF509B9CC22AD7052B25B09C22B1A`. It visibly contains the battle characters. The region count alone cannot attribute every pixel uniquely to the clone where sprites overlap; the current central command and camera output together establish this limited render path.

Two first differences remain preserved. `q07-clone-pixel-1.json` FAILed because the test checked the content fingerprint before prewarm publication; the concurrent existing physical-input result later passed. `q07-clone-pixel-2.json` FAILed with an all-white screenshot despite finding the exact tick12 OID33 command. Source review found that the observer held the central submission read lease during `Camera.Render()`, preventing the render feature's own acquisition. Releasing the lease before the third capture yielded non-clear pixels. Those failures are test setup/read-order evidence, not proof of a production visual defect, and were not overwritten.

The existing Editor reimported and compiled the new test script; no new `error CS` appeared in the current log tail. `Tools/Validate-ChangeLedger.ps1` passed (651 records, 13 governed code files), and the targeted `git diff --check` passed. The Editor returned idle outside Play on `NTSD_Battle.unity`, `isDirty=false`, rootCount14; both request JSON files are `requested:false`. Battle Scene SHA-256 remains `BCD1047BF912C6A4A8BC9F3A76EAF3FA954211AD064E0402B1C01BF3BA0E9FB6`, Menu Scene remains `6CF124A17F692325CED5774C3C10C3324AA047439BC6C3EB08856188A78AB4B1`. No production script, Scene, content, ProjectSettings, old resource or nonbattle file was edited by this package; no computer-use or second Editor was used.

Q07 remains `IN_PROGRESS`: this single clone witness does not establish compositing and layer order against the formal EXE, shadow placement, other DAT/image associations, natural hits/lifetime, full Menu→Battle closure or platform parity. Q09/Q12 and R17/R18 retain final presentation and full-skill exits. The known empty Build Settings scene list remains a separate Menu Scene first difference.
