# Q09 knockout-feed row projection audit (2026-09-22)

Status: `STATIC_FIRST_DIFFERENCE_CONFIRMED`; no Unity compile, NUnit, Play, or pixel acceptance is claimed by this audit.

## Authority path

- Formal `source/ntsd28_core/src/rendering/render_snapshot.cpp:832-853` builds a player label from `RenderSnapshotConfig28::player_names[slot]`, defaulting to `Com`, optionally brackets it, and optionally appends the entity BMP `name` (or `none`). This label is not derivable from knockout counters alone.
- Formal `render_snapshot.cpp:1977-2099` applies the mode feed's lifetime to a newest-tail record count, then gates drawing on `enabled`, `runtime_display_enabled`, and the allowed battle mode. A row is drawable only when its timestamp is positive and `snapshot.tick < event.battle_time_ticks + 30`, its victim is not excluded, and both four-owner attacker and victim slots still resolve. An absent actor produces a diagnostic, not a substitute row. Source object types outside 1..6 use type icon 0.
- Formal row placement starts at `screen_top`. The loop advances `row_spacing` even for excluded/missing actors and at exactly event time +30; after +30 it reverses that advance. This means filtering the event list first and then packing rows densely is not equivalent. Right alignment subtracts `text.size() * native_glyph_width` from the configured X.
- Formal `source/ntsd28_playable/src/d3d11_renderer.cpp:2076-2118` consumes each row after mode-specific HUD layers and before the later controls/scoreboard. It draws the whole selected type PNG, then attacker and victim glyph strings, using WORDS resources when available and a fallback glyph path otherwise. Audio is a separate Q10 concern.

## Original Unity project, current source

- `Assets/NTSD/Scripts/Simulation/Core/SimulationWorld.cs:547-598` owns per-knockout events; `Runtime/BattleRuntimeState.cs:549-603`, `Host/SimulationTickDriver.cs:1319-1327`, and `Core/NTSDBattleTickSystem.cs:553-554` carry the mode lifetime and tail prune. These are logic prerequisites, not row publication.
- `Assets/NTSD/Scripts/Animation/LoganModeComboInput.cs:11-73` captures the needed feed fields from the selected formal mode child. The three referenced kill-icon PNGs are byte-staged separately under `Assets/NTSD/Content/LoganRuntime/vfs/sprite/kill/`; their image byte identity in the visual publication contract is still open.
- `Assets/NTSD/Scripts/Simulation/Presentation/BattlePresentationShadowBuild.cs:12-18` declares only Shadow, Entity, OverlayGlyph, and HitRecord command types. `BattlePresentationFrame` at lines 888-919 contains entity/hit-record/command storage but no knockout-feed rows; its frozen copy at lines 974-1020 copies only those current structures. `BuildCommands` starts at line 2574 and builds the existing entity overlay path. A static search of the battle presentation/render path found no knockout-feed row consumer. This is the first missing presentation handoff, not evidence that Unity pixels already match the formal renderer.

## Next bounded implementation and evidence

1. Establish icon PNG bytes and the selected mode-feed input in the visual candidate/publication identity, retaining the existing battle content generation and lifetime contract. Do not replace ordinary HUD or menu UI.
2. With a pre-change Task Contract and Change Record, add a battle-owned row snapshot/projection preserving the formal record order, 30-tick edge, missing/excluded-slot spacing, labels, icon selection, and layer order. Keep screen coordinates and interpolation out of World logic truth. Account for frozen-frame copy, publication lifetime, renderer consumption, and orderly shutdown.
3. Focused source/Unity cases should cover `t+29/t+30/t+31`, two records with an excluded first victim, missing attacker/victim, type 0/1..6 selection, mode/bound gating, labels/right alignment, and snapshot/restore. Once the original Editor accepts current source, run only the relevant NUnit/self-check and targeted battle scene; compare observable drawing with the formal EXE. Q09 and R17 remain open until that evidence exists.

No Unity project or Editor was launched for this audit. The original project remains `I:/GitHub/Unity_GAS/gameplay-ability-system-for-unity`.

## 2026-09-22 icon publication ownership follow-up

- The selected formal `decoded_dat/data/mode/ntsd.dat` maps `pic_type0` to `sprite/kill/c.png`, `pic_type3` to `sprite/kill/sk1.png`, and types 1/2/4/5/6 to `sprite/kill/sk2.png`: seven logical type slots, three unique PNG inputs. The original project's staged VFS currently contains those three files. This is content identity, not a live Sprite publication.
- `LoganVisualContentCandidate.NativeKillIconInput` captures all seven selected paths, presence and SHA, then `AssertInputsCurrent` rechecks that identity before commit. `CharacterAnimtorManager`'s native prewarm at lines 1542-1636 builds SPARK and optional WORDS, calls `TryBuildUnifiedCentralAtlasPublication`, then commits with `TryCommitNativePublication`. It currently has no `BuildKillIconPublicationAsync` or seven-slot icon catalog. A row's `TypeResourceAvailable` therefore cannot imply a drawable Unity image.
- The existing publication transaction owns staged Sprite/Texture resources in `NativePrewarmOperation.Sprites/Resources`, transfers them at commit, and retires the old generation only after catalog renderer bindings release (`TryCommitNativePublication` 391-445; `RegisterRendererCatalogBinding`/`UnregisterRendererCatalogBinding`/`TryRetireCatalogIfUnbound` 3206-3285). A future icon resource must be validated/decode-checked, staged before the no-await commit, transferred into that same lifetime, and resolved against the row frame's bound generation rather than the mutable current manager. Three unique textures/sprites can back seven type aliases; absent formal paths must stay absent rather than silently using a different type.
- The screen consumer is still undecided: `BattlePresentationFrame` already retains a sprite-catalog publication binding for frozen frames, but the currently observed central renderer is tied to the world camera, whereas the formal draw is after mode HUD. A battle-only HUD-camera layer candidate requires original-scene Play coordinate/order proof. Do not add icons to unrelated ordinary HUD or bypass the frozen-frame catalog lease. A bounded follow-up Task/Change must declare the exact publication/catalog/consumer paths and a focused replacement/retirement test before scripting.

Current gate: static owner and cardinality identified. No icon publication, original-Editor NUnit, scene pixel or formal EXE comparison has been completed; Q09/R17 remain open.

## 2026-09-22 presentation-owner and name follow-up

- Formal `source/ntsd28_playable/include/ntsd28_playable/game_session.h:303-312` has a separate ten-slot battle name table (`Remie`, `Kezeal`, `John`, `Herkato`, `5`..`8`, `<No name>`, empty) and eight bracket flags. `GameSession28::render_snapshot` forwards both at `game_session.cpp:3340-3342`. Scenario input can override the names (`scenario28.cpp:710-736`). Unity `App/GameLocalSettings.cs:12-40` has four persistent `Player1`..`Player4` defaults, and `App/PlayerSlotConfig.cs:6-23` has no battle-name field. Using those four local settings as the native ten-slot table would alter visible text. A battle-only name configuration/capture boundary is required before calling row text aligned; changing the app's ordinary player-name settings is outside this Q09 seam.
- Original battle scene `Assets/NTSD/Scene/NTSD_Battle.unity:6534-6615` already has a Screen Space Camera Canvas, `HUDCamera`, and a 1920x1080 CanvasScaler reference. `HUD` and `HUDBg` are existing children; the user-confirmed `HUDBg` x=30 is present at scene line 5842 and must be preserved. `BattleCentralRenderSystem.CanRenderCamera` at lines 1035-1052 only accepts the world base camera, while the central feature injects after transparent world rendering (`BattleRenderFeature.cs:27,52-57`). Therefore a new central entity command alone does not establish the formal D3D order after mode HUD; actual layer relationship is still a runtime question. Do not modify the Canvas or global HUD merely to make a command visible.
- The appropriate split is now explicit: (1) deterministic, immutable battle row data in `BattlePresentationFrame` with the existing frozen-copy/reset/release lifetime; (2) battle-only screen consumption using the selected published icon/WORDS generation and the actual HUD camera/viewport mapping. The second half must be chosen after a focused original-scene layer/coordinate probe, not from `CanvasScaler` reference size alone. The row contract can be source-tested independently, but Q09 cannot close without the visual probe and formal comparison.

## 2026-09-22 18:20 Canvas layer candidate, not a runtime result

Read-only Scene YAML mapping of `Canvas` (`RectTransform` fileID `1947371493`) gives direct children in serialization order: `HUD`, `ControBg`, `ComboBg`, `ComboTemp`, `ComboArraw`, three image nodes, attack/jump/defend buttons, `HP`, and another image. `HUDCamera` has camera depth 0; the world base camera has depth -1. The Canvas uses Screen Space Camera on `HUDCamera`, reference resolution 1920x1080, and no override sorting. Thus a battle-owned Canvas child immediately after `HUD` is a plausible screen-layer insertion point after existing HUD and before controls, whereas the current central world-camera command is earlier than the HUD camera. This is a **static candidate only**: serialized order alone does not establish the exact formal mode-HUD and scoreboard layering, effective viewport/letterboxing, or an EXE pixel match. Preserve the user-confirmed `HUDBg` x=30 and do not modify the Scene before a focused original-project Play probe.
