# Q07 production battle-content root — scoped acceptance (2026-09-22)

Status: `VERIFIED_SERIALIZED_BATTLE_SCENE_ONLY`. The referenced production GameConfig asset now has the single added serialized field `BattleContentRuntimeRoot: Assets/NTSD/Content/LoganRuntime`. Scene and Menu references both point to GUID `513170c32731baa449d4d0e9abf8c827`; this run exercised the battle Scene, while Menu behavior under the serialized default remains a later focused return. No old DAT/image resource was deleted or rebound.

Pre-change GameConfig SHA-256 was `685E13823A6D726219B37B1B0B2A8E9B9A76B5B7975ED9CC8E598763B366B004`; post-change is `C4DB45C7426045D09FB481B2FC1D3FF27089CAF902A7AFE0CA413DE1795DF4B2`. `git diff --numstat` shows exactly one added YAML line and no deletion. The protected battle Scene SHA-256 remained `BCD1047BF912C6A4A8BC9F3A76EAF3FA954211AD064E0402B1C01BF3BA0E9FB6`; Scene, Menu and ProjectSettings have no Git diff.

Unique Windows 64-bit Mono Development `BuildPipeline.BuildPlayer` run `q07-serialized-build-1` succeeded with 0 build errors and reported 130,681,174 bytes. Its output has the 1,343 formal sidecar files and SPARK. The built Player ran hidden with `-ntsd-q07-serialized-root-probe` and `Start-Process -Wait -PassThru`, returning exit code **0**. Its terminal `q07-player-runtime.json` reports **PASS**, `selectionMode=serialized`, `serializedRootBefore=Assets/NTSD/Content/LoganRuntime`, and `sceneConfigMatched=true`. In this mode the probe does not assign the content-root field.

The actual Player published expected formal semantic fingerprint `FD18D668B9D4EF0FAD4EE3D8056F98754049B3F25FB6927EC562C3F60B008147` with equal manager/data/UI source keys, a Running World of 4 objects, and 29,400 tracked publication resources. Ordered shutdown reached `RuntimeMapCleared`; World stayed Stopped after two frames, pool borrowers were 0 and tracked resource survivors after unload were 0. These checks establish default battle Scene content selection and lifecycle on this Windows Player build. They do not establish natural-skill parity, full visible presentation, audio or other platforms. The Player log still contains 13 missing legacy `NTSD/Sound/SFX_*` directory reports; Q10 owns that portability/audio return.
