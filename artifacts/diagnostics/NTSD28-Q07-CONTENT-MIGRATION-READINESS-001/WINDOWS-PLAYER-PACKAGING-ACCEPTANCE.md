# Q07 Windows Player raw content packaging — scoped acceptance (2026-09-22)

Status: `BUILD_OUTPUT_VERIFIED / PLAYER_RUNTIME_PENDING`. The production GameConfig still has an empty formal-content root; this report proves a Windows Mono Development Player build artifact and its raw formal-content sidecar, not startup under formal content or natural-skill parity.

The first R8 Windows Mono build failed at 28 Player-only C# conditional compilation errors before postprocessing. `NTSD28-Q07-PLAYER-COMPILE-GUARD-001` resolved those in three battle files without changing AI decisions. A subsequent R8 build reported success and produced an EXE but had **zero** Q07 files: the new `IPostprocessBuildWithReport` callback had returned while `BuildReport.summary.result` was still `Unknown`. That failed artifact is retained under `Temp/R8-Windows-Player/Mono`; it is not a Q07 acceptance build.

The request-driven unique-output probe first lost a `delayCall` continuation (`q07-package-build-1/2`, no BuildPipeline invocation). It then entered BuildPipeline but hit Burst 1.8.21's assembly-reference timestamp error (`q07-package-build-3`, `BuildResult.Failed`, 2 build errors). The probe now calls BuildPipeline directly from its guarded Editor update and reuses the existing R8 diagnostic Burst setting helper, restoring the exact prior bytes in `finally`. No prior output was cleaned or overwritten.

Final build `q07-package-build-4`: actual `BuildPipeline.BuildPlayer`, Windows 64-bit Mono Development, result `Succeeded`, total errors `0`, reported build size `130,671,528` bytes. The callback logged entry with `platform=StandaloneWindows64`, `result=Unknown` at postprocess time, then logged packaging `1,343 files, 46,594,829 bytes` plus existing `SPARK.bmp`. The independent output audit compared every Player-sidecar file to the frozen CSV and project-local staged source: **1,343/1,343 present, 0 extra, 0 size/hash mismatches**. The EXE and Data directory exist; Player raw SPARK SHA equals the source. Output: `Temp/Q07-Windows-Player/q07-package-build-4/`.

The temporary Burst settings SHA-256 was `72601656F7F4B74E53D13C65A01CF8A26A450257D75A3F145A0D5D6EBF5D1296` both before and after. Protected battle Scene SHA-256 remained `BCD1047BF912C6A4A8BC9F3A76EAF3FA954211AD064E0402B1C01BF3BA0E9FB6`; production GameConfig asset and Scene have no Git diff. The first R8 build generated four untracked Odin asset/meta files under `Assets/Plugins/Sirenix/Odin Inspector/Config/Editor`; they were preserved, not attributed to the Q07 source change or removed. `ProjectSettings/ProjectSettings.asset` is clean again after the successful build.

Focused EditMode job `57ed5ec69c0b4b99b90bcbf24e7c3f99` passed 11/11 targeted AI injection/nearest snapshot cases after the compile-guard edit. Editor compilation was clean; the actual Player build resolved the earlier 28 C# errors. Next gate: run this built Player with formal content selected through a test-only startup seam, verify publication, World creation, shutdown and zero resource residue, then decide the production GameConfig switch. Windows sidecar distribution must include the entire EXE directory, not only the EXE. Android/WebGL remain outside this build acceptance.
