# Q07 built Windows Player runtime — scoped acceptance (2026-09-22)

Status: `VERIFIED_PLAYER_CONTENT_BOOTSTRAP_ONLY`. The production GameConfig still has an empty content root, so this is an explicit Development Player probe of the staged formal source, not the production switch or natural-skill parity.

`q07-player-runtime-build-1` was a unique Windows 64-bit Mono Development `BuildPipeline.BuildPlayer` invocation. It succeeded with 0 errors, reported size 130,680,494 bytes, and contained 1,343 formal DAT/catalog/image sidecar files plus SPARK. A fresh binary/content copy at `Temp/Q07-Windows-Player/q07-player-runtime-run-3/` was launched with `-ntsd-q07-player-probe` and a hidden window. `Start-Process -Wait -PassThru` returned exit code **0**. Its `q07-player-runtime.json` ended in **PASS**.

The Player selected `Assets/NTSD/Content/LoganRuntime`, published formal fingerprint `FD18D668B9D4EF0FAD4EE3D8056F98754049B3F25FB6927EC562C3F60B008147`, and reported identical manager/data/UI source keys. A Running World held 4 objects; representative formal IDs 0, 50 and 52 were present. The probe tracked 29,400 owned publication resources. Ordered shutdown completed through `RuntimeMapCleared`; the World remained Stopped after two frames, active pool borrowers were 0, and all tracked resources had 0 survivors after scene unload. The initial run-1 also reported PASS, but its process exit code was not captured; run-3 independently captured it.

The Player log contains **13 `NTSD/Sound/SFX_*` directory-not-found reports** from the legacy audio loader. They do not invalidate the observed DAT/image bootstrap or orderly close, but prevent any claim that portable audio or full battle presentation is complete. Keep audio ownership/packaging in the Q10 return, and assess natural skills/visible assets after the production content switch. Android/WebGL were not built. Production GameConfig and ProjectSettings have no Git diff; protected Scene SHA-256 remained `BCD1047BF912C6A4A8BC9F3A76EAF3FA954211AD064E0402B1C01BF3BA0E9FB6`. No old resource was deleted.
