# Q07 resume: formal content remainder and owner gate

Status: `READ_ONLY_CURRENT_COUNTS / OWNER_ROUTING_PARTIAL`, 2026-09-23. User reports the earlier Naruto landing run and Rasengan follow-up now feel basically normal and permits resuming the original alignment sequence. This is user feedback, not formal EXE parity or a completed D-024 all-entity motion certificate.

Current disk enumeration, without changing any source or asset: formal `resources/runtime/decoded_dat` has **405 DAT** and project `Assets/NTSD/Content/LoganRuntime/decoded_dat` has **338 DAT**, leaving 67 names absent. The existing `NTSD28-Q07-REMAINING-DAT-REACHABILITY-001/REPORT.md` and its 67-row CSV classify them as background 24, background-mode children 25, background-mode index 1, battle HUD child 1, minibar 5, audio 2, menu 1, stage/story 7 and user-held default `stage.dat` 1. That earlier report established 338/338 shared paths with equal SHA-256; this resume pass checked counts, not every hash again.

Current PNG enumeration under formal and project-local `vfs` is **1255 versus 1031**, leaving 224 names absent. Every missing PNG is under `b/` (110) or `sprite/` (114); there is no missing `c/` character PNG in this name comparison. The `sprite/` set groups as `UI/` 81, `radar/` 16, `menu/` 8, `minibar/` 4, `frame/` 2, `Loading/` 2 and `small/` 1. The two frame files are `sprite/frame/INKHUD2/BARS.png` and `FRAME.png`, which belong with Q09 frame-HUD consumer work. Directory labels alone do not prove all other files' runtime owners; the `b/` and background-mode sets need Q07/Q08 caller review, and UI/menu groups must respect the nonbattle boundary.

The default battle object content root and Menu-first Windows Player callback path already have scoped acceptance in `NTSD28-Q07-CONTENT-MIGRATION-READINESS-001/PRODUCTION-ROOT-SWITCH-ACCEPTANCE.md` and `NTSD28-Q07-MENU-FIRST-WINDOWS-PLAYER-001/REPORT.md`. Q07 remains open for broader natural skills, formally reachable non-object resources and runtime presentation. The old-content retirement audit has 521 rows with `deleteAuthorized=false`: legacy loading, Editor preview/tests and serialized references still own old assets. Nothing here authorizes copying all missing resources, modifying any DAT token, rebinding Scene references or deleting old files.

Next bounded Q07 gate: start from the formal post-roster background selection and active battle HUD caller, record which absent DAT/PNG each actually consumes, and route background selection to Q07/Q08, frame HUD to Q09, audio to Q10, and protected Menu-only resources outside battle changes. Choose any copy/rebind package only after that exact caller and reference list is frozen. The known missed-Rasengan OID434 motionX first difference and remaining D-024 all-entity distance work retain independent battle-return ownership; they do not block this read-only Q07 content audit.
