# OID32 published SpriteCatalog in the original Battle Scene

Status: `VERIFIED_SCOPED_PUBLICATION_OBSERVATION / PIXEL_AND_NATURAL_REACHABILITY_PENDING`. The original Unity Editor PID11944 compiled the new opt-in probe and its Unity-generated `.meta`, then consumed one create-new request (`oid32-catalog-20260925-01`) in the saved `NTSD_Battle` Scene. It entered Play once, waited for formal content prewarm and driver tick5, read the production `CharacterAnimtorManager.SpriteCatalog` without mutation, wrote one result, and exited Play. The live Editor subsequently reported `Play=false`, `Changing=false`, idle. Menu/Battle Scene disk SHA-256 remained `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13` / `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`; neither Scene has a Git diff.

The result JSON SHA-256 is `A74AFB2BED00E839843DEDA9E9D8832BABBECBF26A502AE425089C972E6C573F`. It reports `status=OBSERVED`, `sceneDirty=false`, content root `Assets/NTSD/Content/LoganRuntime`, a nonempty published visual key, catalog count **27,825**, and OID32 character data loaded. The authored pic0 control is present, points to staged `m/nin/hun.png` (799×480), and has a valid central binding. The authored frame95 pic64 is **absent** from the same published catalog. This establishes a production resource-publication difference at a controlled formal data key, building on the focused original-Editor rect-builder result (pic64 absent) and formal source/root EXE controlled action95/pic64 sprite command.

This probe did not materialize OID32 as a Unity battle entity, force action95 in Unity, render a pic64 pixel, or observe a naturally selectable OID32 path. The formal root EXE replay exposes aggregate sprite counts rather than GPU pixels, while paired source D3D point/clamp plus fully opaque bottom edge predicts visible repeated edge pixels. Those limits remain explicit. No DAT/PNG/production/Scene/Prefab/nonbattle change was made; only the new Editor diagnostic script/meta and governance documents changed. The next gate is a controlled same-state Unity entity/presentation witness and paired formal offscreen pixel capture if the formal offscreen gate can run with the locked content, followed by natural reachability classification before a generic renderer fix.
