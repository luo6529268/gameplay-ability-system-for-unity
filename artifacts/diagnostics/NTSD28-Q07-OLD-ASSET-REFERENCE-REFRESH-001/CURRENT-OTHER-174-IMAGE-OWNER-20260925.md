# Q07 non-indexed Unity image owner boundary

Status: `READ_ONLY_OWNER_CLASSIFICATION / NOT_BULK_DELETE_SCOPE`. The current 174 paths came from the previous reference graph as `OTHER_IMAGE_OWNER_REVIEW`; they are separate from the 383 old indexed character images. `CURRENT-OTHER-174-IMAGE-OWNER-20260925.csv/.json` records current file/meta presence, SHA-256, GUID and selected serialized referrers. The 2026-09-25 scan covered 877 `.unity`, `.prefab`, `.asset`, `.mat`, `.controller` and `.playable` files in `Assets` plus `ProjectSettings`, excluding staged LoganRuntime.

| Existing path family | Rows | Scope implication |
| --- | ---: | --- |
| `Sprite/UIPanels` | 95 | Menu and Battle HUD visuals; preserve nonbattle UI and approved HUD exceptions. |
| `Sprite/OtherBg` | 24 | Project-owned backgrounds; formal background replacement is user-excluded. |
| `Sprite/Common` | 37 | Mixed shared and battle effects; `black.bmp` is an explicit SelfCheck fixture input. Need individual producer/consumer evidence before any replacement. |
| `Sprite/DropItem` | 8 | Potential battle item visuals; inspect runtime resource readers before classifying as old character content. |
| `Sprite/XueYuan` | 6 | Historical image family with at least one current serialized owner; keep until exact owner is classified. |
| `Sprite/Character` | 4 | Old Sasuke CS2 images outside the 383 indexed set. No selected serialized GUID owner, but that is not dynamic reachability proof; investigate formal equivalents and authoring readers separately. |

Current existence: 164/174 files and 164/174 metas; ten previously catalogued UIPanels paths are currently absent. This audit did not move, restore or delete them. Current selected serialized GUID owners appear on 38 rows, concentrated in `OtherBg` 2, `UIPanels` 35 and `XueYuan` 1. This is a narrower present-state result than the older graph's reference count; neither count is a deletion rule. All 174 rows retain `deleteAuthorized=false`.

An exact basename-only search against the 138 old indexed DAT can produce false matches: `Sprite/Common/shuriken.bmp` and `Sprite/Common/weapon8.bmp` share names with **different** `Sprite/Weapon/...` paths referenced by old OID 121 and 123 DAT. Do not infer ownership or a formal mapping from basename. Full DAT path resolution, Editor fixtures, shared resource loaders and nonbattle references remain to be audited before any item is added to a delete request.

The four unindexed old `Sprite/Character/Zuozhu/sasuke_cs2*.bmp` files need explicit character-image treatment: both old `Config/data.txt` and formal `decoded_dat/data/data.txt` identify **OID 38** as Sasuke CS2, while the formal `c/sasu/cs2.dat` declares `c/sasu/cs2.png`, `c/sasu/cs22.png`, `sprite/face/sasuke_cs2_f.png` and `sprite/small/sasuke_cs2_s.png`. The formal indexed-image SHA table confirms all four PNG paths are deployed byte-identically to formal content for OID 38. This is a role/slot mapping candidate, not proof that the four old BMPs are byte or pixel equivalent, nor proof that the old encrypted OID 38 DAT directly references each BMP. Current selected serialized GUID referrers are zero for those old BMPs; empty-root old DAT loading and any dynamic authoring reader still need closure before an exact deletion decision.

2026-09-25 exact shared-shadow owner follow-up: the one serialized `Sprite/XueYuan` image hit is `s.png` (GUID `04bffa57fa05fe44e8b259e1ba07141a`) in `Assets/NTSD/Prefabs/Common/Shadow.prefab`, serialized as its sprite at line 63. The prefab GUID `a74e0aa2ef455834393473f33ee52faf` is selected by `Assets/NTSD/Config/GameConfig/GameConfig.asset:38` as `ShadowPrefab` and is the source of `Assets/NTSD/Prefabs/Common/EntityObject.prefab`. The central-render `BattleCommonShadowDescriptor` validates that prefab's sprite/material metrics. This is a current battle presentation dependency owned by the project's common shadow, not an unreferenced old character sheet; keep `s.png` and its GUID stable. The eight `Sprite/DropItem` paths have no literal directory/file references in `Assets/NTSD/Scripts` or selected serialized GUID owners in this audit, but dynamic prefab/resource loading has not been disproven, so their deletion authorization remains false. No asset or runtime code changed in this follow-up.

2026-09-25 broader GUID check: all eight `Sprite/DropItem` and four unindexed Sasuke CS2 `Sprite/Character` GUIDs were searched together through current `Assets` and `ProjectSettings` using fixed-string `rg` arguments, excluding `.meta` and known binary media/DAT files. No reference was found (`rg` exit 1, no stderr). This widens the static negative beyond the 877 selected serialized files, but still does not resolve runtime path construction, old encrypted DAT declarations, loaded bundles or external readers. All 12 rows retain `deleteAuthorized=false`; the Sasuke CS2 formal OID38 mapping remains a candidate rather than a replacement certificate.

2026-09-25 Sasuke CS2 duplicate-byte finding: each of the four unindexed `Sprite/Character/Zuozhu/sasuke_cs2*.bmp` files is byte-for-byte SHA-256 identical to its same-name file in the old indexed `Sprite/Character/sasuke_cs2/` folder. Their `.meta` bytes/GUIDs differ, so a serialized owner could distinguish the two locations despite identical image bytes. The Q01 old indexed image projection explicitly ties the indexed head, small and two named sheet files to old OID38 `Config/Character/sasuke_cs2.dat` (`report-rerun/indexed-image-references.csv`, lines 2022–2028). This establishes the duplicate files' *image content* provenance more strongly than the prior OID/name candidate, but does not establish that the unindexed `Zuozhu` paths are reachable from the old DAT. The official OID38 PNGs remain distinct new-authority files, and no old BMP is deleted.

| Unindexed `Zuozhu` file | Identical old indexed OID38 file SHA-256 |
| --- | --- |
| `sasuke_cs2.bmp` | `59948653874FF915981EA143E943D147B5BC2F66309D4FC0B58C7678C51ADC88` |
| `sasuke_cs2_2.bmp` | `4C9F3CB9A23F673241EFFF67C859251AAA51E245BB95B4791B239CF39FEBDCA0` |
| `sasuke_cs2_f.bmp` | `CA93A5FE8C75165B38D99BB5BC6FF778408687B943A6C67E84447B7A0E174164` |
| `sasuke_cs2_s.bmp` | `5E1A02AFEFC98D934F821248E02CEAAA832800B771C9E0CA2AAE0009423CABC6` |

The eight old `Sprite/DropItem/*.png` current SHA-256 values have no byte-identical match in the 1,010 formal indexed-image SHA rows. This rules out an exact-file alias in that catalog, not an equivalent rendered sprite or a separate formal nonindexed resource. Because no active producer/consumer has been established and dynamic loads remain possible, these eight stay `OWNER_PENDING / deleteAuthorized=false` rather than being treated as migrated character or weapon images.
