# Q07 old Config DAT/image reader reachability after direct Battle edit

## Current battle-entry correction, 2026-09-25 (supersedes older Play-pending text below)

The production Menu→Battle empty-root gate is now `VERIFIED_SCOPED`, not Play-pending. Current `AppManager.InitializeBattleAsync` checks `CharacterAnimtorManager.ConfiguredContentRoot.Length == 0` before `ValidateConfiguredContentForBattleAsync`, `InitializeBattleSingletons`, and `BattleRunning`. The original Editor's two separate Menu callback Play requests are archived as `q07-empty-root-gate-20260925.json` (PASS, SHA-256 `68B2C2230CCF6E00B54B24009951520BB53F598D5FC56362CD00996F3155BD45`) and `q07-formal-root-gate-20260925.json` (PASS, SHA-256 `8217489BDAF57162D88DAB7E93C18232E4E262A40F20576674DC0B37AD506CFB`) under `NTSD28-Q07-MENU-SCENE-CALLBACK-PLAY-001`. The empty-root case completed legacy Menu prewarm and selection but was rejected before `BattleRunning`; the formal-root case reached BattleRunning/World2 and returned through ordered shutdown. The detailed scene/asset stability evidence is in `NTSD28-Q07-MENU-BATTLE-FORMAL-ROOT-GATE-001/ACCEPTANCE-PENDING-20260925.md` (its current top conclusion overrides the filename's older pending label).

This closes the **tested production battle-entry route**, not legacy-reader retirement. The old Config DAT/path-built image readers remain reachable from the empty-root Menu prewarm, Inspector refresh, Editor preview, explicit historical tools, and potentially external/default API callers. They are not evidence that the currently serialized nonempty formal-root Battle run reads old content, but they still forbid deleting any of the 521 inventoried old DAT/image files or their meta files. All rows stay `deleteAuthorized=false`; no nonbattle Menu or authoring behavior is changed here. The next Q07 owner is the remaining image/skill publication and exact old-file disposition, not another generic replay of this now-passed gate.

Status: `STATIC_CALLER_CENSUS / OLD_DELETE_AUTHORIZATION_ZERO / UNITY_FOCUSED_PENDING`. Source snapshot 2026-09-25. This is a current-tree audit of calls to `ParseCharacterFrameConfigs`, `LoadCharacterSpritesAsync` and `GameDataManager.LoadDataFile` under `Assets/NTSD/Scripts`; it is not a dynamic proof of no reflection, generated call or external plugin consumer. The serialized `GameConfig.asset` currently selects `Assets/NTSD/Content/LoganRuntime`.

| Reader owner | Current reachability and classification | Retirement action |
| --- | --- | --- |
| `BattleTestBootstrap.LoadCharacterDataAsync` | Direct Battle test entry now rejects an empty formal root and otherwise prewarms formal content. Its old parse/sprite calls are gone in current source. The new two-test request is queued but has not compiled or run in the original Editor. | Keep as `CODE_WRITTEN` until focused tests and Scene/Console check. No deletion based on static diff alone. |
| `LoadingPrewarmController.PrewarmOnceCoreAsync` | Production Menu→Battle prewarm uses formal content when the configured root is nonempty. On empty root, it still schedules `CreateCharacterConfigTask` and `CreateCharacterSpriteTask`, which invoke old Config DAT and image readers. | This is a battle-content entry through a normally excluded Menu/loading owner. Preserve Menu behavior; any code change needs a separately declared exact battle-impact scope and approval if it crosses the project's large/cross-module threshold. |
| `CharacterAnimtorManager.RefreshAllData` | Inspector authoring action uses formal prewarm for nonempty root; empty root directly invokes the old parse and sprite readers. | Decide explicit fixture mode or reject empty root in a separate Editor authoring Change, after its callers/tests are mapped. |
| `CharacterFramePreviewWindow.LoadDataFile` | Formal root returns before old index read; empty root directly loads old `Config/data.txt`. | Separate Editor preview fixture/missing-source contract and focused preview check. |
| `BattleRuntimeSelfCheck.CheckDeployableResolvedGeometryRisks` | `RunAll` directly invokes the old parser regardless of configured formal root. If `GameDataManager` already has formal objects, its `LoadDataFile` returns early while DAT paths still resolve relative to old Config. The check also contains old-content exact geometry assertions. | `NTSD28-Q07-SELFCHECK-FORMAL-GEOMETRY-001`: first measure formal Unity parser/cache results, then change this check while retaining synthetic collision-rule tests. |
| Explicit historical test/capture tooling | `BattleParityTraceEditor` loads a specified index; `NTSD28Q07LegacyDataLazyLoadEditorTests` uses the old index; `NTSD28UnityRawCaptureEditor` uses the old index constant. Content-source and preview tests assert historic old-root behavior. | Keep historical inputs or migrate each to owned fixture before old-file retirement; do not silently redefine a raw-capture baseline. |
| `GameDataManager.LoadDataFile` default argument | Public API still defaults to `Assets/NTSD/Config/data.txt`, but every direct source call found by this census passes an explicit path. | Do not treat the unused direct default as safe deletion proof. Review reflective/external callers and make an explicit missing-source contract only after known owners migrate. |

Exact source search run: `rg -n 'ParseCharacterFrameConfigs\(|LoadCharacterSpritesAsync\(|LoadDataFile\(' Assets/NTSD/Scripts -g '*.cs'`. The resulting method call set is the six owners above plus definitions. Old `Config/data.txt` and 138 indexed DAT cannot be removed: the Menu empty-root path, authoring path, SelfCheck and historical tests remain reachable. The 383 old indexed image GUID scan is separately zero in selected serialized file types, but path-built old sprite loading is still reachable through Menu/Inspector. No DAT, image, Scene or production script changed in this audit.

2026-09-25 per-file retirement overlay: `CURRENT-OLD-521-FILE-GATE-20260925.csv/.json` rereads all 138 old DAT and 383 old indexed image rows from the historical gate; all 521 files and 521 `.meta` files exist and their current SHA-256/GUID are recorded. A single-pass GUID scan over 877 selected serialized files in `Assets` and `ProjectSettings` (excluding staged LoganRuntime) found zero references to those 521 GUIDs. This **does not** clear any row: the old index and path-built readers above remain, and the scan does not cover dynamic strings, reflection, other extensions or external projects. Every row stays `deleteAuthorized=false`. The older Sasuke serialized-GUID hit is a historical pre-rebind snapshot, not a current referrer.

## 2026-09-25 post-SelfCheck caller refresh (supersedes the SelfCheck row above)

Fresh current-tree `rg -n 'ParseCharacterFrameConfigs\(|LoadCharacterSpritesAsync\(|LoadDataFile\(|LoadProductionDatWrapper\(' Assets/NTSD/Scripts -g '*.cs'` finds no direct Battle bootstrap call to the old parser/sprite methods. The current `BattleRuntimeSelfCheck.RunAll` calls `CheckDeployableResolvedGeometryRisks`, then `LoadFormalSelfCheckConfigs`, `CheckSpriteFileRangeParsingContracts(formalSelfCheckConfigs)` and `CheckMovementDatLoadingContracts(formalSelfCheckConfigs)`; it does **not** call `CheckNarutoDdjSixCloneProductionChain`. The old `LoadProductionDatWrapper` references are only inside that retained historical method and its two overload definitions. Original Editor full `BattleRuntimeSelfCheck` returned exactly `PASS`; the focused formal fixture projection and flash-catalog tests passed 2/2. Evidence and result SHA are in `docs/ai/CHANGE-RECORDS/NTSD28-Q07-SELFCHECK-LEGACY-DAT-FIXTURES-001.md`. Therefore the old Config reader is no longer reachable through these four audited aggregate SelfCheck callers. The historical method remains in source and is not a deletion clearance for its fixtures.

The remaining directly observed legacy routes are: `LoadingPrewarmController.PrewarmOnceCoreAsync` when `CharacterAnimtorManager.ConfiguredContentRoot` is empty (schedules both old config and sprite tasks); `CharacterAnimtorManager.RefreshAllData` when `HasConfiguredLoganContent` is false; `CharacterFramePreviewWindow.LoadDataFile` under the same false gate; explicit Editor historical tooling (`BattleParityTraceEditor`, `NTSD28Q07LegacyDataLazyLoadEditorTests`, `NTSD28UnityRawCaptureEditor`); and the public `GameDataManager.LoadDataFile` default path whose direct caller uses were checked but whose reflective/external use is not disproved. With the serialized nonempty GameConfig root, the Menu's selected branch calls configured Logan prewarm; that selected-branch fact does not make the empty-root branch unreachable. These paths, plus unknown external/dynamic readers, keep old-file deletion authorization at zero. This is a source call census and one aggregate Editor run, not a production Menu empty-root Play proof or all-extension reference scan. The next retirement action needs owner-specific fixture/missing-source contracts and fresh runtime evidence; it must not change Menu or other nonbattle behavior as a side effect.

## 2026-09-25 direct battle-entry guard follow-up

Fresh current-source read adds a second battle-entry condition to the retirement gate. `AppManager` calls `CharacterAnimtorManager.ValidateConfiguredContentForBattleAsync` before activating the Battle Scene. In that validator, an empty `ConfiguredContentRoot` may return a null publication key when legacy prewarm is complete and no Logan visual key is published; it is not an unconditional formal-root rejection. `LoadingPrewarmController.PrewarmOnceCoreAsync` is the Menu owner that can create that legacy publication from the old Config DAT and path-built images when the root is empty. The current serialized `GameConfig.asset` has a nonempty Logan root, so this is a reachable configuration branch, not evidence that the selected production Menu route currently reads old files. The direct Battle test bootstrap separately rejects an empty root; its guard does not close the Menu-to-`AppManager` branch.

Therefore an isolated change to the Menu prewarm fallback would not by itself prove that every battle entry requires formal content, and a zero serialized-GUID count is still insufficient to retire any of the 138 old DAT or 383 images. Keep `deleteAuthorized=false` for all 521. A future behavior change to the Menu/authoring empty-root path must preserve the project's nonbattle behavior boundary and receive its own exact Task/Change and focused runtime evidence; this read-only audit makes no such change. Exact current call sites: `Assets/NTSD/Scripts/App/AppManager.cs` (`ValidateConfiguredContentForBattleAsync` call), `Assets/NTSD/Scripts/Animation/Manager/CharacterAnimtorManager.cs` (empty-root branch in that validator), and `Assets/NTSD/Scripts/UI/LoadingPrewarmController.cs` (`PrewarmOnceCoreAsync` legacy task branch). No Unity test or Play was run for this follow-up.
