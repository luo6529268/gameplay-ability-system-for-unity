# Q07 control-only ITR candidate audit (2026-09-25)

Status: `STATIC_FIRST_DIFFERENCE_CONFIRMED / RUNTIME_WITNESS_PENDING / NO_SCRIPT_EDIT`.

The formal indexed DAT and the current Unity converter/cache produce 82 `kind=100100` ITR records with incomplete geometry (`hasGeometry=false`). The paired formal/staged Unity-assembly measurement is `UNITY-ASSEMBLY-FORMAL-GEOMETRY-TWO-ROOTS-20260925.json`; the count and classification are equal for both roots. This audit addresses their **candidate admission**, not the separate 27 complete `kind=0,w=0,h=79` records.

Native playable-source behavior: `ntsd28_core/src/simulation/collision_geometry.cpp:8-43` decodes an incomplete `kind=100100` ITR as `control_only`. `hit_candidates.cpp:154-169` counts the authored ITR, then skips it before candidate generation and before missing-geometry diagnostics. `collision_geometry.cpp:101-115` also omits it from projected frame boxes.

The existing native focused source test `ntsd28_core/tests/hit_candidates_tests.cpp:45-62` is an especially useful same-record baseline: it puts a control-only kind-100100 ITR at index 0 and a complete ordinary ITR at index 1, then asserts one appended candidate whose index remains 1. This means the Unity correction must **skip candidate production while retaining authored ITR indexing**. The test source is evidence of the native contract; no newly built or executed native test result is claimed here.

Current Unity source behavior: `LoganCombatRecordDecoder.ApplyInteractionGeometry` transfers `BattleBodyBoxValue.HasGeometry` into `InteractionArea.hasGeometry` (`LoganCombatRecordDecoder.cs:217-229`). But `BruteForceSceneQuery.IsReleaseItrGeometry` (`BruteForceSceneQuery.cs:7684-7687`) returns `itr != null` and ignores that flag. The same predicate admits an ITR to the role-aware attack list (`:4383-4404`), exact cache (`:2532-2536`), and pair collector (`:5440-5456`, `:5550-5588`). This is a **confirmed static source-level admission difference**. It does not yet prove an in-game damage difference, because later kind/effect/group gates can reject a particular pair.

The current strict `Overlap` expression (`BruteForceSceneQuery.cs:7722-7725`) does not reject a degenerate rectangle. For a zero-size ITR point `(10,10,10,10)` and body `(0,0,20,20)`, all four comparisons evaluate true (`10<20`, `10>0`, `10<20`, `10>0`). The converter defaults absent `x/y/w/h` to zero and records `hasGeometry=false` (`LoganCombatRecordDecoder.cs:232-252`), so a containing body is a possible downstream witness. The broadphase also widens a zero-width ITR's X range in `TryBuildFormalItrAabb` (`:7401-7430`). These arithmetic/source facts establish a concrete candidate risk, not a claim that a formal scene has already produced a false hit.

Current compiled-assembly witness (read-only external PowerShell reflection, no project or scene edit): `Library/ScriptAssemblies/Assembly-CSharp.dll` SHA-256 `5EF4A473DF1C99FCDF929E01349C3008C5CBA8C0508BB538E8AA0FDDDE266184` returned `IsReleaseItrGeometry(kind=100100,hasGeometry=false) == true`. Calling its private `Overlap` on `WorldRect(10,10,10,10)` and `WorldRect(0,0,20,20)` returned `true`. Unity 2022.3.62f3's `UnityEngine.CoreModule.dll` was loaded only to resolve types outside the Editor. This confirms the current compiled implementation has the same two gates as the source; it is still **not** an original-Editor candidate-collector run or a native/Unity same-state hit trace.

Downstream narrowing: `BattleHitCandidateEffectTypeResolver.Accepts` allows default `effect=0`; `BattleKindTableRules.RejectCandidate` does not reject a character target by kind alone; `BattleHitGroupEligibilityResolver` permits differing groups; `CandidateAccepts` returns true after earlier gates; and `AcceptReleaseSelectFlagCandidate` accepts otherwise eligible kinds outside 1/2/7. Thus there is no general kind-100100 rejection before `TryRecordReleaseCandidate` writes into the attacker's 20-entry `CollisionCandidateStore`. The current compiled `LF2HitResolveRuntimeData.ResolveCandidateDisposition(null, kind=100100 ITR, consume=true)` returned `Unsupported`; the consumer exits before damage for this unmodified kind (`BattleHitCandidateSequenceRunner.cs:200-216`). The **known risk is false candidate admission, count/capacity/order and pre-disposition observation**, not a demonstrated direct damage hit. A scene-specific candidate and side-effect trace is still required before asserting an observable battle outcome.

The focused capacity witness can stay small: use a frame with the incomplete kind-100100 ITR at source index 0 and a complete ordinary ITR at index 1, as in the native test. Put 20 target BDYs around the control point and one BDY under the ordinary ITR but away from that point; use ordinary opposing groups, no hit stop or exemptions. Native must append only the ordinary index-1 candidate. Current Unity source would attempt to fill the 20-slot row with control-record candidates before visiting index 1 if all earlier pair gates accept; the Editor test must observe the actual candidate row/count and valid candidate survival in both brute-force and role-aware profiles. A second fixture with only one enclosing BDY distinguishes simple extra-candidate admission from capacity exhaustion. This is a proposed reproducer, not an executed result.

Original-Editor transport check on 2026-09-25: Unity PID 11944 still listened on the local framed MCP bridge at port 6403. A direct read-only `get_editor_state` call returned idle, out of Play, no compile/test/import in progress, and the original `NTSD_Battle` scene active. The MCP `execute_code` tool with a trivial read-only expression and explicit `compiler=roslyn` returned `Roslyn (Microsoft.CodeAnalysis) is not available`; an earlier CodeDom/auto attempt on this Editor had failed before executing because compiler arguments exceeded the Windows command-line limit (see Q07 direct-Battle `ACCEPTANCE.md`). No live candidate probe was executed. The correct next runtime route remains a registered focused Editor test in the original project after safe recovery of the damaged governance documents; no new Unity project or second Editor is needed.

Next focused gate: in the **original Editor**, construct or locate a formal `kind=100100,hasGeometry=false` ITR and a body enclosing its projected point, run the same attacker/target pair through the current brute-force and role-aware formal collectors, and record candidate count/side effects. Compare with native `HitCandidateCollector28` on the same records and state; include a complete `kind=100100` geometry control case plus the valid zero-width `kind=0` case so the fix does not erase authored geometry. After the damaged progress documents are safely recovered and a Change Record is registered, the likely minimal production correction is for the shared ITR admission predicate to use the converted geometry-presence contract, subject to the focused result. Do not alter any DAT value.

2026-09-25 native focused-test follow-up: the earlier sentence saying no native test was built or run is superseded. The unchanged formal source's `ntsd28_core/scripts/build.ps1 -Target hit_candidates_tests` completed with exit code 0, and the resulting `ntsd28_core/build/hit_candidates_tests.exe` printed `hit_candidates_tests: PASS` with exit code 0. The executable SHA-256 is `BF37129FE0D2CB3333699D7992A02EE977A0AB5F4D18F489EDBE23C14E69BE33`; `hit_candidates_tests.cpp` SHA-256 is `65258B8AD7A84025B9B76360513D62C12F9E21347E95BFF4BC9CE5031BF968AC`, and `hit_candidates.cpp` SHA-256 is `4168C78A23849EAA17DD749002B89DD335841DB6CECA8B04FAE8356B10F1B905`. The formal root EXE SHA-256 was freshly checked as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. This focused native PASS confirms the source fixture's control-only skip/index assertion and neighboring candidate tests; it is not an EXE-visible or Unity collector runtime comparison. Unity candidate admission remains `RUNTIME_WITNESS_PENDING`, and no DAT or Unity script was changed.

2026-09-25 authored-corpus priority correction: a fresh read of the SHA-locked formal parser JSONL and formal `catalog.csv` found all **82** incomplete kind-100100 records in 82 distinct frames of four type-0 objects: OID854 Puppet5 74, OID54 KonanAngel 4, OID20 Orochimaru 2, OID51 Sasori 2. All 82 control records are the **last authored ITR** in their frame. Puppet5's 74 frames also have a preceding complete kind-8 ITR; the other eight frames have no other ITR. The full per-frame fields, ordinal and source identity are in `FORMAL-CONTROL-ITR-AUTHORED-FRAME-DISTRIBUTION-20260925.json` (SHA-256 `BD3455518C68ECDB4D3D773704542952B07B56E51DF6A74E3C9936F3FB26091F`). This changes the runtime-witness priority: first use a real Puppet5 frame such as OID854/frame407, with its complete kind-8 ITR at ordinal0 and control-only ITR at ordinal1, plus one of the eight control-only frames. The earlier suggested control-first/20-BDY capacity fixture is a **synthetic stress case**, not an authored formal-frame order; it must not be presented as a naturally occurring formal scene. Verify candidate count and downstream row effects for the actual order before claiming capacity starvation or visible battle impact. These are parser/corpus observations, not an EXE gameplay trace or Unity collector run.

The four authored DATs were also re-read directly from formal `resources/runtime/decoded_dat` and staged `Assets/NTSD/Content/LoganRuntime/decoded_dat`: each same-path pair exists with identical length and SHA-256. In the order Puppet5/KonanAngel/Orochimaru/Sasori, their SHA-256 values are `E2483952B8C4390A93A957533382948FEA5DB9952D430AF849BCA6EE615581B4`, `CD5380DD4934FB51BE095841BADF7C218181D2D3547414C65DAF4409938E9F29`, `D0BF93D80AF467D48B79738A061A6923D498E034A669B7EDE1877D0A64A5814E`, and `3A617FC575608A144AC5E44979EE642871C296F973D0C6503BE40BD2BED26063`. Thus this finding is not caused by a formal/staged byte mismatch; runtime candidate behavior remains the unresolved gate.

2026-09-25 authored-frame geometry fixture refinement: formal/staged `c/chi/pup5.dat` frame407 has `centerx=39,centery=79`, complete kind-8 ITR `(x=21,y=8504500,w=43,h=199)` at ordinal0, and incomplete kind-100100 `z=1` at ordinal1. Its first BDY is `(x=28,y=15,w=27,h=65)`. Under Unity's current `LocalRectWorldRect` and `VerticalWorldRect`, a right-facing attacker at `(XInt,YInt)=(400,300)` projects the missing-geometry control record to the zero-size point `(361,221)`; a same-frame right-facing opposing target at `(361,270)` projects its first BDY to `X[350,377],Y[206,271]`, strictly containing that point. Equal collision Z stays within the default 15 half-width; the complete kind-8 ITR is vertically remote. These coordinates give the focused original-Editor collector test a concrete **authored-data geometry arrangement** with an independent target group. They are calculated from current source and DAT, not a Play witness: group gates, object state, candidate store, and both collector profiles must still be executed before asserting an extra candidate. Formal source `collision_geometry.cpp` remains the skip authority, and the synthetic offset does not establish that natural gameplay reaches this arrangement.

The control-only authored comparison is `c/kon/ang.dat` frame11: `centerx=114,centery=201`, only ITR `kind=100100,x=218,y=218` (no width/height), first BDY `(x=57,y=59,w=115,h=81)`. With a right-facing attacker at `(400,300)` the degenerate ITR point is `(504,317)`; a same-frame independently grouped target at `(504,400)` has first BDY `X[447,562],Y[258,339]`, again enclosing the point. This tests an actual control-only frame rather than deriving its outcome from the Puppet5 frame's preceding kind-8 ITR. Its chosen pair placement is a test arrangement, not a natural encounter observation.
