# Q07 empty-root old-content retirement owner matrix (2026-09-25)

Status: `STATIC_OWNER_MATRIX / NO_RETIREMENT_OR_DELETE_YET`. This is a current-tree caller audit for the user-approved Logan DAT/character-image replacement. The serialized `GameConfig.asset` selects `Assets/NTSD/Content/LoganRuntime`; the old 138-DAT/character-image tree remains in Assets. No DAT values, asset paths, production scripts, Scene or Prefab were changed by this audit. The formal EXE/playable remains battle-rule authority; an old Unity fixture cannot define target content.

| Owner / exact entry | Current old-content reachability | Necessary retirement condition |
| --- | --- | --- |
| `LoadingPrewarmController.PrewarmOnceCoreAsync` (normal Menu battle prewarm) | If `ConfiguredContentRoot` is empty, it schedules `CreateCharacterConfigTask` → `CharacterAnimtorManager.ParseCharacterFrameConfigs` → old `Config/data.txt` and old indexed DAT, then `CreateCharacterSpriteTask` → legacy sprite loader. With the current nonempty serialized root it calls formal `PrewarmConfiguredLoganContentAsync`. | Treat a missing formal root as an explicit battle-content configuration failure, or migrate this branch to an explicitly selected isolated fixture. Do not silently select old production content. Preserve the ordinary Menu UI/lifecycle and nonbattle functions. |
| `BattleTestBootstrap.LoadCharacterDataAsync` (direct Battle scene test bootstrap) | Nonempty formal root uses formal prewarm. Empty root still invokes the same legacy parse and sprite methods. | Migrate direct-battle test setup to formal source or an explicit fixture before removing old files. Keep test-only scope separate from Menu production. |
| `CharacterAnimtorManager.RefreshAllData` (battle-content Inspector button) | Formal root already uses formal prewarm; empty root explicitly parses old index and sprites. | Decide whether this historical authoring mode remains an isolated fixture; it cannot retain an implicit dependency on removed production assets. Formal-root path is already scoped verified. |
| `CharacterFramePreviewWindow.LoadDataFile` (Editor battle frame preview) | Formal root now skips old index; empty root still loads `Config/data.txt` into shared `GameDataManager`. | Give empty-root preview an explicit fixture or remove that optional mode only after its tests/guidance are updated. Do not let preview mutate the formal publication. |
| `BattleRuntimeSelfCheck.CheckDeployableResolvedGeometryRisks` | `RunAll` invokes it; it directly calls `ParseCharacterFrameConfigs(dataManager)`, which requires the old `Config/data.txt` file and resolves DAT paths from the old Config directory. The check's exact behavior when a formal publication already occupies `GameDataManager` needs a focused run; static code alone does not prove its formal-content result. | Migrate the geometry check to declared formal content or a self-contained fixture before old-file retirement. A green full SelfCheck while old files exist is not evidence they can be removed. |
| `NTSD28Q07LegacyDataLazyLoadEditorTests` and `NTSD28Q07FramePreviewFormalIndexEditorTests` | Both explicitly assert empty-root legacy loading; removing the old tree makes those historical tests fail by design. | Replace their expected behavior with the formal-only/missing-source contract in the same scoped change that retires each entry; preserve the old result as historical evidence. |
| `NTSD28B11ContentSourceEditorTests`, `NTSD28UnityRawCaptureEditor`, `FormalContentClosureResourcePatcher` and legacy production Editor tests | They contain explicit old Config/old BMP paths as fixture or historical-capture inputs. These are not the selected production root, but they still rely on old files for selected test workflows. | Inventory exact test method/file needs, then use independent fixtures or mark old-capture workflows historical before a per-file deletion list. Do not alter DAT tokens to satisfy tests. |
| `GameDataManager.LoadDataFile` | Public method retains default old `Config/data.txt` path. Searched `Assets/NTSD/Scripts` call sites pass an explicit path; the default overload remains a future/reflective caller risk. It returns early when object lookup is already populated. | After callers migrate, remove or reject the old implicit default in a dedicated scoped change; retain explicit formal publication API. A textual caller search does not prove no reflective caller exists. |

Evidence: current source branches in `Assets/NTSD/Scripts/UI/LoadingPrewarmController.cs`, `Test/BattleTestBootstrap.cs`, `Animation/Manager/CharacterAnimtorManager.cs`, `Animation/Editor/CharacterFramePreviewWindow.cs`, `Animation/GameDataManager.cs`, and `Test/BattleRuntimeSelfCheck.cs`; `rg` over `Assets/NTSD/Scripts` for `ParseCharacterFrameConfigs(`, `LoadCharacterSpritesAsync(`, `LoadDataFile(` and literal old Config/Sprite paths. Existing formal-root Editor tests cover selected branches, but this audit ran no new Unity test or Play. The 383 old-image GUID scan in `CURRENT-DYNAMIC-REACHABILITY-20260924.md` covers selected serialized files only and cannot close these path-built readers.

Priority: (1) migrate the SelfCheck and explicit fixture tests that would fail upon old-file removal; (2) make the two battle-entry empty-root behaviors explicit and test them; (3) retire Editor-only optional old-index consumers; (4) rerun formal Menu→Battle and direct Battle, SelfCheck and old-reference scans; (5) produce a concrete per-file deletion list and follow the repository's deletion approval rule. These are separate bounded Tasks/Changes because they have different owners. Q07 stays `IN_PROGRESS`; this matrix authorizes no deletion and does not close natural skills, Player/EXE comparison or D-024.

2026-09-25 follow-up (current-tree static evidence): direct `BattleTestBootstrap.LoadCharacterDataAsync` empty-root fallback has been changed under `NTSD28-Q07-EMPTY-ROOT-BATTLE-ENTRY-001`; independent Roslyn compilation using Unity-generated response files passed for runtime and Editor (0 errors), but original Editor import and its two focused tests remain pending because neither the MCP bridge nor Unity CLI Pipeline connects to the running Editor. The direct-Battle row above is a pre-change snapshot, not current reachability. No old file can yet be retired.

SelfCheck migration has a second dependency beyond the old path: `CheckDeployableResolvedGeometryRisks` expects `negativeHeightPositiveWidthBodyCount == 5` and precisely `x39/y-555/w21/h-999/kind0` raw inverted body entries, and also expects zero-width positive-height ITRs. These are assertions over the old Unity-adapted content baseline, not a proven formal Logan count. A follow-up must first collect the formal catalog's normalized geometry and define its own expected values or classification before changing this check's source. Keep the separate synthetic `CheckZeroDimensionCollisionGeometry` and `CheckNegativeHeightCollisionGeometry` rule checks; their fixture semantics do not require the old production DAT. The explicit `NTSD28Q07LegacyDataLazyLoadEditorTests.SingletonAwake_DefersLegacyIndexUntilExplicitLoad` also still calls `Assets/NTSD/Config/data.txt`, so its legacy-input expectation must be independently replaced or isolated. No Unity test was run in this read-only follow-up.

2026-09-25 deeper SelfCheck source audit: `GameDataManager.LoadDataFile` returns without changing `objectLookup` when objects are already present. `CheckDeployableResolvedGeometryRisks` then calls `ParseCharacterFrameConfigs(dataManager)`, which always checks the old `Assets/NTSD/Config/data.txt`, gets whichever object list the manager currently holds, and resolves each DAT path **relative to the old Config directory**. Therefore a formal publication present in `GameDataManager` does not make this SelfCheck consume formal DAT; it can pair formal object definitions with old DAT paths, omit missing matches and still log a partial `loadedCount`. The formal source's separate `BuildCharacterFrameConfigsFromCatalog` is the appropriate candidate API, but exact behavior and geometry expectations need a scoped test before a code change. This is a proven static source-selection defect in the check, not proof that the current full SelfCheck passes or fails in any live order. The original Editor is running without MCP/Pipeline access, so no new Unity runtime assertion was made.

Formal authored-geometry diagnostic: `FORMAL-RAW-GEOMETRY-INDEX-20260925.json` records SHA-256 of the formal `decoded_dat/data/data.txt` index and existing native parser JSONL, with 330/330 unique indexed DAT paths matched. Across their authored normalized subblocks it counts 86,383 bdy and 19,461 itr entries; positive-width/negative-height bdy = **0**, zero-width/positive-height itr = 27, and all nonpositive itr = 109. These numbers contradict directly carrying over the old SelfCheck's “exactly five inverted bdy and no other nonpositive itr” content expectation, but they are a **source-model diagnostic**, not Unity `LF2FrameCache` resolved geometry or a new battle-rule authority. The next scoped check must compute resolved formal frames with the production Unity converter/cache before revising assertions.

Unity converter path check: `BuildCharacterFrameConfigsFromCatalog` calls `BuildCharacterDataFromSource`, which selects `Lf2DatParserV2.ParseLoganContent` and `Lf2DatConverter.ConvertLoganFrameData`; its bdy/itr branches call `LoganCombatRecordDecoder.ReadGeometry`, which copies parsed `x/y/w/h` integers into the runtime values. `LF2FrameCache.Load` indexes those frame objects and does not rewrite geometry. This makes the source-model zero inverted-bdy result a strong **static** prediction for the Unity formal path, and a raw-text search across staged decoded DAT also found no `bdy:` line with negative `h`. It still does not establish the full runtime resolved count: Unity parser admission, duplicate frame IDs, cache range and `HasGeometry` must be measured in the scoped validation before replacing the old SelfCheck assertions.

2026-09-25 full-SelfCheck dependency correction: a separate current-source audit found three additional `RunAll` checks reading old Config DAT through `LoadProductionDatWrapper`: sprite range, movement headers and Naruto DDJ six-clone chain. Formal index has no old OID205, and formal Naruto frame272 lacks the old asserted OID205/action98 OPoint. The geometry-only Task cannot certify full SelfCheck or old-file retirement; see `SELFCHECK-LEGACY-DAT-CALLER-AUDIT-20260925.md` and new Task `NTSD28-Q07-SELFCHECK-LEGACY-DAT-FIXTURES-001`. No files were retired or gameplay scripts changed.

More precise offline prediction in the same JSON artifact applies current cache range 0..856 and last-frame-wins mapping: 330 objects, 55,348 unique authored frame slots, 86,383 bdy, 19,461 itr. The 109 nonpositive itr split into 27 complete kind-0 zero-width/height-79 lines in OID 419/411/422 and **82 kind-100100 records with missing geometry fields** in OID 854/20/51/54. `LoganCombatRecordDecoder.ReadGeometry` marks missing fields via `HasGeometry`; current SelfCheck counts all itr regardless of that flag. Also `LF2FrameCache.GetFrameDataById` returns an empty frame for absent IDs, so its `resolvedFrameCount` is 857 per loaded definition, not the 55,348 authored slots. Do not simply replace old expected 5 with 0 or old allowed-itr count with 109: first run the formal Unity converter/cache and explicitly decide the intended `HasGeometry` and frame-presence contract without weakening collision-rule fixture checks.
