# Q07 old-image serialized owners: current-tree correction (2026-09-25)

Status: `READ_ONLY_CURRENT_GUID_SCAN / NO_DELETE_AUTHORIZATION`. This supersedes the **current-state** claims in `SERIALIZED-OWNER-CLASSIFICATION.md` dated 2026-09-22; that document and its CSV remain historical snapshots. The exact 52-row before/current comparison is `CURRENT-SERIALIZED-OWNER-REFRESH-20260925.json`.

The scan searched 933 current serialized files under `Assets` and `ProjectSettings` (`.unity`, `.prefab`, `.asset`, `.mat`, `.controller`, `.anim`, `.overrideController`, `.uxml`, `.uss`, `.json`) for the 52 old GUIDs in the prior classification. It found 37 old-path rows with references and 39 file-reference pairs. Fifteen prior rows now have no GUID reference in this scan: the one old Sasuke editor-preview sheet, 13 Battle HUD/UI images, and one Menu UI image. The other 37 rows retained the same referrer sets. This is a bounded serialized GUID scan, not proof of no path-built, reflective or external consumer and not a deletion gate.

The inactive `BattleCentralEditorPreview` in current `NTSD_Battle.unity` already serializes formal `vfs/c/sasu/sasu.png` GUID `b5d608d7fe42c474ea0f2bb728a122a2` with source rect `(0,881,79,79)`; its old `sasuke_0.bmp` GUID `6d174fff55a50784d9bbf85531fb7d86` is absent from the current Scene. The Inspector sample, editor validation and current focused test also name formal `sasu.png`; the test checks imported/raw 799×960 dimensions, top-left rect and mixed visible/transparent pixels. `BattleSpriteGridSeparatorEditorTests` retains a **synthetic** 800×560 legacy-grid fixture but no literal old BMP path. The Battle Scene SHA-256 is `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A` and has no current Git diff. Thus the older report's proposed Scene/Inspector rebind is already present in the current committed tree. No original-Editor test was run in this scan, so current visual acceptance is not newly proven.

The 13 Battle HUD and one Menu UI lost GUID references in the scanned current files; their owner intent, runtime path loading and replacement history were not determined here. Preserve these and all other user/nonbattle assets. The old 138 indexed DAT, 383 indexed images and 174 additional image paths still have `deleteAuthorized=false` in the Q07 retirement inventory because the old Config/loader, Editor and historical test reachability has not been retired. Do not promote a zero serialized reference count to deletion approval.

Current Battle Scene Sprite mapping check: the 13 scene UI `Image` objects with nonzero `m_Sprite` point to existing current project graphics (`HeadImg`→`naruto_s.png`, `HP`→`HP.png`, `MP`→`MP.png`, `HpXu`→`HP-Xu.png`, three Combo images, three action/control images, `HUDBg`, `ControBg`, `ControPoint`). The Scene has 14 nonzero `m_Sprite` GUID fields in total, including a map sprite; all 14 resolve to present `.meta` GUIDs. The Menu Scene has 40 nonzero `m_Sprite` GUID fields, also all resolving to present `.meta` GUIDs. Thus the absent old GUIDs did not leave a **serialized missing Sprite reference** in either current Scene. This is a static asset-binding check only; it does not prove the HUD layout, animation, dynamic loading or visible Play result. The user-owned scene-resident HUD/Combo task remains independently `RUNTIME_PENDING`, and the separate `BattleControlsView` action-binding task remains `IN_PROGRESS`.

No Scene, script, DAT, image, importer, Prefab, project setting or original progress document was changed by this correction.
