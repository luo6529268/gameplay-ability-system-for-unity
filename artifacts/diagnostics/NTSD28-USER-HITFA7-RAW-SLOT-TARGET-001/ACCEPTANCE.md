# Non-character hit_Fa7 raw-slot target and self-check correction

Status: `FOCUSED_TEST_PASS / FULL_SELFCHECK_PASS / FORMAL_EXE_EMPTY_SLOT_UNOBSERVED`, 2026-09-24. This is a non-perceptual battle-path follow-up discovered by the D-024 audit. Formal root EXE SHA-256 was checked again as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The paired `source/README_SOURCE.md` declares the playable source snapshot; `NativeAi28::step_non_character_hit_fa` behavior7 adds the horizontal chase acceleration twice. Its inactive-target branch explicitly states that native keeps an inactive slot pointer but the standalone C++ World releases that storage and returns unresolved target context. Therefore the source model alone cannot supply an empty-slot numeric witness, and no formal EXE runtime probe was obtained here.

Before correction, Unity `LF2Entity.RunNonCharacterHitFa7FrameLogic` returned as soon as an active entity query found no target. The existing full `BattleRuntimeSelfCheck` stopped at FL-05: preassigned empty slot10, subject slot50 at X100, expected raw-slot-driven Vx=-1.4 but observed Vx=0. Unity already has reset raw runtime storage for addressable empty slots and uses it in the neighboring hit_Fa4 path. A new original-project Editor test loads staged official Logan OID875/action55 (`hit_Fa:7`), registers only the subject, verifies slot10 is empty and its raw X is zero, then calls production `RunFrameLogicBeforeAdvance`. Pre-fix exact job `3e887fc7321e4c7aafb40eafde6ea172` failed 0/1 at Vx expected -1.4/actual0. The generic noncharacter writer now uses the raw slot XInt/ZInt only when there is no active query entity and the preassigned slot is addressable; invalid/sentinel slots retain the early return. After Editor recompilation, exact job `7be6d87ef35e47a3b275b9b6f5b5898a` passed 2/2: official OID875 empty-slot case and existing official OID875 active-target vertical/no-clone case. No DAT token or actor-specific branch was changed.

The next full SelfCheck did clear FL-05 and stopped at a separate FL-02/HITFA-7 assertion: a near-full fixture constructed a **type Other** source OID707 and expected a same-OID child in the last free slot. The formal noncharacter behavior7 branch has no direct clone spawn, and the existing official OID875 active-target test already required no extra clone. The test-only correction kept the near-full fixture but asserted source retention, last slot empty, no extra dynamic occupant and unchanged RNG for hitFa7; hitFa5/11/13 publication expectations remain. After recompilation, one full SelfCheck request completed with exact result `PASS` in `Temp/NTSD_BattleRuntimeSelfCheck.result` at local 2026-09-24 03:27:48. This is a self-check pass for the current project state, not proof of formal EXE empty-slot behavior, full battle Driver parity, Play Mode, or D-024 coordinate-history parity.

The two independent package contracts are `NTSD28-USER-HITFA7-RAW-SLOT-TARGET-001` (production and official-content test) and `NTSD28-USER-HITFA7-NONCHAR-SELF-CHECK-CORRECTION-001` (test-only expectation). No Scene, camera, ProjectSettings, Config, DAT, character image or nonbattle file was changed by these packages. Q07 remains open and behind the D-024 OID219/fusion/source-domain carrier work.
