# D-024 kind14 collision coordinate-domain audit

2026-09-24 authority correction: the earlier claim in this file that playable kind-14 compares precise X/Z is wrong. `physics_integrator.h` declares `Position28::x/z` as integer mirrors and `precise_x/z` as separate doubles; `battle_world.cpp:5558-5568` uses `position.x/z`. `NTSD28-USER-KIND14-INTEGER-OPERAND-AUTHORITY-CORRECTION-001` owns the Unity operand/assertion correction. The D-024 *source-rule versus scaled battle* coordinate-history and one-tick flag divergence remains open. All earlier precise-threshold RED/GREEN observations below are historical tests against the wrong expected rule, not a parity certificate.

2026-09-24 correction: the factor1 precise-versus-integer candidate below has now been reproduced and fixed to focused scope. See `KIND14-PRECISE-THRESHOLD-ACCEPTANCE.md` (original Editor RED then 7/7 GREEN, direct fallback C32 1/1). The separate D-024 scaled battle-versus-source history/flag divergence remains open; do not read the older `RUNTIME_WITNESS_PENDING` status below as the current state of the precision defect.

Status: `STATIC_FIRST_DIFFERENCE_CANDIDATE / RUNTIME_WITNESS_PENDING`, 2026-09-24. This is a read-only authority/caller audit. No production, DAT, camera, Scene, or test data changed for this note.

The formal playable source `battle_world.cpp`, `BattleWorld28::resolve_special_relation_hit` kind14 branch around lines 5542-5578, compares **precise** attacker/target X with strict `> target.x+5` / `< target.x-5`, and precise Z with strict `> target.z+2` / `< target.z-2`. It publishes four directional collision flags when motion or pending impulse points toward the attacker. The World physics path copies these flags into the integrator context (around line 1856), and clears the entity flags after consumption (around lines 1962-1964). `physics_integrator.cpp`, `PhysicsIntegrator28::step`, suppresses the corresponding raw-axis translation when a flag is set.

Unity's active `BattleBoundaryWriter.TryApplyKind14DirectionalBlock` evaluates the same strict numeric thresholds on **battle integer** `attacker.Runtime.XInt/ZInt` and `victim.Runtime.XInt/ZInt`, publishes `XBoundPositive/Negative` and `ZBoundPositive/Negative`, then synchronizes the input row. `LF2CharacterDatHitResolver` delegates to it for a registered World and has an integer-domain fallback; `BattleDamageWriter` calls it in ECS hit paths. `CharacterMechanics.StepBattleLogic` consumes these battle flags while applying the D-024 scaled physical X/Z displacement. The Ecs hit execution projection also reads battle integer X/Z at its kind14 branch (around lines 2756-2775).

This yields two independent questions that a focused same-input witness must resolve before implementation:

1. **Existing rounding candidate:** even at view factor 1, a precise separation near 5/2 can cross a strict threshold while the truncated integer mirrors do not. The source uses precise positions; Unity uses integer mirrors. This is a static semantic difference, not yet a measured reachable hit outcome in current official content.
2. **D-024 history divergence:** after physical motion is scaled, battle coordinates can cross kind14 distance/geometry thresholds earlier than formal raw coordinates. The stage-edge and fusion witnesses already prove two-domain positional divergence, but do not prove an actual kind14 candidate. A source-rule reference advanced using the already-published *battle* block flags would be wrong if the formal flag differs. Its counterfactual flags need their own source-domain producer, lifetime, physics consumption, and snapshot/reset/checksum contract, or a demonstrated equivalent that preserves the formal sequence.

The staged indexed Logan content contains actual kind14 records, for example `Assets/NTSD/Content/LoganRuntime/decoded_dat/w/1.dat` lines 82-121 (`vrest: 1`) and `s/3/3.dat` lines 29-30. This establishes content presence, not a live candidate or official EXE hit.

Next gate: establish candidate geometry/reachability for one of these official records; capture precise/int attacker and victim positions, pending impulse, candidate acceptance, flag publication, next physics displacement, and flag clear in paired playable and original-Editor factor1/configured-view cases. Also trace AI consumers of these flags before choosing whether reference flags are separate state. Do not replace a formal threshold with a scaled constant or derive raw history by dividing the current battle position. Keep Q07 paused until the non-perceptual D-024 audit and its confirmed defects are resolved.
