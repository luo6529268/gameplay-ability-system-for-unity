2026-09-24 attachment classification (read-only): canonical Unity BattleHeldObjectWriter SyncHeldFrameAndPosition and BattleCpointWriter SyncHeldPosition/ApplyThrow compute an absolute held/caught position from the holder X plus frame center and wpoint/cpoint local X offsets; paired playable BattleWorld28 does the same for held weapons (battle_world.cpp near 7996) and cpoint throw (near 5970). These DAT offsets align sprite attachment points, so they are local pose geometry, not free-travel pixel deltas. When holder moves by scaled D-024 travel, recalculating held position carries that same holder displacement; blindly scaling local center/wpoint/cpoint offsets would break sprite contact. A focused holder-move and render-anchor test is still required; this read-only classification does not prove complete grab/throw behavior. LF2Entity.ApplyCpointThrowStep10 has no production caller found by current rg (only a test), so BattleCpointWriter is the active throw owner. Stage absolute coordinates and Y/floor remain open.

2026-09-24 source-derived velocity follow-up: formal indexed OID219 w/e.dat frame51 hit_Fa5 computes integer (targetX-childX)/50 before double conversion; Unity used floating /50.0. Original Editor four actual child births RED at 3.02 versus formal 3 for gap151. Only that producer arithmetic changed; formal/staged DAT SHA matched. Source-DAT-loaded actual World birth plus next production mechanics step focused class 8/8 GREEN. Equal-start configured screen fraction abs(3*Sx)/2048=3/1333, so no producer inverse scaling. Dynamic targets, full Driver/Play and formal EXE view are unverified; hit_Fa6 is not indexed in current formal DAT. See NTSD28-USER-HITFA5-TARGET-VELOCITY-001.

# D-024 non-integrator movement audit

Status: `CONFIRMED_BYPASS_CANDIDATES / SEMANTIC_CLOSURE_PENDING`, 2026-09-23. Read-only code-path audit against the formal playable source and current Unity production code. It is not an exhaustive runtime trace or a formal EXE visible certificate. The user asked to check non-perceptual defects, especially paths that still preserve raw displacement pixels while the Unity fixed full-background view is larger.

2026-09-23 scoped corrections since the initial read-only audit: native frame dx/dz direct writes, linked-platform passenger X/Z carry, state400/401 target-relative teleport X, and built-in/DAT weapon-fragment relative X/Z birth all had original Editor configured-view RED cases and now have focused GREEN plus adjacent default-source regressions. See their four Change Records and the parent Task. The following original paragraphs are the pre-fix diagnosis and are retained as historical evidence, not current code status. These corrections do not close Y/floor, grab/hold, state18 transition particles, teammate-average respawn placement, stage/boundary coordinates, or the full writer census.

2026-09-24 later scoped corrections: state18/19 particle precise-X random offset, ordinary revival random X/Z around teammate average, independent weapon identity X extra and specialized type3 weapon frame hit_j precise-Z all demonstrated configured-view RED and now pass narrow original-Editor GREEN cases. Their relevant source/integer/RNG controls also passed; see four new Change Records. The last sentence of the preceding snapshot about state18 and revival being open is historical. Open production classes remain `LF2Entity` HitFa11 relative effect birth, grab/cpoint/WPoint attachments, stage-absolute random/clone spawn, stage bounds and Y/floor/collision. `rg` over direct X/Z position/task assignments returned 108 lexical lines in LF2Objects/Ecs Writers/Passes; this is a candidate list, not an exhaustive reachable writer closure. Do not apply a uniform factor to attachment offsets or stage-absolute positions before their coordinate contract is established.

Latest read-only authority classification: Unity `LF2Entity.RunHitFa11FrameLogic` has a 14-entry hardcoded relative child-birth catalog (nonzero X/Z offsets), but paired playable `NativeAi28::step_non_character_hit_fa` explicitly routes behavior 11 to `unsupported_special_behavior` rather than a recovered spawn transaction. The current playable source therefore does not establish the formal offsets or child-birth order needed for an authorized D-024 rewrite. Mark `AUTHORITY_GAP`, not a confirmed fix candidate; obtain formal EXE observable trace or authoritative source recovery before modifying. Unity grab/CPoint/WPoint offsets are body/sprite attachment placements, not yet classified as travel; preserving image and hitbox registration can require raw local offsets. Stage-absolute random/clone spawns and stage bounds operate in stage coordinate domain, not parent-relative motion. Whole-entity Y/floor must be settled with landing and collision reference together. A separate source-derived-velocity audit is required wherever velocity is computed from already-scaled current World positions, to avoid a second view factor at final integration.

The shared character/non-character X/Z physics integration now scales final position deltas while retaining raw velocity and friction. This does **not** cover every position writer. The late OPoint parent-relative X/Z birth offset was a measured example: both actual materializers failed four configured-view RED cases and now pass the 16-case kind1/kind2, direction, owner and scale matrix under `NTSD28-USER-OPOINT-BIRTH-RATIO-001`. This is a focused exit only; collision and Play remain open.

Next strongest uncovered route is **native frame dx/dz direct displacement**. Production `NTSDBattleTickSystem` calls `SimulationWorld.NativeFrameMotionAll`, which calls `LF2Entity.ApplyNativeFrameMotionForWorldPass` and `ApplyNativeFrameMotionTail`; the latter writes `Runtime.X/Y/Z` from integer position plus decoded frame displacement. Its existing `scale` is DelayTimer134's 0.25/1, not the World view factor. Nonzero direct displacement clears corresponding velocity, so the shared physics integrator cannot later supply the omitted factor. The paired formal `battle_world.cpp` applies raw frame displacement in the playable tick order. Formal content has a reachable example: OID736 frame120 `dz:-2`, `dvz:550` in `a/wal/wal.dat`, indexed in `data/data.txt`, and OPoints in `c/jiro/a/cav.dat` and `c/hir/a/atk.dat` spawn OID736/action120. With a configured 2048×1152 World at ZInt250 and no Delay134 or platform, the current Unity direct Z target is 248; D-024's same screen-fraction target is `250 - 2×1152/730 = 246.8438356…`. This is a scoped static first-difference prediction that still needs a focused original-Editor test before a production fix. Keep Y out of that first package until vertical height/collision semantics are settled.

Other production bypasses need independent contracts rather than a global multiply: `LF2Entity.ApplyLinkedPlatformMotion` adds decoded platform DV to passenger X/Z/Y; `LF2Entity.RunNativeTeleportState` places beside an already-world-positioned target using raw 120/60 X offset and Z+1; `BattleNativeWeaponPieceWriter.Materialize` adds random fragment offsets to an already-positioned source weapon before an absolute direct spawn task. A test must distinguish parent-relative offset from absolute destination, preserve random draw order and avoid double scaling.

Whole-entity Y height integration in `CharacterMechanics` still adds raw Vy. Its screen projection interacts with ground Z, floor, platform, collision and landing, so applying the 1152/730 factor without that contract would be unsafe. Held-object WPoint/CPoint anchors, kind8 Z+1, random stage-absolute drops and stage-wave spawn coordinates, stage boundaries/clearing and interaction repositioning also use raw or copied coordinates; some are attachment or stage-domain values rather than travel and are **candidates for classification**, not automatically defects. The fixed camera remains the user's approved exception; DAT values must remain unchanged.

Priority after the OPoint slice: prove frame dx/dz via the existing frame-motion-tail test with one configured World and default-scale control, then implement a one-time X/Z direct-displacement transform if RED; inspect platform and teleport relative offsets next; handle weapon-piece random offsets with its own spawn test; settle Y and stage/boundary/geometry together. Continue the producer census and actual-call reachability review before declaring D-024 complete. Q07 must not be resumed on a claim that no other similar issues exist, because several reachable routes remain open.
