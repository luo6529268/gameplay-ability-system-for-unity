# D-024 X/Z writer and consumer inventory (2026-09-24)

Status: `PARTIAL_INVENTORY / TWO_CONFIRMED_FIRST_DIFFERENCES_OPEN`. This is a reachable-path inventory, not a declaration that all writers or all battle effects have been covered. Formal rule references are to the shipped Logan playable source path; the existing fixed full-background Unity camera and unmodified DAT remain the user-approved constraints. The subsequent fusion original-Editor witness is in `FUSION-VIEW-DISTANCE-FIRST-DIFFERENCE.md`; the fusion risk below is now a confirmed scoped first difference, not merely a prediction.

## Confirmed non-perceptual difference

Indexed OID219 `w/e.dat` frame51 `hit_Fa5` derives child Vx from an integer target-child X gap divided by 50. The original Unity Editor's registered-World tests proved that two histories can have the same current Unity integer gap 224 but need different source-rule results: raw-absolute spawn gap224 requires Vx4, while spawn gap151 followed by one raw48px target move requires Vx3. Unity currently returns 4 in both cases. Focused job `d248af5583144fa4822db084fa43cee3` was 11/11 PASS because the moved case is a defect-characterization assertion, not a parity assertion. The earlier floating-division first difference at an unmoved gap151 is corrected separately. See `TARGET-HISTORY-VELOCITY-FIRST-DIFFERENCE.md` and `COORDINATE-HISTORY-CONTRACT-DESIGN.md`.

## Reachable position writer classes

| Class / Unity entry | Semantics and formal pairing | Current disposition |
| --- | --- | --- |
| `AppManager` initial participant, `SimulationStageWaveModule` `SetPos`, `BattleRandomWeaponDropModule` initialRuntimeX, `BattleNativePostDisplayResourceWriter` stage reset | Absolute stage/world coordinates | Keep raw at birth; dual source-rule reference should start equal to raw placement. Stage bounds still require a separate contract. |
| `BattleLogicEntityFactory` task materialization and `LF2Entity.ApplyInitialRuntimePosition`; `PhysicsState.x`/`BindRuntime` alias | Final birth placement; task can be absolute or prepared parent-relative | Must inherit the producer's semantic category. A direct `Runtime.X` text search misses the `PS.x` and `SetPos` aliases. |
| `CharacterMechanics.StepBattleLogic`, `LF2Entity` frame `dx/dz`, identity extra X and platform carry, `LF2Weapon` identity X, specialized type3 Z | Physical travel from raw velocity/frame deltas | D-024 already scales final battle X/Z travel in bounded packages. A second raw source-rule position must advance by the formal unscaled deltas with matching integer sync. Existing focused GREEN is limited to those exits. |
| `BattleLogicObjectPointRuntime` and `LF2ObjectPointFactory` late OPoint, `BattleNativeWeaponPieceWriter`, `BattleNativeState18ParticleWriter` | Parent-relative child birth or precise position shift | Bounded D-024 X/Z exits have focused tests. The reference child must be born from the parent's reference position plus raw offset, preserving the separate integer/precise rules. |
| `LF2Entity` state400/401 teleport, `BattleRespawnModule` peer-average-plus-random, `BattleLateEntityLifecycleModule.SpawnState9996Children` | Target/peer/parent-relative placement; state9996 formally uses source integer X+random X and Z+1 | Teleport/random revival and now state9996 child relative X/Z final birth offsets have focused D-024 exits. State9996 original Editor configured RED then all-five-child GREEN under `NTSD28-USER-STATE9996-CHILD-RATIO-001`; exact formal row0 immediate/following factor1 tests passed. Raw source-rule reference position/history for the parent and child remains unimplemented. |
| `BattleInteractionWriter.AlignGrabPair`, `BattleCpointWriter`, `BattleHeldObjectWriter`, `LF2CharacterWeaponLinkResolver`, `LF2WeaponBase` | Grab reposition and held pose from frame-local CPoint/WPoint anchors | `TryApplyGrab` is called by character, weapon and special attack interaction resolvers. Paired `battle_world.cpp` near 5220 writes blended precise X to both attacker and target. Held pose local anchors should not be blindly enlarged; grab pair reposition remains untested under configured view. |
| `BattleKind8ControlRelationWriter.TryApply` from `BattleHitCandidateSequenceRunner` | Target X copy and target Z + 1 | Active relation path. Reference position should copy target reference X/Z plus the raw unit Z offset if the paired source confirms that exact relation. No configured-view witness yet. |
| `BattleOid5152RuntimeModule.TryMerge/TrySplit`, called from `SimulationPassPipeline`/`SimulationWorld` | Fusion proximity, midpoint and partner position copy | Paired `battle_world.cpp` near 2741 and 2780 checks raw integer distance X<50/Z<8 and writes raw integer midpoint; split copies primary position. Original Editor job `42504562540d48f29a0f42433c24eced` confirmed factor1 gap40 merges and configured gap50 rejects after the same raw20px move. Midpoint/split reference-coordinate effects remain untested. |
| `LF2Entity.ApplyPreFrameXBounds`, `BattleEcsCharacterPreFrameBoundsPass` | Stage clamp/correction | Stage-absolute width/edge, not free travel. Must coordinate with changed physical position and formal stage scale before changing thresholds. |
| `LF2CharacterDatHitResolver`, `LF2CharacterHitResolver` attacker-to-victim X copies | Hit/relation snap | Caller selection and paired source branch still need exact classification. |
| `SimulationAiDecisionModule`, `SimulationAiSensingModule`, `AiDecisionKernel`, `AiSensingKernel` | AI range and prediction consumers of current X/Z | Multiple raw threshold constants (`50`, `80`, `150`, etc.) are reachable candidates. Do not globally multiply thresholds: some calculations mix raw velocity and positions, and optimized/legacy AI paths must agree. Formal producer, row publication and first-difference cases are required. |
| `SimulationAiInputModule` `ForSelfCheck`, `BattleRenderingBenchmark` and older `LF2Entity` cpoint helper methods | Diagnostic/benchmark or apparently dead legacy writers | Exclude from normal path only after exact caller proof; they remain in the lexical list, not in the confirmed production count. |

`PhysicsState.x` forwards to bound `NTSDEntityRuntime.X`, so `PS.x` is an actual position writer. Search must also include `SetPosition`, `SetPos`, direct spawn task coordinates, integer mirrors, registration and fast paths. The table above does not close those alias and branch searches.

The completed focused witness uses the existing valid formal fusion row 0 (primary X320 to340, partner X300) rather than the initially proposed synthetic X100/120 illustration. With raw20px movement, the formal gap40 merges; the configured Unity battle gap50 rejects. See the first-difference report for exact test scope.

## Next gate

1. Capture representative Battle Scene Play for the focused state9996 child-birth correction. Fusion and OID219 history first differences still await the shared coordinate correction; do not treat state9996's local outlet as general history closure.
2. Complete the remaining alias/caller audit and paired playable field-write order. Include grab-pair snap, kind8 relation, AI row consumers, bounds, Y/floor and collision side effects. Record untested categories explicitly.
3. Before adding a runtime history carrier, create a bounded Task/Change and account for entity birth, precise/integer sync, reset, snapshot/restore, checksum, replay, pool reuse and every reachable writer. Do not repair OID219 by a one-off inverse multiplier: the equal-current-gap counterexample rules that out.
4. Re-run only affected focused tests, then representative original Battle Scene Play and matched formal source/EXE view-fraction checks. Q07 remains paused while this confirmed D-024 difference is open.
