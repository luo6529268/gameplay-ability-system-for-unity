# D-024 position-history carrier writer gate

2026-09-24 correction: the earlier addendum's formal kind-14 **precise** threshold claim is superseded. The live playable branch compares integer `Position28::x/z` mirrors with strict 5/2; the need for independent source-domain directional flags after D-024 scaled physical travel remains. See `NTSD28-USER-KIND14-INTEGER-OPERAND-AUTHORITY-CORRECTION-001`.

> 2026-09-24 addendum: precise-versus-integer kind14 collision flag publication is another source-rule history dependency. Formal kind14 compares precise X/Z and physics consumes the resulting flags once; Unity's active BoundaryWriter and ECS hit projection compare battle integer X/Z. A source-reference carrier cannot advance on battle-derived block flags if the two coordinate domains diverge. This is a static first-difference candidate pending an official-content runtime witness; see `KIND14-COORDINATE-DOMAIN-AUDIT.md`. Include flag producer/consumer, candidate geometry and AI readers in the carrier implementation gate.

Status: `SOURCE_AND_UNITY_CALLER_AUDIT_IN_PROGRESS / NO_CARRIER_CODE`, 2026-09-24. The goal is one reusable source-rule X/Z coordinate history alongside the user-approved scaled battle coordinate. This table is a pre-implementation ownership gate, not a completed full-writer census or a permission to alter source-rule constants, DAT or the camera.

2026-09-24 implementation-state correction: source-rule carrier state/snapshot/64-bit and JSON checksum packages are now focused-test PASS, but gameplay carrier is `CARRIER_NOT_ACTIVE`; the historical status line above describes the original read-only gate. The bounded late-OPoint/stage birth foundation is PLANNED. A direct participant raw-absolute birth path also exists at `Assets/NTSD/Scripts/App/AppManager.cs` near its `lf2.PS.x=spawnX`, `PS.z=spawnZ`, `Runtime.SyncIntegerPosition()` sequence; it is outside the first birth package and must explicitly set/sync source-rule X/Z before any source-rule reader activates. Do not infer that stage-spawn coverage initializes menu-created participants. The random-weapon normal and mode2 raw-absolute task paths, relative state18/state9996/fragments, hit_Fa/N30/respawn and special-effect tasks remain separate birth gates.

2026-09-24 later status correction: late OPoint/stage foundation, participant, kind1 random, state18/19, state9996 and weapon fragments are now focused-test PASS; the first has a real pooled Renderer Battle Scene Play witness, participant still lacks Menu→Battle Play. Continuation OID998/action6 source birth is `CODE_WRITTEN / COMPILE_PENDING` under its own Task/Change. Ordinary revival averaging, other births, motion and source-domain boundary flags remain open. Normal random-drop nonempty candidate creation is outside the currently locked native playable closure (`game_session.cpp` reset gate and `simulation_tick_driver.cpp` empty-table stop), so its Unity code cannot be promoted as current formal authority merely by existing.

## State and synchronization owners

| Owner | Verified current behavior | Required history behavior |
| --- | --- | --- |
| `NTSDEntityRuntime.X/Z`, `XInt/ZInt` | Current battle precise/int fields. `SyncIntegerPosition()` truncates current precise X/Z to int. | A source-rule precise/int pair must sync independently at the same call boundary; deriving it from battle coordinates after the fact loses mixed birth/movement history. Manual integer writes (frame motion, teleport, spawn) require their own matching source-rule write. |
| `PhysicsState.x/z` and `BindRuntime` | Bound properties forward to `NTSDEntityRuntime.X/Z`; bind copies pre-bind values into runtime. These are actual write aliases. | Bind/absolute initialization must initialize both coordinate domains. Post-bind `PS.x/z` writes must be classified by their caller; adding only `Runtime.X=` hooks misses them. |
| `LF2Entity.ApplyInitialRuntimePosition`, `BattleLogicEntityFactory`, component `LF2ObjectPointFactory` | Spawn task carries precise direct X/Y/Z and optional integer mirrors. A producer may already have applied D-024 scaling to a relative offset. | Task needs explicit raw source-rule birth coordinates (or the producer must atomically initialize them) so the materializer does not attempt to infer raw position by dividing the final precise battle X/Z. Default/absolute task can copy raw absolute values into both domains. |
| `NTSDEntityRuntime.TryCopyCanonicalStateTo` / `Reset`; `BattleWorldEntityRuntimeSnapshot`; `BattleLockstepChecksumModule.AppendEntityRuntime` | Copies/reset/hashes battle position only. Snapshot restore uses canonical copy for entity/raw runtimes. | New carrier must be copied both ways, reset on pool reuse, included in checksum with an explicit version/trace contract and tested across snapshot/restore/replay. An unsnapshotted cache is invalid. |

## Active position transactions and their source-rule update

| Transaction and Unity caller | Source-rule operation required | Evidence / unresolved branch |
| --- | --- | --- |
| `AppManager` participant spawn, `SimulationStageWaveModule.SetPos`, random weapon drop and `BattleNativePostDisplayResourceWriter` stage reset | Raw absolute placement: reference equals raw stage X/Z at birth/reset. | Existing stage width and bounds remain in raw coordinates; do not scale entire absolute X/Z. |
| `CharacterMechanics.StepBattleLogic`, `WeaponDynamics`, `StepNonCharacterBattleLogic` | Add raw Vx/Vz to reference precise coordinate only if the axis is not blocked; battle precise adds `V*viewScale`. Friction follows movement in both paths. | Three active physics methods, with `LF2Entity`/ECS callers and later independent `SyncIntegerPosition`. The current `runtime.X += ...` lexical search must include lower-case `runtime` and all fast-path/SoA writers. |
| `LF2Entity.ApplyNativeFrameMotionTail`, `ApplyLinkedPlatformMotion`, weapon/entity identity extras and type3 precise-Z | Apply formal raw frame/carry/identity delta to reference at the same write order, including integer rounding policy; battle continues existing scaled outlet. | D-024 battle outlets have focused GREEN but no source-rule carrier. Frame direct writes start from current integer mirrors, not always previous precise coordinates. |
| Late OPoint World/component materializers, weapon fragments, state18 particles, state9996 clone children | New child reference birth = parent reference integer/precise coordinate (whichever formal producer reads) + original raw offset; battle birth = parent battle anchor + scaled relative offset. | Existing D-024 relative-birth fixes cover battle coordinate only. State9996 five-child X/Z focused RED→GREEN; source child uses `source_position.x + random_x`, Z+1 in paired playable `battle_world.cpp`. Random sequence must remain unchanged. |
| State400/401 teleport and kind8 target-follow relation | Reference target raw position plus original raw relative offset; battle target current position plus approved scaled relative offset, preserving target selection and integer convention. | Teleport battle offset already focused GREEN. Kind8 active through `BattleHitCandidateSequenceRunner`; precise X target copy/Z+1 and its integer sync need paired source verification. |
| `BattleRespawnModule` peer average and random offset | Reference average of **peers' reference integer positions** plus raw synchronized random; battle average of battle integer positions plus scaled random. | Existing D-024 fix scaled only random around current battle peer average. Copying the battle average into the reference would make the OID219 history defect recur after revival. |
| `BattleInteractionWriter.AlignGrabPair` kind1/kind3 | Recompute source-rule attacker/victim anchor and half-gap using reference integer positions and unscaled DAT-local CPoint geometry; apply the corresponding battle pose so sprites remain registered. | Active `TryApplyGrab` callers include character/weapon/special attack resolvers; paired playable `battle_world.cpp` near 5220 writes blended precise positions to both entities. It is not free-travel distance, so raw DAT anchors cannot be blanket-scaled. Requires focused catch pose/collision test. |
| `BattleCpointWriter` held/throw and `BattleHeldObjectWriter` WPoint pose | Source-rule held position from holder reference and raw local anchors; battle pose from holder battle position with raw local art/contact anchors pending renderer test. | Active writer paths. `LF2Entity.ApplyCpointThrowStep10` and `SyncCpointHeldPositionStep10` have no production caller found in current repository-wide search; keep as legacy candidates, not current ownership. |
| `BattleOid5152RuntimeModule.TryMerge/TrySplit` | Fusion eligibility reads source-rule integer X/Z. On merge, source-rule primary position becomes raw integer midpoint; on split, partner copies the primary source-rule position. Battle midpoint/split placement needs an explicit visual/physical contract, not blind scaling. | Original Editor production scan proved formal gap40 merges but scaled battle gap50 rejects. Directly scaling threshold 50 would not solve OID219's equal-current-gap history counterexample. |
| `BattleEcsCharacterPreFrameBoundsPass` and `LF2Entity.ApplyPreFrameXBounds`/Z bounds | Apply the formal raw stage edge/clamp to source-rule reference and define how the approved larger battle travel is represented near the fixed background edge. | Paired playable `BattleWorld28::settle_ordinary_stage_bounds` uses `std::clamp(precise_x,left_boundary,bounds.width)` with unchanged absolute width; `simulation_tick_driver.cpp` calls it after the earlier physics/fusion phases. Unity's stage render module routes through the ECS character preframe bounds pass (default) or `LF2Entity` compatibility path. Neither width nor edge is a free-travel offset. |
| `LF2CharacterDatHitResolver` attacker position copy | Reference copies victim source-rule X/Z plus the paired raw Z offset. | Active calls through `LF2Character.TryResolveHit`, `LF2Weapon` and `BattleDamageWriter`. `LF2CharacterHitResolver` is instantiated in `LF2Character` but its `_hitResolver.ResolveHit` has no caller in the current repo-wide search; classify separately if call graph changes. |

### Grab and held-pose source formula resolved to field writes

The paired playable `BattleWorld28::apply_native_relation_hit` (`battle_world.cpp` around lines 5212-5254) calculates target X from attacker's **integer** X and unscaled frame `centerx`/inline `cpoint.x`, blends half the target-to-anchor gap into both precise X values, then truncates both X mirrors. The active Unity `BattleInteractionWriter.AlignGrabPair` (around lines 257-283) follows the same shape using battle X integers. A source-rule carrier must run the same formula over both actors' reference integers; scaling the DAT-local `centerx` or `cpoint.x` is not justified by the approved free-travel ratio exception. The battle pose still needs a focused sprite/contact test because the real holder/victim world gap changed under D-024.

The paired playable `BattleWorld28::settle_catch_relations` (`battle_world.cpp` around lines 6168-6201) first builds a catcher anchor from integer position plus raw local frame geometry, then builds caught X/Z from that anchor and the caught frame/cpoint. `BattleCpointWriter.SyncHeldPosition` (around lines 393-437) writes the analogous active Unity pose and immediately synchronizes precise-to-int. The source-rule carrier must use the catcher's reference integer anchor and raw local geometry at that same write boundary. The formal throw branch near lines 5976-5985 likewise writes integer catch-relative X and mirrors it to precise X; Unity `BattleCpointWriter.ApplyThrow` near lines 459-474 is a separate writer transaction. The paired WPoint follower is `BattleWorld28::settle_held_refill_objects` (`battle_world.cpp` around lines 7945-8019): it derives the child's integer X/Z from the parent's integer position and both frame-local WPoints. Unity `BattleHeldObjectWriter.SyncHeldFrameAndPosition` (around lines 132-168) is the active non-weapon-component pose writer; `BattleHeldObjectWriter.RunStep12` delegates actual `LF2WeaponBase` objects through `LF2WeaponHeldStateResolver.ApplyHeldWPointSync` and `LF2WeaponBase.CoincideXYWithWPoint`, which is a second active X/Z writer. The reference child must use the reference parent plus the same raw local anchors, while the battle child follows the battle parent; retain current sprite-local anchors until a focused pose/contact witness shows otherwise. These are pose transactions, not generic scaled velocity integration.

The original-project Editor has now verified the kind-3 grab writer's two-view formula with a focused registered-World test: job `6fe9428e5d1f45ea9bd5e4049b3eb22e`, 2/2. Factor1 and configured post-grab precise X pairs are respectively 148/140 and 160.5/152.5; both keep the raw local pose gap -8. See `GRAB-POSE-TWO-DOMAIN-WITNESS.md`. This closes the grab-formula witness gate, not source-rule carrier implementation or sprite/contact acceptance. Both WPoint pose branches and the CPoint held/throw position writers have later two-view witnesses below.

The reachable `BattleNativeOpointBirthWriter.InitializeBirth` kind-1 randomizer is an additional post-materialization writer: formal playable adds random raw integer X/Y/Z/action in order, then mirrors precise XYZ. Official Yagura418 has nonzero `centerx` and `centerz`. Before D-024 correction, Unity added the same raw X/Z offset to the configured battle child, yielding a measured raw X delta147 where the full-view fraction required225.848462. Under `NTSD28-USER-OPOINT-KIND1-RANDOM-BIRTH-RATIO-001`, the shared battle X/Z outlet now scales that already-drawn offset once; factor1 and native draw order remain unchanged, with focused X/Z and default-source World/component materializer passes. The future source-rule reference must independently add the unscaled random integer result in the same transaction; the current battle precise/int result is insufficient. See `KIND1-OPOINT-RANDOM-BIRTH-RATIO-ACCEPTANCE.md`.

## Exclusions and open consumers

`LF2CharacterInteractionResolver.HandlePreInteractionKind` contains `PS.x` grab writes but is private and has no caller found; current `TryApplyPreInteraction` dispatches kind1/3 directly to `BattleInteractionWriter`. `LF2WeaponBase.CoincideXYForInit` and `LF2SpecialAttack.CoincideXYForInit` have no callers found. `BattleRenderingBenchmark` writes diagnostic positions; `SimulationAiInputModule` `ForSelfCheck` writes are diagnostic-only. These are lexical findings, not licenses to delete old code.

AI input/sensing/decision readers (`SimulationAiDecisionModule`, `SimulationAiSensingModule`, SoA kernels) use current battle X/Z against raw constants and mixed raw velocities. Collision candidate geometry, stage boundaries and object-target selection may also read battle positions. Each needs formal-source producer mapping and focused first-difference evidence before choosing battle versus reference reads; globally scaling thresholds is not a valid history fix.

The stage-edge counterexample now has original Editor evidence: focused job `57c29991fd964a428e6e856c41e1c209` (2/2) ran production character mechanics and default ECS preframe bounds with absolute width2048, initial X2000 and raw Vx40. Factor1 finished X2040; at `Sx=2048/1333` battle position first became about2061.46 then clamped to2048. The paired playable raw source position2040 remains inside its `bounds.width` clamp. See `STAGE-EDGE-TWO-DOMAIN-WITNESS.md`. This does not authorize enlarging the background or changing stage width; the reference needs its own formally ordered bound result while battle contact remains under the approved physical-distance exception.

## Required gate before production carrier

### 2026-09-24 alias and materialization follow-up

The additional read-only caller audit closes several *inventory* questions, not the coordinate-history implementation:

| Production path | Exact ownership finding | Required carrier acceptance |
| --- | --- | --- |
| `NTSDEntityRuntime.SyncIntegerPosition` | It returns early when the three **battle** integer mirrors are unchanged. `TryCopyCanonicalStateTo` copies precise and integer fields separately; `Reset` clears them separately. | Synchronize reference integers even when battle integers do not change. Copy and reset both reference forms without reconstructing one from the other. Test this early-return case. |
| `PhysicsState.BindRuntime`, bound `PS.x/z`, and `Reset` | Bound setters alias battle runtime X/Z. A bind can read values from an already bound runtime, so it is not necessarily first-time raw initialization. | Distinguish first bind, same-runtime rebind, different-runtime bind, and pool reset. Preserve existing reference history on rebind; do not infer it by dividing battle position. |
| `SimulationStageWaveModule.SetPos` and `BattleNativePostDisplayResourceWriter.SetPosition` | These are reachable raw-absolute placement/reset callers. `SetPosition` writes precise coordinates only; their callers explicitly synchronize integer mirrors. The AI `SetPosition` hit is in a self-check method. | Initialize both domains at the absolute write and preserve the subsequent integer synchronization boundary. |
| `OPointCreateTask` and `LF2Entity.ApplyInitialRuntimePosition` | Direct precise XYZ and optional initial integer XYZ are separate task fields. The materializer returns early after explicit initial integers, without calling `SyncIntegerPosition`. World and component factories share `PrepareFinalRuntimePosition`, which supplies the default Z+1 once. | Carry explicit reference precise and optional reference integers in the task; initialize them before the early return. Preserve the formal parent-integer versus parent-precise choice at each producer. Clear the added task state on pool reuse. |
| Late OPoint and kind-1 post-birth | `BattleLogicObjectPointRuntime.ConfigureLateOpointPosition` and `LF2ObjectPointFactory.ConfigureLateOpointPosition` are separate relative-birth producers. The shared `BattleNativeOpointBirthWriter` then applies the same synchronized raw random draw to both materialization routes. | Use parent reference integer plus raw local offset for reference birth. Reuse the already drawn random delta to advance reference once; do not draw again or inverse-scale the battle result. |
| Multiple OPoint compatibility | `BattleLogicObjectPointRuntime.CopyMultipleTaskToSingle` and two `LF2ObjectPointFactory` branches manually copy only `pos/z`. A non-test producer exists in `LF2ObjectPointModule`, but its `ProcessFrame` has no confirmed production caller. | Retain this as an explicit reachability gate. If active, propagate reference fields through multiple-to-single copies; do not silently assume that the late OPoint path uses it. |
| Distinct stage Z pass | `NTSDBattleTickSystem.ClampCharacterZToStageBounds` routes to the default `BattleEcsCharacterStageZPass.ExecuteDataOriented`, which writes Z/ZInt independently of the later `PreFrameBounds` pass. The paired playable `BattleWorld28::settle_ordinary_stage_bounds` begins with `clamp_type0_stage_depth`, which clamps precise Z and refreshes its integer mirror for each active entity. | Give reference Z its own formally ordered stage-depth clamp. Do not let the earlier physical Z clamp overwrite raw reference history. Trace the Unity non-character depth route separately before claiming all-object parity. |
| Relation and attachment mirrors | `BattleKind8ControlRelationWriter` writes precise target-relative X/Z without an immediate integer sync; `LF2CharacterDatHitResolver` writes precise and integer copies together. Active `BattleHeldObjectWriter` remains the confirmed WPoint pose route; other held-state resolver entrypoints need reachability checks. | Preserve each writer's actual mirror timing, and test CPoint/WPoint pose before scaling any DAT-local anchor. |

The paired formal EXE SHA-256 was rechecked as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The `source/README_SOURCE.md` declares the adjacent playable source snapshot. The alias/materialization inventory above was static; a subsequent separate test-only package added the StageZ witness below. Neither package changed production, DAT, Scene, camera, or nonbattle files.

The independent StageZ boundary now has original-project Editor evidence: final strengthened job `ddce615b36c54e6b902682f627431db7` ran two registered-character production mechanics and default StageZ cases, 2/2, with independently specified view factor and pre-bound numeric Z. Absolute far Z350, start Z300 and raw Vz40 leave the factor1 entity at340, while the configured `1152/730` physical position reaches363.123... and is clamped to350. The future source-rule reference must retain raw Z340 and apply its own formally ordered Z clamp; it cannot copy the battle clamp. See `STAGE-DEPTH-TWO-DOMAIN-WITNESS.md`. This does not cover non-character Z or a formal EXE tick capture.

Further read-only pass-order audit found a separate **non-character timing candidate**. The paired playable `SimulationTickDriver28::step` invokes `BattleWorld28::clamp_type0_stage_depth` before geometry and again after hits; despite its name, `battle_world.cpp` iterates all active entities and gives non-type0 Z bounds `[z_near-1,z_far+1]`. Unity `NTSDBattleTickSystem` also invokes `ClampCharacterZToStageBounds` in those two positions, but default `BattleEcsCharacterStageZPass.IsEligible` filters to `IsStageBoundedCharacter()` (type0). Non-character Z is eventually clamped at `PreFrameBounds` through `BattleEcsCharacterPreFrameBoundsPass` fallback → `LF2Entity.ApplyPreFrameZBounds`, using the formal ±1 margins. Thus final bound range has a route, while the two earlier read points can observe an unclamped non-character Z. This is a source/Unity ordering mismatch candidate, not yet a measured same-input behavior difference; a focused registered-World/Driver witness must decide whether it affects held/collision results before a production edit.

The preceding paragraph is the pre-fix static diagnosis. Under `NTSD28-USER-NONCHAR-EARLY-STAGE-Z-001`, an original Editor registered-World test confirmed the first-pass RED (type1 Z500 versus formal expected351); the default, Legacy and ShadowCompare battle writers were corrected to all-active type-specific depth bounds, and final related targeted tests passed 16/16. See `NONCHAR-EARLY-STAGE-Z-ACCEPTANCE.md`. This closes the focused first-pass writer difference for registered type1/type3, not downstream full Driver/hit parity or the two-domain source-rule reference history.

The WPoint non-`LF2WeaponBase` held pose has a two-view registered-World witness: original Editor job `bdb426058e5f448da3a79d711c11d9f4` passed 2/2; after raw Vx48 holder travel, holder/held integer X was348/359 at factor1 and373/384 in the configured view, preserving DAT-local gap11. See `WPOINT-POSE-TWO-DOMAIN-WITNESS.md`. The separate active type1 weapon-component writer has its own unchanged-source row21 and original Editor job `13805c76522f442cac6bc7917647d39b` 2/2: holder/held XInt348/359 and373/384, raw local gap11/Z251. See `WPOINT-WEAPON-COMPONENT-TWO-VIEW-WITNESS.md`. The active CPoint held-pose writer has an unchanged-source row1 and original Editor job `b1e0ecd2071448b580d1b62e66b45f0a` 2/2: catcher/caught XInt348/373 and373/398, raw local gap25/Y4/Z249. See `CPOINT-HELD-POSE-TWO-VIEW-WITNESS.md`. Its separate throw-position writer now has formal row0 and original Editor job `a75fa674dcb1434d9324c5a5fa351453` 2/2: catcher/caught XInt148/159 and173/184, gap11/Y-24/Z200. See `CPOINT-THROW-POSE-TWO-VIEW-WITNESS.md`. These focused pose witnesses do not implement the dual-coordinate source-rule carrier or prove downstream full Driver/Play contact.

Static branch audit also found two content-conditional rule gaps outside those two focused WPoint poses: formal `settle_held_refill_objects` applies no ±1 Y/Z displacement when parent `wpoint.cover == 2`, while both Unity held pose branches currently choose the nonzero-cover `-1/+1` path; formal `settle_catch_relations` uses a nonzero `cpoint.z` as a direct local Z offset, while `BattleCpointWriter.SyncHeldPosition` currently only chooses ±1 from `cover % 10`. The prior locked 330-object held-relation audit reports zero cover2 among its 668 reachable edges, and no current formal-content nonzero-CPoint-Z witness has been established here. These are static branch differences, not evidence that the current official DAT reaches them or that the user-authorized view scaling caused them. Keep them in the content-reachability inventory and test the exact branch before any production fix; do not edit DAT to create a case.

1. Finish reachability and transaction classification for direct frame/physics aliases, multiple OPoint compatibility, alternative held-state paths, non-character depth clamp and fallback/optimized writers. The table above resolves the listed core aliases but does not certify that every lexical X/Z match in the scoped search is a production writer or covered. Mark each reachable writer `absolute`, `scaled physical`, `raw relative birth`, `relation snap`, `attachment`, `stage bound` or `presentation-only`.
2. The registered type1/type3 early StageZ writer, both active WPoint pose branches and CPoint held/throw now have paired formal source and focused original Editor evidence above. Finish remaining reachability and downstream full Driver/hit parity; the two-domain source-rule history still needs its own carrier and must not be inferred from these battle-pose results.
3. Only then create an implementation Task/Change that owns the carrier, birth/physics/writer updates and snapshot/reset/checksum together; record versioned diagnostic implications. OID219 and fusion consumers must be tested against both static and moved histories, factor1/configured Worlds, full tick and Battle Scene Play. Q07 remains paused.
