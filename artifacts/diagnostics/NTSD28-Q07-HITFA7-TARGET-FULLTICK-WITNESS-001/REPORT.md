# Target-present `hit_Fa=7` full-tick witness

Status: `FOCUSED_TEST_PASS / TARGET_PRESENT_TWO_TICKS`; the first difference was corrected by `NTSD28-Q07-NONCHARACTER-DVY-SINGLE-APPLICATION-001`. This is a repository diagnostic model built from the paired playable source, not a formal EXE screen observation. Q07/R15 and the full alignment goal remain open.

The dedicated [runner](../../../Tools/NTSD28AuthorityTrace/hitfa7_target_fulltick_main.cpp) and [fixture](scenario.json) use formal runtime OID99/action0 as target in slot0 and OID875/type3/action55 as subject in slot1. The fixture uses Stage23 Z620/Z600 and three no-input ticks. After `GameSession28::initialize`, the runner verifies both authored entities, then sets only the subject's preassigned target slot to 0 and initial Vy to 3.8 before calling real `GameSession28::step()` for each tick. It does not modify formal J: sources, the generic scenario schema or the existing shared capture runner. The fixture's `referenceExeSha256`/`dataSha256` values are the loader's inherited locked scenario metadata; the current formal EXE identity is verified independently by the build script.

[The corrected build manifest](source-stage23-build-manifest.json) records formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired-source manifest `07CD47A0623F23D2C439E0E85EABF2ED10F8EAE8FC7D70DDB8396C704B3D778F`, runner SHA `E09082407F562DA711B61DE5C6FCAF1B8A29A6C58E35B62E637C150C332364D1` and binary SHA `DA8297D82DBF792E4866B56EC630C93448131F1851BA4C58FD88D7B8BFFA18FE`. The [source witness](source-stage23-witness.jsonl) is:

| Completed tick | Action | Vy | Integer Y | Precise Y | Previous Y | Entities |
|---:|---:|---:|---:|---:|---:|---:|
| 0 | 55 | 3.8 | -30 | -30 | 0 | 2 |
| 1 | 55 | 5.2 | -20 | -20.600000000000001 | -30 | 2 |
| 2 | 60 | 0 | -15 | -15.400000000000002 | -20 | 2 |
| 3 | 61 | 0 | -15 | -15.400000000000002 | -15 | 3 |

Action55 has `dvy:1` in the formal DAT. The full tick includes native AI, frame motion and physics in that order, so the earlier local AI→physics seam's `-21.6` is not this full-tick expectation. Tick3's third entity is an observed source-model birth, not yet classified or matched in Unity.

The first Editor attempt used a two-tick Z100/Z120 fixture and failed *before battle setup* because the existing Unity replay wrapper requires three ticks and Stage23 Z542..712. Its [earlier source output](source-witness.jsonl) and [build manifest](source-build-manifest.json) are retained solely as superseded fixture history. The fixture and Unity test now target the corrected three-tick Stage23 witness.

The original-project Editor then captured two real full ticks with the corrected Stage23 fixture. Its [raw Unity rows](unity-pre-numeric-comparer.jsonl) were independently compared to the source JSONL by numeric value, avoiding the first NUnit comparator's false mismatch between JSON `0` and `0.0`. Tick 0 matches. At tick 1, source/Unity are `Vy 5.2/6.2`, `integerY -20/-19`, `preciseY -20.6/-19.6`; action 55, target slot 0, X/Z motion, previous Y and entity count match. Tick 2 is downstream: source/Unity integer Y `-15/-13` and previous Y `-20/-19`. The focused NUnit assertion has been changed to numeric comparison, but its revised run is pending. The raw values establish a full-tick first difference.

Static caller closure now identifies the likely duplicate writer: `NTSDBattleTickSystem` calls `NativeFrameMotion` before `NativePhysicsAndDeadCharacterResourceNormalize`; the first pass applies action55 `dvy` through `LF2Entity.ApplyNativeFrameMotionForWorldPass`. For type3, the second pass calls `LF2SpecialAttack.RunNativePhysicsForWorldPass` → `RunSharedNonCharacterDatFrameAdvance` → `ApplyNonCharacterFrameVelocityForFrameAdvance`, which applies nonzero `dvy` again. The legacy suppression flag is set only later around `SimulationWorld.SerialTickAll`, so it does not protect this native physics call. The observed +1 matches action55's `dvy:1`. This is a source-path diagnosis, pending a focused rerun and a separate predeclared production Change; do not alter unrelated character/weapon routes based on this one sample.

Attempting that focused rerun through the Editor bridge used an unsupported `filter` parameter and inadvertently started an 8,259-case EditMode job `807dbc7e717a4928bb4be19941856920`. The installed bridge's `RunTests.cs` actually requires `testNames`. The accidental job had unrelated failures and was still running at the last observation; no broad-suite result is used as Q07 acceptance evidence. Do not launch a concurrent test; wait for its terminal status and rerun only the exact Q07 case with `testNames`.

Later correction and result: the old original-Editor process exited after its last observed 7,706/8,259-case progress, and the same project reopened in a new Editor. The broad job's terminal result was not recovered; its unrelated failures and generated diagnostics are not used as Q07 evidence. The exact `testNames` selector then ran only this Q07 test. Numeric comparison uses `Within(1e-9)` for binary64 representation differences; pre-fix job `43584c3d7f314ed0b464c5825526bb17` failed 0/1 at tick1 integerY expected -20/actual -19. After the separately declared production fix, job `83c62d25b9864b97b1e135bf87b0b4f4` passed 1/1 for initial and completed ticks1/2. Tick3's diagnostic pool prerequisite remains open. The production fix and fresh R15 three-stream recapture are documented in the [follow-up report](../NTSD28-Q07-NONCHARACTER-DVY-SINGLE-APPLICATION-001/REPORT.md).
