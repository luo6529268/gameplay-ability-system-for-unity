# NTSD28-R15-NATIVE-MODE-V2-CAPTURE-001

Status: FOCUSED_TEST_PASS_SOURCE_MODEL_ONLY / UNITY_RUNTIME_PENDING. Parent: BATCH-04/Q07 and R15. Authority is the formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, its declared playable `game_session.cpp` selection and `NativeComboHud28`, and the formal `resources/runtime` bytes. The read-only exact resolver/hash/schema design is `../NTSD28-R15-NATIVE-MODE-CAPTURE-CONTRACT-001/REPORT.md`. The historical diagnostic runner is V1 object/fusion only with schema 17/25/28/2/2; new current captures need selected mode V2 and current compatibility tuple 17/26/29/2/2. Old captures are immutable history.

Exact code path: `Tools/NTSD28AuthorityTrace/authority_source_capture_main.cpp` only. Build script, formal C++ source, Unity production/runtime/test scripts, Scene, Prefab, resources and nonbattle code are outside this package. Governance: this Task, matching Change Record, Ledger, STATE, handoff, alignment R15. Generated build/capture/validation evidence belongs under this Task's diagnostics folder or existing repository Temp, without deleting old outputs.

Required behavior: capture the same parent/first selected child as the formal `GameSession28` resolver, preserving original virtual path for hash; parse captured child with formal `NativeComboHud28`, compare full 13-field config to initialized `session.config().native_combo_hud`, and fail closed on missing/invalid selected content. Compute mode input/semantic SHA with Unity's exact BinaryWriter preimage, then V2 five-component composite and semantic/projection. Include selected paths, presence, priority and both bytes in freshness before header and after simulation. Validate the whole bundle's success if late inputs change, including optional side streams. Emit strict V2 mode fields/scope and current schema tuple for fresh diagnostic captures only; keep `SOURCE_MODEL_DIAGNOSTIC_ONLY`, `certificateEligible=false` and all formal-source/binary identity fields. Do not claim native implements Unity snapshot/checksum internals. Never modify or relabel historical JSONL. Optional no-mode fixture must preserve V1 component algorithm, with current schema tuple for new captures.

Acceptance: build runner from formal unchanged source with `Build-AuthoritySourceCapture.ps1` into a fresh repository-local directory; run one formal scenario twice with identical seed/input and compare complete outputs; verify actual formal/staged mode hash targets independently against current Unity projection; validate new output with `NTSD28Parity validate-authority-capture`; exercise missing/changed parent or child and priority candidate emergence in separate temporary fixtures where practical. Preserve actual failure outputs. Original Unity Editor compile, Unity V2 capture, same-seed/native vs Unity first-difference and formal EXE observable fidelity remain separate R15 gates. Rollback by reviewing/reversing only this runner diff and governance entry; do not clean/reset user work.
