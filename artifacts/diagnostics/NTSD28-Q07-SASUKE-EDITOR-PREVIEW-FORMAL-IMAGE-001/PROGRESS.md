# Q07 formal Sasuke battle editor preview — pending runtime acceptance

Status update 2026-09-24: `VERIFIED_SCOPED_ORIGINAL_EDITOR_PREVIEW`; this is not a Q07 certificate. The former `ORIGINAL_EDITOR_RELOAD_PENDING` text below is a historical snapshot. Current original Editor Battle Scene was clean/reloaded; its visible preview menu result is [PASS JSON](ORIGINAL-EDITOR-VISIBLE-PREVIEW.json) with formal `sasu.png`, actor/shadow/foot/health present, 1131 non-clear pixels, zero green separators and unchanged Scene dirty state. The [512×512 PNG](ORIGINAL-EDITOR-VISIBLE-PREVIEW.png) was inspected. Original Editor [focused test job](ORIGINAL-EDITOR-PREVIEW-TEST-JOB.json) `b35b41641194482ba5126a4b306525d0` passed 11/11, failed0; a prior wrong-namespace request selected 0 tests and is excluded. Console error0, final Battle Scene clean, Battle/Menu disk hashes unchanged at `471396E7...7B9` / `785F828C...81E13`. This validates the authoring preview only; do not retire the old BMP or close Q07 from it.

The isolated Unity 2022.3.62f3 copy also opened the Battle Scene and executed the existing `BattleCentralEditorPreviewValidationEditor.Run` in Edit Mode with graphics enabled (no `-nographics`). A clone-only `-executeMethod` wrapper archived [the original validation JSON](ISOLATED-VISIBLE-PREVIEW.json), [512×512 rendered PNG](ISOLATED-VISIBLE-PREVIEW.png), and [the wrapper source](ISOLATED-VISIBLE-PREVIEW-ENTRY.cs.txt) before Unity cleared its `Temp` directory on quit. The report says `PASS`, `usedProcessedSourceSheet=true`, `usedCharacterManagerSprite=false`, and formal `sasu.png` source path; actor1, common shadow1/1, foot marker1/1, health1/3, 1135 non-clear pixels, 207 red-dominant, 215 yellow-dominant, zero green-separator pixels, and `sceneDirtyUnchanged=true`. The rendered image was opened and visually inspected: the character, health bar, shadow/foot marker are visible against the white background. Clone and original Battle Scene SHA-256 match `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0` after the run. This proves the existing preview validation path on the isolated copy, not the original Editor's in-memory Scene state or pixel identity with the formal EXE.

On 2026-09-22, an isolated project copy at `I:\GitHub\Unity_GAS\ntsd-q07-isolated-validation-20260922` ran Unity 2022.3.62f3 EditMode tests without sharing the original project's Library. Before the run, SHA-256 matched the original for the Battle Scene, formal Sasuke PNG and meta, preview implementation, Editor entrypoint, and test file. The focused formal PNG test passed 1/1 ([ISOLATED-FOCUSED.xml](ISOLATED-FOCUSED.xml)); the preview Editor test class passed 11/11 ([ISOLATED-PREVIEW-CLASS.xml](ISOLATED-PREVIEW-CLASS.xml)), including the existing BMP grid test. This establishes compilation and those focused EditMode assertions for the copied, hash-matched inputs. The original Editor has not reloaded the externally edited Scene, and no actual preview image was rendered or compared. The copy and its Library remain outside this repository; no cleanup was performed.

The original focused Unity importer test failed at imported width 1024 versus raw/formal 799 (job `e11d6c4c95ed40a39a837b14cc20bf5a`, `IMPORTER-RED.json`). A separate exact resource Task changed only `vfs/c/sasu/sasu.png.meta` `nPOTScale: 1→0`. After reimport, job `3fc9eefefb1142ba9dccfce8b23ef253` passed 1/1 (`IMPORTER-PASS.json`): importer and raw image 799×960, frame0 rect (0,881,79,79), opaque and transparent cell pixels.

The preview source now preserves authored PNG alpha; its BMP processing remains unchanged. The Inspector sample and validation fallback use formal `vfs/c/sasu/sasu.png`. `NTSD_Battle.unity` diff is exactly inactive preview sourceSheet GUID `6d174fff55a50784d9bbf85531fb7d86→b5d608d7fe42c474ea0f2bb728a122a2` and source rect y `481→881`; HUDBg x30 remains. New Battle Scene SHA-256 `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`. Menu Scene stays `6CF124A17F692325CED5774C3C10C3324AA047439BC6C3EB08856188A78AB4B1`. PNG byte SHA remains `2AEB4FF667A9EE63E60EE0E866E4700C780974746D47E231CAEF1EAFCAD7C694`. Old BMP and historical grid tests remain intact.

Read-only image comparison on 2026-09-22: formal `sasu.png` is 799×960 palette PNG with transparency, while old `sasuke_0.bmp` is 800×560 RGB. In the preview's first 79×79 cell, the formal PNG has 1,252 nontransparent pixels; after compositing that cell on black, 281 of 6,241 RGB pixels differ from the same top-left old BMP cell. The old sheet's first cell border sample `(79,0)` is `(1,255,1)` and the formal sheet's is `(36,36,36,255)`. This proves the formal rebind is a content change, not merely a file-extension substitution. It is raw-file evidence only; it does not show Unity's post-edit imported rendering or final screen parity. Formal PNG bytes, Scene, and old BMP were not altered by this comparison.

Post-edit Unity compile, focused EditMode assertions, and visible preview rendering passed in the isolated copy. The original NTSD Unity Editor PID 33236 previously showed `The open scene(s) have been modified externally` with Reload/Ignore, and MCP main-thread commands timed out while it was open. No computer-use or synthetic click/key was used; original Editor Reload has not been confirmed. Do not start a second Unity Editor on its Library or claim its in-memory preview success before it reconnects and renders the focused validation. A client retry previously rediscovered FPSTest's port; use the exact NTSD instance id `gameplay-ability-system-for-unity@b1b02287` and check returned scene/project before any command.

`git diff --check` passed; in-progress Change Ledger validation passed 656 Records/22 governed code files before the isolated run. Once original Editor Reload is confirmed, check Scene dirty/hash and Console errors, then run its existing `NTSD/Battle Rendering/Validate Edit Mode Central Preview` menu report/image and inspect its rendered output. No resource deletion or nonbattle change is authorized by this package.
