# NTSD28-Q08-NATIVE-KNOCKOUT-EVENT-STATE-001

Latest status (2026-09-22): `CODE_WRITTEN / OFFLINE_COMPILE_PASS / ORIGINAL_EDITOR_RUNTIME_PENDING`. The exact production/test code paths and validation limits are in the Change Record; the earlier `IN_PROGRESS` and `PLANNED` lines below are creation history. The original Editor has not produced this package's compile/test/Play evidence.

Current status (2026-09-22): `IN_PROGRESS`. The `PLANNED` sentence below records this Task's pre-edit creation state. Before script edits, the exact code-path list was amended with `Assets/NTSD/Scripts/Simulation/Core/NTSDBattleTickSystem.cs` for post-core newest-tail expiry. The selected formal `#killtext` uses `times:70`; until the Q09 mode reader publishes that field, this captured value only covers the active default mode. Other content/mode variants remain pending.

Status: `PLANNED`, no script modified by this package as of 2026-09-22. Parent: `NTSD28-UNITY-BATTLE-REALIGNMENT-001`, BATCH-04/Q08; Q09 knockout display and Q10 audio are downstream. Authority: formal root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033` and its playable `battle_world.cpp` standard-hit `record_native_knockout`/tail-prune, `game_session.cpp` post-core ordering, `render_snapshot.cpp` record lifetime. See `NTSD28-Q08-Q09-KNOCKOUT-FEED-CHAIN-AUDIT-001/REPORT.md`.

Current Unity state: `BattleDamageWriter.ApplyNativeStandardHitKnockout` already increments the correct lethal credit's `KnockoutCount358` before HP subtraction. There is no found persistent per-KO event carrier. `BattleWorldPendingEventSnapshotBuffer` currently captures sounds only; lockstep checksum, parity snapshot, restore, shutdown/reset and render handoff must not silently omit new world truth. The existing `SessionKnockoutFeedAndPrune` is a pass-order declaration, not yet an executing event consumer.

Implementation ownership for this package: preserve the existing lethal gate and single count increment; add a value record containing native tick, victim/source/credit/four-owner slots and source object type at that exact pre-HP point in the two standard-hit branches. Add a world-owned ordered record store with native newest-tail-only expiry, full state capture/restore and checksum participation; reset and ordered shutdown must leave no old-world event. Do not invent a fixed count cap that changes the formal unbounded record behavior or permit accidental double credit. Q09's mode parser, projection, PNG/text commands and Q10 audio are explicitly separate packages; this Q08 package must expose only the exact data they require.

Capacity decision after inspecting `BattleRuntimeAllocationGate` and `BattleManagedMemoryBoundary`: the seal hard-rejects growth for its covered pools/task queues, while the memory boundary measures and reports other tick allocations. Prewarm the KO value-record store and snapshot buffer to at least one event per runtime slot before the battle window. If the formal newest-tail rule retains more records under sustained KO traffic, allow measured list growth rather than dropping records or applying an invented cap; the allocation diagnostic must remain visible. This preserves gameplay semantics and makes the rare excess-allocation cost explicit. No GC collection or zero-allocation claim is allowed for that stress path without a measured bound.

Candidate owned script paths, to be narrowed or amended in the Change Record **before editing any script**: `Assets/NTSD/Scripts/Simulation/Ecs/Writers/BattleDamageWriter.cs`, `Assets/NTSD/Scripts/Simulation/Core/SimulationWorld.cs`, `Assets/NTSD/Scripts/Simulation/Runtime/SimulationBattleBufferModule.cs`, `Assets/NTSD/Scripts/Simulation/Lockstep/Snapshot/BattleWorldPendingEventSnapshot.cs`, `Assets/NTSD/Scripts/Simulation/Lockstep/Snapshot/BattleStateSnapshot.cs`, `Assets/NTSD/Scripts/Simulation/Lockstep/Snapshot/BattleStateSnapshotRestore.cs`, `Assets/NTSD/Scripts/Simulation/Lockstep/Checksum/BattleLockstepChecksumModule.cs`, `Assets/NTSD/Scripts/Simulation/Lockstep/Checksum/BattleParitySnapshot.cs`, and a new focused Editor test. If the existing capture/reset owner requires another path, amend this list first. No Scene, Prefab, menu, result UI, third-party, audio, old asset, or nonbattle logic ownership.

Acceptance: focused formal-source same-tick lethal cases for unarmored/armor, ineligible gate and source/credit/four-owner distinction; same-seed Unity full-tick event fields and count with no double increment; newest-tail retention/expiry, capture/restore/replay and checksum first-difference; original-project Unity compile, focused test, stable SelfCheck, representative Battle Scene Play, enter/exit/re-enter and zero residual. Preserve existing Q06 lethal count evidence. Do not rerun the whole character matrix for this shared event path unless a first difference requires it. `CODE_WRITTEN` and compile/focused/runtime/EXE parity are separate statuses.

Rollback boundary: only this Change ID's declared script diffs and test/meta after reviewing the current dirty tree; do not reset/clean, delete unrelated content, alter existing Scene changes or start a second Unity project. Current original Editor PID 33236 may remain open; investigate Q09 WORDS pending run separately before issuing a competing TestRunner request.
