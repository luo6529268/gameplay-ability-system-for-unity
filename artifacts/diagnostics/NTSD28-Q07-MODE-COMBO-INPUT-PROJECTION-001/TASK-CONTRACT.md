# NTSD28-Q07-MODE-COMBO-INPUT-PROJECTION-001

Status: `CODE_WRITTEN / STANDALONE_FOCUSED_PASS / UNITY_COMPILE_PENDING`. Parent: BATCH-04/Q07, formal mode combo content activation. Authority: locked formal `GameSession28::first_mode_child_virtual_path`, `NativeComboHud28::parse_text/complete`, `GameSession28` selected-mode combo options. Input files: already-staged `decoded_dat/data/mode.dat` and selected `decoded_dat/data/mode/ntsd.dat`. Formal tuple: present/1/1/50/1.

This bounded step adds only `Assets/NTSD/Scripts/Animation/LoganModeComboInput.cs` and its Unity `.meta`. It reuses `Lf2DatTokenizer` to parse the first `mode_information` child and one complete `<combo>` block; captures the selected two files' hashes and a deterministic semantic fingerprint; rejects path escape/malformed fields; and exposes a freshness check. Missing `mode.dat` remains an explicit no-config result, matching the current legacy/unmigrated publication boundary. No World state, catalog identity, cache key, menu, results page, Scene, resource, or existing parser is changed by this step.

Acceptance: the formal staged root yields the documented tuple and paths, its parent/child hashes match authority, parsing rejects an escaped child and incomplete combo, and the same content yields stable fingerprints. Use an in-memory compiler/fixture or the original project's focused Unity test; neither a second Unity project nor computer-use is allowed. If Unity compilation is unavailable, report it as pending rather than claiming runtime parity. Before the next activation step, establish a separate Task/Change covering `LoganObjectCatalog`, `LoganContentIdentity`, publication freshness and `ApplyMatchConfig` together, with snapshot/session identity checks. Rollback is a reviewed inverse of only this new script and `.meta`; no old resource or unrelated user file may be changed.

Actual: added only the declared new source and `.meta` GUID `f8f9acaddf2a4de1b49c3ee37daf5852`. PowerShell `Add-Type -Path` compiled the new source with its existing pure dependencies in memory; formal and staged input/semantic fingerprints matched, projected tuple was `1/1/50/1`, incomplete combo and child-path escape were rejected. An initial reflection test command failed because PowerShell wraps the expected exception as `MethodInvocationException`; the corrected same checks passed. This is an external compile/fixture check, not Unity compilation, Battle Play or World activation. Original Editor assembly timestamp 12:23:44 predates this source at 13:54:38. `git diff --check` passed, Scene/ProjectSettings unchanged in scoped status.
