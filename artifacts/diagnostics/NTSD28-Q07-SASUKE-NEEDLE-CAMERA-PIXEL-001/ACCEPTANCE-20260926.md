# Q07 Sasuke natural OID440 camera witness — 2026-09-26

Scope: original Unity Editor, saved `NTSD_Battle.unity`, selected formal `Assets/NTSD/Content/LoganRuntime`, existing physical L/D/J input probe plus one opt-in Editor-only camera observer. Formal root EXE SHA-256 is `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; its GPU output was not captured by this package. No DAT value, image, production script, Scene or camera serialization was edited.

The first run `q07-sasuke-needle-camera-20260926-01` is preserved as a diagnostic FAIL: the input probe passed and produced four natural OID440, while the new observer incorrectly required the *current* cell to remain `pic0`. It saved a baseline PNG but no after PNG. The second run `...-02` passed the revised four-current-pic command gate at tick15 with 959 changed nonblack union-ROI pixels, but raw PNG review showed the four child commands still coincided and the moving Sasuke parent overlapped the ROI. It is only a scoped camera-render result, not child-pixel attribution.

The final run `q07-sasuke-needle-camera-20260926-03` has a physical-input `PASS` result in `../NTSD28-Q07-SASUKE-NATURAL-STATE15-MOTION-001/` with four natural OID440 and the separately observed formal `chi.png` pic0 binding. At tick18, the camera observer matched four child stable IDs 101–104 and four CentralOnly `Entity` commands (slots 50–53, current effective pic1) from formal `vfs/c/sasu/a/chi.png`. The projected child centers were distinct. The union ROI was x419–483, y35–115 in a 960×540 bottom-origin capture. Its baseline had 0 nonblack pixels, while the after image had 424; an independent Pillow read of the two saved PNGs reproduced `beforeNonblack=0`, `afterNonblack=424`, `changedNonblack=424` over 5,265 ROI pixels. Both PNGs were visually inspected: a blue child group appears to the right of Sasuke in the after image and the same area is black before. The camera observer reported restored camera fields. Four commands and a newly visible group are evidenced; this A/B does not prove four individually distinguishable sprite silhouettes or formal EXE GPU parity.

Original Editor recompiled after each observer correction with no C# Console errors; final Console error query returned 0 entries. It returned to EditMode after each Play. The saved Menu and Battle Scene SHA-256 remained `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13` and `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`. GameConfig and ProjectBattleModeConfig remained `0527D737A1FA38FC56B51D00DC6E96A421D3C67222546368B147C2D074CB8EA7` and `88E10D43B047952FD3053A87B7E5F60D0F87A6CD1A23313F37EA1503C686F55C`. This probe did not record post-exit World/slot/pool borrower counters, so the package remains `RUNTIME_PENDING` for that acceptance item. Q07/BATCH-04 and full battle alignment remain open.

## Same-scope shutdown follow-up

The original-Editor run `q07-sasuke-needle-camera-20260926-04` used the same saved Battle Scene and physical L/D/J request. Physical probe `PASS` again observed four natural OID440; camera observer `PASS_SCOPED_UNITY_CAMERA` again matched four formal `chi.png/pic1` commands at tick18. Independent Pillow reread of the new PNGs reproduced 0 baseline nonblack and 424 after nonblack pixels in the 5,265-pixel ROI; the after image was viewed. The added opt-in `ExitingPlayMode` witness ran **after** the existing shutdown owner had reached `Stopped`. Its read-only, idempotent report showed `AlreadyStopped / RuntimeMapCleared`, `remainingWorldObjects=0`, `remainingRuntimeSlots=0`, `remainingPoolBorrowers=0`, saved in `q07-sasuke-needle-camera-20260926-04-shutdown.json`. It did not initiate or reorder shutdown.

The Editor returned to idle EditMode, C# Console error query returned 0, and the four hashes above remained unchanged. This closes the scoped original-Unity camera-and-shutdown witness. It still does not prove four individually separated sprite silhouettes, formal EXE same-world GPU parity, or Q07/BATCH-04 completion. Earlier failed/scoped result files remain intact. Status of this exact package: `VERIFIED_SCOPED_UNITY_CHILD_GROUP_CAMERA_AND_SHUTDOWN`; aggregate Q07 remains `IN_PROGRESS`.

Interpretation of the shutdown numbers: `BuildShutdownReport` returns zero for World/slot fields after `_world` has been unbound and for the pool field after its owner reference has been cleared, so the final JSON numbers alone are not a direct pre-unbind count sample. The stronger shutdown evidence is the observed `Stopped / RuntimeMapCleared` transition: the production stage-8 `TryShutdownAndClearLogicState` refuses to advance if World objects, claimed slots, buckets or logic borrowers remain, and stage-10 `CompleteBattleQuiesce` refuses active object/sprite borrowers. This package observed that guarded stage sequence completing; it did not independently snapshot each collection before unbinding.
