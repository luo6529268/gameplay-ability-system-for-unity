# Q09/P-04 natural-tick same-Z pixel witness: partial result

Status: `RUNTIME_PENDING`. The original Battle Scene production driver completed one focused tick from 5 to 6 with formal OID120/121 alive in physical slots 51/52. Both advanced from frame0/pic0 to frame1/pic1, remained at equal Z240 in the later run, and the actual CentralOnly tick-6 captured frame materialized body commands at indices 3 (slot51) and 1 (slot52). The actual-tick body projections intersected at x[1000,1069), y[550,606). This proves the natural tick reached the correctly ordered command/publication stage. It does **not** prove the final GPU pixel order: the test-only reference stage failed before all four images were available.

The first focused run under Scene SHA `9409F2...3B3A39` failed because the new natural route projected bounds before initializing capture height. The result is `first-zero-height-projection-failure.json`; object/slot/two-pool/pause cleanup passed, Scene disk unchanged. The second attempt was rejected before Play because the Editor Scene was dirty in memory; its result is `second-preplay-scene-dirty-failure.json`. A read-only inspection after domain reload showed a clean Scene and both Cameras enabled (`scene-dirty-after-domain-reload.json`).

Before the third attempt, an independent UI task changed the original Battle Scene disk to SHA `56B49C593E5C8CDD9065E6DF74302D0D95AA2162896E18D70DF654C3844A146B` (new `ComboPanel`/`BattleControls` hierarchy and UI layout). The Editor loaded that revision. The third run produced a real tick-6 composite image (`third-natural-composite-only.png`, 8553 nonclear pixels) and the ordered, overlapping body commands above, then failed because the old controlled-route assertion still required tick5 during reference capture. Its complete result is `third-stale-real-tick-oracle-failure.json`; cleanup and Scene disk hash passed. The probe assertion and reference positioning were corrected afterward: natural-mode A/B references now preserve each entity's exact post-tick X/Y/Z.

The final probe revision compiled in the original Editor with 0 Console errors. A further Play is currently deferred because the Editor's original Scene is dirty in memory after UI hierarchy/Canvas changes from another active task. That task saved the Scene again after the third run: latest observed disk SHA at 21:35 was `25BA977C5D59452D179E636E2333151F4E462415D49245FFB47339B9F3D9CB74`, so the third-run SHA above is historical. Both serialized Cameras remain enabled. No unsaved UI state was reloaded, saved over, reverted, or deleted. Once the original Scene is clean and stable, record a **fresh** disk hash, run one focused request `Temp/NTSD28_Q09_SameZNaturalTickPixel.request`, then independently inspect all four PNGs and the result, and verify post-Play Scene SHA/diff against that fresh baseline. Do not promote the existing command observation to pixel PASS.

Final governance: `Tools/Validate-ChangeLedger.ps1` PASS with 792 records and this test script covered by both the original controlled Change ID and this natural-tick Change ID. Repository-wide `git diff --check` currently fails only in the other task's modified Battle Scene YAML (trailing spaces on serialized empty fields); this package did not touch that Scene and will not format it. Scoped `git diff --check` of this package's tracked governance files passed, and `rg` found no trailing whitespace in its untracked script/Task/Record/progress text.

Legacy renderer and formal EXE same-condition A/B remain separate P-04 exits. The earlier controlled CentralOnly frame0 GPU witness remains 11/11 PASS within its own scope.

Separate-session update: the revised original-Editor test compiled with 0 Console errors. The `baseline` session under Scene SHA `9C7EDD3A145643E3A392B24FE3414EE6A2E4954B429C65587A3F82F6BA6CFD7D` returned `PASS_CAPTURE`: actual tick5→6, both formal weapons alive at frame1/pic1 and equal Z240, physical slots51/52, X1400, initial shared RNG state/calls `3126301419/5`, post `3437711874/6`, native CRT state unchanged, object/slot/two-pool/pause cleanup passed and Scene disk hash unchanged. Its result/image are `independent-baseline.json` and `independent-baseline.png`. After Play exit the Editor Scene was clean. Before the `a` variant could be requested, a parallel task saved the Battle Scene to a different SHA `711FE77B...E5D605457` at 21:58:56, so the four-run same-Scene comparison stopped. This baseline is a single valid capture, **not** a four-image pixel conclusion. Both serialized Cameras remained enabled. Do not compare this image to variants captured under another Scene SHA; restart all four only when the Scene remains stable across the whole set.

22:06 check: Battle Scene disk changed again to SHA `402AE9D9E32E1CE52061A723744369D26F18610C7A1DEFB81CECBFFAAB6B9C95`; Git diff shows concurrent UI/Map scene changes, and both serialized Camera components still have `m_Enabled: 1`. No natural-variant request was sent against this revision. Same-Scene four-image set remains pending; no Scene content was edited or reverted by this package.

22:22 final scoped update: a newer clean Battle Scene revision, SHA `471396E7692BA0C4B5CC8C2CD1355D4CF88B8AB2ACF23A6CAE6BF1E6475BC7B9`, remained stable across four independent original-Editor Play sessions. `baseline/a/b/both` each captured actual tick5→6, formal OID120/121 frame1/pic1 same Z240, slots51/52 and identical initial RNG, then restored counts/pools/pause, exited Play and kept Scene disk unchanged. The original exact-color-only oracle stayed at zero because baseline has 463 nonwhite underlay pixels and five edge blends. Independent analysis of **all** 82 pixels distinguishable in both single-weapon images found 82/82 consistent with OID120-later compositing within one RGB level, 44 rejecting OID121-later, none favoring it. The raw JSON/PNG and formula are archived in `scene-471396E7-series/ANALYSIS.md`. The scoped natural CentralOnly GPU same-Z exit is VERIFIED; Legacy and formal EXE same-condition pixels, other Q09 P-items and total alignment remain open. The earlier pending/failure paragraphs above are preserved as history.
