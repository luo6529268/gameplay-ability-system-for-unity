# Q07 same-state KO to feed-row production witness

Status: `FOCUSED_TEST_PASS` for one original-Editor EditMode production Driver scenario. Q07/Q09 aggregate exits and formal EXE GPU pixel comparison remain open.

The existing formal Naruto-punch scenario (two Naruto, X500/X525, seed 682973786, J once at the scenario input edge) was loaded from the selected staged NTSD 2.8-Logan runtime. The project-owned `ProjectBattleModeConfig.asset` supplied mode/feed values; no original mode DAT was restored. The existing replay helper gained a default-false opt-in for that Asset. Only the opt-in path uses a hidden, isolated object pool because an EditMode singleton can have null managed state after domain reload; the prior singleton is restored when the fixture exits. The test appends neutral input through completed tick38 and runs the full `SimulationTickDriver` with presentation enabled.

Three initial attempts were retained as evidence. Job `f6010a5ef8654e41a6f5ca300bb37294` failed at tick6 because the test fixture had not entered the existing formal allocation seal and positional `StepOneTick` flags disabled presentation; its partial CSV and NUnit stack are saved. Job `7241422e8a44460596dd996b5959e871` produced all 38 expected rows but failed the zero-residual gate with two objects/two slots. A diagnostic-only compile pass then caught three CS0103 errors from a scope-local variable; the declaration was corrected. Job `1a0561ebcf824988a5276eb605be861f` identified the teardown cause as `object-pool-runtime-state-is-invalid` at shutdown stage `PendingObjectPointTasksDiscarded`. No production shutdown rule was changed.

After the isolated test pool was added, the original Editor compiled with zero errors and exact focused job `3a25c06fb233489f972a390006a92e70` passed 1/1. `unity-row-38.csv` SHA-256 is `1C299F63CB2ECD84B63977E877D827AFE53C7F405A6641AB60D9728809A69AB1`. It records no KO through tick7, one KO on tick8 with event battle time7, one native record and one published row from tick8 through tick36, and zero published rows at ticks37/38 while the native record remains. The helper verified zero World objects, zero claimed slots and zero logic borrowers after ordered shutdown. Final `Assembly-CSharp-Editor.dll` timestamp was later than both changed scripts. This is a production tick-to-publication witness in EditMode, not a pixel witness.

Menu Scene SHA-256 stayed `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`, Battle Scene `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`, GameConfig `0527D737A1FA38FC56B51D00DC6E96A421D3C67222546368B147C2D074CB8EA7`, and project mode Asset `88E10D43B047952FD3053A87B7E5F60D0F87A6CD1A23313F37EA1503C686F55C`. No production code, DAT, Asset, Scene or nonbattle code was edited. Governance validation is recorded in `change-ledger-validation.log`.

`Tools/Validate-ChangeLedger.ps1` exited 0 (`PASSED`, 857 Records, 16 governed code files in the current dirty diff) and `git diff --check` exited 0. The original Editor returned to idle EditMode on Battle Scene with zero Console errors. The three restored live progress documents remained NUL-free; this package did not reinstall or alter the preserved v3 recovery candidates or corrupt snapshots.
