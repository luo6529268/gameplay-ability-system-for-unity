# R15 formal kind-dependent source-model trace preflight

Status: `SOURCE_MODEL_3TICK_MAIN_B2_VALID / UNITY_PENDING / B0_CONTRACT_FIRST_DIFFERENCE`. This is not a formal EXE capture or an R15/Q07 exit certificate. No formal J: file, Unity production script, saved Scene or project setting was changed.

The selected formal `data/kind.dat` has one record: effect 209, frame 40, bound IDs 8/209/213 and respond IDs 200/203/205/206/207/215/216. Formal `data.txt` identifies OID213 as type3 `c/saso/a/pup.dat` and OID206 as type3 `c/dei/a/cla2.dat`. `pup.dat` has frame176 with a kind0 ITR; it has no frame40. Formal `battle_world.cpp` kind branch transfers the attacker's definition/ID/type/group/owner and sets action/latch/previous to the record frame without a local existence guard. These are source/static facts; tick behavior below is a separate measurement.

The [diagnostic scenario](kind213-action176-to-206-action0-candidate.json) derives from the existing three-tick Stage23 two-slot baseline: seed 682973786, no inputs, slot0 OID213/action176 at (560,0,650), slot1 OID206/action0 at (560,-70,650), teams 1/2. The scenario's `referenceExeSha256` and `dataSha256` are inherited legacy fixture metadata; actual capture provenance is in the output header. The reused capture binary, runner source and formal EXE rehashed against the immediately prior R15 V3 build manifest, all matching. The formal `resources/runtime` root was supplied both as catalog root and complete VFS root. An initial call incorrectly passed `decoded_dat` as the catalog root and failed before simulation with `content identity catalog load failed`; its invalid output is retained and not used as evidence.

Two source-model runs without B0 were byte-identical: [main raw](source-main-b2.raw.jsonl) SHA-256 `000A422DD5E48735975295119D80AD32C3B4B18AACA21F43C6C552C5E0B46B98`; [B2 input/RNG](source-main-b2.b2.jsonl) SHA-256 `3A9897F8ECCE5BAB95D8E0C18019A54AAC178AB34593A8B06D806D853F18F66E`. Both completed three ticks, and the independent [main validator](source-main-validation.json) and [B2 validator](source-b2-validation.json) passed with `certificateEligible=false`. At tick1 the source main stream contains only slot1: object ID 213/type3, allocation epoch1, action/action-latch/previous40, owner0/group1, HP445 and special-hit latch true. Ticks2/3 retain that object/action with hit timer 8/7. Slot0 is absent after tick1. This confirms a kind-dependent transformation in the source model on this diagnostic initial state. It does not prove the same sequence is naturally reached by roster play or the formal EXE.

Enabling B0 for the same scenario produced `unhandled_std_exception=domain occupant changed without allocation epoch` and an `INVALID_CAPTURE_BUNDLE`; those B0 files must not be compared as valid traces. The B0 emitter's snapshot-derived lifecycle contract currently accepts birth/death/reuse but rejects an object ID change at a stable allocation epoch. The observed source transformation changes slot1 ID206 to 213 in place at epoch1. This is a diagnostic schema/capture blocker, not proof of a Unity battle-rule difference. Existing Unity raw capture also lacks a combatant `action` field and unconditionally instantiates `LF2Character`, so it cannot yet represent this source initial state/type3 identity; no Unity comparison was attempted.

Next bounded work: declare a diagnostic-only Task/Change for the raw-capture initial action and type3 carrier plus a versioned or otherwise strictly validated B0 `identity-change` observation that preserves allocation epoch. Run focused negative/positive B0 contract checks, then original Editor capture on the exact scenario and compare main/B2/B0 according to their declared RNG topology. Keep source-model and formal EXE behavior separate; do not add a synthetic frame40 or modify production battle logic to make a trace pass.
