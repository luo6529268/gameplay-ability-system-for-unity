# NTSD28-Q09-WORDS-PUBLICATION-001

Status: `PLANNED` before code modification (2026-09-22). Parent BATCH-05/Q09, R17 and upstream `NTSD28-Q09-WORDS-INPUT-IDENTITY-001` (`CODE_WRITTEN`, original Unity pending).

Authority: formal `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; playable `GameSession28::initialize_resources` resolves `resource.dat` indices 16..21, `render_snapshot.cpp` creates name/HUD glyph commands and `d3d11_renderer.cpp` uses 8x16 source glyphs on 16-pixel stride. Formal/staged six WORDS PNGs are 251x257 indexed images with tRNS values including intermediate alpha; decoder must preserve straight RGBA. Current Unity production common catalog only publishes Shadow+Spark; existing `WithWords` and atlas binder support six sheets but are dormant without a producer/source map.

Exact production ownership: `Assets/NTSD/Scripts/Animation/Manager/CharacterAnimtorManager.cs` only. Focused test: new `Assets/NTSD/Scripts/Test/Editor/NTSD28Q09WordsPublishedCatalogEditorTests.cs` plus `.meta`. Governance: this Task, Change Record, Ledger, STATE, handoff and alignment. No DAT, PNG, Scene, Prefab, GameConfig, generated/third-party script, menu, nonbattle HUD, battle-rule or old image edits. Q06 combo/spark producers and approved presentation exceptions stay intact.

Implementation: for a Logan candidate with the already-captured `WordsInput`, verify/decode exactly its six PNG paths on a worker with `BMPLoader.LoadVerifiedImageData`, require PNG/251x257, preserve source alpha, create one Texture2D and 256 Sprite bindings per sheet on the Unity main thread using existing `BattleCommonVisualCatalog.GetWordGlyphPixelRect`/pivot. Add each transient texture/sprite immediately to the existing staged ownership sets. Build `BattleCommonVisualCatalog.WithWords` before the existing single no-await publication, and register six source-pixel sheets plus 1,536 word-glyph source-key mappings in the existing unified atlas path. Absent `WordsInput` retains current Shadow+Spark behavior. Any decode/cancel/bind error must destroy staged resources and leave the previously published catalog untouched. Do not create a new manager, queue, worker, shutdown phase or tick dependency.

Acceptance: current-source offline compile and focused source-input checks are proxies; original Editor must freshly compile and run the new focused test. Test the actual formal six PNG dimensions/alpha, catalog glyph geometry, common source mappings, central/SourceTexture2D fallback, cancellation/failure preserving previous publication, then representative nameplate/HUD pixels and formal EXE comparison at equivalent state. Reuse existing lifetime/ordered-shutdown evidence only for unchanged paths; verify this new resource branch's exit/re-entry and zero residuals. Do not claim Q09/R17 closed from compile or one fixture. Rollback: review and reverse only this Change ID's exact code/test diff after authorization, preserving all dirty work and staged resources; no reset/clean/delete.
