# NTSD28-Q10-KNOCKOUT-MODE-SOUND-001 Task Contract

Status: `PLANNED` before any script/resource edit. Parent BATCH-05/Q10; Q06 frame-sound producer and Q09 icon/row work are not part of this package.

## Authority and original state

- Formal release EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. Its playable `GameSession28::step` (`source/ntsd28_playable/src/game_session.cpp:3248-3251,3292-3304`) captures pre-tick KO count and appends audio for only newly recorded KOs after result audio and before newest-tail pruning. `native_knockout_feed_audio_event` (`source/ntsd28_core/src/rendering/render_snapshot.cpp:858-892`) requires mode 1, runtime display latch, a still-live victim of battle group 1/5, and a nonempty path or valid channel. It takes victim world X and ignores the row draw `bound`/allowed lists. Formal selected `decoded_dat/data/mode/ntsd.dat:81-82` paths are `data\m_ok.wav` and `data\m_join.wav`.
- Existing formal source tests cover pure route and one real GameSession lethal tick (`ntsd28_core/tests/render_snapshot_tests.cpp:1879-1903`, `ntsd28_playable/tests/game_session_tests.cpp:2928-3030`). These are source-level existing evidence, not a fresh formal EXE audible test.
- Unity currently captures both mode sound paths, records native KO events and prunes the tail, but has no KO audio event producer. `PendingSoundEvent` carries cue/worldX/tick, and the current `NTSDSoundPlayer` seals prewarmed cues before battle. `CharacterAnimtorManager.CollectBattleSoundIds` collects frame/weapon cues but not the mode KO fields. The selected formal WAVs exist only under formal `resources/runtime/vfs/data`; `Assets/NTSD/Sound/data/m_ok.wav` and `m_join.wav` are absent. Existing `Assets/NTSD/Sound/Click` files with those basenames have different SHA and must remain untouched.

## Exact scope and invariants

- Modify only `Assets/NTSD/Scripts/Simulation/Core/NTSDBattleTickSystem.cs`, `Assets/NTSD/Scripts/Simulation/Host/SimulationTickDriver.cs`, `Assets/NTSD/Scripts/Animation/Manager/CharacterAnimtorManager.cs`, and new `Assets/NTSD/Scripts/Test/Editor/NTSD28Q10KnockoutModeSoundEditorTests.cs` plus its `.meta`. Stage only `Assets/NTSD/Sound/data/m_ok.wav` and `m_join.wav` plus their `.meta` from the two formal VFS files. Do not alter Scene, menus, ordinary HUD, nonbattle audio, existing Click sounds, or old resources.
- The tick system captures the KO event count before core combat and queues the selected mode path only for events appended this tick, after results and before KO tail prune, preserving existing sound order. Publish source paths and display latch from the already captured selected mode/match config before the first tick; no singleton lookup inside tick, no audio file I/O from core, no gameplay state feedback from audio. Skip mode!=1, display off, missing victim, non-1/5 group and empty selected path. Use current victim integer world X. Do not filter audio through row `bound` or draw-list gates, and do not replay retained KO events on later ticks.
- Prewarm both selected mode cues through the existing sealed battle audio catalog; do not change the general sound root or existing frame-sound Q06 producer. Existing `PendingSoundEvent` path projection is sufficient for selected formal mode paths, which are nonempty; synthetic built-in-channel fallback remains a separately named Q10 gap. Keep worker/main tick paths sharing `RunReleaseTick`; keep reset/shutdown of old World isolated by the existing tick-system lifetime.
- Sound resources are a precisely listed Q10 battle-audio dependency, not a blanket D-023 WAV migration. Verify formal/staged SHA byte equality and GUID uniqueness. Player portable packaging and audible EXE comparison remain separate Q10 gates; Editor event tests cannot close them.

## Acceptance, risk and rollback

1. Preserve pre-edit exact-path state and first RED where practical. Focused original-project Editor tests cover a real lethal tick to group 5 and mirrored group 1, single event only on lethal tick, exact cue/worldX/order, and mode/display/no-feed gates. Verify selected prewarm adds both cues before seal without changing unrelated cue collection.
2. Compile the current original project with zero error, run only new focused tests and any directly affected audio test, then run a targeted battle scene with staged cues to confirm loaded clips/no rejected-unprepared cue; separately check stop/re-entry and Scene dirty unchanged if this package reaches runtime validation.
3. Document all actual commands/results and unresolved Player/EXE/remaining 974 cue gates. Run `Tools/Validate-ChangeLedger.ps1` and `git diff --check` before handoff.

Risk: a missing prewarm would silently reject a newly queued cue after the catalog seal; reusing the Click basename would play nonformal bytes; wrong KO count or pruning order would replay/drop audio. Rollback is only this package's reviewed script diffs and the two new WAV/meta files, while preserving all preexisting dirty work. Any deletion of already-existing content requires separate authorization under repository rules.
