# Q09 formal platform carrier through the original Battle Scene Driver

Status: `VERIFIED_SCOPED_FULL_DRIVER_CENTRAL_COMMAND`. Original Editor request `platform-hidan-01` produced the raw JSON `platform-hidan-01.json`, SHA-256 `2A17E2EC059D59E561815F87BB6210B42205DC35DA440A6D2CD0C67FC5DCAEB6`. This is a controlled Play witness, not a natural-input or root-EXE graphics claim.

The original Unity Editor `gameplay-ability-system-for-unity@b1b02287` refreshed and recompiled the new Editor probe (updated `Assembly-CSharp-Editor.dll`, no observed C# compile errors). The first refresh returned after an MCP disconnect/retry before the assembly changed, so its 50-second pending request was not a Play failure. The second refresh updated the assembly and consumed that **same** opt-in request; no second Unity project or repeated test request was started. The Console contained five `MCP-FOR-UNITY: Client handler error: Cannot access a disposed object` entries associated with the bridge reconnect; they are recorded and are not counted as script compile errors or formal battle failures.

The saved original `NTSD_Battle` Scene ran on `Assets/NTSD/Content/LoganRuntime`. Formal OID56/current frame130 and tick-action snapshot130 started in slot50 at (X200,Y0,Z287), with formal OID2/type0 in different group in slot51 at (X205,Y-10,Z287). One production `SimulationTickDriver.StepOneTick` completed at tick6. Post-tick OID56 action became212 while its recorded snapshot remained130. The target's `CollisionYReference` became `-5`, `PlatformSourceSlotF4` became50, and `RenderShadowOffset10C` became`-5`. Thus the formal indexed `kind:50` record reached the platform operation30 write path in this controlled full-tick World; the post-tick action alone must not be mistaken for the candidate's entry frame.

The same tick's CentralOnly plan was valid and yielded exactly one target Shadow command. Its depth ZInt remained287; the command Y was approximately `-4.87000084`, equal within the probe's 0.0001-world-unit tolerance to `ScreenPixelToWorld(..., Z287 + offset-5).y`. The raw JSON's `expectedShadowPosition.x` uses pixel-X zero only to obtain this Y oracle; X is intentionally **not** compared because the command uses the runtime camera/offset X. This establishes central command consumption of the nonzero carrier, not actual camera pixels or Legacy renderer Play behavior.

The probe unregistered both temporary entities: World objects4→4, claimed slots2→2, Renderer borrowers2→2, `fixtureUnregistered=true`. Menu Scene SHA-256 stayed `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`; Battle Scene SHA-256 before, after and post-exit stayed `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`, with no Scene Git diff. The original Editor was re-polled after a transient null MCP state and was idle/non-Play on `NTSD_Battle`; the request was marked not running. No DAT, PNG, Scene, production battle logic, input or nonbattle script was changed by this package.

Remaining Q09 gates: actual camera pixels at the produced frame, Legacy Play shadow and any approved display exceptions, naturally reached formal frame/target conditions, and same-condition formal EXE graphics. Q07/Q08 dependencies and Q09 aggregate exit also remain open. The existing focused Q09 Legacy/Central 2/2 test and this Play result cover different layers; neither replaces the other.

Governance check after the scoped result: `Tools/Validate-ChangeLedger.ps1` exited 0 with 843 Records and two current governed code files covered (the prior shadow test and this new probe); `git diff --check` exited 0 with only line-ending warnings. All three live progress documents remained NUL-free. The two pre-existing untracked B11 authority-content JSONL files observed in `git status` were not touched by this package.
