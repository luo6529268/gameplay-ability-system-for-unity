# Q07 direct Battle empty-root entry: original Editor focused result

Status: `FOCUSED_2_OF_2_PASS / ORIGINAL_EDITOR_COMPILE_PASS / GOVERNANCE_BLOCKED / PLAY_PENDING`. Scope: `NTSD28-Q07-EMPTY-ROOT-BATTLE-ENTRY-001` only. The original Unity Editor PID 11944, not a second project instance, consumed the task's request-file TestRunner after it imported the current Editor test source.

The first request (`20260925-004534`) ran exactly the two declared direct-caller tests and returned 1 pass/1 error. `DirectCaller_EmptyRootRejectsLegacyFallback` passed. The existing formal-root test raised `DirectoryNotFoundException` before its target assertion because its shared `NTSD28B11AtomicPublicationEditorTests.Candidate` helper reads `Temp/NTSD28PngAlpha/fixture.dat`, which was absent. This is a missing test fixture, not an observed battle-content first difference. Its full untouched result is `first-missing-fixture.result` (SHA-256 `B376ECFC2D7FCD10AC06E0FE462F3FE747E1124B7663BB44E542B6A110437D78`).

The referenced PNG-alpha test normally creates that 6×2 RGBA fixture in its own `OneTimeSetUp`. The two-test filter does not include that suite, so the same pixel layout was written as an 89-byte valid PNG to the previously absent `Temp/NTSD28PngAlpha/fixture.dat`; its archived copy `fixture.png` has SHA-256 `9ABF1E3BC04A79339614DE99973595371C029587D4D420BA32D8C3CF0871EAE5`. No existing fixture was overwritten, and no test or production script was changed during this rerun.

The second unique request (`20260925-054422`) returned `state=Passed`, **2 passed / 0 failed / 0 skipped / 0 inconclusive** for exactly `DirectCaller_EmptyRootRejectsLegacyFallback` and `DirectCaller_LoadsTheSameConfiguredSource`. The untouched result is `second-two-pass.result` (SHA-256 `CCE6098584219BA31AC9368F84A81E114F81A670D2241A3C2DE22DCE5019A25E`). Original `Editor.log` records successful Tundra builds and no `error CS` in the inspected build lines. The Editor remained responsive.

Menu Scene SHA-256 remained `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`; Battle Scene SHA-256 remained `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`, both with no current Git Scene diff. Scoped `git diff --check` exited 0. `Tools/Validate-ChangeLedger.ps1` still failed because the live `STATE.md` is NUL-truncated and therefore omits many active IDs; this is a governance blockage, not a focused-test failure. No direct Battle Play, SelfCheck or formal EXE A/B was performed by this package, and Q07 aggregate remains open.
