# Q07 original Editor MCP transport boundary

Status: `READ_ONLY_CONTROL_DIAGNOSIS / ROOT_CAUSE_UNPROVEN`. This is a diagnostic for why the original Unity Editor cannot yet run the queued focused test; it is not a change to battle code or MCP configuration.

At 2026-09-25 01:45 local time the original Unity Editor PID 11944 was responsive, with `Editor.log` last written at 01:32:33 and no fresh script import after the current Q07 Editor test-runner edit. The exact `Temp/NTSD28-Q07-EmptyRootBattleEntry-20260925-004534.request` remained alone, without `.started` or `.result`. The current EditorPrefs selected `MCPForUnity.UseHttpTransport=1`, `MCPForUnity.AutoStartOnLoad=1`, and `MCPForUnity.HttpUrl=http://127.0.0.1:8080`. There was no listener on local 8080 or stored stdio 6403, no running `uv`/`uvx`/Python server process in the current process snapshot, and no `mcp_http_8080.pid` under the project's `Library/MCPForUnity/RunState`.

The installed MCP package is the local `I:/GitHub/Unity_GAS/unity-mcp/MCPForUnity` source and its Editor assembly exists in `Library/ScriptAssemblies`. `StdioBridgeHost.ShouldAutoStartBridge` does not start the stdio listener while HTTP transport is selected. `HttpAutoStartHandler` attempts HTTP startup once per Editor session (`SessionState` guard), then tries to start a local server and connect. `HttpBridgeReloadHandler` resumes only a bridge that was running before a domain reload; it does not turn a previously failed fresh-session start into a new automatic launch. `ServerManagementService.StartLocalHttpServer` launches a separate local server process and may fail in command construction, process startup, package resolution or connection. The current evidence does **not** isolate which of those occurred; the Editor log has no MCP-specific failure message.

Consequently, starting a server externally alone is not proven to reconnect the current Editor. Do not infer a bridge session from `AutoStartOnLoad=1` or a configured port. Safe validation remains: wait for the original Editor to import current scripts, then let the already queued request return a result or use an explicitly established editor control channel. Do not start a second Editor against this `Library`, mutate EditorPrefs/registry, change MCP package source, kill processes, or use computer-use to force focus. Independent Roslyn compilation is archived separately and does not replace the original Editor focused test.

2026-09-25 01:53 local read-only retry: `unity status --format json --project-path <this repository> --non-interactive` returned `STATUS_NO_INSTANCES` (zero Pipeline-connected Editors) while PID 11944 was still responsive. The exact Q07 request still had no `.started` or `.result`, and `Editor.log` was unchanged since 01:32:33. Project `EditorSettings.asset` shows Asset Pipeline mode 1; read-only EditorPrefs exposed `kAutoRefresh=1` and `kAutoRefreshMode=0`, but those stored values alone do not establish the current Editor's refresh behavior. No CLI command was sent to the Editor, no second Editor was launched, and no project or Editor preference was changed. The original-Editor focused gate remains pending.

Current source check of the installed MCP package: `HttpAutoStartHandler` marks a `SessionState` key and attempts local HTTP server start/bridge connection only once in the Editor process session. `HttpBridgeReloadHandler` resumes only a bridge already running before domain reload. `ServerManagementService.StartLocalHttpServer` first cleans local build artifacts and invokes a stop-existing-server routine before launching a new server. With no current listener and no proved Editor bridge retry, launching a standalone HTTP process now would not prove reconnection and could disturb shared MCP server state. It was not attempted. The running original Editor handle, absent listener and unchanged request are evidence of a current control-channel gap, not a compile or battle-behavior failure.
