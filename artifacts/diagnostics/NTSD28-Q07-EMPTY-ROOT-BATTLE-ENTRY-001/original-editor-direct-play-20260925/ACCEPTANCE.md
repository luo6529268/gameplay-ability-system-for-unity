# Original Editor direct Battle Play, 2026-09-25

- Original Unity Editor PID 11944, Unity 2022.3.62f3, project `I:/GitHub/Unity_GAS/gameplay-ability-system-for-unity`; no second Editor was launched. The local Unity MCP stdio bridge on port 6403 was used, without computer-use.
- Before Play, `manage_scene/get_active` returned `NTSD_Battle`, build index 1, loaded, `isDirty=false`. `get_editor_state` reported no compilation or test in progress.
- `manage_editor/play` returned success. The original `Editor.log` then recorded formal PNG content loading, `[BattleTestBootstrap] SetupTestCharacters called.` (line 190716), `BattleBootstrap presentation enabled.`, `SimulationTickDriver resumed.`, and `[BattleTestBootstrap] === Test bootstrap complete ===` (line 190818). This proves the configured nonempty formal-root direct Battle startup reached those checkpoints; it does not prove every entity, pixel, input or formal EXE behavior.
- The same Play exposed audio prewarm warnings such as `c\\tobi\\w\\sp.wav; HTTP/1.1 404 Not Found` (line 189813). Formal sound-table DAT is staged, but its referenced WAV resources are not staged; this remains Q10-owned. It did not stop this direct Battle bootstrap.
- A read-only `execute_code` attempt to inspect the live tick failed before execution because the MCP tool's Mono compiler command exceeded the Windows command-line limit. No runtime value was obtained from that attempt.
- `manage_editor/stop` returned success. After exit, `get_editor_state` reported `is_playing=false`, `is_changing=false`, compilation false and idle; `manage_scene/get_active` again returned `isDirty=false`. Battle Scene SHA-256 remained `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`; Menu Scene remained `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`.

Result: direct Battle formal-content startup is witnessed in the original Editor. The empty-root rejection is covered by the separate 2/2 focused EditMode result, not by this nonempty-root Play. This bounded change remains `FOCUSED_TEST_PASS / DIRECT_BATTLE_PLAY_PASS / GOVERNANCE_BLOCKED`: the live progress documents' NUL corruption still prevents a valid Change Ledger gate. Q07 as a whole, Q10 audio, SelfCheck and formal EXE A/B remain open.
