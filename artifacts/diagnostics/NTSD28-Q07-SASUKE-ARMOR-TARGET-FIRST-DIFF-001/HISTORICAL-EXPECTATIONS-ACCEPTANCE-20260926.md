# Q07 historical ShadowCompare expectations: scoped acceptance

`NTSD28-Q07-HIT-SHADOW-HISTORICAL-EXPECTATIONS-001` is `FOCUSED_TEST_PASS / TEST_ONLY`. It changes only `BattleHitExecutionPlanEditorTests.cs`. Formal root EXE SHA remains `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. Paired `battle_world.cpp::resolve_unarmored_reaction`, unarmored/reduced hit blocks, and `append_confirmed_native_spark` are the rule evidence. Unity `BattleNativeHitSparkWriter.Append` is the corresponding production reader of the native CRT stream. No production writer, DAT value, image, Scene, camera, config or nonbattle script changed in this package.

The previous neighboring run selected 21/21 and failed old expected values, but its three reduced cases included a transient parameterized assembly. The current source has 18 standard and two reduced cases. Before correction, current-assembly job `be3c26b03877474da691de0c0995aabe` selected 20/20 and failed old `HitStateCount=45/15` and `FallCounter=0` expectations. The formal unarmored hit sets bdefend to 45; the reduced hit adds bdefend 10 with the fixture's zero initial bdefend; a 70-fall or lethal ordinary hit reaches timer 80. Only those explicit expected values were corrected.

After that correction and a fresh original Editor compile, job `0575071ec8034d8e9fa3f5797fbce2e2` selected 20/20 and failed with a uniform X-coordinate mismatch, expected 12 versus actual 9. Exact-case job `5d45bffb360640fd8c678aa9ef7da662` reproduced it. A temporary two-case direct wrapper, job `cbfa79f34bc046509d3519a13de938f5`, captured NUnit stacks at `GetHitRecordX` in both methods. Its own 2/2 PASS means only that the wrapper caught/logged exceptions, not that the underlying cases passed. The temporary wrapper was then removed.

The old fixture seeded and asserted `world.Rng`, but the formal spark tail and Unity production writer use two draws from the distinct native CRT stream. The two test methods now reset `world.NativeRandom`, compute expected Y/X jitter from its CRT state *after* table initialization, and assert native CRT final state/call count. The actual production paths were not changed. Original Editor current-assembly job `e093dd020cbf40c4888d216c5a740790` selected the exact 18+2 cases and **passed 20/20**, including plan-valid and writer-mask-zero gates. The temporary probe was removed afterward; original Editor recompiled at 14:02:24 local and `read_console` returned zero errors, while `get_tests` found zero probe tests. The 20 cases were not rerun after removal because their method bodies were unchanged; this is the test-evidence boundary.

This closes only historical test assertions for this Q07 ShadowCompare subset. Prior formal EXE/Unity X550 1428/1428 and X1200 2100/2100 raw production evidence belongs to earlier packages and was not rerun here. Natural Battle Play, D-024 scene-distance ratio, remaining hit branches, Q07 content retirement, formal GPU and full Q07/Q09 exits remain open.

Final governance and scope checks: `Tools/Validate-ChangeLedger.ps1` PASS (869 records, 29 governed code files in the aggregate dirty diff); `git diff --check` exit 0, with only Git's LF→CRLF working-copy warnings. The three restored progress documents (`ntsd28-logan-vs-unity-battle-alignment.md`, `CODEX-CURRENT-HANDOFF.md`, `docs/ai/STATE.md`) contain zero NUL bytes. Protected Menu Scene, Battle Scene, GameConfig asset and ProjectBattleModeConfig asset SHA-256 remain respectively `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`, `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`, `0527D737A1FA38FC56B51D00DC6E96A421D3C67222546368B147C2D074CB8EA7`, `88E10D43B047952FD3053A87B7E5F60D0F87A6CD1A23313F37EA1503C686F55C`.
