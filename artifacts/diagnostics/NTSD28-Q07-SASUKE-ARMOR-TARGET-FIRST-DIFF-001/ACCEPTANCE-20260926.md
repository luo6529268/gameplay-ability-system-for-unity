# OID87 type-1 armor target: source/root EXE/Unity first difference

Status `VERIFIED_SCOPED_DIAGNOSTIC / FIRST_DIFFERENCE_FOUND`. This diagnostic does not change production and does not close Q07/D-024.

The root formal EXE SHA-256 remains `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. Indexed OID87 `c/sasu/sus.dat` formal and staged Unity decoded bytes have SHA-256 `40D57B84AC362E24270DE66AA0B53862ABC9D70FF326250AFFF45AFFBED4D28C`. Paired source C++ diagnostic compiled with exit 0. Both X550/X1200 source-authored 26-tick LFR cases replayed through the root EXE and reports say `passed=true`, `completedTicks=27` including initial row, `nativeParityClaim=false`. Original Unity Editor PID11944 recompiled with zero Console errors and its strict X550/X1200 raw capture requests both returned PASS. No second project or computer-use was used.

The actor is OID11 Sasuke/action110/X500, target OID87/X550 or X1200/current MP300, same seed `0x28A55A5A`, formal Stage23 and defend/right/attack at completed ticks2/4/6. Formal X550 at tick15 has four OID440/action2 children. At tick16 all four hit: the root-EXE event records HP damage `[3,3,35,35]`, proving the first two use the reduced/armor path and the last two use ordinary damage after armor response. Target HP500→424/action186 and attacker MP400→500/target MP300→404. X1200 has no hit, target HP500/MP300 and attacker MP400 at tick26.

`entity-comparison.json` compares 21 common fields for every occupied entity over 26 completed ticks. X1200: 100 rows, 2100 comparisons, zero differences. X550: 68 rows, 1428 comparisons, 34 differences. The earliest is completed tick16 attacker current MP formal500/Unity452; same tick target current MP formal404/Unity352. These 52-point deficits equal two missed injury rewards of 35×75% truncated to26 each. Target HP/action and four child lifecycle match. Current Unity `BattleDamageWriter.ApplyAlternateDamage` writes reduced vital/credit fields but omits the formally called resource transaction; subsequent unarmored hits already use the prior shared fix. This is a generic selected-armor/reduced branch, not an OID87-only rule.

The same trace has a later, independent position difference: from tick20, target `preciseX` is formal558.4 versus Unity558; by tick26 it is formal602.8 versus Unity600, and integer X differs from tick22. The first-difference diagnosis must not hide this behind the resource fix. D-024 physical/source-coordinate and hit-motion ownership need separate evidence before changing movement; it is not attributed to resource settlement.

The diagnostic-only script additions were the new C++ harness and a distinct strict Unity raw schema. No production script, DAT numeric value, image, Scene, camera, ProjectSettings or nonbattle file was edited. Root EXE GUI-subsystem invocation returned before its report appeared; the first immediate report check failed, then both reports were observed and parsed as PASS. Preserve these pre-fix traces; any production repair requires a new Task/Change and new post-fix output names.
