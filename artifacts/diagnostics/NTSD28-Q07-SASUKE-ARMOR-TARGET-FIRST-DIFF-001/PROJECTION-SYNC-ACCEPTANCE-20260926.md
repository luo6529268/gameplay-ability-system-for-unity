# Q07 hit ShadowCompare projection: scoped result

`NTSD28-Q07-HIT-PROJECTION-RESOURCE-MOTION-SYNC-001` is `FOCUSED_TEST_PASS / SCOPED_SHADOW_FIELD_PARITY`. Formal root EXE SHA-256 remains `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The preceding Q07 packages made the production standard/reduced hit writer's PP transfer and reduced-ground fractional horizontal impulse match the formal 26-tick X550/X1200 raw scenarios. This package synchronizes the separate ECS ShadowCompare expected-effect projection; it does not edit the production writer or those prior raw traces.

Before editing this projection, original Editor existing reduced ShadowCompare two cases failed 2/2 at `CurrentTickPlanValid` with writer mask `0x20000000000000` (job `eb4d108e6e0c4cbdacefa5dead4c74ab`), the target PP/vitals comparison group. The projection had omitted resource transfer and integer-divided odd `dvx`. A temporary parameterized test edit initially caused CS7036 at two outside callers; this was corrected, then original Editor RED job `6155fc68c3894b889472ea1a85df4c40` failed 3/3: existing two masks `0x20000000000000`, new odd case `0x20200000000000` (PP plus horizontal impulse). The old test signature and its other expectations were restored; the final focused test is separate.

`BattleEcsHitExecutionPlan` now projects type-0 resource owner/target PP and consumption after standard or reduced hit credit using captured handles, the same per-hit injury resolver and active mode rule values as the writer. It handles owner-target alias in the projection carriers and uses `double dvx / 2.0` for reduced ground hits. No object-id or skill condition was added. Original Editor compiled with zero Console errors. New `ShadowCompare_ReducedOddGroundDvxAndResourceMatchWriter` (odd `dvx=-5`, owner PP400, target PP300) passed 1/1 in job `06a8d15c637c49529663935f073df0cc`: one writer observation, difference mask0, valid plan, pending-X -2.4 and both current PP rewards observed.

The neighboring existing standard/reduced ShadowCompare selection ran 21 tests in job `7843cb4886ea46c684e0694dacfdd7d8`. **The full run failed 21/21.** In every case the test progressed past its plan-valid and writer-mask-zero assertions, then failed on later old expected values such as `HitStateCount` or bdefend. Their pre-package baseline was not independently measured. They are recorded as open test/authority expectation work, not silently rebaselined to Unity output and not attributed as newly fixed. The 1/1 focused test is the passing exit for this package; the 21-case failure limits any broader claim.

The production X550/X1200 1428/1428 and 2100/2100 mapped-field proof was completed before this diagnostic-only source edit and was not repeated afterward. Natural physical Battle Play, source-coordinate scaling in the project scene, formal EXE GPU and full Q07/Q09 exits remain separate. No DAT, image, Scene, camera, config asset or nonbattle source was changed here.
