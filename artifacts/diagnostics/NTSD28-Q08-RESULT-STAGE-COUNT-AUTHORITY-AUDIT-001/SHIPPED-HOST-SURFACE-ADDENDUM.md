# Shipped EXE host surface for mode-4 result-settings stage action

Status: `CURRENT_AUTHORITY_SURFACE_ABSENT / NO_UNSUPPORTED_VALUE_CHANGE`. This addendum sharpens the previous `SAME_MODE_EXE_BEHAVIOR_PENDING` next step; it does not prove a result-settings wrap value or change Unity behavior.

The root formal `NTSD2.8-Logan.exe` was freshly hashed as SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. In its matching playable source, `main.cpp:631–835` parses the supported switches. It exposes character, enemy, background, difficulty, GUI acceptance, headless smoke and LFR playback/report/trace; there is no switch to enter a mode-4 result-settings cursor or inject its stage Attack action. `main.cpp:844–850` expressly selects a battle-scene-only loop for ordinary playable/smoke/GUI routes and freezes LFR playback at the source transition boundary. `wWinMain` dispatches only those three routes at `main.cpp:2928–2932`. `run_windowed` initializes `GameSession28` directly at `main.cpp:2251`, and `GameSession28::step` handles battle result transitions rather than publishing a mode-4 result-settings stage menu in the inspected live path.

Consequently, the earlier instruction to obtain a same-mode stage-action witness from an existing **supported** formal EXE interface is not currently executable. Injecting Windows input or reading pixels would not create a missing stage-settings frontend state in this shipped host. The source's 24 background IDs remain a post-roster selection list, not a result-settings count. Unity's zero `RuntimeStageCount` remains a static candidate difference only. Do not write 24, delete the Unity consumer or invent a wrap rule from this absence.

Q08 can continue source-backed stage, KO and event obligations independently. The mode-4 result-settings count is parked as an authority-surface question: either obtain a newly identified current EXE route that actually exposes that state, or receive an explicit user decision to retain/replace the Unity-only behavior as an exception. This addendum does not request such a decision now and does not block Q07 or the other Q08 packages. No computer-use, executable modification, source build, Unity run or project code/resource edit was made for this audit.
