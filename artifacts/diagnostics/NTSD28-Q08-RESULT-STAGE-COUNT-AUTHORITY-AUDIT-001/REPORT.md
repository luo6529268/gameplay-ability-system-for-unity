# Q08 result-settings stage count: authority boundary — 2026-09-22

Status: `VERIFIED_SOURCE_BOUNDARY / CURRENT_AUTHORITY_SURFACE_ABSENT`. Formal EXE SHA-256 was freshly checked as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The shipped host surface is further audited in [SHIPPED-HOST-SURFACE-ADDENDUM.md](SHIPPED-HOST-SURFACE-ADDENDUM.md). This report is read-only; no Unity or formal EXE runtime test was run for this result-settings question.

## What the playable source proves

- `source/ntsd28_playable/src/game_session.cpp:4090` builds `available_background_ids()` from nonempty native DAT paths; the prior Q07 audit established 24 unique formal `data/data.txt` IDs 0–23 and present DAT files. `GameSession28::step` passes those IDs into `PostRosterMenu28` at lines 2877–2882. `selection_flow.cpp:23–42` sorts/deduplicates usable IDs and appends the random sentinel 100; `cycle_background` at line 97 cycles this vector. The source test at `game_session_tests.cpp:343–363` exercises last concrete → random → first concrete for the menu, including a sparse `{0,1,23}` vector. This is a **pre-battle post-roster menu** rule, not a result-settings count rule.
- `ntsd28_core/src/simulation/battle_flow.cpp:44–123` sends a completed ordinary result to transition state 2 and mode 4 to state 202. Its `upper_scene_entered` continuation is explicitly implemented for state 2 in the `result_complete` branch. `game_session.cpp:2748–2782` enters the source-modeled upper selection flow from that continuation; the inspected playable source has no corresponding mode-4 result-settings stage action or count transfer at state 202. `battle_flow_tests.cpp:78–96` asserts the mode-to-transition number only, not the later settings behavior.
- `ntsd28_playable/src/main.cpp` exposes headless smoke and LFR playback/report/trace arguments in the inspected parser; no mode-4 results-settings stage selection argument or report field was found. This is an inspected-surface limit, not proof that the formal interactive EXE lacks the native behavior.

## Unity path and comparison limit

- `Assets/NTSD/Scripts/Simulation/Host/SimulationTickDriver.cs:1605–1621` sets `Match.RuntimeStageCount` from `GameDataManager.BackgroundCount`. After Q07 retired the singleton's implicit old `data.txt` read, formal object publication creates a new `GameDataConfig` without formal background rows. Under that observed code path the count is zero. This is static source reasoning, not a newly measured result-mode Play value.
- `Assets/NTSD/Scripts/Simulation/Ecs/Results/BattleResultsWriter.cs:151–189,400–428` consumes that count only when results phase 202, SettingsCursor 3, and Attack selects the stage action; zero suppresses its last-concrete → random transition. `BattleRuntimeSelfCheck.cs:26540–26549` injects a synthetic count of 3 and proves the algorithmic branch, not the formal content count or the official EXE's same-mode outcome.
- The native menu's 24 available IDs and Unity result-settings `RuntimeStageCount` are different variables in different UI phases. The evidence does **not** establish that 24 is the formal result-settings wrap count, nor that zero is an observed first difference. Do not write 24 or alter `AdvanceStageSelection` from this audit.

## Exact next exit

No supported route in the current shipped battle-scene host exposes this result-settings action: ordinary GUI/smoke loops omit that frontend, and LFR playback freezes at the transition boundary. Do not pursue a purported same-mode EXE result witness until a genuine current-authority route is identified. Preserve the Unity consumer unchanged and do not infer a count from the 24 post-roster background IDs. If an actual formal route is later found, compare identical mode/stage/input before declaring a fix; otherwise this is an authority-surface/exception decision, not a reason to invent a rule. Q08's other source-backed stage/flow items can proceed. The default `stage.dat` deployment hold and approved background presentation exception remain.
