# Q08 living-group contract and carrier audit — 2026-09-22

Status: `NATIVE_PLAYABLE_FULL_DRIVER_6_PASS / UNITY_FOCUSED_RED_4_CASES / NO_PRODUCTION_EDIT`. This is a Q08 G-05 dependency audit, not a formal-EXE-versus-Unity full-session certificate. Q06 local exit stays closed.

Current playable source: `GameSession28::step` calls `BattleFlow28::step` before combat update. `battle_flow.cpp::classify` iterates character entities, accepts `battle_group` 1..39 except 5, includes HP0 when `revive_lives_30c>1`, retains each distinct eligible group, sorts the vector and chooses a winner only when exactly one remains. Its `step` starts the result timer when fewer than two eligible groups remain even on the first classification; it does not require a historical two-group latch. The fresh native `battle_flow_tests.exe` had already passed; its neutral/zero-group case covers part of this source contract. The formal root EXE identity is `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`.

The isolated Unity 2022.3.62f3 project copy (separate Library) compiled and executed four newly focused cases against the current producer, after the separate HP/revive-lives fix:

| Initial groups and source-defined expectation | Current Unity observation | Evidence |
|---|---|---|
| Eligible group 1 plus invalid 0 | Result timer should start at 1 | `BattleEndPhase=0`; [INVALID-GROUP-RED.xml](INVALID-GROUP-RED.xml) |
| Eligible group 1 plus excluded story group 5 | Result timer should start at 1 | `BattleEndPhase=0`; same XML |
| Eligible group 1 plus out-of-range 40 | Result timer should start at 1 | `BattleEndPhase=0`; same XML |
| Eligible groups 1, 2 and 3 | Keep three distinct living groups, timer 0 | `TeamCount=2`; [THREE-ELIGIBLE-GROUPS-RED.xml](THREE-ELIGIBLE-GROUPS-RED.xml) |

All four tests reached the target assertion (3/3 and 1/1 intentional failures); [isolated test source](ISOLATED-GROUP-FIXTURE.cs.txt) is archived for reproduction. These are direct `UpdateBattleResultsFlow` observations using the existing registered-character fixture. They prove Unity differences against the current source-defined rule; the original project test file and production scripts were not changed for this audit.

An isolated copy of the matching playable source now runs six group cases plus timer, combat-lethal and mode-4 cases through `GameSession28::step()`. [Fixture](native_game_session_flow_fixture.cpp) SHA-256 `C8465D5C610B1AEB097B4E4AF8FB381580BCF96A985725ED1093E9E732D95B49`; isolated console executable SHA-256 `1941A03F0A311A59E69BD65807F45F0711E5FF6BD9C655BE3290B174B188589C`. `build.ps1 -Target game_session_tests` succeeded, and two runs produced identical [output](NATIVE-FULL-DRIVER-6.txt): invalid 0/5/40 plus 1 => `[1]`, winner 1, timer 1; 1/2/3 => all three, no winner, timer 0; 0/5 => no groups, no winner, timer 1; HP0/revive2 group 1 with group 2 => both groups, timer 0. The complete-session timing case starts with `[1]`/timer 1, restores the other entity to group 2, and observes timer 2 with the original winner still latched. It then observes the end signal at 80, result record at 101, and Attack at 144 jumping to 350 with transition started and timer-after 0. A separate existing formal natural-attack setup (character 2 versus object 510, close positions, target HP20, physical Attack pulses at ticks 5/6, two eligible groups) produces a combat-caused lethal hit on tick 12; `GameSession28::step()` still reports result timer 0 on the lethal tick and 1 on tick 13. This directly witnesses the pre-combat classifier boundary. A mode-4 complete-session case with one living group starts timer 1 and reaches timer 350/transition command 202; its input has no Unity-style committed reserve payload, so it is not a matching reserve comparison. See [mode-4 caller audit](MODE4-RESERVE-CALLER-AUDIT.md). The formal root EXE hash was freshly checked as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The first attempt used `CombatantConfig.team=0`, which the host normalized to 11; that failed trace is not a matching-state comparison. The accepted group fixture sets `entity->battle_group` after initialization and before the complete step. The source was copied to a separate directory, and its authority original was not edited. These results establish the playable full-driver source behavior for the group and timing states, but still do not establish a formal-EXE pixel/UI result or full cross-engine identical-world trace.

The Unity `BattleResultsOutcomeHostWriter` still has persistent two-bucket `TeamIds`, `TeamCount<2` and `HadBoth` logic, plus the mode-4 reserve callback and phase-11 activation. `BattleResultsRuntimeState.TeamIds` is fixed to length 2; `ActivateSummary` accepts only two team IDs and resets the live guard. This array also participates in two-side result UI state, not merely living-group classification. `BattleWorldRosterResultsSnapshotBuffer` fixes `ResultSideCount=2`, copies these IDs through capture/restore, validates a canonical two-element array, and currently uses schema version 1. `BattleLockstepChecksumModule` hashes `HadBoth`, phase, count and the full array; `BattleParitySnapshot` exports the same fields. `BattleRuntimeSelfCheck` still asserts group 0, groups 61/62 and ignored third group as historical behavior; its mode-4 reserve fixtures seed the old guard. Therefore changing only the invalid-group predicate cannot produce the native timer: with one eligible group the existing `HadBoth` gate still returns. Simply resizing `TeamIds` would also change result UI, snapshot shape, checksum and old fixtures. This is an implementation-dependency conclusion from current code, not a claim that a particular replacement structure is already approved or implemented.

Next executable scope: define a separate source-matched living-group/timer carrier and its reset, snapshot, checksum and parity identity contract, while preserving the two-side result UI and the already tested after-world result-page input owner. The classifier must run before the combat update, while result-page input remains at its existing after-world owner; the playable complete driver now directly witnesses a combat-lethal tick with timer 0 followed by timer 1. A latched winner and timer must persist if another group becomes eligible after timer start. Unity's group-rule and monotonic/early-page focused RED cases are captured, including a complete Unity tick monotonic case. A corresponding Unity combat-caused lethal-hit full-tick RED, exact 80/101/350 and 144-continue cases and mode-4 reserve interaction still need to follow before or as part of the precisely recorded implementation. Validate focused tests, existing result UI seams, snapshot restore/replay and a representative real battle; update only old SelfCheck assertions contradicted by proven source behavior. The result-page visual exception and absent mode-4 result-settings stage-count authority remain separate Q08 exits. No Scene, resource, deletion, nonbattle or computer-use action occurred in this audit.
