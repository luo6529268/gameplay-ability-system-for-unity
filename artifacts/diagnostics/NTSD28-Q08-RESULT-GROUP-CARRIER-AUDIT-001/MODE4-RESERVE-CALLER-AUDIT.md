# Q08 mode-4 reserve caller boundary — 2026-09-22

Status: `PLAYABLE_MODE4_FLOW_WITNESSED / COMMITTED_RESERVE_MATCHING_INPUT_UNAVAILABLE / NO_PRODUCTION_EDIT`.

The formal EXE SHA-256 is `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; its matching playable source includes `GameSession28::step()` and `BattleFlow28::step()`. `GameSession28::step()` calls the battle flow before the entity combat update for non-story battle scenes. `BattleFlow28::classify` uses the same valid-group/HP/revive rule for mode 4; `BattleFlow28::step` maps timer 350 to mode-4 transition command 202. An isolated full `GameSession28` mode-4 case with two combatants, group 2 HP0/revive1 and group 1 living double-ran identically: winner group 1, first timer 1 and at 350 transition 202. See [native fixture and output](NATIVE-FULL-DRIVER-6.txt). No formal EXE visible mode-4 result was captured.

Unity's production `BattleResultsOutcomeHostWriter.UpdateSummaryActivation` has an additional mode-4 branch after its two persistent `TeamIds`/`HadBoth` bucket scan. If a side is empty, it calls `SimulationWorld.TrySpawnBattleResultsReserveBeforeResults`, implemented by `BattleResultsReserveHostWriter.TrySpawnBeforeResults`, which consumes `ResultCommittedTotal/Hp` and may spawn reserve entries through `SimulationStageWaveModule`. `BattleRuntimeSelfCheck` `RESULT-RESERVE-09` explicitly seeds `HadBoth`, two side IDs, phase 6 and committed reserve, expects a new slot-20 character, RNG +1 and guard reset to zero, then expects the next scan to pause the guard. These are current Unity behaviors and existing regression contracts, not proof that the current formal playable performs the same transaction at that point.

The inspected playable `BattleConfig28` exposes battle mode and selected story reserve, but no ResultCommittedTotal/Hp two-side input corresponding to that Unity self-check. Focused searches of the playable `GameSession28::step` route and core `battle_flow.cpp` found no equivalent committed-results reserve callback. This absence in the inspected source surface does **not** prove that every formal EXE mode-4 setup lacks reserve behavior, nor does the isolated native case reproduce Unity's committed-reserve precondition. It does prove that the currently tested playable mode-4 flow itself starts its timer and reaches command 202 without that Unity precondition.

Implementation boundary: do not make the new ordinary native timer depend on historical `HadBoth` or the two-side reserve buckets, and do not silently delete or re-time the Unity reserve transaction based only on this negative source search. Preserve its existing owner while adding a separate native result-flow carrier; keep a distinct Q08 mode-4 return to obtain an actually matching reserve configuration or current-authority observable trace before declaring reserve semantics aligned or retiring that branch. The mode-4 result-settings stage-count action is a separate absent playable-host surface and must not be inferred from pre-battle background IDs. No formal source, production script, Scene, content, nonbattle or computer-use action occurred in this audit.
