# Q08 story selector: caller audit and corrected test oracle — 2026-09-22

Status: `PREVIOUS_STAGE_PROXY_RED_RETRACTED / CURRENT_MENU_DIRECT_ONLY_STATIC / CORRECTED_EDITMODE_5_PASS_0_FAIL`.

The preceding `STORY-SELECTOR-UNITY-RED.md` remains an honest record of what the test first observed, but its assertion equated Unity `StageProgressionValid` with the formal playable host's paired `story_mission_id` plus `story_child_stage_id`. That equivalence was not established. Therefore its 4PASS/1FAIL result is **not an authority-backed story-selector first difference** and must not drive a production gate. The observed phase1 is only the behavior of a Unity World with a configured campaign and no formal-equivalent story selection field.

Current caller evidence:

- `CharacterSelectionController.StartBattle()` and `ConfirmMatch()` each create `MatchConfig` with game mode, background, difficulty, seed and players. Neither sets `stageCampaignFilePath`/`stageSeriesId`; project-wide script search finds no `MatchConfirmed` subscriber that mutates this config.
- `AppManager.InitializeBattleAsync()` passes that config to `SimulationTickDriver.ApplyMatchConfig()` before setup. The driver loads `config.stageCampaignFilePath` and configures `StageProgressionValid` from a selected campaign. The only observed script assignment of `stageCampaignFilePath` is the explicit `BattleRuntimeSelfCheck` stage fixture. Other code can call `SetMatchConfig`, so this is a current-menu static conclusion, not proof no external caller can ever supply a campaign.
- `MatchConfig` has no fields for the formal playable pair of story mission/child IDs. The formal host validates and tests that pair independently of `battle_mode`; its `GameSession28::step()` skips `BattleFlow28::step` only when both IDs are selected. Unity's stage campaign path and selected series are a separate data contract.

The declared Q08 test Change corrected only the previously appended stage proxy method: it is now `ModeOneConfiguredStageWithoutStorySelectorKeepsOrdinaryGuard` and expects current phase1, while the mode1 direct control remains. Isolated Unity 2022.3.62f3 EditMode [corrected XML](UNITY-STORY-SELECTOR-CORRECTED.xml) compiled and passed 5/5, including the three old revive-life controls. Corrected isolated test SHA-256 `F452B1CBFD339465CE5EA5D8B617E3337DBF5CA35D4907448855E56E0825F92E`; original project test SHA-256 `02A957D9CE3593378607C72573D19726A15057210EF2D023AAF3DE2F858A77AE`. Unity log has no `error CS...` or compilation-failed marker. Original Battle Scene SHA-256 remains `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`.

Production consequence: the current menu's direct mode1 battle must remain eligible for ordinary result classification. Do not infer a story session merely from `BattleGameModeId==1` or `StageProgressionValid`. To support a formal story child later, define an explicit selected-story session input/owner, prove its menu/host caller, then include that deterministic flag or paired IDs in reset, snapshot and checksum before skipping ordinary BattleFlow. Q08's separate group/timing RED cases remain valid and may advance for the current direct battle path. Formal story Play and full Q08 acceptance remain open.
