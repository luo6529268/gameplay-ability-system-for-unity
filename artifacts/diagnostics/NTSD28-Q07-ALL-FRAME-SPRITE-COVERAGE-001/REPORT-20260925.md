# Q07 all-frame sprite source census (2026-09-25)

Status: `VERIFIED_STATIC_SCOPE_ONLY`. Formal root EXE SHA-256 was rechecked as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; `source/README_SOURCE.md` declares the paired playable source. The read-only audit compared the current staged LoganRuntime with formal `resources/runtime`, and saved per-frame exceptions to `frame-sprite-static-audit-20260925.json`.

The JSON SHA-256 is `BED66260A205468BB71AEA09A40E82AB2940C12B63EDC099012D737494A025A9`. No project script, DAT, PNG, Scene, Prefab or importer was changed for this census.

| Current staged/formal-equivalent data | Count |
| --- | ---: |
| DAT with numeric frames and BMP section | 330 |
| Declared image-sheet rows | 773 |
| Numeric frame records | 55,347 |
| Base `pic` within native cumulative sheet capacity | 50,676 |
| Authored hidden sentinel `pic:999` | 4,587 |
| Other base `pic` beyond native sheet capacity | 84, across 21 DAT |
| Unique source PNG paths referenced by the 773 sheets | 703 |
| Same-path DAT/PNG missing or SHA mismatch | 0 |
| In-range frame source rectangles exceeding actual PNG dimensions | 6, across 4 DAT |
| Sheet lines whose authored endpoints differ from native cumulative endpoints | 40 |

The critical parser rule is formal `source/ntsd28_core/src/data/dat_parser.cpp:192-222`: the native sheet starts at 0 and each next start is the prior `row*col` capacity; authored `file(first-last)` endpoints are retained for diagnostics but do not set runtime ranges. Formal `SpriteFrameResolver28::resolve` in `source/ntsd28_core/src/rendering/render_snapshot.cpp:1105-1153` uses those cumulative ranges, and its `is_native_hidden_pic` returns hidden when no range covers the value. Unity's Logan source branch in `CharacterAnimtorManager.BuildSpriteFilesForSource` likewise computes cumulative `row*col` before catalog construction. An initial scratch scan using authored endpoints reported 425 out-of-range frames; that number is invalid for runtime and is replaced by the 84 non-999 native-range cases above. No DAT value was changed to make the count smaller.

The six in-range but outside-image base rectangles are `c/saso/pup.dat` frame105/pic44; `m/nin/hun.dat` frame95/pic64; `m/nin/nin.dat` frames31/pic81 and41/pic91; and `m/nin/nin2.dat` frames31/pic81 and41/pic91. The formal resolver creates a source frame from sheet capacity; Unity `CharacterAnimtorManager.BuildIndexedSpriteRects` clips to actual PNG bounds and drops an empty rectangle. These six require targeted dynamic reachability and formal renderer sampling checks before any parity claim. A base `pic` can also be shifted by runtime visual offset (`render_snapshot.cpp` near `base_pic + revive_visual_runtime_318`; Unity `LF2Entity.GetRenderPicIndex`), which this static census does not enumerate.

One precise dynamic candidate is `c/saso/pup.dat` (formal `data.txt` OID55, type0): authored frame104 has `next:105`, and frame105 has `pic:44`, whose source rect starts at y320 while `c/saso/pup.png` is only 319 pixels high. This proves a DAT transition edge and an out-of-image base rect, not that a selected battle reaches frame104/105, that the formal EXE displays a pixel there, or that runtime visual offset is zero. Use this as a targeted reachability check rather than altering the DAT or inventing a fallback.

This audit proves that the 330 decoded DAT and all 703 source PNG inputs required by their sheet declarations are present byte-for-byte in the current staged formal root. It does not prove every frame draws, that all runtime visual offsets resolve, or that Unity and formal EXE pixels match. The 84 non-999 out-of-range records may be intentionally hidden in the formal renderer; do not repair their DAT numbers or add fallback image logic without same-state evidence. The next focused Q07 task should select a reachable one of the six bounds cases or 84 range cases and compare formal release visibility with Unity publication/rendering, while leaving old Config/image files protected. Q07, Q09/R17 and the parent goal remain open.
