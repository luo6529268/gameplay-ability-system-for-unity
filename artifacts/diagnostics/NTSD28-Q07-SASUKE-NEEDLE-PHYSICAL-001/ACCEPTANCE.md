# Q07 Sasuke needle physical-input witness (2026-09-22)

Decision: `VERIFIED_SOURCE_FORMULA_AND_SCOPED_PLAY_ONLY`. The new Editor-only request used the already open `NTSD_Battle` scene, the formal GameConfig runtime root and a temporary unsaved first-player override to selectable Sasuke OID 11. It did not edit the Scene or production combat scripts.

`q07-sasuke-needle-1.json` PASS: physical L/D/J registered at ticks 2/4/6, authored target frame 261 at tick 6, frame 264 and four OID 440 at tick 15, and at least one frame-1/pic-0 formal `c/sasu/a/chi.png` central binding. Baseline world object count 2; peak 6; four distinct child stable IDs. The request was marked `requested:false`; no prior result was overwritten.

After adding birth-state capture, `q07-sasuke-needle-2.json` independently PASS with the same physical-input/frame/object progression. The first four-child observation at tick 15 recorded parent integer position `(800,0,714)` and four right-facing children. Source `ObjectSpawnPlanner28::plan_frame` with formal frame-264 point `(x40,y46,z0,dvx40,dvy4,dvz0,oid440,action1,facing40)` and parent frame center `(39,79)` predicts child position `(801,-33,715)` for each under the observed right-facing setup, and four `(vx,vy,vz)` vectors `(35,4,-5)`, `(38.333333333333336,4,-1.6666666666666665)`, `(38.333333333333336,4,1.666666666666667)`, `(35,4,5)`. The independently calculated values match all four captured children (stable IDs 101–104, slots 50–53) within absolute `1e-12`. At the sampled end-of-tick observation, their frame was 2/pic 1; the separate live pic-0 binding flag proves frame-1/pic-0 was observed during the probe, not that these birth rows themselves are action 1.

Validation: current Editor compiled after each test-only script revision; `read_console` returned zero error entries. Both Play requests exited to Editor idle/outside Play. Active `NTSD_Battle` Scene is `isDirty:false`, rootCount14, SHA-256 `BCD1047BF912C6A4A8BC9F3A76EAF3FA954211AD064E0402B1C01BF3BA0E9FB6` before and after. `pwsh -NoProfile -File Tools/Validate-ChangeLedger.ps1` PASS (653 records, 15 governed code files). No full SelfCheck or broad character suite was rerun because only an Editor diagnostic changed. Explicit pool/worker shutdown counters were not captured by this probe; the observed exit to Edit Mode and scene cleanliness do not substitute for that separate Q11/Q12 gate.

Limit: this compares a real Unity natural-input representative with the current formal playable source formula and formal content. It is not a same-condition original EXE tick trace or proof of collision, hit damage, whole projectile lifetime, screen pixels, sorting, shadow, or all Sasuke skills. Q07 and the master alignment goal remain active.
