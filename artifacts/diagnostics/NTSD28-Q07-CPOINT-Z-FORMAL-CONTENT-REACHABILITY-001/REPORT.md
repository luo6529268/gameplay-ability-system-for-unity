# Q07 formal `cpoint.z` content reachability (2026-09-25)

Status: `VERIFIED_STATIC_SCOPED`; no production or DAT edit. The formal root EXE SHA-256 was freshly verified as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`.

The paired playable `BattleWorld28::settle_catch_relations` in `source/ntsd28_core/src/simulation/battle_world.cpp` branches on `catcher_cpoint->z == 0` around line 6182: zero uses the cover-dependent ±1 depth placement; nonzero adds the explicit local `z` at line 6191. `source/ntsd28_core/src/simulation/combat_records.cpp` around line 146 reads `z` through `value_or_zero`, so an omitted field becomes zero. The nonzero branch is real source behavior, but its current-content reachability was the open question.

A read-only PowerShell sweep enumerated every `.dat` under the formal release `resources/runtime/decoded_dat`, read each file and matched complete `cpoint:`…`cpoint_end:` blocks with a single-line/dot-all regex. It counted **405 DAT files, 4,019 CPoint blocks, zero blocks with an explicit `z:` integer token, and zero nonzero values**. A separate `rg -n 'cpoint:.*\bz:' ... -g '*.dat'` also returned no same-line hit. The complete-block sweep handles multi-line blocks, unlike that secondary check. The formal `data/data.txt` indexed 330 object definitions are within this larger decoded-DAT corpus, so the scan is conservative for current declared content.

Consequently, the D-024 note about Unity's possible difference for *nonzero* `cpoint.z` is **dormant under the inspected shipped DAT bytes**. It should not prompt a speculative production branch change or a DAT edit. This is a static content conclusion: dynamically created/overridden definitions, uninspected external content, future DAT changes and other CPoint behavior remain outside it. Existing zero-`z` grab/held/throw focused witnesses stand on their own; this audit supplies no new Unity compile, Play, formal executable input replay, pixel or Q07 aggregate certificate.
