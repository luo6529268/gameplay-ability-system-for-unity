# 2026-09-25 progress-document recovery checkpoint

This is a preservation and review package, **not** permission to replace the three live documents. At 01:17:40 local time, the live alignment, handoff, and STATE files each became a short UTF-8 prefix followed by NUL bytes. Their exact damaged bytes are in `*-corrupt.bin`. The `*-head.md` files are raw Git `HEAD` blobs from commit `d18e1f5c` (2026-09-24 23:48:11 +0800), so they are readable baselines but **not** complete reconstructions of uncommitted updates after that commit. The live files were not overwritten or removed.

| Live file | Bytes | First NUL offset | NUL bytes | Preserved copy | Git baseline |
| --- | ---: | ---: | ---: | --- | --- |
| `Assets/NTSD/Docs/ntsd28-logan-vs-unity-battle-alignment.md` | 723857 | 446 | 723411 | `alignment-corrupt.bin` | `alignment-head.md` |
| `Assets/NTSD/Docs/CODEX-CURRENT-HANDOFF.md` | 743838 | 297 | 743541 | `handoff-corrupt.bin` | `handoff-head.md` |
| `docs/ai/STATE.md` | 852585 | 240 | 852345 | `state-corrupt.bin` | `state-head.md` |

The Git baseline's Q table reports: Q01 delivered; Q02 load infrastructure delivered; Q03 contract delivered; Q04 behavior retirement delivered; Q05 joint schema/replay delivered; Q06 scoped local exit delivered; Q07 and Q08 in progress; Q09 dependent with independent subpackages; Q10 in progress with selected mode/KO audio evidence; Q11 and Q12 waiting on dependencies. Current task-specific records confirm Q07 is still open and the direct Battle empty-root test change is `CODE_WRITTEN`, awaiting original Editor compile and focused results. The full goal remains open. The baseline contains historical snapshots that are superseded by later surviving Task/Change/diagnostic records; do not use it to revert any completed work or to declare Q07 closed.

Recovery requires reconstructing the post-commit deltas from surviving Task/Change Records, diagnostic reports and, if available, application/editor history. No `git restore`, overwrite, deletion, or branch operation has been performed. The current worktree and Scene files remain untouched by this preservation step.

Later 2026-09-25 Q07 test update: the original Editor consumed the queued direct-Battle two-test request. After a first 1/2 run failed on a missing shared Temp PNG fixture (while the new empty-root case passed), the fixture was reproduced and a second uniquely named request passed 2/2. The package Record/Ledger/Task and recovery addendum now report `FOCUSED_TEST_PASS / GOVERNANCE_BLOCKED / PLAY_PENDING`, superseding this report's earlier `CODE_WRITTEN` snapshot. Exact evidence is `artifacts/diagnostics/NTSD28-Q07-EMPTY-ROOT-BATTLE-ENTRY-001/original-editor-focused-20260925/ACCEPTANCE.md`. This does not repair the three live NUL-corrupt documents or complete Q07.

Three `*-recovery-candidate.md` files now combine the corresponding Git baseline with `RECOVERY-ADDENDUM.md` at the top. They contain no NUL bytes and are a concrete review proposal. The added note explicitly identifies its limits; these candidates are **not** an exact replay of all post-commit edits. Replacing the live files remains pending the user's explicit approval under repository `AGENTS.md`.

Candidate QA at 2026-09-25: the addendum now records the independent Unity-response-file Roslyn compile (runtime and Editor both 0 errors) while keeping the original Editor focused tests pending. The readable Git HEAD `STATE.md` already contains all six IDs reported missing from the NUL-truncated live file. The post-commit `NTSD28-BATTLE-UI-CONTROLS-ACTION-BINDING-001` ID is explicitly added with its Record status `IN_PROGRESS`. A text-level scan found all 272 currently active Change Record IDs in `state-recovery-candidate.md`; each of the three regenerated candidates contains no NUL bytes. This scan is not a replacement for running `Tools/Validate-ChangeLedger.ps1` against recovered live documents and does not prove the candidates reproduce every lost post-commit edit.

Read-only backup search at 2026-09-25: the project file-name inventory found no second complete copy of the three live documents. VS Code `User/History` `entries.json` records had no matching path for these three filenames. The local JetBrains tree exposed no `LocalHistory` directory. This is a bounded search, not proof that a system backup cannot exist. A separately preserved 2026-09-24 handoff corruption sample also has a short readable prefix followed by NUL bytes; it is older than the Git baseline and cannot restore the present lost changes. The three current originals remain at the same byte lengths and first-NUL offsets recorded above. The candidates therefore remain a documented reconstruction, not exact byte recovery, and still require explicit approval before replacing the originals.
