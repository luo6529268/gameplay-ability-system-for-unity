# Refill source witness scope

Implementation written, not built or executed by the delegated writer. Root owns compilation and evidence capture.

`--refill` has 230 declared rows; no-argument 140 and `--release` 1150 retain their previous output schema and default values. Byte identity still requires the rebuilt executable to be checked.

- depletion: 84 = OID 122/123 × child HP -1/0/1/2/5/6/30 × kind1/3 × type6/1/0.
- caps: 72 = two OIDs × child HP5/6/30 × three parent cap tuples × independently declared/implicit parent/child action0. Parent tuples HP/HPBound/MP are 496/498/498, 500/500/500, 100/480/497; baseMaxHP500.
- credit: 18 = OID123/type6, 2F4 -1/0 × child MP149/151/500 × parent MP497/499/500; child HP30.
- non17: 24 = two OIDs × holder state0/3 × kind1/3 × type6/1/0.
- zero_binding: 32 = two OIDs × child HP1/2 × kind1/3 × independently declared/implicit parent/child action0; type6.

Defaults: holder state17, child HP30/MP500, 2F4 -1, parent HP496/HPBound498/MP498. Both actors initially action10 with distinct counter/latch, previous and snapshot10. Initial WPoint destination20 is declared and distinct from action0. Kind1/3 has no DVX. Holder slot0/OID77; child slot70 uses actual OID122 or123 in SpawnRequest and catalog. Only official default refill lists are used. Stage and mode gates reuse the existing explicit configuration.

`params` adds refillMode/group/childOid/holderState/kind/childHp/childMp/credit2F4/parentHp/parentHpBound/parentMp/parentZeroDeclared/childZeroDeclared. Refill entity projections add ordinaryCreditGate2F4/hpConsumed/mpConsumed; consumption counters begin parent13/17 and child19/23 to detect accidental modification. No Unity Zz/native field equivalence is assumed. Raw precise and integer XYZ, motion XYZ, counter/latch and snapshot preserve the existing independent sentinels.

Immediate expected branch counts derived from source (not measured): charging206, HP updates82, MP updates112, exhaustion54, WPoint updates176, kind3 release43. Every exhaustion must skip WPoint even kind3, draw exactly one configured HP/MP exhaustion random event, preserve pre-follow position and Vz, clear native active relations and child/parent links to0, set both raw actions0/counters0 and child weaponHP0. OID122 HP<=0 performs no HP decrement/exhaustion; OID123 still subtracts2 even HP<=0. Non17 follows WPoint without refill. Exact source outputs and independent validator remain required.

The matrix covers selected refill arithmetic/identity/type/zero-frame paths, not all cap/owner/state combinations. Full following tick is captured but is a separate verification obligation from immediate branch arithmetic.

Root extension: ordinary_oid adds12 rows (OID78, types6/1/0, HP1/30, kind1/3), total242. These are non-refill controls even in holder state17. New independent validator models complete before and immediate states without copying actual before; following remains outside its model. Predicted WPoint updates188/kind3 releases49; charging and refill totals unchanged. Validator written, not yet executed by delegated writer.
