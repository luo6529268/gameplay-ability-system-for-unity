# NTSD28-Q09-NATIVE-SPARK-PUBLICATION-001 acceptance

Status: `RUNTIME_PENDING`. Production formal SPARK PNG publication and both resource-selection consumers are written and focused-tested. Natural-hit Play and original-EXE pixel parity are **not** accepted; BATCH-05/Q09, R14/R17 and the total goal remain open.

The configured formal branch now takes the already verified resource index 43, 99x79 dimensions and PNG SHA, decodes the 500x320 SPARK.png, turns only RGB(0,0,0) pixels transparent, and creates 20 in-bounds cells. The 20 central resource keys are a storage index; C01 continues to own raw logical ID 0..99. The shared catalog maps drawable raw IDs 0-4, 10-14, 20-24 and 30-34 to those keys; terminal x9 and source rectangles outside 500x320 produce no visual. Both legacy SpriteRenderer and central HitRecord command paths read the same binding. The 99x79 pivot reproduces native integer left/top offsets (49,39) rather than half-pixel geometric centering. The old BMP branch and its assets remain in place. No DAT values, images, Scenes, Prefabs, nonbattle UI, RNG or lifecycle code changed.

Authority: root formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033` was freshly rechecked. The release live chain is `GameSession28::initialize_resources` -> `render_snapshot.cpp:1635-1662` -> `d3d11_renderer.cpp:1791-1823`; source selection is formal `resource.dat` index43 and `system.dat` spark_w/h. The earlier reachability CSV/report remains the 100-ID geometry inventory.

Original Unity Editor checks: native geometry 1/1 (`83facf6049794962bc869be187f01708`), black key plus geometry 2/2 (`d288d47d9c9140588536f9b0ef8f0601`), adjacent common-atlas class 6/6 (`c371eda3b0b849d0bc2a85313dbbc684`), and real 330-object staged publication/recycle 1/1 after final pivot (`a1e1991531a2486285c5685203a90680`). An earlier 330-object run also passed 1/1 before the pivot change (`1b4c695b9f524365bbcc6c2ec9cec7c1`). The original Editor compiled with 0 final errors; a transient misplaced-method compile failure was corrected before tests. Post-pivot full `BattleRuntimeSelfCheck` result was `PASS` at 2026-09-24T11:13:05Z, archived as `post-pivot-selfcheck-result.txt`. `Tools/Validate-ChangeLedger.ps1` passed with 786 Records and 25 current code files covered; `git -c core.safecrlf=false diff --check` passed. No computer-use or second Unity project was used.

The first actual Battle Scene natural-hit C01 probe failed at tick 3573 before SPARK visual assertions: RNG call delta was 1 where its existing fixture expected 3. The probe reported `cleanupCompleted=true`; its full original result is preserved in `c01-natural-hit-play-first-difference.json` (SHA-256 `B29C5483D2C23D7FDA7CD7AC9CA002C1904A0A1262E142D69D5B1775CF3ABC20`). This cannot be counted as a SPARK Play pass or a SPARK pixel failure. The original Editor exited Play and returned idle. Battle Scene SHA-256 stayed `9409F2BCFE3E657A6C3C88A7527045CC384D50AAACC99197D53AACA38F3B3A39`; Menu Scene stayed `3B0F58AA88BEC495AA999D014CB2779E935B21F0374826357B4DC64AE5B80228`.

Next gate: audit the C01 probe's RNG first difference against the current formal hit-producer call chain without changing render code or test expectations to hide it. Separately obtain a Battle Scene visual witness for both legacy and central branches, 30/60/120 presentation sampling with unchanged logical checksum, stop/reenter/zero-residual, and same-seed original-EXE pixel comparison. Until those pass, this package and Q09 remain `RUNTIME_PENDING`.

Read-only first-difference audit after the failed Play run: the formal playable `battle_world.cpp::append_confirmed_native_spark` draws two times from `NativeRandom28::crt_next` for Y then X. Unity's current `BattleNativeHitSparkWriter.Append` likewise draws two times from `world.NativeRandom.CrtNext`; the Q06 transaction record reports source/runtime CRT and full-driver verification. The older C01 probe instead computes both anchors from `new DeterministicRng(world.Rng.State)` and expects those two calls plus `BattleRandomWeaponDropModule.RunNormalDrop`'s one `world.Rng.NextInt(0,200)` to advance the *same* `world.Rng` three times. Thus its expected random stream no longer matches the Q06 production writer. The observed `world.Rng` delta 1 is consistent with only the random-weapon gate on that stream, but the probe result does not record the native CRT delta or produced coordinates, so the two native calls are not independently proven by this Play attempt. A separate test-only probe correction must capture/restore native scalar state, assert its two CRT calls and Y/X coordinates, keep the one shared-RNG weapon-gate assertion, and then rerun the full Play and presentation evidence. Production RNG/logic must not be altered to satisfy the old fixture.

Follow-up `NTSD28-Q09-C01-PLAY-PROBE-RNG-001` completed that test-only correction and the actual Battle Scene CentralOnly Play passed ticks 1928-1931; each tick showed native CRT2/shared RNG1, and commands 1/2/3/0. Its result and cleanup details are in `artifacts/diagnostics/NTSD28-Q09-C01-PLAY-PROBE-RNG-001/ACCEPTANCE.md`. This does not change the parent `RUNTIME_PENDING` status or remove the legacy/30/60/120/formal-pixel gates.
