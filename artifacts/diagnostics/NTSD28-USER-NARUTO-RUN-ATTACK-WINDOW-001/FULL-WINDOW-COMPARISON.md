# Naruto Rasengan attack window: complete controlled Driver trace

2026-09-23. Status: `SOURCE_MODEL_AND_UNITY_TRACE_CAPTURED / PHYSICAL_PLAY_FIRST_AND_SECOND_253_PASS / USER_SYMPTOM_OPEN / FORMAL_EXE_VISIBLE_PENDING`. This is a comparison of paired playable-source diagnostic model and the original project's real Unity `SimulationTickDriver` in EditMode, plus two scoped physical-input Battle Scene Play probes; it is not a formal EXE visible-play claim.

The formal-source runner used the unchanged paired C++ source and formal runtime DAT. The Unity diagnostic used `NTSD28UnityRawCaptureEditor.WithLoganScenarioForReplayTests`, formal runtime root, Stage 23 seed 682973786, Naruto OID 2 at action 241, OID 7 far away at X=1200, and 32 completed ticks. `J` maps to the crossed Unity `SimulationInputButtons.Jump` carrier. The source scenarios and the Unity three-tick seed plus output JSONL files are in `Temp/diagnostics/NTSD28-USER-NARUTO-RUN-ATTACK-WINDOW-001/`.

| Physical J scenario input | Source-model action | Unity action | Naruto action/counter/MP over all 32 ticks |
| --- | --- | --- | --- |
| start tick 23, hold 2 | action 301 by completed tick 24 | action 301 by completed tick 24 | identical |
| start tick 25, hold 2 | action 301 by completed tick 26 | action 301 by completed tick 26 | identical |
| start tick 26, hold 2 | action 254 at completed tick 26; transform missed | action 254 at completed tick 26; transform missed | identical |

The source and Unity entity arrays were structurally identical for all 32 ticks in the start-23 and start-25 scenarios. In the start-26 scenario, the first full-entity difference is at completed tick 30, generated OID 434 in slot 52/action 397: source motion X=0, Unity motion X=550; its position is the same at that tick. This is a separate generated-object motion discrepancy after the missed transform, not evidence of a Naruto input-window first difference. It must remain in the broader battle alignment backlog.

Test chronology: initial focused cases failed at tick 24 because the EditMode fixture had not initialized `LF2ObjectPool`. After initializing the pool, all three generated 32 raw rows but teardown showed a missing driver-owned pool reference. The shared helper materializes its roster before registering shutdown service owners; the new diagnostic test now prepares the same singleton pool/factory and records those exact owner references for cleanup. The original Editor's final focused job `c8aa5cebb03e4879aa90beee82ec67f1` executed 3/3 PASS. Every case had 32 source and Unity rows; every shutdown reached `ObjectPoolQuiesced` with zero objects, slots and borrowers. The earlier `.error.txt` in Temp is historical output from the first fixture failure, not the final run state.

This controlled result rules out faster Unity logic tick progression or an earlier Unity action-253 transition for these exact canonical inputs. It does not rule out physical-key sampling, the render-time display of the current action, input phase when the user presses during an animation, or the formal EXE's observable response. Those are the next user-reported-symptom gates; no DAT wait or Naruto-specific skill rule is authorized from the controlled trace.

After the focused run, the original Editor PID 288224 accepted one `manage_editor play` request but exposed only `is_playing=true / is_changing=true` transition snapshots, including a stale bridge interval; no fresh stable Play state appeared and no physical-key probe was invoked. A subsequent `manage_editor stop` succeeded and a fresh state returned idle Edit Mode. This attempt supplies no natural-input evidence. Static input-path review confirms physical `AttackAction.performed` calls `CharacterInputModule.SetAttackActionPressed(true)`, which records the crossed `FuncKeyMask.jump` carrier; `LocalSimulationFrameInputProvider` samples `ILocalFrameInputSource.CaptureHeldSimulationButtons` for active human roster slots. The observed earlier 0-held-row Play result therefore still needs a live callback-to-roster trace, rather than a speculative DAT/window change.

Subsequent original-Editor Play probes resolved that specific physical-input gate. With Naruto OID2 initialized to authored action241 and MP200, pressing physical J after the first observed action253 tick produced canonical J held/pressed32, runtime KeyJump1 and action301 on the following tick (591→592). Pressing only after the second observed action253 tick also produced the same transition (1291→1292). The first report has a diagnostic `counter` field that used frame-binding WaitCounter; only the second report measures `Runtime.AttackingCounter`. Raw reports: `rasengan-physical-first253-play.json`, `rasengan-physical-second253-play.json`. Both menu runs returned PASS, Editor Play stopped and Scene SHA remained unchanged. These results reject a narrower Unity input window for these two precise Play cases. They do not reproduce the user's perceived late failure or establish the formal EXE screen-visible cutoff. Formal Naruto DAT action253 has `hit_a:300`; action254 has no `hit_a`. The source presentation interpolation uses current action/pic and interpolates positions only, so the unresolved check is the EXE-versus-Unity rendered cue and physical press phase near the transition to254. No production or DAT modification has been made for this symptom.
