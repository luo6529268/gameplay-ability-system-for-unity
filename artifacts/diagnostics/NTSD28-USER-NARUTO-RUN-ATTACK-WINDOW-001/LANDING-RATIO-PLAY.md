# Naruto held direction+jump landing distance: original Editor Play

2026-09-23. Status `SCOPED_ORIGINAL_EDITOR_PLAY_RATIO_PASS / FORMAL_EXE_VISIBLE_PENDING / ALL_ENTITY_D024_OPEN`.

The original Unity project Editor PID 288224 entered the saved `NTSD_Battle` Scene. The existing Editor-only physical-input probe synthesized one D press and one K press, then held both through Naruto's jump and landing. The probe was invoked only after the current run's `Editor.log` recorded `[BattleTestBootstrap] === Test bootstrap complete ===`; the MCP bridge's cached `is_changing` snapshot was not used as proof of Play readiness. The final Play report is `held-landing-run-play-result.json` in this directory. It records PASS, right edge tick 725, jump edge tick 730, airborne tick 736, landing tick 756, `FrameInputSet` held buttons 65 (D+K), and no retry pulse (one attempt for each key). The first run of this probe had failed falsely because its one-tick release/repress fallback repeatedly missed native 2tu input sampling; the held variant was corrected to maintain an already-arrived key until that sample. The old release-before-landing variant's pulse behavior was not changed.

The Unity Play trace after landing shows tick 756 action 215 at precise X `1080.3684264724625`, then tick 757 action 215, tick 758 action 0, and tick 759 walking action 7 at precise X `1092.1473712086463`. The resulting three-tick precise distance is `11.778944736183803`. The report's integer-position distance is 12 pixels; use precise position for proportion comparison, because integer rounding changes short-window ratios.

The paired formal-source diagnostic runner used the unchanged playable/core source, formal runtime DAT, Stage 23, Naruto OID 2 starting at X800/action0, and D for four scenario ticks followed by continuous D+K. Its 40-tick raw capture and strict validator result are stored here as `landing-d4-dk-held-x800.source.raw.jsonl` and `landing-d4-dk-held-x800.validation.json` (`valid=True`, 40 ticks, `certificate=False`). Naruto lands in action 215 at completed tick 32 with precise X `974`, then reaches action 7 at completed tick 35 with precise X `981.6666666666667`, a three-tick precise distance of `7.6666666666667425`. The source-model sequence and Unity Play sequence match action 215→215→0→7 and their post-landing raw motion values 2.3333333333333335→1.3333333333333335→0.3333333333333335→3. The two runs are not identical complete Worlds/seeds/input edges, so the conclusion is limited to this matching post-landing segment.

At the 16:9 Training view, the formal release viewport width is 1333 and the fixed Unity full-background view width is 2048. `7.6666666666667425 / 1333 = 0.005751437859464923`; `11.778944736183803 / 2048 = 0.0057514378594647475`. The measured fractions agree to floating-point precision, and the Unity/source distance factor is `1.5363840960239592`, matching `2048/1333`. This is the user-approved D-024 actual-world-motion exception, not same-pixel combat-distance parity with the formal release. Source-model output is diagnostic and cannot replace direct visible formal EXE comparison.

The original Editor was stopped after the probe. The saved Battle Scene SHA-256 remained `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`; Config/Scene git status had no changed entries. No DAT data, scene, camera or production runtime code was changed in this probe correction. This acceptance closes only Naruto's sampled held D+K post-landing screen-travel fraction at the tested 16:9 view. D-024's other entity movements, other camera/aspect cases and formal EXE visible presentation are still open.
