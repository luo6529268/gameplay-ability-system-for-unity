# Q09 formal platform-shadow trigger: scoped read-only audit (2026-09-26)

Status: `STATIC_FORMAL_CONTENT_CANDIDATE / PLAY_PENDING`. This report selects a real formal-content candidate for the already implemented Q09 shadow-offset display consumer. It does not close Q09 or the parent battle-alignment goal.

The root formal `NTSD2.8-Logan.exe` SHA-256 is `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. Unity's staged `decoded_dat/data/data.txt` and `c/hid/rea.dat` have the same SHA-256 as the formal `resources/runtime/decoded_dat` copies: `3ED7DE4918AA7B5E94FE73A2B7D9B43DED9D10575DD182B9CA2B647EAE29A8F0` and `27F0B91F92C09BEB9BBE11127D0EF4AC3B0267F1978737F94C76841BC010F75D`, respectively. The formal index selects `c\hid\rea.dat` as OID 56/type 0 at `data.txt:270`.

A line-by-line literal census of every staged decoded DAT's `itr:` records for the complete `30 <= kind < 100` platform-operation range returns exactly 17 records, all in `c/hid/rea.dat`, all `kind: 50`, with `x:39`, `y:564000`, `w:10`, `h:6`, and `dvy:-5`. Five are in `picked_caught` frames 130–134 (DAT lines 734, 744, 754, 764, 772); twelve are in `falling` frames 180–191 (lines 853, 864, 875, 886, 898, 907, 916, 927, 939, 951, 962, 971). This is a literal content census, not proof that every frame occurs in ordinary play.

The paired playable `battle_world.cpp` platform direction scan reads ITRs from the source's **current** frame and center/state/attacking from its tick-action snapshot frame. For `kind 50`, target type 0 or 3 and **different battle groups** normalize it to operation 30; an extended target type is rejected by this kind. Strict X/Z gates, the previous/current Y reference tests and the target's `collision_y_reference` then determine whether the link and `render_shadow_offset_10c` are written. The ITR's unusually large `y:564000` is **not** consulted in this platform scan, so it cannot be used alone to dismiss this formal record. Unity's `BruteForceSceneQuery.CollectPlatformDirection` has the corresponding kind/group/geometry and 10C write path; the Q09 production consumer now moves the Legacy/Central shadow at `Z + 10C` while retaining entity Z as depth.

The current evidence establishes a formal indexed DAT/field entry and a source/Unity candidate route. It does **not** establish natural action reachability, a qualifying pair in the original Battle Scene, nonzero offset publication in Play, rendered shadow pixels, or same-condition root EXE graphics. The existing Q06 synthetic platform transaction and Q09 2/2 display tests cover separate, narrower layers. Next: use a narrowly scoped opt-in original-Editor Battle Scene probe with formal OID56 and a target of type 0 or 3 in a different group. First record the actual frame/snapshot pair and candidate/link value through the complete Driver; only if nonzero 10C appears should the same frame be checked at both presentation exits and at the camera. Keep the original Scene and camera serialized hashes stable, restore World/pool state, and do not modify DAT values or trigger unrelated probe chains. A controlled frame is not a natural-input or root-EXE pixel claim.

The separately approved three-document v3 recovery had already been installed before this audit. The recovery QA and corrupt snapshots remain at `artifacts/diagnostics/NTSD28-Q07-PROGRESS-DOC-RECOVERY-20260925/`; all three current live documents have zero NUL bytes and later addenda, so recopying the v3 candidates would erase subsequent progress.
