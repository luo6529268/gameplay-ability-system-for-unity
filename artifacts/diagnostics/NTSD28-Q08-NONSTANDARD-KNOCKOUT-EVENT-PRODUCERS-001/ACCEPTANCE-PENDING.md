# Nonstandard knockout event producers: current evidence

2026-09-22. `CODE_WRITTEN / OFFLINE_COMPILE_PASS / ORIGINAL_EDITOR_RUNTIME_PENDING`.

Formal `battle_world.cpp` record calls 1898, 2262 and 6133 are mapped to the original Unity project's floor-contact environment, negative-environment recovery and held CPoint writers. Each writes the new event beside its pre-HP existing KO increment; the shared world writer handles source/credit/four-owner independently and preserves formal -1/1000 defaults for an absent source. The two standard-hit branches reuse that writer. Scope is seven declared script paths; no Scene, Prefab, resource, audio or nonbattle code was edited.

Checks run: seven-path `git diff --check` exit 0; Change Ledger validator exit 0 with pre-existing unrelated warnings; original-project offline `dotnet msbuild Assembly-CSharp-Editor.csproj -t:Build -clp:ErrorsOnly -nologo` exit 0 through the Temp-only absolute targets file, yielding runtime and Editor DLLs. Evaluated Editor Compile includes all three edited existing fixtures and the earlier Q08 event-state fixture. See `Temp/diagnostics/NTSD28-Q08-NONSTANDARD-KNOCKOUT-EVENT-PRODUCERS-001/offline-build.log` and `Build/`.

Not yet run or proved: original live Editor compilation, focused NUnit, stable BattleRuntimeSelfCheck, same-seed/full-tick native event comparison, Battle Scene Play, exit/re-entry and zero residual. The pre-existing Q09 WORDS TestRunner request was consumed but has no result; an observation timeout is not a terminal result, so no competing request or second Editor was started. Q08, Q09, Q10 and R10/R15/R17 stay open.
