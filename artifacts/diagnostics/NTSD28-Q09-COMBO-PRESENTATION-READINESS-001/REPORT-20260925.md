# Q09/P-11 combo presentation readiness (2026-09-25)

Status correction: `DEFAULT_DISABLED_IN_INSPECTED_ORDINARY_PLAYABLE_PATH / VISIBLE_GAP_NOT_PROVEN`. The earlier `CONFIRMED_STATIC_PRESENTATION_GAP` label and unconditional implementation instruction below are superseded by `DEFAULT-DISABLED-LATCH-CORRECTION-20260925.md`. This is a read-only Q07→Q09/R14/R17 dependency audit, not a gameplay or pixel acceptance. The formal root `NTSD2.8-Logan.exe` SHA-256 was checked as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; `source/README_SOURCE.md` declares the paired playable source. The project intentionally excludes native mode DAT and instead uses `ProjectBattleModeConfig.asset`, while the formal `data/mode/ntsd.dat` remains a read-only rule/presentation reference.

The selected formal `<combo>` record declares `bound:1`, `name:%d_hit`, `pic:sprite/combo_hits.png`, `h/w:15`, `offset_x/y:20/21`, `times:30`, `respond:50`, `facing:1`, `state:1`, `caughtact:1`, and `effect:20`. `GameSession28` loads its atlas and passes the mode configuration into render handoff. `RenderSnapshotBuilder28::project_native_combo_command` (`source/ntsd28_core/src/rendering/render_snapshot.cpp:650-753`) suppresses the draw unless the display latch, complete record, bound, type0 entity, positive hit count, effect slot ceiling, available atlas, and signed expiry pass. It projects text, source-space X/Z/Y position, depth, 15×15 glyph cells with 16-pixel stride, and resource identity into `RenderComboCommand28`. `source/ntsd28_playable/src/d3d11_renderer.cpp:1772-1773` consumes `snapshot.combo_commands` in entity draw order. This is a distinct per-entity battle overlay command, not the user-excluded native character HUD panel.

The selected Unity `ProjectBattleModeConfig.asset` currently stores only combo `enabled`, `facing`, `respond`, and `caughtAct`, and `ProjectBattleModeConfig.Snapshot` carries only those four fields into `NTSD28NativeComboRuntimeState`. The four values are sufficient for the existing combo gameplay producer/expiry, but not for the formal display gate and command. Formal `sprite/combo_hits.png` is already staged at `Assets/NTSD/Content/LoganRuntime/vfs/sprite/combo_hits.png`; formal and staged SHA-256 are both `1D398C0AE0AE01495B7FF5FEC78B7978690CFE53B98D10913E1BCEC92426AD48`. Therefore **the atlas bytes are present; the missing work is selected visual configuration, publication and runtime consumption**, not DAT/image copying.

Current Unity `BattleComboView.Apply` only toggles a Canvas object and sets optional TMP text. `BattleUiContracts` exposes a `BattleComboState`, but a current `Assets` source/serialized search found no `IBattleUiSnapshotSource.TryCopySnapshot` implementation, no production `BattleComboView.Apply` caller, and no combo-hit field reader under `Simulation/Presentation` or `Animation/Rendering`. The saved Battle Scene contains `ComboPanel` and three Image bindings but `comboText` is null. This establishes absent Unity consumption for the optional draw branch, not an ordinary-play first difference while the formal latch is disabled. It does not establish Canvas visibility in every runtime circumstance or prove a pixel difference.

Conditional follow-up only: if an ordinary formal release path or same-condition EXE observation establishes that the display latch can become enabled, create an exact pre-edit Task/Change for a battle-only P-11 implementation. Preserve the existing combo hit/expiry writers and Project Mode Asset ownership; do not restore native mode DAT or change DAT numbers. The presentation-field, atlas, command, gate, lifecycle and pixel checks described by this audit would then apply. Until that trigger is proved, adding a default-visible combo overlay would diverge from the inspected formal playable path.

No Unity script, Asset, DAT, PNG, Scene, Prefab or ProjectSettings file was edited here. Q07 content availability for this atlas is established by byte identity, but Q09/P-11, R14/R17 and the aggregate goal remain open. Existing P-17 user-excluded native HUD remains untouched.
