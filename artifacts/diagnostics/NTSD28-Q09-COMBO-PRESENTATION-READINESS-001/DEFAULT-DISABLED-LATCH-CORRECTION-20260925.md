# Q09/P-11 formal combo display latch correction (2026-09-25)

Status: `INSPECTED_ORDINARY_PLAYABLE_DEFAULT_DISABLED / VISIBLE_GAP_NOT_PROVEN`. This corrects the earlier `REPORT-20260925.md` claim that Unity's absent combo command is a confirmed ordinary-play presentation gap requiring immediate implementation. No Unity script, Asset, DAT, PNG, Scene, Prefab or ProjectSettings file was changed by this correction.

Authority: the formal root `NTSD2.8-Logan.exe` has SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; `source/README_SOURCE.md` declares its paired playable source. `source/ntsd28_playable/include/ntsd28_playable/game_session.h:296-299` documents locked executable `DAT_004A9FD8` initialization to zero and initializes `BattleConfig28::native_combo_runtime_display_enabled_49fd8 = false`. `source/ntsd28_playable/src/game_session.cpp:3331-3334` passes this value into the render snapshot configuration. `source/ntsd28_core/src/rendering/render_snapshot.cpp:650-660` returns before the combo record/atlas/command checks when the latch is false. A literal search of the paired `source` tree found no production writer that changes this `BattleConfig28` field; the references outside its declaration/handoff are scenario diagnostics. Core render tests set the latch to true explicitly to cover the dormant branch, then test false suppression.

The playable `source/ntsd28_playable/tests/game_session_tests.cpp:3072-3132` checks a selected locked mode with combo state observed and no combo command drawn. This supports the distinction between combo gameplay state and optional visual overlay. The staged `combo_hits.png` byte identity and Unity's absent production `BattleComboView.Apply` caller remain valid observations, but they do not establish a visible first difference while the formal display latch is false.

Disposition: keep P-11 as a conditional dormant-display branch, outside the immediate Q09 implementation queue. Do not enable a default combo overlay or add presentation fields merely to make this branch visible. Reopen a P-11 implementation package only if a production writer in the declared playable closure or a same-condition formal EXE observation proves the latch enabled. Then validate gate, command, atlas and pixels with the original Unity Editor. No independent GUI pixel run was performed for this correction, so this report does not claim that every possible formal EXE path is visually silent.
