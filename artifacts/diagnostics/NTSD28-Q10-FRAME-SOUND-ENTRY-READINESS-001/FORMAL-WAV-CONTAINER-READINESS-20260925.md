# Q10 formal WAV container readiness (2026-09-25)

Status: `READ_ONLY_FILE_FORMAT_AUDIT / UNITY_DECODE_PENDING / NO_RESOURCE_EDIT`.

All **981** WAV files under the formal NTSD 2.8-Logan `resources/runtime/vfs` were inspected without copying or converting them. Every file has a RIFF/WAVE header and a readable `fmt ` chunk with **format tag 1 (PCM)**; Python 3.12 `wave` accepted all 981 and agreed with each file's channel count, sample rate and bit depth. Structural parse failures: **0**. The group totals are **968 mono / 13 stereo** and **797 8-bit / 184 16-bit**. Sample rates vary across 21 values, including 22050 Hz (428 files), 48000 Hz (226), 44100 Hz (153), 16000 Hz (103), 18000 Hz (22), 16384 Hz (14), and isolated rates as low as 10989 Hz and as high as 96000 Hz. Total WAV bytes remain 33,571,573 (32.02 MiB).

This removes a proposed need for non-PCM WAV transcoding at the file-container level. It does **not** prove UnityWebRequest/DownloadHandlerAudioClip successfully imports each unusual sample rate, that playback matches native XAudio2 output, or that BGM WMA is supported. Current `NTSD_ResourceLoader.GetAudioType` maps `.wav` to `AudioType.WAV`, but an original-Editor focused load sample and final deployed-root runtime check remain necessary. The eight WMA BGM files are separate and still unsupported by this loader. See `FORMAL-AUDIO-DEPLOYMENT-BOUNDARY-20260925.md` for route/deployment ownership.

Original-Editor focused sample (2026-09-25): the existing `NTSD.Test.NTSD28Q10KnockoutModeSoundEditorTests.SelectedModeWavsResolveAndDecodeThroughBattleAudioPath` ran through the local framed Unity MCP bridge on the **original** Editor PID 11944, job `2fa15821c9ae4d08a095ff270a1acf23`, and passed **1/1** EditMode. It loads `data/m_ok.wav` and `data/m_join.wav` from the current `Assets/NTSD/Sound` path through `NTSD_ResourceLoader.LoadSingleAudioClipAsync` and asserts nonnull clips with positive samples. Fresh SHA checks showed both current files are byte-identical to the formal VFS versions, so this proves the existing Unity WAV loader decodes these two exact formal PCM samples. It does not test the remaining 979 WAVs, unusual sample rates, the future formal VFS route, audible playback or WMA. Exact tool result: `original-editor-formal-two-wav-decode-20260925.json`, SHA-256 `D805933BD70A14AA86BEA8DADB046D91749396284F0D8653E6C3E11E82D7AAEC`. After the test the Editor was idle, out of Play, not compiling or running tests; Battle/Menu Scene SHA-256 remained `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A` / `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`.
