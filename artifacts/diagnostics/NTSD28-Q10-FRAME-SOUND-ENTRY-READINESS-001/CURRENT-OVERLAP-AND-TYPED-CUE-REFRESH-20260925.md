# Q10 current overlap and typed cue refresh (2026-09-25)

Status: `READ_ONLY_CURRENT_CONTENT_REFRESH / OFFLINE_UNITY_ASSEMBLY_MEASURED / PLAYBACK_PENDING`.

The 2026-09-22 `REPORT.md` and CSVs are historical snapshots, not the exact current file set. The formal `formal-indexed-cue-paths.csv` contains 976 case-sensitive `.wav` cue spellings; after slash normalization and Windows case-insensitive path identity, these are **970 distinct physical relative paths**. A fresh same-path check against the current `Assets/NTSD/Sound` tree found **68** of those 970 paths. **One** (`data/m_ok.wav`) is whole-file SHA-256 identical to the formal VFS file; **67** are whole-file different. The old CSV marked `data/m_ok.wav` absent, but its current file was created locally on 2026-09-23 (preserving an older last-write timestamp), after the 2026-09-22 inventory. Therefore the old `0/71 whole-file identical` statement must not be reused as a current claim. Its 71 lexical path rows collapse to 67 physical paths after separator normalization; plus current `data/m_ok.wav` gives the measured 68.

The old PCM and WAV metadata CSV rows also collapse from 71 to 67 physical paths. After deduplication, 26 of those 67 have identical PCM payload bytes, 41 differ, and 16 differ in duration. The newly present `data/m_ok.wav` is byte-identical and therefore PCM/duration identical too. **Current 68-path total: 27 same PCM, 41 different PCM, 16 different duration**. These content comparisons do not prove which cue fires in an actual battle or whether a listener hears the difference.

The original Editor direct-Battle log has 905 failed-prewarm warnings for 903 distinct WAV paths. Its warning set intersects 902 of the 970 case-normalized formal indexed *frame* cue paths; the remaining warning `data/0.wav` comes from object-level weapon sound fields, outside the frame-only lexical CSV. Of the 68 indexed frame cues with no failed-prewarm warning, 67 are listed as old `Sound` files in the historical CSV and the last is the now-present `data/m_ok.wav`. This reconciles the warning count with the corrected local overlap; a warning is still a load failure at prewarm, not proof of an emitted in-game sound.

A separate read-only reflection run used the current `Library/ScriptAssemblies/Assembly-CSharp.dll` SHA-256 `5EF4A473DF1C99FCDF929E01349C3008C5CBA8C0508BB538E8AA0FDDDE266184`, the formal `BattleContentSource.ForLoganRuntime`, `LoganObjectCatalog.Read` with a fresh default project-mode snapshot, and the actual `CharacterAnimtorManager.BuildCharacterFrameConfigsFromCatalog` converter. For 330 objects it measured **8,864 `FrameSounds` entries**, **zero frames with more than one `FrameSounds` entry**, and **976 distinct ordinal frame cue spellings**. The set of nonempty `LF2FrameData.sound` values used by `CollectBattleSoundIds` was the same 976-value set. This disproves a proposed missing-prewarm cause based on multiple `sound` declarations per converted frame for the current formal content; it is offline Unity-assembly evidence, not original-Editor prewarm or audible parity. The earlier first reflection attempt failed because PowerShell's extended type system could not bind `LF2FrameData.state` and `State` differing only by case; the successful retry used fixed reflection metadata. No partial counts from the failed attempt were retained.

The Q10 actionable gap remains the source root and content closure: `NTSDSoundPlayer` reads `Application.dataPath/NTSD/Sound`, while formal battle WAVs are under formal VFS and staged `LoganRuntime/vfs` has no WAV. Routing, deployment, PCM differences, BGM WMA handling, and focused audible comparison remain open. No audio file, DAT, script or Scene was modified in this refresh.
