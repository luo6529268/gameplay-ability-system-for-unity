# OID32 action95 paired-source offscreen pixel witness

Status: `VERIFIED_SCOPED_PAIRED_SOURCE_GPU`. This is a diagnostic render of the formal EXE's paired playable source and formal runtime resources, not a pixel capture from the root release EXE. The root EXE's action/pic/sprite-command agreement was established in `NTSD28-Q07-OID32-FRAME95-RELEASE-VISIBILITY-001`; natural gameplay reachability is not established.

The standalone `Tools/NTSD28Q07Diagnostics/hunter_frame95_offscreen_probe.cpp` compiled with GCC 15.1 and the playable/core source closure, linking the existing `d3d11_renderer.cpp` and the D3D/WIC libraries used by `build.ps1 -Target offscreen_gate`. Compilation exited 0 with an empty `compile.log`; the diagnostic exited 0. It initialized two independent sessions with seed `0x28A55A5A`, OID32 slot0 `(500,350)` and OID7 slot1 `(1200,350)`, background23, equal HP/MP/team settings, changing only slot0 action95 versus action0 at tick0. Each snapshot had one actor sprite, two total sprites, and the renderer reported 117 quads. The renderer used its existing console-only 1333×730 D3D11 WARP target and PNG readback; no interactive window or computer-use was involved.

The action95 actor sprite used pic64, source rectangle `(320,480,79,79)` in formal `m/nin/hun.png`, and screen rectangle `(461,271,79,79)`. The action0 control used pic0/source `(0,0,79,79)` at the same screen rectangle. `pixel-analysis-20260925.json` compares the saved PNGs in RGB: the exact 79×79 region contains 6,241 pure white pixels for action95; all 6,241 pixels differ from action0, while **zero** pixels outside that region differ. The RGB difference bounding box is exactly `[461,271,540,350]`; the centre is white `(255,255,255)` for action95 and `(10,10,10)` for action0. The earlier RGBA `ImageChops.getbbox()` result was null because unchanged alpha formed a zero fourth band; the final analysis uses RGB and counts the changed pixels directly.

The two readback files are `oid32_action95_tick0.png` (SHA-256 `D886D6BD9AEA4786094FA1A69D41DC27688BE6DEB4D73510C2FEB4E88252B538`) and `oid32_action0_tick0.png` (`052DCA1014E75398C871F3F9EE302A3DFE27F03DA8A4255CA22951F8DFE501EF`). Geometry and pixel counts are in `oid32_offscreen_sprite_geometry.csv` and `pixel-analysis-20260925.json`. The previously observed original Unity Battle Scene formal catalog has the authored OID32 pic0 control but omits pic64; Unity entity pixels in the same state still need a separate Play witness. This package does not change DAT, PNG, Unity production code, Scene, Prefab, or the formal EXE.

Q07 and Q09/R17 remain open. A source-renderer white rectangle plus a Unity catalog omission is a scoped presentation discrepancy candidate, not proof that players naturally enter action95 or that the root release EXE's GPU output has been captured.
