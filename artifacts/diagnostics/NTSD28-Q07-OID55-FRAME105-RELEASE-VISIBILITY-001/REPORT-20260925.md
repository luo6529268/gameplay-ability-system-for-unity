# OID55 frame105 controlled formal release visibility witness

Status: `VERIFIED_SCOPED_SOURCE_AND_RELEASE_TRACE / PIXEL_PENDING`. The root formal EXE SHA-256 was `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033` before and after replay. The new tool compiled the paired playable/core closure with `g++ -std=c++17 -O2 -Wall -Wextra -Wpedantic -municode`, 0 errors and an empty `compile.log`. Formal DAT/VFS were read only.

Controlled initial world: OID55 type0 in physical slot0, action103, HP/MP500, position `(500,0,350)`, team1; distant OID7 team2 at `(1200,0,350)`; seed `0x28A55A5A`, background23. Twelve ticks had no input. The source helper emitted one source CSV and LFR. Its first invocation was given `decoded_dat` and `vfs` as constructor roots, which failed before output with `complete VFS decoded_dat directory was not found`; the corrected invocation passed the formal runtime root for both constructor roots and generated the immutable files. No failed output was overwritten.

The source snapshot reached action105 at ticks5–7 with base `pic:44`, visual offset0 and one OID55 sprite command per tick. The root EXE then replayed the LFR using both formal root flags and `--lfr-slot0-action 103`: report `passed:true`, `failureCode:0`, 12 declared ticks, 13 completed ticks including terminal, 14 trace rows including tick0. Across ticks1–12, source CSV vs release trace compared slot0 action, slot0 effective pic and aggregate sprite count: **36/36 equal, no first difference**. Release trace has two total sprites at ticks5–7; it does not expose per-slot sprite identity, so the per-slot command witness is from the paired source, not a direct root-EXE pixel capture. `nativeParityClaim:false` remains in the release report.

The first PowerShell direct invocation returned before the GUI-subsystem process completed, so its process exit code was not observed even though the report was later written. A second create-new run used `Start-Process -WindowStyle Hidden -Wait -PassThru` and **observed exit code 0**, `passed:true`, `failureCode:0`, 14 trace rows. Its trace SHA-256 is exactly the same as the first trace (`4B7C00C1F20D414453CC8D475C2D1E088B6B150CED0D10A5E3CC7C8E273140B4`). The waited report SHA-256 is `6EF35EAFBBD9930ADC3C32D592D8D22CCDE197DF60BA138345C3067559DF1F8B`. No process remained after the waited run.

At frame105, the source rectangle is `(320,320,79,79)` while formal/staged `c/saso/pup.png` is `799×319`. Formal `D3D11Renderer28` passes the out-of-range UV to a point sampler with `D3D11_TEXTURE_ADDRESS_CLAMP`; the sampled bottom row of that PNG at x320–398 has **0/79 nontransparent pixels**. This predicts no visible OID55 body pixels from that edge row, but no formal EXE GPU readback was made. Unity's `BuildIndexedSpriteRects` drops a wholly outside rectangle; this case does not establish a user-visible first difference. The initial action103 is a controlled fixture, not proof that normal character selection or inputs reach it.

| Artifact | SHA-256 |
| --- | --- |
| `pup_frame105_lfr_probe.exe` | `C5DEDB545676926107F65CE5CE163643980FEB3612DCF010E7002698D02D554A` |
| `oid55_frame105_source_packets.lfr` | `408D47A649891ABCCBB62D96F4AC9DD3AF75EABA5BBB25D83BA8DE72BC89AEA2` |
| `oid55_frame105_source_ticks.csv` | `B0C3F35FEE9B8783FE89A8DA2F61B65EAABB8FD838277887959231E3E1329275` |
| `formal_release_report.json` | `736965458BD89917AA21D19E5D8D3C7D72B631B688BCBDBA4A98621F6E5B821F` |
| `formal_release_trace.jsonl` | `4B7C00C1F20D414453CC8D475C2D1E088B6B150CED0D10A5E3CC7C8E273140B4` |
| `source-release-comparison.json` | `F995DDE196834E089A62C9CFFA241C9B57797C86B71BA358982F9C9595990D2D` |

The other boundary cases have a different potential: formal-equivalent `m/nin/hun.png`, `m/nin/nin.png` and `m/nin/nin2.png` each have 79/79 nontransparent pixels in the relevant bottom-edge x range. Since the native draw uses clamp and Unity drops wholly invalid sprite rectangles, these are focused **candidate** visual differences, not proven production failures. Q07 should next check a controlled formal release path and Unity publication/pixels for one reachable case, then natural selection/input reachability. No DAT or PNG value should be edited. Q07/R17 and the total goal remain open.
