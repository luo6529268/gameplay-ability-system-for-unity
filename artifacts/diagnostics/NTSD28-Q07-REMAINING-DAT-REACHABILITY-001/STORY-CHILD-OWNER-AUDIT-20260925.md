# Seven missing story-child DAT owner audit (2026-09-25)

Status: `VERIFIED_STATIC_DEPENDENCY / DEPLOYMENT_HELD`. This is a read-only source and content check, not permission to deploy the default story catalog or a claim of battle parity.

The formal root `NTSD2.8-Logan.exe` SHA-256 was rechecked as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The paired playable `source/ntsd28_playable/src/game_session.cpp` is within the declared playable build closure. Its `load_native_story_missions28` reads `data/stage.dat`, parses each `<stage_information>` record's `file:`, resolves each child through `optional_virtual_file`, and only accepts a mission when the child has usable stages (lines 854–974). `GameSession28` loads the parent optionally at lines 1448–1466; if absent, it leaves the story mission catalog unavailable. A story selection must provide both `story_mission_id` and `story_child_stage_id` and requires `battle_mode==1` (lines 1506–1525). Later story transitions consume the selected child (lines 1992–2100). These are live-path source observations, not an independent GUI recording.

The formal `resources/runtime/decoded_dat/data/stage.dat` has seven `file:` records, IDs 0–6, referencing exactly `s/0/stage0.dat`, `s/1/stage1.dat`, `s/2/stage2.dat`, `s/3/stage3.dat`, `s/4/stage4.dat`, `s/5/stage5.dat`, and `s/S/su.dat`. All seven files exist under formal `decoded_dat` and all seven are absent from Unity `Assets/NTSD/Content/LoganRuntime/decoded_dat`. Unity's `BattleStageCampaignLoader` instead defaults to `StreamingAssets/NTSD/data/stage.dat`, and its driver reads `config?.stageCampaignFilePath`; the current menu caller correction is recorded in the alignment document as not supplying a formal paired mission/child selection. The two stage loaders therefore must not be treated as interchangeable merely because both mention `stage.dat`.

Disposition: keep these seven in the Q08 story/stage owner queue. The user's explicit hold on default `stage.dat` deployment remains in force; copying children alone would leave the parent catalog and formal paired selector unconnected and would not make Q07 or Q08 complete. If the story path is resumed, first define an approved parent/child deployment and project-map adaptation contract, then test a real selected story entry and transition with the project background exception. For the present worktree, no DAT, script, Scene, asset, or nonbattle file was changed by this audit. The Q07 non-excluded DAT byte count remains 338/353 with 15 absent, including these seven.
