# Q07 non-excluded formal DAT byte inventory (2026-09-25)

2026-09-25 story-child owner refinement: the seven absent `s/*` files are exactly the seven `file:` children in formal `data/stage.dat` (IDs 0–6). Playable `GameSession28` reads the parent first and resolves children only through that catalog; absent parent leaves native story missions unavailable, while a selected story requires paired mission/child IDs in mode1. The default parent deployment is on user hold and the current Unity menu has no formal paired selector. Keep all seven in the Q08 story/stage owner queue rather than copying them independently for a byte-count target. Read-only evidence: `STORY-CHILD-OWNER-AUDIT-20260925.md`. The current 338/353 count and other owners are unchanged.

2026-09-25 Q10 content-entry delta (overrides only this snapshot's `data/bgm.dat` row/count): `NTSD28-Q10-BGM-DAT-CONTENT-ENTRY-001` copied formal `data/bgm.dat` with identical 454 bytes/SHA-256 `ADA6B4E9C8F5C6FBD9A9A85F5E6188CD1CEE5E85139CD9367915EF786B64CAEA`. Original Editor generated `.meta`; staged non-excluded DAT count is now **338/353**, 15 paths remain absent under their previously recorded owners. No BGM playback is proven: all eight referenced WMA are absent from staged VFS and no Unity `bgm.dat` consumer was found. See the Q10 same-ID acceptance report. The 337/353 numbers below are the pre-addition byte snapshot, not current inventory.

Status: `READ_ONLY_BYTE_RECHECK / OWNER_EXIT_PENDING`. The formal root executable SHA-256 was freshly checked as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. This inventory compares `resources/runtime/decoded_dat` under that release with `Assets/NTSD/Content/LoganRuntime/decoded_dat` in the current worktree. It does not change a DAT or authorize staging/deletion.

User-excluded DAT paths were removed from both sides before counting: `b/*/b.dat`, `data/bg_mode.dat`, `data/bg/*.dat`, `data/mode.dat`, and `data/mode/ntsd.dat`. The recursive `.dat` file inventory then has **353 formal included paths**, **337 staged included paths**, **337/337 common paths with equal SHA-256**, **zero staged extra paths**, and **16 absent formal paths**. Each included path was compared by relative path and SHA-256 of its actual bytes; this is a byte inventory, not consumer or Play proof.

| Provisional owner and disposition | Absent formal paths | Count |
| --- | --- | ---: |
| Q10 audio owner: assess cue/BGM consumer and sound scope before any asset change | `data/bgm.dat` | 1 |
| Alternate `INKHUDV2` participant HUD child: not selected by the inspected playable battle render path; Q09/HUD exception owner only if a real target path later requires it | `data/frame/INKHUD2.dat` | 1 |
| Protected nonbattle menu owner: do not migrate within battle-only scope | `data/menu.dat` | 1 |
| P-14 approved overhead-health-bar exception: formal battle render consumes these, but Unity's own overhead bar is retained; do not migrate solely for parity | `data/minibar.dat`, `data/minibar/none.dat`, `data/minibar/st1.dat`, `data/minibar/st2.dat`, `data/minibar/stage.dat` | 5 |
| Q08 default stage deployment: explicitly held by user | `data/stage.dat` | 1 |
| Q08 stage/story owner: verify current formal battle reachability, project map exception and test prerequisites first | `s/0/stage0.dat`, `s/1/stage1.dat`, `s/2/stage2.dat`, `s/3/stage3.dat`, `s/4/stage4.dat`, `s/5/stage5.dat`, `s/S/su.dat` | 7 |

This refresh supersedes the old 67-absent snapshot in this directory for the **current non-excluded** DAT byte question. The older report still documents why `kind.dat` needed a separate consumer/identity migration; it is not a current missing-count source. The current 337/353 result establishes a precise owner queue, not Q07 delivery: runtime publication, natural skills, dynamic legacy readers, formal reference closure, image/visual proof and old-file retirement gates remain separately open. No excluded background or mode DAT should be staged to make this count appear complete, and no old Config DAT/image should be deleted from this inventory alone.

Consumer correction after the byte count: the five minibar files are not merely an unresolved menu/UI guess. Formal `game_session.cpp` loads `data/minibar.dat` and its child styles, selects a style for `RenderSnapshotConfig28`, and `render_snapshot.cpp::project_native_minibar_commands` emits per-living-character health/MP bar layers; `d3d11_renderer.cpp` consumes the resulting commands. `minibar_catalog.cpp`, `render_snapshot.cpp` and `game_session.cpp` are in the playable build closure. Unity currently has its own `BattleHealthBarBatchBackend` / `BattlePresentationShadowBuild` overhead bars. The alignment plan explicitly lists P-14, the Unity overhead bar, as `USER_ACCEPTED_EXCEPTION` (`P-14` row; also §0.14 exception scope). Thus formal reachability does not itself require importing these five DAT or replacing Unity's approved bar. This correction leaves the 353/337/16 byte counts unchanged and does not claim pixel equivalence for the exception.

The other frame-HUD child also has a narrower current disposition than the initial provisional Q09 row: formal `data/frame.dat` lists index 0 `INKHUD.dat` and index 1 `INKHUD2.dat` (`INKHUDV2`). The inspected playable `GameSession28` loads exactly `NativeFrameHudCatalog28::captured_active_index`, documented there as index 0, then passes only the selected definition into the render snapshot. The active index-0 `INKHUD.dat` had already been staged by `NTSD28-Q09-ACTIVE-FRAME-HUD-CONTENT-STAGING-001`. This proves `INKHUD2.dat` is not required by that inspected selected battle-render path; it does not prove no other formal executable option or uninspected path can ever select it. Keep it outside a blind Q07 copy and under the existing HUD exception/Q09 scope until an actual caller is evidenced.
