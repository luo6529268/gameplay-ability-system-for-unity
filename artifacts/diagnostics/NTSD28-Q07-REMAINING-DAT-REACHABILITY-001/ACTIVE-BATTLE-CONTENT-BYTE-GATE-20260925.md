# Q07 current selected-battle content byte gate — 2026-09-25

This is a fresh read-only disk and owner check, not Q07 runtime or pixel acceptance. The formal root `NTSD2.8-Logan.exe` SHA-256 was rechecked as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. Unity's serialized `Assets/NTSD/Config/GameConfig/GameConfig.asset` currently selects `Assets/NTSD/Content/LoganRuntime`. No DAT, image, importer, Scene or script was changed.

The `resources/runtime/decoded_dat` and staged `decoded_dat` trees were enumerated by relative `.dat` path, excluding only the user-excluded `b/*/b.dat`, `data/bg_mode.dat`, `data/bg/*.dat`, `data/mode.dat`, and `data/mode/ntsd.dat`. Fresh result: **353 formal, 338 staged, 15 absent, 0 staged extra, 0 differing SHA-256 among the 338 common files**. The 15 absent paths are completely assigned:

| Owner | Exact paths | Count | Current disposition |
| --- | --- | ---: | --- |
| Q09 HUD selected-path check | `data/frame/INKHUD2.dat` | 1 | Formal `frame.dat` index 1; paired playable `GameSession28` selects `NativeFrameHudCatalog28::captured_active_index=0` and uses staged `INKHUD.dat`. No selected-path missing byte proved. Other selectable entry remains unknown. |
| Protected nonbattle Menu | `data/menu.dat` | 1 | Unity Menu behavior is preserved by the battle-only scope. No battle-content migration is authorized by a count alone. |
| P-14 approved overhead-bar exception | `data/minibar.dat`, `data/minibar/none.dat`, `data/minibar/st1.dat`, `data/minibar/st2.dat`, `data/minibar/stage.dat` | 5 | Formal playable render does consume this tree; Unity's existing overhead bar is explicitly retained under P-14. |
| Q08 story/stage dependency on user hold | `data/stage.dat`, `s/0/stage0.dat`, `s/1/stage1.dat`, `s/2/stage2.dat`, `s/3/stage3.dat`, `s/4/stage4.dat`, `s/5/stage5.dat`, `s/S/su.dat` | 8 | Formal parent references all seven child paths and must be selected with paired mission/child IDs. Default parent deployment is explicitly deferred; do not copy children alone. |

The two VFS trees were also enumerated by `.png` relative path and every common file SHA-256 was checked. Fresh result: **1,255 formal, 1,031 staged, 224 absent, 0 staged extra, 0 differing among 1,031 common files**. Exactly 110 absent paths are `b/*` background images, which the user excludes. The other 114 are `sprite/*`: `frame` 2, `Loading` 2, `menu` 8, `minibar` 4, `radar` 16, `small` 1 and `UI` 81. All 600 character-associated `c/*` and `custom/*` PNGs are on disk, consistent with the earlier indexed image inventory. The existing direct playable `resource.dat` consumer audit identifies active-battle `WORDS0..5` and `SPARK` as 7/7 staged, while selected results and menu references are protected separately. This does **not** classify every one of the 114 `sprite/*` files or certify publication/importer/pixels.

The actionable Q07 conclusion is narrower than either “353/353 required” or “Q07 complete”: **none of the 15 current non-excluded DAT absences is an unassigned file to copy for the inspected selected battle route**. The user hold, P-14, nonbattle boundary and alternate index explain each path. The next content investigation should classify the remaining `sprite/*` consumers by actual selected battle path and approved exception, then test publication and natural skill/pixel behavior. Q07/Q08/Q09/R17/R18 and BATCH-04 remain open; no old resource deletion is authorized.
