# NTSD28-R15-NATIVE-MODE-CAPTURE-CONTRACT-001

Status: `READONLY_CONTRACT_READY / NATIVE_IMPLEMENTATION_NOT_STARTED` (2026-09-22). This is an implementation boundary for the next R15 package, not a native V2 capture or parity certificate. No formal source, diagnostic runner, Unity code or resource was modified for this audit.

## Authority and current defect

Formal `NTSD2.8-Logan.exe` SHA-256 is `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The declared playable source `game_session.cpp` resolves `data/mode.dat`, selects the first `<mode_information>` `file:` entry, resolves that virtual child, loads `NativeComboHud28`, and only publishes a complete parse without new diagnostics. `tick_options()` projects the resulting `bound/facing/respond/caughtact` to World options. Current formal selected child is the original virtual string `data\mode\ntsd.dat`; current tuple is `1/1/50/1`. The parent and child raw SHA-256 are respectively `A1BEBF1E92A8438206EFD1FBDB7ECD6AE24AF9865858C217301937537C8E73A9` and `556DF3014F575D25D0642E840AC70F27CEB52DADA7109FEA666458FA0F316F12`.

`Tools/NTSD28AuthorityTrace/authority_source_capture_main.cpp` currently computes an object/fusion-only V1 composite, checks only those inputs at initialization/end, and emits historical schema tuple `17/25/28/2/2`. This is a diagnostic source-model runner, not the formal EXE's own trace. Its old captures and manifests must stay unchanged.

## Exact next package boundary

The minimum production-code path is `Tools/NTSD28AuthorityTrace/authority_source_capture_main.cpp`. `Build-AuthoritySourceCapture.ps1` already compiles the formal `game_session.cpp` and `native_combo_hud.cpp` without changing them. Before editing the runner, establish a distinct Task/Change Record, exact fixture/test paths and rollback. Keep the formal source directory read-only; no Scene, Prefab, Unity gameplay or nonbattle edit belongs to this package.

1. Capture the parent using the same `optional_global_dat` priority as `GameSession28`; select the first child with the same whitespace-token and inline-`file:` rules; resolve the child using the same virtual-file priority. Preserve the original child virtual string for hashing and the resolved path for file reads. Missing/invalid selected child or incomplete combo must fail, not silently become an absent-mode V1 capture.
2. Save parent/child raw SHA-256 and their immutable selected paths. Parse captured child bytes with formal `NativeComboHud28::parse_text()`, require complete parse with no diagnostics, then after `session.initialize()` compare the full parsed combo configuration with public `session.config().native_combo_hud`. `GameSession28::config()` is public; selected input paths are not. Matching full config alone does not prove path identity, so retain resolver order, bytes and before/after freshness evidence.
3. Calculate mode input exactly as Unity `LoganModeComboInput`: SHA-256 of `BinaryWriter` UTF-8 length-prefixed strings `NTSD28-MODE-COMBO-INPUT-v1`, original selected virtual path, uppercase parent raw SHA, uppercase child raw SHA. Calculate semantic SHA from length-prefixed `NTSD28-MODE-COMBO-SEMANTIC-v1` followed by four signed Int32LE values in `bound/facing/respond/caughtact` order. Append the two 32-byte digests after object/fusion components under ASCII `NTSD28_LOGAN_BATTLE_INPUTS_V2` + NUL; apply existing `NTSD28_LOGAN_DAT_SEMANTICS_V3` digest and LE projection.
4. Extend freshness to mode presence, candidate priority, selected paths and both raw hashes before header and after simulation. An upstream candidate appearing later or parent/child bytes changing must invalidate the capture even when the four semantic values stay the same. Ensure the main and optional domain/B2 side streams are all marked failed on late mutation; do not let a complete-looking side stream from a failed bundle be validated as independent success.
5. Emit exact V2 fields `battleInputContract`, `modeInputSha256`, `modeSemanticSha256` and `catalog-object-fusion-mode-definitions` scope. Fresh captures may declare the **current compatibility target** `17/26/29/2/2`; this is not proof that native implements Unity snapshot/checksum internals. Preserve `SOURCE_MODEL_DIAGNOSTIC_ONLY`, `certificateEligible=false`, formal EXE identity and source/runner/binary manifest hashes. Never relabel an old capture's header.

Independent cross-check target from actual formal and staged content: V2 raw `33B0341C58D2A708B208F2CA4A0C4DA740E942D49A8A9D82E106F1D1D9C419BE`, semantic `FF1218FF3FEB409FF6B2F8EDB1090591612B3D82D7FA91601E596D29CDF13DFB`, projection `9F40EB3FFF1812FF`. The runner must derive these from bytes, not embed them as answers.

Focused validation needs a fresh build/output directory under this repository, two byte-identical formal scenario captures, full actual config comparison, V1 no-mode fixture, changed-selected-path and same-tuple byte mutation rejection, missing/invalid child rejection, priority-candidate appearance rejection, and initialization/late mutation bundle failure. Then run `NTSD28Parity validate-authority-capture` and the focused self-tests. Only after the original Unity Editor recompiles can the matching Unity V2 capture and same seed/input tick first-difference be compared. Formal EXE observable parity and Q07/R15 closure remain separate.
