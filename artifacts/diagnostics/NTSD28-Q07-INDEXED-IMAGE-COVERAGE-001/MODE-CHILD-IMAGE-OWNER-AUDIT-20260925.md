# Mode child image owner audit (2026-09-25)

## Four previously unresolved references: playable caller follow-up

The formal root EXE SHA-256 was freshly rechecked as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. All four PNGs exist under formal `resources/runtime/vfs` and are absent from staged `LoganRuntime/vfs`; an initial direct check against `resources/runtime` without its `vfs` segment was incorrect and was corrected before drawing conclusions. This is a read-only source/data audit, not a new EXE-visible or Unity Play result.

| Formal mode-child reference | Checked playable consumer | Bounded owner conclusion |
| --- | --- | --- |
| `sprite/UI/CharMenu.png` (`<menu_control>`) | `GameSession28::selection_render_snapshot` resolves resource catalog index 32 from `data/resource.dat` (`game_session.cpp:3507-3509`); that index is `CHARMENU.png`. `D3D11Renderer28::render_selection` draws `background_source_path` (`d3d11_renderer.cpp:1123-1150`). | Active ordinary character-selection background. The exact draw uses the resource catalog rather than the mode-child `pic:` token. Protected selection presentation; not a Q07 combat image gap. |
| `sprite/small/random.png` (`<grid_char>`) | `game_session.cpp:3513,3652-3655` resolves it into `random_roster.small_source_path`; `d3d11_renderer.cpp:1135-1176` draws the random roster cell. | Active ordinary character-selection grid image; not a battle rendering dependency. |
| `sprite/UI/BGBOX.png` (`<grid_bg>`, `<grid_bg_mode>`, `<grid_stage>`) | The selected-child reader at `game_session.cpp:978-1021` consumes only `<grid_stage>` `row:`; no `BGBOX` path or corresponding `pic:` projection occurs in the inspected playable source. The selection snapshot instead resolves `BG_FRAME.png` for the background preview at `game_session.cpp:3562-3564`. | Mode-child declaration belongs to prebattle grid configuration. No active PNG consumer established in the inspected playable closure; do not claim the formal EXE never displays it. |
| `sprite/UI/menu_clip2b.png` (`<misc>`) | No exact path, field projection or render-snapshot consumer was found in the inspected playable source; the selected-child consumers observed here load knockout/combo data and the stage-grid `row:` (`game_session.cpp:1332-1397,978-1021`). | Mode-child declaration only on current evidence. Active formal EXE display is unknown; no basis for Q07 combat staging. |

The original mode DAT is user-excluded, and the Unity menu/selection presentation is retained. None of these four images should be copied merely to reduce the 114 missing `sprite/*` count. A later claim of battle relevance would need an exact formal battle caller and visible trigger. The 1255/1031/224 and 27/4/23 inventory counts are unchanged; no DAT, PNG, script, Scene, menu or project configuration was modified.

Status: `VERIFIED_STATIC_MODE_BATTLE_IMAGE_PRESENCE / MENU_OWNER_PENDING`. Read-only check of the formal root release's `resources/runtime/decoded_dat/data/mode/ntsd.dat`, corresponding VFS PNGs, Unity staged VFS, the paired playable `GameSession28`, and project `ProjectBattleModeConfig`. The root formal EXE identity is SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The original mode DAT is user-excluded from Unity production; this audit does not propose copying it.

The formal child text references 27 distinct `sprite/*.png` paths. All 27 exist in the formal VFS. Exactly four are staged in Unity at the same virtual paths: `sprite/kill/c.png`, `sprite/kill/sk1.png`, `sprite/kill/sk2.png`, and `sprite/combo_hits.png`; the preceding full-VFS byte audit established all common PNG paths match the formal SHA-256 bytes. `GameSession28`'s inspected playable mode-consumer branch loads knockout-feed and combo definitions from the selected child at lines 1332–1377. The project's `ProjectBattleModeConfig` supplies the seven formal type-image choices from the three staged kill images, while combo uses the staged atlas. Thus the 23 absent images are not missing from those two specific battle image inputs. This is input presence and source mapping, not proof of Unity publication, drawing, or audible/visual parity; those remain Q09/Q10.

Fresh per-file SHA-256 equality was also confirmed for the four staged images: `c.png` `2D0B189CCF2DD8D1BCDA14E3E104E0655A2A3F17C4F5711F8A6C7215E981B3C1`, `sk1.png` `570F877D12D532AAA45CE4BFE697ACF6D7F77C76476A54416197D64598C65974`, `sk2.png` `714D16C60B11D21FDF63A594DAE528BD3E406A1751BE07B01E4756F1DF2439F4`, and `combo_hits.png` `1D398C0AE0AE01495B7FF5FEC78B7978690CFE53B98D10913E1BCEC92426AD48`.

The 23 absent references are `sprite/small/random.png` plus 22 `sprite/UI/*` entries: four other UI images (`BG_RANDOM`, `BGBOX`, `CharMenu`, `menu_clip2b`), thirteen `menu_small` paths (`base`, `base2`, `base2b`, `m1`, `m2`, `m3`, `m3b`, `m4`, `m4b`, `m5`, `m6`, `m7`, `m8`), and five team flags (`TF0`–`TF4`). This is 1 + 4 + 13 + 5 = 23. `GameSession28`'s inspected post-roster projection uses the `menu_small` selection art, `BG_RANDOM` and team flags at lines 3557–3643. `menu_clip2b`, `BGBOX`, `CharMenu`, and `sprite/small/random.png` are present in the formal mode text but their exact active caller was not established by this check; they stay with the protected menu/selection owner rather than being declared battle defects or proven unreachable. The project does not import the original mode DAT by user decision, and its own nonbattle menu is retained.

Disposition: do not bulk-copy these 23 PNGs into Q07 for a numeric VFS target. Keep any future non-exception battle caller under Q09/R17 with exact image, trigger and pixel evidence. This audit changed no DAT, PNG, script, Scene or project configuration; the whole-VFS PNG count remains formal 1255 / Unity staged 1031, with 224 absent including 110 user-excluded background images and these 23 mode-child references.
