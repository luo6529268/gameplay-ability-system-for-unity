# Q07 formal `resource.dat` production consumer closure (2026-09-25)

Status: `VERIFIED_STATIC_CATALOG_SCOPE_ONLY`. This read-only audit narrows the 48-entry native resource catalog in the current formal playable source; it does not close Q07, Q09, R17, runtime publication, pixel parity, or old-file retirement. The formal release EXE was freshly SHA-256 checked as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. `source/README_SOURCE.md` declares the paired source and the playable build script includes `native_resource_catalog.cpp`, `game_session.cpp`, `d3d11_renderer.cpp`, and `main.cpp`.

`NativeResourceCatalog28::parse_text` (`ntsd28_core/src/rendering/native_resource_catalog.cpp:39-81`) reads only `pic:` tokens in `<bmp_begin>` and caps the catalog at 48 entries. `virtual_path` is bounds checked and `resolved_path` (`:96-119`) resolves only a selected entry, with a BMP-to-PNG fallback. The inspected production `ntsd28_core`/`ntsd28_playable` source does not enumerate `entries()` or select a catalog slot through another caller. Seven later `<frame>` paths in `resource.dat` are outside this catalog; that parser fact alone does not prove they are unused elsewhere.

| Source-selected purpose | Catalog indices | Production path and gate |
| --- | --- | --- |
| Active battle name/lives glyphs | 16-21 (6) | `game_session.cpp:1225-1239` resolves WORDS0..5, `:3317` passes the paths to `RenderSnapshotBuilder28`, whose glyph commands reach `d3d11_renderer.cpp`'s glyph atlas draw. |
| Active battle spark | 43 (1) | `game_session.cpp:1241-1246,3316` resolves SPARK; `render_snapshot.cpp:1643` assigns it to spark commands and `d3d11_renderer.cpp:1799` consumes the texture path. |
| Ordinary result scoreboard and story result | 24-26,28-31 (7); 3 (1) | `game_session.cpp:3353-3393`; scoreboard drawing is gated by `battle_mode == 0 && result_visible` (`render_snapshot.cpp:1192`), story result by mission completion/result state/timer. These are result surfaces, not ongoing battle sprites. |
| Selection | 0,32-38,41-42 (10) | `game_session.cpp:3485-3524`; `selection_render_snapshot()` requires `selection_active_`. The `33 + digit` loop has five digits; 38 is the join prompt. |
| Loading atlas | 47 (1) | `game_session.cpp:367-421`; consumed by the dedicated loading snapshot/renderer. |
| Selection background preview via literal path | 7,8 (2) | `game_session.cpp:3557-3588` uses fixed `BG_RANDOM`/`BG_FRAME` paths through `resolved(...)`, outside catalog-index selection and within the selection snapshot. |
| No selected production catalog consumer found | 1-2,4-6,9-15,22-23,27,39-40,44-46 (20) | The inspected playable/core production sources contain no call selecting these indices. Index 22 is PAUSE; 40 is mentioned only in a comment about an alternate gate while the live selection call chooses 41. This is a bounded negative source finding, not an EXE-wide never-used claim. |

The 48 catalog indices are accounted for without overlap: 7 ongoing battle + 8 result + 10 selection + 1 loading + 2 selection-literal + 20 without a located selected consumer. The existing report's *nine absent selection images* is a different denominator from the ten selection **indices** above: index 32 belongs to the two-DAT owner group, not the `resource.dat`-only missing group.

The `selection_render_snapshot()` dynamic `resolved(...)` fields (`game_session.cpp:3493-3496,3564-3577,3640-3647,3666-3667,3710,3758`) come from fixed menu paths, selected character `small`/`head`, or menu-face-layer `pic`. That whole function is guarded by `selection_active_`; it does not create another ongoing-battle consumer of the 20 unselected catalog entries. The playable host calls separate selection/loading renderers (`main.cpp:2658-2699`). Other DAT-driven battle image paths are outside this *resource-catalog* conclusion and retain their own Q07/Q09 audit.

Fresh disk check parsed the formal 48-entry `<bmp_begin>` block and independently hashed formal `resources/runtime/vfs` against Unity `Assets/NTSD/Content/LoganRuntime/vfs` for indices 16-21 and 43: all **7/7 paths exist and all 7/7 SHA-256 values match**. This proves selected content bytes on disk, not Unity importer binding, publication or drawn pixels. No DAT, PNG, Unity script, Scene, asset, or old file was changed. The next Q07 work should test remaining formal-content runtime publication/natural skills and old-reader ownership; Q09 retains actual visual comparison. Do not copy the other catalog images solely to make counts match or delete existing assets from this static finding.
