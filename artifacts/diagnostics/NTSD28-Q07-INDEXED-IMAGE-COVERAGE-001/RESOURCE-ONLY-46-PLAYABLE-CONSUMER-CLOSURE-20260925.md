# Q07 `resource.dat`-only absent PNG consumer closure (2026-09-25)

Status: `VERIFIED_STATIC_SCOPE_ONLY`. This is a read-only continuation of `MISSING-SPRITE-DISJOINT-OWNER-MATRIX-20260925.md`; Q07/Q09/R17 and final visual acceptance remain open. The formal root `NTSD2.8-Logan.exe` was freshly SHA-256 checked as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. `source/README_SOURCE.md` identifies its paired source, and `source/ntsd28_playable/scripts/build.ps1` includes `game_session.cpp`, `d3d11_renderer.cpp` and core `native_resource_catalog.cpp` in the playable target. This audit does not execute either runtime.

The formal `resources/runtime/decoded_dat/data/resource.dat` contains exactly 48 `<bmp_begin>` entries and seven later `<frame>` paths. `NativeResourceCatalog28::parse_text` in `source/ntsd28_core/src/rendering/native_resource_catalog.cpp:39-81` collects only the first block; the seven `UI/extra/*` paths cannot be selected through that catalog. Current staged VFS has the six WORDS images at indices 16-21 and SPARK at 43, all previously byte-matched to formal. Of the other 41 absent catalog images, indices 7 (`BG_RANDOM`) and 32 (`CHARMENU`) also have a formal mode-child DAT reference. The remaining **39** are the `resource.dat`-only absent `<bmp_begin>` images in the disjoint owner matrix. Adding seven `<frame>` paths yields that matrix's **46** resource-only absent PNGs.

| `resource.dat`-only subgroup | Count | Inspected playable consumer and disposition |
| --- | ---: | --- |
| Direct indices 0, 33-38, 41-42 | 9 | `GameSession28` selection snapshot in `game_session.cpp:3507-3526`; protected character-selection surface, not an active battle sprite. Index 32 is a tenth direct selection image but belongs to the shared-DAT group and is excluded from this row's 46. |
| Direct indices 3, 24-26, 28-31 | 8 | Story result index 3 and ordinary result scoreboard indices 24-26/28-31 at `game_session.cpp:3350-3390`; result surfaces are outside the current battle-presentation replacement scope. Index 27 is declared but is not among the inspected scoreboard snapshot paths. |
| Index 8 `BG_FRAME` | 1 | A literal `resolved("sprite\\UI\\BG_FRAME.png")` in post-roster selection projection at `game_session.cpp:3558-3568`, rather than a `native_resources_.resolved_path` call. Protected selection surface. |
| Index 47 `Loading/loading.png` | 1 | `FrontendScene28::loading` atlas at `game_session.cpp:395-425`; pre-battle loading surface. |
| Index 22 `PAUSE` | 1 | Declared, but no selected image consumer was proved in the inspected playable battle renderer; the prior pause audit found a solid-color pause bar. Do not infer that the formal EXE can never show this PNG. |
| Other declared indices 1-2, 4-6, 9-15, 23, 27, 39-40, 44-46 | 19 | No `NativeResourceCatalog28::resolved_path` selection for these indices was found in the inspected production `GameSession28`/core callers. A basename scan of playable/core `src` and `include` found no exact filename literal for these 19. This is **unproved selected consumer**, not an EXE-wide unreachability proof or an approved exception. |
| Later `UI/extra/*` `<frame>` paths | 7 | Outside the selected 48-entry parser block; no direct basename consumer was found in the prior targeted audit. |
| **Total resource-only absent paths** | **46** | **17 + 1 + 1 + 1 + 19 + 7**; disjoint with mode-child and other DAT-owner rows. |

The complete 19-path unproved-index list is: `MENU_CLIP`, `MENU_CLIP2`, `MENU_CLIP4`, `MENU_CLIP5`, `MENU_CLIP6`, `SLOGAN`, `ENDING`, `CS2`, `CS3`, `CS4`, `CS5`, `CS6`, `DEMO`, `SCORE_BOARD4`, `CMA2`, `CMC`, `BATTLEMODE`, `BATTLETROOPS`, `information` (all under `sprite/UI/` with `.png` suffix). The exact filenames matter because a declared resource slot is not itself evidence of a battle draw call. Index 7 `BG_RANDOM` is a shared-DAT post-roster selection literal; index 32 `CHARMENU` is the shared-DAT ordinary selection background. Both are separately classified in `MODE-CHILD-IMAGE-OWNER-AUDIT-20260925.md`.

Method: enumerate the formal `<bmp_begin>` index order, check every indexed staged path on current disk, subtract the two shared-DAT paths, then classify direct catalog call sites and inspected literal call sites. The basename scan covered `source/ntsd28_playable/{src,include}` and `source/ntsd28_core/{src,include}` C++/header text; it was not a data-flow proof for dynamically constructed paths. No DAT, PNG, Unity code, Scene or config was changed. This closes only the 46-path **static owner/inspected-consumer classification**. It does not authorize copying the 19 unknowns, removing old files, replacing protected UI, or marking Q07/Q09/R17 complete. The remaining battle-facing verification stays with natural skills, runtime publication, old reader retirement and image pixels.

Fresh count assertion on the formal DAT and current staged VFS exited 0: `RESOURCE_ONLY_46_CLASSIFICATION_PASS 39 bmp + 7 frame; staged battle=7; shared indices=7,32`. It checked the 48/7 parser sections, exact seven staged battle indices, subgroup sizes 9/8/19, disjointness and the 39+7 total. This count check does not execute C++ or Unity.
