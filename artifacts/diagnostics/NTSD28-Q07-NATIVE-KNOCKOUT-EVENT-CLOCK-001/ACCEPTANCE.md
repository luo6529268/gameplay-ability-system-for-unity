# Q07 native knockout event clock focused result

Status: `FOCUSED_TEST_PASS`; full Q07/runtime presentation exit remains open.

The shared Unity KO writer now records the already maintained C24 `NativeFrameSequence`. It does not subtract from the host tick or branch on character, attack or producer type. The formal paired source stores its current `sequence_` in `BattleWorld28::record_native_knockout`; `SimulationTickDriver28::step` increments that sequence after ordinary hit and catch settlement, before resource and late-slot work.

Original-project Editor import compiled runtime and Editor assemblies later than the changed sources with zero Console errors. Focused pre-/post-C24 production-path tests passed 2/2 (`7990a04b97504912b48ede5251f7c9a3`): ordinary/reduced KO records sequence 7 despite host tick 8, and negative-environment resource KO records sequence 8 despite host tick 9. Q08 event lifetime/snapshot/checksum/reset tests passed 3/3 (`0f1c7f586f154bbc8b3ac627792cd468`); Q10 selected mode cue event tests passed 5/5 (`6dd8d8d1f9464e5b8f7710428082a289`). The first attempted Q10 filter matched zero tests and is not counted.

The exact existing 30-tick Naruto scenario ran once in the occupied original Editor and returned `PASS`. New versus pre-change raw/domain/input-RNG tick payloads are unchanged 30/30 each. The first KO still appears on completed tick 8 and now has battle time 7 plus the same five source/owner/victim/type fields as the formal root EXE trace. `comparison.json`, the four `original-editor-run1.*.jsonl` files and the test-job JSON files contain the machine evidence. The raw stream header changed because the source/assembly hash changed; this is not a tick payload difference.

One downstream Q09 row projection test failed in `SetUp` because it reads `Assets/NTSD/Content/LoganRuntime/decoded_dat/data/mode/ntsd.dat`, an original mode DAT excluded by user decision. No mode DAT was restored. That test does not assess this timestamp fix until its fixture consumes the project mode Asset or a self-contained fixture; it remains a Q09 test-dependency item. Event buffer lifetime/snapshot/checksum were independently tested as above, but the production row's precise visible end tick and formal pixel comparison remain pending. The edited Q07 Menu and Q10 Battle Play probe assertions compiled but were not executed in Play for this package.

The original Editor returned to idle EditMode with zero Console errors. Menu Scene SHA-256 stayed `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`, Battle Scene stayed `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`, and GameConfig Asset stayed `0527D737A1FA38FC56B51D00DC6E96A421D3C67222546368B147C2D074CB8EA7`. No DAT, PNG, Scene, ProjectSettings or nonbattle implementation was changed. Q07 and the overall alignment remain open.

## Original Scene Play follow-up (2026-09-26)

The existing real-Menu physical-J probe was rerun once because its ordinary-KO timestamp assertion changed. Unique run `q07-ko-native-clock-menu-play-20260926-1` returned `PASS`; the copied exact report `original-battle-play-physical-j.json` has SHA-256 `B5A69F7404130D9D72DC9A51C1D1FE5FCC393C876CB1C8C3E97BC541AC056225`. It reached an authored Naruto punch and attributed KO at completed tick102, recorded the required native event, observed result timer 0→1→80→101→350/transition2, returned through the production selection route, destroyed the old Driver and left zero pool borrowers. This is a new run under a different Play initial state, so tick102 is not compared with the exact Editor/source tick8 fixture. After exit, the original Editor was idle EditMode on Battle Scene with zero Console errors, the request restore flag was false and the three protected disk hashes above were unchanged. The report does not contain the numerical event timestamp; the updated probe asserted the pre-C24 sequence rule in Play, while the exact exported value 7 is independently proven by the 30-tick EditMode trace.

## Q09 test dependency follow-up (2026-09-26)

`NTSD28-Q09-PROJECT-MODE-TEST-FIXTURES-001` removed the excluded mode-DAT assumption from the row fixture. The original Editor now completes the affected row/mode/icon set 9/9, including the synthetic 30-tick row boundary. The earlier `SetUp` failure is resolved without restoring mode DAT. Exact same-state production event-to-visible-end timing and formal pixel comparison remain pending, so this Q07 package stays `FOCUSED_TEST_PASS`.

## Same-state production row follow-up (2026-09-26)

`NTSD28-Q07-KO-FEED-PRODUCTION-ROW-WITNESS-001` now gives an original-Editor full-Driver 38-tick continuation of the exact Naruto-punch scenario: event born on completed tick8/time7, one published feed row at ticks8–36, zero at ticks37–38, one retained native record, and zero World objects/slots/borrowers after ordered shutdown. The exact focused test passed 1/1; its three earlier fixture failures and final CSV/job are retained in that package. This closes the scoped event-to-published-row timing question in EditMode; formal EXE/Unity pixels, other Q07 cases and the aggregate Q07 exit remain open.
