# Unity platform continuous tick and replay result

Status: FOCUSED_TEST_PASS / FULLTICK_AND_REPLAY_THREE_PASS.

Actual Unity EditMode job af41921adf134f708c2da41377bedc53 executed exactly three tests, all Passed (0.8989312 seconds). The runner discovery total8113 does not mean 8113 tests executed. Archived XML: fulltick-pass-af41921a/results.xml; twelve per-tick observations are stored beside it. Unity refresh requested compilation; after reload all three tests ran, and error-CS Console query returned zero entries. Change Ledger validator passed.

Only NTSD28Q06PlatformTransactionEditorTests.cs was changed, after exact symbol declarations in Task/Change. Production code was not modified. Existing actual World/catalog/factory are used, with canonical AI profile established before registration and AI disabled. Both entities start from pinned source before/platformBefore; initial projected fields are asserted. Seed42 and stage800/180/350 match the source fixture. No candidate/motion helper is run before the actual NTSDBattleTickSystem.RunReleaseTick calls.

Three representatives: nominal, fractional ITR dvy -2.5, left-facing platform. Each compares rider AND platform integer/precise XYZ, collision reference, platform slot, shadow offset carrier and previousY after ticks1 and2. Restoring the initial snapshot and repeating both ticks matches the same source projections and each original Unity full runtime checksum.

Scope limits: native previousX/Z and all other native raw fields are not compared; checksum repeatability is Unity replay proof, not a full native-state checksum equivalence claim. Diagnostic source witness is not formal EXE image recording. Synthetic data is not the Q07 formal-content migration. Shadow offset carrier is not the Q09 visible shadow consumer.

Reuse unchanged candidate42, linked-motion representatives, physics branches, mixed-candidate3, stable SelfCheck12:14:34Z, and actual Renderer platform0/physics20 with Q05 teardown12:22:52Z. No redundant all-role/all-scene/SelfCheck run performed. Q06 remains incomplete. Parent scope and Q09 deferred shadow contract must be reconciled explicitly before whole-package VERIFIED; do not silently drop that requirement.
