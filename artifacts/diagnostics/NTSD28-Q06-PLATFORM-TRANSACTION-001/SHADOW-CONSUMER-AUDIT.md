# Platform shadow consumer audit

Read-only current-path evidence, 2026-09-21. Source render_snapshot.cpp:1623 positions shadow at entity.position.z + render_shadow_offset_10c, while depth_order remains entity.position.z. Logic carrier reset/copy/checksum exists; presence does not prove display consumption.

Unity gaps: LF2Entity.UpdateShadow line559 uses GetRenderZInt only. BattlePresentationEntitySnapshot in BattlePresentationShadowBuild.cs:373 has no offset; capture constructor at2248 and copy methods WithResolvedSprite(523)/WithPresentationBaseOrder(573) must preserve a future explicit value. BuildCommands at2700 builds stableGroundPosition from ZInt alone; shadow uses it at2726. The same position feeds entity self-foot-marker anchor at2804, so changing it globally would alter an approved UI/presentation exception. A future offset must affect only shadow command position; keep entity Z, depth sort, health anchor and foot marker unchanged. Logic state must never read back presentation offset.

Q06 platform logic source/carrier/candidate/motion gates remain separate. Q09 formal graphical consumption has Q07/Q08 dependencies in queue; retain explicit cross-phase return rather than claim platform appearance complete or make all graphics an entry gate for DAT migration. Exact future production scope: LF2Entity.cs UpdateShadow; BattlePresentationShadowBuild.cs snapshot constructor/property/two copy methods/capture/only shadow command position. Existing publication ownership unchanged, no new shutdown owner. Must declare Task/Change and focused tests before script changes, verify publication/copy and legacy/central output plus unchanged foot-marker/order/checksum; real graphics validation follows resource dependencies. No implementation or tests executed by this audit.

Next Q06 action: actual following full-tick source capture and snapshot replay for frame-motion tail representatives. Preserve two unresolved late-update failures and Destroy-owner Renderer closure.
