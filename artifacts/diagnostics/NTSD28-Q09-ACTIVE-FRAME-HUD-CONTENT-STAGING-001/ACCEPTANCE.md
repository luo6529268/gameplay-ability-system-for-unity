# Active INKHUD content staging acceptance

Date: 2026-09-22. Status: `VERIFIED_EXACT_CONTENT_STAGING_ONLY`; BATCH-05/Q09 and R17 remain open.

The formal root executable was rehashed SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The corresponding playable `GameSession28::initialize_resources` loads `data/frame.dat`, selects captured active entry 0 (`data/frame/INKHUD.dat`), and passes the parsed `native_frame_hud` to `render_snapshot.cpp`. The child table's `frame`, `bars` and team-flag paths select exactly seven unique PNGs (team5 aliases team0). Its radar section has `bound: 0`, so radar images are not included in this active closure. INKHUD2 and minibar styles are separate conditions.

Before this Task, all nine selected destinations were absent from the original project's `Assets/NTSD/Content/LoganRuntime`. Two formal DATs and seven formal PNGs were copied to matching relative paths, with nine asset `.meta` files and three folder `.meta` files. [MANIFEST.csv](MANIFEST.csv) records each relative path, byte length and source SHA-256. All nine destination byte lengths and SHA-256 values were independently rechecked against both the manifest and formal source. Seven PNG headers are valid: `FRAME` 175×73, `BARS` 100×50 and five team flags 54×37. The 12 new GUIDs are pairwise distinct and have zero collisions against all `Assets/**/*.meta` GUIDs. Staged `LoganRuntime` now contains 337 DAT and 1028 PNG; compared with the 1255 formal PNGs, 227 remain unstaged.

Only the named battle content files and their `.meta` files were added. This Task did not change scripts, scenes, prefabs, GameConfig, ProjectSettings, menus, audio, legacy assets or nonbattle behavior. It did not delete any file. The original Editor may later import these staged assets, but no fresh Unity reader, INKHUD screen pixel, formal EXE comparison or ordered exit/re-entry was verified by this content-only Task. The separate Q09 WORDS focused request remains result-pending and must not be treated as this package's runtime evidence.
