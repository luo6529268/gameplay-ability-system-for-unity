# Q07 out-of-range base-frame visibility gate — 2026-09-26

Result: `VERIFIED_STATIC_VISIBILITY_SCOPE_ONLY`. The 84 non-999 base `pic` rows in the prior [all-frame census](../NTSD28-Q07-ALL-FRAME-SPRITE-COVERAGE-001/REPORT-20260925.md) are outside the formal native cumulative sheet capacity for their own DATs. In the inspected current playable production path, the render picture offset starts at zero and its writers reset it to zero or assign a positive value. Adding that offset cannot bring any of these 84 pics back into range. The formal resolver therefore issues no body-sprite command for those selected pics. They are not 84 missing PNG files or 84 Unity image-repair tasks.

Fresh identity and content checks:

- Root release EXE SHA-256: `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`.
- The prior census contains exactly 84 rows in 21 DATs. Recomputing cumulative `row*col` capacity from the current formal DAT text found 84/84 `pic >= capacity`; the 21 corresponding staged DATs match formal SHA-256, with zero mismatches.
- Across all 330 indexed object DATs, staged and formal DAT SHA-256 matched 330/330. Their largest cumulative sheet capacity is 560, so the authored `pic:999` sentinel is also beyond every current object sheet range.

The paired `dat_parser.cpp` `project_sprite_sheet` builds each range from cumulative `row*col`, ignoring authored `file(first-last)` endpoints for runtime selection. `render_snapshot.cpp` computes `pic = frame.pic + revive_visual_runtime_318` before calling `SpriteFrameResolver28::resolve`; `resolve` returns no sprite when no cumulative range covers the pic. The checked playable writers of `revive_visual_runtime_318` initialize it to zero, reset it to zero in fusion, or assign `visual` only after `visual > 0` in revival (`battle_world.cpp` around 2776 and 8417–8430). No negative production writer was found in the paired core/playable closure. Unity's current `GetRenderPicIndex` also leaves `pic:999` hidden before applying `RenderPicOffset`; its nearby comment describes a different order from the paired render-snapshot source, but with current maximum capacity 560 and nonnegative production offsets the visibility result is the same. This audit does not change that script comment or production logic.

This is a static *no-selected-body-sprite* conclusion for the 84 recorded base rows under the inspected offset writers. It does not prove which actions naturally occur, cover future DATs or externally injected negative offsets, validate shadows/effects/sparks, or provide root-EXE GPU pixels. The six **in-range** source rectangles outside their PNG image dimensions are a separate issue: their generic catalog publication was previously fixed and tested, while natural reachability and formal/Unity same-condition pixels remain Q07/R17 work. Q07, R17, BATCH-04 and the total alignment goal remain open.

The verification was read-only. No Unity test or Play was run because no runtime code or resource changed. No DAT, PNG, Scene, importer, old asset or script was modified.
