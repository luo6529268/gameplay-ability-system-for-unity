# Type3 target generic frame-binding audit

Current formal playable source and Unity reviewed by root and /root/effect_fall_review. Production remains unchanged at this checkpoint.

Actual single old binder: BattleDamageWriter.ApplyNativeType3TargetGenericContinuation -> DirectWriteHeldFramePreserveWaitCounter. Native raw replacement preserves raw action, latch/history and no frame-entry effects. Retain existing team/owner/control/speciallatch transfer, active negative-link parent resolution, pendingXYZzero with contribution count preserved, current motion preserved, Fj/Uj selection, zero-only30/20fallback and counter0. Locked kind transform is separately gated.

Complete source successor matters: reset_native_matched_projectile_pair reads NEW current descriptors; matched3005/3006 pair selects each latch hit_Uj/20 and clears both pending totals/counters. A generic selected900state3005 may therefore end at a different final action. Source11 includes a real post-selection pair rather than assuming intermediate900 is final. Effect override occurs later; effect0 isolates it. Initialtarget3005skip is already represented in prior Type3 attacker sourcecase12.

Projection is an independent limitation: CanProjectStandardType3DamageWriterEffect uses old generic destination getter and rejects high frames even declared; low implicit returns legacy EmptyFrame and is admitted. ProjectStandardType3DamageWriterEffect responseState also uses old getter to decide pair reset. Widening gate alone would mispredict high3005/3006; both points require exact declared follow-up and source validation. Locked HasFrame40 is not generic and must not be batch-replaced. Invalid/missing raw actions are legal writes even while projection is unsupported.

Static formal150 type3 objects(non3005 frame headers): Fj5865declaredlow/128implicitlow;Uj5809declaredlow/184implicitlow;312nondeclared responses,66 on frames withBDY. HasBDY is not dynamic hit eligibility. Full inventory in remaining-live-readers/FORMAL330-TYPE3-TARGET-RESPONSES.json.

No new source execution, Unity verification or production changes are claimed by this audit. OldB5 generic continuation responsibility remains closed; new work concerns native descriptor and its actual successors.
