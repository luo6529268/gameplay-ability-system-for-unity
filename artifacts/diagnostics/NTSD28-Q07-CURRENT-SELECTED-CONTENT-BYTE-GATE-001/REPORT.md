# Current Q07 selected battle content byte gate

Status: `VERIFIED / SCOPED_SELECTED_BATTLE_BYTE_GATE`, **not** Q07 completion. Read-only check on 2026-09-26. Root formal `NTSD2.8-Logan.exe` SHA-256 was freshly verified as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. Its `resources/runtime` is the byte source; Unity's `Assets/NTSD/Content/LoganRuntime` is the staged destination. The user's background and both mode DAT exclusions were applied before the DAT count.

| Current byte group | Formal | Staged | Missing | Extra | Same-path SHA differences |
|---|---:|---:|---:|---:|---:|
| Nonexcluded `decoded_dat/**/*.dat` | 353 | 338 | 15 | 0 | 0/338 |
| `vfs/**/*.png` | 1255 | 1031 | 224 | 0 | 0/1031 |

The 15 DAT paths are exactly: alternate native HUD `data/frame/INKHUD2.dat` (1), protected Menu `data/menu.dat` (1), approved P-14 overhead bars `data/minibar.dat` plus four style children (5), user-held default `data/stage.dat` and its seven story children `s/*` (8). The formal `data/bgm.dat` is already staged and included in the common 338; WMA/BGM playback is a separate Q10 gap. None of these 15 is a newly evidenced missing non-exception selected ordinary-battle DAT; the stage/story hold is preserved, not silently classified as complete.

The 224 missing PNG split exactly into 110 `b/*` background images, excluded by the user, and 114 `sprite/*` paths. A fresh reread of `NTSD28-Q07-REMAINING-SPRITE-CONSUMER-AUDIT-001/remaining-114-sprite-consumers.csv` found 114 unique paths equal to the current formal-minus-staged `sprite/*` set, with all 114 formal per-file SHA values still matching. Its ownership groups conserve 114: P-17 native HUD18, P-14 overhead bar4, P-19 result8, protected nonbattle frontend11, native Menu/result presentation64, shared Menu/story/result1, and declaration-only eight. The eight are seven `sprite/UI/extra/*` names in `resource.dat`'s trailing `<frame>` section plus `sprite/UI/PAUSE.png` in table index22. `NativeResourceCatalog28::parse_text` in paired playable code imports only `<bmp_begin>` blocks; inspected `GameSession28` battle resource resolution uses indices16–21 and43, while its other resolved indices belong to native selection/result surfaces. The paired D3D11 renderer's paused branch draws a solid bar. This supports **no selected battle PNG reader in the inspected source path** for those eight; it is not proof that the root EXE can never read them through an uninspected path.

As a positive control, formal `resource.dat`'s selected battle `WORDS0..5` and `SPARK` seven PNGs are all present at the same staged paths with matching whole-file SHA-256. The current Battle Scene Lee J→L run also used staged/formal identical `a/cha/cha.png` and produced visible child pixels over the retained project background, but that one case does not certify the entire catalog. Prior all-frame and geometry audits remain separate evidence.

**Decision for the next Q07 work:** no current evidence authorizes copying the 15 DAT or 224 PNG merely to reach 353/1255. Treat the inspected active battle byte selection as satisfied within this scope, and move Q07 effort to natural skill/lifecycle first differences, current legacy dynamic-reader ownership, and presentation/release verification. Keep the eight declaration-only paths as named R17/Q09 uncertainties; reopen an exact path if a real selected battle reader is found. Old 521-row retirement worksheet and deletion authorization remain unchanged (`deleteAuthorized=false`). This does not alter Q08 story, Q09 presentation, Q10 audio or Q12 final integration ownership.

Verification: Python enumerated formal/staged DAT and PNG relative paths and hashed each common file; the first inline run produced the full 338/353 DAT result and then stopped on a Python `dict - dict` error before PNG evaluation. A corrected independent PNG run produced 1031/1255, zero SHA differences, 110 background plus 114 sprite missing. A separate CSV reread checked 114/114 path-set equality and 114/114 formal SHA values; all seven selected WORDS/SPARK paths matched. Paired playable source was read at `native_resource_catalog.cpp::parse_text`, `game_session.cpp` resource resolution and `d3d11_renderer.cpp` paused draw; these are in the formal playable build closure. No Unity run or root-EXE GUI pixel capture was performed for this byte audit; the prior Lee run is linked only as a bounded positive control. No project source, resource, Scene or old asset was changed here.

After this report and the progress addenda, `Tools/Validate-ChangeLedger.ps1` exited 0 with 849 Records and five currently governed code diffs covered; `git diff --check` exited 0. The three live progress documents each still contained zero NUL bytes. These governance checks do not add runtime or release-GUI proof.
