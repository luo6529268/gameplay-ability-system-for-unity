# Q07 KO event to feed-row code boundary

Status: `VERIFIED_SCOPED_STATIC_BOUNDARY`, not a same-state runtime pixel witness. This is a read-only follow-up after the Q09 project-mode test fixture migration; no code, DAT, Asset or Scene changed for this report.

Formal playable source `BattleWorld28::record_native_knockout` in `source/ntsd28_core/src/simulation/battle_world.cpp:1403` stores current `sequence_` in both `event.sequence` and `event.battle_time_ticks`. The earlier exact Naruto-punch trace observed the ordinary-hit KO on completed tick 8 with event battle time 7. The core render snapshot reads `snapshot.tick = world.sequence()` at `render_snapshot.cpp:1309`; its feed projection at lines 1977-2090 draws a row only when `snapshot.tick < event.battle_time_ticks + 30`, keeps that row's spacing while `snapshot.tick <= event.battle_time_ticks + 30`, and tail-prunes by `lifetime_ticks`. The playable `GameSession28::step` calls `prune_native_knockout_tail(world_->sequence(), lifetime_ticks)` before snapshot handoff (`game_session.cpp:3292-3305`).

Unity's shared KO writer now stores `NativeFrameSequence` (`SimulationWorld.cs:614`); its runtime tail prune is called before render dispatch (`NTSDBattleTickSystem.cs:572-577`). `BattleKnockoutFeedRowProjection.Project` has the same strict row-window and inclusive spacing boundaries (`BattleKnockoutFeedRowProjection.cs:203-259`), and `SimulationBattleBufferModule.PruneNativeKnockoutTail` has the same newest-tail lifetime condition (`SimulationBattleBufferModule.cs:124-141`). The project-owned `ProjectBattleModeConfig.asset` sets `lifetimeTicks: 70`, `rowSpacing: 40`, and enables modes 0/1/4; these are current Unity content values, not claims that excluded original mode DAT is used.

For the *one event with battle time 7*, the inspected formulas predict a row on snapshot ticks 8 through 36 when both actors and mode/display gates remain valid; the row stops at tick 37, its row spacing remains on tick 37 and is removed at tick 38. If no newer KO arrives, newest-tail retention lasts through tick 77 and removal occurs at tick 78. These are code-level consequences of the recorded event time and current configured lifetime; they are not an observation that the actual Battle Scene still contains both actors or draws the row at each tick. The now-passing Q09 synthetic row test independently checks the equivalent strict/inclusive boundary for an event at time 10 (visible at 39, hidden but spaced at 40, spacing gone at 41).

No first difference was found in the inspected time-window and newest-tail predicates. Remaining exit: same-state production tick-to-published-row trace at the real Naruto event, plus visible formal EXE/Unity pixel comparison where permitted by the existing presentation exceptions. Preserve the 30-tick scenario and its root-EXE evidence; do not infer a full Q07/Q09 close from these static predicates or the synthetic test.

Follow-up: `NTSD28-Q07-KO-FEED-PRODUCTION-ROW-WITNESS-001` now provides the same-state Unity production Driver tick-to-published-row trace through tick38 with zero shutdown residue. The formal EXE/Unity pixel comparison and Q07/Q09 aggregate exit remain open.
