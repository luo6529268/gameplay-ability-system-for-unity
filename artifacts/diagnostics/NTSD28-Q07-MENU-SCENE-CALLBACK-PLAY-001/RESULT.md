# Q07 actual Menu Scene callback Play: first difference

Decision: `RUNTIME_FIRST_DIFFERENCE / BUILD_SCENE_CLOSURE_MISSING`. The actual Menu Scene callback chain reached the Fight confirmation, but this checkout could not additively load `NTSD_Battle`; Q07 Menu-to-battle acceptance remains open.

The test-only probe uses the saved `Assets/NTSD/Scene/NTSD_Menu.unity`, not a synthetic menu GameObject. It invokes the Scene's GameStart button pointer callback, waits for `LoadingPrewarmController` to publish formal content and drain its progress text, then confirms the VS option, Naruto OID2 role/team, zero-computer CMC and Fight option through those Scene components. It does not synthesize a physical pointer or keyboard and does not assess rendered UX.

First run `q07-menu-scene-callback-1.json` failed at the mode panel's original 10-second timeout after `prewarmed=true`. Source inspection showed `LoadingPrewarmController.Start` separately waits for pending progress text to drain. The original result remains. The probe then recorded that the formal-content prewarm had 749 pending text entries and gave the panel a bounded queue-based wait without changing UI production timing.

Second run `q07-menu-scene-callback-2.json` reached `phase=battle`: `prewarmed=true`, `pendingLoadingTextsAfterPrewarm=749`, `modeSelected=true`, `roleConfirmed=true`, `selectedCharacterId=2`. It then failed to reach `AppFlowState.BattleRunning`; no battle Scene or World actor was observed. The live Unity Editor log's first pertinent error is: `Scene 'NTSD_Battle' couldn't be loaded because it has not been added to the build settings or the AssetBundle has not been loaded`, from `AppManager.LoadBattleAdditive()` called by `CharacterSelectionController.StartBattle()`. Current `ProjectSettings/EditorBuildSettings.asset` has `m_Scenes: []`; the existing Q07 Windows Player build probe supplies the Battle Scene explicitly in its own `BuildPlayerOptions.scenes`, which explains why that separate Player check did not establish the Menu scene closure. The next probe revision adds a read-only enabled-Scene preflight so this exact dependency fails quickly and explicitly.

After that revision, a fresh Editor import/compile returned idle without a new `error CS` in the current log tail. The narrow negative request `q07-menu-scene-preflight-1.json` reported `FAIL / edit-preflight` with the exact missing-enabled-Battle-Scene reason in under a second and did not switch Scene or enter Play. This is an expected guard failure, not Menu acceptance.

This is a project Scene closure/configuration difference, not evidence that the formal DAT parser, character image publication or combat rules failed. The actual Menu return/ordered battle exit could not run because battle never loaded. `Assets/NTSD/Docs/android-mobile-readiness/h-02-android-build-scene-closure.md` independently records the empty Build Settings as an open build issue and requires a formal first-Scene decision for its Android implementation. This Q07 test did not edit Build Settings or implement that Android task. Do not mark the Menu callback chain PASS until the intended production Scene closure is established and the same flow is rerun.

After both attempts, the existing Editor was idle outside Play with `Assets/NTSD/Scene/NTSD_Battle.unity` restored. Requests were marked false and both result files were preserved. Menu Scene SHA-256 remained `6CF124A17F692325CED5774C3C10C3324AA047439BC6C3EB08856188A78AB4B1`; Battle Scene SHA-256 remained `BCD1047BF912C6A4A8BC9F3A76EAF3FA954211AD064E0402B1C01BF3BA0E9FB6`. No Menu/UI/App production code, Scene, GameConfig, resource or ProjectSettings file changed in this package. No computer-use was used.
