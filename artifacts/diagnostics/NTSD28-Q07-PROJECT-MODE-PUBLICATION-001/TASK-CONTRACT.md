> Final status (2026-09-24): VERIFIED for configured production mode-DAT replacement; event-level combo/KO behavior remains in its own Q08/Q09/Q10/Q12 gates.

# NTSD28-Q07-PROJECT-MODE-PUBLICATION-001

Status: VERIFIED for the configured production content path. User confirmed the original `data/mode.dat`/`data/mode/ntsd.dat` are excluded and requested independent `ProjectBattleModeConfig.asset` as their replacement. Asset schema package `NTSD28-Q07-PROJECT-MODE-CONFIG-ASSET-001` has focused Editor evidence; this package changes only the formal-content publication path and battle-side consumers to use the project Asset snapshot. The final adjacent staged/formal identity fixture job `c82592c1ad694d3dbf5cb4e358ab36af` passed 3/3 after the original DAT move; Q09/Q10 event behavior is separately gated.

Exact code ownership: `LoganModeComboInput.cs` adds a project snapshot factory, retaining native DAT parsing only for historical/diagnostic tests; `LoganObjectCatalog.cs` accepts a project snapshot and includes it in identity; `LoganVisualContentCandidate.cs` carries it through capture/freshness; `CharacterAnimtorManager.cs` captures the Unity asset on the main thread and passes only immutable fields to worker capture, with cache/publication freshness checks. Existing `SimulationTickDriver` and KO/icon/audio consumers continue through the same published input shape and do not need behavioral rewrites. Focused new Editor tests verify production candidate ignores original mode DAT bytes, changing the Asset snapshot changes identity, published combo/KO values and icon/cue settings come from the snapshot, and worker-side capture invokes no Unity API. The existing `NTSD28B11SourceCacheCallerEditorTests.cs` shared candidate fixture must use the same project snapshot as production because its caller tests assert exact candidate keys; this is a scoped expectation update, not a rule change.

Invariants: no original mode DAT read in configured production prewarm; no DAT token edit; no changes to GameConfig, Menu, Scene, Prefab, nonbattle logic or asset names; no new tick-time Unity API or allocation; both Scene disk hashes stable. First-tick/reset, snapshot/checksum and content seal continue through existing carrier. Old mode parser remains only for explicit historical test calls and cannot define project policy. The asset's initial serialized values preserve the current visible baseline while allowing project-specific future edits.

After the excluded files are moved, three existing Q07 Editor fixtures that compare staged and formal inputs must supply the same project Asset snapshot when testing current publication; retain the external formal native-mode hash vector separately as historical evidence. Their exact paths are declared in the Change Record. This prevents falsely classifying an intentional content-policy difference as a migration defect.

Validation: original Editor compile, narrow new focused tests, adjacent mode publication/KO tests where relevant, standard battle self-check and one Battle Scene Play publication/exit-reentry scenario after no full test job is running. The existing formal-staged `NTSD28B11SourceCallerPlayProbeEditor.cs` has a hardcoded pre-Asset identity; replace this assertion with explicit project mode snapshot provenance while retaining its source and shutdown gates. Track old tests that intentionally assert native mode identity as historical rather than modifying their expected evidence silently. Run ChangeLedger validator and inspect scoped diff. A test suite result from the accidental broad run does not certify this change.

Rollback: revert only this package's code changes through explicit repository approval; retain the independently created project asset. Do not remove the original two mode DATs until the production no-reader and runtime acceptance gates pass and the user performs the final file cleanup.
