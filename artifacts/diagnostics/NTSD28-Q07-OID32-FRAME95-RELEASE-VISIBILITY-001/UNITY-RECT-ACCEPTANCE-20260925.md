# OID32 formal sheet, original Unity Editor indexed-rect witness

Status: `FOCUSED_TEST_PASS / RECT_BUILDER_DIFFERENCE_CONFIRMED / PUBLICATION_AND_PIXEL_PENDING`. Original Unity Editor PID11944 was idle/out of Play on the saved Battle Scene before the test. The new test file was imported and `Assembly-CSharp-Editor.dll` recompiled in the original project. `refresh_unity` first returned a recovered-disconnect response before the file was imported; a second explicit refresh produced its `.meta` and a new Editor assembly. The first exact-name and category requests ran **0 selected tests** (`c7784660d1984f87b6a9f48a7fd396db`, `56a30398c2c34f1c95622ab8a649ea46`); those are not passes. After the import, category job `fe2dd9581b3b4f2791644341f2baf33e` selected the one exact `FormalHunterPic64CurrentIndexedRectResultIsMeasured` method and returned 1 total / 1 passed / 0 failed.

The test reads the staged formal Logan catalog with the project mode Asset, locates OID32's actual first `SpriteFileInfo`, decodes its referenced PNG, asserts the formal source path `m/nin/hun.png`, native cumulative range 0–139, cell 79×79, row10/col14, and PNG 799×480. It calls the production `CharacterAnimtorManager.BuildIndexedSpriteRects` on that actual file definition/dimensions. The adjacent valid pic54 has a rect; pic64 has no rect. This proves the current Unity indexed-rect builder omits pic64 for the deployed formal source data. The test's assertion documents **current behavior**, not acceptance of release parity; it should be revised or retired when a generic parity fix is made.

The previous formal source/root EXE controlled trace proves OID32/action95/pic64 remains active ticks0–8 and the paired source emits one slot0 sprite command each tick. The formal renderer's point/clamp handling plus 79/79 opaque bottom-edge pixels predicts a repeated edge strip, but no formal GUI GPU capture or Unity production SpriteCatalog/Play pixel was made. OID32 is in the formal `stageComHiddenObjectIds` set, so natural selectable reachability remains unproven. Do not infer a player-visible natural defect yet or modify DAT values/OID-specific logic.

`unity-rect-editor-result.json` SHA-256 `6CB0D78BB253C0D962D571D1FFF716990EEF63AA010361ECF7AFE1265C900D1B` records the selected job and both Scene hashes. Menu stayed `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`; Battle stayed `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`. No Scene, production script, DAT, PNG, Prefab or nonbattle logic was edited. The attempted in-memory MCP C# query failed before execution because its `mono.exe` command line was too long; it is not Unity behavior evidence.

Governance: the first validator run failed solely because the Record metadata incorrectly classified the Unity-generated `.meta` as a governed `code-path`; that line was corrected while retaining `.meta` in the declared Task scope. The second run passed with 829 Records / 25 governed code files in the shared diff. Both logs are retained in this directory. `git diff --check` returned exit0. No broad SelfCheck or Play was run for this diagnostic.
