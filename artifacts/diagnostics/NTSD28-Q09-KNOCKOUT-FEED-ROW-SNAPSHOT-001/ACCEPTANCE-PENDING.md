# Q09 knockout-feed row snapshot: current evidence (2026-09-22)

Status: `CODE_WRITTEN / OFFLINE_COMPILE_PASS / ORIGINAL_EDITOR_RUNTIME_PENDING`. This does not close Q09 or R17.

Production paths changed under the pre-edit Task/Change: `MatchConfig` gained optional battle-only ten-name/eight-bracket overrides and the native runtime-display flag; `SimulationTickDriver` configures names after match reset, selected published feed before tick 1, and clears the presentation input after worker join during ordered shutdown. `BattleKnockoutFeedRowProjection` reads the Q08 event list and raw/runtime slots, applies formal newest-tail count, display/mode gate, independent 30-tick window, excluded/missing actor row spacing, type0..6 selection, selected icon presence, labels, team color fields and 9-pixel-per-byte right alignment. It caches label strings per slot/object id/append mode to avoid a new string on every visible tick. `BattlePresentationFrame` now owns rows and diagnostics through reset and frozen copy, and the coordinator captures them on the common main/worker publication path. No render command, screen consumer, Scene, ordinary HUD/menu, old resource or Q10 sound was changed.

Focused Editor test source covers `t+29/+30/+31`, two ordered records, type3 and fallback type0, icon presence, excluded and missing actor spacing, native record/missing diagnostics, default and override names, mode/runtime-display/absent-feed gates, frozen copy/reset, CentralOnly publication, restore-style frame reset, and shutdown clear. The test is **compiled but has not been executed** by the original Editor.

`dotnet msbuild Assembly-CSharp-Editor.csproj -t:Build -p:Configuration=Debug -p:CustomAfterMicrosoftCommonTargets=<absolute Temp/diagnostics/NTSD28-Q09-KNOCKOUT-FEED-ROW-SNAPSHOT-001/current-sources.targets> -v:quiet -nologo` exited 0 with no `error CS` for current runtime and Editor source, including both new `.cs` files. This is an offline compile proxy only. `git diff --check` returned 0. The original project's live Editor PID 33236 still had 17:44 runtime/Editor DLLs, older than this change, and the earlier WORDS run still had no result file; no new TestRunner request or second Unity project was started.

Remaining acceptance: original Editor must import/compile the exact current scripts, run only the focused test after the prior WORDS run is terminal, and check its result. Then source/Unity first-difference at the same tick and seed, actual battle Scene screen consumer/layer/coordinate and formal EXE visual comparison remain. The native type-icon PNG is only marked present in row data; image publication, WORDS glyph draw and lifetime validation are separate Q09 work.

18:19 local Editor-state correction: a read-only bridge query returned a cache from 09:44:49 UTC (`sequence=9`) rather than a current state. Its `tests.is_running=false` is not proof that the older WORDS run is terminal. This package has not requested an Editor test. The row snapshot includes the formal team-colored glyph resource slot (0 or battle group 1..5); later glyph availability and screen consumption are still open.

2026-09-22 focused input-boundary correction: source review of playable `scenario28.cpp:710-725` established that a nonempty battle `playerNames` override requires exactly ten entries, each at most ten UTF-8 bytes with no NUL. The first Unity projection accepted partial and oversized lists. The declared projection now rejects those invalid overrides before changing its session state, and its focused test uses a valid ten-slot override plus partial/11-byte/NUL rejection cases. Current-source offline runtime+Editor MSBuild exited 0; `git diff --check` exited 0; `pwsh -NoProfile -File Tools/Validate-ChangeLedger.ps1 -RepositoryRoot <original project>` passed with 692 records/27 governed code files. This is source/compile evidence only; original Editor compilation, NUnit and formal same-tick evidence remain pending.

2026-09-22 original-Editor focused acceptance update: original Editor compiled current row/test source at 10:30:43/45Z. First five-test job `c4b9fdf9b056401bac3e9d5508b654ac` failed one fixture assertion (expected native record count 1, actual 0) because its second `SetNames` reset selected feed and the fixture omitted production's subsequent `SetFeed`. The test-only correction was declared before edit; Editor rebuilt the test DLL at 10:33:35Z and domain reload completed. Single failed case job `30798c0742634db9b2ec83c7b33cbf40` passed 1/1. The current five-case focused job `42d19ac30b5f44dfaff03be3ff84fecd` then completed `succeeded`, summary 5/5 Passed, 0 failed/skipped. Only the original project and five targeted methods were used. Status advances to `FOCUSED_TEST_PASS`; formal same-tick trace, screen consumer/icons/WORDS pixels, Play Scene layer and shutdown/exit are still open.

The original Editor bridge results are preserved as `original-editor-first-red-result.json`, `original-editor-single-green-result.json`, and `original-editor-focused-result.json` in this package. `pwsh Tools/Validate-ChangeLedger.ps1 -RepositoryRoot <original project>` passed after the test correction (692 records/27 governed code files), and `git diff --check` passed. The Editor result's discovery-progress `total=8208` counts discovered tests; its filtered run summary is exactly five executed tests, so it is not a full-suite result.
