# NTSD28-Q09-KNOCKOUT-FEED-ROW-SNAPSHOT-001

Status: `PLANNED`, before script modification. Parent: BATCH-05/Q09 and R17. This package owns battle-only row **data publication**; icon/WORDS screen consumption and native-pixel comparison are dependent Q09 work, not an acceptance claim here.

Authority: formal root `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; playable `GameSession28::render_snapshot` forwards its ten player names and eight bracket flags; core `render_snapshot.cpp:832-853,1977-2099` defines row names, newest-tail count, gates, record order, 30-tick display window, slot/exclusion behavior, icon type and spacing. The formal renderer consumes rows after mode HUD. Existing source audit: `../NTSD28-Q09-KNOCKOUT-FEED-ROW-PROJECTION-AUDIT-001/REPORT.md`.

Declared code ownership for this package only:

- `Assets/NTSD/Scripts/App/MatchConfig.cs`: optional battle-session-only name/bracket overrides; preserve existing ordinary local settings and serialized defaults.
- `Assets/NTSD/Scripts/Simulation/Host/SimulationTickDriver.cs`: capture the selected published mode feed and battle names after match reset, before first tick; do not read mutable DAT or Unity scene state on the worker.
- `Assets/NTSD/Scripts/Simulation/Presentation/BattlePresentationShadowBuild.cs`: retain a battle-owned row array/count in the frame, include it in frozen copy/reset, and invoke projection on both main-thread and CentralOnly worker publication paths.
- New `Assets/NTSD/Scripts/Simulation/Presentation/BattleKnockoutFeedRowProjection.cs` and `.meta`: immutable selected feed/name input and formal row projection using runtime slot read-only views and the published data catalog; no Transform or Mono singleton lookup.
- New `Assets/NTSD/Scripts/Test/Editor/NTSD28Q09KnockoutFeedRowSnapshotEditorTests.cs` and `.meta`: focused raw-slot/full-publication parity and frozen-copy/reset cases.

No Scene, Canvas/HUDBg, ordinary HUD/menu, GameLocalSettings, audio, render-command backend, nonbattle code, old resources or third-party/generated files are in this package. Any newly discovered need outside these paths requires a Task/Change correction **before** that script is edited. Preserve all existing dirty work on the listed files.

Contract: absent selected `#killtext` means no row projection and no invented 70-tick fallback; `bound=0` still leaves Q08 lifetime pruning active but suppresses display. Present feed uses its mode allowlist and current battle mode. Read records in original order after the formal newest-tail count; never compact before calculating Y. At `eventTime+29` a valid row can draw, at `+30` it cannot draw but consumes spacing, and only after `+30` does it stop consuming spacing. An excluded victim or missing actor consumes the same spacing while drawing nothing. Source type 1..6 chooses its matching path, all other types choose 0. The attacker is the four-owner slot; labels use the formal ten-slot defaults or explicitly supplied battle overrides, optional bracket flags, optional BMP `name`/`none`, and right alignment by 9 pixels per native text byte. Capture fields needed for later icon/WORDS consumption without binding a mutable Unity object into logical truth. Do not change knockout statistics, event producer, retention or Q10 audio.

Validation: exact native source-first cases for `+29/+30/+31`, consecutive records, excluded/missing slots, type 0/1..6, bound/mode gate, player/BMP name, bracket and right alignment; corresponding focused original-project Unity tests including frozen frame copy/reset, worker-safe raw slot lookup and snapshot/restore. Current-source compile, `git diff --check` and `Tools/Validate-ChangeLedger.ps1` after code. Use the existing original Editor only when the earlier WORDS TestRunner attempt is terminal; no concurrent test run or second Unity project. This package's data check does not close Q09/R17 until published icon/WORDS screen rows and formal EXE visuals are compared.

Risk/rollback: a wrong slot-to-name mapping, frame copy omission, race between published feed input and worker capture, or retained catalog reference could corrupt display or leak resources. Reverse only the reviewed diff owned by this Change ID if needed; never blanket reset, clean, delete or touch the user-confirmed HUDBg x=30 change.

2026-09-22 pre-edit correction, same declared paths: formal playable `scenario28.cpp:710-725` accepts a nonempty `playerNames` override only as exactly ten entries, each at most ten payload bytes and without an embedded NUL. The first implementation silently accepted partial/overlong Unity lists. Tighten only the battle override boundary in `BattleKnockoutFeedRowProjection.SetNames`, retain empty-list native defaults, and update its focused Editor test for valid ten-slot and invalid inputs. Do not alter `GameLocalSettings`, Scene or nonbattle input. Acceptance adds a focused validation case and the same current-source compile/diff/Ledger gates; original Editor runtime remains pending.

2026-09-22 pre-edit test correction: original Editor's focused `run_tests` job `c4b9fdf9b056401bac3e9d5508b654ac` finished failed with 5 completed and one `BattleOnlyNamesAndModeGateDoNotUseLocalPlayerSettings` assertion (`expected 1, actual 0`). The fixture calls `SetNames` again to disable runtime display; this deliberately clears the previous match's selected feed, while the production host calls `SetFeed` afterward. Correct only this declared test method to restore the feed after the second `SetNames`; keep production reset semantics. Rerun the single failed test, then assess the complete focused fixture evidence; no broad suite or Scene change.
