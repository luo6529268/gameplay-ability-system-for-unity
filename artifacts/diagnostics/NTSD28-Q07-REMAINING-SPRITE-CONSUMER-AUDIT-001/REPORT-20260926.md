# Q07 remaining 114 formal sprite PNG consumer audit — 2026-09-26

Scope: read-only classification of the 114 formal `resources/runtime/vfs/sprite/*` PNGs absent from staged `Assets/NTSD/Content/LoganRuntime/vfs`. Root formal `NTSD2.8-Logan.exe` SHA-256 was freshly checked as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The exact 114-path inventory with formal per-file SHA-256, all literal DAT declaration owners and scoped dispositions is `remaining-114-sprite-consumers.csv` (SHA-256 `126A6496B042FCA368F72E862FA8FC85BD93C5C55356A9339C463A32371E615B`). Every one of the 114 paths has at least one formal DAT declaration; this is **not** evidence that a selected battle frame draws it. No DAT, PNG, code, Scene, importer, old resource or nonbattle module was changed.

| Disposition | Paths | Authority/selected-path result |
| --- | ---: | --- |
| P-17 native character HUD/radar excluded | 18 | Two `INKHUD2` pictures belong to `frame.dat` index 1; paired playable `GameSession28` selects index 0. Sixteen radar pictures are declared by `INKHUD`/`INKHUD2`; selected `INKHUD.dat` has `bound:0`. User excludes the complete native character HUD regardless. |
| P-14 existing overhead-bar exception | 4 | `data/minibar/st1.dat`, `st2.dat`, `stage.dat` declare them; Unity's overhead bar is retained. |
| P-19 native scoreboard/status result presentation excluded | 8 | `SCORE_BOARD1..4`, `WIN_*`, `LOSE_*`; paired playable still resolves 1/2/3 and four statuses for ordinary result snapshots, but the user retains Unity's result presentation. Logical outcome and transition remain Q08 duties. |
| Protected Unity frontend/loading/selection or result presentation | 76 | Eleven `Loading/menu/small` paths plus 64 other `UI/*` paths belong to the preserved native-menu/loading or native-result surfaces. `MENU_CLIP3.png` is shared: `resource.dat` index 3 is also used for a story result, still within the protected result presentation. These declarations do not authorize a native UI swap. |
| Declaration-only paths, selected consumer not proved | 8 | Seven `UI/extra/*` paths occur only in `resource.dat`'s `<frame>` section. Paired `NativeResourceCatalog28` parses the `<bmp_begin>` table, and the searched playable path did not show a reader for these seven. `UI/PAUSE.png` is table index 22, but paired `D3D11Renderer28` draws a solid bar when paused rather than this texture. Root EXE visible pause behavior and any other active reader remain unverified; retain as named Q07/Q09 follow-up, do not silently mark them excluded or deploy them by count. |

The rows conserve the inventory: **18 + 4 + 8 + 76 + 8 = 114**. The 76 protected paths are further split in the CSV into 11 nonbattle frontend, 64 native menu-or-result UI, and one shared menu/story-result resource. Thus 106 have a documented user-protected or accepted-exception surface; eight are declaration-only under the inspected playable path, with current selection/use unresolved. The earlier Q07 statement “114 need consumer classification” is now narrowed to those eight and to independent runtime proof, not converted into “Q07 assets complete.”

The 64 general `UI/*` paths are classified by their native menu/result presentation role and the user's retained Unity frontend/result boundary, not by an individual GUI screenshot for each path. This is a scope disposition, not proof that each is selected or unselected in every root-EXE state. If a later concrete battle-only consumer crosses that boundary, reopen its exact row rather than applying the group disposition mechanically.

Paired release live-path checks: `GameSession28::load` uses `data/resource.dat` to resolve resource indices 16–21 (`WORDS0..5`) and 43 (`SPARK`) for active battle overlays; all seven are staged with byte-identical SHA-256 to formal VFS. `GameSession28` selects frame-HUD index 0 from `data/frame.dat`; `render_snapshot.cpp` gates radar on `bound:1` and handles ordinary scoreboard under its result condition. `GameSession28::selection_render_snapshot` consumes native cursor/character-selection paths; this is a protected frontend. `NativeResourceCatalog28::parse_text` only imports the `<bmp_begin>` entries, not the trailing `<frame>` list. `D3D11Renderer28::render`'s paused branch issues `draw_solid` and does not refer to index 22. Source anchors: paired playable `game_session.cpp` around 1217–1250, 1282–1303, 3348–3372 and 3507–3527; paired core `native_resource_catalog.cpp` around 42–82, `render_snapshot.cpp` around 1192–1300 and 1311–1321; paired renderer `d3d11_renderer.cpp` around 2292–2297. These are current checked source paths, not a claim that static source alone proves every root-EXE pixel.

Build participation was checked in the paired `source/ntsd28_playable/scripts/build.ps1 -Target playable` source list: it includes `ntsd28_core` rendering sources, `game_session.cpp` and `d3d11_renderer.cpp`; `source/README_SOURCE.md` identifies this as the playable build path. The diagnostic `<frame>` non-reader claim is limited to these inspected production paths.

Next: do not copy the 114 by inventory count. For `UI/PAUSE.png`, compare the formal root EXE's visible F1-pause frame with the paired source/current Unity pause presentation without computer-use; only an observed non-exception gap can authorize a Q09 content and render subtask. The seven `UI/extra/*` paths need a concrete production consumer or can remain declared-only frontend candidates. Q07's separate open exits—old dynamic readers/521 old asset ownership and natural formal skill/content coverage—remain. R17 gets only this selected-content disposition; runtime pixels, old-resource retirement and Q07/BATCH-04 closure are not established.

Verification: an independent CSV reread confirmed 114 rows, 114 unique relative paths, exact equality with the current formal-minus-staged `sprite/*` set, 114/114 per-file SHA matches and 114/114 nonempty DAT declaration references. The CSV SHA remains the value above. `git diff --check` exited 0; the three live alignment/handoff/STATE files each had zero NUL bytes. `Tools/Validate-ChangeLedger.ps1` exited 0 with 844 Records and two pre-existing governed code files covered; this audit itself changed no script. The validator output is `Temp/NTSD28-Q07-REMAINING-SPRITE-CONSUMER-AUDIT-001-validator.log`. No Unity runtime or full test suite was run for this read-only content classification.
