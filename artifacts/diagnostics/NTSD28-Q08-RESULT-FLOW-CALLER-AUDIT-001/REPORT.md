# Q08 result flow caller and carrier audit (2026-09-22)

## Focused Unity RED added 2026-09-22

A second isolated test drove the actual Unity `NTSDBattleTickSystem.RunReleaseTick`: tick 1 established `HadBoth=true` for groups 1/2; before tick 2, group 2 was set to HP0/`HP2Orig=2`. Tick 2 ended with `BattleEndPhase=1` against the native source classification expectation 0. It compiled and ran one test with the intended assertion failure (exit 2), with no earlier fixture assertion or exception: [REVIVE2-UNITY-FULLTICK-RED.xml](REVIVE2-UNITY-FULLTICK-RED.xml), [updated isolated fixture](REVIVE2-FULLTICK-ISOLATED-FIXTURE.cs.txt). This confirms a Unity full-tick manifestation, but a same-initial-state native full-driver trace has not yet been captured; it is not a two-runtime tick parity certificate.

The isolated Unity 2022.3.62f3 project copy used in Q07 (separate Library; the original NTSD Editor was not touched) compiled and ran one focused Q08 EditMode case. Its initial fixture establishes two living character groups 1 and 2, then sets group 2 HP to 0 and `HP2Orig` to 2. The second `UpdateBattleResultsFlow()` yielded `BattleEndPhase=1`; the source-matched expected value was 0. The runner returned exit 2 with one executed test, zero passes and one assertion failure, **not** a compilation failure. Exact output: [REVIVE2-UNITY-RED.xml](REVIVE2-UNITY-RED.xml); fixture source: [REVIVE2-ISOLATED-FIXTURE.cs.txt](REVIVE2-ISOLATED-FIXTURE.cs.txt). The native `battle_flow_tests` source case `test_remaining_native_life_suppresses_result_timer` uses groups 1/2, HP0 and `revive_lives_30c=2`, expects timer0/two groups, and the freshly built native suite passed. This is a focused direct-writer first difference for the revival carrier. It does not yet establish the full `GameSession28::step` versus Unity `RunReleaseTick` first-difference tick, or resolve the two-bucket, mode-4 reserve, result-page input and 80/101/350/144 milestones. No production code or original-project test script was changed for this RED.

Status of this audit: `VERIFIED_STATIC_CALL_CHAIN`; later child `NTSD28-Q08-REVIVE-LIVES-LIVING-GROUP-001` is `FOCUSED_TEST_PASS_ISOLATED`. This audit was initially read-only; the child subsequently changed the Unity HP/revive-lives eligibility branch under its own Task/Change Record. Neither this audit nor the child is an EXE-versus-Unity full result-flow parity certificate. See the child ACCEPTANCE for post-change evidence.

Focused source validation on 2026-09-22: `source/ntsd28_core/scripts/build.ps1 -Target battle_flow_tests` exited 0, then the freshly built `source/ntsd28_core/build/battle_flow_tests.exe` printed `battle_flow_tests: PASS` and exited 0. Built test SHA-256 is `73F6173615F474CDDC9087C6A0E2C17403D7CAAAB0E89ADB922CC367CFF80DDA`; independently rechecked formal root EXE SHA-256 is `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The built test is only a source-side executable and does not replace or prove the behavior of that formal EXE, nor any Unity implementation.

## Current authority and live path

- The declared playable build includes `ntsd28_core/src/simulation/battle_flow.cpp` (`source/ntsd28_playable/scripts/build.ps1:75`). `GameSession28::step()` calls `battle_flow_.step(*world_, config_.battle_mode, result_continue_requested)` in `source/ntsd28_playable/src/game_session.cpp:2723-2742`, after global-to-entity projection and **before** its per-entity combat-world update. Its source comment explicitly says a lethal hit begins the result timer on the following tick. Story mission with a child stage bypasses this ordinary result flow because group 5 is owned by its stage flow.
- `BattleFlow28::classify` in `source/ntsd28_core/src/simulation/battle_flow.cpp:16-39` scans all runtime slots, admits object type 0 only, excludes group `<=0`, `>=40`, and `5`, and counts a character with `revive_lives_30c > 1` as living even at HP `<=0`. It retains every distinct eligible group, sorts them, and reports a sole group as winner. `step` holds timer 0 while at least two groups live; after the first terminal classification, timer is monotonic (no automatic reset on revival). It emits end signal at 80, result record at 101, transition at 350, and accepts Attack/Jump continue only once the timer reaches 144, then jumps to 350. Transition command varies by mode. The current core tests cover these thresholds and the revive/neutral cases in `source/ntsd28_core/tests/battle_flow_tests.cpp:33-145`; tests alone do not prove every formal EXE observable path.

## Unity comparison and concrete first differences

- `Assets/NTSD/Scripts/Simulation/Core/NTSDBattleTickSystem.cs:303-327,536-538,735-746` runs `BattleResultsFlow` after the entity/post-frame tail. This placement matters: a lethal hit in the current Unity tick is visible to its end classification in that same tick, while the playable source classifies before combat and starts the timer on the following tick. This is a static caller-order difference; first-difference tick trace is still required before a production edit.
- `Assets/NTSD/Scripts/Simulation/Ecs/Results/BattleResultsOutcomeHostWriter.cs:31-125` admits HP-positive characters only, keeps at most two team IDs, requires historical `HadBoth`, attempts a mode-4 reserve, and activates summary when `BattleEndPhase >= 11`. It has no check for `HP2Orig > 1` and no `1..39 except 5` group gate. `HP2Orig` is already mapped to native `revive_lives_30c` by `Assets/NTSD/Scripts/Simulation/Diagnostics/NTSD28UnityEntityRawCapture.cs:140`, so this is an existing carrier to validate, not a reason to invent a new field.
- The existing `BattleResultsWriter` starts only after `Results.IsActive`; it owns Unity result navigation and settings. Its interface does not close the missing pre-combat classification or 80/101/350 battle-result milestones. The user-approved replacement results UI remains excluded from visual replication; the winner, timing and battle/upper-state transition effects are still in scope.
- Existing `Assets/NTSD/Scripts/Test/Editor/BattleResultsSceneHostTickAlignmentEditorTests.cs` explicitly guards result-screen host input **after** a full world tick. `BattleResultsOutcomeHostWriterSeamEditorTests.cs` also guards an active Results scene against retriggering the inactive outcome writer. These tests protect different ownership from the pre-combat `BattleFlow28::step` classification. The implementation must split/retain those responsibilities rather than move all Results host processing earlier. Tests that assert the old `HadBoth`/phase-11 behavior need correction only after a source-matched RED fixture proves the new expected state.
- `BattleRuntimeSelfCheck.CheckBattleResultsSlotAndRelationContracts` and `CheckBattleResultsPersistentBucketAndPauseContracts` currently assert group 0 counts, groups 61/62 count, a third group is ignored after two buckets, and a started guard pauses when both old buckets live. These assumptions conflict with the current `BattleFlow28` source and must not be treated as a parity pass. `RESULT-RESERVE-09` depends on the same old guard and needs its mode-4 owner reviewed before any change. The existing result-screen input/reset checks remain separate; do not wholesale rewrite SelfCheck.

## Next bounded execution

1. Create a precise Q08 Task and Change Record before any script edit. Trace `AdvanceBattleFlowTick`, `BattleResultsFlow`, `BattleResultsRuntimeState`, reserve writer, and host transition consumers together. Keep Q07 formal-content baseline and the Q08 stage-count authority gap separate.
2. Run matched native/Unity focused fixtures for at least: two live groups; HP-zero with remaining lives 2; groups 0/5/40; three eligible groups; lethal hit in the current tick; revive after timer start; and milestones 79/80/100/101/143/144/349/350 with and without Attack/Jump. Compare first differing tick, winner/group set, timer, transition command, and the reserve branch. Only broaden if a first difference or shared behavior justifies it.
3. Move battle classification to the source-matched tick boundary and adjust logic only after those traces establish exact data and pass ownership. Validate with focused EditMode, compilation, current `BattleRuntimeSelfCheck`, and representative real battle/runtime close; preserve the Results UI exception and Scene/user changes. No whole-character matrix is required for a shared rule change unless evidence identifies character-specific branches.

This audit advances G-05/G-06 planning and does not close either item, Q08, or the master goal. The separate mode-4 result-settings stage-count item remains `RESULT_SETTINGS_AUTHORITY_SURFACE_ABSENT`; the 24 pre-battle background IDs are not its value.
