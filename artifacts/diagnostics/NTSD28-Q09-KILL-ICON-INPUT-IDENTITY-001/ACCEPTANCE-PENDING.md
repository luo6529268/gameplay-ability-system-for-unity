# Q09 kill-icon input identity: current evidence (2026-09-22)

Status: `CODE_WRITTEN / OFFLINE_COMPILE_PASS / ORIGINAL_EDITOR_RUNTIME_PENDING`. This package establishes image input identity, not publication, row drawing, or formal-pixel equivalence.

The original project `LoganVisualContentCandidate` now captures the seven mode-selected pic_type0..6 paths, including duplicate references. Existing images contribute SHA-256; missing or empty entries contribute an explicit absence bit. A present `#killtext` input selects a V3 visual preimage; no-feed V1/V2 branches and actor-image count are unchanged. `AssertInputsCurrent` re-captures these seven slots before publication. One new focused Editor test covers formal/staged capture and temporary missing, changed, appeared, removed, duplicate, and escape inputs.

Actual checks:

- `dotnet msbuild Assembly-CSharp-Editor.csproj -t:Build -p:Configuration=Debug -p:CustomAfterMicrosoftCommonTargets=<absolute Temp-only current-sources.targets> -v:minimal -nologo` exited 0. Evaluated `Compile` includes the new Editor test; reflection finds `NTSD.Animation.LoganVisualContentCandidate+NativeKillIconInput` in the runtime DLL and `NTSD.Test.Editor.NTSD28Q09KillIconInputIdentityEditorTests` in the Editor DLL. This is an offline current-source compilation proxy, not original Editor compilation.
- Direct invocation of the compiled `NativeKillIconInput.Capture` on the formal and project-staged mode child selected all seven slots in order: `c, sk2, sk2, sk1, sk2, sk2, sk2`. Both roots produced fingerprint `8D6FF192732FB9884B6909CEA0486C6593566F27E77CE6CA5BAA11F91A8D7DC8`; all seven paths resolved to their root-local PNGs.
- Direct full `LoganVisualContentCandidate.Capture` outside Unity stopped at missing `UnityEngine.CoreModule`; it is **not** evidence for full V3 candidate equality or publication. The new Editor NUnit cases have been compiled but not run. The original Editor's script assemblies at 07:58:15Z/08:11:44Z predate this package's 09:38:03Z source and test, so original Editor compilation remains unverified.

Pending exit: run the two focused Editor cases only after the original Editor reloads current source; verify full candidate V3 equality and freshness, then independently connect icon publication/row drawing and compare targeted battle scene against formal EXE. Q09/R17 remain open. No second Unity Editor or computer-use was used.

Current-state correction (2026-09-22): the original Editor process PID 33236 remained live and imported `NTSD28Q09KillIconInputIdentityEditorTests.cs` (Editor.log line 12852842). Its log later reported `Tundra build success (3.54 seconds)` with the runtime and Editor DLL copies, and `Library/ScriptAssemblies/Assembly-CSharp.dll`/`Assembly-CSharp-Editor.dll` are 09:44:23Z/09:44:24Z, after the package's 09:38:03Z source/test. Original-project Unity compilation is therefore a current `COMPILE_PASS`. The log search found no later `Mono: successfully reloaded assembly` for this compilation, and the prior WORDS focused TestRunner has a `RunStarted` but no new result file or confirmed `RunFinished`; domain load, this package's two NUnit tests, full V3 candidate and visual behavior remain pending. The earlier "original Editor assemblies predate" sentence is the pre-refresh snapshot.
