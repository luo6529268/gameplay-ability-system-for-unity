# Q07 Menu-first Windows Player cold-start acceptance

Run `menu-first-v2-20260923-01`, original Unity project, 2026-09-23. The user confirmed Menu as Build index 0 and Battle as index 1. This is a scoped Development Player callback diagnostic, not a claim of physical input, rendered-pixel or formal EXE parity.

The original Editor compiled the two diagnostic scripts successfully (`Tundra build success`, idle after domain reload). The Windows Mono Development Player [build report](menu-first-v2-20260923-01-build.json) is `PASS`, `Succeeded`, zero build errors and 191,574,181 build-report bytes; its ordered scene list is exactly `NTSD_Menu.unity;NTSD_Battle.unity`. The v2 postprocessor accepted the 1371-row manifest. Independent postbuild verification found exactly 1371 non-meta sidecar files totaling 46,883,057 bytes, each matching the manifest SHA-256 and size, with no missing or extra file. This also closes the separate v2 manifest gate's Player-side acceptance.

The first launch used `-batchmode -nographics`. It reached formal prewarm but [failed](no-graphics-first-run.txt) because `GameConfig.ShadowPrefab BattleCommonShadowDescriptor Material` did not pass the premultiplied alpha semantic check. Its own diagnostic process was stopped before the probe's long timeout; this run is not counted as a Player PASS. The same built Player was then launched with graphics enabled in a hidden window, with no computer-use. The first graphics [report](menu-first-v2-20260923-01-first-player.json) was `PASS`; the second graphics [report](menu-first-v2-20260923-01-player.json) was also `PASS` with recorded [exit code 0](menu-first-v2-20260923-01-exitcode.txt).

Both graphics runs started from `NTSD_Menu`, prewarmed formal content, invoked the real Menu component callbacks to select Naruto OID 2, loaded `NTSD_Battle` additively, reached a running World with two actors, and confirmed semantic fingerprint `B8B13894088DDE96D71771C9110FBD84E2C03AE712FE32222B99C5DFE8155A45` and three matching publication keys. Ordered unload returned to Menu, driver stayed Stopped with no World, and active pool borrowers were zero. The report is a callback chain test; it does not send physical pointer/keyboard input.

The Menu Scene SHA-256 remained `6CF124A17F692325CED5774C3C10C3324AA047439BC6C3EB08856188A78AB4B1`; Battle Scene remained `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`. `ProjectSettings/EditorBuildSettings.asset` remained `8D621A077642B5154BA305861CA57DFB549FF4FDE5BC58F3E284536A5982F01E`. Scoped `git diff --check` passed. `Tools/Validate-ChangeLedger.ps1` passed; unrelated historical record-path warnings remain in its output. No new Unity project, Scene serialization, production Menu code, battle rule or formal J: content was modified by this package.

This closes only the Q07 Menu-first Windows Player callback/content bootstrap subgate. Q07 remains open for physical input and visible gameplay/pixel acceptance, broader formal EXE behavior and the remaining resource/scene dependencies; Android Player/device was not tested.
