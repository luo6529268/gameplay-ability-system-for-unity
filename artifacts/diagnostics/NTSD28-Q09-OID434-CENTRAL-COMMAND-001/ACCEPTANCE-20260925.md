# Q09 OID434/action396 central command publication (2026-09-25)

Status: `VERIFIED_SCOPED_COMMAND_PUBLICATION / CAMERA_DRAW_PENDING / GPU_PIXEL_PENDING / Q09_OPEN`.

The original Unity Editor imported the opt-in Q09 extension. The generated Editor C# build after import succeeded with zero errors (22 warnings in its incremental result); `read_console` returned zero errors. One create-new request `oid434-central-command-20260925-1` ran in the saved original Battle Scene; JSON SHA-256 `E7BFE0866EEA276FC2068BD072A8C076CFDA6096D321FBC9C229DA8EBA419A55`, status PASS. The diagnostic's `scope` string was inherited from its Q07 entity-binding mode and is narrower than the explicit Q09 plan fields below; do not use that string to erase or enlarge the recorded fields.

The existing natural after254 physical-input probe passed with 45 continuous tick samples; follow-up physical J entered frame input at tick53. Actor ordinary attack action20 and OID434/action100 were observed, and separate OID434 stable ID121/slot50/action396 at tick54 resolved formal-source pic36, key `(434,36)`, 48×48 and valid central binding. With the logic tick held at 54, the production current pixel plan was valid, owner `Central`, requested mode `CentralOnly`, simulation/display tick54, generation52, with a live submission at tick54. Its frozen frame tick54 had commands materialized, seven commands total and **exactly one** `BattleRenderCommandType.Entity` for that stable ID/slot/visual ID434/effective pic36, size48×48 and sort order5. The reader checked the published submission's frozen command list, not a newly built test-only coordinator.

The Editor exited Play to idle Edit mode and had zero Console errors. Battle/Menu/GameConfig SHA-256 remained `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`, `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`, `0527D737A1FA38FC56B51D00DC6E96A421D3C67222546368B147C2D074CB8EA7`. No production renderer, input, DAT, PNG, Scene, Prefab or nonbattle file was edited by this subtask.

This proves one natural-input-derived OID434 visible frame is **present in the production frozen central command submission**. It does not prove a camera acquired or executed that submission, that pixels were drawn, that the formal EXE renders the same pixels in the same initial World, or that Q09/R17/overall alignment is complete. The next Q09 evidence should sample the camera submission/draw and a bounded pixel region in a separate opt-in run.
