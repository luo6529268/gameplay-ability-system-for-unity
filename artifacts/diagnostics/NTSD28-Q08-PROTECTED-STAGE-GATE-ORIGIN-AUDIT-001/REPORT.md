# Q08 mode1 protected OID122/123 stage-gate origin

Status: `VERIFIED_STATIC_SCOPED / GUI_OBSERVATION_PENDING`. The root formal EXE SHA-256 was checked again as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. This is a read-only source/caller census, not an executable GUI A/B test.

In the paired playable Core, `SimulationTickDriver28::step` passes `options.selected_stage_participant_gate_178914` into `BattleWorld28::settle_ordinary_stage_bounds`. For OID122/123, the special 100-pixel clamp is selected when `participant_class_344 > 0`, or when `battle_mode == 1` **and** the selected-stage gate equals exactly 1. The ordinary positive-participant arm is already implemented and has a controlled original-Unity-Battle-Scene full-Driver witness in `NTSD28-USER-D024-PROTECTED-X-SCENE-WITNESS-001`; the second arm is the subject here.

`BattleConfig28` declares the stage gate with default 0 (`game_session.h:357`). `GameSession28::tick_options` copies it into the Core option (`game_session.cpp:4136-4137`). `GameSession28::initialize` copies the caller config, while `main.cpp` creates an `Options28` with a default `BattleConfig28` and passes `options.battle` to the session (`main.cpp:92-93,2251`). The post-roster start path clones `config_`, updates its background/mode selection and calls `apply_native_background_mode_record`; `project_native_background_mode_record28` projects named record fields but **does not assign** `selected_stage_participant_gate_178914` (`game_session.cpp:42-76,2934 onward,3953-3982`). A source-wide exact-assignment search found an explicit setter in `scenario28.cpp:814-815` from `selectedStageParticipantGate178914`, plus tests; it found no production menu/background-record setter. The scenario gate is thus directly injectable for controlled playback, while the inspected default normal playable path retains 0.

Unity's current `LF2Entity.ApplyPreFrameXBounds` applies the 100-pixel arm for positive `Unk344` on OID122/123. The project-owned `ProjectBattleModeConfig` contains combo/KO settings, not a selected-stage participant field. These observations alone do **not** justify adding such a field: the user explicitly excludes original background and mode DAT, and the current formal default menu source has not shown a nonzero writer. The second arm remains a conditional authority rule for scenario/test configurations, not a proven missing behavior on the user's selected production route.

Next decision gate: if a real formal EXE menu/GUI trace or a project-owned stage/mode requirement demonstrates `battle_mode=1`, OID122/123 `participant_class_344<=0` and gate1 in the same tick, open a separate Q08 Task/Change to define the project-side carrier and verify both gate0/gate1 full Driver paths. Until then, keep Q08's conditional coverage item open but do not change DAT, ProjectBattleModeConfig, Scene, nonbattle menu or the existing positive-participant clamp. This static audit does not cover executable observation, selected-stage menus outside the inspected caller graph, or natural OID122/123 birth/consumption. D-024/Q07/Q08 remain open.
