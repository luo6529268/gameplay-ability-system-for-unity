# Q07 non-character `hit_Fa=7` first-difference follow-up

Status: `FOCUSED_TEST_PASS / RUNTIME_PENDING`. Q07, R15 and the full battle-alignment goal remain open.

The prior formal-source-model/Unity V3 kind scenario differed at tick 1: Unity alone spawned slot 50/OID206. The paired playable source routes positive non-character `hit_Fa` through `SimulationTickDriver28` to `NativeAi28::step_non_character_hit_fa`; behavior 7 consumes the preassigned target, returns when it is absent, and has no unconditional clone birth. Its target-present path applies two double 0.7 X contributions, double 0.4 Z/Y contributions, advances precise Y once, tests the existing integer Y for action 60, clamps X/Z and writes facing. This is source-model evidence, not a formal EXE screen capture.

The original-project Editor imported the new test script and its generated meta. Before production editing, exact focused job `cade51ba91a0445abcfe3a2cc5e443b8` ran 2 tests and both failed: missing target created an extra slot; real OID875/action55 with preassigned target yielded 3 entities instead of 2. The separate DAT non-character branch was then implemented in `LF2Entity.cs`; the character path and the file's unrelated pre-existing knockout change were preserved. Independent review caught an early integer-Y synchronization and float-literal precision issue; both were corrected before final validation. Final original-Editor compile/reload succeeded, and focused job `870e12d4f9784c70baafd151636a9565` passed 2/2, including exact double velocity/precise-Y, unchanged integer Y, clamps, action60 and no clone. Two relevant Q06 non-character/AI-alias regressions passed 2/2 after the final correction in job `53986dbd474043debbe237ac1ac98937`. An intervening regression filter used the wrong namespace and ran 0 tests; it is not counted as evidence.

The frozen kind/type3 scenario was recaptured without changing its initial state or exporter. The paired [formal](formal-kind.raw.jsonl) and [Unity](unity-kind.raw.jsonl) main streams compare `equal-raw`: 3 ticks, 3 entity pairs, 150 field occurrences and 0 differences in [raw-compare.json](raw-compare.json). [B2](b2-compare.json) reports equal input/RNG joint raw; [B0](domain-compare.json) reports equal shared input/slots/lifecycle and the separately known RNG stream-topology difference. The Unity B0v2 and B2 streams pass their strict validators. `unity-raw-validation.json` is an intentionally inapplicable probe: `validate-authority-capture` requires a source-producer header and therefore rejects a Unity producer at the header; raw equality is evidenced by the comparator, not that file.

The target-present assertion now continues from the local behavior into `CharacterMechanics.StepNonCharacterBattleLogic` and checks that `NativePreviousY104` remains the old integer `-30`; original Editor job `218c8c1ed3264db3bb812b75dc0cc956` passed 2/2 after this focused extension. This deliberately omits the intervening frame-motion pass. Formal OID875/action55 has `dvy:1`, so this local seam's precise Y `-21.6` is **not** a full-tick authority prediction. A source-matched target-present full tick must include frame motion and compare `previous_y` before closing that branch. The general scenario runner currently lacks preassigned target/initial velocity inputs, so an independent diagnostic-only witness is required; do not alter its frozen scenario to manufacture a pass. Formal EXE visible behavior, longer combat, Player and all-character presentation are not established by this package. The saved Menu and Battle Scene SHA-256 values remain `6CF124A17F692325CED5774C3C10C3324AA047439BC6C3EB08856188A78AB4B1` and `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`. Change Ledger validation passed with existing declared-path warnings; `git diff --check` passed.
