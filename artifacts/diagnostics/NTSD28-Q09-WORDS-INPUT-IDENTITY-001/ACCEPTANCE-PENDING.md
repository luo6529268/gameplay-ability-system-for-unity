# NTSD28-Q09-WORDS-INPUT-IDENTITY-001 evidence

Status: `CODE_WRITTEN / OFFLINE_COMPILE_PASS / ORIGINAL_UNITY_PENDING` (2026-09-22). Only the declared original-project visual-candidate script and one new focused Editor test/meta changed. No battle simulation, menu, Scene, Prefab, old image, renderer, package or Unity configuration changed.

The existing generated original-project `.csproj` files still omit earlier Q07 new files and this new Editor test. A `Temp/diagnostics/NTSD28-Q09-WORDS-INPUT-IDENTITY-001/current-sources.targets` file adds exactly the two earlier missing Q07 Compile items and this test item to offline MSBuild. `dotnet msbuild Assembly-CSharp-Editor.csproj -t:Build` with that targets file and a Temp-only output path exited 0 twice, the final run after the test assertion correction. The evaluated Editor Compile list contains the new test and old Q07 test. The resulting runtime/Editor DLLs are under this diagnostic Temp/Build, not the live Editor's `Library/ScriptAssemblies`.

Reflection invoked the **compiled actual** `LoganVisualContentCandidate.NativeWordsInput.Capture` method. Formal and staged runtime roots each selected six PNGs and produced identical input fingerprint `0E963A3DE1F56B71F9DB276EFB79D7010B1CD3B08B84E83D0DD9009B894C5B24`. A generated Temp fixture (retained under `Temp/diagnostics`, no existing user file overwritten) returned null with absent `resource.dat`; independent new fixtures changed WORDS3 bytes or unrelated raw resource DAT bytes and each produced a different fingerprint; a selected `../../outside.png` path was rejected. All four predicates returned true. This is focused input-projection evidence, not a Unity test or display result.

The new NUnit fixture also checks formal/staged complete visual candidate equality, keeps the 906 actor image count separate from six global WORDS inputs, and tests the same parser/freshness boundaries. It has **compiled offline but not run** in the original Editor. Current live `Library/ScriptAssemblies` still predates the Q07/Q09 source changes; Unity import, NUnit, atomic common-catalog publication, nameplate/HUD pixels, lifecycle and formal EXE parity are pending. Q09/R17 remain open.
Governance validation after Record metadata correction: `Tools/Validate-ChangeLedger.ps1` exit 0 (`PASSED`, 685 Records, 22 governed code files in the current dirty diff). `git diff --check` exit 0. An initial validator run before adding the required `CHANGE-RECORD` metadata failed on this new Record and test; the exact metadata was then added and validation rerun successfully. No project script outside this Record was changed to satisfy the validator.
An attempted **offline full-candidate** reflection call did not execute: the external PowerShell runtime first lacked `UnityEngine.CoreModule`; after explicitly loading that original Editor assembly, it next lacked `UniTask`. This is an external assembly-resolution limitation, not a passing or failing `LoganVisualContentCandidate.Capture` assertion. The direct compiled `NativeWordsInput.Capture` checks above did execute and pass. Full candidate equality remains for the new NUnit test in the original Editor after a fresh compile; do not report it as already observed.
