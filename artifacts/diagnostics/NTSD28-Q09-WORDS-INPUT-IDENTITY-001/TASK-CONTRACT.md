# NTSD28-Q09-WORDS-INPUT-IDENTITY-001

Status: `PLANNED` before script modification (2026-09-22). Parent: BATCH-05/Q09 and R17. This is the input/freshness portion of formal battle WORDS publication, not the draw implementation or a claim of runtime parity.

Authority: formal `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; playable `GameSession28::initialize_resources` resolves `resource.dat` native indices 16..21 and renderer consumes them for battle glyphs. Exact formal/staged `resource.dat` and six PNG files are present; source-to-Unity proof is in `NTSD28-Q09-NATIVE-WORDS-PUBLICATION-CALLER-AUDIT-001/REPORT.md`.

Exact code ownership: `Assets/NTSD/Scripts/Animation/LoganVisualContentCandidate.cs` and a new focused `Assets/NTSD/Scripts/Test/Editor/NTSD28Q09WordsInputIdentityEditorTests.cs` with `.meta`. Governance: matching Change Record, Ledger, STATE, handoff and alignment. No other script, Scene, Prefab, ProjectSettings, menu, audio, battle-rule, old BMP, third-party or generated file is in scope.

Implement an optional immutable global WORDS input capture for Logan roots that have `decoded_dat/data/resource.dat`. Select the first native `<bmp_begin>` table and its zero-based entries 16..21, resolve six VFS paths within the configured root, and hash the raw resource DAT plus those six PNG bytes. Preserve the exact existing visual V1 fingerprint/cache key for roots with no `resource.dat`; use a versioned V2 visual preimage only when the global input is present. On freshness checks, detect appearance, removal, DAT byte change and any selected PNG byte change. Never add global resources to battle-rule `LoganContentIdentity` or alter actor DAT/image counts. The later publication package will consume these selected paths, not hard-coded filenames.

Validation: focused parser/identity tests with a six-file temporary fixture for selection, path traversal rejection, DAT mutation, image mutation, removal/appearance and V1 absence; formal/staged capture must produce matching selected virtual paths and input fingerprint. Run external current-source compile as a proxy if original Editor still has stale assemblies; run original-project Unity focused tests only after a fresh Editor compile, then later actual pixels in the publication package. A failed capture must not commit any content. Rollback boundary is only this Record's exact code/test diff after review; no blanket reset/clean or deletion without authorization. No new Unity Editor, Pipeline installation or computer-use.
