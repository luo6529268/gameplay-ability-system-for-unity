# Q07 DDJ controlled trace entry audit

Status: `READ_ONLY_IMPLEMENTATION_BOUNDARY / Q07_OPEN`. This audit follows the original-Editor `ddj-state-20260925-5` result. It changes no script, DAT, image, Scene, Prefab or project setting.

The natural Battle Scene physical-key route and the existing formal release playback do not have equal tick-0 worlds: Unity starts OID2/OID2, action0, a project map and different positions/RNG; the formal LFR fixture starts OID2/OID7, action110, background23 and its own seed/positions. The natural probe observes from `EditorApplication.update`; its actual `-5` result omitted ticks 1 and 3 when local free-run advanced more than once between callbacks. Repeating the same physical Play with only a new result name cannot close the same-state or continuous-input gate.

There is a reusable one-tick *mechanism* in current Unity source. `SimulationTickDriver.ApplyMatchConfig` writes match seed/mode/background and resets the runtime before first tick (`Assets/NTSD/Scripts/Simulation/Host/SimulationTickDriver.cs`); `SetFrameInputProvider` accepts a discrete provider and `StepOneTick` runs exactly one explicit frame. `BattleParityTraceEditor.RunScenario` demonstrates a Manual provider, one `StepOneTick` per requested tick and `BattleParityFrameSnapshot` publication. This is a diagnostic design reference, not current formal-content evidence: its `RunScenario` unconditionally derives `gameRoot/data/data.txt`, `ExternalDatScope` reads that index and decrypts DAT through the old loader, so invoking it unchanged would reintroduce the pre-migration content path. `BattleTestBootstrap` likewise cannot furnish the formal LFR initial state from the saved Scene: it uses serialized `overrideCharacterIds` (currently OID2/OID2) and automatically resumes the local host. Neither owner should be changed merely to make a test pass.

The current content entry is already available separately. `CharacterAnimtorManager.PrewarmConfiguredLoganContentAsync` captures `ProjectBattleModeConfig.LoadDefault().Capture()` and publishes a `LoganVisualContentCandidate` from the configured Logan root; staged publication tests assert catalog/data/UI identity. A future bounded Q07 diagnostic can use this formal staged candidate and an isolated Manual battle world to set explicit OID2/OID7 participants, action110, seed and positions, then feed the same per-tick input sequence and capture every tick. It must state which source fields are genuinely equal before comparing: the user's project map and project mode Asset are approved replacements for original background and mode DAT, and D-024 deliberately scales visual position separately from source-rule position. A whole-world checksum comparison without projecting those approved differences would be misleading. Native RNG, input remap/phase and renderer/borrower teardown require explicit checks in that package; this audit does not assert they are already equal.

The next implementation needs its own pre-edit Task/Change and exact test-owned path. A useful first exit is one formal-content Manual DDJ trace with tick0 contract plus ticks1–26 and no missing rows, followed by a field-by-field comparison against the preserved formal `formal_release_playback_action110_trace.jsonl`. The natural physical Play remains a separate usability witness. No old Config DAT, formal background DAT or formal mode DAT may be loaded for the Unity side; no saved Scene, production input/Driver or nonbattle code should be changed for this diagnostic.
