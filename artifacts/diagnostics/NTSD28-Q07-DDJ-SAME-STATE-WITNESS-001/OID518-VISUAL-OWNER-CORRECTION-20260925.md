# Q07 Naruto DDJ OID518 visual-owner correction (2026-09-25)

Status: `VERIFIED_STATIC_HIDDEN_FRAME_CLASSIFICATION / DYNAMIC_PUBLICATION_PENDING`.

The formal root `NTSD2.8-Logan.exe` was rehashed as `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The formal and staged `c/nar/a/atk.dat` SHA-256 values both equal `881060DBA197C8FD39B1F089BE0FE89D30CA3D0AABDA37402B5DFAD90918CF99`. Formal `data/data.txt` binds OID518/type3 to that DAT. The **formal release tick16 entity trace** records the surviving OID518 at slot50/action390 with `pic:999`; its spawn event cites Naruto DAT line1695, the frame273 OPoint with `action:390`. Frame390 in `atk.dat` has `pic:999` and an ITR. A different frame495 OPoint authors OID518/action708 and appears as a spawn event at tick6; frame708 also has `pic:999` and an `at5.wav` sound. Frame497 authors OID518/action719, which likewise has `pic:999`. These are distinct birth routes; the tick16 entity must not be labeled action708 or a sound-trigger witness.

The `atk.dat` declared sprite sheets cover pic indices 0–133. The paired playable `SpriteFrameResolver28::is_native_hidden_pic` in `ntsd28_core/src/rendering/render_snapshot.cpp:1150-1159` returns hidden when no sheet range covers the selected pic, and the file participates in `ntsd28_playable/scripts/build.ps1 -Target playable`. On the recorded frame values, OID518/action390 at tick16 is a hidden frame under this formal source path. Its observed birth proves a logic/object milestone, not a visible attack-sprite publication. The existing trace does not expose the runtime visual-pic offset, so this classification is bounded by that unobserved offset; do not claim a pixel absence certificate from it alone.

For a visible Naruto attack image candidate, formal `data/data.txt` binds OID434/type3 to `c/nar/a/ras.dat`. That DAT declares `c/nar/a/ras.png` for pic0–39; frame396 has `pic:36` and frame397 switches to `pic:999`. The formal and staged `ras.png` are present at the same VFS path and both SHA-256 values are `21A69B52A561CA4773FFC8FDBFE0E5B23E43B6B0F966EAF76E13888C8E23FD1A`. In the already-preserved 32-tick missed-window replay, the formal release trace has OID434/slot52/action396/`pic:36` at tick29 and action397/`pic:999` at tick30. The paired Unity raw trace also has OID434/slot52/action396 at completed tick29, then action397 at tick30. This pins the candidate to an observed same-scenario tick rather than an unreachable DAT frame. Those raw rows still do not expose Unity's selected `BattleSpriteEntry`, Renderer command, or GPU pixel, and this controlled replay does not substitute for natural physical-key Play.

The existing two DDJ controlled Play results and scoped source/release comparisons remain unchanged. Do not add a visible OID518 assertion to the DDJ probe merely to extend coverage; it would target recorded hidden-frame data. This audit changed no DAT, PNG, script, Scene, asset or request file. Q07, Q09, R17 and natural-skill presentation remain open.
