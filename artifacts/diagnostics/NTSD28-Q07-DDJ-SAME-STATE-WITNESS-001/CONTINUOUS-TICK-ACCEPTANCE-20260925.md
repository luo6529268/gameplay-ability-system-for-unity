# Q07 Naruto DDJ continuous-tick witness (2026-09-25)

Status: `FOCUSED_PLAY_PASS / SAME_STATE_NOT_ESTABLISHED / BORROWERS_UNVERIFIED / Q07_OPEN`.

Visual-owner correction: the formal tick16 surviving OID518 is action390/`pic:999` under `atk.dat`, not action708. Its birth is not a visible sprite-binding certificate. See `OID518-VISUAL-OWNER-CORRECTION-20260925.md`. A visible OID434/`ras.png` publication requires its own dynamic witness.

The original Unity Editor consumed the unique `ddj-contiguous-20260925-1` request in the saved `NTSD_Battle` Scene. The opt-in DDJ diagnostic paused LocalFreeRun and advanced one production `StepOneTick(ignorePaused: true)` through its existing input provider per observation. Its preserved JSON (`ddj-contiguous-20260925-1.json`, SHA-256 `6B6F2706E0D2CBA21E7708B4DC30B5ADA4545F571C1873B12EC2ED023476C9FA`) reports `PASS`, `controlledTickStepping=true`, `continuousTickCapture=true`, `missedObservedTicks=0` and precisely ticks 1–26. Physical L/S/K input requested frame 271, resolved action 272, reached full-tick frame 495, spent PP 500→150, and first spawned OID518 at tick 16.

The separate `ddj-contiguous-20260925-1-comparison.json` compares those 26 ticks with the preserved formal release action110 trace. PP/MP, input phase and OID518 count match on 26/26 rows; action matches on 25/26, with Unity action0 versus formal action110 at tick1. The initial World is still different: Unity has OID2/OID2 and four objects, while the formal trace has OID2/OID7 and two; actor position, initial action, RNG and project background also differ. Unity proxy bitsets have not been normalized to formal input-mask schema. These field counts therefore establish a scoped observation, not same-state simulation parity or complete battle alignment.

The Editor returned to Edit mode; Console error filter returned zero. Battle Scene, Menu Scene and GameConfig asset disk SHA-256 stayed respectively `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`, `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`, and `0527D737A1FA38FC56B51D00DC6E96A421D3C67222546368B147C2D074CB8EA7`. A read-only MCP `execute_code` attempt to inspect pool borrower counts failed before evaluation because the Mono command line exceeded the Windows limit. Borrower count is unverified; Edit-mode return does not establish zero borrowers.

Independent static review then found a diagnostic false-PASS edge: if DDJ resolves early, the prior `step3Tick + 20` finish gate can stop before the required 26th tick, while `continuousTickCapture` was informational rather than part of `passed`. The owned Editor-only DDJ branch now waits for both `step3Tick + 20` and `startTick + 26`, and its PASS predicate requires a consecutive 26-tick trace. Other probe gates are unchanged. The corrected script's generated Editor project build exited 0 with 163 warnings and 0 errors (`Temp/NTSD28-Q07-DDJ-continuous-gate-build-20260925.log`). The preserved first Play predates this guard correction; the second Play below verifies the corrected diagnostic's normal DDJ route. Same-state parity, borrower observation and aggregate Q07 remain pending.

After the gate correction, the already-running original Unity Editor accepted one asset refresh, completed compilation/domain reload and returned to idle Edit mode in the clean `NTSD_Battle` Scene (14 roots); its Console error query returned zero entries. `Tools/Validate-ChangeLedger.ps1` exited 0 (`Change ledger validation PASSED`, 821 Records and 20 governed code files in the current shared diff) and focused `git diff --check` exited 0.

The previous consumed request bytes were preserved as `ddj-contiguous-20260925-1-request-consumed.json` (SHA-256 `59B1AF3FE1051D4B37E577E19C192FAD27C37EC3EDD5F583D17E0C8B7D85772F`) before reusing this task-owned request path with the new `ddj-contiguous-20260925-2` ID. The original Editor consumed it and returned a second `PASS` after the guard correction. Its JSON SHA-256 is again `6B6F2706E0D2CBA21E7708B4DC30B5ADA4545F571C1873B12EC2ED023476C9FA`, byte-identical to the first controlled witness: ticks 1–26, zero missed, frame/action/PP and first OID518 tick16 all unchanged. The Editor exited Play, active Battle Scene was clean with 14 roots, Console error query returned zero, and Battle/Menu/GameConfig disk hashes remained the values above. This closes the diagnostic early-finish correction's focused Play gate, but it does not establish same-state parity or the unobserved Renderer borrower count.

The three previously recovered live progress documents are NUL-free. Their exact v3 candidate and `*-corrupt.bin` snapshot pairs remain in `NTSD28-Q07-PROGRESS-DOC-RECOVERY-20260925`; the live documents contain later Q07 additions and must not be recopied from the older candidates.
