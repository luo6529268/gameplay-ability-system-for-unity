# Q07 non-character frame velocity single application

Status: `FOCUSED_TEST_PASS / Q07_OPEN`. The formal source-model and original-project Unity real-driver target-present comparison now agree for the initial state and completed ticks 1–2. This is a bounded source-model alignment result, not formal EXE visible behavior, full Q07 closure, or tick3 OPoint acceptance.

The pre-change exact EditMode job `43584c3d7f314ed0b464c5825526bb17` ran one named test after the comparator was corrected to compare numeric values within `1e-9`; it failed at `tick 1, field integerY`, expected `-20`, actual `-19`. The frozen raw rows independently show source/Unity Vy `5.2/6.2`, integer Y `-20/-19`, precise Y `-20.6/-19.6` at that tick. The comparator tolerance handles JSON representations such as `3.7999999999999998` versus `3.8` without relaxing the one-unit gameplay difference.

The sole production edit is in `SimulationWorld.NativePhysicsAndDeadCharacterResourceNormalizeAll`: preserve and set the existing legacy-frame-motion suppression during this native physics pass, then restore it in `finally`. The global `NativeFrameMotionAll` pass remains the owner of frame `dvx/dvy/dvz`; the native physics pass still performs its existing per-slot physics, dead-character normalization and snapshot refresh. The same real-driver test passed 1/1 in original Editor job `83c62d25b9864b97b1e135bf87b0b4f4`. The narrow adjacent job `66c0cc2d532b46b5a606e3789d2e85b4` passed 5/5: missing-target hit_Fa7, isolated preassigned-target behavior, serial C04 velocity ownership, direct legacy SimTU compatibility, and full C06 physics owner/serial remainder.

The original Editor's final job responses are preserved as [RED](red-editor-job.json), [GREEN](green-editor-job.json), and [adjacent regression](adjacent-editor-job.json) JSON. Each confirms the requested filter completed only its named test set, despite the Editor test tree containing 8,259 total cases.

The frozen R15 kind/type3 scenario was freshly recaptured without changing the scenario. [Source build manifest](source-build-manifest.json) confirms formal EXE SHA `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033` and paired source manifest `07CD47A0623F23D2C439E0E85EABF2ED10F8EAE8FC7D70DDB8396C704B3D778F`. The original Editor request returned `PASS` and produced main/B0v2/B2 streams. [Main comparator](main-compare.json): 3 ticks, 3 entity pairs, 150 field occurrences, 0 differences. [B2 comparator](b2-compare.json): exact input/RNG joint raw equal for 3 ticks. [B0 comparator](b0-compare.json): shared input/slots/lifecycle equal; source-native versus Unity RNG stream availability remains a known topology difference, not an equal-RNG claim. The source capture is `SOURCE_MODEL_DIAGNOSTIC_ONLY` and certificate-ineligible.

Menu/Battle Scene SHA-256 remain `6CF124A17F692325CED5774C3C10C3324AA047439BC6C3EB08856188A78AB4B1` and `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`. No Scene, content, UI or nonbattle script was edited. Formal EXE visible behavior, Player, and the tick3 diagnostic OPoint renderer-pool prerequisite remain open. The earlier accidentally broad EditMode job stopped being observable when the original Editor process exited after 7,706/8,259 reported completions; its unrelated failures and generated diagnostic files are not used as acceptance evidence and have not been cleaned or reverted.
