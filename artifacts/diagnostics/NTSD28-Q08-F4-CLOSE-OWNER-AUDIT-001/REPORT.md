# Q08 F4 host-close owner audit (2026-09-22)

Decision: `CONFIRMED_PRODUCTION_EFFECT_GAP / READ_ONLY`. This is separate from the unresolved mode-4 result-stage-count question. No production code or ProjectSettings were changed.

Formal authority is the root `NTSD2.8-Logan.exe` with SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; the `ntsd28_playable` `build.ps1 -Target playable` closure includes `src/main.cpp` (build script line 671) and its `native_function_keys.h` header. On a fresh non-repeat `WM_KEYDOWN`, `main.cpp` routes F4 only when the frontend scene is battle. `native_function_keys.h::route_native_function_key28` returns the `leave_battle` host command; `apply_native_function_key_host_command28` sets `request_close=true`. The `main.cpp` caller sends `WM_CLOSE`, whose handler retries any `save_pending` encoded recording and vetoes close on persistence failure. Otherwise it destroys the window and `WM_DESTROY` ends the application loop. F4 therefore requests application/window close, not a return to the menu. Its recording guard also applies to Esc/title-bar/OS close.

Unity's `NTSD28NativeFunctionKeyRouter.Route` likewise returns `LeaveBattle` for F4. `SimulationTickDriver.CaptureBattleFunctionKeyEdges` or `CollectNativeFunctionKeyRoute` sets `_nativeFunctionKeyLeaveBattleRequested`. A repository-wide search of self-written Unity scripts found the flag's only consumer to be `ConsumeNativeFunctionKeyLeaveBattleRequestForDiagnostics()`, used by Editor tests. No AppManager, Player host or normal battle lifecycle caller consumes it; `AppManager.OnApplicationQuit` can orderly stop the runtime only after a quit occurs for some other reason. The physical F4 probe asserts and then consumes the handoff for its own validation, masking the absent production effect. This establishes a source/call-graph difference, not a fresh end-to-end Player F4 measurement.

Required next implementation contract: identify one battle host owner for an accepted F4 close request; consume each accepted one-shot once, route through the existing ordered shutdown before Player exit, and preserve the native pending-recording veto if/when a corresponding Unity recording owner is in scope. Editor Play must expose a safe, testable close-intent seam without quitting the Unity Editor. A precise Task/Change must name the production paths and use an accepted/rejected physical F4 pair plus Player exit/zero-residual evidence. Do not implement F4 as `AppManager.UnloadBattle()` to return to Menu: that would contradict the current playable close path. Do not treat the existing physical handoff test as this effect's acceptance.

Q08 remains open. Q07's Scene-closure choice and result-stage-count authority match are independent. No Unity test, native rebuild or Play was run for this read-only audit.
