# Shared standard hit resource transfer: focused acceptance

Change `NTSD28-Q07-STANDARD-HIT-RESOURCE-TRANSACTION-001` is `FOCUSED_TEST_PASS / SCOPED_RAW_PARITY`. It does not close Q07, D-024, natural Battle Scene Play, other armor/reduced hit routes or formal EXE GPU presentation.

Before editing production, original Unity Editor compiled two new focused tests, then job `9e33d33e04c24d0287244d43321c00db` failed 2/2 for the expected reasons: pure transaction left current `PP` at 40, and type-3 child hit left root-owner `PP` at 400 instead of 426. The production edit now calls the common transaction on the standard unarmored type-0 victim route after HP/credit and before confirmed statuses. It uses the physical attack-effect source for injury/suppression, the existing two-hop resource owner for reward, world mode percentages, entity local-mode gate, and current `PP`/`PPMax`; it does not introduce an OID440 condition or change DAT values.

The original Editor recompiled with zero Console errors. The same two focused tests passed 2/2 in job `ec3afd83b41342ce9adb2d4d200f8506`; the pure transaction class passed 8/8 in job `f24b79da21584182a3c722f82304f461`. A wider two-class selection completed 24 tests with 23 passing and one failure: `NTSD28B5UnarmoredHpConsumptionProductionEditorTests.HitPlan_CharacterProjectionAccumulatesEffectiveHpDamage` throws `TargetParameterCountException` in a reflection helper. The failing test has no new resource assertion; its baseline status was not separately measured, so this run is retained as an adjacent failure, not counted as a green suite or attributed to this fix.

The unchanged formal root EXE traces and exact pre-fix scenario JSONs were reused. The original Editor wrote fresh `unity-x550-post.*` and `unity-x1200-post.*` files; both result files say PASS. `entity-comparison-post.json` reports X550 68 occupied entity rows × 21 mapped fields = 1428/1428 equal and X1200 100 rows × 21 = 2100/2100 equal across completed ticks 1–26. The nested pre/post Unity raw rows differ only at `.entities[0].vitals.currentMp` for X550 ticks16–26 (11 values); X1200 has zero nested raw differences. Formal/Unity target HP500→360, action186, OID440 birth/action/disappearance and both input/RNG call counts remain covered by those preserved focused traces. The formal LFR report has `nativeParityClaim=false`; no independent native recording, human physical key Play or GPU equality is claimed here.

`git diff --check` exited 0 with existing LF→CRLF notices. `Tools/Validate-ChangeLedger.ps1` passed when invoked under PowerShell 7 with explicit `-RepositoryRoot` (864 records, 26 governed changed code paths); the first Windows PowerShell invocation without explicit root failed in the script's default-parameter evaluation before validation, then was corrected. Menu/Battle Scene and GameConfig/ProjectBattleModeConfig SHA-256 hashes stayed at `785F828C...81E13`, `2EE465D8...B77A`, `0527D737...B8EA7`, `88E10D43...F55C`; the three recovered progress documents remain NUL-free. No DAT, image, Scene, camera, config asset or nonbattle file was modified by this package.
