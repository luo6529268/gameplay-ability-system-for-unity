# Next resource-transfer entry audit (read only)

2026-09-26 correction and next witness entry: the reduced selected-armor/guarded branch is no longer a missing-transfer candidate. `NTSD28-Q07-REDUCED-HIT-RESOURCE-TRANSACTION-001` connected its shared transaction, and the subsequent formal X550/X1200 scoped raw traces matched after the separate half-dvx fix. The paragraph below describes the state **before** those repairs. The remaining static candidates are the two CPoint callers at formal `battle_world.cpp` lines ~5962 (throw injury) and ~6098 (held injury), corresponding to `BattleCpointWriter.ApplyThrowInjuryDisplayLead` and `ApplyHeldInjury`. Neither Unity function currently invokes the shared hit-resource transaction; this is not yet an observed same-initial-state difference.

A real-content held-injury entry is formal `decoded_dat/c/hid/hid.dat` frame 239 (`state:9`, kind-1 CPoint, `injury:30`, `vaction:135`, `next:248`), source lines 1247–1250. Its Unity staged DAT has identical SHA-256 `8361BDF408E75163796307413B684991A7D8F187BF30101BA19FBAD766D86C82`. This proves a content candidate, not natural reachability or a live mismatch. `NTSD28B6HeldInjuryAccountingCoverProductionEditorTests.cs` already has a synthetic positive-injury fixture and `Tools/NTSD28AuthorityTrace/cpoint_settlement_remainder_witness.cpp` has the formal reciprocal-relation harness, but the latter uses injury zero; neither existing result proves the formal frame-239 resource transaction. A next independent Q07 Task should select the real frame and reciprocal kind-2 victim, arrange non-max current PP for both and a no-injury/invalid-relation control, then collect formal source/root EXE and original Unity same seed/input/tick first difference before any production edit. Do not mutate DAT values or recycle the now-closed reduced branch as new work.

This is a caller inventory, not a measured first difference or authorization to edit another branch. The focused `NTSD28-Q07-STANDARD-HIT-RESOURCE-TRANSACTION-001` change only covers the standard unarmored type-0 victim call at formal `battle_world.cpp` around line 6716 and Unity `BattleDamageWriter.ApplyStandardCharacterDamage`.

The same formal `apply_native_hit_resource_transfer` helper also appears in `battle_world.cpp` at approximately lines 5962 (cpoint throw injury), 6098 (held cpoint injury), and 7151 (selected-armor/guarded reduced hit). Current Unity `BattleCpointWriter.ApplyThrowInjuryDisplayLead` and `ApplyHeldInjury` resolve the resource owner for display/credit but do not visibly call the transaction. `BattleDamageWriter.ApplyAlternateDamage` applies reduced HP/MP damage and credit but does not visibly call the transaction after these writes. A narrowly searched `rg` across these two Unity writers found only the new standard-unarmored production caller of `ApplyNativeHitResourceTransaction`. These are static call-chain candidates, not proof that any particular playable matchup reaches them or differs in runtime values.

Next Q07 work should select one formally reachable reduced or held/throw hit with positive injury, non-max current MP, and a no-hit/disabled-gate control; then compare root formal EXE and original Unity Editor at the same seed/input/tick before editing that caller. Reuse the corrected current-PP pure transaction and source/owner semantics, and use a new Task/Change Record with exact paths. Do not alter DAT numbers, mode DAT, Scenes, nonbattle code or the fixed-view displacement exception.
