# Q07 Sasuke needle target-hit first difference

Status: `VERIFIED_SCOPED_DIAGNOSTIC / FIRST_DIFFERENCE_FOUND`. This closes only the diagnostic; Q07, D-024 and the full battle alignment remain open.

The unchanged root formal EXE SHA-256 is `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The paired playable/core C++ diagnostic compiled with exit 0. Both X550 and X1200 source-authored LFR files replayed through the root EXE with `passed=true`, 27 rows including initial state, and 26 completed ticks. The reports explicitly say `nativeParityClaim=false`: the source-authored LFR is an input witness, not an independently recorded native replay. The already-open original Unity Editor compiled with zero Console errors and produced PASS for both strict 26-tick raw-capture scenarios; no second Unity project or computer-use was used.

All other scenario inputs are fixed: seed `0x28A55A5A`, Sasuke OID11/action110 at X500, Naruto OID7 at X550 or X1200, defend/right/attack at completed ticks 2/4/6. The hit case creates four OID440/action2 at tick15; at tick16 their hits reduce Naruto HP 500→360 and change action to 186 on both sides. The children disappear at tick19. The miss case leaves Naruto HP500 and four OID440 children through tick26. Source CSV, root-EXE traces and Unity raw/domain/input-RNG streams are preserved separately in this directory.

`entity-comparison.json` compares the 21 common fields on every occupied entity at completed ticks 1–26. X1200 has 100 entity rows, 2100 field comparisons and zero differences. X550 has 68 entity rows, 1428 comparisons and 11 differences. Its first and only differing field is Sasuke slot0 `mp`: formal 500 versus Unity 400 at tick16, persisting through tick26. Native CRT counts at tick16 are 3008 (hit) and 3000 (miss); Unity's hit trace also records eight CRT calls at that tick. CRT seed/state parity is not claimed because root LFR initial CRT state differs from the Unity explicit-seed fixture.

The first-difference call chain is the shared native `BattleWorld28::apply_standard_hit` unarmored/type-0 victim branch: after HP and score writes it resolves the resource attacker and calls `apply_native_hit_resource_transfer`, which rewards positive resource injury under the local-mode/type-0/suppression gates, then processes drain/gain. Unity `BattleDamageWriter.ApplyStandardCharacterDamage` performs HP/stat writes and display/status writes but no equivalent resource transaction. Its standalone `ApplyNativeHitResourceTransaction` helper has no production caller and currently writes the legacy `MP` bank rather than native-current-MP `PP`. This is a shared character-hit path, not an OID440 special case. The exact production fix requires a separate Task and Change Record; this diagnostic does not authorize it.

No production script, DAT value, PNG, Scene, camera, ProjectSettings or nonbattle logic was changed. Two initial invocation failures (wrong VFS root, then missing X1200 output directory) were corrected before successful runs; no failed run is presented as PASS. The next task must preserve the pre-fix traces and write new post-fix outputs rather than overwriting them.
