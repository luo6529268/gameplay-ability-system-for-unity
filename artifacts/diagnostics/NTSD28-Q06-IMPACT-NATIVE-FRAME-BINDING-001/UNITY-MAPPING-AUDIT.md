# Native impact frame binding mapping

Only live Action writer uses old DirectWriteRawFramePreserveWaitCounter. HitPlan ProjectNativeImpactWriterEffect uses the ordered plan and projects frame/runtimeframe only; adjacent kind15 old getter is a different transaction. Keep existing B6 I1/I2/I3 verified rules/operation order and captured fields unchanged.

New fixture needs source before/after extras environment->EnvironmentState320, encoded credit->CatchSourceSlot90, impact source->ImpactSourceSlot164, pending XYZ->KnockbackVx/Vy/Vz, descriptor/state/wait/next and existing frame/counter/latch/snapshot. Capture every participant including two-hop owner/credit; ordered operation can change target only after valid preflight. Same seed/input/tick following may revisit the hit, so fixture must use the same source ITR and owner state and cannot disable a later hit only on Unity.

Static formal330: kind10 121 / kind11 61, all respond0; no kind17/18. Potential type0 default182 declared157/implicit1 OID899 c/0/spe.dat. Additional inspection found its22frames have zero bdy and zero itr blocks, so the static default content does not demonstrate it can receive this geometric hit. Do not elevate this to a current formal gameplay defect. Source20 isolates native contract and future identity/domain concerns.

Single caller change is only justified after measured Unity descriptor first difference, exact Record expansion and narrow regression. No expansion to global getter, HitPlan carrier schema, B6 eligibility logic, Scene/resources or framework. Representative testing replaces duplicate profile/character products under latest user instruction.
