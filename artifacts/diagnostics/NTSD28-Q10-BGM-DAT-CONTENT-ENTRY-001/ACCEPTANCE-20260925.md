# Q10 formal BGM DAT content entry

Status: `DELIVERED_CONTENT_ONLY / PLAYBACK_OPEN`. User D-023 selects formal non-excluded DAT content. This package added exactly `Assets/NTSD/Content/LoganRuntime/decoded_dat/data/bgm.dat` and its Unity-generated `.meta`; it did not modify a DAT value or copy music files.

The formal release `resources/runtime/decoded_dat/data/bgm.dat` was 454 bytes with SHA-256 `ADA6B4E9C8F5C6FBD9A9A85F5E6188CD1CEE5E85139CD9367915EF786B64CAEA`. The destination did not exist before the copy and has the same 454 bytes and SHA-256 afterward. The original Unity Editor's forced asset refresh imported the file and generated DefaultImporter `.meta` GUID `12d46eaf5869cb243a05f5178581d5b2`; its error console query returned 0 entries. Non-excluded staged DAT count is now 338 of the previously inventoried 353 formal paths, assuming no concurrent content changes; this count was freshly read from the project tree after this addition.

The formal DAT names eight BGM entries: `bgm/main.wma`, `stage1.wma` through `stage5.wma`, `boss1.wma`, and `boss2.wma`. All eight exist under formal `resources/runtime/vfs/bgm/`; none exists under the project's staged `LoganRuntime/vfs/bgm/`. In the paired playable `game_session.cpp`, `GameSession28::initialize` loads optional `data/bgm.dat` into `native_bgm_virtual_paths_`, and `resolve_native_bgm_selection` selects a concrete virtual path, using synchronized RNG for random selection. A source search under Unity `Assets/NTSD/Scripts` found no `bgm.dat` reader or equivalent BGM-path table consumer; the existing `DirectBattleRandomBgmCallSite` is a random-call contract only. Therefore this package proves byte availability, **not** Unity BGM selection, media loading, playback or audible parity.

Saved disk SHA-256 remained Battle Scene `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`, Menu Scene `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`, and `GameConfig.asset` `0527D737A1FA38FC56B51D00DC6E96A421D3C67222546368B147C2D074CB8EA7`. No script, audio file, old Config resource, Scene, or nonbattle file was changed by this package.

Q10 next: define a battle-only BGM selection/playback contract from the formal release and the project's approved audio scope, then make a separate Task/Change for runtime consumers and media handling. D-023 does not itself authorize an eight-file WMA or general WAV deployment. The remaining 15 absent formal non-excluded DAT paths retain their pre-existing Q08/Q09/nonbattle/exception owners; the older 337/353 report is a pre-addition snapshot. Q07, Q10, R17 and the overall goal remain open.
