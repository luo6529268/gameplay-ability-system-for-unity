# Q07 actual Menu scene interaction audit

Decision: `STATIC_CALL_CHAIN_CONFIRMED / ACTUAL_MENU_RUNTIME_PENDING`. This audit changes the next verification target; it is not a Menu Play pass.

`Assets/NTSD/Scene/NTSD_Menu.unity` serializes `AppManagerGO` with `menuSceneName: NTSD_Menu`, `battleSceneName: NTSD_Battle` and `gameConfig` GUID `513170c32731baa449d4d0e9abf8c827`. That GUID equals `Assets/NTSD/Config/GameConfig/GameConfig.asset.meta`; the asset currently serializes `BattleContentRuntimeRoot: Assets/NTSD/Content/LoganRuntime`. The scene has `GameStartBtn`, `MainMenuPanel`, `LoadingPanel`, `SelectGameModePanel`, and `SelectCharacterPanel`. At audit, Menu Scene SHA-256 was `6CF124A17F692325CED5774C3C10C3324AA047439BC6C3EB08856188A78AB4B1`; protected Battle Scene SHA-256 was `BCD1047BF912C6A4A8BC9F3A76EAF3FA954211AD064E0402B1C01BF3BA0E9FB6`.

Current code path: `MainMenuController.Awake` attaches `_GameStartButton.onClickCallback`; its `OnGameStartCallBack` calls `MenuUIController.ShowLoading`. Active `LoadingPrewarmController.Start` calls `PrewarmOnceAsync`, where a nonempty configured root selects `CharacterAnimtorManager.PrewarmConfiguredLoganContentAsync`; after prewarm it calls `ShowSelectGameMode`. `SelectGameModeController` index 0 enters VS and calls `ShowSelectCharacter`. `CharacterSelectionController` goes through join/selection, countdown, CMC and battle settings; its settings confirmation builds `MatchConfig` and calls `AppManager.SetMatchConfig` plus `LoadBattleAdditive`. `AppManager.OnSceneLoaded` starts `InitializeBattleAsync`, validates configured content, applies MatchConfig, prepares characters and moves to `BattleRunning`.

Existing `NTSD28B11SourceCallerPlayProbeEditor` in `mode=menu` directly creates a `LoadingPrewarmController` and calls `PrewarmOnceAsync`, then invokes private `AppManager.InitializeBattleAsync` using the active probe Scene. It proves the serialized formal root and menu prewarm owner/candidate cache path, as accepted in `SERIALIZED-MENU-CALLER-ACCEPTANCE.md`; it does not exercise `GameStartBtn`, the Menu scene panels, actual selection/confirmation or `LoadBattleAdditive`.

Next evidence required: a bounded test-only Menu Play sequence through the actual scene UI flow, formal content publication, additive battle Scene, ordered exit and no residue. If the flow fails, retain the first observed failing state and create an exact battle-boundary Task/Change before a production correction. Do not alter nonbattle UI or Scene to make the probe pass, and do not claim visual/menu UX parity from this static audit. No computer-use was used.
