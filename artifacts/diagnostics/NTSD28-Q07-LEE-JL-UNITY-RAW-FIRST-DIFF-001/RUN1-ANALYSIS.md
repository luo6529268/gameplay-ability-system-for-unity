# Run 1: formal Lee J,L versus original-Editor raw capture

The original Unity Editor refreshed and compiled `NTSD28UnityRawCaptureEditor.cs` with zero Console errors. Its EditMode request for `lee-jl-formal-45.json` returned PASS and wrote 45 entity/domain/input-RNG ticks to unique `original-editor-run1.*` files. The pre-existing `Temp/NTSD28UnityTrace/unity-raw-capture.result` remained at SHA-256 `C5ADAAC2E58D39DECD8311FB6A34950804DA7D4E276F1906BE9645D35F4E0859`. The original Menu/Battle Scene SHA-256 remained `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13` / `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`.

Across completed ticks 1–45, 2557 mapped comparisons found zero differences: input update phase, and for every occupied slot the OID, type, action, state, frame counter, facing, X/Y/Z, Vx/Vy/Vz, HP, MP, team and owner. Tick 5 OID814/slot50, tick 6 OID204/slots51–55/action20/owner0 at `(437,-35,651)`, and the later births/lifetimes match the formal EXE LFR trace. This is a bounded entity-state comparison, not pixel or physical-key proof.

The initial RNG states do differ because the fixtures are not yet identical. The Unity raw tool's `ConfigureWorldAndRoster` always calls `ResetForDirectBattle(seed)`, which consumes one synchronized `0x004021E0` random BGM choice; its header starts at counter/index/calls 1/1/1. The formal source diagnostic supplies explicit BGM selection 2; `GameSession28::resolve_native_bgm_selection` only draws at selection 0, and the root LFR manager restores its serialized synchronized state at 0/0/0. The Unity seed `682973786` yields CRT state `1758127634` after the 3000 table draws. The formal EXE LFR playback uses `BattleConfig28`'s default seed 0, whose 3000-draw CRT state is `3374725112`; this explains the CRT state mismatch in replay without showing a production Unity RNG rule defect. Neither stream drew an additional CRT value during the 45 measured ticks. The synchronized call schedule has the same single call site `130` at tick 2, but Unity's absolute counter/index is offset by one.

Next: retain this run and change only the exact Editor Lee fixture to `ResetFromSeed(seed)` so it models the native source's explicit BGM selection, then run a unique second capture. The 3/26-tick frozen scenarios and production bootstrap must stay unchanged. Compare source/release/Unity input, RNG call results and entity state again; report any new first difference. Do not call this first run full RNG parity.
