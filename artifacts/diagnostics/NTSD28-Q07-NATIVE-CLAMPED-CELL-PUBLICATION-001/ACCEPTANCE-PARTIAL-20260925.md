# Q07 formal PNG native-clamped cell publication: partial acceptance

Status: `RUNTIME_PENDING / ORIGINAL_EDITOR_CATALOG_PLAY_PASS`. The generic formal-PNG prewarm adapter is implemented in `CharacterAnimtorManager.cs`; original DAT/PNG bytes, Battle/Menu Scene files and battle rules were not changed. Formal root release EXE SHA-256 remains `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`.

The adapter samples out-of-image source cells from the decoded formal PNG with point/CLAMP semantics into full-size derived cells. It includes wholly and partially outside cells, all four edges and transparent/nonwhite pixels. Identical `(width,height,RGBA)` cell data shares one derived Texture2D/Sprite/virtual atlas source per publication. Each resource enters the existing staged sprite/texture/atlas ownership transaction. In-bounds cells keep the prior path; `BattleSpriteCentralBinding.IsValid` is unchanged. The `BuildBattleSpriteCatalog` overload uses the derived cell's valid texture/rect for both Legacy Sprite and central binding. The virtual path is an identity under project `Temp/NTSD28NativeClampCells`; no file is written there.

In the original Editor and original saved `NTSD_Battle` Scene, the selected three new EditMode tests passed (job `bc5e225232f44cf09162132df13c8761`, 3/3), covering point/CLAMP boundaries, formal OID32 white cell/hash reuse and catalog Legacy/central source binding. A focused 5/5 run (job `b4903168ddd143ff86a58328fc3250ba`) included the new three plus first-declared overlap ownership; six exact relevant native range/PNG alpha/atlas tests passed in job `d52c41594bc84b5d847c2ef3a090724f` (6/6). Unity runtime and Editor script compilation completed; these test jobs used the current imported assemblies.

The first original Battle Scene Play after production edit (`oid32-catalog-clamp-20260925-01.json`) reached driver tick5 with formal content and reported catalog count **29,034**, OID32 pic0 valid and pic64 present as a 79×79 derived texture with valid central binding. Prior same-scene baseline count was 27,825 and pic64 was missing. After a read-only count extension to the existing one-shot probe, independent Play `oid32-catalog-clamp-20260925-02.json` repeated count 29,034 and found **1,212** clamped catalog entries sharing **36** virtual sources and **36** textures; invalid central bindings **0**. Pic64 had a Legacy Sprite and valid `SourceTexture2D` central binding. The second JSON SHA-256 is `98881CFF5B7293C3249D040DBC356AC486B2D2D6673587A06817BD68E65BDC2D`. These are production publication observations, not merely a helper test.

Both probes exited Play. The original Editor reported idle/nonPlay afterward. Menu/Battle Scene disk hashes remained `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13` / `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`, and the probes reported `sceneDirty=false`. No computer-use or second Unity project/Editor was used.

Remaining gates: the original Editor has not rendered a controlled OID32/action95 **entity** into a captured Battle Scene pixel image; paired-source D3D11 offscreen OID32 action95 pixels are proven separately, while root release EXE GPU capture and natural path are not. Actual memory high-water and exact post-stop borrower count for derived sources were not reported by this probe; staged resource ownership follows the existing transaction but still needs a targeted lifecycle witness. The full `BattleRuntimeSelfCheck` was not rerun for this presentation-only edit; the focused catalog/atlas tests above were selected to match the changed paths. Q07, Q09/R17 and the parent goal remain open.

2026-09-25 update: the separate `NTSD28-Q07-OID32-UNITY-ENTITY-PIXEL-001` original-Editor witness now proves the controlled entity command and visible WorldCamera white pixels, restores fixture counts, and observes zero live `native_clamp_` textures after Play exit. The preceding “has not rendered” sentence is historical. The new evidence does not measure peak memory, prove natural entry, or compare direct root-EXE GPU images; Q07/R17 and this production package's wider exit remain open. See `artifacts/diagnostics/NTSD28-Q07-OID32-UNITY-ENTITY-PIXEL-001/ACCEPTANCE-20260925.md`.
