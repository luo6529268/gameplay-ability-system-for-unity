# NTSD28-Q07-MODE-COMBO-PUBLISHED-ACTIVATION-001

Status: `CODE_WRITTEN / FOCUSED_ACCEPTANCE_IN_PROGRESS / UNITY_COMPILE_PENDING` (2026-09-22). Parent: BATCH-04/Q07, R15 Q07 content-identity trigger. Authority: formal `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; declared playable `game_session.cpp:1332-1374,4148-4158` and native combo core tick consumers. The formal selected `data/mode/ntsd.dat` combo is present with bound/facing/respond/caughtact `1/1/50/1`. Source and staged hashes, pure projection and R15 impact are in `NTSD28-Q07-MODE-COMBO-INPUT-PROJECTION-001` and `NTSD28-Q07-MODE-COMBO-R15-IDENTITY-IMPACT-001`.

## Exact ownership and boundary

Production scripts: `Assets/NTSD/Scripts/Animation/LoganObjectCatalog.cs`, `LoganContentIdentity.cs`, `LoganVisualContentCandidate.cs`, and `Assets/NTSD/Scripts/Simulation/Host/SimulationTickDriver.cs`. Focused test script: `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07ModeComboPublishedActivationEditorTests.cs` plus its `.meta` if created. The existing `LoganModeComboInput.cs` is an input dependency, not a second parser to rewrite. Governance files are this Task, matching Change Record, Ledger, STATE, handoff, and alignment R15 status. No Scene, Prefab, ProjectSettings, menu/results, Q06 producer, audio, old resource, `Gen`, or third-party script change.

Capture the selected mode input with the object catalog; version the joint identity when mode is present, retaining the three-component V1 identity only for truly absent mode/legacy fixtures. Reject changed, removed or newly appeared mode inputs from a captured publication, including byte changes that leave the four tuple values unchanged. Reuse the existing three-owner atomic content publication. Apply the **published** tuple to the existing `NTSD28NativeComboRuntimeState` after any World reset and before the first simulation tick for both `AppManager` and direct Battle Scene bootstrap/rematch. The common seal path is the last shared preparation boundary; ensure its repeated sealed call is a no-op and do not overwrite snapshot restoration or live battle state. No per-tick file reads or new battle runtime owner.

R15 trace emitters/comparator remain an explicit downstream contract, not silently declared aligned: the formal native old trace header only contains object/fusion, while Unity's versioned full identity will also contain mode. Do not fill an absent native mode field with zero or compare V1 and V2 full hashes as equal. Q07 cannot close until same-version trace treatment and original-project runtime evidence are in place. Current entity/aggregate/checksum schema versions stay unchanged unless an observed test proves otherwise.

## Acceptance, risk, rollback

Focused tests must verify formal/staged tuple and V2 vector; absent mode V1 vector; altered/same-tuple mode bytes and absent↔present cache invalidation; three publication owners' common identity; World reset→seal and direct seal/re-entry tuple before first tick; repeated seal and snapshot safety; existing ordinary/caughtact gates without changing their producer. Compile and run only relevant focused tests in the original Unity project when possible, then original Battle Scene and same seed/input formal comparison. If the current Editor has not recompiled new scripts, report `UNITY_COMPILE_PENDING` and do not use a second Unity project or computer-use. Static/pure external compile is a proxy only. Preserve dirty user files and unrelated work. Rollback means reviewing and reversing only this Change ID's exact diff and its new test/meta; never use blanket reset/clean or delete old resources. A failure before seal must leave the battle stopped/preparing, not resume with an unconfigured formal World.
