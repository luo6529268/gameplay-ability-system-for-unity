# NTSD28-Q09-NATIVE-WORDS-PUBLICATION-CALLER-AUDIT-001

Status: `READ_ONLY_CALLER_AUDIT / PRODUCTION_PUBLICATION_GAP_CONFIRMED` (2026-09-22). Original Unity project only; no Editor launch, script change or runtime test.

## Formal battle path

`resources/runtime/decoded_dat/data/resource.dat:18-23` lists `sprite/UI/WORDS0.png` through `WORDS5.png`, indices 16-21. `GameSession28::initialize_resources` resolves all six (`source/ntsd28_playable/src/game_session.cpp:1217-1241`). `render_snapshot.cpp:778-816,1939,2187-2193` assigns a resource slot/path to name/HUD glyph and entity-nameplate commands. `d3d11_renderer.cpp:738-774,1739-1770` selects the atlas and draws each glyph from an 8×16 source rect on a 16-pixel horizontal/vertical stride with 1-pixel top offset. All six staged formal PNG headers are 251×257, indexed 8-bit color-type 3; exact source/staged file SHA is in `NTSD28-Q09-NATIVE-WORDS-PNG-STAGING-001/TASK-CONTRACT.md`.

## Current Unity production path

`CharacterAnimtorManager.cs:1553-1556` calls only `BattleCommonVisualCatalog.Build(shadowPrefab, stagedSpark.Texture, stagedSpark.Sprites)`. The current codebase's `.WithWords(...)` calls are in `BattleRuntimeSelfCheck.cs` test setup; production has no such call. `BattleCommonVisualCatalog.IsRuntimeReady` requires Shadow+Spark but `IsComplete` separately requires `IsWordsValid` (`BattleSpriteCatalog.cs:460-488`). Thus the production common visual publication does **not** contain WORDS glyph bindings; the previous description “Unity still publishes old WORDS BMP” is corrected. The source API and diagnostic still mention BMP, but they are not evidence of an active BMP producer.

`BattlePresentationShadowBuild.cs:2955-2985,3154-3181` can emit word-glyph commands when a catalog binding exists; with the current production publication, `TryGetWordGlyph` fails and those commands are skipped. `BattleCentralRenderTypes.cs:575-640` has a downstream word-glyph resolver but likewise cannot supply missing production bindings. This is a static first difference in the battle presentation path, **not** an observed original-Editor screen comparison. The Unity 251×257 size and 8×16/16-stride geometry contract (`BattleSpriteCatalog.cs:383-389,563-576`) is compatible at the basic rect level; formal PNG decoding, alpha/color-key behavior, resource identity, epoch/freshness, atlas fallback, publication lifecycle and actual pixels still require implementation and verification.

## Next implementation boundary

After the original Editor has freshly compiled the already-written Q07/R15 scripts, define one exact Q09 Task/Change for formal WORDS0..5 capture and immutable identity, six-sheet decode/Sprite binding, integration into the existing atomic common catalog publication, central/legacy draw lifetime and focused nameplate/HUD cases. Keep the existing Unity/GAS structures and old image owners intact. Cover entry, cancellation, rollback, shutdown and re-entry, and compare a small equivalent set of normal/combat-group/unsupported-glyph examples against the formal source/EXE and original-project pixels. No menu/ordinary HUD rewrite is implied. Q09 and R17 remain open.
## Candidate identity dependency found on return

The exact six PNGs are also absent from the **current content-candidate freshness set**. `LoganVisualContentCandidate.Capture` (`Assets/NTSD/Scripts/Animation/LoganVisualContentCandidate.cs:60-78`) enumerates only each character's DAT `files`, `head`, and `small`, then hashes those image paths into `VisualFingerprint`/`SourceCacheKey` (`:36-56`). `AssertInputsCurrent` rehashes only that captured image list (`:81-105`). Formal global `resource.dat` WORDS paths are not selected here. `CharacterAnimtorManager` uses this candidate key for publication/currentness (`:122-164,425-433`) and final freshness gate (`:1608`). Therefore merely decoding six PNGs inside the current prewarm would leave changes to those files invisible to the published visual identity. The Q09 implementation must add the six formal global inputs to an exact versioned candidate/freshness contract before atomically publishing glyph resources; do not borrow the actor image list by implication or drop the existing test/legacy paths.

## Original-Editor control probe

The original Unity Editor remains PID 33236 on `I:\GitHub\Unity_GAS\gameplay-ability-system-for-unity`. `unity status --format json --no-banner --non-interactive` returned `STATUS_NO_INSTANCES`; `Packages/manifest.json` has no `com.unity.pipeline`, and this project uses Unity 2022.3.62f3. Thus Unity CLI has no connected-editor command for this existing instance. The `Library/ScriptAssemblies` timestamps are 2026-09-22 04:23:43/44Z, earlier than current `LoganModeComboInput.cs` at 05:54:38Z. This is not evidence of a current Unity compile. No Pipeline install, Editor restart, second instance or computer-use was attempted. Continue source/package work independently; Unity import/Play proof remains pending on the original Editor.
