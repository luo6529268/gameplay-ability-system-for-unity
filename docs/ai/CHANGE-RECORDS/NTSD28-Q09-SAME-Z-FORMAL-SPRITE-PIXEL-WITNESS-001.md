<!-- CHANGE-RECORD
id: NTSD28-Q09-SAME-Z-FORMAL-SPRITE-PIXEL-WITNESS-001
status: VERIFIED
change-kind: TEST_ONLY_BATTLE_SCENE_FORMAL_SPRITE_PIXEL_WITNESS
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28Q09SameZFormalSpritePixelProbeEditor.cs
authority: Formal NTSD2.8-Logan playable render_snapshot.cpp equal-Z painter order, OID120/121 formal runtime sprites, user Q09 alignment and camera-enabled instruction
evidence: docs/ai/TASKS/NTSD28-Q09-SAME-Z-FORMAL-SPRITE-PIXEL-WITNESS-001.md
-->

# 同 Z 正式精灵 GPU 遮挡见证

Pre-change: current production sorter has focused test and scene materialized-order evidence, but that scene fixture has no sprite or draw command. Original Battle Scene central camera has been restored to enabled; current Scene disk SHA is `9409F2BCFE3E657A6C3C88A7527045CC384D50AAACC99197D53AACA38F3B3A39`, with no Scene Git diff. No GPU overlap-pixel witness exists. The older R07B probe is blocked by a legacy DAT `hit_Fa` prerequisite.

Affected code: one new Editor-only Play diagnostic class and its `.meta`. No production API or serialized Scene/asset changes are authorized. The probe will use formal OID120/121 frame0/pic0, original World/factory and original world camera temporary target. Because the central plan caches by simulation tick, paused A-only/B-only/overlap reference captures use distinct diagnostic presentation tick numbers; this does not advance the battle driver and must be identified as controlled publication. It will report controlled camera-render pixels separately from full-tick natural display and formal EXE parity.

Expected side effects: temporary owned World entities, renderer/logic-pool borrowing, central publication, camera target/culling/clear state during capture, and one diagnostic result/image under `artifacts/diagnostics/NTSD28-Q09-SAME-Z-FORMAL-SPRITE-PIXEL-WITNESS-001/`. All temporary state must be restored. User-authored Scene/HUDBg and both enabled cameras are protected.

Acceptance and rollback: see Task Contract. Validation must include compile 0 errors, focused original Scene Play, true submitted formal drawable commands, discriminating overlap pixel, cleanup, unchanged Scene disk hash and Git diff. Natural full-tick rendering, Legacy and EXE A/B remain pending even after this package passes. Do not remove files as rollback without explicit authorization.

Current result: CODE_WRITTEN. Added only `NTSD28Q09SameZFormalSpritePixelProbeEditor.cs` (Unity `.meta` pending import). The class now owns a bounded request/Play state machine, the formal OID120/121 producer and handles, four controlled presentation publications, the original world-camera RT readback, color-oracle comparison and cleanup. A-only/B-only/both use temporary position changes and distinct diagnostic presentation tick labels while the real logic tick remains paused. It does not change camera `enabled`, production runtime, DAT or Scene. Compile and actual Play have not yet been run; syntax/API/runtime failures and cleanup are unverified.

First original Editor compile found three missing LF2Entity abstract implementations in the temporary producer. After adding those, original Editor reported 0 Console compile errors and imported `.meta`. First original Battle Scene Play reached real tick5 and safe pause, then failed at producer constructor `Health.BindRuntime` because the probe directly inherited `LF2Entity` instead of the existing `LF2OtherObject` producer base. The failure JSON is retained under `artifacts/diagnostics/NTSD28-Q09-SAME-Z-FORMAL-SPRITE-PIXEL-WITNESS-001/first-producer-constructor-failure.json`; cleanup restored objects 4/4, slots 2/2, renderer/logic pool 2/2 and pause, Scene hash unchanged. Test-only producer now inherits `LF2OtherObject`, matching the existing R07B helper; recompile and rerun pending. This first failure is not a P-04 ordering result.

Second original Editor compile again had 0 Console errors. Second original Battle Scene Play reached all four controlled publication stages and produced actual central body commands: OID120 slot51, OID121 slot52, both Z241, command indices 4 and 2 respectively (greater slot first), real logic tick remained 5. It failed only at the overlap oracle: zero discriminating pixels found. Cleanup again restored objects 4/4, slots 2/2, both pools 2/2, pause and Scene hash. Result archived as `second-no-discriminating-pixel.json`. This does not prove GPU ordering; the third diagnostic revision records per-capture pixel counts, command geometry and four readback images to identify whether the sprites are outside the camera, do not overlap, or are filtered by rendering.

Third focused Play confirmed both formal body commands submitted and still equal Z241 with slot52 command index2 before slot51 index4, but the four GPU captures showed the weapon at the same top-center location even in the supposed hidden baseline. Difference bounds were confined to the baseline characters' lower pixels. `LF2Entity.SetPos` writes precise Runtime position only, while the presentation builder reads `XInt/YInt`; the probe had omitted `Runtime.SyncIntegerPosition()` after its temporary moves. Archived `third-stale-integer-position.json` and four PNG readbacks. This is a test-fixture defect, not evidence that production ordering or rendering is wrong. The probe now synchronizes the integer position mirror at each controlled move; focused recompile/Play pending.

Fourth focused Play produced a nominal PASS after the integer-mirror fix: formal body commands had equal Z240, slot52 index1 before slot51 index3, and controlled GPU A/B/composite images were saved; cleanup and Scene hash remained clean. Independent review and screenshot coordinates then invalidated the pixel conclusion: the matching sample `(757,135)` lies by a baseline character, outside the formal weapon body intersection. The full-screen oracle could confuse unrelated pixel variation with overlap. Archived `fourth-unrestricted-pixel-pass.json` and four images solely as superseded diagnostic evidence; this PASS must not be cited as P-04 pixel acceptance. Reviewer also found the spawn-before-owned-return cleanup gap and repeat-on-pre-Play-failure request gap. Current test-only revision restricts sampling to projected intersection of the two actual body commands, reports full candidate/match/opposing/mixed counts, registers new spawned entities for cleanup in a `finally`, and consumes failed requests in `Finish()`. Original Editor compile and rerun pending; P-04 remains runtime pending.

Final scoped validation: the original Editor recompiled the final probe with 0 Console errors. The fifth focused original Battle Scene Play returned `PASS`: OID120 slot51 and OID121 slot52, both Z240, central body commands index3 and index1 respectively; the two body-command projected intersection was x[1000,1069), y[551,607). All 11 discriminating overlap pixels matched later-painted OID120 and none matched earlier OID121 or mixed colors. Representative bottom-origin `(1033,578)` was white in baseline, RGB165/165/147 for A, RGB201/201/182 for B, and RGB165/165/147 in composite. Separate saved-PNG Python/Pillow read reproduced this sample and 11/11/0/0 count. Editor exited Play, Battle Scene SHA remained `9409F2...3B3A39` and Git Scene diff empty; object count4/4, claimed slots2/2, two pools2/2 and pause restored. Formal staged OID120/121 PNGs individually SHA-match the original runtime files. Independent reviewer rechecked the three P2 fixes and found no remaining P0-P2 affecting this scoped GPU evidence. `Tools/Validate-ChangeLedger.ps1` and final diff check are required before handoff. Detailed evidence: `artifacts/diagnostics/NTSD28-Q09-SAME-Z-FORMAL-SPRITE-PIXEL-WITNESS-001/ACCEPTANCE.md`. Status VERIFIED applies only to original-Scene controlled central GPU overlap; natural full-tick, Legacy, formal EXE A/B and parent P-04/Q09 remain open.

Handoff gate: `Tools/Validate-ChangeLedger.ps1 -RepositoryRoot <workspace>` returned PASS (791 records, four governed script files currently in the shared diff, including one from another active UI task; this probe script is covered by this Change ID). `git diff --check` returned 0. New probe `.meta` GUID has exactly one match under Assets. The original Scene still has no Git diff after Play. The other UI task's files and untracked B11 inventory outputs were preserved without inclusion in this Change ID.
