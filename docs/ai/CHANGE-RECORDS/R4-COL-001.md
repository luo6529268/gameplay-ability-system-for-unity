# R4-COL-001 — C++ `hit_confirm2` attacker abort gate

<!-- CHANGE-RECORD
id: R4-COL-001
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Animation/LF2Objects/BattleHitCandidateSequenceRunner.cs
code-path: Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\entity\collision.cpp:57-65,1257,1263-1266; J:\QQFile\NTSD2.4\ntsd_release\src\entity\game_tick.cpp Loop1/Loop2 live scheduling
evidence: SOURCE-CONTRACT-VERIFIED / UNITY-PREFLIGHT-VERIFIED / UNITY-COMPILE-PASS-2026-08-22-034414 / FULL-SELF-CHECK-PASS-2026-08-22-034457 / PLAYMODE-PENDING / CXX-RUNTIME-TRACE-BLOCKED
-->

> 创建日期：2026-08-22  
> 最后更新：2026-08-22  
> 类型：battle / collision / candidate-consume / `hit_confirm2` / test  
> 所属 Work Package：`R4-COL-01`  
> 当前状态：`RUNTIME_PENDING` — 最小 runner gate、exact/shared fixture和 kind5 fixture isolation 已写入；current Unity compile和full self-check已通过，仍缺 Play Mode与C++ runtime trace。

## 1. 目标与允许范围

只在 `BattleHitCandidateSequenceRunner` 的 shared frozen-candidate consume path 实现 C++ C07-A。
只允许修改 runner 和 `BattleRuntimeSelfCheck` 的 focused fixture；禁止改候选采集、scheduler、CPoint、
held/link、opoint、renderer或 C++。若 C07-A 使既有 kind5 test 暴露 holder→held weapon 的同 tick
合法命中，则允许把**测试 holder**设为 collect-time `AttackExempt>0` 来隔离该非目标攻击，并断言 weapon
carrier为0；不允许改变 production `HitConfirm2` writer或 gate。

## 2. Authority / 差异依据

- **C++ VERIFIED**：`collision.cpp:57-65` 先 vrest skip，再对
  `attacker.hit_confirm2 != 0 && victim.char_data->obj_type == 0` 转 `next_attacker`；1257 label 位于
  pair loop 外，1263-1266 证明 Loop1 / Loop2 共用该行为。
- **Unity VERIFIED**：统一 runner 当前没有 `HitConfirm2` reader，并在 vrest recheck 前执行 runtime ITR
  resolution；现有 return `true` 已表达 sequence break。
- **C++ runtime trace BLOCKED**：R1-WP02 未解除；本包不运行 C++ executable。

## 3. 计划中的最小修改

1. 保留既有 `ItrIndex` 防御性有效性检查；target resolve 后先完成既有 `CanConsumeRecordedCandidate`，若失败保持 current-candidate skip；
2. 若 `attacker.HitConfirm2 != 0` 且 target current DAT type 是 character，返回 sequence-break；
3. 仅在不 abort 时继续现有 runtime ITR / disposition / dispatch；
4. 已加 exact 与 shared-character-DAT two-candidate fixture，包含 `HitConfirm2=0` 连续消费对照。

## 4. 验收、风险与回滚

- 已完成代码写入；接下来依次记录 static、existing Unity Editor scripts refresh/compile、full self-check、ledger validator、
  diff check 的真实结果；任何失败原样保留。
- 最大状态仅为 `RUNTIME_PENDING`，直到拥有风险相称的 Play Mode / C++ trace 证据。
- 回滚范围严格限于本 Record 的两个代码路径及关联文档；提交 hash：未提交。

## 4.1 首次 self-check 失败与最小夹具修正（2026-08-22）

- **实际运行**：existing Unity Editor compile后执行完整 `BattleRuntimeSelfCheck`；结果文件
  `Temp/NTSD_BattleRuntimeSelfCheck.result` 于 03:40:54 写入 `FAIL`。
- **首个失败**：`BATTLE-AUDIT3-05: held kind5 must consume the frozen pair ... hp=100, vrest=0`。
- **C++ source解释**：`collision_collect.cpp:104-120` 对 `attack_exempt>0` 跳过攻击者采集；
  `collision_collect.cpp:242-360` 允许 same-relation character 对 light weapon 的 kind0 candidate；
  `collision.cpp:57-65` 再证明：若 holder 先消费此 candidate而命中 held weapon，weapon 的
  `hit_confirm2` 会在它的 object-loop character target前终止 sequence。
- **结论**：旧 kind5 fixture没有隔离 holder的非目标攻击，因此其旧“必命中”断言与新确认的 C++ C07-A
  不能同时成立。最小修正只会在该 fixture collect 前设 `holder.AttackExempt=1`，并在 object consume前
  断言 `heldWeapon.HitConfirm2==0`。生产 runner不再改动。

## 5. 交接

继续前先读本 Record、Task Contract 和
`RESEARCH/R4-COL-01-hit-confirm2-attacker-abort-preflight-20260822.md`。不得将 C07-A 与 caught-cpoint、
effect21、kind1 target type 或伤害 writer 合并处理。

## 6. 实际验证结果（2026-08-22）

| 层级 | 命令 / 场景 | 实际结果 | 状态 |
|---|---|---|---|
| source/static | C++ `collision.cpp:57-65,1257,1263-1266` 与 Unity shared runner crosswalk。 | valid candidate的 vrest recheck 后仅对 current-DAT character target执行 `HitConfirm2` sequence break；runner无额外 CLR type gate。 | `PASS` |
| focused fixture | `CheckHitConfirm2AttackerAbortContracts`。 | exact/shared × abort/continue 四例已注册；full self-check PASS覆盖其断言。 | `PASS` |
| fixture correction | `CheckHeldKind5ConsumesFrozenCandidates`。 | holder collect-time `AttackExempt=1` 隔离其非目标 candidate；object consume 前 weapon carrier断言为0。 | `PASS` |
| Unity compile | UnityMCP `refresh_unity(force/scripts/compile)`。 | 03:44:14 +08:00 recovered/ready；后续无 `error CS`。 | `PASS` |
| full self-check | `NTSD/验证/运行战斗运行时自检`。 | `Temp/NTSD_BattleRuntimeSelfCheck.result=PASS`，最后写入 2026-08-22 03:44:57 +08:00。 | `PASS` |
| Play Mode | 真实 weapon/special attacker multi-target sequence。 | 未运行。 | `RUNTIME_PENDING` |
| C++ authority trace | R1-WP02。 | 不运行 C++ executable。 | `BLOCKED` |
