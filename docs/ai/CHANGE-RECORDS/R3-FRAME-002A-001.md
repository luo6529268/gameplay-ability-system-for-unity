# R3-FRAME-002A-001 — remove Unity-only ThrowFrameGuard reader gates

<!-- CHANGE-RECORD
id: R3-FRAME-002A-001
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Animation/LF2Objects/LF2Entity.cs
code-path: Assets/NTSD/Scripts/Simulation/Ecs/BattleEcsCharacterFrameTickPass.cs
code-path: Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\entity\frame_advance.cpp and complete release source field inventory for throw_frame_guard
evidence: SOURCE-CONTRACT-VERIFIED / UNITY-PREFLIGHT-VERIFIED / CURRENT-UNITY-COMPILE-PASS-2026-08-22-034014 / FULL-SELF-CHECK-PASS-2026-08-22-034457 / PLAYMODE-PENDING / CXX-RUNTIME-TRACE-BLOCKED
-->

> 创建日期：2026-08-22  
> 最后更新：2026-08-22  
> 类型：battle / frame-advance / frame-tick / hidden-extra-gate / test  
> 所属 Work Package：`R3-FRAME-02A`  
> 当前状态：`RUNTIME_PENDING` — shared fixture 两处编译修复已完成，当前 Unity compile和full self-check
> 已重验通过；仍缺 Play Mode 与 C++ runtime trace。

## 1. 目标与允许范围

只删除 Unity F03/F07 对 `ThrowFrameGuard == Frame.N` 的三处 reader。只允许：

- `LF2Entity.cs`；
- `BattleEcsCharacterFrameTickPass.cs`；
- `BattleRuntimeSelfCheck.cs` 的 focused fixture。

不得写 field、不得改 cleanup writer、不得改 `D-MOV-005` 或任何 held/weapon/scheduler/physics结构。

## 2. Authority / 差异依据

- **C++ VERIFIED**：release build source inventory有9个 `throw_frame_guard` occurrence，0个 conditional read，0个
  nonnegative writer；F03/F07 live path没有该 gate。
- **Unity VERIFIED**：`LF2Entity` F03/F07及 exact ECS F07均用 matching value early return；normal production
  writer不存在，nonnegative values仅来自当前 self-check。
- **C++ runtime trace BLOCKED**：R1-WP02未解除；本包绝不运行 C++ executable。

## 3. 实际修改 / 验收

1. 删除三处 reader，保留全部 `-1` cleanup/reset writer；属性注释同步为 C++ release source事实。
2. 新 fixture用 matching test-only value验证 exact/shared F03的 physics/tail int写入和F07的 counter继续执行；
   fixture不改变 field值，以证明 field cleanup仍归既有 writer所有。
3. 已运行 existing Unity Editor refresh/compile、full self-check、ledger validator和diff check。

## 4. 实际验证证据

| 层级 | 命令 / 场景 | 实际结果 | 状态 |
|---|---|---|---|
| source/static | C++ field inventory、F03/F07 source与Unity three-reader crosswalk。 | C++ reader=0、C++ nonnegative writer=0；Unity three readers删除。 | `PASS` |
| focused fixture | `CheckThrowFrameGuardDoesNotGateReleasePasses`。 | exact/shared F03 `XInt=13`，exact/shared F07 `AttackingCounter=1`；field值仍为0。 | `PASS` |
| Unity compile | UnityMCP `refresh_unity(force/scripts/compile)`。 | 03:14:53 +08:00 editor idle/ready。 | `PASS` |
| full self-check | `NTSD/验证/运行战斗运行时自检`。 | `Temp/NTSD_BattleRuntimeSelfCheck.result=PASS`，最后写入 2026-08-22 03:15:33 +08:00。 | `PASS` |
| ledger / diff | `Tools/Validate-ChangeLedger.ps1`、`git diff --check`。 | ledger PASS（14 / 12）；diff check exit 0，只有既有 line-ending warning。 | `PASS` |
| Play Mode | real held-throw / F03/F07 sequence。 | 本包未运行。 | `RUNTIME_PENDING` |
| C++ authority trace | R1-WP02。 | 不运行 C++ executable。 | `BLOCKED` |

## 4.1 后续编译修复（2026-08-22，进行中）

- 在执行 `R4-COL-001` 的 existing Unity Editor scripts refresh 后，Console 报出：
  - `BattleRuntimeSelfCheck.cs(24399,59) CS1061`；
  - `BattleRuntimeSelfCheck.cs(24425,63) CS1061`。
- 两处都在本 Record 新增的 `CheckThrowFrameGuardDoesNotGateReleasePasses` shared-DAT fixture：
  `SelfCheckCharacterDatShell` 没有 `CurrentFrameId` 属性；该 shell 的统一当前帧字段是
  `Frame.N`（其绑定 helper 同时写 `Frame.N` / `Runtime.Frame`）。
- 允许且仅计划将两处 test-only `shared*.CurrentFrameId` 改为 `shared*.Frame.N`。不改生产
  F03/F07 reader、field/reset writer、scheduler、held/link/CPoint或 R4 行为。
- 在该两行修复并重新运行 existing Unity Editor compile / full self-check 前，本 Record 不能继续表述为
  当前工作树的 compile PASS 或 `RUNTIME_PENDING`。

### 修复结果

- 两处 `SelfCheckCharacterDatShell.CurrentFrameId` 已改为 `Frame.N`；没有改动生产 F03/F07 reader或
  `ThrowFrameGuard` cleanup ownership。
- existing Unity Editor（MCP port 6401）在 03:40:14 +08:00 refresh/compile 后 ready，随后读取 Console
  `error CS` 为0。
- 完整 `BattleRuntimeSelfCheck` 于 03:44:57 +08:00 写入
  `Temp/NTSD_BattleRuntimeSelfCheck.result=PASS`。因此本 Record 恢复为 `RUNTIME_PENDING`；Play Mode / C++ trace仍未关闭。

## 5. 风险、停止与回滚

- 若一个已知 production path需要 nonnegative guard值才能维持 C++ source行为，停止并重开 source audit；
- 若删 reader要求改变 held release timing、frame-delay或 CPoint，停止并拆包；
- 回滚只涉及本 Record三条代码路径与关联 docs；提交 hash：未提交。

## 6. 交接

- 继续时先读本 Record、`TASKS/R3-FRAME-02A-throw-frame-guard-contract.md`、
  `RESEARCH/R3-FRAME-02-reachability-preflight-20260822.md` 和新 handoff。
- `D-MOV-004` 为 `RUNTIME_PENDING`；不能由 test-only nonnegative value证明 real held throw完整对齐。
- `D-MOV-005` 保持 current exact route `INFERRED` not reachable，不得以本次 PASS删除 fallback state2000规则。
- 按 `D-009` 接着进入 R4 的只读 source preflight；先选择一个独立 collision / hit差异建立 Task Contract，
  在此之前不改 R4脚本。
