# R7-AI-002B — current-frame `hit_j` optimized data contract

<!-- CHANGE-RECORD
id: R7-AI-002B
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Simulation/Ai/AiSensingSnapshot.cs
code-path: Assets/NTSD/Scripts/Simulation/Ecs/BattleAiUnifiedRowPublisher.cs
code-path: Assets/NTSD/Scripts/Simulation/Ecs/BattleFrameMotionStore.cs
code-path: Assets/NTSD/Scripts/Simulation/Ecs/BattleFrameMotionWriter.cs
code-path: Assets/NTSD/Scripts/Simulation/SimulationWorld.cs
code-path: Assets/NTSD/Scripts/Simulation/SimulationWorld.Registry.partial.cs
code-path: Assets/NTSD/Scripts/Simulation/SimulationWorld.AiSoaShadow.partial.cs
code-path: Assets/NTSD/Scripts/Simulation/SimulationWorld.AiDecisionShadow.partial.cs
code-path: Assets/NTSD/Scripts/Simulation/SimulationWorld.AiInput.partial.cs
code-path: Assets/NTSD/Scripts/Test/Editor/AiDecisionSoAShadowEditorTests.cs
code-path: Assets/NTSD/Scripts/Test/Editor/CharacterInputLiveSlotLoopEditorTests.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\input\input_handler.cpp::ai_frame_hit_j / ai_update_oid11_frame290_side_effect
evidence: SOURCE-CONTRACT-VERIFIED / DOTNET-COMPILE-0-ERROR / FRESH-UNITY-COMPILE-0-ERROR / FOCUSED-4-OF-4-PASS / AI-REGRESSION-212-OF-212-PASS / WARMED-0-B / FRESH-DOMAIN-FULL-SELF-CHECK-PASS / RUNTIME-PENDING
-->

> 创建日期：2026-08-22  
> 最后更新：2026-08-22  
> 类型：runtime data contract / AI input

## 1. 状态与范围

- 当前状态：`RUNTIME_PENDING`
- Work Package：`R7-AI-02B`
- 本记录只覆盖 current-frame DAT `hit_j` 在 fallback/SoA/unified sensing 中的采集、增量刷新、比较与测试。

## 2. Authority / 需求依据

C++ `input_handler.cpp:569-572` 以 `char_data->get_frame(obj.frame)` 读取 `hit_j`，frame/data 不可用时为 0。
`input_handler.cpp:575-583` 的 OID11 key side effect 是该字段的消费者，但不在本 Change 内实现。

## 3. Unity 原状

- `AiSensingSnapshot` 有 `Frame`、`State`，没有 `HitJ`；
- legacy/SoA/unified capture 与 publisher 不发布该值；
- CharacterInput 后的增量 row refresh 因而无法保证后续高 slot 看见前一 slot 的当前帧 `hit_j`；
- `D-INP-008` 已登记为 source-confirmed data-contract difference。

## 4. 计划改动

| 文件族 | 计划 | 目的 |
|---|---|---|
| snapshot / SoA | 新增、采集、复制 `HitJ` | 建立稳定列合同 |
| frame-motion canonical store | 在bind与Frame writer边界解析一次DAT并保存派生HitJ | UnifiedAuthority消费不回读Entity/DAT引用 |
| unified publisher | Frame writer同步pending HitJ并commit | 保持same-pass ascending visibility |
| unified validation | full/refresh/published comparison 纳入 `HitJ` | 防止 optimized silent stale value |
| AI accessor | 新增只读 `HitJ(entity)` | 为后续 helper 提供统一读取，不在本包消费 |
| Editor tests | initial/refresh/grow/publisher/0B | 固定功能与性能边界 |

## 5. 不可回退边界

- 不接入 OID11 helper，不写 key/combo，不消费 RNG；
- 不改变 CharacterInput pass、slot order、gate/profile 或 fallback policy；
- 不改变 runtime Frame 真值、DAT、资源、render、capacity policy；
- 不修改 C++ authority。

## 6. 验收标准

- frame/data 缺失时 HitJ=0；有效当前 frame 时严格等于该 DAT frame 的 `hit_j`；
- same-pass frame mutation 后，后续 consumer 看到更新后的 HitJ；
- snapshot grow 保留旧 slot HitJ；
- existing unified incremental/full parity 继续通过；
- warmed publisher/capture update 不新增 managed allocation；
- compile、focused tests、AI baseline、full self-check、validator 均通过。

## 7. 回滚

只回退本记录列出的 HitJ 列、publisher pending 字段、比较项、accessor 与 focused tests；不得回退其他历史/用户改动。

## 8. 实际改动与证据

脚本已写入，Unity正式验证进行中。首次4项focused job
`efa6fb79b23b47d7aabcc82e4b5ddeb4`为3 PASS / 1 FAIL：UnifiedAuthority initial test使用正式OID11，
注册流程命中catalog并使自建frame不是最终current data，expected HitJ=290 / actual=0。manual refresh、grow/copy与
publisher warmed 0 B均已通过。该失败判定为fixture identity collision；测试改用不命中catalog的5011后必须重跑，
不得删除本记录或把首次结果算作PASS。

第二次job `7e399383cd644e09a100344ae37e49e8`仍为3 PASS / 1 FAIL，且同一published HitJ仍为0；
因此“仅为catalog identity collision”的判断已被证伪。fixture现加入pass前/后的current logical frame DAT
直接断言，以区分DAT frame变化与unified capture/commit first difference；尚未取得第三次结果。

第三次exact job `c571ae07ffb04321a44121d3d434d63c`在新增直接断言处FAIL：pass前current frame
HitJ=290，pass后current logical frame DAT HitJ=0。由此确认published 0并非stale row，而是跟随了pass后的
current frame；固定期待290违反C++ `get_frame(obj.frame)`合同。集成断言已修正为published值必须等于pass后
current-frame DAT值；明确290→77刷新仍由manual sensing fixture裁决。

修正后的focused job `b441adc029be42d4b922f2f63abdf45a`为4/4 PASS；AI sensing/decision/unified/live-slot
回归job `448c9dd4efbd4890a1f0fd7c0092c6a8`为212/212 PASS。其后同domain full self-check于
23:34:04在既有`D-TEST-001 / R3-INP-01`静态污染断言失败；没有HitJ失败。必须fresh domain reload后重跑，
本次FAIL不作废也不算本包PASS。

在最终diff review中发现当前实现的UnifiedAuthority capture仍调用`LF2Entity.GetFrameDataById`。虽然功能与0 B
已通过，但不满足optimized consumer只读canonical SoA的目标。因此02B重新进入实现：`BattleFrameMotionStore`
将增加entity owner与派生HitJ列，在bind及Frame write边界解析DAT；AI projection、initial capture和incremental
publisher只读该列。上面已取得的PASS只能作为中间证据，必须在重构后全部重跑。

最终实现：

- `AiSensingSnapshot`新增`HitJ`列并纳入grow/copy；
- fallback/legacy capture在既有capture边界解析current-frame DAT；
- `BattleFrameMotionStore`持有generation-owned entity/runtime binding，在bind和`RuntimeFrameMotionField.Frame`
  writer边界解析一次`hit_j`并保存在canonical SoA；
- `BattleFrameMotionAiProjection`携带HitJ，UnifiedAuthority initial/full/incremental capture不再回读Entity/DAT；
- frame writer与现有Frame pending同点发布HitJ，保持ascending-slot same-pass visibility；
- unified full/refresh comparison、published validation、只读AI accessor纳入HitJ；未接任何OID helper。

最终验证：

| 层级 | 结果 |
|---|---|
| generated C# `dotnet build Assembly-CSharp.csproj --no-restore` | 0 error，43 existing warnings |
| fresh Unity compile | `Assembly-CSharp.dll` 23:41:26、Editor 23:41:27，Console error 0 |
| focused HitJ matrix | job `d0670d95986c41e7b115b8a77754d23b`，4/4 PASS |
| AI sensing/decision/unified/live-slot regression | job `a525edc1f1c64bfe854f3d3218bd1d4e`，212/212 PASS |
| managed allocation | publisher 512次warmed commit 0 B；existing warmed CharacterInput regression PASS |
| first same-domain full self-check | 23:34:04在既有`D-TEST-001 / R3-INP-01`污染点FAIL，已留档 |
| fresh-domain full self-check | `Temp/NTSD_BattleRuntimeSelfCheck.result` 23:43:36 `PASS` |

本Record的`RUNTIME_PENDING`表示数据合同已写且Unity自动证据通过；OID11消费者、真实DAT Play Mode、02F联合
dispatcher与可用C++ runtime trace尚未完成，不能宣称完整AI或战斗已对齐。
