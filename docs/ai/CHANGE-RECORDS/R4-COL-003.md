# R4-COL-003 — effect21 current-state whole-attacker abort

<!-- CHANGE-RECORD
id: R4-COL-003
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Animation/LF2Objects/BattleHitCandidateSequenceRunner.cs
code-path: Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\entity\collision.cpp:57-79,91-194,1253-1258; J:\QQFile\NTSD2.4\ntsd_release\src\entity\collision_collect.cpp:325-333
evidence: SOURCE-CONTRACT-VERIFIED / UNITY-PREFLIGHT-VERIFIED / CODE-WRITTEN / UNITY-COMPILE-PASS-20260822-0416+08 / FULL-SELF-CHECK-PASS-20260822-041617+08 / PLAYMODE-PENDING / CXX-RUNTIME-TRACE-BLOCKED
-->

> 创建日期：2026-08-22  
> 最后更新：2026-08-22  
> 类型：battle / collision / effect21 / candidate-consume / test  
> 所属 Work Package：`R4-COL-03`  
> 当前状态：`RUNTIME_PENDING` — C07-C shared runner gate与 exact/shared focused fixture通过 Unity compile与full self-check；C++ runtime trace与真实 effect21 Play Mode仍未关闭。

## 1. 目标与范围

只在 shared frozen-candidate runner补齐 C++ effect21/current target state18/19的 entire-attacker abort。
允许的代码路径仅为 runner与 self-check。不得把规则移至 collect filter或全局 runtime filter，也不得改
CPoint/held/scheduler/candidate collection。

## 2. Authority / 差异依据

- **C++ VERIFIED**：`collision.cpp:188-194`在 local kind5/4/9变换之后，对 transformed
  `kind==0 && effect==21`及 victim current frame state18/19执行 `goto next_attacker`；label范围见
  `1253-1258`。
- **C++ VERIFIED**：`collision_collect.cpp:325-333`只以 target previous state过滤 source kind0/effect21；
  它不是 consume-time current-state abort。
- **Unity VERIFIED**：runner在 runtime itr resolve后尚无本 gate；`Kind0EffectAllowed`仅有 collect-time
  previous-state filter。
- **C++ trace BLOCKED**：R1-WP02未解除；不运行C++ executable。

## 3. 计划最小改动

1. 在 runner ResolveRuntimeItr成功后读取 transformed `runtimeItr` 和 target current authored state；
2. 条件满足时返回现有 sequence break值，不写 target、vrest、disposition或consumer；
3. 增加 exact/shared state18/state19 abort、ordinary control与可行的 transformed route fixture；
4. 如实记录 compile/self-check与 transformation fixture可达性。

## 4. 实际代码写入（待验证）

- `BattleHitCandidateSequenceRunner.TryConsumeCandidate` 在 `ResolveRuntimeItrForPair(...)` 成功后、
  legacy observation/disposition/任何 writer之前，读取 transformed runtime itr与 `target.Frame.D.state`；
  `kind=0/effect=21`且 state18/19时返回既有 sequence-break值 `true`。
- `BattleRuntimeSelfCheck` 注册 `CheckEffect21CurrentStateAttackerAbortContracts`，覆盖 exact/shared
  source kind0的current state18、state19和ordinary control，以及 `WeaponCount>0` source kind4
  → runtime kind0/effect21的 placement fixture。
- 未修改 candidate collection、`Kind0EffectAllowed`、`RuntimeConsumeItrAllowed`、CPoint/held writer或scheduler。

## 5. 预期副作用与不可回退边界

- 正确副作用：effect21 target在 consume时已是18/19时，当前和后续候选都不消费；
- 不可改变：effect21 target previous state18/19仍由 collect过滤；C07-B仍只skip当前 candidate；
  OID300 redirect的后成功命中 abort保持独立；
- 不回退：CentralOnly/Texture2DArray、1.5视觉缩放、容量、30Hz、FrameInputSet、SoA/pool。

## 6. 验收与回滚

- 验收：Task Contract S0～S6；最高状态为 `RUNTIME_PENDING`；
- 回滚范围：仅 runner、self-check及关联 R4-COL-003文档；未提交；
- 发生 Scope/authority/fixture blocker时停止并更新本记录，不以旧C#或经验补写。

## 7. 实际验证

| 检查 | 实际结果 |
|---|---|
| C++ authority | 只读复核 `collision.cpp:57-79,91-194,1253-1258`、`collision_collect.cpp:325-333`和 `Makefile:10-31`；未运行、构建、修改或写入 authority。 |
| focused fixture | exact/shared source kind0以 current state18/19整 attacker abort、ordinary current state继续；exact/shared source kind4 / `WeaponCount=1`已确认 runtime kind0/effect21并同样 abort。 |
| Unity compile | 现有 Unity Editor / UnityMCP port 6401在 2026-08-22 04:16 +08:00 force scripts refresh/compile；domain reload期间的 TCP 关闭为预期，随后 Console `error CS` 查询为0。 |
| Full self-check | 通过菜单 `NTSD/验证/运行战斗运行时自检` 请求；`Temp/NTSD_BattleRuntimeSelfCheck.result=PASS`，最后写入 2026-08-22 04:16:17 +08:00。 |
| 留痕 / diff hygiene | 待所有 R4-COL-003状态、ledger与handoff更新后进行最终 validator与 `git diff --check`。 |

这些结果确认本包最小实现与现有 Unity regression兼容；不构成 C++ executable trace或真实 Play Mode验收。
