<!-- CHANGE-RECORD
id: NTSD28-Q07-KO-FEED-PRODUCTION-ROW-WITNESS-001
status: FOCUSED_TEST_PASS
change-kind: Q07_KO_FEED_PRODUCTION_ROW_WITNESS
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28UnityRawCaptureEditor.cs
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28Q07KnockoutFeedProductionRowEditorTests.cs
authority: formal root NTSD2.8-Logan EXE B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033 and paired playable KO writer/render snapshot; user-approved project mode Asset
evidence: docs/ai/TASKS/NTSD28-Q07-KO-FEED-PRODUCTION-ROW-WITNESS-001.md; artifacts/diagnostics/NTSD28-Q07-KO-FEED-ROW-CODE-BOUNDARY-001/REPORT.md
-->

# NTSD28-Q07-KO-FEED-PRODUCTION-ROW-WITNESS-001

Pre-change: the existing same-state 30-tick raw capture proves KO born at completed tick8/time7, and Q09's synthetic row tests prove strict/inclusive window logic, but no production Driver publication trace follows that exact KO to its row boundary. `WithLoganScenarioForReplayTests` already permits up to 120 ticks, but its content scope currently omits the project mode Asset, so a new opt-in flag is needed. Existing callers must keep their current default.

Declared edit: one optional parameter and scope forwarding in the existing Editor replay helper; one new focused Editor test and `.meta`. Only test/diagnostic code. Actual assertions, evidence, limitations and rollback are in the Task. No production/Asset/DAT/Scene/nonbattle change.

Post-change code written: the helper's `useProjectMode = false` optional parameter reaches its existing `UnityCurrentDatScope` flag. The new one-test class extends the unchanged Naruto scenario with neutral inputs to tick38, steps the complete Driver with presentation, persists event/row counts and times to CSV, and asserts the declared boundaries. No production/Asset/DAT/Scene change. Original Editor compile and focused run pending.

First original-Editor run: compile 0 errors, exact test discovered 1/1, job `f6010a5ef8654e41a6f5ca300bb37294` failed at tick6 with `NullReferenceException` in `LF2ObjectPool.Get` during late OPoint materialization. The helper's opt-in path omitted the existing formal raw-capture `BeginBattleAllocationSeal` before logic-only mode, so it attempted renderer-pool creation with no pool config. The test also passed `StepOneTick` positional flags as `ignorePaused=true, buildPresentation=false`, leaving published frame null. This is a fixture defect, not a proven production KO-row difference. The partial first-five-tick CSV, exact failure stack and test-job JSON are preserved under the package diagnostic directory. Correct the helper's opt-in ordering and use named `buildPresentation: true` in the declared test, then rerun only one test.

Second original-Editor run: after using the formal raw-capture seal order and named presentation flag, job `7241422e8a44460596dd996b5959e871` produced all 38 rows. The observed first KO is tick8/time7, native record count1 and row count1 through tick36, row count0 at ticks37/38; CSV SHA-256 `1C299F63CB2ECD84B63977E877D827AFE53C7F405A6641AB60D9728809A69AB1`. The test remains RED because the helper's post-disposal invariant saw 2 World objects and 2 slots, borrowers0. Do not accept this run as a clean production witness. Add only diagnostic shutdown status/stage/reason to the helper's existing failure message, then resolve the fixture lifecycle before reporting PASS; no production shutdown change without a separate first-difference package.

Diagnostic compile correction: the first added shutdown-report message referenced the helper's `using (var driverScope...)` variable after its lexical scope, producing three CS0103 Editor compile errors. The local scope declaration was moved immediately before the same `using (driverScope)` block; no runtime behavior changed. This compile failure is retained in the Record, and the corrected helper must be compiled before any rerun.

Third original-Editor run: corrected helper compiled 0 errors; job `1a0561ebcf824988a5276eb605be861f` still produced matching 38-tick row CSV but failed the same zero-residual gate. New diagnostic identifies `shutdown=Failed`, completed stage `PendingObjectPointTasksDiscarded`, reason `object-pool-runtime-state-is-invalid`. `LF2ObjectPool.HasValidRuntimeState` requires seven managed collections initialized by `Awake`, while an existing EditMode singleton may retain null fields across domain reload. The test-only isolated-pool pattern in `NTSD28B6WpointTerminalStructuralProductionEditorTests.RendererScope` creates/initializes a hidden fresh pool and restores the prior singleton; add equivalent opt-in-only scope inside the declared replay helper. This does not license changing production ordered shutdown or ignoring residue.

Final implementation and verification: the replay helper now creates an isolated hidden `LF2ObjectPool` only when `useProjectMode=true`, initializes it if EditMode left its managed runtime state invalid, and restores the prior singleton after Driver disposal. Existing default-false callers retain their previous behavior. Original Editor compiled after final source edits with zero compile errors; exact focused job `3a25c06fb233489f972a390006a92e70` passed 1/1. All 38 CSV rows show first KO tick8/time7, published row count1 at tick8 through tick36 and count0 at tick37/38 while native record count stays1. The helper's post-disposal zero World objects, slots and logic borrowers gate passed. Final CSV SHA-256 `1C299F63CB2ECD84B63977E877D827AFE53C7F405A6641AB60D9728809A69AB1`. Menu/Battle/GameConfig/project-mode Asset hashes stayed `785F828C...81E13` / `2EE465D8...8B77A` / `0527D737...CB8EA7` / `88E10D43...686F55C`. No production, DAT, Asset, Scene or nonbattle change. The exact formal GPU pixel and other Q07/Q09 exits remain open. Full evidence in the package diagnostic directory.

Governance and final Editor state: `Tools/Validate-ChangeLedger.ps1` exited 0 (`PASSED`, 857 Records, 16 governed code files in the current dirty diff); `git diff --check` exited 0. The original Editor was idle EditMode on the Battle Scene after the focused run, Console errors 0. The two new diagnostic scripts compiled into an Editor assembly timestamped later than their final source edits. See `change-ledger-validation.log` and `ACCEPTANCE.md`.
