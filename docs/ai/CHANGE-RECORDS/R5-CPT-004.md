# R5-CPT-004 — CPoint injury belongs only to current weapon-sync

<!-- CHANGE-RECORD
id: R5-CPT-004
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Simulation/Ecs/BattleCpointWriter.cs
code-path: Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs
authority: J:/QQFile/NTSD2.4/ntsd_release/src/entity/game_tick.cpp:659-664;src/entity/cpoint.cpp:23-190;src/entity/weapon.cpp:13-107
evidence: SOURCE-PHASE-OWNERSHIP-VERIFIED / UNITY-EARLY-DUPLICATE-OPPORTUNITY-VERIFIED / UNITY-VALIDATION-PENDING / CPP-TRACE-AND-PLAYMODE-PENDING
-->

> 创建日期：2026-08-22  
> 最后更新：2026-08-22  
> 类型：battle / test  
> 当前状态：`RUNTIME_PENDING` — 已完成唯一 owner transfer、same-PreInteraction fixture、Unity compile与full self-check；C++ trace与真实 Play Mode待验。

## Authority / 差异

C++ step10先运行 `cpoint.cpp`，其只负责 relation/decrease/action/throw/dircontrol；随后
`weapon.cpp` 的 current-frame state9 weapon-sync 才写 victim vaction、injury、position以及相关
attacking/frame-delay字段。Unity `RunKind1` 在 first pass提前调用 `SyncCaughtByCpoint`，later
`SyncHeldCpoint` 又可调用同一 writer。action可在两次之间清attacking，因此现有实现存在 double-injury /
early-injury 的静态可达路径。

## 允许范围

| 文件 | 符号 | 允许改动 |
|---|---|---|
| `BattleCpointWriter.cs` | `RunKind1` | 只移除 early `SyncCaughtByCpoint` call；不改其他CPoint分支。 |
| `BattleRuntimeSelfCheck.cs` | existing/new CPoint phase fixture | 将旧 early-injury expectation改为C++ phase ownership，并加入same-PreInteraction joint matrix。 |

## 明确排除

不改 global `KillStats` / `DamageStats`（D-CPT-002）、reciprocal mismatch early return（D-CPT-003）、
PreInteraction pass order、kind2 validation、throw、held/link、opoint、input、collision、render、DAT、
scene、资源、slot/generation、capacity、performance或 C++ authority。

## 实际改动

| 文件 | 符号 | 实际改动 | 预期副作用 |
|---|---|---|---|
| `BattleCpointWriter.cs` | `RunKind1` | 删除唯一 early `SyncCaughtByCpoint` call。 | prev-frame CPoint仍做C++ cpoint.cpp职责；不再提前写held injury/vaction/position。 |
| `BattleRuntimeSelfCheck.cs` | existing shared-DAT CPoint fixture | 将 `RunCpointCheckStep10` 后的旧 early-injury expectation改为无injury。 | 防止测试把Unity extra phase writer当作 authority。 |
| `BattleRuntimeSelfCheck.cs` | new phase ownership matrix | 以actual `PreInteractionTickAll` 覆盖no-action state9一次、action→state9一次、action→non-state9零次。 | 一次/零次伤害、frame、vaction、position、combo、attacking、frame-delay、links与stats边界均可回归。 |

未修改 `ApplyHeldInjury`、global stats、CPoint pass order或任何其它 production module。

### 首次 self-check failure 与合同内修正

2026-08-22 09:25:20 的首次 full self-check在 `CheckCpointDecreaseEscape` 失败。旧 fixture因Unity
early held-position sync而期望 `KnockbackVx=+4`；C++ `cpoint.cpp:69-75` 在该分支按尚未weapon-sync
的位置计算，fixture attacker X=30、victim X=10，故权威结果为 `-4`。本记录已只更新decrease escape、
escape-before-throw及postprocess velocity assertions为 `-4`；没有恢复 early sync。

## 验收和回滚

| 层级 | 实际操作 | 结果 |
|---|---|---|
| S0 source | 重读 Makefile、C++ step10 owner、Unity all callers与PreInteraction loops。 | PASS；only current `SyncHeldCpoint` remains as `SyncCaughtByCpoint` caller。 |
| S1 joint fixture | same-`PreInteractionTickAll` no-action state9、action→state9、action→non-state9 matrix。 | PASS；分别一次、一次、零次injury，且锁定frame/position/links/attacking/frame-delay。 |
| S2 governance | ledger validator与scoped diff。 | PASS；34个Change Record、26个governed code files均有覆盖；scoped diff exit 0，仅LF→CRLF提示。 |
| S3 Unity | 当前已打开 Unity scripts refresh、filtered `error CS`、full self-check。 | PASS；0条C# compiler error；`Temp/NTSD_BattleRuntimeSelfCheck.result`于2026-08-22 09:27:38为`PASS`。 |
| S4 C++ trace / Play Mode | R1-WP02 / later runtime acceptance。 | PENDING；不在本包关闭。 |

最高状态为`RUNTIME_PENDING`。回滚只恢复 `RunKind1` 的 one early call及本 Record 列出的fixture断言；
不得触碰其他用户或历史改动。
