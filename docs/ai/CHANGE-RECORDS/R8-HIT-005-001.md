# R8-HIT-005-001 — current-DAT-first target hit dispatch

<!-- CHANGE-RECORD
id: R8-HIT-005-001
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Simulation/Ecs/BattleDamageWriter.cs
code-path: Assets/NTSD/Scripts/Animation/LF2Objects/LF2CharacterInteractionResolver.cs
code-path: Assets/NTSD/Scripts/Animation/LF2Objects/LF2CharacterDatInteractionResolver.cs
code-path: Assets/NTSD/Scripts/Animation/LF2Objects/LF2WeaponBase.cs
code-path: Assets/NTSD/Scripts/Animation/LF2Objects/LF2SpecialAttack.cs
code-path: Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\entity\collision.cpp and hit.cpp current char_data obj_type dispatch
evidence: SOURCE-CONFIRMED-DIFFERENCE / UNITY-FOUR-DISPATCHER-CROSSWALK-VERIFIED / FRESH-COMPILE-0 / FULL-SELF-CHECK-PASS / FOCUSED-178-PASS / PRODUCTION-PLAY-UNAVAILABLE / CPP-TRACE-BLOCKED
-->

> 创建日期：2026-08-23  
> 所属Work Package：`R8-WP01G-R02`  
> 当前状态：`RUNTIME_PENDING`

## Authority / requirement

C++统一Entity按current `char_data->obj_type`裁决target hit。Unity多个consumer仍依赖CLR weapon/special壳，
在shell/current-DAT mismatch时会调用错误writer或不处理。用户授权处理`D-HIT-005`。

## Unity before change

- shared Character-DAT与SpecialAttack attacker：type0之后按CLR weapon/special优先；
- Character/Weapon attacker：先看current type，但仍要求matching CLR壳；
- weapon/type3 `BattleDamageWriter` helper主体只使用generic entity字段，却以CLR victim类型签名；
- current type5 mismatch没有统一common kind0 hurt入口。

## Planned files / symbols

见Task合同Files；只修改target dispatch ownership、generic victim签名和focused fixtures。

## Expected side effects

- 同一target current DAT在四类attacker入口选择相同writer；
- shell/current-DAT一致的既有行为保持；
- mismatch目标不再被CLR壳抢占或静默丢弃；
- candidate order、RNG调用位置、vrest与existing type-specific字段顺序保持。

## Protected boundaries

不改C++、DAT、pass order、collector、render、pool、capacity、1.5×、T8、IL2CPP、服务器或F1/F2 debug。

## Acceptance

- mismatch matrix与existing D-HIT regression；
- fresh compile0、full self-check、related focused tests；
- validator/diff PASS；
- production Play/C++ trace未运行时如实保留。

## Rollback

只回退本Record登记的dispatcher/helper signature/fixtures与同一Change文档，不影响其他用户修改。

## Actual changes / verification

- `BattleDamageWriter`新增current-DAT-first dispatcher；matching CLR target复用既有exact `Hit`，mismatch
  weapon/type3/type5 target走generic typed writer；
- weapon/type3 helper victim签名泛化为`LF2Entity`，type5 kind0跳过type3 tail；
- Character、shared Character-DAT、Weapon、SpecialAttack attacker consumer统一调用dispatcher；
- self-check新增四attacker×type3 mismatch及reverse weapon/type5矩阵；
- scoped diff-check exit0，仅既有LF→CRLF warning；
- fresh compile通过后首次full self-check在`BATTLE-AUDIT4-04`失败：fixture文字要求type3 burning，实际
  `SelfCheckCharacterDatShell`却固定current type0；旧CLR-priority使该矛盾被掩盖；
- test-only fixture现改用既有`CurrentDatSelfCheckSpecialAttack(SpecialAttack)`，不回退production dispatcher；
- 第二次self-check仍在同一断言失败；C++ `collision.cpp:881-909`确认type3 tail的effect2 burning分支只有
  current `char_data->obj_type==0`时执行，普通current type3必须保留frame20/motion reset；
- stale断言已改为真实type3预期：frame20、HitConfirm2=1、attacking与三轴motion清零；
- 修正后的fresh compile为0 error；16:40:26左右生成的新runtime assembly已由Unity完整刷新，Console error为0；
- 16:40:37第三次full `BattleRuntimeSelfCheck`为PASS；
- focused EditMode job `9411895645354ca4a241d2a84d8525a5`为178/178 PASS，覆盖hit execution plan、
  type3/type5 writer effect与warmed zero-allocation矩阵；
- current production DAT中没有可独立触发全部shell/current-DAT mismatch组合的现成场景，且C++ full trace仍按
  `B-R1-WP02-*`阻塞，因此保持`RUNTIME_PENDING`，不升级为runtime `VERIFIED`。
