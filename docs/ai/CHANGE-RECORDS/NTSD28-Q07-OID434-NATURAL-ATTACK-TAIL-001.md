<!-- CHANGE-RECORD
id: NTSD28-Q07-OID434-NATURAL-ATTACK-TAIL-001
status: VERIFIED
change-kind: EDITOR_DIAGNOSTIC_ONLY
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28Q07Oid434NaturalBindingProbeEditor.cs
authority: NTSD 2.8-Logan formal ras.dat frame100 spawns OID434/action396, existing formal and Unity controlled raw replay
evidence: original Editor second physical attack-tail Play PASS; OID434/action396 resolves formal ras.png pic36, scoped entity binding only
-->

# NTSD28-Q07-OID434-NATURAL-ATTACK-TAIL-001

Pre-edit state: the observer ends Play as soon as the original natural after254 probe writes its result. That branch reaches OID434/action35 but not action396. Existing controlled raw rows show action25/action100/action396 after the attack tail, so a new opt-in follow-up physical J is needed to test the production input/World path. The script may only add this diagnostic mode, queue device events, advance controlled tick and restore pause. It cannot change original natural probe or production rules. Do not turn a negative result into a graphics defect; compare exact live entity field and source binding when reachable. Validation and rollback are in the Task Contract.

First implementation/result: the diagnostic extension imported and generated Editor C# built with 0 errors/166 warnings after fixing its missing `NTSD.Game` import and outer catch scope. Original Editor first Play wrote `oid434-attack-tail-20260925-1.json` as FAIL, but the live evidence reached physical J in tick53, actor ordinary action20, OID434/action100 and a second OID434/action396 tick54 with rendered pic36, `(434,36)` resolved, formal `ras.png` source, 48×48 and valid central binding. Only the diagnostic predicate is too narrow (`action25` required). Preserve the result; next edit accepts the recorded ordinary action20/25 alternative and uses a new request path/output.

Post-edit/validation: only the opt-in Editor diagnostic was changed; no production code, content value or Scene modified. The predicate now accepts the recorded ordinary attack action20/25 alternative while retaining natural PASS, no-gap, physical frame-input, OID434/action100→396, pic36, key, formal sheet, 48×48 and central-binding gates. Unique second request `oid434-attack-tail-20260925-2` produced PASS in the original Editor, with actual fields and both result hashes in `artifacts/diagnostics/NTSD28-Q07-OID434-NATURAL-ATTACK-TAIL-001/ACCEPTANCE-20260925.md`. Final generated Editor project build 0 errors/166 warnings; Editor idle/Edit, Console error0, Battle/Menu/GameConfig SHA unchanged. Renderer command, pixels, formal EXE same natural input and aggregate Q07/Q09 remain unverified. Rollback limited to the diagnostic extension, respecting protected-file rules.
