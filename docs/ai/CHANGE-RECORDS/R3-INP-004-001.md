# R3-INP-004-001 — P1/P2 roster routing fixture

<!-- CHANGE-RECORD
id: R3-INP-004-001
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\core\main.cpp release live P1/P2 path
evidence: SOURCE-CONTRACT-VERIFIED / STATIC-PASS / UNITY-COMPILE-PASS / FOCUSED-SELF-CHECK-PASS / RUNTIME-PENDING
-->

> 创建日期：2026-08-22  
> 状态：`RUNTIME_PENDING`  
> 所属 Work Package：`R3-INP-04`

## Scope

只增加一个 `BattleRuntimeSelfCheck` fixture，固定 P1/runtime slot0、P2/runtime slot1、roster player
slot0/1，并验证同 tick frame input routing、cooldown与stable binding。允许脚本文件仅
`Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs`。

不得改 roster capacity、production input routing、AppManager spawn、pool/slot allocation、physical input
asset、AI/worker/lockstep protocol或任何 C++ 文件。fixture失败时停止本 Record并新建 implementation
Record，不得把8-slot扩展回退为2。

## Authority / planned verification

- C++ `main.cpp:2379-2380` resolves P1/P2 through object slots 0/1；`4607-4608` polls them in order；
- Unity roster/player mapping source已在 Task Contract列明；
- 计划：static + ledger/diff + UnityMCP compile/error CS + full self-check；Play Mode / C++ trace 保持未关闭。

## 实际改动（编译前）

- `BattleRuntimeSelfCheck.RunAllChecksStatic` 现调用 `CheckP1P2RosterInputRouting`；
- fixture 固定 P1 / runtime slot0 / roster player slot0 和 P2 / runtime slot1 / roster player slot1；
- 同 tick input 为 P1=`Right`、P2=`Jump`，断言各自 key/cooldown/history 和 roster runtime slot/stable id
  不发生串写；
- 未修改任何 production roster、capacity、input provider或physical binding。

## 实际验证结果

| 层级 | 命令 / 输入 | 结果 | 状态 |
|---|---|---|---|
| source/static | C++ P1/P2 object-slot + poll order、Unity roster routing guard | PASS；source slot0/1 与 fixture mapping闭合。 | `PASS` |
| ledger / whitespace | `Tools/Validate-ChangeLedger.ps1`、`git diff --check` | PASS；9 records / 10 governed files，diff check exit 0。 | `PASS` |
| Unity compile | existing Editor UnityMCP force scripts compile | 成功；filtered `error CS`=0。 | `PASS` |
| focused / full self-check | `NTSD/验证/运行战斗运行时自检` | P1 right / P2 jump no-cross fixture和完整 self-check PASS；result 01:28:23 +08:00。 | `PASS` |
| Play Mode / physical binding | InputAction asset / two physical maps | 未运行；移交 R3-PHY-01。 | `OUT_OF_SCOPE` |
| C++ executable trace | R1-WP02 | 未获得，不运行 executable。 | `BLOCKED` |

## 未关闭项与交接

- 本 Record 不改变 Unity 8-slot extension；C++ two-human authority fixture通过不等于3+ player已获C++认证；
- AppManager production spawn与real InputAction binding都未运行；
- 回滚仅需移除本 Record 新增 self-check invocation/method，不得影响其它已有 self-check diff；
- 下一包按 D-011 进入 R3-AI-TGT-01，而不是改物理资产或 capacity。
