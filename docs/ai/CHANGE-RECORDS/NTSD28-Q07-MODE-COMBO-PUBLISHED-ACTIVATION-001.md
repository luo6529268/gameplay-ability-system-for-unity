<!-- CHANGE-RECORD
id: NTSD28-Q07-MODE-COMBO-PUBLISHED-ACTIVATION-001
status: COMPILE_PASS
change-kind: CODE
code-path: Assets/NTSD/Scripts/Animation/LoganObjectCatalog.cs
code-path: Assets/NTSD/Scripts/Animation/LoganContentIdentity.cs
code-path: Assets/NTSD/Scripts/Animation/LoganVisualContentCandidate.cs
code-path: Assets/NTSD/Scripts/Simulation/Host/SimulationTickDriver.cs
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28Q07ModeComboPublishedActivationEditorTests.cs
authority: formal Logan playable game_session.cpp selected mode combo load and World options, plus R15 identity trigger
evidence: artifacts/diagnostics/NTSD28-Q07-MODE-COMBO-R15-IDENTITY-IMPACT-001/REPORT.md
-->

# NTSD28-Q07-MODE-COMBO-PUBLISHED-ACTIVATION-001

Created **before script modification**. Unity currently has `LoganModeComboInput` as a pure, unpublished projection; production object/fusion content identity and visual candidate freshness omit mode, and a new World resets its native combo carrier to `false/0/1/50/0`. Formal playable selects the first mode child and, when its native combo config is complete, supplies present/`1/1/50/1` to World options. `AppManager` uses `ApplyMatchConfig`; direct `BattleTestBootstrap` does not, so the common pre-tick seal is the shared activation boundary. The Task Contract lists exact paths, authority, behavior, forbidden scope, risk, acceptance and rollback.

Expected change: catalog owns captured mode input, joint battle identity is versioned when mode is present, candidate freshness rejects mode changes, and the seal applies the published tuple to the existing World only after reset/before first tick. No new queue, worker, renderer, pool or scene owner; existing eleven-stage ordered shutdown stays unchanged. Old absent-mode identity and legacy Battle Scene behavior remain, as do Q06 combo producers. New mode identity intentionally invalidates old formal-content session/cache keys; the trace comparator's native V1/full Unity V2 mismatch remains explicitly pending R15 rather than being misreported as parity. Nonbattle flows and approved visual exceptions are untouched.

Actual code: `LoganObjectCatalog` now captures `ModeComboInput`; `LoganContentIdentity.FromBattleComponents` uses a five-component `NTSD28_LOGAN_BATTLE_INPUTS_V2` digest when mode is present and preserves the existing three-component V1 digest when absent; `LoganVisualContentCandidate.AssertInputsCurrent` rejects changed mode bytes and re-read identity changes. `SimulationTickDriver.BeginBattleAllocationSeal` applies the **published** tuple after catalog identity validation and before worker/tick. `ApplyMatchConfig` clears the configured-World marker after reset; successful snapshot restore marks that World configured, and shutdown clears its reference. A new focused Editor test and `.meta` cover vectors, formal/staged capture, absent/present/raw freshness and a proposed public seal/reset/tick-zero-restore sequence. Root adjusted that test to use the existing atomic-publication image fixture rather than an image-free candidate for its real publication path. No Scene, Prefab, menu, result, Q06 producer, old resource or third-party file changed.

Validation so far: worker's in-memory `Add-Type` compile of the actual `LoganContentIdentity.cs` with a session stub reproduced V1/V2 independent byte vectors; `git diff --check -- Assets/NTSD/Scripts` passed. `Tools/Validate-ChangeLedger.ps1` passed with 681 Records and six governed code files, including the already-existing untracked pure parser. These are **external/static proxies**, not Unity compilation. The original Editor assembly timestamp still predates the new parser and integration scripts; the new five Editor assertions, public seal sequence, ordinary/caughtact gates, three-owner transaction, original Battle Scene/rematch and same seed/input formal trace have not been run for this change. Independent read-only review found the original private-helper-only test inadequate; it has been supplemented with a public seal-path test, but that supplement also remains uncompiled/unrun. Existing formal-root fixed V1 trace assertions and probes will require a separate R15 version-aware migration, and must not be reported green under the new identity.

Status remains `CODE_WRITTEN / UNITY_COMPILE_PENDING / RUNTIME_PENDING`, not VERIFIED. Review/rollback boundary: inspect and reverse only this Record's four production diffs and new test/meta if necessary; preserve other dirty files and earlier staged formal inputs. No blanket Git reset/clean, no second Unity project, no computer-use.

Focused external validation update: `artifacts/diagnostics/NTSD28-Q07-MODE-COMBO-PUBLISHED-ACTIVATION-001/pure-check.ps1` uses `Add-Type -Path` on ten **actual** pure C# source files (including catalog/identity/mode, fusion parser, tokenizer and session identity), with no stub or Unity copy. `pure-check.log` records 15 passing assertions: V1 vector, independent V2 preimage, formal/staged catalog identity, formal tuple, absent-mode V1, appearance/removal, and same-tuple parent/child byte-change cache invalidation/freshness rejection. Formal/staged complete raw SHA is `33B0341C58D2A708B208F2CA4A0C4DA740E942D49A8A9D82E106F1D1D9C419BE`; semantic SHA is `FF1218FF3FEB409FF6B2F8EDB1090591612B3D82D7FA91601E596D29CDF13DFB`. The original Editor assemblies were last written at 12:23:43/44 local, before the new parser and test; their binary type-name check also lacks these types. This proves the current disk assemblies cannot validate this package. Root made the public-seal test fixture self-contained by generating its own tiny PNG bytes, since the older publication test's shared `Temp/NTSD28PngAlpha/fixture.dat` is absent in this checkout. This new test body has not been recompiled or run.

2026-09-22 current-state correction: original Editor PID 33236 regenerated runtime/Editor assemblies at 07:58:15/19Z after all five declared code paths' latest modifications and logged `Mono: successfully reloaded assembly`; the earlier “compile pending / current disk assemblies cannot validate” sentence above is a historical snapshot. Advance this Record to `COMPILE_PASS` for original-project Unity compilation only. The focused NUnit, public seal/reset/tick-zero restore, actual battle entry and R15 same-seed trace are still unrun/unverified; do not infer `VERIFIED`.
