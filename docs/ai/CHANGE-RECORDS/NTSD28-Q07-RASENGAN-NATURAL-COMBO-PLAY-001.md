<!-- CHANGE-RECORD
id: NTSD28-Q07-RASENGAN-NATURAL-COMBO-PLAY-001
status: VERIFIED
change-kind: Q07_NARUTO_RASENGAN_NATURAL_INPUT_PLAY_DIAGNOSTIC
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28UserRasenganPhysicalPlayProbeEditor.cs
authority: root formal NTSD2.8-Logan EXE B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033 and playable input/frame chain
evidence: original Editor zero-error compile and three scoped Play case outputs; artifacts/diagnostics/NTSD28-Q07-RASENGAN-NATURAL-COMBO-PLAY-001/ACCEPTANCE-20260925.md
-->

# NTSD28-Q07-RASENGAN-NATURAL-COMBO-PLAY-001

Before edit: current `NTSD28UserRasenganPhysicalPlayProbeEditor.RunProbe` directly calls `ImmediateFrame(241)` and rewrites frame history. Its two existing synthetic physical-J cases prove action253 first/second observed tick reaches 301, but cannot witness the player's preceding defense+forward+jump combo. The new diagnostic will add natural-start modes while preserving the old two menu entries. Exact input/trace/report contract and scope are in the same-ID Task. No production code, DAT, image, Scene, Prefab, ProjectSettings or nonbattle file is authorized for this package. On implementation, record exact symbols and evidence; on verification, distinguish Editor compile, targeted Play and formal EXE/pixel evidence. Risk is synthetic key scheduling across 2tu input sampling; explicit canonical combo state and tick-gap fields must prevent false PASS. Rollback is the exact test script diff only, subject to repo protection rules.

After edit: only the declared Editor test script changed. `RunNaturalFirst253FromMenu`, `RunNaturalSecond253FromMenu`, `RunNaturalAfter254FromMenu` and nested `NaturalProbe` drive the existing InputSystem keyboard path, pause LocalFreeRun and call its ordinary local-provider `StepOneTick` once per Editor update, record contiguous tick/input-phase/proxy/action evidence, then release device keys and restore pause state. No production input rule, DAT, Scene, Prefab or resource was modified. First attempt missed a legal double tick between Editor callbacks; a later pre-bootstrap attempt had no actor; both failures are preserved. The first 253 case converted at tick658, post254 did not convert, and the natural second253 case landed at action254/phase1 then sampled at phase0/tick1128 without conversion. This second case is consistent with the formal playable 2tu sampler and corrects the Task's old unconditional-conversion expectation. Original Editor imported final script with 0 C# error, three scoped outputs are PASS on the input-stepping implementation (first/post254 before the final phase-only reporting edit; second afterward), and the Editor was returned to Edit mode. Scene/Menu/GameConfig hashes stayed unchanged. Exact result files/hashes, observed ticks and formal-source limit are in `artifacts/diagnostics/NTSD28-Q07-RASENGAN-NATURAL-COMBO-PLAY-001/ACCEPTANCE-20260925.md`. `VERIFIED` closes only this diagnostic package; Q07/R18 and independent formal EXE natural/pixel proof remain open. Rollback remains the exact Editor test-script diff only, after applicable repo approval.
