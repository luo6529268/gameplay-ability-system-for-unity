# R4-HIT-004 — normal weapon tail ownership timing

<!-- CHANGE-RECORD
id: R4-HIT-004
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Simulation/Ecs/BattleDamageWriter.cs
code-path: Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\entity\collision.cpp:559-632
evidence: SOURCE-CONTRACT-VERIFIED / UNITY-STATIC-READ-AUDIT / UNITY-COMPILE-PASS / FULL-SELFCHECK-PASS / RUNTIME-PENDING
-->

> 创建日期：2026-08-22  
> 当前状态：`RUNTIME_PENDING` — normal weapon timing修复与focused fixture通过Unity compile/full self-check；C++ trace与Play Mode未关闭。

## 差异与目标

C++ normal weapon victim 在 `apply_hurt` / `apply_hurt_reaction` 返回之后，按DAT type的weapon tail才写
`hit_confirm2`与`unk_364`。Unity common writer提前写 `HitConfirm2` / `RelationTeam`，随后现有tail再次写入。
本Record仅让normal type `1/2/4/6` 的第一次写入落回现有 tail；非weapon current-DAT shell fallback不触碰。

## 允许的代码路径

| 文件 | 符号 | 允许改动 |
|---|---|---|
| `Assets/NTSD/Scripts/Simulation/Ecs/BattleDamageWriter.cs` | `ApplyWeaponDamage` | 移除 normal weapon 的early duplicate write，保留现有tail作为唯一writer。 |
| `Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs` | new focused R4-HIT-04 fixture | 验证真实weapon-hit四类型的最终字段和非目标字段稳定。 |

## 禁止扩大

不得修改 current-DAT shell dispatch、`LF2CharacterDatInteractionResolver`、`LF2WeaponBase`、generic helper、raw frame、
vital/stat、durability、candidate、CPoint、held/link、scheduler/input/AI/render、DAT/scene/resource，或C++ authority。

## 预写入依据

- C++ authority：`collision.cpp:559-632`；Makefile参与性已核验；
- Unity read audit：`ApplyWeaponDamage:198-324`、`ApplyKind0WeaponVictimTail:1031-1086`；
- middle helper audit：`LF2CharacterDatHitResolver:121-310`不读取这两字段；
- source preflight：`RESEARCH/R4-HIT-04-weapon-tail-ownership-timing-preflight-20260822.md`；
- Task Contract：`TASKS/R4-HIT-04-weapon-tail-ownership-timing-contract.md`。

## Required verification after code

Change Record, ledger, STATE, full diff register, plan and handoff must be updated; then run ledger validator, diff check,
Unity compile and full self-check. `RUNTIME_PENDING` is the highest allowed outcome because C++ trace is blocked and no targeted
Play Mode run is in scope.

## 实际写入

- 删除 `ApplyWeaponDamage` normal `damageableWeapon` 分支的early `HitConfirm2=1`；
- 将early relation write收窄为`!damageableWeapon` fallback，正常type1/2/4/6继续由既有
  `ApplyKind0WeaponVictimTail`写入；
- 新增 `CheckWeaponTailIdentityTimingContract`：four real weapon hit分支锁定tail final confirm/relation、
  raw-frame mirror、PN、attacking、wait、raw durability和RNG；
- 未改current-DAT dispatch或任何scope外模块。

## 首次验证失败（保留）

- UnityMCP scripts refresh后`error CS`=0；
- 2026-08-22 07:06:16 +08:00 full `BattleRuntimeSelfCheck` 为`FAIL`；
- first failure：`R4-HIT-004 type2-ground`，实际`HitConfirm2=0`、`RelationTeam=23`、`Frame=0/0`、
  `WeaponFlightCounter=100`、RNG=2；说明该fixture未进入预期的normal weapon tail；
- root cause已静态闭合为**fixture OID collision**：新fixture把type2 victim写成oid998，而
  `Assets/NTSD/Config/data.txt:141`已定义`id: 998 type: 5`。`ResolveCurrentDataObjectType`因此取catalog type5，
  使fixture故意走non-damageable fallback；这不是normal type2 production writer的first difference；
- 最小修复只将new fixture OID改为已由02C使用且未在`data.txt`定义的`990 + weaponType`；production writer不再改动。
  修复后必须重新refresh/compile/full self-check，且本次first failure继续保留。

## 最终本地验证

- UnityMCP scripts refresh：`error CS`=0；
- full `BattleRuntimeSelfCheck`：`PASS`，`Temp/NTSD_BattleRuntimeSelfCheck.result`最后写入为
  `2026-08-22 07:10:20 +08:00`；
- console error级别日志只剩两个既有rest-binding negative-control，不含C# compiler或R4-HIT-004 failure；
- `CheckWeaponTailIdentityTimingContract`四分支通过；C++ trace / target Play Mode仍为PENDING。
