# R8-MOV-005-001 — exact character state2000 facing

<!-- CHANGE-RECORD
id: R8-MOV-005-001
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Simulation/Ecs/BattleEcsCharacterFrameTickPass.cs
code-path: Assets/NTSD/Scripts/Test/Editor/FrameAdvanceRuntimeSnapshotEditorTests.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\entity\frame_advance.cpp:884-887
evidence: SOURCE-CONTRACT-VERIFIED / UNITY-EXACT-GAP-VERIFIED / COMPILE-PASS-2026-08-23-161245 / FOCUSED-1-OF-1-PASS-E5E283E740CC49E597C99B7EF994C419 / FULL-SELF-CHECK-PASS-2026-08-23-161446 / PLAY-PENDING / CXX-TRACE-BLOCKED
-->

> 创建日期：2026-08-23  
> 所属Work Package：`R8-WP01G-R02`  
> 当前状态：`RUNTIME_PENDING`

## Authority / requirement

C++ Release live `frame_tick`在state2000无条件按最终Vx写facing；Unity fallback已有同一逻辑，exact
character ECS缺失。用户明确授权处理`D-MOV-005`，因此重开旧的asset-reachability watch并补齐通用规则。

## Unity before change

- current type0 exact character由`BattleEcsCharacterFrameTickPass`接管；
- exact pass在lying处理后直接进入wait/transition，没有state2000 facing writer；
- type2/type4 fallback通过`LF2Entity.RunCommonFrameTick`正确写朝向。

## Planned files / symbols

- `BattleEcsCharacterFrameTickPass.ExecuteExactCharacter`；
- `FrameAdvanceRuntimeSnapshotEditorTests`中的frame-tick focused matrix。

## Expected side effects

- 仅当exact character current frame state为2000时，逻辑Dir/managed presentation direction按Vx更新；
- Vx>0为right，Vx<=0为left；
- counter、transition、frame、position、velocity和fallback不变。

## Protected boundaries

不改C++、DAT、角色/OID特判、frame/physics/input/render/pass order、容量、pool或F1/F2 debug。

## Acceptance

- source/static crosswalk闭合；
- exact Vx正/零/负矩阵；
- compile0、full self-check、ledger validator、diff check；
- Play/C++ trace若未运行必须如实保留。

## Rollback

仅回退本Record登记的exact branch、test fixture和同一Change文档；不得影响工作树其他用户修改。

## Actual changes / verification

- `BattleEcsCharacterFrameTickPass.ExecuteExactCharacter`在lying处理后、wait/transition前，按C++对应时点
  对state2000执行`Vx > 0 ? right : left`；
- `FrameAdvanceRuntimeSnapshotEditorTests`新增exact type0正/零/负Vx矩阵，并断言3个实体都由exact pass处理；
- scoped `git diff --check` exit0，仅有既有LF→CRLF warning；
- UnityMCP force scripts refresh后`Assembly-CSharp.dll`/Editor分别更新至16:12:45/46，Console无`error CS`；
- focused job `e5e283e740cc49e597c99b7ef994c419`为1/1 PASS；
- full self-check于16:14:46写入`PASS`；
- Change Ledger validator为74 records / 74 governed files PASS；
- 正式type0 state2000 Play不可达，C++ trace继续BLOCKED，故保持`RUNTIME_PENDING`。
