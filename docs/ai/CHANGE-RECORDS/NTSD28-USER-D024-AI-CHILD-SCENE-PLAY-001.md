<!-- CHANGE-RECORD
id: NTSD28-USER-D024-AI-CHILD-SCENE-PLAY-001
status: RUNTIME_PENDING
change-kind: D024_AI_CHILD_SCENE_PLAY_PROBE
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28D024AiChildScenePlayProbeEditor.cs
authority: formal root EXE B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033 and playable AI/late OPoint; user D-024
evidence: docs/ai/TASKS/NTSD28-USER-D024-AI-CHILD-SCENE-PLAY-001.md
-->

# NTSD28-USER-D024-AI-CHILD-SCENE-PLAY-001

Created before script edits. The new probe is diagnostic only: it must not write back to Scene, production logic, DAT, content or project settings. Record actual Play result, source/physical child fields, cleanup and Scene hash after implementation. Isolated Editor AI tick4 fails at missing Renderer pool and does not establish a production defect. Existing direct pooled birth Play proof is adjacent but not this whole AI chain.

Actual script: `NTSD28D024AiChildScenePlayProbeEditor` adds a request-driven Editor Play entry. Preflight refuses an unsaved or non-Menu active Scene before calling `EditorApplication.EnterPlaymode`; the Play path uses current serialized formal GameConfig, menu prewarm, `AppManager.LoadBattleAdditive`, production human OID2/AI OID7 participant birth, up to 60 full Driver manual ticks, source-carrier and pooled child observations, and ordered battle unload before exiting Play. It writes only a diagnostic JSON result and Temp request state. The script was imported and compiled by the original running Editor; no new `error CS` appeared in the current Editor log.

First exact request `d024-ai-child-play-1` at local 13:38:16 produced `FAIL / preflight` with `Play probe requires a clean, saved NTSD_Menu scene.` The request was consumed (`requested:false`), and the Editor never entered Play. This is a protection of concurrent user/other-task Menu edits, not a gameplay failure. Before and after request, Menu disk SHA-256 was `14E9335F42ADDAF308046388FDAD7B9F0E7372919334A7F66D421F68F392B196`; Battle disk SHA-256 was `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`. Concurrent Menu/HUDBG/UIPanels changes were not touched.

Read-only source-chain confirmation while Play was deferred: `LF2ObjectPointFactory.ProcessOneLateOpoint` computes source X/Z from the spawner's source integer position when initialized and carries it on `OPointCreateTask`; `LF2ObjectRenderer.SetLogicObject` calls entity `Init`, whose character/special/weapon/other implementations apply `LF2Entity.ApplyInitialRuntimePosition` to initialize child source and physical coordinates. Existing direct pooled Play case `NTSD28Q06OpointMaterializerEditorTests.VerifySourceCoordinateRendererBirthForPlay` exercises that shared path but not the AI full-tick caller. Natural AI child birth remains `RUNTIME_PENDING` until the Menu Scene is saved and the fresh request actually enters Play.

Authority cross-check: current formal EXE SHA-256 still equals the declared `B1E13AE...9033`. Playable `build.ps1 -Target playable` includes `ntsd28_core/src/simulation/object_spawning.cpp`, whose `ObjectSpawnPlanner28::plan_frame` uses integer parent X/Z plus facing-local X and `opoint.z + 1`. Unity's late task uses source integer parent X/Z with the same local offset; `BattleNativeOpointBirthWriter.InitializeBirth` also applies kind1 random raw deltas to source integers while its physical outlet applies the approved view factors. These are static ownership/field-order findings, not a new formal EXE runtime trace or AI child Play acceptance.

Validation: `Tools/Validate-ChangeLedger.ps1` PASSED with 773 Records/22 governed code files; `git -c core.safecrlf=false diff --check` exit0. No production script, DAT, Scene, Prefab, ProjectSettings or nonbattle file was edited by this package. Rollback remains the new probe script/meta and this diagnostic package only, after provenance review.

2026-09-24 next original-Editor attempt: Menu disk SHA changed externally to `3B0F58AA88BEC495AA999D014CB2779E935B21F0374826357B4DC64AE5B80228` at 13:53 local. Fresh request `d024-ai-child-play-2` passed the clean-scene preflight, was consumed, and Editor entered Play. The Editor later returned to idle Menu, but no `d024-ai-child-play-2.json` result was written. The current log tail shows BMP/source prewarm activity and return to a backup Menu scene, not a completed AI child report. Do not infer a Play pass or a production defect; the second attempt is `RESULT_MISSING`, and runtime acceptance remains pending. No Scene file was saved by this probe; Battle disk SHA remains unchanged. Diagnose the missing result before another request.

The test-only probe was then extended within its declared script path to write phase progress at menu prewarm, post-prewarm, app birth, AI Driver and shutdown, so interrupted Play leaves a phase result. Original Editor imported/compiled it without `error CS`. Request `d024-ai-child-play-3` passed preflight and wrote a final `FAIL` JSON: `LoadingPrewarmController.PrewarmOnceAsync()` threw `OperationCanceledException: Configured content request is no longer current` at probe line135 before app birth, tick0. The appended ordered-shutdown/pool message is not independent proof of shutdown failure because no battle runtime was created (`stopped=false`, `poolBorrowersAfter=-1`); diagnostic cleanup wording needs correction before another run. The Editor ended non-Play with active scene `NTSD_Battle`, while Menu/Battle disk SHAs stayed `3B0F58...80228` / `9E7B8A...A0AC0`. CharacterAnimtorManager cancels a configured prewarm on new generation, manager destroy or SimulationTickDriver shutdown; this result does not isolate which trigger occurred. It is a prewarm/interference boundary, not evidence about AI child spawning. Do not retry Play blindly while this current Editor scene/ownership state is unexplained.

2026-09-24 pre-change diagnostic continuation: request `d024-ai-child-play-4` passed prewarm, created OID2 human and OID7 AI, and completed 60 production Driver ticks. It failed the probe's target-selection guard with `aiTargetSlot=-1` and no child, but all observed entities retained source carriers; ordered shutdown completed, returned to Menu, and left zero pool borrowers. Menu/Battle disk SHA stayed `785F828...81E13` / `471396E7...7B9`. The probe did not record enough participant state to attribute the failure. Before changing this declared test-only script again, limit the edit to read-only team/position/type and per-tick target diagnostics; no gameplay, resource, Scene or camera write. A rerun must report evidence, not force an AI child or change authority behavior.

2026-09-24 second pre-change diagnostic note: `d024-ai-child-play-5` and `-6` completed 60 original-Editor full Driver ticks with the same target `-1`, no child, source carriers complete, ordered shutdown and zero borrowers. `-6` showed human/AI HP500, teams1/2, initial X620/1326 and Z506/388, input phase alternating 1/0. The probe's 60-tick limit ends before AppManager's production participant `HitStun=75` can decay to the ground-target eligibility threshold; the air-target override requires X<250/Z<40, whereas this fixture starts X706/Z118 apart. The next script edit remains test-only: extend bounded observation beyond the entry stun and sample HitStop. Do not infer an AI production defect from these runs or alter battle rules to satisfy the fixture.

Correction before next test-only edit: `d024-ai-child-play-7` ran 180 original-Editor full Driver ticks. Human HitStop decreased 75→0 by tick76, while AI target remained `-1` through tick180; therefore entry stun accounts only for the first interval. The natural child was not observed, source carriers stayed complete, and shutdown/zero-borrower checks passed. Next test-only instrumentation records relation teams, current data types/states and AI control flag after the eligibility transition. No production, Scene, DAT, camera or nonbattle edit is authorized by this evidence.

`d024-ai-child-play-8` returned the same full-Driver result through tick180; after tick76 the human was RelationTeam1, HP500, type0, state1, HitStop0, while the AI was RelationTeam2, type0, controlled true. This does not explain why target remained -1. Before the next edit, scope remains only the declared test script: enable existing BattleAiInputDetailDiagnostics and record actual AI producer/nearest call counts at a few checkpoints, then disable it before shutdown. No production behavior or fixture target override.

Post-edit/result: the probe now uses the existing `BattleAiInputDetailDiagnostics` for read-only per-tick sampling and disables it before normal shutdown. Original Editor recompiled with zero Console errors. `d024-ai-child-play-9.json` reports 180 full Driver ticks, `AiCount=1` at sampled ticks 74/75/76/90/120/180, `Unk360=-1` throughout, no child, zero missing source carriers, ordered shutdown complete, returned to Menu, pool borrowers 0. `FindNearestGround` call count was 0 in this production profile; this phase counter does not by itself prove nearest selection was skipped because indexed canonical has separate counters. Current GameConfig asset selects `DataOrientedCanonical`; the isolated legacy-formal-content test that selected a target is not an identical scenario. Next bounded task must compare same initial state/profile candidate and commit paths; no production defect has yet been proven. Menu/Battle disk SHA remained `785F828...81E13` / `471396E7...7B9`.

Final narrow checks for this diagnostic edit: original Editor refreshed and compiled with Console errors 0; `Tools/Validate-ChangeLedger.ps1 -RepositoryRoot <project>` PASSED with 793 records, this test script covered; scoped `git -c core.safecrlf=false diff --check` exit 0. The initial Windows PowerShell `-File` invocation of the validator failed before validation because its default `$PSScriptRoot` evaluated empty; rerunning via the active PowerShell with explicit RepositoryRoot passed. After request 9, original Editor loaded the saved Battle Scene clean; HUDCamera and ScenesCamera serialized `m_Enabled=1`, both Scene hashes still match the before-run values. No natural child acceptance or formal EXE A/B yet.

Additional pre-change note: `FindNearestGround` phase call count in request 9 was 0 but production GameConfig uses indexed canonical AI, whose nearest search has a separate phase counter. The next edit stays in the same declared test script and adds read-only `IndexedCanonicalNearestSearch` and commit counters to the existing diagnostic sample. No production, scene, content or camera mutation.

Post-edit request `d024-ai-child-play-10`: original Editor compile errors 0; formal Menu→Battle and 180 full Driver ticks completed, `IndexedCanonicalNearestSearch` call count 1 at sampled ticks 76/90/120/180, target -1/no child; cleanup returned to Menu with zero pool borrowers. The `IndexedCanonicalCommitApply` phase reports call count 0, but its production code only calls BeginPhase/EndPhase and never RecordPhaseCall, so this metric is non-informative for commit. The indexed query is reached; candidate result versus commit/writeback remains unresolved. Avoid another full Play retry until a focused same-state two-profile witness and existing committed/fallback counters are prepared. `RUNTIME_PENDING` remains accurate.

Pre-change authority correction: exact code inspection supersedes the tentative profile first-difference lead. This Play probe constructs `new MatchConfig { seed=2833 }` without gameMode; `SimulationTickDriver.ApplyMatchConfig` sets BattleGameModeId=1 for null. The formal playable `native_ai.cpp` is in `build.ps1 -Target playable` and `NativeAi28::select_ordinary_target` uses `native_opposing_group`; at mode1, AI team2 cannot target human team1. Unity indexed canonical uses `world.BattleMode` when synchronized native context is active, and its `AiSensingKernel.TeamAllowed(2,1,1)` is false. The target=-1 outcome is therefore a test fixture mode/roster mismatch, not evidence of a production regression. Next edit remains inside this Record's declared Editor probe script: set explicit battle mode0 for team1/team2, record effective mode, then run one focused original Editor request. No production AI, Scene, DAT, resource, or camera change.

Request `d024-ai-child-play-11` with explicit mode0 compiled in the original Editor and selected human slot0 from tick75. It completed 180 production Driver ticks without an owned child, while all extant source carriers remained initialized, ordered shutdown returned to Menu, pool borrowers were zero and Menu/Battle disk hashes unchanged. The prior no-target observation was a fixture error; this run does not establish an AI regression. Pre-change for the next edit: formal OID7 Lee DAT contains OPoint actions, but sampled tick180 AI frame464 is a punch frame with no OPoint. Extend only this diagnostic script's bounded window to 600 ticks and record AI frame transitions/new births to distinguish absent action from transient child. Existing production, Scene, DAT, resource, camera and nonbattle code remain outside scope; rollback is removal of this probe after provenance review.

Post-edit request `d024-ai-child-play-12`: original Editor compiled the test-only script and Play completed 600 full Driver ticks in mode0, target slot0. AI visited Lee frames61 and486; no extant owner1 child was found. Formal `c/lee/lee.dat` OPoints on these frames reference OID814 actions700/701, which formal `c/lee/a/lee.dat` defines as `pic:999 wait:0 next:1000` pure sound. Immediate recycling is expected, so the probe's no-child failure is a fixture expectation error, not a demonstrated production AI or OPoint defect. Three observed OID120/owner -1 births were unrelated random weapons. All scanned extant source carriers initialized, ordered shutdown returned to Menu, pool borrowers0, Scene disk hashes unchanged (`785F828...81E13`/`471396E7...7B9`). Natural materialized AI child proof remains `RUNTIME_PENDING`; next fixture must use a formally reachable non-sound child action and re-evaluate observation across structural playback. No production, DAT, Scene, camera or nonbattle code changed by this package.

Candidate next witness actions were read from formal content only: Lee frame164→OID204 action20 resolves to `a/cha/cha.dat` pic28/wait2, or frame22→OID706 action73 resolves to `a/pof/pof2.dat` pic4/wait1. Neither was observed as a naturally selected AI action in this 600-tick seed. Original Editor final state is non-Play, clean `NTSD_Battle`; both saved cameras enabled, Scene hashes unchanged. `read_console` reports zero errors, scoped `git -c core.safecrlf=false diff --check` exit0, `Tools/Validate-ChangeLedger.ps1 -RepositoryRoot <project>` PASSED with 793 Records. No broad suite was run for this test-only diagnostic change. Record remains `RUNTIME_PENDING` because the intended non-sound AI child Play proof is not achieved.

Pre-change 2026-09-24: next edit remains limited to `Assets/NTSD/Scripts/Test/Editor/NTSD28D024AiChildScenePlayProbeEditor.cs`. Add an opt-in controlled non-sound action to the existing request and diagnostic fields for trigger tick/frame; after a real mode0 AI target is present, write Lee frame22 on that test entity immediately before one full Driver tick and observe the production OPoint materializer. Formal decoded Lee frame22 has wait2 and OID706/action73; formal pof2 action73 has pic4/wait1. Preserve the unforced 600-tick branch. Expected side effect is only a test-world action and diagnostic JSON. This is not authorization to change AI behavior, DAT, Scene, camera, Prefab or nonbattle paths. Acceptance is fresh original-Editor compile/Play, separate controlled-child status, source/physical/Renderer carrier, zero residual borrowers, unchanged saved Scene hashes, scoped diff check and ledger validation. If the action is overwritten before late spawn, record the actual first difference and keep `RUNTIME_PENDING`. Rollback: remove this opt-in probe change after provenance review; retain all user/parallel work.

Run `d024-ai-child-play-13` compiled with zero Console errors and produced a real mode0 target by tick75. The opt-in Lee frame22 trigger was applied at tick80; frame22 survived the tick, and OID706/action73 appeared as AI-owned slot50 at tick82 after the full production Driver step. Source carrier initialized, source X/Z 1167/401, physical X/Z 1082/408, missing source carriers0; ordered shutdown returned to Menu with borrowers0. The run status is accurately FAIL because the old probe asserts a non-null per-entity Renderer. This is an invalid test expectation for the current CentralOnly runtime: `SimulationTickDriver` sets `SetLogicOnlyEntityMaterialization(true)` when the formal catalog is ready, and `LF2ObjectPointFactory.MaterializeObjectForStructuralWriter` creates a logic entity via `LogicEntityFactory`, with no per-entity Renderer. Next edit in the same declared test script only: record the mode and require mode-appropriate Renderer state. This does not establish visible pixels or natural AI action selection. Menu/Battle saved SHA stayed `785F828...81E13` / `2EE465D8...B77A`. No production, DAT, Scene, camera, Prefab or nonbattle change.

Post-edit `d024-ai-child-play-14` result is PASS for the controlled full-Driver child path. The probe now records `logicOnlyMaterialization=true` and accepts `childRendererPresent=false` in that production mode; source initialized, missing carriers0, owner1/OID706/slot50/tick82, source X/Z 1167/401 and physical X/Z 1082/408. Original Editor compiled with zero Console errors. Shutdown complete, returned to Menu, borrower count0; after Play the saved Battle Scene was reloaded clean. Menu/Battle disk SHA-256 remained `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13` / `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`. This is a test-only controlled action, not natural AI selection, renderer pixel proof or formal EXE parity. Record stays `RUNTIME_PENDING` until those separately owned exits; no production/DAT/Scene/Prefab/camera/nonbattle edit.
