<!-- CHANGE-RECORD
id: NTSD28-Q07-OID434-NATURAL-ENTITY-BINDING-001
status: VERIFIED
change-kind: EDITOR_DIAGNOSTIC_ONLY
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28Q07Oid434NaturalBindingProbeEditor.cs
authority: NTSD 2.8-Logan formal ras.dat frame396 and original Editor natural physical-input route
evidence: original Editor two preserved after254 runs, corrected 45-tick gap-free observer, OID434 action35 but no action396; scoped negative branch verified
-->

# NTSD28-Q07-OID434-NATURAL-ENTITY-BINDING-001

Pre-edit state: the Q07 OID434 published catalog probe proved `(434,36)` exists in production, but no current original-Editor result records the **natural entity's** `TryResolveCurrentSpriteEntry` at action396. Existing `NaturalProbe` after254 uses the shared InputSystem and controlled `StepOneTick` with physical L/D/K/J events; its output does not include child entities. Build a separate observer and reuse that method unchanged. The observer must not create or mutate simulation entities, pause state, input frames, Scene, DAT, PNG or production runtime. Side effects are only an independent request, Play entry/exit and create-new report. Prevent request/output collision, preserve negative evidence, and do not convert a SpriteCatalog/entry result into a pixel claim. Validation and rollback follow the Task Contract.

First code/report: new opt-in Editor observer and `.meta` created; original Editor import and generated Editor C# build 0 errors, Console error0. `oid434-natural-20260925-1.json` is FAIL: existing after254 natural probe PASS with skill tick10 and actor `254 -> 0` tick41; no OID434/action396 captured. The observer incorrectly counted a tick gap at tick52 after the natural probe released paused stepping. Before the next script edit, refine report fields to capture the first OID434 frame of any action and count gaps only while the natural probe owns the controlled stepping. Preserve first report.

Post-edit/validation: only the new Editor observer and `.meta` were added; observer correction counts gaps only until the existing natural result is written, records any OID434 first action and keeps a second create-new request. Regenerated Editor C# project includes the script and builds with 0 errors/165 warnings; original Editor Console error0. Second Play result `oid434-natural-20260925-2.json` is a meaningful FAIL with natural route PASS, 45 consecutive controlled samples, OID434 first tick39/action35 across 12 entity-frames, but no action396. The requested action396 Sprite binding is pending on a different reachable continuation. Original Editor exited Play; Battle/Menu/GameConfig SHA unchanged. Evidence and exact result hashes: `artifacts/diagnostics/NTSD28-Q07-OID434-NATURAL-ENTITY-BINDING-001/ACCEPTANCE-20260925.md`. Rollback: new observer only, respecting protected-file approval rules; no production behavior change.
