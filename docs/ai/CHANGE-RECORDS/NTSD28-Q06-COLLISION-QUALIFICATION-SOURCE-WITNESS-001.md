<!-- CHANGE-RECORD
id: NTSD28-Q06-COLLISION-QUALIFICATION-SOURCE-WITNESS-001
status: VERIFIED
change-kind: COLLISION_QUALIFICATION_SOURCE_WITNESS
code-path: Tools/NTSD28AuthorityTrace/collision_qualification_witness.cpp
authority: Current BattleWorld28.rebuild_geometric_hit_candidates snapshot geometry/current platform split, nullable current and previous states in candidate.pair_snapshot, classify_ordinary_hit_eligibility and SimulationTickDriver28.step.
evidence: Prior original336/Unity four routes leave24 candidate differences per route where current1000 and valid snapshot; current/previous nonzero state filtering and live post-snapshot frame changes need original evidence.
-->

# 碰撞准入、非零配对状态与后继消费原函数见证

IN_PROGRESS / SOURCE_ONLY。准确单CPP，复用已核验build工具；不改原source/正式EXE或Unity生产。

合成定义frame0有geometry；10(state18)、998(state13)、可选999(state19)无geometry，450为implicit，1000越界。枚举current动作组合、previous动作组合、effect0/20、同组/异组、999声明与否，调用原collection，输出候选冻结pair各字段、过滤计数、两实体raw；随后把两current写1000，调用原eligibility并与变更前对照。此扰动是端点诊断，不冒充正式driver可达性。

另构造三实体完整driver：slot0 kind3使slot1换到带cpoint但无itr的高帧，slot1原快照有攻击slot2的itr，观察完整step中是否仍消费及HP/动作/快照变化。该见证是否成立以真实源输出为准，不预设成功、不修改source迎合夹具。实际options按已有Normal/Practice默认资源/掉落设置；无stage/fusion catalog。

验收：编译成功、源身份保持、两遍字节一致、初值/候选pair/分类结果可复用；诊断与正式driver证据分开。无生产字段/schema/关闭副作用；回滚仅诊断CPP差量且按规则授权。脚本后登记实际向量和失败。后继Unity子包另准确Record；禁止computer-use、非战斗/框架/资源/Scene/Server变更。
已写单CPP：480端点（current10组×previous6组×effect2×group2×999声明2）加2个完整driver初值；输出冻结pair/分类前后/raw及完整tick命中计数。先前工具JS字串语法错误未执行任何文件操作，本次已落盘，尚未编译验证。production未改。

## 原源限定出口

VERIFIED / SOURCE_MODEL_ONLY。编译退出0、EXE和75源SHA保持，first/repeat482行完全一致SHA175c1a7ece4067ff782d272fe07ea418bc72742e2a55f3b691537065ea08fb45；validation3020断言PASS。480端点273候选/eligible、207过滤，current变1000前后分类一致且raw无副作用/随机0；两个真实SimulationTickDriver step各2候选、1kind3、1普通命中，slot1 current10/998且snapshot0、slot2HP499、无diagnostics、Native0/CRT2。仅源可达性证据，不是正式EXE键盘验收。Unity新的bdefend0/45和CRT/legacy差异不影响原见证有效性，另任务追踪且父包未关闭。
