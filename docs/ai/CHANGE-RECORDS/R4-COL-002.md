# R4-COL-002 — caught-cpoint `hurtable` shared consume guard

<!-- CHANGE-RECORD
id: R4-COL-002
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Animation/LF2Objects/BattleHitCandidateSequenceRunner.cs
code-path: Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\entity\collision.cpp:57-80,1253-1258; J:\QQFile\NTSD2.4\ntsd_release\include\entity_runtime_groups.h:63-64,147-155
evidence: SOURCE-CONTRACT-VERIFIED / UNITY-PREFLIGHT-VERIFIED / CODE-WRITTEN / UNITY-COMPILE-PASS-20260822-0401+08 / FULL-SELF-CHECK-PASS-20260822-0401+08 / LEDGER-PASS / DIFF-CHECK-PASS / PLAYMODE-PENDING / CXX-RUNTIME-TRACE-BLOCKED
-->

> 创建日期：2026-08-22  
> 最后更新：2026-08-22  
> 类型：battle / collision / caught-cpoint / candidate-consume / test  
> 所属 Work Package：`R4-COL-02`  
> 当前状态：`RUNTIME_PENDING` — source、Unity compile和full self-check均已通过；C++ runtime trace与真实 cpoint Play Mode仍未关闭。

## 1. 目标与范围

只在 shared runner中调用现有 C07-B helper，使 caught target / non-hurtable catcher关系只 skip当前
candidate。只允许 runner和对应 self-check改动；不改helper、CPoint writer、candidate collection、scheduler、
held/link、render、C++或其他D-COL/D-HIT条目。

## 2. Authority / 差异依据

- **C++ VERIFIED**：`collision.cpp:69-79` 的 target prev2 cpoint / catcher / caught / hurtable gate，
  其结果是 `next_pair_outer` current-candidate skip，不是 attacker abort；它排在vrest与C07-A之后、ITR replacement之前。
- **Unity VERIFIED**：`BruteForceSceneQuery.TargetBeingCaughtPairBlocked`和
  `IsReleaseConsumerPairBlocked`已等价读取同一关系/prev2字段；`FindEntityByRuntimeSlotForQuery`为active-only。
  shared runner未调用该 helper。
- **C++ trace BLOCKED**：R1-WP02未解除；不运行C++ executable。

## 3. 计划最小改动

1. 在R4-COL-001 C07-A之后调用 `IsReleaseConsumerPairBlocked`，blocked则 `return false`；
2. 不触碰 helper实现；
3. 加 exact/shared kind0 first-skip/second-hit、`hurtable=1` control和kind6 no-hit-confirm fixture；
4. 记录所有验证及任何fixture失败。

## 4. 实际代码写入

- runner在既有C07-A后调用 `BruteForceSceneQuery.IsReleaseConsumerPairBlocked(...)`，blocked时 `return false`；
- self-check覆盖 kind0 two-candidate skip/continuation、`hurtable=1` positive control和kind6 direct writer；
- 未改 helper、candidate collection或CPoint writer。

## 5. 实际验证

| 检查 | 实际结果 |
|---|---|
| C++ authority | 只读复核 `collision.cpp:57-80,1253-1258` 与 `entity_runtime_groups.h:63-64,147-155`；未运行、构建、修改或写入 authority。 |
| Unity compile | 现有 Unity Editor / UnityMCP port 6401 于 2026-08-22 04:01 +08:00 完成 force scripts refresh / compile；之后 Console `error CS` 查询为 0。刷新命令在 domain reload 时连接关闭，后续 console 查询证实 Editor 已恢复且无 C# 编译错误。 |
| Full self-check | 通过菜单 `NTSD/验证/运行战斗运行时自检` 请求；`Temp/NTSD_BattleRuntimeSelfCheck.result=PASS`，最后写入 2026-08-22 04:01:04 +08:00。 |
| 留痕 / diff hygiene | `Tools/Validate-ChangeLedger.ps1` PASS（16 records / 13 governed code files）；`git diff --check` exit 0。已有 CRLF 仅是 Git warning。 |

这些结果确认原子共享 gate 与本包 fixture未破坏当前 Unity 回归；它们不构成 C++ executable trace 或真实 Play Mode cpoint 技能验证。

## 6. 风险、停止、回滚

- C07-B如误用 `return true` 会错误中止 entire attacker；必须以two-candidate continuation防回归。
- 若target/catcher关系需要改写CPoint数据契约，停止而不扩大本包。
- 回滚仅限 runner、self-check及关联文档；提交 hash：未提交。
