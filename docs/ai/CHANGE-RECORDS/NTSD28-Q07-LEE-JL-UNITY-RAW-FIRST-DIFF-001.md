<!-- CHANGE-RECORD
id: NTSD28-Q07-LEE-JL-UNITY-RAW-FIRST-DIFF-001
status: VERIFIED
change-kind: Q07_LEE_JL_UNITY_RAW_FORMAL_FIRST_DIFFERENCE
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28UnityRawCaptureEditor.cs
authority: formal root EXE B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033 and paired playable ordinary-input Lee J,L child trace
evidence: artifacts/diagnostics/NTSD28-Q07-LEE-JL-UNITY-RAW-FIRST-DIFF-001/ACCEPTANCE.md; original Editor run2 PASS and 2557/2557 mapped fields
-->

# NTSD28-Q07-LEE-JL-UNITY-RAW-FIRST-DIFF-001

Before edit: the existing Editor raw-capture tool supports legacy 3-tick and two formal Q07 26-tick frozen scenarios, all preserving their original schema, initial state and output semantics. It uses a fixed `Temp/NTSD28UnityTrace/unity-raw-capture.result`, which already exists and must not be overwritten. It cannot admit the exact 45-tick Lee J→L native witness. Native source/release selected fields and child count are already recorded under `NTSD28-Q07-LEE-NATURAL-NONSOUND-CHILD-001`; Unity same-state full-Driver behavior is unknown.

Declared change: add only an exact Lee 45-tick formal schema branch to the Editor diagnostic, with fixed seed/participants/inputs/content identity and no relaxation of existing schemas; allow a request to specify a unique result path, retaining the previous default when absent. New scenario and output live in a new diagnostics directory. The capture uses original-project EditMode, complete `SimulationTickDriver`, formal staged content and current project mode Asset. Expected side effect is one isolated, directly comparable Unity trace; no production or scene mutation. Protect the existing Temp result and all dirty user work. Risks are stale content SHA, singleton cleanup, actual native/Unity phase or child-source differences, and original Editor being in Play; each is a measured failure, not a reason to force a passing trace. Task gives acceptance and rollback.

After first edit/run: the declared Editor raw-capture script now recognizes only an exact Lee 45-tick formal schema and accepts an optional unique result path while preserving the old default. Scenario SHA-256 `DB10D304084D5569EE2C9F03A39EA77A7D616DA8AA84821BC6EE98F8925AD113`. The original Editor recompiled with zero Console errors and its unique EditMode run1 returned PASS; 45 ticks and 2557 mapped input/entity comparisons matched the root formal EXE trace. The old Temp result SHA and both Scene SHAs remained unchanged. Run1 output and exact limitation are in `RUN1-ANALYSIS.md`.

New measured fixture issue before second script edit: `ConfigureWorldAndRoster` calls `ResetForDirectBattle(seed)`, consuming a synchronized random BGM choice. This exact formal source configuration supplies explicit BGM selection 2, so native `resolve_native_bgm_selection` does not draw. Run1 Unity initial synchronized counter/index/calls 1/1/1 versus root LFR 0/0/0. The root LFR's CRT seed 0 versus source/Unity seed 682973786 explains its separate CRT state; seed-to-state calculations and native source are recorded in RUN1-ANALYSIS. Next edit is confined to the same Editor diagnostic code path: for this Lee schema only, seed its diagnostic World via `ResetFromSeed(seed)` instead of the generic direct-battle BGM draw. This aligns the test fixture's explicit-BGM precondition and does not alter production or the frozen old scenarios. Preserve run1, issue a unique run2, and compare again before advancing status.

After second edit/run: only the new Lee schema uses `ResetFromSeed`; all old schemas retain `ResetForDirectBattle`, and the optional unique result path refuses overwrite. Original Editor compiled and run2's unique request returned PASS. Native root LFR versus Unity: 45 completed ticks, 2557/2557 mapped input-phase/entity-state comparisons and all occupied slot sets equal; synchronized RNG counter/index/call count/last site equal on every tick (initial 0/0/0, one site130 call at tick2); tick6 five OID204 children equal in slot/action/owner/position. Unity's source-seeded CRT initialization differs from the root LFR replay's default-seeded CRT initialization, without further per-tick CRT calls. No production battle/DAT/image/Scene/ProjectSettings/nonbattle script was changed. Old Temp result and both Scene SHA stayed unchanged; `git diff --check` passed. Final scoped evidence and run2 artifact SHA in `artifacts/diagnostics/NTSD28-Q07-LEE-JL-UNITY-RAW-FIRST-DIFF-001/ACCEPTANCE.md`. Physical keys, saved Battle Scene Play, GPU/formal pixels, full child lifetime, and Q07/D-024 exit remain unverified. Rollback remains confined to the new diagnostic schema and request option after provenance review; retained evidence is not deleted.

Final governance validation: `Tools/Validate-ChangeLedger.ps1` exited 0 with 846 Records and four governed code files in the current worktree diff; this Editor diagnostic is explicitly COVERED by this Change ID. Its numerous warnings refer to historical Records declaring files absent from the current diff. `git diff --check` exited 0. The three restored progress documents remain NUL-free after the additive status update.
