<!-- CHANGE-RECORD
id: NTSD28-Q07-SASUKE-NEEDLE-CAMERA-PIXEL-001
status: VERIFIED
change-kind: BATTLE_PRESENTATION_TEST
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28Q07SasukeNeedleCameraProbeEditor.cs
authority: formal NTSD28 root EXE/playable render and formal OID11 to OID440 content; existing original-Editor physical L/D/J witness
evidence: ORIGINAL_EDITOR_COMPILE_ZERO_CSHARP_ERRORS / PHYSICAL_LDJ_PASS / FOUR_FORMAL_CHILD_COMMANDS / SEPARATED_CAMERA_ROI_424_NEW_PIXELS / ORDERED_SHUTDOWN_STAGE11_ZERO_OBJECTS_SLOTS_BORROWERS / FORMAL_EXE_GPU_OUTSIDE_SCOPED_PACKAGE
-->

# NTSD28-Q07-SASUKE-NEEDLE-CAMERA-PIXEL-001

Created before script edit. Original state: existing physical-input probe reaches four formal OID440 and verifies `chi.png` pic0 binding, but captures no camera pixels. Exact write scope is the new opt-in Editor diagnostic script and `.meta`; it reads the saved Battle Scene and current formal publication without modifying production or the Scene. Expected effects are one temporary camera RenderTexture during Play and unique diagnostic PNG/JSON. No persistent runtime owner or new shutdown stage is introduced.

Authority, preconditions, acceptance and rollback are in the Task. The observer must fail closed on wrong content root, scene, command count or camera and must restore all temporary camera fields. It must not change logic positions, input, DAT, resource files, scene bindings or the existing input probe. After editing, append actual symbols, compilation/Play evidence, cleanup/hashes and limitations; never promote a Unity-only pixel result to formal EXE GPU parity.

2026-09-26 first-run evidence before the scoped correction: original Editor compiled with zero C# Console errors; physical probe `q07-sasuke-needle-camera-20260926-01` PASS, natural OID440 peak four and `chi.png` pic0 binding observed. The camera observer FAIL artifact records its own overly strict current-pic0 gate after capturing a tick-0 baseline; no after PNG or camera-pixel conclusion. Original Editor returned to EditMode and Menu/Battle/GameConfig/mode Asset SHA remained unchanged. Next script edit only changes the observer to bind and match each child's current formally resolved `chi.png` effective pic, recording those pics. First-run result stays immutable. This changes no production, input, DAT, image or Scene behavior.

Actual code now written: only `NTSD28Q07SasukeNeedleCameraProbeEditor.cs` and its new `.meta`. The observer keeps a per-stable-ID effective-pic map from current formally bound `chi.png` entries, matches four current `Entity` commands to that map, records each sampled pic, and leaves the first failed result untouched. The original `pic0`-only gate was test-observer logic, not production behavior. Recompile, one focused same-scene physical Play, camera PNG review and final shutdown/hash checks remain pending.

Second original-Editor run `q07-sasuke-needle-camera-20260926-02`: physical PASS, four natural OID440; camera observer `PASS_SCOPED_UNITY_CAMERA` at tick15, four pic1 commands, ROI 58x59 and 959 changed nonblack pixels, camera restoration true. Raw PNG inspection shows the four command positions coincide and the moving parent overlaps that ROI; this is not individual child-pixel proof. The next script-only correction will wait for four distinct projected child centers and a child union ROI black in the baseline, then require new nonblack pixels. It will preserve the earlier JSON/PNG and not touch production or saved Scene. Original Editor returned to EditMode; protected four hashes unchanged.

Third-run observer code now written in the same one-file scope: after collecting four current formal commands, it waits for four distinct projected pixel centers and a union ROI with zero nonblack baseline pixels before taking the after camera PNG; if no qualifying frame appears through tick26, it emits `PIXEL_OWNERSHIP_UNPROVEN`. Per-sample command counters are reset. Original Editor recompile, focused third Play, PNG review and governed validator remain pending.

Final scoped evidence: original Editor recompiled with no C# Console errors, then `q07-sasuke-needle-camera-20260926-03` physical L/D/J PASS and camera observer `PASS_SCOPED_UNITY_CAMERA` at tick18. It matched four naturally born OID440 CentralOnly commands at distinct projected centers, each with current pic1 formal `chi.png` binding. A/B raw PNG visual review and independent Pillow recount found zero pre-birth nonblack and 424 after-birth nonblack pixels in the 5,265-pixel projected child group ROI. Camera fields restored; EditMode return and saved Menu/Battle Scene/GameConfig/mode Asset hashes held. The two earlier run results remain untouched. This proves a visible natural child *group* in Unity, not four separately identified silhouettes or formal EXE GPU parity. The current observer has no post-exit World/slot/pool borrower telemetry, so its full acceptance stays `RUNTIME_PENDING`; Q07 and the parent goal stay open. Evidence: `artifacts/diagnostics/NTSD28-Q07-SASUKE-NEEDLE-CAMERA-PIXEL-001/ACCEPTANCE-20260926.md`. `git diff --check` and Ledger validator are final checks pending at this record update.

Final governance checks: `git diff --check` exited 0. `Tools/Validate-ChangeLedger.ps1` in the existing PowerShell 7 shell exited 0 with 862 Records and 22 governed code files in the current shared diff covered; its historical warnings about Record paths absent from this diff are nonfatal. A first attempt through Windows PowerShell 5.1 failed before validation because a Git CRLF warning was promoted to a terminating native-command error; it does not contradict the successful PowerShell 7 validation. The three restored progress documents remain NUL-free. No new production script, DAT, image, Scene, protected config or nonbattle code was changed by this package.

Pre-edit follow-up contract, same one-file observer scope: the third-run camera artifact is strong but the Task's shutdown postcondition lacks direct per-run counters. Add only an opt-in `ExitingPlayMode` report in `NTSD28Q07SasukeNeedleCameraProbeEditor.cs`, subscribed after the existing runtime shutdown bridge has been registered. Hold the live driver reference, inspect `LifecycleState`, and use its idempotent report accessor only when state is already `Stopped`; never initiate or reorder shutdown. Emit a unique companion JSON with `CompletedStage`, remaining objects/slots/borrowers, or an explicit not-yet-stopped observation. Preserve old outputs, source assets and Scene. A single focused same-scene physical Play, compile0, hash checks, `git diff --check` and Ledger validator are the acceptance steps. Formal EXE GPU parity remains outside this narrow follow-up.

Actual follow-up code now written: the existing opt-in observer owns a per-run `ExitingPlayMode` callback and a new create-only `-shutdown.json` output. The callback reads the cached live driver lifecycle; only if `Stopped` does it call the idempotent `ShutdownBattleRuntime()` report path, then records stage and remaining World object/slot/pool borrower counters. Any earlier callback order records `NOT_STOPPED_AT_OBSERVER_CALLBACK`; it does not drive shutdown. It unsubscribes and clears cached references on exit. Production shutdown, physical input probe, DAT, image, Scene and nonbattle code were not edited. Original Editor compile and one focused Play remain pending.

Run `q07-sasuke-needle-camera-20260926-04` in the original Editor completed the exact scoped acceptance: physical L/D/J natural OID440 four-child PASS; four formal `chi.png/pic1` commands and separated tick18 ROI, with independent PNG reread reproducing 0 before/424 after nonblack pixels; camera fields restored. At `ExitingPlayMode`, the existing owner had already stopped the runtime, and the observer's idempotent read returned `AlreadyStopped / RuntimeMapCleared`, remaining World objects/slots/pool borrowers 0/0/0. The Editor returned to idle EditMode, final Console C# error query 0, and Menu/Battle Scene/GameConfig/mode Asset SHA stayed fixed. Thus this **test-only package** is `VERIFIED` for Unity natural child-group camera and shutdown. It does not claim separate visual ownership of each of the four overlapping silhouettes, formal EXE GPU parity, or closure of Q07/R17/R18/the overall goal. The focused original-Editor Play is the only new runtime run; no broad suite was repeated. Final diff/ledger governance checks follow.

Shutdown-evidence limit: post-`Stopped` report numeric World/slot/pool zeros use null fallbacks after unbinding. The meaningful contract evidence is that the production stage-8 registry cleanup checks zero objects/slots/buckets/logic borrowers before unbind and stage-10 pool quiesce rejects active object/sprite borrowers; the observed stage-11 `Stopped` transition passed those guards. This was not an independent pre-unbind collection snapshot. `git diff --check` exited 0; final `Tools/Validate-ChangeLedger.ps1` exited 0 with 862 Records and 22 governed code files in the shared diff covered. The restored alignment/handoff/STATE documents remain NUL-free.
