<!-- CHANGE-RECORD
id: NTSD28-Q07-SELFCHECK-FORMAL-GEOMETRY-001
status: VERIFIED
change-kind: Q07_FORMAL_GEOMETRY_SELFCHECK_SOURCE
code-path: Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28Q07FormalSelfCheckGeometryEditorTests.cs
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28Q07MenuSceneCallbackPlayProbeEditor.cs
authority: D-023 formal Logan DAT except excluded backgrounds/mode DAT; formal native CollisionGeometry28 control-only semantics; ProjectBattleModeConfig selected project mode snapshot
evidence: original Editor formal/staged measurement 1/1 and shared formal geometry SelfCheck 2/2 PASS; configured-root no-argument Play PASS with ordered shutdown and zero borrowers; aggregate legacy fixture retirement belongs to separate verified Change
-->

# NTSD28-Q07-SELFCHECK-FORMAL-GEOMETRY-001

2026-09-25 scoped verification: local Unity MCP stdio bridge on the existing original Editor accepted `refresh_unity` scripts-only compilation; `Assembly-CSharp-Editor.dll` advanced to UTC 10:52:36. A unique opt-in request `q07-formal-geometry-play-20260925-01` returned PASS (JSON SHA-256 `6B569FFB5C03539DE0491FEC4CE9717C0FF19F0CAA55CCB4072042F4CF4AC391`), with `geometrySelfCheckAudit=true`, `geometrySelfCheckPassed=true`, formal source key, `battleRunning=true`, World2, three publication keys equal, `stayedStopped=true`, `returnedToMenu=true`, and `finalPoolBorrowers=0`. Editor.log recorded R-HC-01 definitions330/resolved282810/ITR19461/BDY86383/27 complete zero-width/82 control-only/0 other invalid, and R-HC-02 OID999 resolved857/transition7/gated15. Local bridge state after the run showed non-Play, idle, no compile/update, active saved Battle Scene; Console error query returned zero. Battle/Menu/GameConfig disk SHA remained at pre-run values. `git diff --check` and Change Ledger validation are required before final scoped delivery; detailed report: `artifacts/diagnostics/NTSD28-Q07-SELFCHECK-FORMAL-GEOMETRY-001/PLAY-ACCEPTANCE-20260925.md`. This is `VERIFIED` for this Change's source/Play boundary only; Q07, natural skill, full visual and EXE parity remain open.

2026-09-25 final scoped checks: `git diff --check` exited0; `Tools/Validate-ChangeLedger.ps1 -RepositoryRoot <repo>` exited0 with 821 Records and 20 governed code files in the current shared worktree diff, including this probe path. Saved Scene/Config hashes were rechecked after documentation and still matched. The validator output is `Temp/q07-formal-geometry-ledger-20260925.log`.

2026-09-25 post-edit Play probe state: the exact optional branch is now written in `NTSD28Q07MenuSceneCallbackPlayProbeEditor.cs`. It uses a separate `Temp/NTSD28_Q07_FormalGeometryPlay.request.json` path, preserving the older Menu callback request file. After formal publication and a live Battle World, it reflects only the no-argument `CheckDeployableResolvedGeometryRisks`, records `geometrySelfCheckPassed`, and follows the existing ordered unload/scene restore. Generated `Assembly-CSharp-Editor.csproj` `dotnet build --no-restore -v:q -nologo` reported 194 warnings, **0 errors**; this is offline compilation, not original Editor import. Original Editor PID11944 was alive but its `Assembly-CSharp-Editor.dll` and Editor.log timestamps did not advance, and the unique request was not consumed after 30 seconds. Unity CLI reported `STATUS_NO_INSTANCES` because this project has no Pipeline package; the local Unity MCP CLI did not connect on its configured port. The new request has been disarmed (`requested=false`) to avoid a delayed surprise Play. No Play result exists; status remains `RUNTIME_PENDING`. Exact report: `artifacts/diagnostics/NTSD28-Q07-SELFCHECK-FORMAL-GEOMETRY-001/PLAY-PROBE-PENDING-20260925.md`.

2026-09-25 pre-edit Play validation extension: exact additional script path is `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07MenuSceneCallbackPlayProbeEditor.cs`, symbols `Request.geometrySelfCheckAudit`, `Report.geometrySelfCheckPassed`, `Poll`/`Run`. The existing Menu→Battle probe already establishes formal publication, `BattleRunning`/World, ordered unload and scene restore. Add only an opt-in branch after publication validation that reflects the no-argument geometry method; absent flag must preserve all prior request behavior. No production method, DAT, Scene, GameConfig, Menu behavior or default probe path changes. Expected side effect is one new diagnostic report field/branch and a potentially expensive full formal geometry read during the opt-in Play. Acceptance is original Editor compile 0 errors, unique opt-in report with geometry pass and existing shutdown postconditions, stable Battle/Menu/GameConfig SHA, `git diff --check` and ledger validator. Failure is recorded rather than converted to a skip. Rollback only this probe hunk under repository approval rules. Current status remains `RUNTIME_PENDING` until Play evidence is inspected.

Task: `docs/ai/TASKS/NTSD28-Q07-SELFCHECK-FORMAL-GEOMETRY-001.md`. Created before any code edit. Parent BATCH-04/Q07. Formal/staged assembly measurement and source scope are in `artifacts/diagnostics/NTSD28-Q07-OLD-ASSET-REFERENCE-REFRESH-001/UNITY-ASSEMBLY-FORMAL-GEOMETRY-TWO-ROOTS-20260925.md`.

Current Unity behavior: `BattleRuntimeSelfCheck.CheckDeployableResolvedGeometryRisks` obtains `GameDataManager.GetAllObjects` and `CharacterAnimtorManager.ParseCharacterFrameConfigs(dataManager)`, which can read old `Assets/NTSD/Config/data.txt` and old DAT. It requires five old raw inverted body records and no other nonpositive ITR. Those content expectations conflict with D-023 formal data. Synthetic zero-dimension and negative-height collision-rule checks remain valid and must be preserved. Other SelfCheck legacy DAT readers belong to `NTSD28-Q07-SELFCHECK-LEGACY-DAT-FIXTURES-001`; this package does not claim aggregate SelfCheck retirement.

Exact intended paths/symbols: add `NTSD28Q07FormalSelfCheckGeometryEditorTests.cs` to measure formal and staged `BattleContentSource.ForLoganRuntime` catalogs with the serialized `ProjectBattleModeConfig.LoadDefault().Capture()` snapshot, actual `BuildCharacterFrameConfigsFromCatalog` and `LF2FrameCache`, including 0..856 resolved access. After original Editor measurement, modify only `BattleRuntimeSelfCheck.CheckDeployableResolvedGeometryRisks` and any private helper required for its source selection/classification. Preserve authored source identity and complete geometry, including valid zero-width ITR; classify incomplete kind-100100/`hasGeometry=false` as control-only. Do not change DAT values, production collision, menu, scene, renderer, GAS or other SelfCheck checks.

Expected side effects: the geometry SelfCheck reads current formal/staged content instead of old Config DAT and asserts the measured formal classes. The R-HC-02 transition-smoke gate must remain checked against the selected catalog, without assuming old OID999 values. Invariants: no silent parser failures, no skipping out-of-range frame IDs, no reducing test coverage to a tautology, no original background/mode DAT, and no old resource deletion. Rollback only this package's exact test and SelfCheck hunk under repository approval rules, preserving all other dirty files.

Acceptance: first obtain original Editor focused measurement for both roots and assert exact counts/classification; capture RED from the existing old-content SelfCheck expectation if a reliable scoped invocation is available; then migrate source/expectations and rerun focused test. Validate same-root content identity, old-reference absence in this method, Scene hashes, `git diff --check` and `Tools/Validate-ChangeLedger.ps1`. Full `BattleRuntimeSelfCheck` without old Config is an aggregate later gate. Evidence/status will be appended after each step.

2026-09-25 test-first code written: added only `NTSD28Q07FormalSelfCheckGeometryEditorTests.cs`; its one filtered test uses the serialized project mode Asset snapshot, formal/staged runtime catalogs, actual converter and `LF2FrameCache` to assert 330 objects, 55,348 authored frames, 282,810 accessor visits, 19,461 ITR, 86,383 BDY, 27 complete zero-width ITR, 82 control-only ITR and zero other nonpositive shapes. It requires exact formal/staged equality. `BattleRuntimeSelfCheck.cs` remains untouched. Original Editor compile/test and any production correction remain pending.

2026-09-25 first original Editor compile failed: `NTSD28Q07FormalSelfCheckGeometryEditorTests.cs:82` CS0246 could not resolve `BattleBodyBoxValue`; the initial filtered bridge job `d1fab7dedeee4459b283859a06ce6fb1` returned `summary.total=0`, so it is **not** a passing test. Added the missing `using NTSD.Simulation` in the test file only. Refresh/compile and a nonzero selected-test run are required before advancing status.

2026-09-25 corrected original Editor measurement: after recompile/domain reload, filtered EditMode job `c1a70998299c40cda86d32f7d6a319c2` selected exactly one test and passed 1/1 in 14.53 s. Full JSON SHA-256 `F4D4DEA3C8D0B72572C598705A2941FF5AFC5F5C42C105C7BDBA323D11B05E33` at `artifacts/diagnostics/NTSD28-Q07-OLD-ASSET-REFERENCE-REFRESH-001/original-editor-formal-geometry-second-20260925.json`. Both roots used `ProjectBattleModeConfig.LoadDefault().Capture()` and the actual Unity parser/cache: all exact expected counts and classifications passed; the first zero-selection job is retained as failed validation, not counted. The full job JSON contains verbose converter output and is about 941 KB; summary is total1/passed1/failed0. `BattleRuntimeSelfCheck.cs` is still unchanged. The next precise production edit is to replace only this method's old `GameDataManager.ParseCharacterFrameConfigs` source with configured Logan catalog + project mode snapshot, classify 27 complete zero-width and 82 control-only nongeometry records, require zero other nonpositive ITR/BDY, and preserve synthetic rule checks and R-HC-02 gate.

2026-09-25 bounded SelfCheck code now written: `CheckDeployableResolvedGeometryRisks` reads `CharacterAnimtorManager.ConfiguredContentRoot`, resolves the current project runtime root, reads `LoganObjectCatalog` with the project mode Asset snapshot, converts all indexed definitions and retains its existing frame iteration and R-HC-02 transition/smoke checks. R-HC-01 now asserts the original-Editor formal measurements: 330 objects, 282810 resolved visits, 19461 ITR, 86383 BDY, 27 valid zero-width ITR, 82 incomplete kind-100100 control records, 109 total nonpositive ITR, zero unclassified ITR/BDY. The previous exact-five old inverted-body expectation was removed; `CheckZeroDimensionCollisionGeometry` and `CheckNegativeHeightCollisionGeometry` remain called and unchanged. The focused Editor test now has an additional invocation of this private configured SelfCheck method; compilation and its 2/2 result are pending. No DAT, scene, other SelfCheck method or battle production simulation changed.

2026-09-25 first post-edit original Editor test selected 2; the full-content measurement passed, but `ConfiguredGeometrySelfCheckAcceptsFormalContent` failed before the method because `GameConfig.Instance` is null in EditMode. This is a test-host precondition, not evidence that the production configured root is wrong. Split the private implementation into the existing no-argument production entry (selects configured root and mode Asset) and an overload accepting `BattleContentSource` and mode snapshot; the test calls that same implementation with the staged formal source, without installing a global GameConfig singleton or changing Play startup. Recompile and actual 2/2 result remain pending.

2026-09-25 original Editor post-refactor: filtered job `ecb21a677ae84970a953407559eb3f7b` selected exactly two tests and passed 2/2. Full JSON SHA-256 `1DDFCFB01103736BD2255D0863F3BE1461E0944C5B0A94D88F6A4173B6C638F7` at `artifacts/diagnostics/NTSD28-Q07-OLD-ASSET-REFERENCE-REFRESH-001/original-editor-formal-geometry-overload-20260925.json`. The test uses the same private geometry implementation with the staged formal source; it does not prove the no-argument GameConfig entry in Play. The original Editor formal/staged conversion test and R-HC-01/R-HC-02 plus synthetic rule checks all passed. A scoped source read of `CheckDeployableResolvedGeometryRisks` lines 21180–21400 found zero `ParseCharacterFrameConfigs`, `data.txt`, `LoadProductionDatWrapper`, `GameDataManager`, or old Config references. Saved Menu/Battle Scene SHA-256 remain `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13` / `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`. `git diff --check` exit0, source/Scene/ProjectSettings diff empty. `Tools/Validate-ChangeLedger.ps1` exit0, 798 Records, four current governed code files covered (including this method and test). Status `RUNTIME_PENDING`: focused geometry source migration is covered, but configured production root in Play, other old DAT SelfCheck fixture readers, fresh aggregate SelfCheck and Q07 total exit remain open.

2026-09-25 aggregate Editor RED: after the legacy-fixture RunAll changes, the existing `Temp/NTSD_BattleRuntimeSelfCheck.request` entry was run once in the original Editor. Result was FAIL at the new geometry entry before later checks: `R-HC-01/02: formal Logan content root must be configured`, because EditMode `GameConfig.Instance` is null. Preserved exact result SHA-256 `688F0B1B063F3981A7164672CEAB3EF866FB615AF83CC6888DA0895FE0F634C8` at `original-editor-full-selfcheck-empty-config-first-20260925.txt`. The serialized project `Assets/NTSD/Config/GameConfig/GameConfig.asset` has `BattleContentRuntimeRoot: Assets/NTSD/Content/LoganRuntime`; production Play still selects `GameConfig.Instance`. Pre-change correction: add one SelfCheck-only root resolver shared by geometry and formal fixture checks, using the serialized GameConfig asset via `UnityEditor.AssetDatabase` only when outside Play and runtime singleton root is absent. Do not set a global singleton, alter GameConfig, or provide a Play fallback. Rerun aggregate after compiling; this correction is part of the geometry entry and the adjacent legacy-fixture package.

2026-09-25 shared resolver written exactly within that boundary. No serialized GameConfig field or Play source selection changed; second aggregate result remains pending.
