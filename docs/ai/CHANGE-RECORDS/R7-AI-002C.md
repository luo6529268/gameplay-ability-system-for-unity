# R7-AI-002C — OID6/7/8/11 helper module

<!-- CHANGE-RECORD
id: R7-AI-002C
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Simulation/Ai/AiDecisionRandomStream.cs
code-path: Assets/NTSD/Scripts/Simulation/Ai/AiCharacterDecisionModule.cs
code-path: Assets/NTSD/Scripts/Simulation/Ai/AiDecisionKernel.cs
code-path: Assets/NTSD/Scripts/Test/Editor/AiCharacterDecisionModuleEditorTests.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\input\input_handler.cpp::positions 7-16 / helpers lines 340-603
evidence: SOURCE-CONTRACT-VERIFIED / GENERATED-CSPROJ-COMPILE-0-ERROR / FRESH-UNITY-COMPILE-0-ERROR / FOCUSED-19-OF-19-PASS / AI-BASELINE-212-OF-212-PASS / RED-WITNESSES-STILL-EXPECTED-FAIL / WARMED-0-B / FRESH-DOMAIN-FULL-SELF-CHECK-PASS / UNWIRED-RUNTIME-PENDING
-->

> 创建日期：2026-08-22  
> 最后更新：2026-08-22  
> 类型：AI helper module / test

## 1. 状态与范围

- 当前状态：`RUNTIME_PENDING / UNWIRED MODULE`
- Work Package：`R7-AI-02C`
- 只实现unwired positions7–16模块与共享RNG数据类型；不得改变默认AI输出。

## 2. Authority合同

| Position | helper | 关键合同 |
|---:|---|---|
| 7 | OID6/18 | 两combo early-return；state9 defend为side-effect后继续 |
| 8 | OID7/4/10 frame268–282 | deterministic attack key/prev，true |
| 9 | OID7 close | exclusions→HP/PP/range→Rand5→DUJ |
| 10 | OID7 midfar | conditional Rand20；未命中后Rand100位于dx判断前 |
| 11 | OID7 facing | Rand15位于facing check前 |
| 12 | OID7 frame255–261 | attack true；否则up/down side-effect并继续 |
| 13 | OID8 | Rand250→Rand15→conditional Rand3→Rand50短路顺序 |
| 14 | OID11 first | Rand10；只有facing target才true；DDA仅right-facing分支写 |
| 15 | OID11 frame290 | HitJ=290且targetY<0写defend，void继续 |
| 16 | OID11 DUA | OID11总先Rand5；state16/8只绕过失败门，不绕过RNG |

## 3. Unity 原状

- Legacy与kernel都缺positions7–16；
- 02B已提供HitJ canonical row；
- kernel私有`RngClone`无法被独立实例模块复用；直接复制LCG会形成双实现风险；
- 02A red witness已证明position7缺失，但本包不得让默认red witness转绿。

## 4. 计划改动

- 提取共享`AiDecisionRandomStream`值类型，并机械替换kernel私有clone；
- 新增`AiCharacterDecisionModule`实例类，无static helper、无partial、无每tick new；
- 新增fixed-seed tests覆盖全部10 positions、RNG calls/state、writes、early-return/continuation与0 B；
- 不在kernel/Legacy dispatcher调用模块。

## 5. 不可回退边界

- 不改变现有kernel RNG公式、hash、trace arrays、publish结果；
- 不改outer gate、input edges、positions1–6/17–39；
- 不修改02A red witness预期；
- 不改C++、render、collision、capacity、profile或pass。

## 6. 验收与回滚

验收按Task Contract。回滚只删除两个new runtime files/new test并恢复kernel RNG type名；不得回退02B或其他用户改动。

## 7. 实际改动与证据

已写入共享RNG值类型、unwired实例module和source-derived focused tests；kernel只机械替换RNG类型，默认
dispatcher仍未调用module。首次generated C# build出现16×CS0246，因为Unity尚未导入两个new runtime files，
旧`Assembly-CSharp.csproj`不包含它们；这不是最终Unity compile结果。必须`scope=all`导入并重新生成工程后重跑，
首次失败保留且不得写为PASS。

最终实际改动：

- 新增`AiDecisionRandomStream`值类型；LCG、call count、trace arrays与order hash从kernel私有clone原样迁移；
- `AiDecisionKernel`只机械改用共享stream，未调用新module；
- 新增非partial、非static的`AiCharacterDecisionModule`实例类，按7→16固定顺序执行，position15 void
  side-effect后继续；
- 新增19个source-derived cases，覆盖OID6/18、OID7/4/10、OID8、OID11、每个position、RNG短路、
  early-return遮蔽、continuation、unrelated OID与4096次warmed 0 B；
- 默认Legacy/DataOriented dispatcher均未接线，02A red witness不得转绿。

最终验证：

| 层级 | 结果 |
|---|---|
| new files导入前generated C# build | 16×CS0246，确认旧csproj未包含new files；已留档 |
| `scope=all`导入后generated C# build | 0 error，43 existing warnings |
| fresh Unity compile | DLL 2026-08-23 00:07:15/16，Console error 0 |
| focused helper module | job `b65adcac443844c183272c984934d061`，19/19 PASS |
| combined baseline+02A request | job `202c6992fb784d219c02d1318f605068`：215 total；仅两个02A Explicit red witness按预期FAIL |
| clean AI baseline | job `12463c4731a24aa4ae9919f96599f720`，212/212 PASS |
| warmed allocation | module 4096次0 B；existing warmed CharacterInput PASS |
| fresh-domain full self-check | 2026-08-23 00:10:58 `PASS` |

本包只证明positions7–16 helper实现与source-derived fixtures闭合、且共享RNG refactor未破坏既有baseline。
由于默认dispatcher未接线，`D-INP-007A`仍是production difference；02F、PlayMode与C++ runtime trace未完成。
