<!-- CHANGE-RECORD
id: NTSD28-Q07-DDJ-FORMAL-MANUAL-TRACE-001
status: FOCUSED_TEST_PASS
change-kind: Q07_DDJ_FORMAL_CONTENT_MANUAL_DIAGNOSTIC
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28UnityRawCaptureEditor.cs
authority: formal NTSD2.8-Logan release DDJ 26-tick playback and current staged Logan/project-mode content contract
evidence: original Editor 26-tick Manual PASS; 2192/2192 mapped fields equal; old three-tick regression PASS; COMPARISON-20260925.md
-->

# NTSD28-Q07-DDJ-FORMAL-MANUAL-TRACE-001

2026-09-25 scoped result: original Editor compiled the changed diagnostic script and consumed unique `manual-ddj-20260925-3`; runner PASS with 26 contiguous completed ticks, OID2/OID7 initial roster, project-mode semantic fingerprint `27CAE01489909C46A5145A5867988CCD8C10E20FEF165D847B7AC7A6DE2DE02D`, action495/MP150 at tick6 and OID518/slot50 at tick16. A keyed formal-release comparison found 1425/1425 listed entity fields and 767/767 input/phase fields equal after the documented physical-key mask mapping; exact inputs, field lists, hashes and RNG boundary are in `artifacts/diagnostics/NTSD28-Q07-DDJ-FORMAL-MANUAL-TRACE-001/COMPARISON-20260925.md`. The old `ntsd28-scenario/1.0` three-tick request independently PASSed. Actual changed code is solely `Assets/NTSD/Scripts/Test/Editor/NTSD28UnityRawCaptureEditor.cs`: separate Q07 scenario validation, project-mode catalog/visual candidate publication and logic-only DDJ child capture. No production Driver, DAT, saved Scene or nonbattle code changed. Initial CRT RNG differs because the formal LFR replay defaults seed zero while this Unity fixture has an explicit seed; neither side draws within the 26 ticks. Borrower count is not exposed by this diagnostic, and natural same-world Play/pixels/full RNG remain pending. This Change is `FOCUSED_TEST_PASS` for the diagnostic, not Q07 aggregate completion.

Post-record checks: `Tools/Validate-ChangeLedger.ps1 -RepositoryRoot <repository>` exit 0 (`Change ledger validation PASSED`, 804 current Records, 9 governed code files in the then-current diff; unrelated historical path warnings only); `git diff --check` exit 0 with line-ending conversion warnings only. Current Battle/Menu Scene SHA-256 respectively `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A` and `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13`; `GameConfig.asset` `0527D737A1FA38FC56B51D00DC6E96A421D3C67222546368B147C2D074CB8EA7`, all equal to the saved pre-run values. The three restored progress documents are NUL-free. No new full BattleRuntimeSelfCheck or renderer Play was run for this diagnostic-only change.

2026-09-25 fixture correction before a new request: unique original-Editor `manual-ddj-20260925-2` returned PASS from the capture runner with 26 completed rows, project-mode semantic fingerprint `27CAE014...DE02D`, and no missing tick output. It is **not** a battle parity PASS: Naruto remains in action110 at tick6, while the formal trace reaches action495. Dynamic input/RNG output proves the first diagnostic input discrepancy: the fixture's `K/S/J` becomes raw Unity current masks 32/8/16 with edge-window labels jump/down/attack at ticks2/4/6, whereas the intended physical DDJ route in the existing original-Editor Play is `L/S/K`. The existing `NTSD28UnityRawCaptureEditor.ParseInputKey` deliberately preserves crossed carrier names; changing production mapping or the common parser to fit a test is not authorized. Preserve all -2 raw/domain/input-RNG files and result; create a new immutable -3 scenario with L/S/K on the same held ticks, then rerun the same compiled diagnostic under a new request/output name. Compare formal and Unity typed fields after that run. This is a fixture correction, not a confirmed production first difference.

2026-09-25 pre-edit correction after first original-Editor request: `manual-ddj-20260925-1` was consumed and returned FAIL before tick capture. `SimulationTickDriver.BeginBattleAllocationSeal` requires `ReferenceEquals(animatorManager.PublishedLoganCatalog?.ContentIdentity, dataManager.PublishedLoganContentIdentity)`. The new Q07 route requested only initial character OIDs 2/7, so `UnityCurrentDatScope` built a project-mode `LoganObjectCatalog` and assigned the data manager identity but did not capture/set `diagnosticCandidate` on `CharacterAnimtorManager`; the existing branch only does that when an initially requested object is `SpecialAttack`. This is a diagnostic fixture publication mismatch, not proof of a production battle failure. Preserve the exact FAIL result SHA `059BBAC1DE0FAEDBDB84E9201DF213AD46E2E9A874925C90DCC134EEBB29C1D2` at `manual-ddj-20260925-1.result.txt`. Within the declared Editor tool only, capture the visual candidate for the Q07 DDJ scenario even though its initial two entities are characters, so both owners share one catalog/identity before allocation seal; leave old scenario behavior and production seal unchanged. Compile in the original Editor and use a new -2 request/result name. Do not overwrite the first artifact.

Pre-edit current state: `NTSD28UnityRawCaptureEditor.RunScenario` already accepts `loganRuntimeRoot`, uses a Manual `FrameInputSet` per tick, writes raw/domain/input-RNG lines and invokes ordered shutdown in `TemporarySimulationDriverScope`. Its `ValidateScenario` only admits the legacy `ntsd28-scenario/1.0`, old EXE SHA and frozen three-tick Stage23 values. `UnityCurrentDatScope` currently calls `LoganObjectCatalog.Read(source)` without the project's mode Asset, so this existing Logan diagnostic route does not match the approved production mode source. The saved Battle Scene natural DDJ result differs from the formal fixture at tick0 and samples only 24/26 ticks.

Change only the declared Editor diagnostic file: add a disjoint Q07 DDJ scenario schema with current formal EXE identity and 26-tick fixture, take `ProjectBattleModeConfig.LoadDefault().Capture()` only for that new schema in `UnityCurrentDatScope`, revalidate the same snapshot during the run, and materialize DDJ child logic-only. Leave the old three-tick scenario's identity, validation, content selection and outputs unchanged. The fixture and result artifacts are diagnostic data, not production DAT edits. No new runtime manager or shutdown phase is introduced; the existing temporary Driver scope must perform ordered shutdown, and evidence must expose whether borrowers are actually checked.

Expected side effects: a new opt-in original-Editor request/result and diagnostic outputs only. No battle simulation state is modified outside the temporary diagnostic World. Do not affect Menu, saved Scene, production Driver, old test fixture, content files or Unity/GAS architecture. Risk: the raw recorder may reveal a genuine first difference or fail before child materialization; retain the failure and resolve only from authoritative evidence. Acceptance and rollback are in the parent Task. Script edits occur only after this Record/Ledger/STATE/handoff registration.
