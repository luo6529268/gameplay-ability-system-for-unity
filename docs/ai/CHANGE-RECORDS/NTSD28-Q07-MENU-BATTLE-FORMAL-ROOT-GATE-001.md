<!-- CHANGE-RECORD
id: NTSD28-Q07-MENU-BATTLE-FORMAL-ROOT-GATE-001
status: VERIFIED
change-kind: Q07_MENU_TO_BATTLE_FORMAL_CONTENT_ROOT_GATE
code-path: Assets/NTSD/Scripts/App/AppManager.cs; Assets/NTSD/Scripts/Test/Editor/NTSD28Q07MenuSceneCallbackPlayProbeEditor.cs
authority: user D-023 formal nonexcluded DAT and character-image content scope; BATCH-04/Q07 formal content entry; preserved nonbattle Menu behavior
evidence: original Editor formal-root and empty-root unique Menu callback Play PASS; ACCEPTANCE-PENDING-20260925.md
-->

# NTSD28-Q07-MENU-BATTLE-FORMAL-ROOT-GATE-001

2026-09-25 scoped exit `VERIFIED`: original Editor compiled the production gate and updated existing Menu Play diagnostic. Formal-root unique callback Play PASS with same three publication keys, BattleRunning/World2 and ordered return; empty-root unique callback Play PASS with legacy Menu prewarm/role selection still working, BattleRunning false, World0/Stopped, explicit expected AppManager error, ordered Menu return, pool borrowers0. The diagnostic sets the empty root only in Play memory and restores it in `finally`; Battle/Menu/GameConfig SHA unchanged. Exact JSON SHA and limitations in `artifacts/diagnostics/NTSD28-Q07-MENU-BATTLE-FORMAL-ROOT-GATE-001/ACCEPTANCE-PENDING-20260925.md`. Code paths actually changed: `AppManager.InitializeBattleAsync` two-line root guard; `NTSD28Q07MenuSceneCallbackPlayProbeEditor` current project-mode fingerprint plus request-scoped empty-root observation/restoration. No Menu production code, DAT, images, Scene or GameConfig asset changed. This verifies only the Menu-to-Battle empty-root gate; old direct readers and all deletion gates remain open, Q07 overall remains open. Rollback scope is these exact hunks under repository approval rules.

2026-09-25 pre-diagnostic amendment: the existing Menu Scene callback probe has a stale fixed formal fingerprint and no empty-root mode, so the original Editor cannot give the two required dynamic observations unchanged. Add a request flag to the same diagnostic script: empty-root run sets the selected GameConfig root only in Play memory, retains Menu prewarm/callback chain, verifies rejection before `BattleRunning` and ordered shutdown, and restores the previous value in `finally`; normal mode checks current project-mode fingerprint and existing Battle return. No production Menu/manager/resource edits. This exact test path was registered before editing it. Existing old result files and unique-run guard stay intact.

2026-09-25 implementation: added only an empty `ConfiguredContentRoot` rejection immediately after the prewarmed manager check in `AppManager.InitializeBattleAsync`, before `ValidateConfiguredContentForBattleAsync` and Battle singleton/setup. Existing Menu legacy prewarm, manager validation, formal-root path, and catch/shutdown flow were not edited. Original project Editor refreshed and `Assembly-CSharp.dll` is newer than this source; Editor reports idle/no compilation pending. Scene/Menu/GameConfig hashes are unchanged and recorded in `artifacts/diagnostics/NTSD28-Q07-MENU-BATTLE-FORMAL-ROOT-GATE-001/ACCEPTANCE-PENDING-20260925.md`. Empty-root and nonempty formal-root Menu Play are **not yet observed**; current status is `RUNTIME_PENDING`, no old-file deletion clearance. Risk is intentional rejection of formerly accepted legacy Battle configurations. Rollback remains the exact two-line guard hunk under repository approval rules.

Before edit: `LoadingPrewarmController` can publish legacy Config content for an empty root, and `CharacterAnimtorManager.ValidateConfiguredContentForBattleAsync` can return null in that state. `AppManager.InitializeBattleAsync` currently accepts that result and advances to Battle preparation. Direct Battle test bootstrap already rejects an empty root, so the Menu entry is the remaining observed production battle gate. The serialized project root is nonempty today; no default-path failure is claimed.

Exact change: add a root check in `AppManager.InitializeBattleAsync` immediately after obtaining the prewarmed manager and before its battle validation. Leave Menu prewarm, manager APIs, scene setup and failure-shutdown ordering unchanged. Expected effect: empty-root Menu prewarm remains available for nonbattle use, while Battle cannot start on legacy Config content; formal-root flow remains the same.

Acceptance and rollback are in the same-ID Task. No old DAT/image deletion is authorized. After editing, record compile, focused Play or its absence, scene hashes and risks here before promoting status.
