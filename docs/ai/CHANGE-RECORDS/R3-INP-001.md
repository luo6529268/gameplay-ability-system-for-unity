# R3-INP-001 — complete callback before OID 7/8/51 maintenance

<!-- CHANGE-RECORD
id: R3-INP-001
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Simulation/NTSDBattleTickSystem.cs
code-path: Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\entity\game_tick.cpp::game_tick(...) release live path
evidence: SOURCE-CONTRACT-VERIFIED / UNITY-COMPILE-PASS / FOCUSED-SELF-CHECK-PASS / RUNTIME-PENDING
-->

> 创建日期：2026-08-21  
> 类型：battle / input / scheduler / focused self-check  
> 所属 Work Package：`R3-INP-01`

## 1. Scope

只修 `D-SCHED-005` 的 normal-path scheduler boundary：将 Unity OID 7/8/51 runtime maintenance
从 `CharacterInput` 前移到完整 `CharacterInput` 后。

本 Record 不实现 F1/F2、`NeedClearInput` 语义重写、negative held/caught、dead/respawn AI、
FrameInputSet、Input Action binding、OID maintenance formula、CPoint/held/link/collision/opoint、
render、DAT、scene 或性能变更。

## 2. Authority / source contract

- C++ `src/entity/game_tick.cpp:945-947`：live `game_tick(...)` 接收 `post_cooldown_input` callback；
- `game_tick.cpp:994-1005`：cooldown / F1-F2 gate 后才调用该 callback；
- C++ `src/core/main.cpp:4607-4608`：callback 内先 poll P1/P2；
- `main.cpp:5505-5522`：callback 内对所有 active character DAT 按 slot 进行 AI prepare 与
  `apply_input`；
- `game_tick.cpp:1006-1008`：该完整 callback 返回后才开始 slot `0..19` 的 OID 7/8/51 maintenance。

这只是 C++ source contract；C++ runtime trace 仍由 `R1-WP02` BLOCKED，不能写成 runtime
equivalence。

## 3. Unity current state / minimal target

- 当前 `NTSDBattleTickSystem.RunFrameAdvancePhase` 先运行 `Oid5152RuntimeMaintenance`，再运行
  `CharacterInput`；
- 目标 normal path：`PostCooldownHumanInput → CharacterInput → Oid5152RuntimeMaintenance →
  EarlyFrameAdvance`；
- `NeedClearInput` 属于 Unity battle-entry clear，非 C++ F1/F2 gate。本次必须维持它的
  maintenance-before-clear / return 行为，不能把它移入 normal callback 顺序修复。

## 4. Allowed code changes

| 文件 | 允许改动 |
|---|---|
| `Assets/NTSD/Scripts/Simulation/NTSDBattleTickSystem.cs` | 在 `RunFrameAdvancePhase` 中仅重排 normal-path 的 `CharacterInput` 与 `Oid5152RuntimeMaintenance` 调用；input-clear branch 保持既有 maintenance-before-clear。 |
| `Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs` | 增加一个 OID7/8 input-writer probe：只有 input pass 先把 frame 写到 state2 时，后续 maintenance 才能在同 tick merge，借此锁定相对顺序。 |

## 5. Acceptance contract

| 层级 | 要求 | 初始状态 |
|---|---|---|
| S0 static | normal path 为 HumanInput → CharacterInput → RuntimeMaintenance；clear branch 保持 maintenance → clear → return。 | `PASS (2026-08-22 local static pattern check)` |
| S1 focused fixture | input writer 在同 tick 写 OID7 frame state2，OID maintenance 同 tick merge；旧顺序应不能满足该断言。 | `PASS (2026-08-22 BattleRuntimeSelfCheck request)` |
| S2 regression | 既有 `CheckGameTickInputClearBoundaries` 仍证明 clear branch 在 M1 后 return。 | `PASS (same full self-check request)` |
| S3 compile/self-check | 现有 Unity Editor compile，`BattleRuntimeSelfCheck` request PASS。 | `PASS (UnityMCP refresh/compile; filtered error CS=0; result PASS)` |
| S4 Play Mode | 输入的物理键位与角色表现不在本 Record 独立关闭。 | `PENDING / later R3` |
| S5 C++ trace | `R1-WP02`。 | `BLOCKED` |

## 6. Protected boundaries / stop conditions

- 保留 CentralOnly / Texture2DArray / dynamic Mesh / URP、1.5× visual scale、fixed-world camera、
  capacity、30Hz、FrameInputSet、SoA/ECS、pool、worker、0-GC；
- 不改 `SimulationWorld.Passes.partial.cs` 内 OID merge/split formula；
- 若需要改变 `NeedClearInput`、F1/F2、input asset、AI decision kernel、frame/physics 或任何
  relationship/collision writer，停止并创建独立 Record；
- 不运行、修改、构建或向 C++ Release authority 目录写入；T8 继续暂缓。

## 7. Actual changes (2026-08-22)

- `NTSDBattleTickSystem.RunFrameAdvancePhase`：normal path 现在先执行 `CharacterInput`，后执行
  `Oid5152RuntimeMaintenance`；随后才进入 early-frame pass。
- 同一方法的 `NeedClearInput` branch 保持 `Oid5152RuntimeMaintenance → InputClear → return false`，
  未重写 entry-clear 语义。
- `BattleRuntimeSelfCheck` 增加 scheduler-driven OID7/8 fixture：专用 character 仅在完整
  CharacterInput writer 中写 frame 10/state2；只有 maintenance 位于该 writer 之后，OID7/8 才会在
  tick 2 合并为 OID51。
- 已运行本地静态模式检查，确认上述 normal / clear 两条顺序均存在且 fixture 已注册。

## 8. Actual verification (2026-08-22)

- 通过已打开的 `gameplay-ability-system-for-unity@b1b02287` Unity Editor 执行 UnityMCP
  `refresh_unity(mode=force, scope=scripts, compile=request)`；domain reload 后 Editor ready。
- UnityMCP `read_console(types=error, filter_text="error CS")` 返回 0 条，证明本次 refresh 后没有
  C# compiler error。
- 写入既有 `Temp/NTSD_BattleRuntimeSelfCheck.request`，Editor request runner 实际生成
  `Temp/NTSD_BattleRuntimeSelfCheck.result`，内容为 `PASS`，时间 2026-08-22 00:06:26。
- full Console error read 仍有 3 条：1 条为 UnityMCP domain-reload `disposed object` bridge 日志；2 条为
  `RegistrationRollbackSelfCheckEntity` / mismatched rest binding 的预期负例日志。它们均非 C# compiler
  error，且全量 self-check 已返回 PASS；本 Record 不将 Console 解释为全零。

## 9. Verification / rollback

- 代码后先运行静态顺序检查与 `Tools/Validate-ChangeLedger.ps1`；
- 再通过现有 UnityMCP 对当前 Editor 做 scripts refresh/compile、C# error query 与 self-check request；
- 回滚仅能创建 correction Record，反向恢复本 Record 的 scheduler 调用位置；禁止 destructive Git
  reset/restore。
