<!-- CHANGE-RECORD
id: NTSD28-Q07-EMPTY-ROOT-BATTLE-ENTRY-001
status: FOCUSED_TEST_PASS
change-kind: Q07_FORMAL_ONLY_BATTLE_CONTENT_ENTRY
code-path: Assets/NTSD/Scripts/Test/BattleTestBootstrap.cs
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28B11SourceCacheCallerEditorTests.cs
authority: user D-023 formal NTSD 2.8-Logan DAT/character images; Q07 empty-root caller matrix
evidence: docs/ai/TASKS/NTSD28-Q07-EMPTY-ROOT-BATTLE-ENTRY-001.md
-->

# NTSD28-Q07-EMPTY-ROOT-BATTLE-ENTRY-001

Created before script edits. Current Menu prewarm and direct Battle test bootstrap can load old `Config/data.txt`/indexed DAT/sprites when `GameConfig.BattleContentRuntimeRoot` is empty. The current serialized root is formal, but this historical fallback prevents a safe formal-only content contract and later old-file retirement.

Pre-edit correction after the user's replacement `AGENTS.md`: the initial proposal included `LoadingPrewarmController`, which belongs to the normally excluded Menu/loading flow. No script was edited under that proposal. This Record now owns only the direct Battle test-bootstrap method and a focused test; the Menu path remains a separate Q07 review item. This keeps the implementation within the user's battle scope and avoids an unapproved cross-module modification.

Declared change: make missing formal root an explicit early configuration failure in `BattleTestBootstrap.LoadCharacterDataAsync`; preserve its nonempty-root formal publication path and AppManager lifecycle. Add one focused test to the existing source-caller fixture to prove no old content is loaded when the root is empty. No Menu UI/loading flow, DAT values, Scene, Prefab, images, GameConfig asset or production battle rules change. Expected side effect: historical empty-root direct Battle test starts fail explicitly; old Editor/SelfCheck fixtures are independently owned and remain available until migrated.

Acceptance and risk/rollback are in the Task. Actual symbols, diff, original Editor compile, focused results, Scene hashes, validator output and remaining gaps will be appended after edits. Keep status honest; this Task does not authorize old asset deletion or Q07 aggregate closure.

Code written 2026-09-25, before validation: `BattleTestBootstrap.LoadCharacterDataAsync` now rejects an empty formal root with an explicit configuration error before any old index or image loader; for a nonempty root it calls the same existing `PrewarmConfiguredLoganContentAsync` as before. The obsolete direct-bootstrap `IsPrewarmCompleted`/legacy Parse/Apply/Sprite fallback is removed. `NTSD28B11SourceCacheCallerEditorTests.DirectCaller_EmptyRootRejectsLegacyFallback` checks rejection and unchanged unloaded data/publication state; the existing `DirectCaller_LoadsTheSameConfiguredSource` covers the formal path. No Menu/loading-flow script, production battle logic, asset or Scene change. Unity compile/test, Scene hashes and ledger checks remain pending at this point.

Validation update 2026-09-25: original project Editor PID 11944 automatically compiled this working tree; `Editor.log` records `*** Tundra build success (36.53 seconds), 9 items updated, 1662 evaluated` and no `error CS` in that build segment. This is a script compile result only: the Editor remains in assembly reload (`Compiling Scripts` title), its local bridge is not listening, and the two focused EditMode tests have **not** run. The log also reports four `VirtualArtifacts/Primary/...` load failures during reload; ownership/cause is unproven, so do not infer a source-code failure or clear Library. Menu and Battle Scene disk SHA-256 respectively remain `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13` and `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`; scoped `git diff --check` exited 0. `Tools/Validate-ChangeLedger.ps1` exited 0 (`Change ledger validation PASSED`), with existing unrelated historical-record warnings; raw output is in `artifacts/diagnostics/NTSD28-Q07-OLD-ASSET-REFERENCE-REFRESH-001/q07-empty-root-ledger-validation.log`. Focused tests, Editor idle/Console, direct Battle Play and formal EXE A/B remain unverified. Status is `COMPILE_PASS`, not Q07 closure.

Later correction: PID 11944 completed reload and again displays the original `NTSD_Battle` scene. The earlier reload-stalled observation is historical. No local MCP bridge listener returned; `unity status --format json` reports `STATUS_NO_INSTANCES` because this Unity 2022 project has no Pipeline connection. Thus the two focused EditMode tests and live Console remain unverified, despite the Editor running. Do not launch another Editor against this Library merely to run them.

Pre-edit validation-entry amendment 2026-09-25: extend the already declared Editor test file with a request-file TestRunner wrapper, fixed to the two direct caller test methods. Existing request-driven Editor test runners in the same Test/Editor module establish the pattern. The wrapper creates/removes only its own `Temp/NTSD28-Q07-EmptyRootBattleEntry.request` and `.result` files, registers/unregisters TestRunner callbacks, writes pass/fail detail, and does not touch battle production or Scene. This is required because the original Editor is live but has no MCP/Pipeline listener. After implementation run the request in that Editor, inspect result/Console/log, repeat compile/diff/ledger and Scene hashes. Status is `IN_PROGRESS` until new code and evidence are recorded.

Actual validation-entry code: `NTSD28Q07EmptyRootBattleEntryRequestRunner` added in the already-owned Editor test source. It polls uniquely named `Temp/NTSD28-Q07-EmptyRootBattleEntry-*.request` files and runs only the two declared direct-caller EditMode tests; writes sibling `.started` and `.result` files without deleting or overwriting previous runs. TestRunner callbacks report pass/fail and failure detail, then unregister and release the runner. The pre-edit note's fixed-name delete behavior is superseded by this safer per-run file protocol. Original Editor recompilation and focused result are pending at this instant; no production, DAT, Scene or Menu script was edited in this substep.

Run request `Temp/NTSD28-Q07-EmptyRootBattleEntry-20260925-004534.request` was created after the code edit. Original Editor PID 11944 is responsive and shows `NTSD_Battle`, but `Editor.log` remains at 00:33:04, earlier than the 00:43 script edit; no fresh compilation, `.started` or `.result` can yet be claimed. Unity may not have refreshed the modified script while unfocused; that is an inference, not a confirmed setting. Do not start a second Editor or call this request a test run until its `.result` exists and fresh assembly compilation is observed. Status `CODE_WRITTEN`.

Transport diagnosis 2026-09-25: current Unity EditorPrefs show MCP `UseHttpTransport=1`, local URL `http://127.0.0.1:8080`, `AutoStartOnLoad=1`, and stored stdio port 6403. The package `StdioBridgeHost.ShouldAutoStartBridge` returns false in HTTP mode, explaining why port 6403 is not expected to listen; the configured local HTTP server on 8080 also refuses connections and has no listener. This is a read-only diagnosis of unavailable control, not proof of the HTTP auto-start failure cause. `Editor.log` and the queued request remain unchanged, so the current script has still not recompiled or run.

Latest governance check: `Tools/Validate-ChangeLedger.ps1` now exits nonzero on six **other** active Change IDs missing from `docs/ai/STATE.md` (`BATTLE-CENTRAL-EDITOR-FOOT-MARKER-PREVIEW-001`, `BATTLE-CENTRAL-EDITOR-PREVIEW-001`, `BATTLE-CENTRAL-FOOT-MARKER-ANIMATION-001`, `BATTLE-CENTRAL-FOOT-MARKER-GAMECONFIG-001`, `BATTLE-CENTRAL-RUNTIME-FOOTSELF-001`, `BATTLE-RUNTIME-ORDERED-SHUTDOWN-001`). This Q07 ID remains present in Ledger/STATE/handoff and the scoped code diff check exits 0. The unrelated status reconciliation is not silently changed by this Q07 Task; overall validator status is **FAILED**, so do not report this Change as deliverable. Original Editor focused tests are also pending.

2026-09-25 isolated compile update: the current `BattleTestBootstrap.cs` and Editor test source were compiled with the original project's Unity-generated Roslyn response files, with only output/reference destinations redirected to a task-specific `Temp` directory. Runtime and Editor assemblies both exited 0 with zero C# errors (24 and 6 warnings respectively); exact commands, response files, source SHA-256 and logs are preserved in `artifacts/diagnostics/NTSD28-Q07-EMPTY-ROOT-BATTLE-ENTRY-001/isolated-compile-20260925/ACCEPTANCE.md`. This is **isolated compile evidence**, not original Editor reimport, focused NUnit, SelfCheck or Play evidence. Original Editor PID 11944 remains responsive but the queued two-test request has not started or produced a result. Keep status `CODE_WRITTEN`; the global ledger failure and three damaged progress documents also remain unresolved.

2026-09-25 original-Editor focused correction: PID 11944 later imported the current test source and consumed the queued two-test request. First run returned 1 pass/1 error: the new empty-root rejection test passed, while the existing formal-root test failed before its assertion because a shared historical helper required absent `Temp/NTSD28PngAlpha/fixture.dat`. That fixture was created in Temp with the same 6×2 RGBA pixel layout as its owner test's `OneTimeSetUp`; no source or existing file was overwritten. A second unique request returned **2 passed / 0 failed / 0 skipped / 0 inconclusive** for the two exact tests. Original Editor Tundra builds succeeded; Menu/Battle Scene SHA-256 and no-diff state remained unchanged; scoped `git diff --check` exited 0. Exact first failure, fixture hash, second result and boundary are archived at `artifacts/diagnostics/NTSD28-Q07-EMPTY-ROOT-BATTLE-ENTRY-001/original-editor-focused-20260925/ACCEPTANCE.md`. Status advances to `FOCUSED_TEST_PASS` for this bounded test-bootstrap behavior only. Global `Tools/Validate-ChangeLedger.ps1` still fails because NUL-truncated live `STATE.md` omits many active IDs; governance delivery, direct Battle Play, SelfCheck, original EXE A/B and Q07 aggregate remain pending.

2026-09-25 direct Battle Play update: original Editor PID 11944 entered the clean `NTSD_Battle` Scene via local MCP bridge, completed formal PNG prewarm and logged `SetupTestCharacters called`, presentation enabled, `SimulationTickDriver resumed`, and `Test bootstrap complete`. After controlled stop, Editor was idle/out of Play, Scene `isDirty=false`, and Battle/Menu SHA-256 unchanged. This witnesses the nonempty formal-root startup path in actual Play, while the separate 2/2 focused tests cover empty-root rejection. Audio prewarm reported missing `c\\tobi\\w\\sp.wav` and similar WAVs (Q10 owner); a read-only live-tick reflection command failed before execution due the Windows command-line limit. Exact scope and evidence: `artifacts/diagnostics/NTSD28-Q07-EMPTY-ROOT-BATTLE-ENTRY-001/original-editor-direct-play-20260925/ACCEPTANCE.md`. Keep Change status `FOCUSED_TEST_PASS` because the NUL-corrupted live progress docs still block the mandatory ledger validator; Q07 aggregate, SelfCheck, formal EXE A/B and Q10 remain open.
