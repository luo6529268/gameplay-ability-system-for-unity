# R8-COL-005B-001 — generic kind1 attacker dispatch

<!-- CHANGE-RECORD
id: R8-COL-005B-001
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Animation/Character/BruteForceSceneQuery.cs
code-path: Assets/NTSD/Scripts/Animation/LF2Objects/LF2WeaponInteractionResolver.cs
code-path: Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\entity\collision_collect.cpp:200-220 and collision.cpp:921-1081
evidence: SOURCE-CONFIRMED-DIFFERENCE / CURRENT-UNITY-DAT-ITR-KIND1-COUNT-0 / COMPILE-PASS-2026-08-23-162101 / FULL-SELF-CHECK-PASS-2026-08-23-162157 / PLAY-UNREACHABLE-CURRENT-ASSETS / CXX-TRACE-BLOCKED
-->

> 创建日期：2026-08-23  
> 所属Work Package：`R8-WP01G-R02`  
> 当前状态：`RUNTIME_PENDING`

## Authority / requirement

C++ kind1 selector读取通用Entity keys，consume为generic grab；pickup只在kind2/7。Unity selector按CLR
character gate keys，weapon case1进入pickup。用户授权处理`D-COL-005B`，本Record只闭合这两个kind1差异。

## Unity before change

- `IsLeftPressed/IsRightPressed`只接受`LF2Character`；
- `LF2WeaponInteractionResolver` case1调用`HandlePreInteractionKind1` pickup helper；
- common `BattleInteractionWriter.TryApplyGrab`已支持generic kind1，并由05A证明non-character target字段写入；
- block-aware全DAT扫描：`itr kind1=0`，current authored route不可达。

## Planned files / symbols

- `BruteForceSceneQuery.IsLeftPressed/IsRightPressed`；
- `LF2WeaponInteractionResolver.DispatchInteractionByKind` case1；
- `BattleRuntimeSelfCheck.CheckKind1NonCharacterTargetConsumeContracts`新增weapon-attacker case。

## Expected side effects

- generic entity只要runtime key已存在即可按C++ selector参与kind1；
- weapon attacker kind1不再错误拾取character，而是generic grab；
- kind2/7、normal attacks、candidate order/RNG/vrest不变；
- current authored assets因无itr kind1不改变现有战斗场景。

## Protected boundaries

不改C++、DAT、input producer、kind2/7、held/pool、pass order、render、容量、F1/F2 debug。

## Acceptance

- source/static、block-aware DAT inventory；
- weapon attacker collect→object consume focused self-check；
- compile0、full self-check、ledger validator、diff check；
- current production Play不可达与C++ trace BLOCKED如实保留。

## Rollback

仅回退本Record两处production分支、一个self-check case及同一Change文档，不触及工作树其他修改。

## Actual changes / verification

- kind1 selector left/right改为读取通用`LF2Entity.Runtime` key；
- weapon consumer case1改用`BattleInteractionWriter.TryApplyGrab(...,1)`；
- self-check新增actual weapon CLR attacker的collect→ObjectInteraction→generic grab矩阵，并断言未进入held route；
- scoped diff-check exit0，仅既有LF→CRLF warning；
- UnityMCP fresh compile程序集时间16:21:01/02，0 compile error；
- full self-check于16:21:57写入`PASS`；
- Change Ledger validator 75 records / 75 governed files PASS；
- current DAT `itr kind1=0`，production Play不可得，C++ trace继续BLOCKED，故保持`RUNTIME_PENDING`。
