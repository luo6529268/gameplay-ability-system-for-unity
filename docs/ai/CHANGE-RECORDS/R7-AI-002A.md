# R7-AI-002A — C++ 39-position authority fixture

<!-- CHANGE-RECORD
id: R7-AI-002A
status: VERIFIED
code-path: Assets/NTSD/Scripts/Test/Editor/AiDecisionAuthorityChainContractEditorTests.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\input\input_handler.cpp::prepare_ai_input positions 1-39
evidence: SOURCE-CONTRACT-VERIFIED / FRESH-EDITOR-COMPILE-PASS / NORMAL-CONTRACT-PASS / TWO-EXPLICIT-RED-WITNESSES-CAPTURED / AI-BASELINE-75-OF-75-PASS / FULL-SELF-CHECK-PASS / TEST-ONLY-VERIFIED
-->

> 创建日期：2026-08-22  
> 最后更新：2026-08-22  
> 类型：test / AI input

## 1. 状态与范围

- 当前状态：`VERIFIED / TEST-ONLY RED-WITNESS CONTRACT`
- Work Package：`R7-AI-02A`
- 只允许新增source-derived Editor fixture；production AI不得修改。

## 2. Authority / 需求依据

- C++ `input_handler.cpp:2055-2204`：一个outer random gate内39个ordered positions；
- position7 OID6 helper见`input_handler.cpp:340-367`；
- position28 OID33 helper见`input_handler.cpp:824-851`；
- 当前Unity缺position7，并把position28放在outer gate外，已登记`D-INP-007A/B`。

## 3. Unity 原状

- `AiDecisionKernel`与Legacy均只在outer gate内执行positions1–6；
- position28/38/39在gate外；
- 现有75/75 profile tests共享同一缩减oracle；
- 当前没有独立C++顺序表或source-derived红测。

## 4. 计划改动

| 文件 | 改动 | 目的 |
|---|---|---|
| `Assets/NTSD/Scripts/Test/Editor/AiDecisionAuthorityChainContractEditorTests.cs` | 新建39-position顺序表与3个tests | 固定authority contract；定向证明missing/gate差异 |

两个差异witness使用`Explicit`，避免日常完整suite永久红；仍必须被精确选择并实际捕获FAIL。

## 5. 不可回退边界

- 不修改`AiDecisionKernel`、`SimulationWorld.AiInput`、snapshot/rows/RNG；
- 不实现任何缺失helper；
- 不改profile、pass、capacity、render、worker、C++；
- 不把red witness或existing Unity parity升级为完整C++对齐。

## 6. 实际改动

| 文件 | 实际改动 | 生产副作用 |
|---|---|---|
| `Assets/NTSD/Scripts/Test/Editor/AiDecisionAuthorityChainContractEditorTests.cs` | 新增39-position ordered table；新增普通contract test；新增OID6 position7与OID33 position28两个`Explicit` source witness | 无；仅test assembly |
| 对应`.meta` | 固定Unity GUID | 无 |

初次fixture错误地漏计outer gate前的boundary `Rand(ai.Rand5+8)`：seed8的OID6测试虽FAIL，但第三raw=311、
`%10==1`，不能作为有效authority red；seed4的OID33测试PASS也不能裁决gate difference。该首次结果已作废但保留。

修正后OID6使用seed=23：boundary→outer→helper第三raw=6130、`%10==0`；C++应写DRJ并以3次RNG退出。
OID33使用seed=5、difficulty1：boundary后outer raw=28693、`%6==1`，第三raw=12255、`%5==0`；C++应
跳过position28，而当前gate外Unity实现会命中。

## 7. 验收与证据

| 层级 | 状态 |
|---|---|
| source合同 | `PASS`；position/seed/条件已从C++逐项闭合 |
| 初次MCP filter | 新文件尚未被AssetDatabase导入；三个exact请求均`total=0`，作废；`scope=all`后Editor DLL更新 | `INVALIDATED / FIXED` |
| Unity编译 | `Assembly-CSharp-Editor.dll` 22:57:58晚于源码22:57:48；Console 0 error/warning | `PASS` |
| normal 39-position contract | job `d2e5b878dab74cb3a04dd0e4920a1a17`：1 PASS，2 Explicit skipped | `PASS` |
| 首次Explicit尝试 | OID6 FAIL但seed oracle无效；OID33 PASS；已作废并修正boundary draw计数 |
| position7 red witness | job `ae7bf5e441c845628067227aacd36c81`：expected DRJ=3 / actual=0 | `EXPECTED FAIL CAPTURED` |
| position28 red witness | job `8ba6322f7a984f669afd80610bad5c5e`：expected DUA=0 / actual=3 | `EXPECTED FAIL CAPTURED` |
| existing AI regression | job `0417660e5b6440c98d93e2c0fb7c8ae1` | `75/75 PASS` |
| fresh-domain full self-check | `Temp/NTSD_BattleRuntimeSelfCheck.result` 2026-08-22 23:01:13 | `PASS` |
| existing AI regression / full self-check | 待执行 |

## 8. 风险与回滚

- 风险：fixture条件若触发common tail其他分支，会模糊first difference；通过combo字段与固定seed隔离。
- 回滚：仅删除本Change ID新增测试文件和对应文档行。
- 未关闭：02B～02F、PlayMode/C++ runtime trace、完整AI。

## 9. Git / 交接

- 工作树已有大量用户/历史改动；只新增目标test文件。
- 不提交、不清理、不回滚用户修改。

本Record的`VERIFIED`只表示source-derived test contract和red witnesses已经正确建立；production
`D-INP-007A/B`仍是confirmed open difference，不能写为已修复。
