<!-- CHANGE-RECORD
id: NTSD28-Q09-SAME-Z-NATURAL-TICK-PIXEL-WITNESS-001
status: VERIFIED
change-kind: TEST_ONLY_BATTLE_SCENE_NATURAL_TICK_PIXEL_WITNESS
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28Q09SameZFormalSpritePixelProbeEditor.cs
authority: Formal NTSD2.8-Logan playable render_snapshot.cpp equal-Z physical-slot order and existing controlled CentralOnly witness
evidence: docs/ai/TASKS/NTSD28-Q09-SAME-Z-NATURAL-TICK-PIXEL-WITNESS-001.md
-->

# Q09/P-04 正式武器完整 tick 遮挡见证

Pre-change: the existing Editor-only probe proves controlled central GPU overlap for formal OID120/121 on frame0. It pauses logic and publishes diagnostic presentation tick labels, so it cannot prove a naturally advanced battle tick. Original Battle Scene camera components are enabled and Scene Git diff is empty. The prior probe is untracked user-visible work and must be preserved.

Affected symbol and path: extend only `NTSD28Q09SameZFormalSpritePixelProbeEditor` in the declared test-only script. Add an opt-in natural-tick request and distinct result/images; preserve current controlled `Run`, request/result and acceptance. No production API, DAT, `.meta`, Scene, Prefab, UI, ProjectSettings, or camera enabled mutation.

Expected side effects: one paused production full tick, temporary OPoint-owned entities and central publication, temporary camera target/culling/clear state for GPU readback, and diagnostic output under the named artifact folder. No disk Scene write. The full tick may advance baseline actors in the transient Play World; exiting Play disposes it, and the report must distinguish in-session count/pool cleanup from a full World rollback.

Acceptance and rollback: use the Task Contract. First verify original Editor compile, then one focused original Scene Play with actual tick-indexed central plan, submitted overlapping formal body commands, restricted four-image oracle, and Play exit/Scene SHA. Run Change Ledger validator and diff check. Keep P-04/Q09 open pending Legacy and formal EXE parity. No destructive rollback without user approval.

Current result: CODE_WRITTEN. The existing Editor-only probe now has a separate request/Menu entry and artifact route for one complete production tick. It records synchronous versus dedicated-worker path, waits for an actual tick-indexed CentralOnly plan, captures that composite before any diagnostic publication, then takes controlled post-tick baseline/A-only/B-only references and applies the overlap-only oracle. The original controlled route remains unchanged. No production/Scene/DAT/Camera enabled edits. Original Editor compile, focused Play and cleanup have not yet been verified.

First original Editor refresh compiled the new route with 0 Console errors. The first focused Battle Scene Play reached real tick5→6 on the synchronous complete-driver path, kept formal OID120/121 in slots51/52 and advanced both to frame1/pic1. Central commands were materialized in greater-slot-first order (indices1/3). The probe then failed its screen-overlap precondition because its new natural route projected bounds before initializing `captureHeight`; the existing controlled route had initialized it during prior captures. It reported Z0 after the full tick, but this alone is not the projection failure. Cleanup restored object/slot/two-pool counts and pause; Play exited, original Scene SHA remained `9409F2...3B3A39` with no Git Scene diff. Archived exact result as `artifacts/diagnostics/NTSD28-Q09-SAME-Z-NATURAL-TICK-PIXEL-WITNESS-001/first-zero-height-projection-failure.json`. The test-only route now computes capture height before projected-bounds validation. Recompile and rerun pending; first FAIL is not a painter-order result.

Second original Editor refresh completed domain reload; two bridge requests sent together during reload produced MCP `Cannot access a disposed object` transport logs, not C# compile errors. The rerun request was consumed but the probe rejected the pre-Play Scene because its in-memory `Scene.isDirty` was true. Disk SHA and Git Scene diff remained unchanged. A test-only, read-only Scene dirty-state report entry was added to identify whether this came from Camera/Bootstrap or other unsaved Scene objects before any reload or retry. No unsaved Scene state has been discarded. Second result remains in Temp until archived.

The second pre-Play failure is archived as `second-preplay-scene-dirty-failure.json`. After the next domain reload, read-only `Inspect Same Z Scene Dirty State` reported `sceneDirty=false`, both HUD/world Cameras enabled, and only ordinary CanvasRenderer dirty flags; report copied as `scene-dirty-after-domain-reload.json`. Separately, another task changed the original Battle Scene disk to SHA `56B49C593E5C8CDD9065E6DF74302D0D95AA2162896E18D70DF654C3844A146B` (UI hierarchy/position changes, 107 added/248 removed lines). The current Editor hierarchy shows new `ComboPanel` and `BattleControls`, so it has loaded that revision. This Change ID did not author or revert those Scene changes. The next run must capture that fresh SHA and reject any mid-run disk change; old SHA is historical only.

Third focused Play used the new original Scene revision and reached real tick5→6. Formal OID120/121 were both frame1/pic1 at equal Z240, with actual central commands slot52/index1 before slot51/index3 and projected body intersection x[1000,1069), y[550,606). The actual-tick composite GPU capture was saved (8553 nonclear pixels), but the subsequent diagnostic reference capture failed because `ObserveCapture` still required the pre-step `realTick` instead of the post-step `naturalTick`. This is an oracle branching defect; it does not invalidate the tick-indexed command observation and cannot establish pixel order without the remaining reference captures. Archived `third-stale-real-tick-oracle-failure.json` and `third-natural-composite-only.png`; cleanup counts/pools/pause passed and Scene disk SHA stayed `56B49C...A146B`. The test-only tick guard now selects the correct expected tick per mode. Recompile and one focused rerun pending.

Review before rerun found another oracle risk: the reference stage had reset both objects to pre-tick fixture Y/Z instead of the exact post-tick positions used by the natural composite. The test-only route now saves each formal weapon's post-tick precise X/Y/Z and restores that position whenever that weapon is visible in A-only/B-only references; only hidden X moves offscreen. This preserves frame1 geometry for the four-image comparison. The original controlled route is unchanged.

Current original Editor Scene is dirty in memory after the third Play/domain reload, while Scene disk SHA remains `56B49C...A146B`. Read-only dirty inspection names `Canvas`, `ComboPanel` and UI RectTransform/CanvasRenderer instances as dirty; both Cameras remain enabled. These UI changes belong to another active task and are preserved. A further Play will wait for a clean, stable original Scene rather than discarding unsaved UI edits. No permission is inferred to reload or save over that task's Scene state.

Final test-only code revision compiled in the original Editor after domain reload with 0 Console errors. The Scene remains in-memory dirty, so no fourth Play or final four-image oracle is claimed. Status is `RUNTIME_PENDING`; actual-tick CentralOnly body command order is observed, but natural-tick GPU pixel, Legacy and formal EXE A/B are open. Detailed scoped progress and preserved failed results are in `artifacts/diagnostics/NTSD28-Q09-SAME-Z-NATURAL-TICK-PIXEL-WITNESS-001/PROGRESS.md`.

The independent UI owner saved the Scene again at 21:35; latest observed disk SHA `25BA977C5D59452D179E636E2333151F4E462415D49245FFB47339B9F3D9CB74`, both Camera components serialized enabled. Third-run SHA `56B49C...A146B` is no longer the current baseline. Repository-wide `git diff --check` reports trailing whitespace solely in that parallel Scene YAML; this Change does not alter/format it. Change Ledger validation PASS (792 records, this code path covered). Scoped tracked-governance diff check and untracked text whitespace scan both passed.

Fourth focused original Scene Play (SHA `25BA977C...D9CB74`) completed the natural tick and all four same-Play images; both weapons were frame1/pic1, same Z240, ordered slot52/index1 before slot51/index3, projected overlap x[1000,1069) y[550,606), cleanup/Scene hash passed. The old clean-background oracle correctly refused acceptance with zero candidates. Independent Pillow inspection of saved PNGs found 463 baseline pixels already nonclear within the body intersection and nonclear content in both supposed single-weapon references, so these four images do not isolate individual contributors. Archived exact JSON and four images as `fourth-*`; this is a test-method problem, not a proved production painter defect. To obtain valid natural-tick references, the next test-only edit will use four separate original-Scene Play sessions (`baseline`,`a`,`b`,`both`), each starting from the same Scene and stepping exactly one full tick, with variant-specific captures and cross-run identity/cleanup gates. Existing controlled variant remains unchanged. No production, DAT or Scene edits are allowed. This Change remains `RUNTIME_PENDING` until the four-run pixel oracle passes or a real first difference is identified.

Test-only separate-session revision written: the same request file now accepts only `baseline`, `a`, `b` or `both` (invalid text maps to a safe diagnostic filename and fails closed). Each variant spawns only its declared formal weapon(s), advances one complete tick, validates actual central plan and submitted body when present, writes one variant-specific GPU PNG and result with Scene/tick/RNG identity, then cleans up/exits Play. The original controlled four-stage branch was restored byte-for-byte in `SpawnFixtures`/`QueueCapture` semantics and remains a separate diagnostic. No production, DAT, Scene, Camera enabled, UI or ProjectSettings edit. Original Editor compile and all four focused runs still pending for this revision.

Pre-compile design correction: to keep physical slots and RNG/entity topology identical across all four sessions, every variant must spawn both formal weapons in the same order. The selected weapon(s) use fixture X1100; unselected weapon(s) use in-bounds reference X1400, far outside the body-overlap region. This supersedes the preceding sentence's "spawns only declared weapon(s)" design; the test code will be adjusted before validation. The original Editor process remained alive/responding and its bridge port listening, but two read-only bridge calls timed out after the refresh; no compile result has yet been established for the separate-session revision. Do not send Play requests until editor state and script compilation are verified.

Final separate-session revision compiled in the original Editor with 0 Console errors. It retains the original controlled branch and uses the same two formal weapons/slots for all four variants, moving unselected weapons to X1400. The first `baseline` original-Scene Play returned `PASS_CAPTURE` under Scene SHA `9C7EDD3A...BA6CFD7D`: real tick5→6, formal frame1/pic1, both X1400, slots51/52, RNG identities recorded, object/slot/two-pool/pause cleanup and Scene hash passed; Editor exited Play and Scene was clean. The exact JSON/PNG were archived. Before `a` request, another task saved the Scene to SHA `711FE77B...E5D605457` at 21:58:56; both serialized Cameras remained enabled. The four-way same-Scene set is interrupted, so no natural GPU order verdict exists. Preserve this single capture as partial evidence and restart all four only on one stable Scene revision.

2026-09-24 final scoped result superseding the prior interruption: original Editor completed `baseline/a/b/both` as four separate Battle Scene Play sessions under identical disk SHA `471396E7692BA0C4B5CC8C2CD1355D4CF88B8AB2ACF23A6CAE6BF1E6475BC7B9`. All four actual tick5→6 captures returned `PASS_CAPTURE`, shared starting RNG and slot51/52 generation1, formal frame1/pic1, equal Z240, central body slot52/index1 before slot51/index3, unchanged Scene SHA, restored pause/counts/pools and exited Play; Editor Scene ended clean. Eight raw JSON/PNG files are archived under `scene-471396E7-series/`. The original exact-color-only probe field stayed zero because 463 stable nonwhite underlay pixels and five filtered-edge composites defeat its simplistic predicate; it is **not** reported as a passing in-probe oracle. An independent four-image analysis instead evaluated **all** 82 pixels where both single-sprite captures differ from the underlay and each other. Every pixel fits the OID120-later bounded RGB composition model within one 8-bit level; 44 reject the opposite order, zero uniquely prefer it, 38 are nondiscriminating. The five non-exact composites are modeled edge blends, not counterexamples. Formula, region, hashes and evidence limits are in `scene-471396E7-series/ANALYSIS.md`. This Change is `VERIFIED` only for the natural complete-tick CentralOnly GPU same-Z body-order witness; Legacy and formal EXE visual A/B remain Q09/P-04 exits. No production, DAT, Scene, Camera enabled or nonbattle code changed for this package.
