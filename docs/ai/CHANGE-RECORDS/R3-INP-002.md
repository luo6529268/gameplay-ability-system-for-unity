# R3-INP-002 — default F1/F2 step gate before / after render boundary

<!-- CHANGE-RECORD
id: R3-INP-002
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Simulation/NTSDBattleTickSystem.cs
code-path: Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\entity\game_tick.cpp::game_tick(...) release live path
evidence: SOURCE-CONTRACT-VERIFIED / STATIC-PASS / UNITY-COMPILE-PASS / FOCUSED-SELF-CHECK-PASS / RUNTIME-PENDING
-->

> 创建日期：2026-08-22  
> 类型：battle / scheduler / input gate / focused self-check  
> 所属 Work Package：`R3-INP-02`

## 1. Scope

只实现 `D-SCHED-010` 的 default `g_dword_449048 == 0` F1/F2 step-gate scheduler adapter：

- 每 tick reset `BattleStepGate`，mode 2 转换为 gate 1 / mode 1；
- F1 wait 跳过完整 human/AI character input callback；
- wait tick 仍通过 OID、frame/interaction、preframe/stage/RenderDispatch，RenderDispatch 后返回；
- `NeedClearInput` 继续保留它的 bootstrap M1→clear→return 契约；
- 新增 F1 wait、F2 one-step 与 entry-clear focused fixture。

不实现 physical F1/F2 / A-B-C debug unlock、`g_dword_449048 != 0`、negative-link input、dead-AI
input、FrameInputSet、CPoint/held/link/collision/opoint、frame physics、render implementation、DAT、scene
或资源。

## 2. Authority / source contract

- C++ `src/core/main.cpp:159-166`：F1 0/1 toggle、F2 写 mode 2；
- C++ `src/entity/game_tick.cpp:994-1005`：每 tick reset gate；mode2 → gate1/mode1；wait 时 skip
  post-cooldown input callback；
- `game_tick.cpp:145-156, 1466, 1878`：后续 recovery / held 消费既有 step-wait read；
- `game_tick.cpp:2066-2077`：default flag=0 wait tick 在 render 后 early return；
- Unity `BattleRuntimeState.cs:305-306`：现有 snapshot/checksum-owned Flow scalar；
- Unity `SimulationTickDriver.cs:1213` 与 `NTSDBattleTickSystem` clear branch：`NeedClearInput` 是独立
  Unity bootstrap clear，必须维持现有优先行为。

C++ runtime trace 仍被 `R1-WP02` BLOCKED；本 Record 只获得 source authority，不能写为 runtime
equivalence。

## 3. Allowed code changes

| 文件 | 允许改动 |
|---|---|
| `Assets/NTSD/Scripts/Simulation/NTSDBattleTickSystem.cs` | 使用现有 Flow step fields，增加无分配 step-gate producer / predicate；仅在 scheduler 中跳过 input，并在 RenderDispatch 后结束 wait tick。 |
| `Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs` | 增加派生 character probe 与 F1/F2/entry-clear scheduler fixture。 |

禁止修改 `BattleRuntimeState.cs`、lockstep snapshot、Input Action asset、`SimulationWorld.Passes.partial.cs`、
AI kernel、renderer、scene、DAT 或 C++。

## 4. Exact target behavior

```text
normal: cooldown → gate reset → human poll → CharacterInput → M1 → ... → Render → tails
F1 wait: cooldown → gate=0 → skip human/CharacterInput → M1 → ... → Render → return
F2 step: cooldown → mode2=>gate1/mode1 → human/CharacterInput → M1 → ... → Render → tails
entry clear: preserve pre-existing human poll → M1 → clear character-DAT input → return
```

The simultaneous `NeedClearInput + F1 wait` combination is Unity-only. This Record preserves entry-clear
priority and does not claim it as a C++ F1/F2 fact.

## 5. Acceptance contract

| 层级 | 要求 | 初始状态 |
|---|---|---|
| S0 static | Scheduler has gate reset / F2 conversion, F1 input skip, render-after-return, and unchanged clear branch. | `PENDING` |
| S0 static | Scheduler has gate reset / F2 conversion, F1 input skip, render-after-return, and unchanged clear branch. | `PASS (2026-08-22 local static contract)` |
| S1 F1 fixture | input counters remain 0; M1 decrement + RenderDispatch occur; frame-post/late counters remain 0. | `PASS (2026-08-22 request self-check)` |
| S2 F2 fixture | mode=2 becomes mode=1/gate=1; input / postrender tails run; next tick returns to wait. | `PASS (2026-08-22 request self-check)` |
| S3 clear fixture | NeedClearInput keeps M1→clear→return even when Flow mode is wait. | `PASS (2026-08-22 request self-check)` |
| S4 compile/self-check | UnityMCP scripts compile, filtered `error CS`, request self-check. | `PASS (filtered `error CS`=0; result PASS)` |
| S5 Play Mode | physical F1/F2 mapping / pause overlay. | `PENDING / out of scope` |
| S6 C++ trace | same fixture trace. | `BLOCKED (R1-WP02)` |

## 6. Actual changes (2026-08-22)

- `NTSDBattleTickSystem` 在 cooldown 后使用现有 Flow scalar reset/advance step gate；wait tick 跳过
  human poll / CharacterInput，F2 mode 2 转 mode 1/gate 1；
- `RunFrameAdvancePhase` 仅在非 wait normal path 调用 CharacterInput，OID maintenance 仍在其后；
- `RunPresentationAndCleanupPhase` 在 wait tick 的 RenderDispatch 后返回，未进入 FramePostProcess / late /
  tail / results；
- `NeedClearInput` 仍先走既有 branch，保留 human poll → M1 → clear → return；
- `BattleRuntimeSelfCheck` 增加 F1 wait、F2 one-step / next wait、entry-clear priority 三组 fixture。

已运行 local static contract check、UnityMCP scripts refresh/compile、filtered C# error query 和 request
self-check；详情见下节。Play Mode / physical binding / C++ trace 仍未运行。

## 7. Actual verification (2026-08-22)

- local static scheduler / fixture contract PASSED；`Tools/Validate-ChangeLedger.ps1` PASSED；
  `git diff --check` 没有 whitespace error（只输出既有 LF/CRLF warning）；
- 已打开 Unity Editor `gameplay-ability-system-for-unity@b1b02287` 的 UnityMCP
  `refresh_unity(mode=force, scope=scripts, compile=request)` 完成 domain reload；
- UnityMCP `read_console(types=error, filter_text="error CS")` 返回 0 条；
- request runner 于 2026-08-22 00:25:29 写入 `Temp/NTSD_BattleRuntimeSelfCheck.result = PASS`；
- full Console error read 有 3 条已知非本 Record failure：UnityMCP disposed-object bridge 1 条，
  `RegistrationRollbackSelfCheckEntity` / mismatched runtime-rest binding 预期负例 2 条。不得报告为
  Console 全零，也不得将它们归因给 R3-INP-002。

## 8. Protected boundaries / stop conditions

- Keep CentralOnly / Texture2DArray / dynamic Mesh / URP, 1.5× scale, fixed-world camera, capacity, 30 Hz,
  FrameInputSet, SoA/ECS, pool, worker and 0-GC boundaries;
- stop if implementation needs a runtime-state schema change, physical binding, `g_dword_449048`, Input Action,
  AI/held/lifecycle/physics/CPoint/collision writer or C++ execution/modification;
- stop if preserving entry-clear requires choosing a new gameplay rule rather than keeping its existing behavior;
- T8 remains deferred.

## 9. Verification / rollback

- After code: static order test, `Tools/Validate-ChangeLedger.ps1`, `git diff --check`, UnityMCP compile / error
  query / request self-check;
- no Play Mode or C++ process run in this Record;
- rollback requires a new correction Record; no destructive Git restore/reset.
