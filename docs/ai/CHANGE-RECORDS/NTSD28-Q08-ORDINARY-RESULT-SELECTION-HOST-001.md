<!-- CHANGE-RECORD
id: NTSD28-Q08-ORDINARY-RESULT-SELECTION-HOST-001
status: RUNTIME_PENDING
change-kind: Q08_ORDINARY_RESULT_SELECTION_HOST
code-path: Assets/NTSD/Scripts/Simulation/Host/SimulationTickDriver.cs
code-path: Assets/NTSD/Scripts/App/AppManager.cs
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28Q08OrdinaryResultSelectionHostPlayModeTests.cs
authority: formal Logan GameSession28 ordinary transition2 to selection1; P-19 G-08 Unity visual exceptions; Q08 host routing prechange audit
evidence: formal ordinary and battle-only source witnesses; isolated Unity Play three of three and adjacent twenty-eight of twenty-eight PASS; full SelfCheck PASS; original Menu to Battle route pending
-->

# NTSD28-Q08-ORDINARY-RESULT-SELECTION-HOST-001

Created before scripts. Task: `docs/ai/TASKS/NTSD28-Q08-ORDINARY-RESULT-SELECTION-HOST-001.md`. Current `SimulationTickDriver` and `AppManager` have no reader for `NativeTransitionState` after the core/driver old-battle guards, so command2 leaves the game frozen without entering Unity selection. `AppManager.UnloadBattle()` already owns ordered shutdown and scene unload but always shows MenuMain; `MenuUIController.ShowSelectCharacter()` and `CharacterSelectionController.ResetAll()` exist. Current Build Settings scenes are empty; tests use temporary scenes and must not alter original scene/config files.

Planned exact behavior: main-thread driver handoff after sync tick or completed worker publication, ordinary command2 only, when the Menu/Battle scenes are loaded and AppManager is BattleRunning/Paused. Reuse ordered unload with a battle-result destination that shows existing Unity selection after completion. If refusal, preserve command/old World. Distinct mode commands28/128/202 and explicit battle-only rematch remain pending with no speculative route. Risk: shutdown during Update/worker publication; test ordered Stop, no repeat dispatch, Scene unload and selection active in PlayMode. Rollback and validation are in Task; record actual diff and evidence immediately after code edits.

Test-first: the declared new PlayMode test creates temporary scenes named `NTSD_Menu` and `NTSD_Battle`, attaches the actual AppManager, MenuUIController and SimulationTickDriver, seeds canonical ordinary result phase3/command2, and waits for production Update to route to the existing selection view. It asserts the Battle Scene unloaded and old driver stopped without an additional host tick. No production method or routing script has been edited yet; isolated PlayMode RED pending. The test creates no project Scene or BuildSettings asset.

Implementation checkpoint (2026-09-22): Unity EditMode-runner test enters real Play; `UNITY-ORDINARY-SELECT-HOST-RED-EDIT2.xml` is a valid 1/1 RED, expected `MenuSelectCharacter` but observed `BattleRunning` after 40 frames. The initial PlayMode test-platform call discovered 0 tests, and the first EditMode invocation with `-quit` generated no XML; neither is counted as RED. `SimulationTickDriver.Update` now reads ordinary native command2 after worker publication and after synchronous ticks, on the main thread. `AppManager.TryReturnToCharacterSelectionFromBattleResult` requires loaded Menu and Battle and battle running/paused, then reuses ordered shutdown/unload, resets existing selection controller and shows existing selection UI only on unload completion. Normal `UnloadBattle()` remains MenuMain; 28/128/202 are untouched. Test assertion now checks destruction of the old scene driver after unload. Postchange validation and negative-branch tests remain pending.

Correction: the above `RED-EDIT2` is a real test failure but is not valid behavioral RED for the missing consumer. The synthetic driver was left paused in its `Preparing` lifecycle; `Update()` correctly did not enter the Running path. `GREEN1` reproduced the same setup error after the production edit. The test now calls `SetPaused(false)` before setting transition2, which enters Running without advancing a tick; this corrected test must be rerun before any acceptance claim.

Postchange checkpoint: corrected synthetic Menu/Battle Play target `GREEN2.xml` passed 1/1. The three-branch test `UNITY-ORDINARY-SELECT-HOST-THREE.xml` passed 3/3: ordinary command2 routed to selection after ordered unload, command202 stayed pending with unchanged host tick, and command2 without Menu stayed pending with unchanged host tick. Combined host/Q08/Results `UNITY-ORDINARY-SELECT-ADJACENT.xml` passed 28/28. `UNITY-ORDINARY-SELECT-SELFCHECK.log` contains `[BattleRuntimeSelfCheck] 战斗运行时自检通过` and editor completion; Change Ledger validator passed, `git diff --check` exited 0, original Battle Scene SHA stayed `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`. Current status is `RUNTIME_PENDING`: temporary-scene Play proved this host seam, while original Menu-to-Battle Player/Editor route remains unverified because the existing Build Settings scene list is empty. Battle-only rematch and commands28/128/202 remain separate pending Q08 branches. No computer-use, original Scene, DAT/image, nonbattle UI code, commit or push.

2026-09-25 gate correction: the final sentence's empty Build Settings reason is now superseded. The current `EditorBuildSettings.asset` (SHA-256 `8D621A077642B5154BA305861CA57DFB549FF4FDE5BC58F3E284536A5982F01E`) already lists enabled Menu first and Battle second, with matching Scene GUIDs and no current Git diff. Original-project Menu→Battle result routing is now an available acceptance route, but it has not been executed for this Change. `RUNTIME_PENDING` remains; no script or Scene edit under this correction.
