<!-- CHANGE-RECORD
id: NTSD28-Q07-STAGED-FULL-PUBLICATION-001
status: FOCUSED_TEST_PASS
change-kind: TEST_ONLY
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28Q07StagedPublicationEditorTests.cs
authority: D-023 formal NTSD 2.8-Logan content and Q02 atomic publication production seam
evidence: EditMode job 1711ab456066461da4b517412f60e4fb 1/1 PASS; full 330-object publication and explicit production cleanup zero survivors
-->

# NTSD28-Q07-STAGED-FULL-PUBLICATION-001

Status: `FOCUSED_TEST_PASS` for full staged content publication and explicit cleanup method (2026-09-22). Parent BATCH-04/Q07.

Before: exact 1,343-file byte staging and same-source candidate identity passed. Q02 production publication was validated with small isolated data; the staged 330-object/906-effective-image candidate has not been fully decoded or published. GameConfig remains empty-root and no player-build packaging contract exists yet.

Exact file/symbol: add `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07StagedPublicationEditorTests.cs::StagedFormalCandidatePublishesAllOwnersAndRecyclesResources`, plus Unity-generated `.meta`; no other scripts or assets declared. Use existing isolated owner fixture and production `LoadLoganContentForOwnersAsync` seam, then assert full owner keys/object registry/UI images/decoded resources and exact fixture teardown. This is a focused test of formal staged content, not a new runtime or fallback.

Expected side effects: transient test-only Unity objects, textures and sprites; they must be destroyed on teardown. No GameConfig asset, Scene, old resource, nonbattle caller, schema, package or production logic change. Acceptance: fresh nonzero EditMode PASS, zero compile errors, Scene checksum unchanged, ChangeLedger covers the new script. A RED is reported with first failure and does not authorize weakening object/image coverage. Rollback of the added script follows exact-file deletion approval. Build packaging, configured caller, Play and Q07 exit remain later obligations.

Actual script change: added only the declared `NTSD28Q07StagedPublicationEditorTests` file. Its single UnityTest captures the full staged production candidate, uses the existing isolated owner fixture's production loader, asserts 330 config/data entries, nonempty decoded publication resources and required UI images, then checks disposal after fixture teardown. It does not change GameConfig or production code. Compilation and test results are pending.

Fresh focused RED job `9c6682aaf6ae4cb2af38a6b86f94ceb9`: exact one EditMode test ran for about 137 s. Candidate capture and full publication returned true; manager/data/UI keys and all 330 config/data/head/small checks completed. First failing assertion was **after** fixture teardown: some tracked publication resources still compared non-null. Current assertion does not identify survivors. This is neither a full acceptance nor proof of a production leak because fixture ownership/teardown semantics have not been isolated. The production `OnDestroy` uses synchronous `DestroyImmediate` for owned presentation resources; inspect actual survivor count/type/name and whether manager cleanup ran before changing any production path. Next change stays within the declared test file and only adds failure diagnostics, retaining the zero-survivor assertion.

Diagnostic rerun `2dc291914bd14f54b5ff103a05d61354` reproduced the same RED after full publication: **29,400/29,400 tracked objects survived** immediate fixture teardown, first survivors are generated `Sprite:sprite_0_0`, etc. This sharply favors manager teardown not executing for the newly published resources over a small subset leak, but is not yet a confirmed cause. No exception was found in the recent Editor log. Next diagnostic remains test-only: capture whether the fixture owner is live before load, whether it becomes destroyed at teardown, and whether its `spritePrewarmDisposed`/published ownership fields changed. Do not change production resource cleanup until that branch is evidenced.

Third exact RED job `d691a2b76704449abd03c49aed33cf5d` measured the branch: pre-load owner is live, full 330-object publication succeeded; after EditMode fixture teardown `managerDestroyed=True` but `spritePrewarmDisposed=False`, and 28,163 published-owned sprites/29,400 tracked resources remain. Therefore this fixture's `DestroyImmediate` did not invoke the production private `OnDestroy`; this EditMode event behavior is a fixture limitation, not evidence of a Player lifecycle leak. Exact test-only amendment: call the existing `OnDestroy` method via reflection while the isolated owner is still live, assert it flips the disposed flag, then run fixture teardown and require zero survivors. Preserve all full-content publication assertions. Actual Play lifecycle exit/re-entry is a separate Q07 gate and must not be claimed from this call.

Fresh exact job `1711ab456066461da4b517412f60e4fb` completed EditMode 1/1 PASS (137.68 s). All 330 staged formal definitions and 906 effective images entered the real candidate; `LoadLoganContentForOwnersAsync` committed, manager/data/UI keys and content identity matched, every catalog entry had published config and data, all declared head/small sprites resolved, and decoded publication resources were nonempty. The isolated EditMode fixture explicitly invoked the existing private production `OnDestroy` before destroying the owner; the test required its disposed flag and **zero** tracked resource survivors. This proves the production cleanup method can retire this full publication, not that the Unity Play lifecycle delivered `OnDestroy` automatically. Existing RED jobs remain as fixture lifecycle evidence; no production source was changed.

Post-run: actual battle Scene SHA-256 `BCD1047BF912C6A4A8BC9F3A76EAF3FA954211AD064E0402B1C01BF3BA0E9FB6` matches protected baseline; old Config/Sprite and GameConfig asset are unchanged (`BattleContentRuntimeRoot` remains unserialized/empty). `git diff --check` for touched files exited 0, line-ending warnings only. `Tools/Validate-ChangeLedger.ps1 -RepositoryRoot <workspace>` exited 0: 641 records, 2 governed script files covered including this new test. Actual App/menu Play, player-build packaging and the content-triggered battle returns remain pending.
