<!-- CHANGE-RECORD
id: NTSD28-Q09-FORMAL-PLATFORM-SHADOW-CAMERA-WITNESS-001
status: RUNTIME_PENDING
change-kind: BATTLE_EDITOR_DIAGNOSTIC
code-path: Assets/NTSD/Scripts/Test/Editor/NTSD28Q09FormalPlatformDriverPlayProbeEditor.cs
authority: root formal NTSD2.8-Logan EXE and paired playable battle_world.cpp operation30 plus render_snapshot.cpp shadow Z+10C
evidence: ORIGINAL_EDITOR_COMPILE_0_CS_ERROR / TWO_CONTROLLED_CAMERA_CAPTURES / SECOND_RESULT_PARTIAL_PIXEL_OWNERSHIP_UNPROVEN / CAMERA_AND_FIXTURE_CLEANUP_PASS / SCENE_SHA_STABLE / ROOT_EXE_AND_PIXEL_OWNERSHIP_PENDING
-->

# NTSD28-Q09-FORMAL-PLATFORM-SHADOW-CAMERA-WITNESS-001

Before script edit: the existing Editor-only probe reaches formal OID56/frame130 through a complete production tick and proves the target carrier and Central Shadow command. It deliberately does not read camera pixels. Existing same-Z pixel probe demonstrates a temporary white-clear RenderTexture capture with camera setting restoration. Central frame planning caches a tick, so changing the carrier after the tick and rendering again would be an invalid counterfactual. Instead the declared extension captures the real pre-fixture baseline and the real post-tick plan without mutating the carrier.

Planned code path and symbols: only `NTSD28Q09FormalPlatformDriverPlayProbeEditor.cs` `Request`, `Report`, `Run`, `Finish`, and private camera/projection helpers. Optional flag defaults false; old report and flow remain. Expected side effects: one request causes two GPU readbacks and writes two diagnostic PNGs and one JSON in this Change's artifact folder. It briefly changes only the target camera's local render settings, restoring them in `finally`; fixture registry, pause, Scene and pool invariants remain those of the prior probe. No production owner, rule, DAT, PNG, Scene, Prefab, ProjectSettings, input or nonbattle code changes.

Acceptance and rollback are the same-ID Task contract. After edit, append actual symbols, compile/Play/Console, pixel attribution and cleanup evidence, command results and limitations. A zero exclusive-pixel outcome is diagnostic, not a pixel PASS. The status must not exceed the observed evidence.

Actual edit: only the declared Editor probe file changed. `Request.captureCamera` gates the new path, while default requests retain the original result root and scope. `Run` captures the pre-fixture central camera baseline, executes the existing full Driver tick, then captures the published post-tick plan. `CompareShadowPixels` projects the target Shadow and excludes every other command's projection with a two-pixel guard; it counts white-baseline pixels changed in the exclusive area and emits `PIXEL_OWNERSHIP_UNPROVEN` if none. `CapturePixels` temporarily uses a white-clear 1920-wide RenderTexture and restores the camera/active RenderTexture and releases readback resources in `finally`; JSON/PNG outputs route to this Change's artifact folder. Code compile and Play evidence remain pending. The diagnostic PNGs are new files; no existing file is removed or replaced.

First original-Editor request `platform-shadow-camera-01` compiled and ran, but the top-level status was `PASS` for the full-Driver carrier/command even though the separate camera status was `PIXEL_OWNERSHIP_UNPROVEN` with zero exclusive pixels. The two actual captures were a white pre-fixture baseline and a post-tick frame where the target shadow's projected 53×11 region overlaps other command bounds. Camera restoration, object/slot/pool counts and Scene SHA passed. To prevent that JSON from being misread as a pixel PASS, the same probe now reports top-level `PARTIAL_PIXEL_OWNERSHIP_UNPROVEN` in this case. A fresh compile and one new request must verify this correction; the first raw JSON remains untouched as evidence.

Fresh original Editor compile and unique `platform-shadow-camera-02` request verified that status correction. Its top-level `PARTIAL_PIXEL_OWNERSHIP_UNPROVEN` and camera status `PIXEL_OWNERSHIP_UNPROVEN` are consistent; carrier/command still PASS at tick6. Baseline and post-tick PNGs were inspected, but exclusive white-baseline and changed pixel counts are both zero because other command bounds cover the target Shadow projection. Camera settings, fixture registration, object/slot/pool counts and both Scene SHA values restored; Editor idle/non-Play and Console error query 0 after the second run. Exact numbers, hashes and evidence limits are in the same-ID ACCEPTANCE. No full suite or SelfCheck was run for this test-only probe. Governance validator and diff-check results are appended after execution. This Record remains `RUNTIME_PENDING` because the intended target Shadow pixel ownership is not established.

Final governance: `Tools/Validate-ChangeLedger.ps1 -RepositoryRoot <current project>` exited 0, `git diff --check` exited 0, and the alignment, handoff and STATE live documents each had zero NUL bytes. Validator output is retained in `Temp/NTSD28-Q09-FORMAL-PLATFORM-SHADOW-CAMERA-WITNESS-001-validator.log`; historical warnings about Records whose paths are outside this diff are not new failures.

Post-review correction in the same declared probe file: aggregate camera restoration across both baseline and post-tick captures. Previously the second capture's `Matches` assignment could overwrite a failed baseline restoration with `true`; now either capture failing restoration leaves the final field false. This does not change production or the observed pixels; compile and any required focused follow-up for the final file revision are recorded below.

Final-file confirmation: original project `Assembly-CSharp-Editor.dll` timestamp was newer than the final probe source; new unique `platform-shadow-camera-03` Play result reproduced top-level PARTIAL, `cameraStateRestored=true`, full-Driver carrier/command, zero exclusive pixels and unchanged cleanup/Scene SHA. An explicitly selected MCP connection confirmed the original Editor Battle Scene was non-Play with zero Console errors. During a refresh the unqualified MCP default briefly reported a different already-open Editor; no Play or Scene/content write was sent there. Exact raw hashes are in ACCEPTANCE. This remains `RUNTIME_PENDING` for target shadow pixel ownership.
