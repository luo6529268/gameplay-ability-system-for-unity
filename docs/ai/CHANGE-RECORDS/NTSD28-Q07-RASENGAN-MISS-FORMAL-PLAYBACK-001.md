<!-- CHANGE-RECORD
id: NTSD28-Q07-RASENGAN-MISS-FORMAL-PLAYBACK-001
status: VERIFIED
change-kind: Q07_RASENGAN_MISSED_TRANSFORM_FORMAL_RELEASE_PLAYBACK
code-path: Tools/NTSD28Q07Diagnostics/rasengan_miss_lfr_probe.cpp
authority: root formal NTSD2.8-Logan EXE B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033 and corresponding playable GameSession28/GameSessionLfr28
evidence: GCC exit0; source vs root formal EXE 32tick x 11 fields = 352/352; current Unity 94 entity rows match listed non-X fields, 26 X differences remain; REPORT.md
-->

# NTSD28-Q07-RASENGAN-MISS-FORMAL-PLAYBACK-001

2026-09-25 scoped diagnostic exit: only new C++ path `Tools/NTSD28Q07Diagnostics/rasengan_miss_lfr_probe.cpp` changed; three versions of the compiled probe and their unique outputs preserve the initial BGM/RNG mismatch. Final BGM-fixed GCC compile exit0/empty log, bridge exit0 and 32tick LFR, root formal EXE report PASS/failureCode0/32 declared ticks/34 trace rows. The 32 source rows and formal EXE trace match on 11 named columns (352/352). Unity raw has identical slot sets and 94 rows agree on OID/action/state/Y/Z/Vx/Vy/Vz/HP/MP; 26 X-field differences are explained by approved relative displacement scaling, including tick29 slot52 OID434 507/494 from its frame100 parent X531 and DAT centerx24/opoint x0. Thus the previous tick30 Vx550 defect is no longer reproduced, but this record only verifies the controlled playback diagnostic, not Q07/R18 full alignment or physical attack window. Formal EXE SHA unchanged; output hashes and limitations in `artifacts/diagnostics/NTSD28-Q07-RASENGAN-MISS-FORMAL-PLAYBACK-001/REPORT.md`. No Unity production/DAT/Scene/Prefab/ProjectSettings change. `Tools/Validate-ChangeLedger.ps1` and `git diff --check` both exit0. Rollback remains scoped to new diagnostic script/artifacts under repository deletion approval; no rollback needed.

2026-09-25 second pre-re-edit finding: source RNG CSV starts with synchronized calls1/counter1/index1/lastSite `0x004021E0` from `GameSession28::resolve_native_bgm_selection`, while formal EXE LFR trace has calls0/counter0/index1 through tick28 and selected `bgmSelection49F18C=2`, `bgm\\stage1.wma`. Both table hashes are `a1ba1b90ea55796d`; formal first call at tick29 selects attack with site0x84, source performs its second call and selects a different action. This is a source-bridge random-BGM initial-state mismatch, not a measured Unity battle-rule defect. Before the next edit, the exact change to the same sole C++ diagnostic path is to set source bridge BGM selection2 and produce new `_bgm_fixed` files, preserving the two existing runs. No formal/Unity production code or DAT changes.

2026-09-25 pre-re-edit observation: first official-source bridge compiled exit0 and produced32tick LFR/CSV. Root formal EXE SHA matched and headless playback reported PASS/failureCode0, but source CSV versus formal trace first differed at tick29 actor action25/20; formal synchronized RNG first drew on that tick. Unity raw matched source actor action25 at tick29, and OID434 slot52/action397 Vx0 on tick30 across all three, while other child X positions differ. The playback PASS is only manager integrity, not full parity. The script currently omits source RNG. Before changing it again, exact amendment: preserve old outputs and add source RNG counter/index/calls/table hash to new suffixed files; compare new source state with release trace. No production scope expansion.

Before edit: prior report named source-model OID434/slot52/action397 tick30 Vx0 versus old Unity Vx550 after missed transform. The current Unity Editor `(26,2)` regression now records Vx0 at the same anchor, but the prior source-model artifacts are absent and this turn has not run the formal EXE. Existing Sasuke release LFR bridge demonstrates the build/playback mechanism, not Naruto's specific result.

Declared edit: one new `rasengan_miss_lfr_probe.cpp` in Tools. It must use formal playable/core source and official runtime files read-only; no production Unity or DAT changes. Its exact roster, input, outputs, comparison/acceptance and rollback are in the same-ID Task. Source-generated LFR is explicitly not independent native capture. Record actual compiler command/result, EXE identity, first differences, hashes and limitations before status promotion.
