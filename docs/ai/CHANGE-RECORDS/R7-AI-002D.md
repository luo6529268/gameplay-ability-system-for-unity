# R7-AI-002D — OID10/1、9/2、32/19/33 helper module

<!-- CHANGE-RECORD
id: R7-AI-002D
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Simulation/Ai/AiCharacterDecisionModule.cs
code-path: Assets/NTSD/Scripts/Test/Editor/AiCharacterDecisionPositions17To28EditorTests.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\input\input_handler.cpp::positions 17-28 / helpers lines 606-841
evidence: SOURCE-CONTRACT-VERIFIED / GENERATED-EDITOR-BUILD-0-ERROR / FRESH-UNITY-COMPILE-0-ERROR / FOCUSED-26-OF-26-PASS / AI-REGRESSION-238-OF-238-PASS / RED-WITNESSES-STILL-EXPECTED-FAIL / WARMED-400-SLOT-SCAN-0-B / FRESH-DOMAIN-FULL-SELF-CHECK-PASS / UNWIRED-RUNTIME-PENDING
-->

> 创建日期：2026-08-23
> 最后更新：2026-08-23
> 类型：AI helper module / test

## 1. 状态与范围

- 当前状态：`RUNTIME_PENDING / UNWIRED MODULE`
- Work Package：`R7-AI-02D`
- 只实现 unwired positions 17–28 module 与 source-derived tests；不得改变默认 AI 输出。

## 2. Authority 合同

完整条件、顺序与 RNG 合同见
`docs/ai/TASKS/R7-AI-02D-oid10-1-9-2-32-19-33-helper-module.md`。

重点不可简化项：

- position 17 左朝向 facing-target 分支会 return true，但 C++ 只在右朝向分支写 DDA；
- position 21 是 void full/team scan，扫描 `MAX_OBJECTS=400`，选严格最远而非最近；
- position 21/22 side-effect 不得阻断 positions 23–28；
- position 24/26 path-B 在检查 X range 之前已经消费 dynamic-modulus RNG；
- position 28 本包只能作为 unwired module 行为，production 仍保留既有错误位置供 02F 一次性修正。

## 3. Unity 原状

- 02C 已有非 partial `AiCharacterDecisionModule` 与共享 allocation-free RNG stream；
- snapshot 已具备 Included、DataObjectType、X/Y/Z、HP/HPMax、PP、Team、Frame、Vx、Facing、LinkState；
- `nearestTargetDistance` 已在 existing decision pipeline 计算，但未传给 02D module；
- production position 28 helper 存在，但错误位于 outer gate 外；本包不得移动或复用到默认 dispatcher。

## 4. 计划改动

- 在现有实例 module 添加 `TryEvaluatePositions17Through28`；
- 通过显式 `nearestTargetDistance` 与 `scanSlotCount` 参数传入 authority context，不回读 GameObject；
- 新增独立 focused test 文件，覆盖 12 positions、RNG trace、400-slot scan、continuation 与 warmed 0 B；
- 不修改 `AiDecisionKernel`、Legacy dispatcher 或 existing position 28 helper。

## 5. 不可回退边界

- 不改变 02C positions 7–16 与 shared RNG；
- 不改变现有 production RNG/order/output；
- 不修改 02A red witness 预期；
- 不改 C++、render、collision、capacity、profile 或 pass。

## 6. 验收与回滚

验收按 Task Contract。回滚只删除本 Change ID 添加的方法与新 test 文件，不得回退 02B/02C 或其他用户改动。

## 7. 实际改动与证据

已写入 positions 17–28 unwired module 段与 focused tests，尚未完成最终验收。

首次 focused job `4c3e6f659c384014a0eefa180d6ae5c6` 运行 26 个目标 cases，仅
`Position28_StateBypassStillConsumesRand5(19)` 失败：expected RNG calls 1 / actual 2。只读回查 dispatcher
确认 OID19 先属于 position26/27 group；position26 path-A 未进入时仍在 path-B 消费 dynamic-modulus RNG，
随后 position28 再消费 Rand5，因此 actual 2 符合 C++ 顺序。该失败是 fixture 漏计前序 draw，不是实现回归；
保留本记录后将 OID19 期望修为2，OID33/16仍为1并重跑。

最终实际改动：

- 在既有非 partial、持久实例 `AiCharacterDecisionModule` 中新增
  `TryEvaluatePositions17Through28`，没有创建第二套 dispatcher owner；
- positions17–28 按 C++ 顺序执行；position21/22 void side-effect 后继续，其他命中 position 立即返回；
- position21 只读 snapshot，按显式 `scanSlotCount` 升序扫描，保留 strict-farthest/first-tie 与过滤合同；
- `position21SelectedSlot` 是无分配的聚焦诊断输出，用于证明 farthest/tie/400-boundary，不写回 runtime；
- position24/26 保留 path-A 短路和 path-B dynamic modulus 在 X range 前的 RNG 消费顺序；
- position28 只在 unwired module 内实现；既有 production helper、outer gate 与 02A red witness未修改；
- 新增26个 source-derived cases，覆盖12 positions、group membership、RNG calls/moduli、early return、
  side-effect continuation、scan filters/farthest/tie/400 boundary、unrelated OID 与 warmed 0 B。

最终验证：

| 层级 | 结果 |
|---|---|
| initial focused fixture | job `4c3e6f659c384014a0eefa180d6ae5c6`：25 PASS / 1 FAIL；OID19漏计position26 path-B draw，已留档 |
| corrected focused module | job `5d265876f2e24159879cd881e7218d80`：26/26 PASS |
| AI regression | job `3cd69caca0f546338f1ced0500cb4062`：238/238 PASS |
| 02A explicit red witnesses | job `0173e28d95bf44ab9df97facc81193cc`：2/2按预期FAIL，production仍未接线 |
| warmed allocation | position21 400-slot scan 4096次，0 B |
| generated Editor build | `dotnet build Assembly-CSharp-Editor.csproj --no-restore`：0 error，51 existing warnings |
| fresh Unity compile | runtime DLL 00:28:17、Editor DLL 00:30:02，Console error 0 |
| fresh-domain full self-check | `Temp/NTSD_BattleRuntimeSelfCheck.result` 00:33:49 `PASS` |

本包只证明 positions17–28 unwired helper 与 source-derived fixtures闭合，且未破坏既有 AI baseline。
默认 dispatcher、完整1–39遮蔽、position28 outer-gate修复、>399 adapter、Play Mode和C++ runtime trace均未完成。
