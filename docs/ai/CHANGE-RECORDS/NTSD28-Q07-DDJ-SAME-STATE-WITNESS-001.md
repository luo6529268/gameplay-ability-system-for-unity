<!-- CHANGE-RECORD
id: NTSD28-Q07-DDJ-SAME-STATE-WITNESS-001
status: RUNTIME_PENDING
change-kind: Q07_DDJ_ORIGINAL_EDITOR_DIAGNOSTIC_ONLY
code-path: Assets/NTSD/Scripts/Test/Editor/BattleComboPlayModeProbeEditor.cs
authority: NTSD 2.8-Logan formal EXE/playable DDJ and current original-project physical-input witness
evidence: original Editor contiguous -1 and post-guard -2 Play captured identical tick1-26 results; generated Editor build 0 errors; same-state and borrower pending; see CONTINUOUS-TICK-ACCEPTANCE-20260925.md
-->

# NTSD28-Q07-DDJ-SAME-STATE-WITNESS-001

2026-09-25 post-guard evidence: original Editor consumed new unique `ddj-contiguous-20260925-2` request after the guarded diagnostic script imported. The prior task-owned consumed request was copied byte-for-byte to an immutable named artifact first. Result `PASS`, SHA identical to the pre-guard controlled run: 26 consecutive rows, zero missed, 271→272→495, PP500→150, first OID518 tick16. Editor exited Play, Console error0, clean Battle Scene/root14, Battle/Menu/GameConfig hashes stable. The early-finish diagnostic issue is corrected and normally exercised; no intentionally early-resolution adversarial fixture was run. Initial state and input schema remain different, Renderer borrower count unverified. Status stays `RUNTIME_PENDING` and Q07 open; see `CONTINUOUS-TICK-ACCEPTANCE-20260925.md`.

2026-09-25 current result: unique original-Editor `ddj-contiguous-20260925-1` Play PASS with controlled one-tick observations 1–26, no missed ticks, 271→272→495, PP500→150 and OID518 first tick16. Formal scoped counts: PP/MP, phase, OID518 26/26; action25/26, initial action mismatch at tick1. Initial roster/object count, position, RNG, map and input schema differ; borrower count remains unverified after the failed read-only MCP CodeDom query. Static review found that early DDJ completion could falsely PASS before 26 ticks. The sole owned Editor diagnostic script now requires start+26 and continuous trace for DDJ PASS, while other probe routes are unchanged; generated Editor build 0 errors/163 warnings. Original Editor refreshed, completed domain reload, returned idle in clean Battle Scene with Console error0. Change Ledger validator passed (821 Records, 20 governed code files in shared diff); focused `git diff --check` 0. A fresh original-Editor Play of that last guard is pending. Status remains `RUNTIME_PENDING`; see `CONTINUOUS-TICK-ACCEPTANCE-20260925.md`.

2026-09-25 pre-edit reviewer correction: the new controlled DDJ run captured consecutive tick 1–26 and passed its milestones, yet static review found that `Observe` can call `Finish(true)` before tick 26 if `step3Tick + 20` is satisfied early. Restrict the DDJ-state completion gate to `max(step3Tick + 20, startTick + 26)` and require the existing continuous trace predicate for DDJ PASS. Other probe modes retain their finish rules. This is confined to the owned Editor diagnostic script, does not alter production behavior or invalidate the already-preserved run, and must be followed by compile and targeted governance checks. Full same-state/runtime acceptance remains pending.

2026-09-25 second pre-edit scope addendum: the old fixed request `Temp/NTSD28_Q07_DdjState.request.json` is present and inactive; protected-file policy forbids overwriting it for the new test. In the same already-owned Editor script, add a separate `Temp/NTSD28_Q07_DdjContinuous.request.json` path to DDJ request polling and `HasPendingRequest`, reusing the exact existing DDJ-state branch/result root. Preserve the old file and its poll semantics, every other route and all production files. Validate no output-name collision and use create-new for the new request/result. The new control input is not gameplay authority, and the runtime result remains `RUNTIME_PENDING` until the original Editor test actually returns.

2026-09-25 pre-edit continuation contract: Existing `q07DdjStateAudit` original-Editor result `ddj-state-20260925-5` passed requested271/resolved272/full-tick495, PP500→150 and OID518, but `continuousTickCapture=false` with gaps at ticks 1 and 3. Source: `BattleComboPlayModeProbeEditor.cs` uses `EditorApplication.update` and LocalFreeRun may drain two ticks in one Update; `NTSD28UserRasenganPhysicalPlayProbeEditor.NaturalProbe` already demonstrates pause + `StepOneTick(ignorePaused:true)` with the normal local input provider in the same saved Battle Scene. Before editing the single declared script path, authorize only this diagnostic branch to save `driver.IsPaused`, call `driver.SetPaused(true)`, step one real tick per observation, and restore the saved state in `StopObservation` including failure cleanup. Add a bounded result field identifying controlled stepping. Keep the queued physical input path and neighboring probe modes unchanged; no world state setters, Scene/asset/DAT or production script changes. A fresh unique result must show every tick 1..26, or report the exact gap/failure. Recheck DDJ milestones, input schema labels, Scene/GameConfig SHA, Editor exit and borrowers; maintain `RUNTIME_PENDING` until actual Play and full task acceptance. Even a continuous trace does not erase the documented different roster/action/position/RNG/map or prove same-state EXE parity. Rollback only this diagnostic branch and its new result subject to protected-file approval.

2026-09-25 current result: original Editor compiled the last diagnostic change and consumed unique `ddj-state-20260925-5`. Focused Play PASS for requested271→action272→full-tick495, PP500→150 and OID518 tick16. Actual pre-input roster OID2/OID2, positions, action0 and RNG differ from formal action110 trace OID2/OID7; raw proxy input masks are not yet normalized; sampled ticks omit 1 and 3 (`continuousTickCapture=false`). Editor returned to Edit mode, both Scene files and GameConfig disk hashes match pre-Play. Renderer borrower count was not exposed. Record status `RUNTIME_PENDING`: the diagnostic has useful Play evidence, but its full acceptance and same-state gate remain open. Exact comparison and limitations: `artifacts/diagnostics/NTSD28-Q07-DDJ-SAME-STATE-WITNESS-001/ACCEPTANCE-20260925.md`. This supersedes the older `UNITY_COMPILE_PENDING` notes below without erasing them.

2026-09-25 pre-edit diagnostic correction: after the original Editor imported this script, unique request `ddj-state-20260925-1` was consumed in the existing Battle Scene and produced a preserved FAIL before input capture. `RunProbe` currently applies the Q07 forward-attack preflight `hit_Fa == 285` whenever `q07FormalActive`, including the new `q07DdjStateAudit` branch. The formal Naruto DDJ frame0 uses `hit_Fj == 271`; prior original-Editor formal DDJ Play reached requested271/resolved272/frame495. The new request therefore cannot reach its own DDJ-specific preflight at all. Within the already-declared sole script path, change only the common preflight's expected field/value by probe kind: DDJ reads `character.Frame.D.hit_Fj == 271`, Sasuke DRA keeps `hit_Fa == 261`, and Naruto forward attack keeps `hit_Fa == 285`. Give the DDJ failure its own message. Preserve all non-DDJ probe behavior and the first FAIL artifact. Then rebuild in the original Editor and rerun using a new unique result ID. No production/Scene/DAT change is authorized by this correction. Current status remains `CODE_WRITTEN` until that evidence exists.

Pre-edit field-name correction to the immediately preceding note: the actual Unity DownJump branch reads `character.Frame.D.hit_Dj`, and formal `c/nar/nar.dat` frame0 contains `hit_Dj: 271` (`hit_Fj: 240`). The intended fix is **`hit_Dj == 271`**, not `hit_Fj`. The first note's named field was an audit transcription error; no script edit occurred between these notes.

2026-09-25 next pre-edit correction: the original Editor recompiled the `hit_Dj` branch (Editor DLL 04:10:52Z after source 04:10:22Z) and consumed new request `ddj-state-20260925-3`. It returned another preserved FAIL from the same combined preflight, now naming `hit_Dj 271`; this proves the branch ran but does not identify which other predicate failed. The first result DTO does not serialize runtime manager details on preflight failure, and generic text is insufficient. In the same declared diagnostic script only, extend the DDJ-specific failure message with observed `BattleContentRuntimeRoot`, published semantic fingerprint, OID, `hit_Dj`, and a boolean for nonempty published visual key. Do not relax any predicate yet; compile and use another unique output to identify the first actual mismatch. This does not authorize production or resource edits.

2026-09-25 pre-edit fingerprint correction: original Editor request `ddj-state-20260925-4` returned a preserved FAIL with formal root, OID2, `hit_Dj=271` and visual key present; only the hardcoded `B8B13894...` fingerprint predicate disagreed with published `27CAE01489909C46A5145A5867988CCD8C10E20FEF165D847B7AC7A6DE2DE02D`. Existing original-Editor formal DDJ PASS `formal-ddj-play-3.json` and Q07 project-mode readiness both independently published `27CAE014...`; `CharacterAnimtorManager.PrewarmConfiguredLoganContentAsync` captures the approved `ProjectBattleModeConfig` snapshot, so comparing to the native mode-DAT composite is invalid for this project-mode exception. In this diagnostic branch only, require that exact observed project-mode fingerprint; retain the old fingerprint predicate for all other probe modes, all formal root/OID/field/key checks, and all production code. Archive the consumed -4 request; compile and use a new -5 result. This is an exact current-content witness, not proof of EXE same-state parity.

Parent Task: `docs/ai/TASKS/NTSD28-Q07-DDJ-SAME-STATE-WITNESS-001.md`. This Record precedes any script edit. Existing original Editor `formal-ddj-play-3.json` is PASS for limited DDJ milestones; formal source/release action110 playback has 26-row five-field equality, but the same-SHA Scene's serialized OID2/OID2 roster differs from the source bridge's OID2/OID7 and Unity's old result omits seed/RNG/coordinates/mode and continuous consumed input. The probe currently samples only `EditorApplication.update`, so two logic ticks in one Update may leave a gap.

Owned symbol: `BattleComboPlayModeProbeEditor` request polling, `RunProbe` initial diagnostic capture, per-tick `Observe`, result DTO and finalization. Expected side effects are only one new opt-in request and uniquely named artifact output. No production, DAT, Scene, menu, resource, input-action or Unity framework behavior may change. Keep the original fixed-output DDJ route and all neighboring probe modes intact.

Acceptance: compile in the already-running original Editor, run one focused new request only, inspect actual result and sampled-tick gaps, preserve pre/post Scene hashes and ordered Play exit, perform only targeted DDJ and neighboring probe checks that the edit touches, then run `Tools/Validate-ChangeLedger.ps1` and `git diff --check`. No result may be labeled a full same-state proof until all required initial fields and continuous input rows are genuinely comparable. If a tick-boundary capture cannot be reached without a production hook, report `RUNTIME_PENDING` with explicit gaps rather than modifying the production Driver for this diagnostic.

Rollback is limited to this Change's appended diagnostic branch/fields and its uniquely named output, subject to the repository's explicit deletion/overwrite rule. Do not revert any prior uncommitted work in the shared script.

2026-09-25 code update: the sole edited script now accepts `Temp/NTSD28_Q07_DdjState.request.json` with a unique run ID, writes only under `artifacts/diagnostics/NTSD28-Q07-DDJ-SAME-STATE-WITNESS-001/`, and reuses the existing physical DownJump route. It captures the pre-input tick, world mode/background/seed/input phase, shared/native scalar RNG, sorted existing entity roster with motion/source-rule/input fields, queued physical key events, and observed-tick input masks/phase/RNG. It counts skipped observed ticks and sets `continuousTickCapture` only if all 26 requested ticks are present. The old fixed DDJ result path, forward-attack, clone and Sasuke routes remain in place. This diagnostic does not assert same-state equality and does not write simulation state.

Generated `Assembly-CSharp-Editor.csproj` contains this script and `dotnet build --no-restore -v:q -nologo` exited 0 with 191 warnings and 0 errors; the log is `Temp/NTSD28-Q07-DDJ-same-state-offline-build.txt`. This is only generated-project C# compilation. The running original Editor has not yet imported the new script (`Library/ScriptAssemblies/Assembly-CSharp-Editor.dll` timestamp predates it), so Unity Editor compilation, focused Play, Scene hash and true runtime result are pending. The Unity CLI `status` reported `STATUS_NO_INSTANCES` because this project does not have `com.unity.pipeline`; an OS process check separately confirmed the original project Editor remains open. No second Editor was started and no new request has been sent. Current status is `CODE_WRITTEN / UNITY_COMPILE_PENDING`.

Review correction: Unity input proxy arrays are now recorded as raw indexed `proxyCurrent`, `proxyPrevious` and `proxyEdgeWindow` bytes, with separate raw bitsets and an explicit schema label. They are not named or compared as formal EXE input masks. The unique run ID and output absence are checked before entering Play, and this new result path uses create-new writes so a late name collision cannot overwrite a prior result. After these corrections, the same generated-project build exited 0 with 160 warnings and 0 errors; log: `Temp/NTSD28-Q07-DDJ-same-state-offline-build-final.txt`. `git diff --check` exited 0. Unity Editor import and Play remain pending; these generated-project results do not advance the Change beyond `CODE_WRITTEN`.
