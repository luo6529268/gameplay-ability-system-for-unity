# R3-INP-003A-001 — canonical full-held packet poll-contract fixture

<!-- CHANGE-RECORD
id: R3-INP-003A-001
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs
authority: J:\QQFile\NTSD2.4\ntsd_release\src\core\main.cpp + src\input\input_handler.cpp release live path
evidence: SOURCE-CONTRACT-VERIFIED / STATIC-PASS / UNITY-COMPILE-PASS / FOCUSED-SELF-CHECK-PASS / RUNTIME-PENDING
-->

> 创建日期：2026-08-22  
> 状态：`RUNTIME_PENDING`  
> 所属 Work Package：`R3-INP-03A`

## Scope

只新增一个 `BattleRuntimeSelfCheck` fixture，验证 canonical full-held `FrameInputSet` 在 human roster slot
的 press → hold → release → multi-key 过程中符合 C++ `InputHandler::poll` 的 observable key/prev/cd/history
contract。

允许代码文件只有：

- `Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs`。

不得修改任何 gameplay writer、FrameInputSet / packet / buffer / roster / worker / AI / physical asset；若
fixture失败，先记录实际 first difference，停止 test-only Record，另建 implementation Record。

## Authority / initial contract

- C++ `main.cpp:4607-4608` poll active P1/P2；
- C++ `input_handler.cpp:1555-1613` 写 prev、current held、cooldown，再按
  `right,left,up,down,attack,defend,jump` order 写 press/history；
- Unity uses canonical full held `Buttons`; `PressedButtons` / `ReleasedButtons` do not replace this C++ source
  contract.

## Planned verification

1. static source/order guard；
2. ledger / `git diff --check`；
3. UnityMCP script compile / filtered `error CS`；
4. complete `BattleRuntimeSelfCheck` request；
5. retain Play Mode / C++ trace as pending / blocked.

## 实际改动（编译前）

- `BattleRuntimeSelfCheck.RunAllChecksStatic` 现调用
  `CheckCanonicalFramePacketPollContract`；
- fixture 为一位 bound human 依次提交完整 `Buttons` packet：
  `Right|Attack|Jump|Defend` press → same held → None release；
- 它断言 C++ source 的 logical edge order
  `right,attack,defend,jump`，以及 key/prev/cooldown/history 在 press、hold、release 三步的结果；
- fixture 不读取 `PressedButtons` / `ReleasedButtons` 作为 gameplay input，也未修改任何 production writer。

## 实际验证结果

| 层级 | 命令 / 输入 | 结果 | 状态 |
|---|---|---|---|
| source/static | C++ `poll` edge order + Unity writer usage guard | PASS；C++ / Unity 都为 right,left,up,down,attack,defend,jump edge sequence；metadata 未驱动 Unity application。 | `PASS` |
| ledger / whitespace | `Tools/Validate-ChangeLedger.ps1`、`git diff --check` | PASS；8 records / 10 governed files，diff check exit 0。 | `PASS` |
| Unity compile | existing Editor 的 UnityMCP force scripts compile | 成功；filtered `error CS` 返回 0。 | `PASS` |
| focused / full self-check | `NTSD/验证/运行战斗运行时自检` | press→hold→release→multi-key fixture 和完整 self-check PASS；result 01:20:32 +08:00。 | `PASS` |
| Play Mode / physical binding | InputAction asset / W-S-A-D-J-K-L | 未运行；明确移交 R3-PHY-01。 | `OUT_OF_SCOPE` |
| C++ executable trace | R1-WP02 | 未获得，且不运行 executable。 | `BLOCKED` |

## 未关闭项与交接

- 这个 test-only Record 不改变任何 production input writer；C++ source 已闭合，但没有 C++ executable
  runtime trace、P1/P2 scene验收或 physical key asset evidence；
- `D-INP-004`、`D-INP-005`、`D-INP-006` 按 D-011 独立处理，不能由当前 PASS 推断完成；
- 若未来 canonical frame 缺少 active human slot，必须先检查 lockstep protocol boundary并建立新 Record，
  不能直接改变 `FrameInputSet` semantics；
- 回滚仅需移除本 Record 新增 self-check invocation/method；不得影响其它已有 self-check diff。
