# R5-CPT-005 — CPoint valid decrease-negative escape tail

<!-- CHANGE-RECORD
id: R5-CPT-005
status: RUNTIME_PENDING
code-path: Assets/NTSD/Scripts/Simulation/Ecs/BattleCpointWriter.cs
code-path: Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs
authority: J:/QQFile/NTSD2.4/ntsd_release/Makefile:20-21;src/entity/game_tick.cpp:659-684;src/entity/cpoint.cpp:60-172
evidence: SOURCE-ESCAPE-TAIL-VERIFIED / UNITY-EARLY-RETURN-DIFFERENCE-VERIFIED / POSTPROCESS-CONSUMER-VERIFIED / UNITY-VALIDATION-PENDING / CPP-TRACE-AND-PLAYMODE-PENDING
-->

> 创建日期：2026-08-22  
> 最后更新：2026-08-22  
> 类型：battle / test  
> 当前状态：RUNTIME_PENDING — 最小 writer、focused fixture、Unity compile与full self-check已通过；C++ trace / Play Mode待验。

## Authority / 差异

C++ valid decrease-negative escape只设置 skip_actions，仍进入 unguarded throw tail / dircontrol；
Unity RunKind1 direct return。由于 escape前已写 attacker frame0，tail必须读 current frame0，而不是
old prev-frame CPoint snapshot。

## 允许范围

| 文件 | 符号 | 允许改动 |
|---|---|---|
| BattleCpointWriter.cs | RunKind1 escape branch / existing ApplyThrow callsite | 保留 escape writes，改为 skip-actions后继续tail，并传 fallback Frame.D。 |
| BattleRuntimeSelfCheck.cs | CPoint escape fixture | 验证 throw immediate/postprocess、dircontrol、action skip、FWC和neutral control。 |

## 明确排除

不改 D-CPT-002/003/004、mismatch path、throw transform body、FramePostProcess implementation、kind2 validation、
held/link/opoint/input/collision/render/DAT/scene/pass order、C++ authority、trace、Play Mode或性能。

## 实际改动

| 文件 | 符号 | 实际改动 |
|---|---|---|
| BattleCpointWriter.cs | RunKind1 decrease-negative escape | 保留existing escape writes，移除direct return，改为skipActions与fallback-frame flag后继续existing tail。 |
| BattleRuntimeSelfCheck.cs | CPoint escape/mismatch control-flow fixture | valid escape+throw现锁定frame0/132、frame0 geometry、throw velocity、hit count/knockback、FWC和step14消费；no-throw case锁定dircontrol。 |

未修改 mismatch path、throw body、FramePostProcess implementation、kind2 validation或其它scope外模块。

## 实际验收

| 层级 | 操作 | 结果 |
|---|---|---|
| S0 source | reread C++ escape、throw/dir tail、step14 postprocess与Unity all callsites。 | PASS；scope未扩张。 |
| S1 focused | valid escape+throw immediate/step14、no-throw dircontrol、FWC与existing neutral control。 | PASS；在full self-check内实际执行。 |
| S2 governance | ledger validator与scoped diff。 | PASS；最终文档更新后重跑仍通过，37条Record、26个governed code file均被覆盖。 |
| S3 Unity | Unity 2022.3.62f3编译与request-file full self-check。 | PASS；Tundra build success、无error CS，结果文件2026-08-22 16:09:37为PASS。 |
| S4 runtime | C++ trace / real Play Mode。 | PENDING；不在本包关闭。 |

## 回滚边界

只回滚该 Record列出的 two-file diff及关联文档；不触碰其它用户/历史工作树修改。
