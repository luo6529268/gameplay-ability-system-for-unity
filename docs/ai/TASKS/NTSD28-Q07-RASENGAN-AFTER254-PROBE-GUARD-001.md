# NTSD28-Q07-RASENGAN-AFTER254-PROBE-GUARD-001

Status: `VERIFIED_SCOPED_DIAGNOSTIC`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001 / BATCH-04 / Q07 / R18`. This is a test-only correction to the existing natural Naruto defense→forward→jump→J window diagnostic. It is not a gameplay rule change.

Authority and trigger: formal root `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033` plus paired playable input/frame path. The original Editor's new `natural-after254-20260925T101154864-968b146a837940a3a2493381205fd7c2.json` fails at tick1170, action110/state7, before any natural skill or 254 window. Existing `NaturalProbe.ObserveNatural` enables its post-window frame guard whenever `timing == 2`, including the defense startup. The second-253 run `natural-second253-20260925T100831646-9edb9114b4a6497e9291a0d223ab3bd7.json` passed and showed PP350/input-consumed150 after J was sampled on 254; do not rerun that case for this correction.

Exact write scope: `Assets/NTSD/Scripts/Test/Editor/NTSD28UserRasenganPhysicalPlayProbeEditor.cs` at the negative-window `IsUninterruptedPostWindowFrame` guard; this Task/Change/Ledger plus alignment/STATE/handoff and result evidence. Gate the guard on `afterWindowTick >= 0` for both negative timing modes. Preserve physical InputSystem event scheduling, fixed-tick stepping, all production scripts, DAT, images, Scene, GameConfig, other probes and earlier result files. No computer-use or second Editor.

Acceptance: retain the pre-fix FAIL; exact one-line guard correction; original Editor compiles with zero errors; one fresh after254 Play from a clean saved Battle Scene after bootstrap readiness, with initial PP500 and a unique output; compare action/phase/PP/`InputMpConsumedTotal350` to formal source/release negative window without claiming same-world pixel parity. Exit Play, verify Battle/Menu/GameConfig disk SHA unchanged and Console errors zero. The older first253 and current second253 evidence are neighboring regression witnesses; rerun them only if this guard change affects their path. Run `Tools/Validate-ChangeLedger.ps1` and `git diff --check`. Rollback is this exact test-only line through repository approval rules; no old resource deletion.

Exit evidence: the test-only condition now requires `afterWindowTick >= 0` for both negative timing modes. Original Editor script compilation and the after254 natural physical-device Play passed after Battle bootstrap; the independently obtained second253 negative Play also passed. The positive first253 evidence remains the adjacent witness. Play exit left the original Battle scene clean, Battle/Menu/GameConfig SHA unchanged, and Console error count zero. Exact result paths, tick/resource milestones, the pre-fix false negative, and limits are in `artifacts/diagnostics/NTSD28-Q07-RASENGAN-AFTER254-PROBE-GUARD-001/ACCEPTANCE-20260925.md`. This closes only this diagnostic guard; Q07/R18 remain open.
