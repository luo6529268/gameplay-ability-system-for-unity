# NTSD28-Q09-PLATFORM-SHADOW-OFFSET-PUBLICATION-001

Status: `IN_PROGRESS`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001` / BATCH-05 Q09, triggered by Q06 platform field publication return. Q07/Q08 aggregate exits remain open; this independent shadow consumer uses existing production data and the project's existing shadow resource.

Authority: formal paired playable `render_snapshot.cpp::build_render_snapshot` places shadow top at `position.z + render_shadow_offset_10c - shadow.height/2` while `depth_order` remains `position.z`. `battle_world.cpp` operation30 writes `render_shadow_offset_10c=current_reference` and resets it each candidate pass. The native nameplate also reads this field, but character HUD/nameplate treatment belongs to a separate Q09/approved-exception decision and is not claimed here. Unity already writes/copies/checksums `NTSDEntityRuntime.RenderShadowOffset10C`; `LF2Entity.UpdateShadow` and `BattlePresentationShadowBuild` currently ignore it.

Precise edit scope: `Assets/NTSD/Scripts/Animation/LF2Objects/LF2Entity.cs` (`UpdateShadow` center Y only); `Assets/NTSD/Scripts/Simulation/Presentation/BattlePresentationShadowBuild.cs` (`BattlePresentationEntitySnapshot` capture/property/copy and shadow command position only); `Assets/NTSD/Scripts/Test/Editor/BattlePresentationCommandWriterEditorTests.cs` focused shadow snapshot/command assertions. No DAT, PNG, shadow prefab, Scene, ProjectSettings, input, mode, entity logic or nonbattle owner changes. Existing dirty work in `LF2Entity.cs` is protected; edit only its separate `UpdateShadow` line.

Test-first acceptance: a zero/nonzero offset pair must retain entity Z, depth sort, base order and existing foot-marker ground anchor, while only shadow command Y changes according to native `Z + offset`. Snapshot resolution and base-order copies preserve the value; legacy center uses the same value without writing logic state. Original Editor compile0 and focused test pass; then target Battle Scene Play/read-only shadow publication if a safe existing fixture exists, otherwise leave runtime acceptance pending. Check the 11-stage shutdown owner invariants (no new owner added), Scene SHA unchanged, relevant regression, `Tools/Validate-ChangeLedger.ps1`, and `git diff --check`.

Risk: presentation coordinate convention or command copy could shift unrelated anchors. Rollback is a reviewed exact reversal of only these new hunks and test, preserving pre-existing dirty work and records. A focused test alone does not close Q09 or establish root EXE pixel parity; any untested Legacy/Play/EXE gate remains explicit.
