# NTSD28-Q07-HIT-PROJECTION-RESOURCE-MOTION-SYNC-001

Status: `FOCUSED_TEST_PASS / SCOPED_SHADOW_FIELD_PARITY`. Parent: `NTSD28-UNITY-BATTLE-REALIGNMENT-001 / BATCH-04 Q07`.

The shared standard/reduced production writer now applies the formal `hit_response.cpp` double half-`dvx` and `battle_world.cpp` hit-resource transaction. The ECS `BattleEcsHitExecutionPlan` ShadowCompare projection still calculates reduced ground half-`dvx` as integer and omits the type-0 standard/reduced resource transaction. Original Editor existing `ShadowCompare_AlternateCharacterDamageWriterEffectMatchesAuthorityState(False/True)` failed 2/2 after the production fix with `ObservationWriterEffectMismatch`, mask `0x20000000000000` (target PP/related vital group); job `eb4d108e6e0c4cbdacefa5dead4c74ab`. This is an observed diagnostic-path mismatch, not an observed gameplay raw difference; the strict X550/X1200 production raw traces already match formal mapped fields.

Declared scripts before editing: `Assets/NTSD/Scripts/Simulation/Ecs/Hit/BattleEcsHitExecutionPlan.cs` and `Assets/NTSD/Scripts/Test/Editor/BattleHitExecutionPlanEditorTests.cs`. Keep production writer, DAT, Scene, camera, config and nonbattle paths unchanged. First extend existing reduced ShadowCompare to odd `dvx` and record RED. Then make projection mirror the already-authorized shared resource transaction for both unarmored and reduced type-0 hit routes, including owner/target PP and MP-consumption aliases, and use floating half-`dvx` for the reduced ground case. No OID/skill special cases, no extra manager or shutdown phase.

Acceptance: original Editor compile zero errors; existing reduced ShadowCompare 2 cases plus odd case GREEN; adjacent standard ShadowCompare relevant cases GREEN; source/root-EXE X550/X1200 raw production result remains applicable because production code is unchanged. Run Ledger validator, diff check, protected hashes and recovered-doc NUL checks. Any unsupported owner alias or uncommon resource branch must be reported honestly instead of weakening assertion. ShadowCompare parity does not close Q07 natural Play/GPU/content gates.

Rollback only exact projection and test hunks after review, keeping previous production fixes and user work.

2026-09-26 scoped exit: newly added odd-`dvx`/non-full-PP ShadowCompare test is GREEN 1/1 after original Editor compile zero errors. Existing reduced/standard 21-case run now reaches writer-mask-zero and plan-valid assertions in all cases but fails later historical value assertions; it is reported FAIL, not GREEN. This differs from the contract's intended whole-test exit and remains open for separately source-backed expectation audit. Production X550/X1200 raw parity was established by the preceding packages and was not rerun after this diagnostic-only projection edit. Exact jobs and boundaries are in `artifacts/diagnostics/NTSD28-Q07-SASUKE-ARMOR-TARGET-FIRST-DIFF-001/PROJECTION-SYNC-ACCEPTANCE-20260926.md`.
