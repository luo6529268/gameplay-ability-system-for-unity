# NTSD28-Q07-NATIVE-CLAMPED-CELL-PUBLICATION-001

Status: `RUNTIME_PENDING / ORIGINAL_EDITOR_CATALOG_PLAY_PASS`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001` / BATCH-04 Q07, presentation revisit Q09/R17.

Authority: formal root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; paired playable D3D11 renderer submits original DAT sprite rectangles with point/CLAMP sampling. `NTSD28-Q07-OID32-PAIRED-OFFSCREEN-PIXEL-001` proves one action95 79×79 white GPU region, while original Unity Battle Scene publishes pic0 but omits pic64. The six in-range base-pic overflows and source bytes are catalogued by `NTSD28-Q07-ALL-FRAME-SPRITE-COVERAGE-001`. No DAT value changes are permitted.

Production change scope: `Assets/NTSD/Scripts/Animation/Manager/CharacterAnimtorManager.cs` only. Add a generic formal-PNG prewarm adapter for declared drawable cells whose source rectangle extends beyond the decoded image. Sample each requested pixel from the decoded sheet at clamped coordinates in the existing bottom-origin pixel array, preserving width/height and RGBA. Deduplicate derived cell textures/sprites by dimensions and sampled pixel content within one publication, stage them in existing sprite/texture/atlas source ownership sets, and publish catalog entries with valid in-bounds derived source rects. Do not enlarge the original sheets or weaken `BattleSpriteCentralBinding.IsValid`; keep legacy Sprite and central atlas consumers on the same derived pixels. Do not apply the adapter to legacy/nonformal BMP or unrelated UI/background/mode art. Allow source cell wholly or partially outside any edge, including nonwhite and transparent pixels. The original PNG and DAT remain byte-identical.

Focused test ownership: one new Editor test file and its Unity-generated `.meta` under `Assets/NTSD/Scripts/Test/Editor/`, covering in-bounds unchanged, bottom/right/left/top and partial overflow, transparent/nonwhite edge, OID32 formal PNG pic64, dedup reuse, and builder/central-binding contract. Extend the existing opt-in `NTSD28Q07Oid32PublishedCatalogProbeEditor.cs` only to count virtual clamped catalog entries, unique derived textures and central binding modes during its original-Scene Play witness; preserve its one-shot request/exit behavior and prior report schema fields. Revise the earlier diagnostic test's expected `pic64 == null` only after the production adapter and publication witness are correct; `BuildIndexedSpriteRects` may remain a raw clipped-rect helper with its diagnostic test unchanged if the new adapter wraps that result. A separate original-Editor Battle Scene Play witness must find pic64 with a valid central binding and compare the entity pixel region against the paired-source PNG, without changing scene files.

Pre-change sizing audit: a scratch parser recognized 769/773 previously inventoried sheet rows; 1,212 declared cells extend beyond their images, 1,209 wholly so. Clamped pixel bytes total about 46.29 MiB before dedup and 36 unique `(width,height,RGBA)` payloads / about 3.88 MiB after dedup. This is a sizing signal, not complete runtime-offset coverage or a production allocation guarantee. Guard checked dimensions, generated-pixel length, stable virtual source identity and failure atomicity; observe actual prewarm/atlas counts and allocations in the original Editor.

Risks: adding virtual atlas sources can change page layout or publication memory; a partial cell must retain full native visual size/pivot; transaction cancellation/failure must release every derived Sprite/Texture; content fingerprints must still derive only from formal source bytes, not temporary outputs. Central publication may reject virtual-source geometry and must fail closed without replacing the prior catalog. Preserve Unity/GAS framework, nonbattle behavior, all user exceptions, and current 11-stage shutdown order.

Exit: Task/Change/Ledger/STATE/handoff recorded before code; focused EditMode tests in original Editor; current Unity compile 0 errors; original Battle Scene formal-root prewarm/catalog pic64 and central binding; actual same-state Play pixel witness against the paired-source control; existing relevant catalog/atlas tests; `BattleRuntimeSelfCheck` only if production publication change requires it; Scene disk hashes/dirty unchanged, zero borrowed derived resources after stop; `Tools/Validate-ChangeLedger.ps1` and `git diff --check` pass. Any unrun tier is reported as pending. Rollback is the exact new production/test change under the repository's protected-worktree approval rule, retaining prior evidence. No file deletion, Git reset/restore/clean, second Unity project/Editor or computer-use.

Partial result: original Editor new tests 3/3, combined ownership tests 5/5, six exact native-range/alpha/atlas tests 6/6. Two independent formal-root Battle Scene Play publications reached tick5; pic64 present with Legacy Sprite and valid central binding, count 29,034 versus prior 27,825. Second Play counted 1,212 clamped entries, 36 unique derived textures/sources and 0 invalid bindings. Scene hashes and dirty state remained stable. Controlled entity pixels, actual memory high-water and post-stop derived-resource borrower count remain unverified; see `artifacts/diagnostics/NTSD28-Q07-NATIVE-CLAMPED-CELL-PUBLICATION-001/ACCEPTANCE-PARTIAL-20260925.md`.

2026-09-25 later scoped exit: `NTSD28-Q07-OID32-UNITY-ENTITY-PIXEL-001` verified the controlled OID32/frame95/pic64 production command and WorldCamera white pixels after a full Driver tick, restored fixture object/slot/pool counts, and counted zero live `native_clamp_` textures after Play exit. This supersedes only the prior sentence's entity-pixel and post-stop texture-count gaps. Transient memory high-water, natural entrance and direct root-EXE GPU A/B remain open; this production Task stays `RUNTIME_PENDING` until its wider exit is met.
