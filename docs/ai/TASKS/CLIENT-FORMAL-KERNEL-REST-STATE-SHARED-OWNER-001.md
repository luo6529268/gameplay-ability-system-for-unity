# Task Contract — CLIENT-FORMAL-KERNEL-REST-STATE-SHARED-OWNER-001

> 状态：`FOCUSED_TEST_PASS / SHARED_REST_STATE_OWNER_READY / GOVERNANCE_CLOSED / USER_STANDING_AUTHORIZED / CLIENT_INTEGRATION_REQUIRED / FORMAL_MARKER_FALSE / S0_NOT_VERIFIED`
> 创建：2026-08-30

## 1. 目标

将已经dependency-closed、平台无关的`RuntimeRestStore` production source和原GUID移入Server-owned `com.ntsd.battle-kernel/Runtime/Core`，建立Unity/.NET单一物理owner，将package tuple对齐到`0.4.0`，并消费冻结57-line corpus与全回归。只允许三个既有collision-eligibility方法及fresh Unity compile发现的既有SelfCheck只读`RuntimeRestBindingHandle.Token`做internal→public可见性调整；属性/方法体与顺序不变。

## 2. 验收

先取得package owner缺失的test-first红证据；随后证明single source/GUID、57-line digest/all hashes、dense/sparse/lease/0B、0.4.0 direct+locked artifact、Unity compile/focused/S0/lockstep/SelfCheck、Server全回归与治理通过。

## 3. 禁止

禁止修改rest/battle语义、checksum/snapshot schema、recovery policy、StageSpawn、30Hz、RuntimeSlotTable、Registry allocation、Scene/资源/Input Actions、TargetTick/InputDelay、formal AI、S1 wire、transport/Socket/数据库/公网/控制面和formal marker。

## 4. 回滚

只将同一source/GUID移回原Client路径、恢复0.3.0 tuple并移除本包消费者；保留Cut D seam与其他用户改动。

## 5. 完成证据

单一Server owner/GUID、57-line digest/all hashes、`0.4.0` direct+locked artifact、Unity compile0、package1/1、rest related26/26、S0+session17/17、extra lockstep21/21、fresh SelfCheck、Server Debug/Release双配置全回归与治理均通过。Formal marker保持false，S0/S5未晋升。
