# 帧推进与生命周期原函数见证

IN_PROGRESS / SOURCE_MODEL_DIAGNOSTIC_ONLY。准确唯一脚本 Tools/NTSD28AuthorityTrace/frame_step_lifecycle_witness.cpp。以既有只读构建器链接正式playable closure的BattleWorld28::step_frame_slot/resolve_pending_lifecycle，真实DatParser读取合成声明，不在runner重写规则算法。验证Normal return_to_zero策略、隐式0..998/声明999、wait/latch、正负next/212/999/encoded terminal、成本符号/相等/不足/原destination保持、stats.recmp/mode/double/waiver/local gate、holder/hold/terminal14/type3与cpoint。

输出完整输入、after-frame和after-lifecycle状态及事件/声音，确保可复跑字节一致，当前正式EXE和75源码/header身份复核。此见证仅调用两个真实World成员，不冒称SimulationTickDriver完整pass或正式EXE人工输入验收；中间OPoint/particles/previous078/weapon fragments未被该runner执行，需要另外实际driver验证。无Unity生产代码、Scene、资源、版本、框架、非战斗、外部源码变更；不创建runtime manager或关闭阶段。

验收：构建成功；所有document.ok与合法spawn通过；矩阵执行完成、选定关键对照事实断言和重复stdout逐字节一致；记录原source分支和Unity实际caller对照/first-difference待实测；ChangeLedger通过。出错须保留诊断，不用旧实现补authority。输出仅当前repo Temp及本artifact。回滚须批准仅删除此新增诊断差量；用户工作不回退。父reader及完整战斗保持未完，禁止computer-use。

最终VERIFIED / SOURCE_MODEL_DIAGNOSTIC_ONLY，2676向量/重复字节一致，详见artifacts/diagnostics/NTSD28-Q06-NATIVE-FRAME-STEP-LIFECYCLE-SOURCE-WITNESS-001/REPORT.md与CALLER-MAP.md。Unity实现未改，next为FRAME-TRANSACTION-INTEGRATION。
