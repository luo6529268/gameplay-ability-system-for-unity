# NTSD28-Q09-SAME-Z-PAINTER-ORDER-001

Status: FOCUSED_TEST_PASS / RUNTIME_PENDING; original Editor compile 0 errors, focused 14/14 and SelfCheck PASS; original Battle Scene painter pixel witness pending. Parent: BATCH-05/Q09, P-04 and R16. Formal authority: root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; playable `render_snapshot.cpp` stable-sorts `entity_commands` by depth ascending and physical slot descending at equal depth, and `d3d11_renderer.cpp` consumes those commands in order. Separate sprite/spark source arrays have their own ascending-slot internal sort; the painter-order target is the interleaved `entity_commands` stream, not those source arrays.

Current first difference: Unity `SimulationStageRenderModule.ComparePresentationRenderOrder` and `BattlePresentationCoordinator.ComparePresentationSnapshots` use slot ascending on equal Z; the central stable-slot radix materialization also preserves that ascending tie. Thus both its compatibility route and central render orders disagree with the formal command painter order for overlapping equal-Z entities. Fixed world camera and other user exceptions stay intact.

Exact production paths: `Assets/NTSD/Scripts/Simulation/Stage/SimulationStageRenderModule.cs` and `Assets/NTSD/Scripts/Simulation/Presentation/BattlePresentationShadowBuild.cs`. Exact test/probe paths: `Assets/NTSD/Scripts/Test/Editor/BattlePresentationBeginFrameReuseEditorTests.cs` for independent equal-Z and signed-Z reference/fast/fallback behavior; `Assets/NTSD/Scripts/Test/Editor/BattleCentralLivenessIdentityVisibilityPlayModeProbeEditor.cs` for its existing same-Z painter oracle. No DAT, sprite, Scene, Prefab, HUD, logic order, collision, position, checksum, camera, or nonbattle change.

Implementation: preserve depth ascending. Reverse only equal-depth physical-slot rank in both comparison paths and in each equal-depth run of the central radix index list, without allocation or moving frozen physical rows. Keep the stable-id tie as a deterministic same-slot fallback. Review resulting `PresentationBaseOrder`/SpriteRenderer sorting semantics so larger slot draws first, smaller slot appears in front. Do not reverse the independent native sprite/spark source-array semantics or reorder logic entities.

Test-first acceptance: focused Editor reference tests should fail on the old tie; after implementation both fast and fallback/legacy paths should pass, including extreme signed Z, same-Z overlapping physical slots and snapshot copy. Existing formal OID223/224 Battle Scene Play probe should verify same-Z painter order and ordered cleanup; compile 0 errors, focused tests, `BattleRuntimeSelfCheck` and unchanged Scene hashes. If Play is blocked, report `RUNTIME_PENDING` rather than verified. Rollback is the exact two production and two test/probe file deltas only, preserving all preexisting dirty work.

Scope amendment before editing: the fresh BattleRuntimeSelfCheck run failed at the existing compact painter-order oracle because it assumes ascending equal-Z slots. Add only Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs to this package to update compact and legacy sorting expectations to the formal descending-slot painter contract; rerun the full self-check. No production scope expansion.
