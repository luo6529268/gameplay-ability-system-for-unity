# NTSD28-Q07-RASENGAN-MISS-FORMAL-PLAYBACK-001

2026-09-25 second diagnostic amendment before re-editing: RNG-extended source CSV proves one synchronized call already at tick1 with call site `0x004021E0` (random BGM selection), while formal LFR playback tick0–28 has zero calls and stores selected BGM 2 / `bgm\\stage1.wma`; both table hashes match. This one-call offset explains the tick29 attack-selector value difference as a diagnostic initial-BGM mismatch. Set **only this source bridge's** `bgm_selection_49f18c=2`, the formally observed selected value, to remove the source-only random pre-draw. Generate new `_bgm_fixed` outputs and new EXE/report/trace names; preserve both earlier source/LFR/trace sets. If actor action and RNG now align, report the actual remaining Unity differences separately and retain Q07 open.

2026-09-25 diagnostic expansion before re-editing its sole C++ path: first source LFR/formal EXE replay found one action difference at tick29 (source25/release20) while tick30 OID434 Vx is0 on both. Formal trace recorded its first synchronized RNG draw at tick29; original source CSV omitted RNG state. Add counter/index/calls/table hash columns to this existing probe and use **new suffixed output filenames** so the first LFR/CSV/EXE trace remain untouched. Rebuild into a new EXE path and re-run to classify the exact RNG seed/table state; do not silently substitute the new run for the preserved first result. Scope remains the same sole diagnostic C++ file.

Status: `VERIFIED_SCOPED_DIAGNOSTIC`; parent `BATCH-04/Q07`, triggered `R18` and the prior `NTSD28-USER-NARUTO-RUN-ATTACK-WINDOW-001` tick30 OID434 first difference. Formal source/release 32-tick scoped comparison is 352/352; Unity differs only in 26 X fields among the listed fields, explained by the user-approved relative displacement scale when each spawn parent is used as the local origin. Q07/R18 remain open for natural input and picture result. Full report: `artifacts/diagnostics/NTSD28-Q07-RASENGAN-MISS-FORMAL-PLAYBACK-001/REPORT.md`.

Authority: root formal `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, corresponding playable `GameSession28`/`GameSessionLfr28` build closure and formal runtime DAT. Existing original Editor 32tick `(26,2)` output is at `artifacts/diagnostics/NTSD28-Q07-STATE15-POSTPHYSICS-VELOCITY-001/`; its reconstructed legacy-schema seed is a diagnostic input, not current rule authority.

Only new governed code path: `Tools/NTSD28Q07Diagnostics/rasengan_miss_lfr_probe.cpp`. Build a source bridge from the formal playable/core closure. Use an explicit two-entity ordinary battle matching the current Unity fixture: Naruto OID2/action241/HP500/MP200 at (500,0,650), OID7/action0/HP500/MP500 at (1200,0,650), Stage23, seed682973786. Record 32 completed ticks with physical J/attack for player0 on ticks27–28, no other buttons; save a new LFR and source rows, refusing to overwrite previous outputs. Replay only through the unchanged root formal EXE with explicit external action241/MP200 and both formal VFS roots. Compare tick/action/MP and OID434 slot/action/state/position/velocity against source and current Unity raw output, reporting the first difference and exact comparison scope.

Do not modify formal source/EXE, Unity runtime, DAT, PNG, Scene, Prefab, ProjectSettings, Menu or preexisting artifacts. Source-generated LFR plus formal EXE playback is a controlled release witness, not an independent native physical recording or pixel/input-window certificate. The user's excluded native background/mode DAT are allowed only as read-only formal EXE inputs, never staged into Unity.

Exit: verified EXE SHA before/after; diagnostic compile0; unique source LFR/rows; formal EXE exit/report/trace; first-difference result with hashes; Change Ledger/diff check. If the reconstructed Unity seed and formal initial world differ, keep the discrepancy explicit and do not claim same-state parity. Rollback is only the new C++ diagnostic file and new artifacts under repository approval rules.
