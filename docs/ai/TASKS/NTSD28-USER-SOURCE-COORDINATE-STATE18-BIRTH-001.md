# NTSD28-USER-SOURCE-COORDINATE-STATE18-BIRTH-001

Status: FOCUSED_TEST_PASS / CARRIER_NOT_ACTIVE. Parent D-024 all-entity displacement ratio audit. This package covers only state-18/19 broken-weapon particle birth in the active `BattleNativeState18ParticleWriter.Materialize` route.

Authority: paired formal playable `BattleWorld28::materialize_state18_broken_weapon_particles` (`battle_world.cpp` around 3030-3165) uses the source's integer X/Y/Z for generic child birth, then writes particle precise X=`source.precise_x + random_x`, precise Y=`source.precise_y-random_y`, precise Z=`source.precise_z`, retaining the initial integer mirrors until the later sync boundary. Unity already applies D-024 X ratio to physical precise X and preserves battle initial integers. Its new independent source-rule X/Z task fields are currently absent for this producer. No new DAT values may be edited.

Exact script scope: `Assets/NTSD/Scripts/Simulation/Ecs/Writers/BattleNativeState18ParticleWriter.cs` and `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06State18SpawnEditorTests.cs`. No other script, resource, Scene, camera or ProjectSettings is in this batch.

Implementation: when source history is initialized, pass explicit source-rule precise X=`source.SourceRuleX + already-drawn dx`, Z=`source.SourceRuleZ`, and independent source initial integers from `source.SourceRuleXInt/ZInt` through the existing pooled OPoint task. Keep source absent otherwise. Preserve random draw order/count, physical battle X/Z, Y and velocities, lifecycle/admission, default Z skip and snapshot semantics. Do not derive raw coordinates by inverse-scaling battle coordinates and do not turn on source-rule gameplay readers.

Acceptance: focused original Editor test-first RED and GREEN with an initialized source whose source precise/int differ from scaled battle precise/int, factor1/configured views and an absent-source control; assert child source precise/int and current battle physical positions, RNG order/count and no extra objects. Reuse the existing state18 formal witness. Run narrow original Editor compile/test, ledger validator, diff check and Scene/content boundary check. Real Play is optional for this production writer increment unless focused test reveals a lifecycle discrepancy. Rollback only the two scoped script changes, preserving all other dirty work.

Execution 2026-09-24: original Editor test-first RED dc51741a3def4b858cc89621cdc87972 ran four focused cases: absent-source controls passed, initialized-source cases failed at child SourceRulePositionInitialized=false. Production now forwards only initialized source precise/int via the existing task; same dx draw is reused. GREEN 6a2dc3bdf77941218379ca945b28e1af passed 6/6 (new four plus previous physical-ratio two), errors console empty after fresh compile/domain reload. Ledger validator PASS (748 records), diff check PASS, Scene SHA unchanged and scoped content/config/Scene/ProjectSettings status empty. No full matrix, SelfCheck or real Play run; no gameplay source reader activated.
