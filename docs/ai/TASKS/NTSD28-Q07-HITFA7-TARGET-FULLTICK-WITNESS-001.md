# NTSD28-Q07-HITFA7-TARGET-FULLTICK-WITNESS-001

Status: CODE_WRITTEN / corrected formal Stage23 three-tick source witness captured, Unity compile and same-initial-state full-tick comparison pending. Parent: BATCH-04/Q07, R15 and `NTSD28-Q07-NONCHARACTER-HITFA7-PRODUCTION-001` target-present acceptance.

Trigger: the existing missing-target kind trace is equal after the production correction, but target-present OID875/action55 is only covered by local AI and a direct physics seam. Formal action55 also has `dvy:1` between AI and physics; `-21.6` from that local seam is not a full-tick expectation. The generic scenario schema has no initial target-slot or velocity field, and source raw capture omits previous Y. Do not infer full-tick parity from the local test.

Authority: formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033` and its paired playable source. `GameSession28::step` drives `SimulationTickDriver28::step`; non-character AI precedes frame motion (`dvy`) and physics. Formal `resources/runtime` OID875/type3 action55 has `hit_Fa:7`, `dvy:1`; OID99/type0 is the preassigned target. Build participation and exact line references are in the preceding source audit and will be recorded in the witness report.

Declared script paths: new single-purpose repository diagnostic runner `Tools/NTSD28AuthorityTrace/hitfa7_target_fulltick_main.cpp` and battle-focused Editor test `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07NonCharacterHitFa7EditorTests.cs`. The generic `authority_source_capture_main.cpp` already has unrelated dirty R15 changes; a separate runner avoids modifying or coupling this witness to that file. This is a pre-edit scope correction. No J: authority source, production runtime, general scenario schema, Scene, Prefab, ProjectSettings, old asset, or nonbattle edit. A new fixture JSON and source/Unity witness outputs live under `artifacts/diagnostics/NTSD28-Q07-HITFA7-TARGET-FULLTICK-WITNESS-001/` (Temp build outputs allowed). The dedicated runner must reject any non-matching fixture rather than silently injecting targets into ordinary captures.

Acceptance: build the diagnostic runner against the unchanged paired playable source and verify its manifest hashes; initialize legal slots0/1 with OID99/action0 and OID875/action55 from formal content; only after session initialization set subject target slot0 and initial Vy3.8. Capture initial, completed tick1 and preferably tick2 action, Vx/Vy/Vz, integer/precise/previous Y and entity count. Unity must use the same formal staged content, world setup, real driver full tick, input and initial mutations; compare first differences including frame-motion dvy. Keep the existing generic capture and frozen kind scenario behavior unchanged. Original Editor compile, exact focused test, Change Ledger, scene hashes and rollback state must be recorded. If the source witness differs from static estimate, source runtime wins. Formal EXE screen/Player remains separate.

Risk and rollback: the new runner must prove the intended initial fixture rather than invent a different simulation; no existing diagnostic executable or ordinary CLI behavior changes. Preserve other dirty work. Review exact declared diff; destructive rollback requires repository approval.
