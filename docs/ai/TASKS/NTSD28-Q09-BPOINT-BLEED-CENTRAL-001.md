# NTSD28-Q09-BPOINT-BLEED-CENTRAL-001

Status: `FOCUSED_TEST_PASS / RUNTIME_PENDING`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001`, BATCH-05/Q09/P-08. Q07/Q08 aggregate dependencies remain open. This is the central ordered-render outlet for current formal low-HP `bpoint` content; it is not the P-08 aggregate exit.

Authority: root formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable `render_snapshot.cpp` body/bpoint block and `entity_commands` ordinal, and `d3d11_renderer.cpp` solid-rectangle draw. `artifacts/diagnostics/NTSD28-Q09-BPOINT-BLEED-20260926/AUDIT.md` records the exact live consumer gap. Current formal content has 318 bpoints in ita/sasu; all declare X/Y only, no optional width/height/color/respond or stats bleed_hp. Thus the current release behavior uses generic source defaults: HP <= baseMaxHP/3, opaque red, 1x3 source pixels. No OID branch or DAT edit is allowed.

Pre-change Unity: `LF2FrameData.BloodPoints` already holds immutable source-order X/Y; `BattlePresentationEntitySnapshot` omits it; `BuildCommands` emits no bleed command. `BattleCatalogCentralResourceResolver` accepts no solid primitive. The existing central mesh backend is ordered and multiplies source-pixel size by `NTSDRenderSpace.BattleVisualScale` as it does for body sprites. `Texture2D.whiteTexture` is already used by battle rendering for other solid UI quads.

Declared script scope: `Assets/NTSD/Scripts/Simulation/Presentation/BattlePresentationShadowBuild.cs` (optional immutable bpoint capture/copy; only CentralOnly body-visible command after Entity; capacity); `Assets/NTSD/Scripts/Animation/Runtime/BattleSpriteCatalog.cs` (append one solid resource key); `Assets/NTSD/Scripts/Animation/Rendering/BattleCentralRenderTypes.cs` (validate and resolve solid command to built-in white texture + existing central material); `Assets/NTSD/Scripts/Test/Editor/BattlePresentationCommandWriterEditorTests.cs` (focused command geometry/threshold/order/copy tests); `Assets/NTSD/Scripts/Animation/Rendering/Editor/BattleCatalogCentralResourceResolverEditorTests.cs` (focused solid resource resolution). The existing dirty test-file hunk is protected. No DAT, PNG, Scene, Prefab, GameConfig, mode, camera, combat world rule or nonbattle edit. No new lifetime owner/shutdown stage.

Implementation contract: derive bleed placement from the resolved displayed body rectangle, so frame facing, center, body shake and approved project visual scale affect the mark and body together; retain logical X/Y/Z and sorting depth unchanged. For facing right use body-left+X; for facing left follow the paired source's body-left+1-X. Top follows body-top+Y. Use current content defaults only because no optional fields exist in this formal corpus; optional-field schema support is a separate explicit content-contract gate if the authority content changes. Emit only when the existing body draw gate succeeds, frame/action <1000 and current HP <= baseMaxHP/3. Preserve source order for multiple bpoints, with capacity based on actual count. Solid resource must fail closed on wrong key, unsupported render state or invalid material contract. Commands must remain after Entity and before tail commands.

Acceptance: test-first RED on missing bpoint/supported solid outlet; original Editor compile0; exact focused threshold above/equal/below, facing placement, source order, body/hidden action gate, snapshot copy and resolver success/failure. Run relevant existing presentation regressions and a fresh original Battle Scene controlled low-HP Ita/Sasu camera readback if the current Editor allows it. Compare at least one same-state mark with formal EXE before declaring P-08. Legacy sprite outlet and formal EXE GPU remain open even if central focused tests pass. Protect Menu/Battle Scene and GameConfig/mode asset SHA; `Tools/Validate-ChangeLedger.ps1` and `git diff --check` must pass.

Risk: wrong command order, pivot/world conversion or resource material can give incorrect pixels or interrupt batching; overestimating body gate can draw bleed during hidden frames. Rollback means reviewing and reversing only this ID's exact hunks without touching pre-existing dirty work. Do not use `git restore/reset/clean`.

Current bounded exit: original Editor compile0, new 2/2, adjacent 32/32 after separately governed old reflection fixture repair, actual mesh vertex/quad/size 1/1, final below-threshold 1/1, ledger/diff and four protected SHA stable. See `artifacts/diagnostics/NTSD28-Q09-BPOINT-BLEED-CENTRAL-001/ACCEPTANCE.md`. Battle Scene camera/Legacy/formal EXE remain open.
