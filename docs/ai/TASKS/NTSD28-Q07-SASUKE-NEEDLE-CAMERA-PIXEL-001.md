# NTSD28-Q07-SASUKE-NEEDLE-CAMERA-PIXEL-001

Status: `VERIFIED_SCOPED_UNITY_CHILD_GROUP_CAMERA_AND_SHUTDOWN / Q07_OPEN`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001 / BATCH-04 / Q07`, adjacent R17/R18. Authority: root formal NTSD2.8-Logan.exe SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable render snapshot and formal OID11→OID440 `chi.dat`/`chi.png` content. Prior same-initial-state 26-tick entity/input comparison and original Battle Scene physical L/D/J natural OID440 births are in `NTSD28-Q07-SASUKE-FORMAL-MANUAL-TRACE-001/COMPARISON-20260925.md` and `NTSD28-Q07-SASUKE-NATURAL-STATE15-MOTION-001/ACCEPTANCE-20260925.md`.

Problem: those checks prove selected fields and formal image binding, but not that the four naturally born OID440 sprites appear in the original Unity camera. The existing `BattleComboPlayModeProbeEditor` owns physical L/D/J input and will remain unchanged. Add one opt-in Editor-only observer `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07SasukeNeedleCameraProbeEditor.cs` plus `.meta` to watch that existing Play run. It must read only the current World/presentation and use the saved Battle world camera. No production script, DAT token/value, image, Scene, camera serialized setting, menu/nonbattle code or additional Unity project.

The observer must require current formal root, original Battle Scene, CentralOnly publication, four naturally born OID440 objects and four matching OID440/pic0 Entity commands; capture a pre-birth camera baseline and post-birth camera image in the same Play, using a temporary RenderTexture with original camera fields restored. Record command handles/positions, projected union ROI, changed nonblack pixels and raw PNG/JSON to unique paths; never overwrite a prior result. Coordinate with the existing physical-input probe by matching run ID, then read both final results. If a step fails, preserve a FAIL artifact and distinguish fixture/camera/command/pixel causes. Do not claim exact formal EXE GPU parity from a Unity-only screenshot.

2026-09-26 first-run correction before the next script edit: the physical L/D/J probe passed and naturally reached four OID440, while the new camera observer saved its failure because it required all four *current* sprite entries to be `pic0`. At the later sampled child frame the natural sprites had advanced to `pic1`; the pre-birth PNG was captured and the Scene/protected Asset hashes remained stable. The observer's visual acceptance is therefore the four current, formally bound `chi.png` sprite cells and four commands matching each child's current effective pic, with the sampled pics recorded. This retains natural births and the same input/Scene; the earlier `pic0` binding remains separately proven by the physical probe. Preserve first-run FAIL JSON and do not rewrite it.

2026-09-26 second-run review before the next observer edit: run `...-02` physically passed and observed four formal `chi.png` pic1 commands at tick15, with 959 changed nonblack camera pixels in their union ROI. Its raw PNGs show all child command positions coincide at birth and the parent changes in that same ROI, so that delta cannot uniquely attribute visible pixels to the child group. Keep the scoped result and both PNGs. The next opt-in capture should wait for four distinct child screen centers and a union ROI that was entirely black in the pre-birth PNG, then record new nonblack pixels. This is a diagnostic sampling criterion, not a change to battle tick, movement, DAT or rendering. If the natural lifespan provides no such frame, report `PIXEL_OWNERSHIP_UNPROVEN` rather than manufacturing one.

Acceptance: original Editor compiles with 0 C# errors; exactly one focused original Battle Scene Play uses existing physical L/D/J, reaches four OID440 and returns to EditMode; observer confirms camera/command/ROI and independently inspected PNG delta; camera state and World/pool shutdown remain clean; Menu/Battle Scene, GameConfig and mode Asset hashes remain unchanged; `git diff --check` and `Tools/Validate-ChangeLedger.ps1` pass. This is a scoped Q07 visual witness, not Q07 completion. Rollback is the new probe/meta and this package's diagnostic request/results only, under repository deletion/restore approval rules; preserve every existing dirty/user file.

Final bounded result: three focused runs were needed because the first observer gate fixed current `pic0` and the second captured overlapping birth sprites. Run `q07-sasuke-needle-camera-20260926-03` provides four natural child commands at distinct projected centers and a baseline-black ROI with 424 independently recounted new nonblack pixels. Physical input and formal image bindings pass; original Editor returned to EditMode, C# Console error query is 0, camera state restored, and four protected hashes stayed fixed. Post-exit World/slot/pool borrower counts and formal EXE same-camera GPU output were not measured here; keep this Task `RUNTIME_PENDING` and Q07 open. Full evidence and preserved failed/scoped runs: `artifacts/diagnostics/NTSD28-Q07-SASUKE-NEEDLE-CAMERA-PIXEL-001/ACCEPTANCE-20260926.md`.

2026-09-26 follow-up declared before editing the same opt-in observer: close only the missing original-Editor exit telemetry for this natural Play. Register an `ExitingPlayMode` read-only witness after the existing `BattleRuntimeEditorShutdownBridge` is registered, retain the current driver reference, and inspect its lifecycle state. Call its idempotent `ShutdownBattleRuntime()` report path **only if already `Stopped`**; never initiate shutdown or reorder the eleven stages. Save a new, unique `runId-shutdown.json` with status, completed stage and remaining World objects/runtime slots/pool borrowers. If the bridge has not finished by callback time, record `NOT_STOPPED_AT_OBSERVER_CALLBACK` rather than changing runtime state. Do one focused physical L/D/J Play with the existing request mechanism, preserve all prior JSON/PNG, recheck camera result, protected hashes, compile and ledger. This does not attempt formal EXE GPU comparison or close aggregate Q07.

Final same-scope follow-up: run `q07-sasuke-needle-camera-20260926-04` repeats physical natural four-OID440 and the tick18 baseline-black 424-pixel child-group camera result. The companion shutdown witness observed `Stopped`, idempotent report `AlreadyStopped / RuntimeMapCleared` and 0/0/0 remaining World objects/slots/pool borrowers. Original Editor returned to EditMode, Console C# errors 0, saved Scene/config hashes unchanged. This closes this Task's Unity-only camera and shutdown acceptance. Formal EXE same-world GPU pixels and aggregate Q07/R17/R18 remain outside this bounded result; full evidence is appended in the linked acceptance report.
