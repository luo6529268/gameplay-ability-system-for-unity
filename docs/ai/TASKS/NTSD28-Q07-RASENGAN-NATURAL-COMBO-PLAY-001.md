# NTSD28-Q07-RASENGAN-NATURAL-COMBO-PLAY-001

Current result: `VERIFIED_SCOPED` for the original Editor synthetic physical-device diagnostic. The earlier exit expectation below that both 253 queue points must convert is superseded: the playable 2tu sampler updates pending local input only on phase 0. Natural second-253 queueing can arrive on action254/phase1 and only be sampled on the next phase0, yielding no action301 exactly as observed. First253 conversion and post254 nonconversion passed; the second253 phase-gated nonconversion passed on a unique rerun. Evidence and limitations: `artifacts/diagnostics/NTSD28-Q07-RASENGAN-NATURAL-COMBO-PLAY-001/ACCEPTANCE-20260925.md`. Q07/R18 remain open.

Status: `VERIFIED_SCOPED`. Parent: BATCH-04/Q07, R18. Authority: formal root NTSD2.8-Logan EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, playable input/host/frame closure, formal Naruto DAT. User reported defense+forward+jump Rasengan and attack during its continuing frames; DAT values may not change. This package is a scoped diagnostic, not a gameplay fix.

Only declared script path: `Assets/NTSD/Scripts/Test/Editor/NTSD28UserRasenganPhysicalPlayProbeEditor.cs`. Extend its existing original-Editor Battle Scene InputSystem keyboard probe with separate, uniquely named natural-start cases for first action253 tick, second action253 tick, and after action254. Preserve the two historical action241-injected menu probes and result paths. Start from existing live Naruto OID2 in authored ordinary standing state; no `ImmediateFrame`, direct action/combo mutation, direct `FrameInputSet`, or synthetic hit. MP may be raised to 200 only if needed and must be reported. Queue L→forward direction→K as separate physical device states; wait for real canonical `NativeInputProxy.ComboState[1]` step1 then right/left step2/3 before K. Record actual consumed FrameInputSet held/pressed/released and exact completed tick, action, pic, combo step, MP, and whether action240/241/253/254/301 was reached. Queue J only after the selected observed action253/254 point; release all synthetic keys on every exit. Fail clearly on missed tick around the window or lack of battle readiness. A menu entry is sufficient; the root will use the existing original Editor via UnityMCP, no second Unity project and no computer-use.

Exit: original Editor compiles with 0 errors; run each of three cases in a distinct Play session and preserve unique JSON outputs; check battle shutdown and Scene/Menu/GameConfig hashes. First/second 253 should reach 301 with canonical J; post254 should not convert in the observed bounded window. If results differ, record first difference and do not adjust DAT or add Naruto-specific production logic. Compare formal playable/EXE in a separate package if necessary; this synthetic device-event diagnostic alone does not prove human physical timing or EXE pixels. Rollback: remove only this exact test-script diff under repository deletion/restore approval; preserve all existing user files.
