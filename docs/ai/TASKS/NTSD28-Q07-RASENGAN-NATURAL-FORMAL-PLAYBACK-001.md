# NTSD28-Q07-RASENGAN-NATURAL-FORMAL-PLAYBACK-001

Current result: `VERIFIED_SCOPED` for the declared source→formal-release controlled replay. Three 55tick reports PASS and six comparable fields across 165 ticks are 990/990 equal. Second253/after254 LFRs encode the same sampled stream, so independent physical press timing remains outside this report. The old Unity `Runtime.MP` is not its skill resource (`Health.PP`); Unity PP comparison remains open. Detailed evidence: `artifacts/diagnostics/NTSD28-Q07-RASENGAN-NATURAL-FORMAL-PLAYBACK-001/REPORT.md`.

Status: `VERIFIED_SCOPED`. Parent: BATCH-04/Q07 and R18. Authority: formal root `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, declared playable `GameSession28::step`/`SimulationTickDriver28::step`/`InputRouter28`, and formal Naruto `c/nar/nar.dat` frames 253/254. Input schedule comes from original Editor `NTSD28-Q07-RASENGAN-NATURAL-COMBO-PLAY-001` JSON, not an independent native keyboard recording.

Write scope: one new diagnostic C++ file `Tools/NTSD28Q07Diagnostics/rasengan_natural_lfr_probe.cpp` plus this Task, same-ID Change Record, Ledger, State/handoff/alignment entries and artifacts. No edit to formal source/EXE/DAT, Unity production, assets, Scene, Prefab, ProjectSettings or nonbattle files. The diagnostic will set a declared two-combatant standing initial state, fixed BGM selection and seed, then use ordinary `GameSession28::set_input` and `step` for three schedules derived from physical L/D/K/J trace: J on the tick after first 253, after second 253, and after 254. Record per-tick input, native phase, actor action/MP and input proxy state; encode each schedule as unique LFR without overwriting prior evidence. Replay each LFR through the hash-verified root formal EXE `--headless-playback-lfr` with trace/report.

Exit: source bridge compiles from the declared playable closure; three source runs produce actions/phases without diagnostics errors; formal EXE reports pass/failureCode0 and comparable trace; first difference for action/phase/MP is calculated, including explicit schedule-vs-Unity initial phase mapping. If a replay disagrees, preserve RED and investigate without editing DAT or adding a Naruto-specific Unity rule. Mark this package scoped only: LFR is generated from current source and replayed by the formal EXE, so it does not prove independent native keyboard timing, pixels, or full Q07/R18. Verify no authority writes, current Unity Scene/config hashes and project diff boundaries. Rollback only the new diagnostic file and this package's additions, subject to repository protection rules.
