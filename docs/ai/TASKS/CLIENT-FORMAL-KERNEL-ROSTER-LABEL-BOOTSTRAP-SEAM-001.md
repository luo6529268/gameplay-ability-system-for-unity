# Task Contract — CLIENT-FORMAL-KERNEL-ROSTER-LABEL-BOOTSTRAP-SEAM-001

> 状态：`FOCUSED_TEST_PASS / ROSTER_LABEL_BOOTSTRAP_SEAM_READY / GOVERNANCE_CLOSED / USER_STANDING_AUTHORIZED / CLIENT_INTEGRATION_REQUIRED / SOURCE_SEAM_ONLY / FORMAL_MARKER_FALSE / S0_NOT_VERIFIED`

## 1. 目标与范围

只将`BattleRosterRuntimeState`和`BattleSlotLabelRuntimeState`的BCL storage/reset定义拆入新的Client-owned state source，并把`MatchConfig`/team/input/label转换放入Client adapter。保持instance call syntax和所有结果值不变；只允许AppManager两处static resolver owner替换、新focused test，以及旧world-scalar结构测试的一处Roster owner路径更新。

## 2. 验收与禁止

先取得new source absent结构红灯；再验证8-slot、10x12 labels、reset、null/disabled/config mapping、independent team/input和source dependency。禁止results/root/package/checksum/snapshot/recovery/battle/tick/input/Scene/resources/formal AI/wire/transport/marker变更。

完成证据：test-first4/1 expected red；compile0；focused5/5；旧结构断言更正后pair10/10和related87/87；fresh SelfCheck于21:12:11为PASS。只完成Client source seam，shared owner与S0/S5未完成。
