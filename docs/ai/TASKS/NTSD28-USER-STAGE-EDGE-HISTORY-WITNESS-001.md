# NTSD28-USER-STAGE-EDGE-HISTORY-WITNESS-001

Status: `FOCUSED_TEST_PASS / TEST_ONLY`. Parent `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (D-024). Original-project Editor job `57c29991fd964a428e6e856c41e1c209` ran only the two declared cases, 2/2 PASS. Factor1 raw40 movement yielded X2040 inside absolute width2048; configured `2048/1333` battle position exceeded the width and production ECS bounds clamped it to2048. This verifies the two-domain boundary dependency before a shared source-rule coordinate-history carrier is implemented. It does not authorize a stage width, DAT, camera or production behavior change. Evidence: `artifacts/diagnostics/NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001/STAGE-EDGE-TWO-DOMAIN-WITNESS.md`.

Authority: shipped Logan playable `BattleWorld28::settle_ordinary_stage_bounds` in `source/ntsd28_core/src/simulation/battle_world.cpp` clamps type0 precise X to the absolute `StageBounds28.width`; `SimulationTickDriver28::step` calls it after prior physics/fusion phases. Unity production `SimulationStageRenderModule.ApplyPreFrameBoundsAll` routes exact characters through `BattleEcsCharacterPreFrameBoundsPass` and compatibility characters through `LF2Entity.ApplyPreFrameXBounds`. User D-024 keeps actual battle travel scaled in a fixed full-background camera; it does not change the absolute stage width.

Declared test-only path: `Assets/NTSD/Scripts/Test/Editor/BattleEcsCharacterPreFrameBoundsPassEditorTests.cs`. Reuse its exact-character registered World factory. Configure absolute width2048, initial X2000, raw Vx40, no X block and legal Z; step through production `CharacterMechanics.StepBattleLogic` with factor1 versus `2048/1333`, then run production `ApplyPreFrameBoundsAll`. Formal raw expected X2040 is inside the width; the approved scaled battle position is expected to reach the same edge and clamp X2048. Record precise/int position and verify both modes. This is a dependency witness, not a claim that the approved D-024 battle distance should be undone.

Acceptance: original project Editor compiles and runs only two focused cases; factor1 and configured current battle results are as stated, with no production changes. Report how a future source-rule reference coordinate must retain its own raw-bound trajectory instead of copying the battle clamp result. No second Unity project/computer-use. Rollback removes this one test method only. Subsequent carrier implementation needs its own complete Task/Change including snapshot/reset/checksum and all writers.
