# NTSD28-Q07-OLD-ASSET-REFERENCE-REFRESH-001

2026-09-25 battle-entry correction: the separate `NTSD28-Q07-MENU-BATTLE-FORMAL-ROOT-GATE-001` package now has original-Editor Menu callback PASS for both empty-root rejection before BattleRunning and formal-root BattleRunning/World2 with ordered return. `CURRENT-LEGACY-READER-REACHABILITY-20260925.md` records the superseding evidence. This does not change this task's static-only status or authorize deletion of 521 old DAT/image files; Menu empty-root prewarm, authoring/preview, explicit historical tools and unknown external callers remain. Do not rerun that gate as if it were still Play-pending.

Status: `VERIFIED_STATIC_REFERENCE_REFRESH_ONLY / READ_ONLY`. Parent BATCH-04/Q07. Entry: production GameConfig formal root and serialized battle/menu caller results; Q01 `old-asset-disposition-review.csv` holds the frozen pre-migration 695-file baseline. This task refreshes old-asset disposition evidence after the root switch. It does not authorize deletion.

Read scope: Q01's exact 695 old Config DAT/image paths and GUIDs; their current existence, size/hash and meta GUID; serialized GUID references in current Assets/NTSD scenes/prefabs/assets; exact quoted literal path references in current NTSD scripts; current data.txt / manager and menu content routing. Classify separately: indexed old DAT/character image, non-character/unknown image, serialized referrer, editor/test consumer, default formal-root versus legacy fallback. Dynamic runtime paths and actual Menu scene/skill presentation remain unknown unless directly verified; do not infer deletion safety from zero static hits.

Output: a new diagnostic report and machine-readable per-file CSV under `artifacts/diagnostics/NTSD28-Q07-OLD-ASSET-REFERENCE-REFRESH-001/`; update Q07 alignment status only with proved facts. No script, asset, Scene, prefab, importer, old file, external authority or Git state modification. No computer-use. Acceptance is a complete 695-path reconciliation with zero silent missing/mismatched input, explicit counts by class/referrer and a precise list of protected examples plus remaining dynamic-reference limits. Any later rebind/delete needs a separate exact Task/Change and the repository's deletion authorization.

2026-09-22 owner extension: the 52 current-old-path serialized references are further grouped in `SERIALIZED-OWNER-CLASSIFICATION.md` and `serialized-owner-classification.csv` (1 inactive Editor preview indexed sheet, 26 Battle UI, 14 Menu UI, 8 GameConfig UI, 2 maps, 1 common shadow). The only formal character-sheet rebind candidate is documented with its DAT/pixel-grid coordinate preconditions; no rebind or deletion was performed.

2026-09-24 current-state addendum: the sole indexed old-image Scene GUID was removed by the separate Sasuke preview rebind, which now has original-Editor visible/11-test evidence. A fresh scan of 383 old indexed image GUIDs across the listed NTSD serialized file extensions found 0 hits. Current `CharacterAssetDeploymentEditorTests` checks formal Logan type-0 DAT/images, not the legacy registry; original Editor job `ed1e857c29b84159bea5bbbe3c583f5c` passed 1/1. The serialized GameConfig formal root uses formal prewarm in Menu/Battle, but empty-root legacy fallback and Editor refresh still consume old data and images. Exact scope, output and non-deletion conclusion: `CURRENT-DYNAMIC-REACHABILITY-20260924.md`. The 2026-09-22 classifications remain historical snapshots; delete authorization remains zero.

2026-09-25 SelfCheck follow-up: the current aggregate `BattleRuntimeSelfCheck.RunAll` no longer reaches any of the four audited old Config DAT readers, and its original Editor result is `PASS` after formal fixture migration. `CURRENT-LEGACY-READER-REACHABILITY-20260925.md` now separates that retired aggregate route from the still-reachable empty-root Menu prewarm, Inspector refresh, Editor preview, explicit historical tooling and unresolved external/default API uses. This narrows the Q07 reference backlog but does not authorize deleting any old DAT/image; all 521 indexed rows remain `deleteAuthorized=false`. The old OID205 six-clone helper stays historical and the formal DDJ chain requires its own live-path witness.
