# NTSD28-Q08-NATIVE-RESULT-CARRIER-001

Status: `PLANNED / SOURCE_FIRST / PRECHANGE`. Parent: `NTSD28-Q08-RESULT-FLOW-SOURCE-MATCHED-001`, BATCH-04/Q08 G-05/G-06.

Authority: matching playable `GameSession28::step()` calls `BattleFlow28::step()` before combat for ordinary direct battles. `battle_flow.cpp` classifies object-type-0 slot occupants, group 1..39 excluding 5, HP positive or revive lives >1; it latches the first terminal outcome, advances a timer across revival, and emits 80/101/350 milestones. Existing full-driver source evidence and Unity group/timing RED are archived in `artifacts/diagnostics/NTSD28-Q08-RESULT-GROUP-CARRIER-AUDIT-001/`. The configured-stage proxy test was retracted by `STORY-SELECTOR-CALLER-CORRECTION.md`; current menu direct mode1 must remain eligible. Formal story selection is a separate missing host input.

Implementation scope: add an independent native result carrier in `BattleResultsRuntimeState` for living-group mask, latched timer, phase/transition and last emitted timer; classify immediately before combat in `NTSDBattleTickSystem` through `SimulationWorld`/`BattleResultsOutcomeHostWriter`. Preserve the existing two-side results UI/reserve transaction during this carrier package; its 101-tick consumption and mode4 reserve authority are parent follow-ups. Limit this package's behavioral acceptance to group eligibility, first terminal tick, latch after revival, 80/101/350 timer state and direct-mode1. Continue-input routing at >=144 requires a separate exact effective-combatant input audit; do not fake it from two-side UI input. A source-matched carrier is an intermediate implementation, not Q08 completion.

Persistence scope: `BattleWorldRosterResultsSnapshot` (payload schema 1->2), `BattleStateSnapshot` (aggregate 25->26), `BattleLockstepChecksumModule` (28->29), `BattleParitySnapshot`, and `BattleStateSnapshotRestore` preflight must capture/restore/hash/export the new deterministic state and reject noncanonical group masks/phase/timer before mutation. Reset/rematch must clear native fields; `ActivateSummary` must not. Update only the relevant exact schema assertion in `NTSD28Q05TraceIdentityEditorTests` if needed. Tests belong in the existing Q08 focused Editor class or a new exact fixture after Record expansion.

Exact code ownership: `Assets/NTSD/Scripts/Simulation/Runtime/BattleRuntimeState.cs`; `.../Ecs/Results/BattleResultsOutcomeHostWriter.cs`; `.../Core/NTSDBattleTickSystem.cs`; `.../Core/SimulationWorld.cs`; `.../Lockstep/Snapshot/BattleWorldRosterResultsSnapshot.cs`; `.../Lockstep/Snapshot/BattleStateSnapshot.cs`; `.../Lockstep/Snapshot/BattleStateSnapshotRestore.cs`; `.../Lockstep/Checksum/BattleLockstepChecksumModule.cs`; `.../Lockstep/Checksum/BattleParitySnapshot.cs`; `Assets/NTSD/Scripts/Test/Editor/NTSD28Q08BattleFlowRedProbeEditorTests.cs`; `Assets/NTSD/Scripts/Test/Editor/NTSD28Q05TraceIdentityEditorTests.cs` for its current version assertion; and `Assets/NTSD/Scripts/Test/Editor/BattleStateSnapshotRestoreEditorTests.cs` for one carrier restore/checksum/preflight case (added before editing that fixture). No Scene, Prefab, content, nonbattle UI, GAS, generated or third-party edit.

Acceptance: current native source fixture identity; Unity focused group/timing RED->PASS on new carrier, zero compiler errors, exact pre/post-combat lethal timing, snapshot restore/checksum replay including reset/invalid-mask no-mutation, nearby result-page/reserve seam, one stable SelfCheck and representative real battle plus close/reenter when implementation is stable. Do not rerun every character or already-closed Q06 suite. Preserve original Scene and user edits. Run `Tools/Validate-ChangeLedger.ps1` before delivery. Roll back only owned hunks under repo approval rules; never reset/clean.
