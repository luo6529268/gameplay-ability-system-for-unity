# NTSD28-Q07-RASENGAN-ACTION35-TARGET-SOURCE-001

Status: VERIFIED / SCOPED_FORMAL_SOURCE_NEGATIVE_CONSUMER_GATE. Parent: BATCH-04/Q07, R09/R12/R18. Q07 and the overall alignment goal remain open.

Authority: root formal `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033` and paired playable `GameSession28::step`, `BattleWorld28::rebuild_geometric_hit_candidates`, and authored formal `c/nar/nar.dat` frame254 and `c/nar/a/ras.dat` frame35. Frame254 creates OID434/action35 through kind2 OPoint; action35 authors kind5 ITR. Prior natural L/D/K/no-conversion 55-tick formal LFR at opponent X1200/Z650 shows action35 at ticks35–37 but no target interaction. Formal/staged DAT values must remain byte-for-byte unchanged.

Scope: add one `Tools/NTSD28Q07Diagnostics/rasengan_action35_target_source_probe.cpp` only after this Task/Change and Ledger/STATE/handoff/alignment registration. Compile against exactly the paired playable/core closure. Keep seed682973786, Naruto OID2 X500/Z650, stage23, mode0, BGM2, natural defend ticks1–2/right3–4/jump5–6/attack36–37 and idle opponent input. Scan a small declared, bounded set of opponent X/Z initial positions without mutating frames, ITR, DAT or runtime rules. Emit non-overwriting per-case CSV with complete-tick Naruto/OID434 action, target HP/action and OID434 candidate-to-target evidence. If a case has an outgoing action35 candidate and an actual target effect, record an LFR for root-EXE playback; if not, report the bounded negative scan and next authority question. Do not treat target HP alone as child-hit attribution.

Acceptance: source compile 0 errors; exact formal EXE and DAT SHA checks; candidate/hit or explicit bounded negative with no invented parity; if LFR emitted, unchanged root EXE headless replay PASS plus per-tick first-difference analysis. This package does not run or modify Unity, claim physical keyboard/visual parity, or close Q07. Next Unity work needs a separate exact Task/Change after source case is established. Do not use computer-use, create a second Unity project, modify DAT/PNG/Scene/ProjectSettings/nonbattle code or delete old assets.

Rollback: only this new diagnostic source and additive package records after exact review; preserve all unrelated dirty work and existing probe outputs.

Result: final paired-source compile exit0 and two independent 11-position/605-tick scans matched byte-for-byte. All cases reached OID434/action35; five positions gave 10 child→target geometric candidates at ticks36–37, and the formal ordinary-hit classifier rejected all 10 because the holder/wpoint weapon-strength substitution was invalid. Target HP stayed 500 throughout; this probe does not claim every possible target field was unchanged. Formal Naruto frame254 has `attacking:0`, while the consumer requires 1..9. No candidate-plus-HP-change LFR was emitted and no root-EXE target-present or Unity run was claimed. Exact hashes, build/setup failures, source branch and next consumer-qualified route are in `artifacts/diagnostics/NTSD28-Q07-RASENGAN-ACTION35-TARGET-SOURCE-001/REPORT.md`.
