# Task Contract — NTSD28-B5-REMAINING-EXIT-AUDIT-009

> 状态：`VERIFIED / GOVERNANCE_ONLY / SYSTEM_TABLE_ATTACKER_TERMINAL_ROUTED / B5_EXIT_NOT_READY`
> 依赖：`NTSD28-B5-FIRST-BDY-RESPONSE-ATOMIC-PRODUCTION-INTEGRATION-001 / VERIFIED`

## 目标

在first-current-BDY response规则族完成production、HitPlan、正式criminal Play验收后，从当前
NTSD 2.8-Logan `resolve_confirmed_unarmored_hit`后续分支及其caller重新选择下一项未闭合、
production-reachable的B5首差；若没有，给出B5最终退出门所需的逐族证据。

## 边界

- 只读Authority playable closure、Unity production、既有Change/manifest/tests与冻结内容catalog。
- 不改C#、Scene、Prefab、Config、资源、ProjectSettings或Authority目录。
- 已验证的first-BDY、hit-group、special-hit latch、kind4/kind8、type1 armor、ordinary defense、
  standard/reduced rest、damage-scale和type3规则族不得无证据重开。
- B6 cpoint/held、B7 spawn/lifecycle、B8 flow、B9/B10 presentation/audio、H/B11内容继续独立路由。

## 验收

- 从first-BDY成功/失败后的精确下一语句继续闭合Authority顺序、字段、副作用与提前返回。
- 为每个候选差异确认Unity owner、formal reachability、既有Change覆盖或真实缺口。
- 唯一选出下一实现/owner-audit包，或形成B5 exit audit；同步Ledger/STATE/handoff/总表。

## 回滚

仅移除本次治理记录；没有行为、内容或Scene回滚。

## 结果

已确认下一首差为system-DAT `henry_arrow`/`john_biscuit`攻击者终结规则的route与顺序：Unity旧
shell tail会泄漏到reduced hit，且OID214清零时点偏晚。下一包为
`NTSD28-B5-SYSTEM-TABLE-ATTACKER-TERMINAL-PRODUCTION-001`。
