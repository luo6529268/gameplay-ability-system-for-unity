# NTSD28-USER-OPOINT-BIRTH-RATIO-001

Status: `FOCUSED_TEST_PASS / PLAY_AND_FORMAL_VISIBLE_PENDING`. Parent `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001`; user decision D-024. This is a bounded late-OPoint child-placement slice, not closure of all spawn kinds or D-024.

Authority: the current formal 2.8-Logan `source/ntsd28_core/src/simulation/object_spawning.cpp`, `ObjectSpawnPlanner28::plan_frame`, belongs to the playable build closure (`source/ntsd28_playable/scripts/build.ps1:73`). It places a child at the parent's integer X/Y/Z plus facing-dependent frame-center/OPoint offsets, with `+1` Z, then assigns launch motion separately. The Unity late producer is `BattleLogicObjectPointRuntime.ConfigureLateOpointPosition`; `BattleLogicEntityFactory.PrepareFinalRuntimePosition` and `LF2Entity.ApplyInitialRuntimePosition` consume its task. The approved fixed-camera D-024 exception changes the **screen fraction of the parent-relative birth offset** while DAT and authored launch motion remain raw.

Scope and ownership: modify both production late-OPoint owners, `Assets/NTSD/Scripts/Simulation/Runtime/BattleLogicObjectPointRuntime.cs` and `Assets/NTSD/Scripts/Animation/Character/LF2ObjectPointFactory.cs`, plus the focused test `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06OpointDepthLivesEditorTests.cs`. The World logic materializer and component materializer each have an independent `ConfigureLateOpointPosition` formula; both must compute right/left relative X offset and `(opoint.Z+1)` depth offset with World X/Z factors once, add them to the parent's already-scaled integer position, and write precise and truncating integer task coordinates consistently. Leave Y, `opoint` data, launch velocity, lifecycle order, stage spawns, drops, pieces and other absolute task positions unchanged. Default-scale diagnostic Worlds must retain the formal integer result on both owners.

Pre-change evidence: both current producers add raw X/Z offsets to a parent already moved under D-024; the final factory handles absolute coordinates and cannot safely multiply them without scaling the parent twice. `NTSD28Q06OpointMaterializerEditorTests` exercises both owners for the default-scale authority source. The matching formal and Unity formulas and other spawn producers are recorded in `artifacts/diagnostics/NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001/INITIAL-INVENTORY.md`.

Validation: the prior broad test belonged to a previous Editor run; the current original Editor returned fresh idle/EditMode with no active test or compile job at 2026-09-23 15:04 UTC. First run new exact cases RED before production edits. Then add right/left configured-world birth cases for **both** actual late materializers. Assert child precise X/Z deltas, integer truncation, raw launch motion and default-scale identity. Compile/reload the original Editor and run only the new focused cases and adjacent OPoint depth class; widen tests only on a relevant first difference. Run `Tools/Validate-ChangeLedger.ps1`, check DAT git status and Battle Scene hash. Real Battle Scene and formal visible ratio are later gates.

Risk and rollback: actual child coordinates alter collision/reach and stage arrival, as approved by D-024; follow-up collision and bounds checks remain required. Roll back by reviewing and inversely patching only this Change's lines, preserving concurrent work. No DAT or nonbattle modification, and no Git reset/restore/clean.
