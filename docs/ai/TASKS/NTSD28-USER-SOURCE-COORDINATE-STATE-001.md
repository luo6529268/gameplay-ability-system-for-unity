# NTSD28-USER-SOURCE-COORDINATE-STATE-001

Status: `FOCUSED_TEST_PASS / CARRIER_NOT_ACTIVE / LOCKSTEP_JSON_PROJECTION_PENDING`. Parent: `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (D-024). User approved fixed full-background camera and proportional physical X/Z travel while requiring generic battle logic, unchanged DAT, and no nonbattle changes. Confirmed first differences: official OID219 behavior5 same current gap224 needs source-history gap224 versus199, and official fusion row0 raw gap40 merges while scaled battle gap50 rejects. Formal playable `NativeAi28::step_non_character_hit_fa` and `BattleWorld28::advance_native_fusions` read source integer positions. This package creates only the storage and versioned runtime-checksum contract; it does not switch either gameplay consumer. Independent JSON lockstep checksum projection remains an explicit successor gate.

Exact script scope: `Assets/NTSD/Scripts/Simulation/Core/NTSDEntityRuntime.cs`; `Assets/NTSD/Scripts/Simulation/Lockstep/Snapshot/BattleWorldEntityRuntimeSnapshot.cs`; `Assets/NTSD/Scripts/Simulation/Lockstep/Snapshot/BattleStateSnapshot.cs`; `Assets/NTSD/Scripts/Simulation/Lockstep/Checksum/BattleLockstepChecksumModule.cs`; focused `Assets/NTSD/Scripts/Test/Editor/BattleWorldEntityRuntimeSnapshotEditorTests.cs` and `Assets/NTSD/Scripts/Test/Editor/BattleLockstepChecksumEditorTests.cs`. No other code path is authorized by this Change ID.

Contract: store separate source-rule precise and integer X/Z, four directional source-rule block flags, and an explicit initialized marker in `NTSDEntityRuntime`. Add explicit source-rule set, integer-sync, and clear-flags methods; do not alias battle position or ECS frame-motion store. Canonical copy and pool reset must include all new state. Runtime component snapshot version 17→18, aggregate snapshot version 28→29, checksum schema 31→32 after confirming live constants; checksum must include all new fields in stable order for entity and raw-slot runtime. Existing snapshot copy already handles both active entity and raw slot, so avoid a second cache. Old-version aggregate restore must reject. No gameplay writer/reader uses this carrier yet; default marker remains false. No DAT/Scene/camera/ProjectSettings/nonbattle edits.

Acceptance: test-first focused RED for absent carrier contract, then original project Editor compile and focused tests covering distinct battle/source values, source integer sync even when battle mirrors unchanged, canonical entity/raw snapshot and reset, reference-only checksum difference, old schema rejection, and existing relevant snapshot/checksum controls. A full SelfCheck is unnecessary for storage-only code unless focused tests uncover a cross-pass failure. Run `Tools/Validate-ChangeLedger.ps1`, `git diff --check`, and verify Battle Scene/content unchanged. State remains `CARRIER_NOT_ACTIVE`; do not claim OID219/fusion/Play/formal EXE parity. Rollback only the six declared files and this package's version increments, preserving all other work; old-format snapshots remain intentionally version-incompatible while this package is installed.

2026-09-24 pre-follow-up scope clarification within the same declared checksum and test files: the first 17/17 pass established active entity and empty raw-slot sensitivity, but independent review found that occupied slots also retain a distinct `view.RawRuntime` payload which snapshot/restore preserves. Before this package exits, add a focused RED case where only that occupied raw payload's SourceRule state changes, then hash its nine SourceRule fields separately in the existing 64-bit checksum path and prove restoration of the original checksum. Keep the version at the as-yet-unpublished 32 for this one package and do not hash unrelated legacy raw fields by accident. Add a successful aggregate capture→mutate both entity/raw source fields→restore readback case in the already-declared snapshot test file. The separate JSON lockstep/extended checksum projection is outside these six paths and remains a successor gate before gameplay activation.
