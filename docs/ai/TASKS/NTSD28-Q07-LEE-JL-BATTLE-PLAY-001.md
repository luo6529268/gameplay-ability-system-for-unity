# NTSD28-Q07-LEE-JL-BATTLE-PLAY-001

Status: VERIFIED / SCOPED_BATTLE_PLAY_NATURAL_CHILD_PUBLICATION. Parent: BATCH-04/Q07 and D-024, both open.

Authority: root formal NTSD2.8-Logan.exe SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable ordinary Lee J→L chain, formal runtime content. `NTSD28-Q07-LEE-NATURAL-NONSOUND-CHILD-001` proved source/release reachability at tick6; `NTSD28-Q07-LEE-JL-UNITY-RAW-FIRST-DIFF-001` proved original-Editor EditMode 45-tick mapped raw/synchronized-RNG parity. The saved Battle Scene Play path has not yet been witnessed for this sequence.

Declared write scope before any script edit: one new Editor-only request probe `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07LeeJlBattlePlayProbeEditor.cs` and its `.meta`, one unique diagnostic directory `artifacts/diagnostics/NTSD28-Q07-LEE-JL-BATTLE-PLAY-001/`, this Task, Change Record, Ledger and top alignment/STATE/handoff notes. No production script, DAT, image, Scene, Prefab, ProjectSettings, nonbattle function or formal source change. No second Unity Editor, computer-use or old diagnostic/result overwrite.

Execution: require original Editor on clean saved `NTSD_Battle`, formal content root and running production World; enter Play in that Editor only. Pause at a stable tick after battle bootstrap. Add temporary formal Lee OID7 and Naruto OID2 human fixtures at free runtime/roster slots with ordinary frame0/input initialization and separated positions. Submit discrete none, physical J, physical L, held L, then release through `SimulationTickDriver.StepOneTick`, with presentation building; do not write frames or DAT values. Record Lee action trajectory, OID814 and OID204 births, child slot/owner/source/physical position, publication carrier, and result/failure. End by explicit ordered runtime shutdown and exit Play, checking zero pool borrowers and saved Scene hashes. Existing world objects are not modified outside the normal full Driver tick.

Acceptance is deliberately a real Battle Scene Play chain, not a duplicate of EditMode raw parity: observed natural OID204 children in the full Driver, valid source positions and presentation carrier, successful ordered shutdown, Editor out of Play, unchanged Menu/Battle Scene disk SHA, and no new C# compile errors. Physical keyboard, final GPU pixel, native-exe same-camera image and all-skill Q07 exit are separate gates. If the scene's existing actors or timing prevent the sequence, preserve exact first failure and report pending rather than force frames or alter production rules.

Validation: original Editor compile, one unique request/result, `Tools/Validate-ChangeLedger.ps1`, `git diff --check`, Scene hashes/status. Rollback is removing only this new diagnostic script/meta after evidence review; no existing user files or native content are candidates for removal.

First run preserved: `lee-jl-battle-play-01.json` FAIL, Play start tick5/end50. Relative inputs were none@6, J@7, L@8–9; Lee stayed frame0 at tick7 then entered frame110 at tick8 and never spawned OID204. Ordered shutdown complete, borrowers0, original Editor returned to EditMode and both Scene hashes remained unchanged. This is a diagnostic first difference, not yet a proven production rule defect: unlike the zero-based formal capture (J on completed even tick2), the Play probe sent J on completed odd tick7. Before further script edit, narrow the fixture to place J on the first suitable even completed tick after at least one idle tick, L on the following two ticks, and record `world.InputPhase` alongside Lee frames. Do not inject an action or alter production input logic; retain first failed report and run under a new runId.

Second run preserved: `lee-jl-battle-play-02.json` FAIL on the fixture's source-carrier assertion after natural success. With J@completed tick8/InputPhase0 and L@9–10, Lee reached frames60/61/145/146/164 and five owned OID204 at completed tick12; CentralOnly had five matching Entity commands, ordered shutdown/borrowers0/Scene SHAs stayed good. All five children reported uninitialized source-rule position because this temporary fixture called `Runtime.SetPosition` but omitted the production participant-birth `AppManager.SyncParticipantBirthPosition`, which sets source rule X/Z. Also its frame log mislabeled `Runtime.MP` as the charged resource; formal raw `currentMp` and production action cost read `Runtime.PP`. Next diagnostic edit must call the existing production birth-position method on both fixtures and report PP, then issue a new unique run. This is a fixture correction, not a production source-position or MP defect claim.

Final scoped result: unique run3 PASS in the original saved Battle Scene; J@tick8/InputPhase0 and L@9–10 naturally reached frame145 at tick10 with PP500→300 and five OID204/ownerLee at tick12. All five have initialized source `(437,651)`, physical `(403,-35,651)` under the approved full-view ratio and matching CentralOnly Entity commands; logic-only materialization has no child Renderer. Ordered shutdown complete, pool borrowers0, original Editor EditMode, Console errors0 and both saved Scene SHAs unchanged. The earlier failed attempts remain preserved. `ACCEPTANCE.md` gives report SHA and limits. Camera pixels, physical keys, full child lifetime and Q07/D-024 aggregate closure are still open.
