# NTSD28-USER-SOURCE-COORDINATE-REVIVAL-EFFECT-BIRTH-001

Current status 2026-09-24: `FOCUSED_TEST_PASS / CARRIER_NOT_ACTIVE / PLAY_PENDING`. Actual-factory source birth matrix job `0a019ffb7ef6469e85fb2756b58cb7f3ad` passed 4/4 and final C07 class job `6f5089efa8e54b7a98cb594ba2d382c2` passed 7/7 after separate test-only phase correction. The older `CODE_WRITTEN` line below is historical. The mistaken broad job has no trustworthy terminal result and cannot supply test-first RED.

Status: `CODE_WRITTEN / COMPILE_PENDING / CARRIER_NOT_ACTIVE`. Parent: D-024 all-entity position ratio; Q07 remains paused.

Authority: current formal `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; `source/README_SOURCE.md`; playable build closure `ntsd28_playable/scripts/build.ps1` includes `battle_world.cpp` and `simulation_tick_driver.cpp`. `SimulationTickDriver28::step` around lines 625–663 creates the continuation OID998/action6 effect using host integer position, increments integer Z by one and calls `spawn_transient`; `BattleWorld28::spawn_at` rebuilds precise X/Z from request integers. This is distinct from normal revival average/RNG and from N30 OID998 actions.

Unity pre-state: `BattleRespawnModule.TrySpawnRespawnEffect` builds a parentless immediate `OPointCreateTask` with current physical XInt/ZInt and child integer Z+1. It does not propagate the initialized source-rule position. `BattleLogicEntityFactory.PrepareFinalRuntimePosition` already adds +1 to precise Z, including task source precise Z when present; `LF2Entity.ApplyInitialRuntimePosition` can apply task source precise/int independently. Thus the formal source child should have `SourceRuleX = SourceRuleXInt(parent)`, `SourceRuleZ = SourceRuleZInt(parent)+1`, and matching source integers; it must not copy fractional parent precise X/Z or reverse the physical view scale.

Declared script scope: `Assets/NTSD/Scripts/Simulation/Passes/Respawn/BattleRespawnModule.cs`; `Assets/NTSD/Scripts/Test/Editor/NTSD28C07RevivalProductionPlacementEditorTests.cs`. No DAT, image, Scene, camera, ProjectSettings, nonbattle module or generated/third-party code edit. Preserve existing physical child coordinates, OID/action, health, group, team, velocity, RNG, slot order, task recycling and shutdown. When parent source position is absent, do not fabricate source history.

Acceptance: test-first RED/GREEN in original Editor. Exercise actual logic-only factory rather than `SetRespawnEffectSpawnOverrideForSelfCheck`, using a registered continuation host and catalog OID998/action6. Cover factor1 and configured 2048×1152 view, initialized source with precise values distinct from integer mirrors, and absent-source control. Assert child source precise/int, physical precise/int, OID/action, RNG unchanged and original host state; focused existing C07 order tests stay green. Compile 0 error, validate Change Ledger and diff, confirm Scene/content boundary. A component path may use the same final-position helper; report whether separately exercised. Do not claim full Driver/Play/formal EXE equivalent from this focused birth test.

Rollback: reverse only the two declared script diffs, preserving all unrelated dirty work and earlier D-024 packages. Documentation history remains append-only.
