# NTSD28-USER-KIND14-PRECISE-THRESHOLD-WITNESS-001

2026-09-24 correction: the original-Editor 2/2 observation remains valid, but it is not a source/Unity first difference. Formal playable kind-14 compares integer `Position28::x/z`; this Task's precise-formal expectation is superseded by `NTSD28-USER-KIND14-INTEGER-OPERAND-AUTHORITY-CORRECTION-001`.

Status: `FOCUSED_TEST_PASS / FIRST_DIFFERENCE_CONFIRMED / TEST_ONLY`. Parent `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (D-024). Scope is one focused Editor test method in `Assets/NTSD/Scripts/Test/Editor/BattleHitExecutionPlanEditorTests.cs`, using its existing registered-World `CreateScenario` and production `PostInteractionTickAll` route. No production, DAT, image, Scene, camera, ProjectSettings or nonbattle edit.

Authority: shipped Logan playable `BattleWorld28::resolve_special_relation_hit` kind14 compares precise attacker/target X/Z and publishes directional collision flags; `PhysicsIntegrator28::step` consumes them once. The paired source test `test_native_kind14_sets_directional_single_tick_collision_flags` confirms a geometric candidate and both directions at integer positions. Official staged `decoded_dat/w/1.dat` frame20 has a kind14 record, establishing content presence but not this fixture's official in-game reachability. Unity's active `BattleBoundaryWriter.TryApplyKind14DirectionalBlock` and `BattleEcsHitExecutionPlan` projection use battle integer X/Z.

Before modification, the existing `ShadowCompare_Kind14WriterEffectMatchesAuthorityDespiteFalseDispatchReturn` tests only wide integer separation (attacker X20, target X0) and therefore cannot decide precise-versus-int behavior near the strict +5 threshold. The new test sets attacker precise X5.5, target X0, attacker integer mirror5 and target0; target Vx>0; kind14 candidate and dispatch must be observed. Formal precise condition is true; current Unity integer condition predicts no XBoundPositive. Also include X6.5/int6 as a positive control in the same parameterized method. Capture diagnostics and both positions to distinguish geometry/candidate failure from the branch difference. A passing defect-characterization assertion does not count as parity.

Acceptance: original project's existing Unity Editor recompiles, exact two parameterized cases run with candidate/dispatch evidence and expected current flags; record any RED rather than weakening the test. Rollback removes only this method, preserving pre-existing dirty work. Subsequent general carrier/flag implementation needs a separate Task and Change Record.
