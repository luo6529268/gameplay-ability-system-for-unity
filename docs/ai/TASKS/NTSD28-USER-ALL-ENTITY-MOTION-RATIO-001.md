# NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001

Status: `IN_PROGRESS / CORE_XZ_FOCUSED_PASS / SEMANTIC_CLOSURE_PENDING`. Parent: `NTSD28-UNITY-BATTLE-REALIGNMENT-001`. Policy authority: user-confirmed D-024 in `docs/ai/DECISIONS.md`. The initial lexical census and core production-path audit are at `artifacts/diagnostics/NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001/INITIAL-INVENTORY.md`; candidate counts are not a complete or verified writer list. The first bounded implementation `NTSD28-USER-CORE-MOTION-OUTPUT-RATIO-001` removed early run/dash speed scaling and passed focused checks for one-time shared X/Z position integration with raw post-friction velocity on character/non-character examples. Full Driver/Play, Y altitude, spawn/held/teleport/other bypasses, geometry/bounds and aspect variants remain open. No all-entity completion is claimed.

Next classified outlet (read-only diagnosis): the late-OPoint child birth position is a parent-relative X/Y/Z offset, separately written from launch velocity. Its Unity `ConfigureLateOpointPosition` producer still adds raw DAT offsets to an already-moved parent; scaling the child's final absolute coordinate would double-scale the parent. The inventory records the formal/Unity formulas and the required precise/integer-position consistency. Implement only after the current Editor test job is terminal, with a separate exact-path Task/Change and a focused child-birth witness. Immediate effects, stage spawns, drops, weapon pieces and respawns must be classified independently.

Objective: with the existing full-background fixed Unity camera, make battle-entity displacement occupy the same fraction of the visible battle view as the formal NTSD 2.8-Logan release under matched conditions. Scope includes characters, weapons, items, projectiles, summons/effects that move as entities, and other battle objects. This is an explicit user exception to raw per-tick world-pixel equality. No DAT data may be edited without a separate explicit user request; do not change the camera or nonbattle framework as a shortcut.

First package, read-only inventory and contract:

1. Measure formal and Unity view spans under the same stage, aspect, camera phase and render sample. The known Training 16:9 horizontal sample is formal 1333 px and Unity 2048 px, so `Sx=2048/1333`. Establish separate vertical and projected-depth mappings instead of reusing `Sx` without evidence. Record behavior at other supported aspects/maps.
2. Enumerate every production position/velocity/displacement writer and actual final integration consumer by entity kind: player/AI locomotion, frame `dvx/dvy/dvz`, gravity/impulse/knockback, OPoint spawn motion and offset, thrown/held weapons, items, projectiles, platform carriage, follow/teleport/boundary correction, and presentation-only offsets. Mark each as physical travel, spawn placement, teleport, render interpolation, hitbox extent or non-motion datum; do not multiply all numeric fields indiscriminately.
3. Trace where the current partial `NTSD28-USER-FIXED-VIEW-RUN-RATIO-001` already scales run/dash values. Choose a shared one-time transform or disjoint source-specific writers so no speed or displacement is scaled twice. List downstream collision, reach, AI pursuit, stage boundary, attachment and visual synchronization effects before implementation.
4. Split implementation into bounded Change Records with exact code paths and rollback. Verify a representative mechanism per motion category and at least one non-character type in the original Editor/Play, measuring `Unity screen travel / Unity view span` against `formal screen travel / formal view span`. Add more cases only when a new writer, coordinate axis or first difference warrants it. Preserve raw DAT hashes and report remaining categories explicitly.

Completion requires an enumerated full-entity motion inventory, a single coherent coordinate contract per axis and camera mode, implementation and focused tests for every reachable motion category, representative real Battle Scene validation, recorded collision/spawn/boundary side effects, and no unowned displacement writer. Passing the existing character run/dash tests alone does not close this task. Natural Naruto landing and Rasengan follow-up remain separate user-priority repros; Q07 and the overall alignment goal remain open.
