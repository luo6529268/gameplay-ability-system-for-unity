# NTSD28-Q08-ORDINARY-REMATCH-SOURCE-WITNESS-001

Status: `FOCUSED_TEST_PASS / SOURCE_MODEL_ONLY`. Parent BATCH-04/Q08 transition-host audit. Authority: formal Logan root EXE SHA `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, matching playable `GameSession28::step` and `BattleFlow28::step`. Previous mode4 complete-session witness is archived separately.

Scope: create a new `artifacts/diagnostics/NTSD28-Q08-RESULT-TRANSITION-HOST-AUDIT-001/native_ordinary_rematch_fixture.cpp` from the archived diagnostic fixture, add two complete `GameSession28` cases, and copy it to the already isolated `I:/GitHub/Unity_GAS/ntsd-q08-native-validation-20260922/source/ntsd28_playable/tests/game_session_tests.cpp` for focused build. The formal J: source remains read-only. Case A: ordinary mode0 without battle-only loop reaches timer350/transition2 with battle world unchanged and no combat tick, then enters upper selection on the next call. Case B: same battle with `battle_scene_only_loop=true` reaches 350, then explicitly recreates the battle on the following call. Measure scene/flow/last-tick and combatant reset, do not infer that mode4 command202 follows either branch.

Acceptance: fresh source hash parity for cited files, isolated focused build exit0, two executable runs exit0/byte-identical with relevant assertions, report precise branch limits. No Unity production/test code, Scene, DAT/image, GAS or nonbattle edit. Rollback only isolated diagnostic test copy to prior archived fixture under project approval rules; never reset/clean or touch J: authority.

Observed: matching cited formal file hashes; build exit0; two runs exit0 and identical SHA `63A82770A51AB12C08C142C327D4E21DA6B2AA7BC37E61AE12D59DDF39AC8921`. Ordinary mode0 enters selection on next call after transition2; battle-only recreates combat with former victim HP500. The source-witness Task is complete at its declared scope, while Q08 Unity host integration and EXE-visible frontend parity remain open.
