# NTSD28-USER-SOURCE-REVIVAL-AVERAGE-001

Status 2026-09-24: `FOCUSED_TEST_PASS / PLAY_PENDING`; original Editor revival class 11/11 PASS. D-024, Q07 paused.

Authority: formal root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, playable `BattleWorld28::advance_native_revivals` around `battle_world.cpp:8484-8507`, and user D-024. Formal same-group type0 peer integer `sum_x` gates both averages and exactly two synchronized RNG sites `0x90/0x91`; it writes revived precise X/Z but defers integer mirrors. `artifacts/diagnostics/NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001/REVIVAL-AVERAGE-SOURCE-GATE.md` records confirmed divergent-domain cases and dependency on continuous source carrier.

Unity pre-change: `BattleRespawnModule.ApplyRespawnWithoutStoredCount` gates from physical `Runtime.XInt`, averages physical X/Z, applies view-scaled random offsets, and never updates source precise X/Z. Source carrier birth, frame motion and physics are now focused-tested but full Driver/Play proof is not complete.

Declared script scope: `Assets/NTSD/Scripts/Simulation/Passes/Respawn/BattleRespawnModule.cs` and `Assets/NTSD/Scripts/Test/Editor/NTSD28B4RevivalNormalFloorRngProductionEditorTests.cs`. If revived entity and all eligible peers have initialized source history, use source peer integer sum for the single formal gate, draw the two RNG values once, place source precise X/Z from source average plus raw offsets, and place physical precise X/Z from physical average plus those draws scaled once. Preserve both integer mirrors until normal sync. If source history is incomplete, preserve existing physical fallback and do not invent source values. No DAT, Scene, ProjectSettings or nonbattle changes.

Acceptance: source sum zero/physical nonzero and inverse RNG gate witnesses; nonzero divergent averages, negative truncation, excluded peer filtering, source-incomplete fallback, source/physical precise and integer timing, original Editor compile/focused tests. Real participant Driver/Play and formal EXE comparison remain exit gates before declaring parity.

Rollback: review and reverse only declared script hunks, retaining all other work.

Result: `ApplyRespawnWithoutStoredCount` now accumulates physical and source peer integer X/Z in parallel, uses the formal source `sumX` as the single RNG/placement gate when revived entity and all eligible peers have initialized source history, and draws `0x90/0x91` exactly once. It writes source precise average plus raw offsets, physical precise average plus viewport-scaled offsets, and leaves both integer mirrors unchanged. Incomplete source history retains physical fallback without guessing source values. Original Editor job `d9a35ab9ae5d421f8afa8215a72eb128` passed the full focused revival class 11/11, including new source-zero/physical-nonzero and inverse first differences, factor1/configured output, negative averages, excluded peers and old fallback cases. Real participant Driver/Play and formal EXE same-input comparison remain open; do not claim full source carrier or Q07 closure.

Scoped original Battle Scene Play: after extending the existing Play request runner within the declared test file, the original-project Editor consumed `Temp/NTSD28-B4-RevivalNormalFloorRng-Play-v1.request` and wrote a fresh result at local 12:13:41: `state=Passed`, `logicalCases=10`, including both new source/physical zero-gate cases, the configured-view offset case and seven prior controls. The Editor returned to non-Play/idle on `NTSD_Battle`; disk Scene SHA stayed `9E7B8A91ADD396D8A3674915BB5AC9A03B8D1A2EC817BA3B12F135D03EBA0AC0`. This Play probe runs controlled temporary World cases; it does not prove a natural participant's continuous Driver history, physical input or formal EXE equivalence.
