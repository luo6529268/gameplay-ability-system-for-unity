# NTSD28-Q07-REDUCED-HIT-RESOURCE-TRANSACTION-001

Status: `FOCUSED_TEST_PASS / SCOPED_MP_PARITY_POSITION_PENDING`. Parent: NTSD28-UNITY-BATTLE-REALIGNMENT-001 / BATCH-04 Q07. Trigger: `NTSD28-Q07-SASUKE-ARMOR-TARGET-FIRST-DIFF-001` proves formal OID87 type-1 armor target takes four Sasuke OID440 hits at tick16 with HP damages `[3,3,35,35]`; Unity matches HP/action/lifecycle but both character MP values were 52 lower before this package because the first two reduced hits omitted the formally present resource transaction. The later tick20 horizontal-position discrepancy is an independent open item.

Authority: unchanged formal root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; paired `battle_world.cpp` selected-armor/guarded branch at `apply_native_hit_resource_transfer` after reduced HP/MP and credit, before subsequent reaction. Unity's shared `BattleDamageWriter.ApplyAlternateDamage` is the corresponding writer. Reuse the corrected `ApplyNativeStandardHitResourceTransfer` helper from `NTSD28-Q07-STANDARD-HIT-RESOURCE-TRANSACTION-001`; do not add an OID87 or four-needle condition.

Declared script paths before edit: `Assets/NTSD/Scripts/Simulation/Ecs/Writers/BattleDamageWriter.cs` and `Assets/NTSD/Scripts/Test/Editor/NTSD28B5UnarmoredHpConsumptionProductionEditorTests.cs`. First add a focused reduced-hit production test and run RED in the already-open original Editor, then call the existing shared transaction once immediately after `ApplyNativeStandardHitCreditAndConsume` in `ApplyAlternateDamage`. Do not change the unarmored call, armor damage math, formal DAT data, object definitions, other target types, Scene, camera, config assets or nonbattle code.

Acceptance: RED→GREEN focused direct reduced-hit test, original Editor compile zero errors; preserve pre-fix X550/X1200 outputs and rerun exactly those 26-tick scenarios into new post-fix files. Match formal current MP for both actors at tick16 and verify no target HP/action/child-lifecycle or X1200 regression. Compare all 21 mapped fields and classify the already-observed tick20 position difference separately. Run Ledger validator, diff check, protected scene/config SHA and recovered-doc NUL checks. Do not rerun all scenarios absent new failure; natural physical Play and full Q07 remain open.

Rollback only exact declared hunks after provenance review, preserving all old and new traces and unrelated dirty work.

2026-09-26 scoped exit: original Editor compile zero errors, focused RED failed expected root PP426/actual400, then GREEN 2/2 after the one shared reduced-hit helper call. Both post-fix 26-tick raw requests PASS. Against unchanged formal root EXE, X550 is 1428 mapped field comparisons with 12 remaining position differences but zero resource differences; X1200 is 2100/2100 equal. Pre/post Unity rows differ only in attacker and target current MP on X550 ticks16–26; domain/input/RNG rows are unchanged. The independent tick20 victim horizontal position difference remains open. Full Q07 and natural Play remain open; exact results and hashes are in `artifacts/diagnostics/NTSD28-Q07-SASUKE-ARMOR-TARGET-FIRST-DIFF-001/RESOURCE-FIX-ACCEPTANCE-20260926.md`.
