# NTSD28-Q07-KIND-DAT-COMBAT-CONSUMERS-001

Status: FOCUSED_TEST_PASS. Parent: BATCH-04/Q07; R09/R10/R15 triggered. Original Unity project only. Focused evidence and remaining runtime/trace gates are in the Change Record.

Authority: formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; paired playable `source/ntsd28_core/src/simulation/hit_candidates.cpp::classify_kind_table_candidate`, `battle_world.cpp` type-3 continuation, `game_session.cpp` selected-kind startup, and formal selected `resources/runtime/decoded_dat/data/kind.dat` SHA-256 `39E30DF8D86A5FC374B26C80BE358A6503B2C3096D8681C586478D73A0900011`. `NTSD28-Q07-KIND-DAT-IDENTITY-PREPARED-WORLD-001` already publishes the immutable selected table before battle seal.

Before: `BruteForceSceneQuery.IsBlockedReleaseOidInteraction` has a locked OID matrix and omits target type. `BattleDamageWriter` hardcodes bound/respond/frame40. `BattleEcsHitExecutionPlan` projects frame40 in its type-3 transaction and calls the locked candidate helper at multiple sites. The formal default catalog has no OID209, so default-roster Play cannot by itself prove the table gate. Existing dirty files outside these exact paths are protected.

Declared script paths: `Assets/NTSD/Scripts/Animation/LoganKindCatalogInput.cs`, `Assets/NTSD/Scripts/Simulation/Runtime/BattleRuntimeDataCatalog.cs`, `Assets/NTSD/Scripts/Animation/Character/BruteForceSceneQuery.cs`, `Assets/NTSD/Scripts/Simulation/Ecs/Writers/BattleDamageWriter.cs`, `Assets/NTSD/Scripts/Simulation/Ecs/Hit/BattleEcsHitExecutionPlan.cs`, `Assets/NTSD/Scripts/Test/Editor/NTSD28B5Type3KindCatalogTransformEditorTests.cs`, `Assets/NTSD/Scripts/Test/Editor/BattleHitExecutionPlanEditorTests.cs` for selected-frame ShadowCompare. No Scene, Prefab, ProjectSettings, resource, nonbattle code or ordered-shutdown change. No resource deletion.

Behavior contract: one shared read-only resolver takes the prepared World kind catalog, or the exact native locked fallback when no formal catalog is prepared. The candidate gate evaluates before ordinary kind/effect/type/geometry gates, rejects only target type3 plus first effect record matching target OID plus attacker in respond and kind !=9. Type3 transform requires both entity types3, scans records in file order for attacker bound and target respond, applies record frame or zero->40, and retains the existing ownership/definition/slot/latch/reset order. ECS projected target frame, history and pair-reset must use the same selected action as the ordinary writer. No per-tick file I/O or parse, no presentation-to-logic writeback.

Acceptance: focused RED against current literals for target type0 OID209, modified selected record and frame/zero fallback, first-match order and kind9 exception. After implementation, focused original Editor compile/test covers locked fallback and modified catalog on ordinary and ECS paths, with existing B5 type3 tests. Use one representative real Battle Scene smoke if a table-dependent pair can be reached without changing saved Scene; otherwise state the reachability limit. Run Change Ledger and scoped diff. R15 native V3 trace and complete Q07 resource closure remain separate gates.

Rollback: only declared script changes after reviewing current diff and applying the repository's explicit approval rule for deletion/overwrite; preserve all pre-existing dirty work and diagnostic outputs.
