# NTSD28-Q08-COMBAT-LETHAL-PRECOMBAT-TIMING-001

Status: `CODE_WRITTEN / COMPILE_PENDING`. Parent BATCH-04/Q08 native result carrier acceptance. The formal root EXE SHA-256 is `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The playable `GameSession28::step()` calls `BattleFlow28::step()` before the combat driver; the formal complete-session hit witness reports the target dying on tick 12 with result timer 0, then timer 1 on tick 13. The Unity native carrier is already produced at the start of `NTSDBattleTickSystem.RunTick`, but its current focused cases set HP directly before a tick. A collision-caused death inside a full Unity tick remains untested.

Only declared code path: new `Assets/NTSD/Scripts/Test/Editor/NTSD28Q08CombatLethalPrecombatTimingEditorTests.cs`. Construct two minimal type-0 combatants in distinct valid groups, with a current-frame kind-0 itr and a matching bdy at the same position. Set target HP below injury and run the actual `NTSDBattleTickSystem.RunReleaseTick` with no presentation. Assert both groups and native timer 0 before and during the lethal tick, an actual in-tick HP fall to zero or less, then native timer 1 and latched winner group on the next complete tick. Do not call DamageWriter directly, set HP to zero between ticks, or alter production rules to satisfy the fixture. This is a synthetic collision-driven timing witness, not physical input or all-field formal EXE parity.

No Scene, Prefab, DAT/image, UI, GAS, generated/third-party, build setting or production script change. Preserve current dirty work and existing Q06/Q08 tests. Validate only this test with zero Unity compile errors, then the adjacent native group/timer class if this fixture exposes a production difference; reuse prior stable SelfCheck unless production code changes. If the current Editor cannot compile the new file, mark `CODE_WRITTEN / COMPILE_PENDING` and do not claim a test result. Keep original Scene hash unchanged. Rollback is limited to this new test file and its Unity-generated meta after reviewing their diff; do not reset or clean the tree.

Implementation checkpoint: the declared test file now constructs two minimal type-0 LF2Character entities in valid groups, overlaps a kind-0 injury-30 itr with a 20-HP body, and calls full `RunReleaseTick` twice. It asserts the victim is actually killed in tick1 while native timer remains0 with both precombat groups, then expects timer1/latch group1 in tick2. This is a fixture hypothesis pending Unity compilation and run; no outcome is claimed yet.
