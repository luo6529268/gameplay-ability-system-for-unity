# NTSD28-Q07-LEGACY-GRID-TEST-FIXTURE-001

Status: VERIFIED (test fixture only). Parent: BATCH-04 / Q07 old character-image retirement preparation. Authority: D-023 formal character images, Q07 383-image current reference scan, and the existing production split in `CharacterAnimtorManager.PrepareBattleSheetPixels` between legacy BMP color-key/grid processing and formal PNG RGBA preservation.

Observed: `BattleSpriteGridSeparatorEditorTests` has two hardcoded old Naruto/Sasuke BMP paths. The real test contract is that a 800×560 legacy 79×79 sheet with green row/column gutters becomes transparent without changing content pixels, plus a separate inside-frame green protection case. Formal PNG alpha is already covered by `NTSD28B11PngSheetAlphaEditorTests`, including actual formal Naruto production staging/GPU. Keeping old character BMP files solely to run the generic legacy gutter regression would block their eventual exact-file retirement.

Exact script scope: `Assets/NTSD/Scripts/Animation/Rendering/Editor/BattleSpriteGridSeparatorEditorTests.cs` only. Replace the two old-file parameter cases with one deterministic in-memory legacy sheet of the same dimensions/grid/gutter shape, preserving the pre/post assertions. Do not change `RuntimeSpriteProcessor`, `BMPLoader`, formal PNG tests, DAT, images, Scene, Prefab, GameConfig, battle runtime or nonbattle functionality. Do not delete any old resource under this Task.

Acceptance: current Editor compile; focused synthetic gutter and existing inside-frame green tests pass, and actual formal Naruto PNG alpha test remains passing or is cited from fresh existing validation if unchanged. Verify no old Naruto/Sasuke image literal remains in this test, Scene saved hashes stable, ledger validator and `git diff --check`. This removes only test fixture path ownership, not old runtime/empty-root owners or deletion gate.

Risk/rollback: synthetic fixture loses a bit-for-bit check of those two obsolete BMP files, but retains the observed algorithm properties; formal PNG bytes are tested separately. If the synthetic case fails, correct its fixture dimensions/pixel layout within this file; reverting to old resource dependency requires an explicit scoped repository action, not a broad reset.

Acceptance 2026-09-24: original running Unity Editor recompiled and targeted EditMode job `91c3e05b90844d64ae160d2c24bd8003` passed 3/3: synthetic legacy gutter, existing green-inside-frame protection, and actual formal Naruto PNG production stage/catalog/GPU alpha. The test file now has zero old Naruto/Sasuke BMP path literals; this does not certify retirement of the 383 old images. Battle/Menu saved Scene SHA-256 remained `9409F2BCFE3E657A6C3C88A7527045CC384D50AAACC99197D53AACA38F3B3A39` / `3B0F58AA88BEC495AA999D014CB2779E935B21F0374826357B4DC64AE5B80228`. See `artifacts/diagnostics/NTSD28-Q07-LEGACY-GRID-TEST-FIXTURE-001/ACCEPTANCE.md`.
