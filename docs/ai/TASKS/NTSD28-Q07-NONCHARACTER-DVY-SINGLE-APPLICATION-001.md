# NTSD28-Q07-NONCHARACTER-DVY-SINGLE-APPLICATION-001

Status: `FOCUSED_TEST_PASS`; exact evidence and remaining gates are in `artifacts/diagnostics/NTSD28-Q07-NONCHARACTER-DVY-SINGLE-APPLICATION-001/REPORT.md`. Parent: `NTSD28-UNITY-BATTLE-REALIGNMENT-001`, BATCH-04/Q07 and R15 target-present type3 trace. It does not close Q07 or R15 by itself.

Trigger and authority: formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; paired playable `SimulationTickDriver28::step` executes non-character AI, `step_frame_motion`, then nested physics. In the Stage23 OID875/type3/action55 case with a live target slot0 and initial Vy3.8, formal `GameSession28::step` reaches Vy5.2 and integer Y -20 on tick1. Original-project Unity real Driver reaches Vy6.2 and integer Y -19 with the same initial state. Frozen source/Unity rows and limits are in `artifacts/diagnostics/NTSD28-Q07-HITFA7-TARGET-FULLTICK-WITNESS-001/REPORT.md`.

Unity pre-change call chain: `NTSDBattleTickSystem` runs `NativeFrameMotion` before `NativePhysicsAndDeadCharacterResourceNormalize`. The first pass calls `LF2Entity.ApplyNativeFrameMotionForWorldPass` and applies action55 `dvy:1`. The second pass routes type3 through `LF2SpecialAttack.RunNativePhysicsForWorldPass` → `LF2Entity.RunSharedNonCharacterDatFrameAdvance` → `ApplyNonCharacterFrameVelocityForFrameAdvance`, applying the same nonzero `dvy` again because `SimulationWorld.SuppressLegacyFrameMotionForSerialPass` is only set around the later `SerialTickAll`. The observed extra Vy unit matches this duplication. Confirm the focused revised numeric NUnit RED before production edit; do not treat the accidental broad EditMode run as a substitute.

Declared production path: `Assets/NTSD/Scripts/Simulation/Core/SimulationWorld.cs`, limited to suppression of legacy frame velocity during the native physics pass after the global native frame-motion pass. The existing diagnostic test `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07NonCharacterHitFa7EditorTests.cs` and dedicated source witness are validation inputs owned by their existing Change, not new edit targets here. If implementation requires another code file or test change, amend this Task and Change before touching it. No Scene, Prefab, resources, formal J: source, UI, input, nonbattle flow, or unrelated runtime refactor.

Invariants: native frame motion remains the single owner of action-frame velocity in the formal pass order; native physics still integrates position, resolves landing and updates snapshots once; the later serial pass still suppresses legacy velocity. Character behavior, type1/type4/type6 landing, active weapon interactions and object lifecycle must not be changed to satisfy only the type3 example. Preserve existing dirty work and the eleven-stage shutdown contract.

Acceptance: original Editor revised numeric target-present test shows pre-fix RED on tick1 Vy/Y, then after the exact production edit compiles with 0 errors and passes the same test for initial/tick1/tick2 including previous Y. Run narrow adjacent frame-motion and non-character physics tests selected from existing suite, plus the previously passing missing-target hit_Fa7 test. Re-capture the frozen R15 type3 trace if the changed pass affects it. Verify Change Ledger, scoped diff, and Menu/Battle Scene SHA unchanged. Source-model equality is not formal EXE visible or Player proof; leave Q07/R15 open for those and tick3 OPoint pool prerequisite.

Rollback: compare only the declared World method diff against the pre-change state and use a reviewed inverse patch if needed. Do not reset, clean, delete, or overwrite unrelated user work.
