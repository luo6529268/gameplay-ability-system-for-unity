# Remaining special-kind source witness

IN_PROGRESS / SOURCE_FIRST. Q06 remains HOLD. Authority current formal Logan EXE and playable battle_world.cpp resolve_special_relation_hit5269, kind155581 and kind95618; current-source behavior only, not legacy comment addresses. Exact initial code scope Tools/NTSD28AuthorityTrace/damage_remaining_special_kinds_witness.cpp. Reuse existing impact diagnostic writer with isolated two semantic families. Do not edit existing impact runner or production until matched Unity RED.

Source cases: kind9 type3 state3005 vs ordinary; attacker hit_j precedence, target fallback, default30, declared/implicit frames; framecounter/latch/owner/control/group/motion/pending. Kind15 types1/2/4/6 normal vs preserved1000/2000, oid201/202 and Y near/far threshold; assert source horizontal/depth-only response and raw0/native descriptor. Actual geometric candidate and resolve_special_relation_hit, full following tick. Source double-run identity and build manifest. Unity exact paths must be declared before fixture or production changes; branch count representative, no all-role matrix.

No Scene/nonbattle/resources/framework/authority changes, no computer-use. No new runtime owner. Standalone witness World only. Rollback limited to new diagnostic delta under existing authorization; do not delete user files. Not a certificate of formal EXE audiovisual parity. Later acceptance requires matched initial source/Unity trace, focused branches, representative following/replay and runtime as applicable; reuse unaffected evidence.

Pre-change Unity RED scope: new Assets/NTSD/Scripts/Test/Editor/NTSD28Q06DamageSpecialKindsEditorTests.cs only, reuse impact fixture layout/raw50+input/RNG compare with source17. Actual TryApplyCurrentDatTargetHit typed dispatch, same native factory+catalog/OID and initial state. Later actual candidate-runner representatives remain required separately; direct dispatch is not candidate proof. Existing snapshot/Renderer harness may be copied but is not claimed run until executed. No production changes.

Production amendment after actual RED e68867c3: exact BattleDamageWriter.cs kind9 block chooses native current hit_j attacker/target/default30, owner transfer, native binder40/selected; keep existing audio and ResetType3HitMotion/counter semantics. ApplyGenericWeaponWhirlwind removes unsupported201/202 exception and uses native binder preserving counter; helper removes vertical writes. Exact LF2WeaponBase.cs WhirlwindForce same exception/binder correction and ApplyWhirlwindVelocity vertical removal, only called by this kind15 path. No shared global setter/copy-helper changes. No schema/new owner/pass-order/nonbattle/resource changes. Follow same17/representative6 focused source comparison; candidate/replay/Renderer acceptance remains required.

After actual8d814e9c focused2PASS (17+6 initial/immediate/following0diff), extend already declared Unity fixture only with representative actual collision collection/ObjectInteractionTickAll, compare same source after, record candidate count and differences. Run previously declared replay separately. No new production scope.

Pre-change candidate4 root cause: all pair/carrier/coarse/ITR gates true, zero raw Z delta; LF2Entity.GetCollisionZInt(frame) lines4833-34 subtracts positive hit_j-50 when Type3VisualZOffset is zero. Source hit_candidates.cpp constructs depth from entity position.z and has no hit_j read. Exact added production scope Assets/NTSD/Scripts/Animation/LF2Objects/LF2Entity.cs: remove ONLY positive-hit_j fallback in GetCollisionZInt, preserve existing explicit Type3VisualZOffset branch pending separate ownership audit and all presentation methods. This corrects measured candidate4 without rewriting full coordinate family. Explicit nonzero carrier and zero-integer fallback remain unclosed audit items; do not claim whole Z contract aligned. Existing candidate fixture now includes proper PostInteractionTickAll for character attacker, prior object-only errors preserved.

Pre-change explicit-offset audit correction: playable physics_integrator.cpp95 adds hit_j-50 to position.precise_z, source hit_candidates.cpp projects from position.z with no offset reversal. Type3VisualZOffset is a Unity carrier tracking this cumulative advance, not a visual-only offset. Existing LF2Entity.GetCollisionZInt subtracts it when nonzero; incorrect candidate after physics. Add existing fixture two focused geometry controls: (rawZ=250,offset850)->overlap1 and (rawZ=1100,offset850)->overlap0, with attackerZ250/halfwidth100; source geometry result is determined by rawZ, regardless of carrier. RED first, then only LF2Entity.GetCollisionZInt explicit-offset branch removal. Existing ApplyPreFrameZBounds/GetDisplayZ/GetFrameLogicZInt similarly subtract carrier but require separate witness; do not silently claim those closed. Exact paths already declared. Existing historical role-shadow tests with arbitrary offset may change and need inspection after fix.

Pre-change type3 stage bound: playable BattleWorld28::clamp_type0_stage_depth battle_world.cpp8084 clamps precise_z directly into [zNear-1,zFar+1] for every non-type0, writes integer mirror. Unity LF2Entity.ApplyPreFrameZBounds type3 subtracts carrier before clamp, confirmed candidate carrier same scope; exact method correction to clamp Runtime.Z directly. Add existing fixture three controls rawZ80,120,300 with offset30 under bounds100..200, expect99,120,201. Existing BattleRuntimeSelfCheck.cs GT-05 old 129 expectation for raw120/offset30 must be corrected to120 only after witness RED; declare fourth exact path now. No change to display sorting or full physics.

Pre-change raw-Z consumer expansion: native_ai.cpp hit_Fa4 and render_snapshot.cpp use current integer position.z. Exact Unity paths LF2Entity.GetFrameLogicZInt/GetDisplayZForCurrentDataType and LF2WeaponFrameLogicResolver.GetFrameLogicZInt still subtract accumulated type3 carrier, while current authority collision/presentation use rawZ. Add seam controls in already-declared NTSD28Q06DamageSpecialKindsEditorTests.cs, run RED, then change only those helpers. The two stale type3 body-bound assertions in RoleAwareCollisionShadowSelfCheckTests.cs must be rerun and corrected to source rawZ only if they fail. These two new exact paths are declared in paired Change Record before edits. No scene/resource/nonbattle changes.

Next source-only throwing-tail witness: declare Tools/NTSD28AuthorityTrace/damage_state1002_throwing_tail_witness.cpp before writing. Actual ordinary-unarmored geometric hit for type4 attacker state1002 against type3 target, state0 control, and type4/type4 mirror. Record raw50, frame descriptor, synchronized RNG/call-site before/after and following. Build against formal Logan source, repeat to prove identity; then matched Unity RED in existing declared fixture. Production BattleDamageWriter remains unchanged until first difference is measured. No changes to prior 17-case witness or Scene/resources/nonbattle.

RED-first production amendment: source3 synchronized call-site 0xEE, Unity focused de0cadf7 shows only state1002/type3 RNG stream differs (synchronized count0 versus1, legacy RNG consumed) while direct action/velocity/other captured state matches. Exact existing BattleDamageWriter.ApplySpecialWeaponThrowingTail scope: thread through existing SimulationWorld argument and replace its one BattleRandInt(0,16) with NativeRandom.SynchronizedNext(0xEEu,16); no other random sites or action order. Rerun three-case before/after/calls and following tick, then package acceptance. Existing BattleDamageWriter path declared in paired Change Record.

Play acceptance fixture-only amendment after focused and stable SelfCheck: in already-declared NTSD28Q06DamageSpecialKindsEditorTests.cs add two source rows 0/2 state1002 to existing Play probe (Authority actual Renderer, Mobile logic-only), compare before/after/following/RNG and borrower restoration, update expected case count and report scope. Use existing Q05 ordered-close request after probe, preserve scene. No new runtime/Scene paths.

Exit-gate missing-frame control: source3 declared all random actions, so state1002 old raw binder is not yet covered when selected action is absent. Add one existing source witness case state1002/type3, seed42 selects3 but only action0 is declared; double-run then actual Unity RED in existing fixture. Source directly writes action, Unity may fallback. Only on measured difference change this tail to existing native raw binder, rerun immediate/following and final acceptance; no new code paths declared beyond current source/fixture/DamageWriter.

Actual missing-frame RED job97b37a1d: source action3 implicit descriptor wait0, Unity old binder wait1 at immediate and following; all other compared fields match. Change only BattleDamageWriter.ApplySpecialWeaponThrowingTail selected action to existing DirectWriteNativeRawFramePreserveWaitCounter, keep synchronized 0xEE call. Four-case focused then stable acceptance again.

Final Play fixture amendment: include source index3 missing-frame along with indices0/2 in existing RunThrowMatrix, excluding only state0 control. Expect three per profile and 18 total alongside six special-kind cases per profile, with same Renderer/logic split, checksum/borrower/ordered-close criteria. Existing fixture path declared; no new production path.
