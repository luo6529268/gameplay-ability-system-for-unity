# NTSD28-USER-OPOINT-KIND1-RANDOM-BIRTH-RATIO-001

Status: `FOCUSED_TEST_PASS / RUNTIME_PENDING`. Parent: `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` under user D-024. Before this package, Unity `BattleNativeOpointBirthWriter.InitializeBirth` applied formal `kind=1` synchronized random X/Z deltas directly to child integer coordinates after materialization, then copied them to precise coordinates. This retained raw pixels under the 2048/1333 fixed full-background view, reducing the child's screen-distance fraction. Both logic-only and Renderer materializers call this shared writer. Y/action randomization and DAT values are outside the ratio transform.

Authority: shipped Logan playable `BattleWorld28::materialize_spawn_intents` in `source/ntsd28_core/src/simulation/battle_world.cpp` around lines 7710-7740 applies `native_opoint_random_delta` to child integer X,Y,Z,action in exact order and then mirrors precise XYZ. Official OID510 Yagura action418 contains three `kind:1` OID511 OPoints with nonzero `centerx` (600/400/200) and `centerz:80`, proving this path is reachable in current formal content. User D-024 changes the Unity battle displacement screen fraction but forbids DAT edits; a future source-rule reference carrier must separately retain the formal raw integer result.

Pre-script paths: `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06OpointMaterializerEditorTests.cs` and `Assets/NTSD/Scripts/Simulation/Ecs/Writers/BattleNativeOpointBirthWriter.cs` only. Reuse the existing actual late OPoint structural caller/source fixture with factor1 and configured `2048/1333`; change only the fixture's first OPoint random X amplitude to a nonzero value without changing repository DAT. Compare with an otherwise identical zero-amplitude birth so the generated random displacement is isolated from the already-scaled base OPoint offset. The same seed must produce the same raw random draw/call order in both view modes. Before production edit, the configured screen-fraction assertion must fail for the current raw-add behavior while factor1 passes. Then transform only the final battle X/Z random birth offset once; retain native random draws and X/Y/Z/action order, Y and action raw values, integer synchronization, default factor1 bytes, and no extra allocations per child.

Acceptance: narrow original-project Editor RED for configured ratio and GREEN for factor1; after correction both cases GREEN, plus existing focused default-source materializer case for each materializer route. Record actual child precise/int positions, random call counts, source-DAT/default caveats and Scene hash. Do not run the full test suite. Production logic, DAT, Scene, camera and nonbattle files outside the declared writer remain unchanged. Rollback removes the one new test and reverts only the writer's D-024 offset transform; preserve all pre-existing dirty work. OID219/fusion and full source-rule coordinate history remain open.

Pre-fix observed RED: original-project Editor job `126a974eb91243fba35b1eaa740971b0` ran only the new case. Raw random X delta was 147, configured expected 225.84846211552889, current actual 147. Native draw-count and factor1 assertions before the final ratio assertion passed. The shared writer has not yet been edited at this Task checkpoint.

Final focused result: X/Z strengthened job `99d1c0184d6245dfb8232342eb223661` 1/1 PASS; existing default-source World/component materializer rows job `840455c81a6349c89a9efcc35f418ab3` 2/2 PASS. Shared writer transforms only battle X/Z final random birth offsets after the same native draws, leaving Y/action and DAT raw. Change Ledger and diff checks passed; Scene hash unchanged. Real Battle Scene/Yagura418 and formal EXE view capture, plus source-rule coordinate history, remain pending. See `KIND1-OPOINT-RANDOM-BIRTH-RATIO-ACCEPTANCE.md`.
