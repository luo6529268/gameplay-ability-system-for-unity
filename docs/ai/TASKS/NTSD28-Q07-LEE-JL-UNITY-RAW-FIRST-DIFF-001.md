# NTSD28-Q07-LEE-JL-UNITY-RAW-FIRST-DIFF-001

Status: VERIFIED / SCOPED_EDITMODE_JL_ENTITY_RNG_PARITY. Parent: BATCH-04 / Q07, D-024 remain open; follows `NTSD28-Q07-LEE-NATURAL-NONSOUND-CHILD-001`.

Authority: root formal `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable `GameSession28::step()` and input/frame/OPoint path, and formal runtime content. Previous package encoded a 45-tick Lee OID7/J@tick2/L@ticks3–4 LFR from seed 682973786 and observed five OID204/action20 children at tick6 in the formal release trace. The selected four per-tick fields matched source/release 180/180. Unity same-state result is unmeasured.

Declared write scope: narrowly extend `Assets/NTSD/Scripts/Test/Editor/NTSD28UnityRawCaptureEditor.cs` with an exact 45-tick Lee J→L formal scenario contract and an optional unique result path for its request, retaining the existing legacy/frozen scenario contracts and default result path. Add only a new scenario JSON and diagnostic output under `artifacts/diagnostics/NTSD28-Q07-LEE-JL-UNITY-RAW-FIRST-DIFF-001/`, plus this Task/Change/Ledger/progress record. No production battle script, DAT, image, Scene, Prefab, ProjectSettings, formal EXE, native source, or nonbattle functionality changes. The script change is an Editor diagnostic only; no second Unity Editor and no computer-use.

Scenario contract: Lee slot0 OID7/team1/X500/Y0/Z650/HP500/MP500/action0/facing right and Naruto slot1 OID2/team2/X1200/Y0/Z650/HP500/MP500/action0/facing right, both human; seed682973786, battle mode0, Stage23, difficulty0, 45 ticks. Input events are physical J at zero-based scenario tick1 and physical L at zero-based ticks2–3; no other input. Existing `ParseInputKey` maps physical J/L to the project's crossed FrameInput carriers. Formal catalog SHA must match staged current catalog. The raw capture is an EditMode complete-Driver diagnostic, not a scene/pixel or player physical-key acceptance.

Acceptance: original-project Editor compiles the diagnostic, runs only this unique 45-tick scenario in EditMode with formal `Assets/NTSD/Content/LoganRuntime`, writes a unique result and per-tick entity/domain trace without overwriting the existing Temp result, and restores world/singleton state. Compare tick1–45 to the root EXE trace for input phase, Lee action/MP, and OID204 owned child count; report the first difference (including missing child) with exact tick/fields, or the bounded equal result. Preserve all failed attempts. Run `Tools/Validate-ChangeLedger.ps1`, `git diff --check`, and confirm original Editor Scene hashes/status are unchanged. Play/physical input/GPU still require later gates. Rollback affects only the new test-only contract and scenario after provenance review; do not remove pre-existing files.

Run1 interim: original Editor compile/unique EditMode capture PASS; 45-tick mapped input/entity-state comparison 2557/2557. Initial RNG has a fixture offset: raw tool's direct-battle reset chooses random BGM once while this exact native source configuration explicitly selects BGM 2. Before the second EditMode run, only the new Lee schema may use `ResetFromSeed(seed)` in the diagnostic to remove that extra draw; old scenarios and production RNG remain untouched. The root LFR replay's default CRT seed 0 is a separate playback property. See `RUN1-ANALYSIS.md`; run1 outputs are retained.

Final scoped result: independent original-Editor run2 PASS; 45-tick input phase and occupied-entity mapping 2557/2557, synchronized RNG counter/index/call count/site 45/45, zero differences. Tick6's five OID204/action20/owner0 children agree in slots 51–55 and `(437,-35,651)`. Unity CRT state follows the source/Unity seed; root LFR playback uses its default seed0, so no CRT-state parity is claimed against that replay. Existing Temp result and both saved Scene SHAs unchanged. See `ACCEPTANCE.md`. Physical-key Play, pixels, full lifetime, and Q07/D-024 overall exit remain open.
