> VERIFIED / DECLARED_HIT_SPARK_TRANSACTION_SCOPE。34/34、SelfCheck、Play1440/关闭通过；见同ID artifact REPORT。后继NONCHARACTER-ARMOR-FEEDBACK已READY，以下旧执行状态仅历史。

> 执行中：八脚本见同ID Change Record；首次完整driver四组PASS，源端点cover差异定位到新fixture误选legacy converter并已纠正。实际源角色路由282例，非288。尚待最终测试/Play/自检，反馈父缺口未关闭。

# Unity命中火花事务接入

READY_EXACT_CONTEXT_AND_TEST_FIRST_RECORD。前置HIT-SPARK-SOURCE-WITNESS-001已源438/2994验证；先完整读同ID artifact REPORT，不重做原矩阵。

准确生产入口：type0主LF2Character.Hit→DatHitResolver普通分支→DamageWriter→RecordKind0Hit即return；底部旧SpawnSpark不是当前普通hit入口。weapon/special也调用RecordKind0Hit。禁止仅改旧SpawnSpark两行。

实施前确定code-path/符号与Change Record，至少闭合公共无资源emitter、真实route caller、原index与selectedArmor/branch上下文、测试。设计按以下合同：

- helper输入包括World、attacker/target、实际runtime itr、原candidate index、hit前selected armor、armorSparkGate及unarmoredBranch；不能在hit后重新匹配armor。
- SequenceRunner持有原index，RuntimeItr可能是拷贝，不能只用ReferenceEquals找索引；旧CurrentItrIndex通用性不足。优先显式参数或严格try/finally的瞬时scope，需证明嵌套调用、异常和pool复用后无残留，不改persistent schema。direct兼容入口另明示默认索引规则。
- 按源guard/owner/capacity→编码→snapshot存在→几何→Y/X CRT→AddHitRecord顺序；目标Z、cover、整数/2、fall0与armor gate严格照源。每条成功hit只发一次，失败/容量拒绝不消耗CRT。已有hit record数组、C01/C25/有序关闭保持。
- route owner接入时处理外层RecordKind0Hit重复调用；无护甲/feedback/reduced的flags分别是(false,true)/(false,false)/(true,false)。没有selectedArmor的reduced仍是后一个合同。

先新438 Unity RED，比较完整两host数组和CRT值/state/count，两factory、实际candidate index>0与runtime拷贝、Shadow及原full-driver；源438不含kind非0动态覆盖，另补该guard及实际调用一致性测试。编译/SelfCheck/真实Play/回放与池复用后才能标verified，资源仍为旧/合成内容，不能称Logan图片已部署。

之后NONCHARACTER-ARMOR-FEEDBACK→UNARMORED-WEAPON-REACTION/TYPE5 plan→回BDEFEND256/完整driver；所有失败保留。禁止computer-use、global随机算法/C17/框架/非战斗/Scene/资源/Server更改。
