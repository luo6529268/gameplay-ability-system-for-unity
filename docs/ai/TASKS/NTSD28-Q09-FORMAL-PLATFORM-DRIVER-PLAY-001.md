# NTSD28-Q09-FORMAL-PLATFORM-DRIVER-PLAY-001

Status: `VERIFIED_SCOPED_FULL_DRIVER_CENTRAL_COMMAND`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001`, BATCH-05/Q09. Entry evidence: `artifacts/diagnostics/NTSD28-Q09-FORMAL-PLATFORM-REACHABILITY-AUDIT-001/REPORT.md` and existing `NTSD28-Q09-PLATFORM-SHADOW-OFFSET-PUBLICATION-001` display change. Scoped exit evidence: `artifacts/diagnostics/NTSD28-Q09-FORMAL-PLATFORM-DRIVER-PLAY-001/ACCEPTANCE-20260926.md` and `platform-hidan-01.json`; wider Q09 remains open.

Authority: root formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable `battle_world.cpp` platform direction operation 30 and `render_snapshot.cpp` shadow Z+10C; indexed formal `c/hid/rea.dat` OID56 frames 130–134/180–191. Formal staged DAT bytes match the release. Unity runtime `BruteForceSceneQuery.CollectPlatformDirection` writes the existing carrier; Legacy/Central display consumers were separately changed under the prior Q09 ID.

Exact code scope: add only `Assets/NTSD/Scripts/Test/Editor/NTSD28Q09FormalPlatformDriverPlayProbeEditor.cs` and its `.meta`. It is an opt-in, request-file Editor-only probe for the saved original `Assets/NTSD/Scene/NTSD_Battle.unity`. No production script, DAT token, PNG, prefab, Scene, ProjectSettings, input map, mode, nonbattle module, manager, queue or shutdown owner changes.

Run a controlled formal OID56 current/snapshot frame with a different-group type-0 target through one full production `SimulationTickDriver.StepOneTick` in the original Editor. Capture before/after frame and native source-reference carrier, collision reference, platform source slot, `RenderShadowOffset10C`, central Shadow command position/Z and cleanup counts. Require the formal content root, published catalog, clean saved Battle Scene, idle worker and a stable paused World. Do not directly call the platform collector as the claimed full-tick result. A failed candidate is recorded as a diagnostic failure; do not weaken its assertions or call it natural gameplay.

Acceptance: original Editor compile0; one uniquely named opt-in request yields a persistent JSON record with source/target slots, frame/snapshot and full-tick counter; nonzero 10C and matching central Shadow command if reached; registry/slot/pool baselines restored; Menu/Battle Scene disk SHA unchanged; Console inspected; `Tools/Validate-ChangeLedger.ps1` and `git diff --check` pass. This closes only a controlled formal-content Play layer. Natural input reachability, Legacy Play renderer, camera pixel and root EXE same-condition graphics remain pending until separately proved.

Risk: a forced action/position may be reset by native frame or CPoint logic before candidate pass; this is useful first-difference evidence, not permission to change DAT or production rules. A Play exception might leave a temporary fixture; `finally` must unregister both entities and the explicit Battle Runtime shutdown remains the fallback on Scene exit. Rollback is exact reviewed removal/reversal of the new probe and governance addenda, preserving all pre-existing dirty work and user-owned files; destructive deletion requires its own authorization under `AGENTS.md`.
