# NTSD28-Q08-STORY-SELECTOR-RED-001

Status: `FOCUSED_TEST_PASS / STAGE_PROXY_RED_RETRACTED / FORMAL_STORY_SELECTOR_NOT_YET_WIRED`. Parent: `NTSD28-Q08-RESULT-FLOW-SOURCE-MATCHED-001`, BATCH-04/Q08. The first XML 4PASS/1FAIL used an invalid stage-campaign-as-story-selector oracle; the corrected focused class compiled and passed 5/5. Caller evidence and correction: `artifacts/diagnostics/NTSD28-Q08-RESULT-GROUP-CARRIER-AUDIT-001/STORY-SELECTOR-CALLER-CORRECTION.md`. The formal story selection remains a separate parent dependency, not a current production gate inferred from mode1 or `StageProgressionValid`.

Authority: matching playable `GameSession28::step()` skips ordinary `BattleFlow28::step` only when both story mission and child-stage IDs are selected; `battle_mode=1` by itself does not suppress it. See `artifacts/diagnostics/NTSD28-Q08-RESULT-GROUP-CARRIER-AUDIT-001/STORY-SELECTOR-GATE-AUDIT.md`.

Owned project script: append two exact focused methods to `Assets/NTSD/Scripts/Test/Editor/NTSD28Q08BattleFlowRedProbeEditorTests.cs`; no production, Scene, resource or other script edit. Use `BattleStageCampaignLoader.ParseText` and `SimulationWorld.ConfigureStageCampaigns` to exercise a valid selected campaign; compare to the same mode-1 direct battle without one. Retain the existing three revive-lives tests verbatim. Copy the exact test file to the independent Unity validation clone and run only these methods. Record whether the configured campaign is sufficient to express the formal paired story selector; a RED documents a first difference, not authorization to add an unproven gate.

Acceptance for this diagnostic package: compile and targeted XML with one direct control and one configured-story case; report actual `StageProgressionValid`, group state and result guard/phase, protected original Scene unchanged, `git diff --check` and `Tools/Validate-ChangeLedger.ps1`. Production classification and snapshot/checksum contract remain in the parent Q08 Task. Roll back only this package's appended test methods under repository approval rules, never reset/clean the shared worktree.
