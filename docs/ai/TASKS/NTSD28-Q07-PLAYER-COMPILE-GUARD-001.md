# NTSD28-Q07-PLAYER-COMPILE-GUARD-001

Status: `VERIFIED` (2026-09-22; q07-package-build-4 errors0 and targeted EditMode 11/11). Parent BATCH-04/Q07 Player gate. The first actual R8 Windows Mono Development Player build failed before Q07 packaging at 28 C# errors. Errors are in battle simulation/preview code unrelated to the new build callback and are caused by Player condition compilation: two test-injection method definitions disappear while their production call sites remain, six self-check-only World wrappers call test-only module methods, and battle Editor preview directly refers to `EditorApplication` in Player code.

Exact code path: `Assets/NTSD/Scripts/Simulation/Ai/Runtime/SimulationAiDecisionModule.cs`, `Assets/NTSD/Scripts/Simulation/Core/SimulationWorld.cs`, `Assets/NTSD/Scripts/Animation/Rendering/BattleCentralEditorPreview.cs`. For non-test Player builds, provide no-op conditional test-injection stubs that the compiler elides at call sites; retain existing test injection behavior under `UNITY_INCLUDE_TESTS`. Compile the six World self-check wrappers only with tests. Use the existing Editor clock in Editor and a Unity runtime unscaled clock in Player for the preview foot marker. Do not change AI decisions, frame order, game content, GameConfig, Scene, other preview behavior or nonbattle modules.

Acceptance: current Unity Editor compile remains clean; actual Windows Mono Development Player compile advances past all 28 declared errors; focused existing AI snapshot/nearest self-check and preview behavior tests remain passing where available. Then Q07 packaging callback/output validation resumes in the same build attempt or a new attempt only after the prior build is confirmed terminal. Record any new errors exactly and do not claim Player gate until built output and runtime acceptance. Preserve any build-generated ProjectSettings/Odin changes for separate review; no delete/restore without explicit approval.

Risk/rollback: test-only conditional code can inadvertently remove hooks from Editor tests or leave runtime call overhead. Inspect compiled behavior with focused tests and fresh Player compile. Revert only the declared lines under repository rules; unrelated user work stays untouched.
