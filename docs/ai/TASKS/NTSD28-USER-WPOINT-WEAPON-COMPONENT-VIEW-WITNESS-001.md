# NTSD28-USER-WPOINT-WEAPON-COMPONENT-VIEW-WITNESS-001

Status: `FOCUSED_TEST_PASS / TEST_ONLY`. Parent: `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (D-024). Original-project Editor exact job `13805c76522f442cac6bc7917647d39b` completed 2/2 PASS, 0 failed/skipped, for the two declared views. Factor1 holder/held XInt348/359 and configured view373/384 both retain the raw local gap11; held ZInt251. This closes the focused type1 weapon-component pose witness, not full Driver/Play, actual sprite contact, formal EXE capture or the dual-coordinate carrier.

Authority: shipped Logan playable `BattleWorld28::settle_held_refill_objects` (`source/ntsd28_core/src/simulation/battle_world.cpp`) derives held X/Z from holder integer position and raw frame-local WPoints; the user-approved fixed full-background view scales physical travel, not local sprite geometry. Unity's active type1 route is `BattleHeldObjectWriter.RunStep12` -> `LF2WeaponBase.Act` -> `LF2WeaponHeldStateResolver.ApplyHeldWPointSync` -> `LF2WeaponBase.CoincideXYWithWPoint`.

Declared script path: only `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06HeldNativeFrameBindingEditorTests.cs`, adding one parameterized test. Reuse unchanged formal source fixture `artifacts/diagnostics/NTSD28-Q06-HELD-NATIVE-FRAME-BINDING-001/source/first.jsonl` row index 21 (type1, nonterminal action0). Source static holder X300 yields held X311/Z251. Construct the registered World with its existing helper, assert the child is `LF2WeaponBase`, configure `1333×730` or `2048×1152`, move holder by raw Vx48 through production `CharacterMechanics.StepBattleLogic`, synchronize holder integer position, then run `HeldObjectProcessAll(1)`. Expect holder/held integer X348/359 or 373/384, local gap11 and child Z251. If component route gives a different result, retain the failing witness and diagnose before changing production.

No production, DAT, character image, Scene, camera, ProjectSettings or nonbattle edit. Acceptance: existing original-project Editor compiles and both exact focused test cases pass, with source row and branch identity checked. This witnesses one active WPoint component pose transaction; it does not prove full Driver, sprite contact, every WPoint kind, formal EXE visible output or dual-coordinate carrier. Rollback removes the single test method.
