# NTSD28-Q07-NONCHARACTER-HITFA7-PRODUCTION-001

Status: FOCUSED_TEST_PASS / target-present full-tick acceptance pending. Parent: BATCH-04/Q07 and R15 first-difference follow-up. Focused RED 2/2 was observed in original Editor job `cade51ba91a0445abcfe3a2cc5e443b8` before the declared production edit; final exact focused job `870e12d4f9784c70baafd151636a9565` passed 2/2. Full evidence and remaining gate: `artifacts/diagnostics/NTSD28-Q07-NONCHARACTER-HITFA7-PRODUCTION-001/REPORT.md`.

Trigger: same-seed formal source-model and Unity V3/B0v2/B2 trace for type3 OID213/action176 and OID206/action0 shows Unity-only slot50/OID206/action40 birth at tick1. See `artifacts/diagnostics/NTSD28-R15-TYPE3-ACTION-UNITY-SCENARIO-001/REPORT.md`. This is an observed trace difference, not a formal EXE screen comparison.

Authority: formal EXE SHA-256 B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033 and paired playable source. `simulation_tick_driver.cpp` routes non-character positive hit_Fa to `NativeAi28::step_non_character_hit_fa` before later passes; `native_ai.cpp` behavior7 uses a producer-preassigned +0x3F8 target, returns without mutation when the target is missing, and has no unconditional clone birth. With a live preassigned target, it applies two 0.7 X steps, 5-pixel Z dead zone and 0.4 Z step, one Y acceleration capped by condition `<4`, one precise-Y advance, conditional action60/zero motion, X/Z clamps 14/2.2, then facing. The formal core corpus `native_ai_corpus_tests.cpp` uses real OID875/action55 with target slot0 and expects X1.4, Y4.2, Z0.4, precise Y -25.8 in its isolated example. Verify this test's participation in the paired source build/test path before using it as acceptance evidence.

Unity pre-change: `LF2Entity.RunCurrentDatFrameLogicBeforeAdvance` calls `RunHitFa7FrameLogic` for all DAT types. That method calls `SpawnHitFa7Clone` before validating the target and handles the missing/inactive target, integer Y synchronization and clamps differently. Its valid-target branch advances precise Y once; the earlier "twice" description was incorrect and is superseded here. The current kind scenario uses real `LF2SpecialAttack` shells, and the extra slot50 birth is measured. Type0/character hit_Fa behavior and existing other hit_Fa families are separate; do not rewrite them by inference.

Declared production path: `Assets/NTSD/Scripts/Animation/LF2Objects/LF2Entity.cs`. Declared focused test path: a new `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07NonCharacterHitFa7EditorTests.cs` plus its Unity-generated `.meta`, or an exact existing focused test file after reviewing ownership and recording the amendment before editing. Task/Change/Ledger/STATE/handoff/alignment and Temp/artifact output are evidence paths. No formal J: code/content, Scene, Prefab, ProjectSettings, old assets, general input, UI or nonbattle edits.

Acceptance: first write focused RED for missing-target no clone/no new slot and real-DAT preassigned-target motion/order (including action60 and speed/clamp boundary as needed), using current formal source values. Then route only non-character behavior7 through the authority path, retaining type0 behavior until independently proven. Original Editor compile and affected tests must pass. Re-capture the exact existing three-tick kind scenario with V3/B0v2/B2 and compare against the already frozen current-source model; do not massage the scenario or diagnostic exporter. Run the relevant existing hit_Fa and lifecycle regressions, Change Ledger and Scene hash checks. If a new first difference appears, report it and keep Q07/R15 open. Full EXE visible behavior and presentation are later gates.

Rollback: review the declared production/test diff only and preserve all other dirty work. Any destructive rollback requires repository approval.
