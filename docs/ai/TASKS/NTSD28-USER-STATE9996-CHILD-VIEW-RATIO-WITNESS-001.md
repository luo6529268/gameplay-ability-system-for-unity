# NTSD28-USER-STATE9996-CHILD-VIEW-RATIO-WITNESS-001

Status: `FOCUSED_TEST_PASS / RELATIVE_BIRTH_DEFECT_CONFIRMED / TEST_ONLY`. Parent: `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (D-024). Original Editor focused job `f2fbd17c1a1548b480ce8166db78f62f` ran only the two declared cases, 2/2 PASS as a defect witness: factor1 parent/child XInt320/317 gives -3/1333, configured parent/child330/327 gives -3/2048. Production remains unchanged. Evidence: `artifacts/diagnostics/NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001/STATE9996-CHILD-VIEW-FIRST-DIFFERENCE.md`.

Authority: shipped Logan playable `BattleWorld28::advance_native_special_clones` in `source/ntsd28_core/src/simulation/battle_world.cpp` near 2928-3010 uses the parent's integer position plus synchronized `random_x` in [-3,3] and Z+1 to birth OID217/218. Unity `BattleLateEntityLifecycleModule.SpawnState9996Children` uses parent Runtime.XInt plus the same raw offset before direct task materialization. Existing Q06 source row 0 has five valid children and exact synchronized random evidence; source child slot50 has relative X=-3.

Before the test, D-024 scales parent physical X movement at the common outlet but state9996 child X random offset still enters the absolute birth task as an unscaled integer. The expected configured-view child-to-parent screen fraction for relative -3 is `-3/1333`, while current prediction is `-3/2048`. This is a prediction, not yet an original Editor measurement.

Declared test-only scope: one focused parameterized method in `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06State9996DirectSpawnEditorTests.cs`. Reuse the formal row0 World and production `CharacterMechanics` for a raw20px parent step; sync its integer X mirror and call the already-exercised native clone boundary. Assert child0 OID217, exact synchronized offset and observed screen fraction at factor1 and configured 2048/1333. Preserve all formal DAT bytes, random order, child velocities, Scene, camera, production and nonbattle code.

Acceptance: original Editor compiles and runs only the two focused parameter cases; report parent/child integer X, formal raw offset, actual screen fraction and shutdown borrowers. A passing characterization asserts the current defect and must not be described as parity. The correction, if confirmed, requires a separate production Task/Change and must fit the general dual-coordinate birth contract. Rollback is removal of this one test method, preserving all other work.
