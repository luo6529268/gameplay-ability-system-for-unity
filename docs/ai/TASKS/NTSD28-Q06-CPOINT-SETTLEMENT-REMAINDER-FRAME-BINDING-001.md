# CPoint remaining settlement frame binding

READY_SOURCE_AUDIT. Read EXIT-RECONCILIATION-001/EXIT-GATE.md. Exact candidate ownership BattleCpointWriter.ApplySettlementVictimAction and RunKind1 missing/invalid/negative-decrease timeout only. Preserve closed selection/throw/held accounting/pass order. Source advance/settle catch relations uses definition frame; signed facing, raw action/counter/latch/snapshot and post-vaction kind2 preflight must be witnessed together. Cases declared/implicit/high857-998/999/invalid, raw0/181 defaults, reciprocal missing and timeout. Verify source caller reachability and formal content separately; do not infer all domains from one character. Before any scripts create exact Change Record after source entry/path decision. No global setter replacement, no Scene/resource/nonbattle/framework edits, no computer-use. Reuse existing capture infrastructure and only affected tests. Overall Q06 HOLD until live-reader gaps adjudicated.

Pre-change source-only scope: Tools/NTSD28AuthorityTrace/cpoint_settlement_remainder_witness.cpp; exact Record created before implementation. Native helpers5822/6073 verified; broader frame helper/global setter untouched.

Pre-change Unity RED: new exact Assets/NTSD/Scripts/Test/Editor/NTSD28Q06CpointSettlementRemainderEditorTests.cs only. Reuse source24 DAT and before raw values, actual factory logic entities; source SHA pinned. Execute per-slot RunWeaponSyncHeldStep10 for settlement or RunCpointAdvanceStep10 for advance (including victim orphan212), compare raw50 and descriptor availability/wait/next/relations. Two profiles, no all-role matrix. Initial equality mandatory; finally existing World shutdown. No production until measured RED. Restore only before, never after expectations. No new runtime lifecycle owner. Existing rollback/Scene/resource boundaries apply.

Actual Unity RED7e3050c1 two profiles: initial raw/descriptor/relation0differences; post settlement implicitwait1 vs0 and declared857+ descriptor missing blocks actual position sync. Exact production amendment Assets/NTSD/Scripts/Simulation/Ecs/Writers/BattleCpointWriter.cs: RunKind1 missing/invalid reciprocal+negative-timeout four calls and ApplySettlementVictimAction single call only switch to existing DirectWriteNativeRawFramePreserveWaitCounter. Do not change shared legacy helper, selector/throw, signed action handling, counters, injury, relation or pass order. Existing helper binds native action preserving wait/action latch and snapshot. Retain source24 assertions and RED files; focused re-run then representative runtime/following validation before VERIFIED. No new ownership/schema/resources.

Pre-change following/replay acceptance: same declared source CPP adds optional --following mode for indices5/9/22, after original partial API executes one real SimulationTickDriver step with same definitions/catalog and stage800/180/350 and playable default resource/drop controls. Default original24 output must remain byte-identical. Existing Unity remainder fixture adds same three following/source assertions and initial snapshot restore replay with checksum, config/human input binding if needed; no production. Later Renderer exact path must be declared separately. Compare before/after/next raw50+descriptor+relations, do not normalize any differences.

Pre-change Renderer acceptance: add exact Assets/NTSD/Scripts/Test/Editor/NTSD28Q06CpointSettlementRemainderPlayProbe.cs, and extend existing remainder fixture with renderer flag using actual LF2ObjectPointFactory and explicit FreeEntityLikeExe before World shutdown. Same three source following cases5/9/22; compare raw50/descriptors/relations, protect scene checksum and borrower count, then existing Q05 restore/ordered close. Renderer run performs one pass without snapshot replay (already independently passed); no new runtime lifecycle owner or production changes.
