# NTSD28-Q07-STAGED-FULL-PUBLICATION-001

Status: `FOCUSED_TEST_PASS` (2026-09-22). Parent BATCH-04/Q07. Prerequisite staged candidate identity test `NTSD28-Q07-STAGED-CANDIDATE-IDENTITY-001` is focused PASS.

Authority: D-023 formal NTSD 2.8-Logan runtime content, Q02 atomic publication contract, current Q07 project-local staged root. Need actual Unity PNG decode, config/sprite/UI preparation and coherent publication for all 330 object entries before changing the production GameConfig.

Declared code path: only new `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07StagedPublicationEditorTests.cs` and generated matching `.meta`. Use the existing `NTSD28B11AtomicPublicationEditorTests` fixture setup/teardown and its production-owner loading seam by reflection; do not modify that existing fixture, production classes, GameConfig asset, Scene, old resources, third-party code or nonbattle UI. The test is isolated from active battle and uses `Application.dataPath` to select the staged root.

Test acceptance: full staged `LoganVisualContentCandidate.Capture` returns 330; `LoadLoganContentForOwnersAsync` commits; manager/GameData/UI keys and content identity agree; all indexed object configs and data entries are present, required head/small UI sprites resolve; at least one body sheet and owned texture/sprite resource is decoded. The measured EditMode fixture `DestroyImmediate` did not execute the manager's `OnDestroy` (owner destroyed=true but `spritePrewarmDisposed=false` and 29,400/29,400 tracked resources survived); explicitly invoke the existing production `OnDestroy` through reflection **before** fixture teardown and require zero tracked survivors. This verifies the cleanup method, not Unity's Play lifecycle callback; real Play exit/re-entry remains separately required. If an actual formal input is rejected, retain exact first exception and fix only a newly declared contract; no silent fallback to legacy content or smaller subset. Run the exact EditMode test, check compile, run Scene-hash and ChangeLedger checks. Do not rerun Q06 or all 8,000 tests.

Risk/rollback: a full 906-effective-image publication may expose load time, memory, or disposal defects; keep GameConfig root empty so failure cannot alter normal battle. Changes are test-only and can be superseded or removed only under the repository deletion rule. This package does not prove App/menu Play callers, player-build packaging or natural skill presentation.

Result: three exact RED runs isolated the EditMode fixture's missing `OnDestroy` callback (owner destroyed, disposed flag false, all 29,400 tracked generated resources survived); they did not show a formal-content decode or owner-publication failure. With the production `OnDestroy` method explicitly invoked in the isolated fixture, exact job `1711ab456066461da4b517412f60e4fb` passed 1/1 for full publication, 330 config/data entries, all declared UI images, coherent manager/data/UI keys and zero tracked survivors. Scene checksum and GameConfig remain unchanged. Real Play delivery of the lifecycle callback and App/menu callers are separate Q07 gates.
