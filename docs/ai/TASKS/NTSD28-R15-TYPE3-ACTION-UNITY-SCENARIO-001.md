# NTSD28-R15-TYPE3-ACTION-UNITY-SCENARIO-001

Status: FOCUSED_TEST_PASS / FIRST_DIFFERENCE_FOUND. Parent: BATCH-04/Q07 and R15. Diagnostic Unity capture only. Actual evidence and the gameplay follow-up are in the matching Change Record and REPORT.

Authority: formal NTSD2.8-Logan.exe SHA-256 B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033; paired playable GameSession28 scenario roster and BattleWorld28::spawn_at, with formal resources/runtime DAT and kind.dat. The source-model candidate `artifacts/diagnostics/NTSD28-R15-KIND-DEPENDENT-SOURCE-PREFLIGHT-001/kind213-action176-to-206-action0-candidate.json` exercises physical slots 0/1, actual type3 definitions, authored actions 176/0, three ticks and same-slot OID206→213.

Pre-change difference: Unity raw capture `ConfigureWorldAndRoster` always creates `LF2Character`, its scenario combatant DTO ignores `action`, and its per-tick exact-character diagnostic expects two character-pass executions. A type3/action scene therefore cannot be compared as the same initial state. The formal source capture and B0 v2 contract/producer are already separate completed diagnostic packages; do not change them here.

Declared code paths: `Assets/NTSD/Scripts/Test/Editor/NTSD28UnityRawCaptureEditor.cs` and `Assets/NTSD/Scripts/Test/Editor/NTSD28UnityRawCaptureEditorTests.cs`. Documentation, Task/Change/Ledger/STATE/handoff/alignment and new Temp/artifact outputs are evidence paths. No battle production, formal J: source/resources, Scene, Prefab, importers, ProjectSettings, old assets, or nonbattle changes.

Behavior: accept optional authored initial action (omitted means zero). Resolve each selected catalog definition type; preserve the existing type0 `LF2Character` route. For type3 only, create a real `LF2SpecialAttack` shell, load its formal wrapper, verify its authored action, initialize the formal spawn-at frame quartet, position, HP/MP/base values, team/owner/facing and definition metadata, register at its required physical slot and verify the registration/identity. Reject unsupported types or missing actions before writing trace output. Do not use OPoint task initialization, fake kind, artificial lifecycle event, forced epoch, or a character-derived shell. Preserve existing neutral/V1/V2 behavior and character-pass assertion for type0; type3 is not expected in that exact character pass.

Acceptance: focused test-first checks for explicit action, missing action rejection, real type3 runtime identity, required slots and unchanged neutral type0 capture. Original project Editor compiles and runs only affected focused tests; capture formal three-tick kind scenario twice with V3 content header and explicit B0 v2; validate all streams and compare main/B2/B0 with same-seed formal source-model outputs, reporting first differences rather than making production changes to force equality. Verify scene hashes, `git diff --check`, Change Ledger. Source-model trace is diagnostic evidence, not formal EXE observable parity. Q07/R15 and the total goal remain open pending actual gameplay/EXE/presentation acceptance.

Rollback: review only the two declared diagnostic files and this package's documentation/output; preserve other dirty files and historical captures. Any destructive rollback requires separate authorization under repository rules.
