# NTSD28-Q08-RESULT-FLOW-SOURCE-MATCHED-001

Status: `IN_PROGRESS / NATIVE_GROUP_TIMING_LETHAL_MODE4_PASS / UNITY_GROUP_RED_4 / TIMING_DIRECT_RED_2_FULLTICK_RED_1 / REVIVE2_BRANCH_FOCUSED_TEST_PASS`. Parent: BATCH-04 / Q08, G-05/G-06 and I-08. The independently recorded child `NTSD28-Q08-REVIVE-LIVES-LIVING-GROUP-001` corrected the HP0/revive-lives-2 predicate and passed isolated RED→PASS 3/3 plus adjacent seams 5/5. A further audit reproduced four focused Unity group-rule RED cases: invalid groups 0/5/40 with valid 1 fail to start the timer, and groups 1/2/3 collapse to two buckets. Matching playable source was then compiled in isolation and `GameSession28::step()` passed six group/revive cases, a latched-timer/milestone/144-continue case, an actual combat-caused HP20 lethal hit on tick12 timer0/tick13 timer1, and mode-4 timer1/transition202; two runs were identical. Isolated Unity direct-writer tests intentionally fail at timer latching and premature result-page activation (class 3 pass/6 fail); an added complete `RunReleaseTick` case also fails at timer latching after another group returns (class 3 pass/7 fail). `artifacts/diagnostics/NTSD28-Q08-RESULT-GROUP-CARRIER-AUDIT-001/REPORT.md`, `TIMING-FIRST-DIFFERENCE.md`, `MODE4-RESERVE-CALLER-AUDIT.md` and `CARRIER-CONTRACT.md` map evidence and prechange state/snapshot/checksum/UI boundaries. The playable mode-4 input has no matching Unity committed-reserve payload, so its negative source search cannot authorize deleting the current reserve owner. A simple predicate or array resize is insufficient. The corresponding Unity natural lethal-hit tick, cross-engine identical-world trace, original Editor/real battle and broader result cases remain pending. Each further production edit needs its own exact Change Record and owned paths before editing.

## Authority and measured starting point

Formal root `NTSD2.8-Logan.exe` SHA-256: `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`. The matching playable build includes `ntsd28_core/src/simulation/battle_flow.cpp`; `GameSession28::step()` calls its `BattleFlow28::step` before entity combat update. The focused core `battle_flow_tests.exe` was freshly built and run `PASS`/exit 0 on 2026-09-22. Static Unity caller, carrier and test-owner evidence is in `artifacts/diagnostics/NTSD28-Q08-RESULT-FLOW-CALLER-AUDIT-001/REPORT.md`. A native source test is not a formal-EXE-versus-Unity parity certificate.

The Unity battle tick currently calls `BattleResultsOutcomeHostWriter.UpdateSummaryActivation` after the post-frame tail through `NTSDBattleTickSystem.BattleResultsFlow`. The HP-positive-only eligibility was corrected by the child Q08 revive-lives Change; two-team/`HadBoth`/phase-11 logic and the call point remain. `LF2Entity.HP2Orig` is the existing native revive-lives carrier. An isolated Unity `RunReleaseTick` tick-2 RED and post-change PASS now exist for HP0/`HP2Orig=2`; native full-driver group-state cases now exist, while a full cross-engine identical-world trace and broader result-flow first-difference matrix do not. Q07 formal content is already the production root but Q07 aggregate exit and the Sasuke editor-preview acceptance remain pending.

Story-selector prechange gate: `artifacts/diagnostics/NTSD28-Q08-RESULT-GROUP-CARRIER-AUDIT-001/STORY-SELECTOR-GATE-AUDIT.md` confirms that playable skips ordinary BattleFlow only when the paired story mission/child is selected. Unity mode 1 is also the direct-battle default; `StageProgressionValid` is only a campaign-selection candidate, not yet a proven equivalent of that paired host selector. Obtain direct/story bootstrap full-tick evidence before wiring the precombat producer; do not gate all mode-1 battles or classify a selected story child through ordinary VS results.

Follow-up focused Unity evidence: `STORY-SELECTOR-UNITY-RED.md` records a complete-tick direct-mode-1 control PASS and configured-stage mode-1 target RED (`BattleEndPhase` expected 0, actual 1), class 4PASS/1FAIL in isolated EditMode. This proves the present configured-stage producer difference but does not prove `StageProgressionValid` equals the formal paired story selector. An exact session selector remains a production prechange dependency.

Correction: the preceding configured-stage RED is not an authority-backed defect. The actual menu builds `MatchConfig` without campaign fields, and the config has no formal paired story IDs; `StageProgressionValid` cannot be substituted for story selection. Corrected isolated EditMode 5/5 PASS and the retained original RED are reconciled in `artifacts/diagnostics/NTSD28-Q08-RESULT-GROUP-CARRIER-AUDIT-001/STORY-SELECTOR-CALLER-CORRECTION.md`. Keep ordinary direct-mode-1 BattleFlow active; formal story selector remains a separate missing host input. The group/timer first differences remain valid for direct battle.

## Exact investigation and likely code ownership

- `Assets/NTSD/Scripts/Simulation/Core/NTSDBattleTickSystem.cs`: battle classifier tick placement versus existing after-world Results host input. The two responsibilities must remain distinct.
- `Assets/NTSD/Scripts/Simulation/Ecs/Results/BattleResultsOutcomeHostWriter.cs`: eligible living groups, revive lives, monotonic timer and milestone outputs. Keep mode-4 reserve behavior only where current playable mode ownership proves it; do not silently erase it.
- `Assets/NTSD/Scripts/Simulation/Runtime/BattleRuntimeState.cs`: existing Results/guard/timer fields, reset and rematch contract. If new state is truly needed, trace snapshot/checksum/restore writers before declaring any code-path list complete.
- `Assets/NTSD/Scripts/Simulation/Ecs/Results/BattleResultsWriter.cs`: existing result-page input/nav owner; inspect only unless evidence shows it must consume the native 144-continue boundary. The approved result-page visual exception stays.
- `Assets/NTSD/Scripts/Test/Editor/BattleResultsSceneHostTickAlignmentEditorTests.cs` and `BattleResultsOutcomeHostWriterSeamEditorTests.cs`: preserve after-world result-page input and active-scene non-retrigger; correct old `HadBoth` expectations only after exact RED evidence. Prefer a new focused test fixture for source-matched living groups, first terminal tick and timer milestones.
- `Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs`: `CheckBattleResultsSlotAndRelationContracts` and `CheckBattleResultsPersistentBucketAndPauseContracts` explicitly assert old team 0, group 61/62, two-bucket, and timer-pause behavior (`FW-RESULT-01`); the reserve checks around `RESULT-RESERVE-09` also depend on the old guard. Review and update only the assertions whose native first difference is proven. Preserve active Results page input and reset checks that remain valid. This path must be included in the exact Change Record if any assertion changes.

This is an ownership map, not a permission to edit all listed files. The Change Record must name only the paths actually required after focused RED and call-chain closure. Scene, Prefab, GameConfig, formal or legacy DAT/images, nonbattle UI, GAS, generated code, and third-party packages are outside this Task.

## Test-first and acceptance exit

1. Establish matching initial World/seed/input and capture the first differing tick for: two live groups; HP-zero with revive lives 2; groups 0/5/40; three eligible groups; current-tick lethal hit; revival after timer start; exact 79/80/100/101/143/144/349/350 boundaries and Attack/Jump continue. Start with `CheckBattleResultsSlotAndRelationContracts`' existing registered World fixture, or an equivalent direct test setup; its old expectations are not the authority. Do not run every character for these shared rules; add a character-specific case only if a specific branch or observed first difference requires it.
2. Implement source-matched classification and timing only after the RED trace and exact Change Record. Keep result-page input after the world tick, exclude story child-stage/group-5 flow from ordinary VS outcome, and preserve the existing ordered shutdown and User-approved results UI exception.
3. Require Unity compilation with zero errors, relevant focused tests, current `BattleRuntimeSelfCheck`, a representative real battle terminal flow/continue and close/re-enter, first-difference trace to formal playable source, unchanged protected Scene/user changes, and `Tools/Validate-ChangeLedger.ps1` PASS. Q08 mode-4 result-settings stage-count authority gap is a separate unresolved item and cannot be filled from the 24 pre-battle background IDs.

Rollback boundary: revert only edits owned by this Change ID after recording the failed evidence; never reset or clean the shared worktree. Q06 local exit remains closed. Q08, Q07 and the master goal remain active until their respective exits have evidence.
