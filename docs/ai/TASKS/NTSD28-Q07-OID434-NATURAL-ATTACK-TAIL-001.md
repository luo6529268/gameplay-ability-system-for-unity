# NTSD28-Q07-OID434-NATURAL-ATTACK-TAIL-001

Status: `VERIFIED_SCOPED_ENTITY_BINDING`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001`, BATCH-04/Q07 with Q09 visual handoff; Q07 and Q09 remain open.

Starting evidence: original Editor natural after254 branch has 45 gap-free controlled ticks, OID434/action35 but no action396; actor goes `254 -> 0`. Existing controlled Logan/Unity raw trace reaches actor action25, OID434 action100 and second OID434/action396 at tick29. Production SpriteCatalog `(434,36)` already published with formal `ras.png` SHA. The target is to identify the natural physical-input continuation that actually reaches action396, then read the live entity's `GetRenderPicIndex()` and `TryResolveCurrentSpriteEntry()` against that published entry.

Exact script scope: extend only `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07Oid434NaturalBindingProbeEditor.cs` to support a new explicit request mode after its existing after254 natural probe has finished. It may queue a fresh physical J press through the shared InputSystem once Naruto has returned to standing, pause the LocalFreeRun driver and advance one controlled tick per Editor update, release J after two ticks, inspect OID434/action396 for up to 20 follow-up ticks, then restore pause and exit Play. The existing negative-branch mode and reports remain intact. Unique create-new request/output. No production logic, DAT, image, Scene, Prefab, menu or nonbattle edits.

Exit: imported script/Editor build 0 errors, original Editor Play records both the natural baseline and follow-up attack input, one-tick cadence and OID434/action396 entry or an explicit first missing step. If action396 exists, compare effective pic/key/sheet/crop/central binding to formal source; do not claim renderer command, GPU pixel, formal EXE same-input visual parity, all Q07 or Q09. Verify Editor exits, Console error0, Scene/GameConfig SHA unchanged, Change Ledger and diff check. Preserve FAIL. Rollback limited to this new opt-in diagnostic change subject to protected-file rules.

Diagnostic correction before second script edit: first Play `oid434-attack-tail-20260925-1` reached OID434/action396 at tick54 with pic36, resolved formal `ras.png`, 48×48 and valid central binding; physical J entered tick53. The actor's ordinary attack was action20, while the diagnostic PASS predicate required action25 from the earlier controlled replay, so status FAIL is an over-specific assertion. Preserve this first result and make the predicate accept the observed native ordinary attack alternative 20/25 via the already-recorded `followupFirstAttackAction`, without changing production behavior or hiding other checks. Re-run with a fresh request/result.
