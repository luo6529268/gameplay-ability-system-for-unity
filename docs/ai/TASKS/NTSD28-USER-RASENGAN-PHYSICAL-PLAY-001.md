# NTSD28-USER-RASENGAN-PHYSICAL-PLAY-001

Status `FOCUSED_TEST_PASS / DIAGNOSTIC_ONLY / USER_SYMPTOM_OPEN`. Parent user report `NTSD28-USER-NARUTO-RUN-ATTACK-WINDOW-001`; follows the 32-tick canonical trace package `NTSD28-USER-RASENGAN-WINDOW-PARITY-001`.

Authority: formal NTSD 2.8-Logan EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable/core source and formal runtime DAT; source-model diagnostic window shows first action 253 at completed tick 24, a two-tick physical J begun after that frame reaches action 301 by tick 26, and J begun one tick later misses. The controlled original Unity Driver matched Naruto action/counter/MP across all 32 ticks, but physical GameView input and displayed frame are unmeasured.

Scope: add only `Assets/NTSD/Scripts/Test/Editor/NTSD28UserRasenganPhysicalPlayProbeEditor.cs` and its Unity `.meta`. It must run inside the existing original-project `NTSD_Battle` Play clone, use existing Naruto player OID 2 and Input System physical J, log frame/action/counter/input held/edge and wall time from action 241 through action 253 and the transform attempt, and save a JSON report under `Temp/diagnostics/NTSD28-USER-NARUTO-RUN-ATTACK-WINDOW-001/`. The probe may place the existing Play-only Naruto at authored action 241 and reset its MP for repeatability, but it must not modify saved Scene, production runtime, DAT, camera, input bindings or nonbattle owners. It must release synthetic keyboard input in all outcomes.

Acceptance: original Editor compile without errors; current Battle Scene bootstrap completes before invoking the menu; one targeted run observes the canonical physical J state and exact subsequent action, with no false PASS if the Input System action or human roster binding is absent. Compare the actual press/FrameInputSet/action sequence with source-model tick phase, keeping formal EXE visible behavior as a separate open gate. Verify Scene SHA and DAT/Scene git status unchanged and run the Change Ledger validator. The user issue is not closed by a diagnostic test alone; any generic production fix needs a separate Task/Change and first-difference evidence.

Rollback: reviewed inverse of only this new Editor probe and its Task/Change registrations, preserving all pre-existing dirty work. No destructive Git operation.

Amendment before the next edit: the first original-Editor Play run observed Naruto's first action-253 tick, queued physical J 3.424 ms later, and the next tick showed canonical Jump held+pressed 32, runtime KeyJump 1 and action 301. Add a second diagnostic menu in this same new Editor-only script that waits through the second observed action-253 tick before queuing J and saves a separate late-window report. Keep the first-menu behavior intact. Correct the row field named `counter` to read runtime `AttackingCounter` rather than `Trans.WaitCounter`, which is a frame binding indicator; this changes diagnosis output only. Hold J for two ticks relative to its actual queue tick. Recompile and run only the late boundary in the same original Battle Scene, compare with source-model tick26 start, and do not infer formal EXE visible parity.

Scoped exit: original Editor Play first253 and second253 physical J probes both PASS; second253 tick1291 press enters the next canonical FrameInputSet and action301 at tick1292. Reports are `rasengan-physical-first253-play.json` and `rasengan-physical-second253-play.json` in the declared artifact directory. No production behavior changed. The user's earlier-than-EXE feeling remains open until the formal EXE's on-screen cue and the same physical press timing are compared. Q07 new work remains behind this regression.
