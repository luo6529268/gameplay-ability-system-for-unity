# NTSD28-Q08-ATLAS-ALLOCATION-TRACE-001

Status: `RUNTIME_PENDING / TRACE_CAPTURED`. Parent BATCH-04/Q08. Requirement: unblock formal-content second-result Scene validation without substituting a no-image proxy. Two earlier isolated Editor attempts and the one opt-in trace run crashed before battle bootstrap inside `BattleAtlasResourceBuilder.TryBuild` at ordered-page `Texture2D` construction. The trace measured 145 pages, array budget rejection, page 88 crash and sampled process memory; details in `artifacts/diagnostics/NTSD28-Q08-RESULT-TRANSITION-HOST-AUDIT-001/ATLAS-ALLOCATION-TRACE-RESULT.md`. No second-result runtime verdict exists.

Exact governed script path: `Assets/NTSD/Scripts/Animation/Runtime/BattleAtlasResources.cs` only. Add an opt-in command-line diagnostic that reports atlas plan page count, array policy decision and ordered-page allocation progress. No change to planning, texture allocation, upload, fallback, ownership or production default. Do not touch Scene, GameConfig, content, GAS, J: authority or nonbattle code. The isolated clone may receive the same one-file diagnostic copy for validation; original and isolated scene hashes must remain unchanged.

Acceptance: build/compile, run a single isolated formal-content Scene test with this flag plus external process memory sampling, and record planned pages, last attempted page, array decision, peak observed process private bytes, XML/crash outcome. A crash without XML is environmental evidence, not a battle-result verdict. Once diagnosed, either fix the measured cause in a separate accurate Task/Change or keep real Scene acceptance pending. Run `Tools/Validate-ChangeLedger.ps1` and `git diff --check`. Rollback only the declared diagnostic script lines after reviewed diff under repository rules.
