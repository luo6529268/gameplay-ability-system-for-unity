# NTSD28-USER-D024-RANDOM-WEAPON-SOURCE-BIRTH-001

Status: `VERIFIED_SCOPED / D024_Q07_OPEN`. Parent: D-024 / BATCH-04 Q07; W-07 user-approved Unity random-weapon exception. Formal release identity: root `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; paired playable `SimulationTickDriver28::step` proves the selected-mode empty candidate-table stop, **not** nonempty random-drop creation parity. This package implements the user's all-entity source-coordinate history requirement for the already retained Unity exception, not a new formal random-drop rule.

Observed evidence: original Editor 600-tick historical `d024-ai-child-play-12.json` (SHA-256 `5117798FE4804750A6DA0FFB6B48B5A8B8AFE6B18E98F4E43AF707F8980CB228`) reports `missingSourceCount=231`, `missingSourceSlots=51:120`, after owner-1 OID120 random-weapon births. Current `BattleRandomWeaponDropModule.RunNormalDrop` and `SpawnMode2RandomWeapons` create parent-null `OPointCreateTask` without `useSourceRulePosition`. `BattleLogicEntityFactory.PrepareFinalRuntimePosition` adds Z+1 for mode-2 tasks, including the source Z only if the source flag is set. `LF2Entity.ApplyInitialRuntimePosition` initializes the carrier only when that flag is true. Later controlled AI-child run -14 zero misses does not cover random drops.

Exact script write scope: `Assets/NTSD/Scripts/Simulation/Passes/RandomWeapon/BattleRandomWeaponDropModule.cs` (only task emission in normal and mode-2 random drops) and `Assets/NTSD/Scripts/Test/Editor/NTSD28B0F8Owner99ProductionEditorTests.cs` (existing deterministic direct-birth fixture). Register this Change in Ledger/STATE/handoff/alignment. Do not change DAT tokens/numbers, selected-mode candidate source, RNG order/count, selected OID, slot, owner sentinel, physical X/Y/Z, velocity, frame, Scene, camera, UI or nonbattle code. Preserve all dirty work.

Implementation: initialize source-rule X/Z from each drop's existing absolute spawn X/Z; normal drop already skips factory Z+1, while mode-2 retains factory Z+1 for both physical and source Z. This is an initial dual-coordinate carrier, not a new distance scaling or source-domain reader. Leave the formal nonempty-table branch unclaimed.

Acceptance: add assertions to the existing deterministic normal and mode-2 tests that source carrier is initialized and source precise/integer X/Z equal the expected absolute final spawn position. Run focused original-project Editor test with RED before production write if available, then GREEN; verify prior owner/RNG/position assertions, Unity compile 0 error, unchanged Scene SHA, `git diff --check`, and `Tools/Validate-ChangeLedger.ps1`. If original Editor test transport is unavailable, report `CODE_WRITTEN`/`RUNTIME_PENDING`, never full alignment. Rollback only the exact new source task fields and test assertions after provenance review.

Current scoped result: original Editor forced recompile of new assertions produced RED 0/3 (job `dbc4e6852b75406fba839f8f4667a098`), then recompiled production and passed the same three tests 3/3 (job `f0ae3b9cc1224fb598c34cd8d41a7ef8`). Existing position/owner/frame/RNG assertions remained green. Editor Console returned zero errors, Menu/Battle saved Scene SHA stayed `785F828...81E13` / `2EE465D8...B77A`, diff check and Ledger validator passed (820 Records, 20 governed code files in the shared diff). The earlier stale-DLL 3/3 is excluded. A new full-Driver natural-drop Play/EXE same-condition result and broader D-024/Q07 exit remain pending. See the same-ID acceptance report.

2026-09-25 post-fix original-Editor natural full-Driver result: `d024-ai-child-play-15.json` SHA-256 `2721A943F5E1D0DCE27E444ABD10E6BB199B7295C92CB0FAFEA6A62B0C70971F` ran the same mode0/seed2833 600-tick Menu→Battle path and again spawned owner-1 OID120 random weapons at ticks231/476/540. `missingSourceCount=0` versus historical pre-fix 231, with ordered shutdown complete, return to Menu, pool borrowers0 and both saved Scene hashes unchanged. The shared AI-child probe reports `FAIL / NO_CHILD_OBSERVED` because natural AI did not produce an owned non-sound child; that is a separate unresolved acceptance, not a random-weapon source-carrier failure. Original clean Battle Scene was reopened. This closes only W-07 normal random-weapon source birth for this natural seed plus deterministic mode-2 source birth; source-consumer all-entity coverage, formal nonempty candidate table, D-024/Q07 and total goal remain open.
