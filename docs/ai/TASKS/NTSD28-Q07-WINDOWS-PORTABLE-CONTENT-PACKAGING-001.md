# NTSD28-Q07-WINDOWS-PORTABLE-CONTENT-PACKAGING-001

Status: `VERIFIED_BUILD_OUTPUT / PLAYER_RUNTIME_PENDING` (2026-09-22; see WINDOWS-PLAYER-PACKAGING-ACCEPTANCE.md). Parent BATCH-04/Q07. Authority: D-023 formal NTSD 2.8-Logan content, Q01 1,343-file SHA manifest, current `BattleContentSource` file layout and `CharacterAnimtorManager.ResolveConfiguredLoganSource`. The formal release target is Windows; this package declares a Windows Player distribution subset and does not certify Android/WebGL or final Q07 content activation.

Exact script scope: add `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07WindowsContentBuildProcessor.cs` and its generated `.meta`. The processor participates in successful Windows Standalone Player builds and copies only the exact Q07 manifest files from `Assets/NTSD/Content/LoganRuntime` to the EXE sibling `Assets/NTSD/Content/LoganRuntime`, with byte length and SHA checks before and after copy. Copy the existing required `Assets/NTSD/Sprite/UIPanels/SPARK.bmp` to `<exe>_Data/NTSD/Sprite/UIPanels/SPARK.bmp` because the current battle prewarm reads that raw path. Do not copy unrelated Config/Sprite trees, stage.dat, audio or nonbattle assets. Existing destination bytes may be retained only if their SHA matches; conflicting or extra formal-content files fail closed without overwrite/deletion. Source manifest, GameConfig, Scene, production loader and other build scripts remain unchanged.

Acceptance: fresh Unity script compile; focused manifest validation; an actual clean Windows Player build invokes the processor and produces all 1,343 expected files with exact source/manifest/destination SHA and no extra non-meta formal files, plus byte-identical SPARK; built EXE and Data directory exist. Validate scene hash, unchanged GameConfig and ChangeLedger. This package proves build output, not Player runtime startup, natural skill behavior or final Q07 delivery. Subsequent Q07 Task must run the built Player with formal content selected before production GameConfig changes.

Test-entry amendment before any second script edit: the first fixed-path R8 build now exists and must not be overwritten. Add `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07WindowsBuildProbeEditor.cs` and `.meta` only to request one unique new `Temp/Q07-Windows-Player/<runId>` Windows Mono Development build in the existing Editor, record its BuildReport, restore any temporary backend setting and avoid running the Player. This is a build-test harness, not a production loader or nonbattle edit.

Build-environment amendment: an actual unique Q07 build reached Burst AOT and failed with its assembly-reference timestamp error before postprocessing; the successful existing R8 build disables Burst temporarily via `ProductionEntityStressPlayerBuild.DisableBurstForDiagnosticBuild` and restores its prior bytes. The Q07 probe may invoke that existing exact helper and restore the captured `BurstAotSettings_StandaloneWindows.json` bytes in `finally`, while still using a new output ID. Verify the settings file's pre/post hash. Do not change BattleContent or unrelated PlayerSettings behavior.

Risk/rollback: an automatic Windows build callback adds about 46.6 MB of raw battle content to build output and can fail a build when source/output diverges. It does not alter non-Windows build behavior. Generated output lives under a unique `Temp` build directory; no existing output is deleted or overwritten. Roll back only the declared build processor under the repository deletion approval rule. Preserve all current user work and existing build scripts.
