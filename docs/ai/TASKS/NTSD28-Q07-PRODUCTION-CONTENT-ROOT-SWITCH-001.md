# NTSD28-Q07-PRODUCTION-CONTENT-ROOT-SWITCH-001

Status: `VERIFIED_SERIALIZED_BATTLE_SCENE_ONLY`. Parent `BATCH-04 / Q07`. Authority: D-023 formally sourced NTSD 2.8-Logan DAT and character-related images, Q07 exact manifest/staged identity/publication/Editor caller/Windows Player bootstrap evidence, and current battle Scene/Menu GUID `513170c32731baa449d4d0e9abf8c827` referencing `Assets/NTSD/Config/GameConfig/GameConfig.asset`.

Pre-change: the asset has no serialized `BattleContentRuntimeRoot` field and therefore uses the empty legacy root. Its SHA-256 is `685E13823A6D726219B37B1B0B2A8E9B9A76B5B7975ED9CC8E598763B366B004`. Formal staged root `Assets/NTSD/Content/LoganRuntime` holds 1,343 manifest-matched files. Explicit-root Editor App/Menu and Windows Player probes passed, but those probes injected the root in memory and do not establish serialized production selection. Scene SHA is `BCD1047BF912C6A4A8BC9F3A76EAF3FA954211AD064E0402B1C01BF3BA0E9FB6`.

Exact write scope: set only `BattleContentRuntimeRoot: Assets/NTSD/Content/LoganRuntime` in `Assets/NTSD/Config/GameConfig/GameConfig.asset`; change only `Assets/NTSD/Scripts/Test/NTSD28Q07WindowsPlayerRuntimeProbe.cs` to add an explicit Development Player flag that reads this serialized root without modifying it in memory. Documentation, Task, Record and unique test/build artifacts may be added. Do not alter GameConfig.cs, AppManager, battle Scene/Menu, prefabs, old Config/Sprite, other production code, audio, unrelated UI or Unity/GAS framework. No old-file deletion.

Acceptance: confirm the asset GUID/ref and exact field diff, Editor imports with zero script errors, unique Windows Mono Development build succeeds and packages the formal sidecar, then launch its Player with the serialized-root probe flag in a hidden window and capture exit code, terminal JSON, formal fingerprint, three owner keys, Running World, ordered shutdown, zero survivors/borrowers, and Scene/hash state. The probe must fail if the serialized root is empty or different; it may assign `GameConfig.Instance` only when null, never overwrite the root. Preserve and report audio warnings as a Q10 return. Do not claim natural skill/visual parity, other-platform portability, or Q07 exit from this package alone.

Risk/rollback: a bad serialized root could break battle content load; the root is project-relative and Player packaging is Windows-only so far. Rollback is restoring this single asset field to its prior empty/default state after recording the outcome; do not reset unrelated edits, delete staged content or mutate the protected Scene. Follow the exact validation result before broadening the next Q07 task.

Acceptance result: `q07-serialized-build-1` Windows Mono Development build Succeeded/0 errors with 1,343 formal sidecar files; hidden Player serialized-root mode returned exit code 0 and PASS, proving the scene-referenced GameConfig already had the formal root before probe logic. Expected fingerprint/three owners, World4, ordered shutdown and zero resource/pool residual all passed. Exact evidence and 13 legacy audio-directory warnings are in Q07 `PRODUCTION-ROOT-SWITCH-ACCEPTANCE.md`. Q07 remains open for natural skills, visible presentation, serialized-default Menu and reference retirement; Q10 owns audio.
