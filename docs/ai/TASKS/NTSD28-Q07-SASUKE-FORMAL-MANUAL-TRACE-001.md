# NTSD28-Q07-SASUKE-FORMAL-MANUAL-TRACE-001

2026-09-25 scoped result: `FOCUSED_TEST_PASS`. Original Editor compiled and captured unique RED/PASS 26-tick traces; after the independent state15 fix, listed entity 2500/2500 and semantically mapped input/phase 1326/1326 comparisons with the formal release have zero first difference. Old DDJ fixture rerun PASS and its 26 tick records did not change. See `artifacts/diagnostics/NTSD28-Q07-SASUKE-FORMAL-MANUAL-TRACE-001/COMPARISON-20260925.md`. Initial LFR/Unity RNG, borrowers, natural same-world Play, pixel/audio and Q07 aggregate remain open. The `IN_PROGRESS` line below is pre-exit history.

Status: `IN_PROGRESS`; parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001 / BATCH-04 / Q07`, with R02/R17/R18 limited return.

Authority and trigger: the formal root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033` produced `artifacts/diagnostics/NTSD28-Q07-SASUKE-NEEDLE-RELEASE-PLAYBACK-001/formal_release_explicit_vfs_trace.jsonl` using the official playable/core build closure. The trace has tick0 Sasuke OID11/action110 at `(500,0,350)`, OID7 at `(1200,0,350)`, tick6 action261/MP400, tick15 action264 and four OID440 in slots50–53. The old natural Unity Play used a different world position, so common milestones did not establish same-world parity.

Scope: extend only `Assets/NTSD/Scripts/Test/Editor/NTSD28UnityRawCaptureEditor.cs` with a separate `ntsd28-q07-sasuke-needle/1.0` formal 26-tick diagnostic contract. Reuse the existing Manual `FrameInputSet`, staged formal catalog, project `ProjectBattleModeConfig` snapshot, raw/domain/input-RNG outputs, visual publication and logic-only child materialization. Require the exact formal EXE identity, catalog SHA, two-person roster and action110. Preserve legacy three-tick and DDJ schemas and their observed behavior. Add a new immutable scenario fixture and uniquely named diagnostic outputs in `artifacts/diagnostics/NTSD28-Q07-SASUKE-FORMAL-MANUAL-TRACE-001/`. No production rule, DAT value, image, saved Scene, Menu, GameConfig, GAS or nonbattle edit.

Acceptance: first recheck Editor idle/compilation and formal source identity. The original Editor must compile, consume one unique request and emit 26 contiguous completed ticks. Compare by tick and stable slot against formal release trace, recording each field list, input physical-key mapping, first difference if any, RNG/init differences and exceptions; specifically inspect OID440 birth/order/velocity/lifetime. Run one DDJ or legacy three-tick regression after extending validation. Recheck Battle/Menu/GameConfig hashes, `git diff --check` and Change Ledger. Do not claim Q07/full-world/pixel parity from a bounded diagnostic. If the diagnostic fails, preserve its result and correct only evidence-backed diagnostic defects. No second Unity Editor or computer-use.

Rollback: remove only this new scenario branch and fixture under repository deletion approval, preserving all previous user work and evidence. Existing temporary Driver ordered-shutdown remains the lifecycle owner; borrower count is unknown unless separately exposed.
