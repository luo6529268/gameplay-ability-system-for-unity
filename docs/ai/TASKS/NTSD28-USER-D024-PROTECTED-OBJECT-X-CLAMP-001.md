# NTSD28-USER-D024-PROTECTED-OBJECT-X-CLAMP-001

Status: `FOCUSED_TEST_PASS / RUNTIME_PENDING`. Parent: D-024, D-025, BATCH-04/Q07 and Q08 mode-stage follow-up. Formal authority: root `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable `BattleWorld28::settle_ordinary_stage_bounds` and indexed formal OID122/123 (`data/data.txt` type6).

Observed first difference: at width800, formal protected OID122 with positive participant class clamps X50→100 and X790→700; Unity `LF2Entity.ApplyPreFrameXBounds` leaves both positions unchanged under its 10-pixel margins. Existing `NTSD28D024NonCharacterXBoundaryWitnessEditorTests` records this; `BattleRuntimeSelfCheck` still expects the old 10/790 values. User D-025 replaces immediate off-walkable culling for noncharacters with a 304-tick logical timer, but explicitly retains unrelated position rules. Formal mode1 plus selected-stage participant gate is separately present in the source; the current Unity carrier for that gate is not evidenced and belongs to Q08, so this package handles only the confirmed positive-`Unk344` branch.

Exact script write scope: `Assets/NTSD/Scripts/Animation/LF2Objects/LF2Entity.cs` protected-object clamp branch; `Assets/NTSD/Scripts/Test/Editor/NTSD28D024NonCharacterXBoundaryWitnessEditorTests.cs` focused assertions; `Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs` only the two stale protected-object expectations. Update this Task, same-ID Change Record, Ledger, STATE/handoff/alignment and diagnostic evidence. Existing dirty edits in SelfCheck remain protected. No DAT, resource, Scene, camera, Menu, project settings, ordinary noncharacter TTL or other battle branch change.

Implementation: for eligible non-type0/non-type3 OID122/123 with `Unk344>0`, clamp physical precise X to `[100,max(100,baseStageWidth-100)]` at the existing PreFrame outlet; independently apply that absolute stage boundary to initialized source-rule X and synchronize its integer mirror. Stage bounds are absolute map geometry; D-024 scales free travel, not DAT-local offsets or the stage width. Keep the current D-025 walkable timer following this clamp and preserve noneligible objects. Do not invent mode1 stage-gate values.

Acceptance: formal factor1 X50/790 and exact-edge100/700 cases, width<200 endpoint, type3/noneligible negative cases, divergent source/physical coordinates with factor1 and configured ratio, D-025 TTL behavior unchanged, focused original-Editor tests and stable SelfCheck; compile zero error, no Scene or nonbattle change. Natural Play/EXE and mode1 stage gate remain separate. Rollback only this package's exact hunks after provenance review, preserving concurrent dirty work.

Current focused exit: original Editor formal-expected RED 0/4 → GREEN 7/7 including three D-025 adjacent cases; SelfCheck `PASS`; saved Menu/Battle/GameConfig SHA unchanged. Detailed result and scope limits: `artifacts/diagnostics/NTSD28-USER-D024-PROTECTED-OBJECT-X-CLAMP-001/ACCEPTANCE-20260925.md`. A real protected-object Play/EXE witness and mode1 stage-gate owner remain open.
