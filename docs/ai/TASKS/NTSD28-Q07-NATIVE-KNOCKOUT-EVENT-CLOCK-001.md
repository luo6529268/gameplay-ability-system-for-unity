# NTSD28-Q07-NATIVE-KNOCKOUT-EVENT-CLOCK-001

Status: FOCUSED_TEST_PASS / PLAY_AND_Q09_ROW_PENDING. Parent: BATCH-04/Q07; Q08/Q09/Q10 event consumers.

Authority: formal root `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable `BattleWorld28::record_native_knockout` and `SimulationTickDriver28::step`. The Q07 same-initial-state Naruto trace shows KO birth on completed tick 8 in both runtimes but stored time formal 7 versus Unity 8. Formal writer stores `sequence_`; C24 `begin_frame_tick` runs after ordinary hit/catch settlement and before per-slot resource/late paths. Unity already advances `NativeFrameSequence` at this boundary but its KO writer uses host `CurrentTickIndex`.

Scope: change the common Unity KO event writer to capture the current `NativeFrameSequence` (no arithmetic offset), preserving the existing event schema, attribution, count and sound queue. Correct focused test/probe expectations that assumed event time equals completed host tick. No DAT/PNG/Scene/ProjectSettings/menu implementation or Q09 presentation production changes. Declared script paths: `Assets/NTSD/Scripts/Simulation/Core/SimulationWorld.cs`, `Assets/NTSD/Scripts/Test/Editor/NTSD28B5StandardReducedKnockoutProducerEditorTests.cs`, `Assets/NTSD/Scripts/Test/Editor/NTSD28B5NegativeEnvironmentRecoveryProductionEditorTests.cs`, `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07MenuSceneCallbackPlayProbeEditor.cs`, `Assets/NTSD/Scripts/Test/Editor/NTSD28Q10KnockoutModeSoundEditorTests.cs`.

Acceptance: preserve current dirty work; pre-register this Task/Record/Ledger/STATE/handoff/alignment before script edits. Prove one pre-C24 ordinary KO stores previous native sequence and one post-C24 resource KO stores current native sequence even when host tick differs. Re-run the exact original-Editor 30-tick Naruto capture and compare KO time plus old raw/domain/input-RNG tracks. Compile zero errors, run only focused affected tests that can execute in the occupied original Editor, verify unchanged Menu/Battle/GameConfig disk hashes, diff check and Change Ledger validator. If a live Play probe cannot run, report that boundary explicitly, not Q07 complete.

Risk/rollback: feed lifetime, row visibility, snapshot and checksum values change by one for pre-C24 KOs; these consumers already read the event field and need targeted boundary verification. A test fixture directly calling the writer outside a full tick sees sequence zero by design. Rollback only this package's exact line/test expectations after reviewing the diff; never restore or overwrite pre-existing unrelated edits. The root LFR CRT seed limitation and formal pixel gate remain separate.

Current evidence: original Editor compile 0 errors; pre/post-C24 2/2, Q08 event state 3/3, Q10 cue conditions 5/5. Exact Naruto 30tick event matches formal at completed tick8/time7 with three prior tick tracks each 30/30 unchanged. Q09 row test SetUp fails on excluded original mode DAT; edited Play probes and row pixel/end-time are not yet verified. See `artifacts/diagnostics/NTSD28-Q07-NATIVE-KNOCKOUT-EVENT-CLOCK-001/ACCEPTANCE.md`.

Play addendum: the existing real-Menu physical-J probe reran with the updated ordinary-KO sequence assertion and passed; it returned to selection and left no pool borrowers, then restored the saved Battle Scene. The exact numerical KO event time for this different-state Play case was not serialized; Q09 row end-time and formal pixels remain pending.
