# Task Contract — CLIENT-FORMAL-KERNEL-OPOINT-VALUE-SEAM-001

> Status: `VERIFIED / CLIENT_INTEGRATION_REQUIRED / STANDING_CLIENT_AUTHORIZED / TEST_FIRST_MISSING_SEAM_RED / UNITY_COMPILE_PASS / UNITY_EDITMODE_52_52_PASS / BATTLE_RUNTIME_SELFCHECK_PASS / S0_WITNESS_PASS / EXISTING_LOCKSTEP_PASS / GOLDEN_CORPUS_SHA_PASS / SERVER_DUAL_CONFIGURATION_PASS / FORMAL_MARKER_FALSE / S0_NOT_VERIFIED`
> Queue: `0cu / CLOSED`

The authoritative Task Contract is mirrored at
`I:\GitHub\Unity_GAS\NTSD_Server\docs\ai\TASKS\CLIENT-FORMAL-KERNEL-OPOINT-VALUE-SEAM-001.md`.
It freezes the exact eight-int OPoint value/adapter, `LF2FrameData` content,
converter, three declared runtime consumers, necessary preview bridge, focused
test and SelfCheck scope. It forbids battle/tick/package/resource/Scene/input/
network/recovery/AI/wire/marker changes and requires test-first, Unity compile,
focused OPoint, SelfCheck, S0/lockstep, Server dual and both Ledger evidence.

2026-08-31 fresh diagnosis: Unity PID `29796` remains responsive, but the Stdio
bridge owns no listener, `Library/MCPForUnity/RunState` is empty, and the Editor
assemblies predate this package's sources. The request-file SelfCheck path does
not import scripts and therefore cannot certify this package. Start the Stdio
session in the existing Editor, then run the Server contract's exact
refresh/compile/test sequence; do not start a second Editor on this `Library`.

This diagnosis is superseded: port `6401` and the framed handshake were live,
and the bridge completed fresh compile, EditMode 52/52 and SelfCheck PASS. The
package-local Editor gates are closed；formal marker/S0 remain unchanged.
