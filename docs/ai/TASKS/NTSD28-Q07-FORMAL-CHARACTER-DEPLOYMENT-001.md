# NTSD28-Q07-FORMAL-CHARACTER-DEPLOYMENT-001

Status: VERIFIED (formal type0 deployment test only). Parent: BATCH-04 / Q07 old character DAT and image retirement preparation. Authority: D-023 formal non-excluded DAT and character images; root formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033` and its `resources/runtime` catalog/content. The project mode uses its own Asset and original background/mode DATs are excluded.

Observed 2026-09-24: `CharacterAssetDeploymentEditorTests` and its menu command only prove 42 old `Assets/NTSD/Config/Character/*.dat` and old `Assets/NTSD/Sprite/Character/*.bmp` references remain. Current formal and staged `catalog.csv` each enumerate 158 `object/type0` rows. The old test therefore rewards retaining obsolete resources and does not prove the selected formal character content is deployed.

Exact script scope: `Assets/NTSD/Scripts/Test/Editor/CharacterAssetDeploymentEditorTests.cs` only. Replace the obsolete count, old decryptor/path regex checks, and old menu-test target with a current-formal source/staged-source test. Enumerate type0 entries by the existing `LoganObjectCatalog`, parse through `BuildCharacterFrameConfigsFromCatalog`, compare each selected DAT by ID/path/SHA, and compare declared head/small/frame image paths and bytes within formal/staged VFS. Retain explicit failure for missing DAT, parse failure, missing image, image mismatch, and catalog omissions. Do not change production code, DAT values, images, Scene, Prefab, GameConfig, mode Asset, or nonbattle code; do not delete or move any resource.

Acceptance: original current Editor compilation; focused replacement test PASS; adjacent staged candidate identity and existing formal PNG alpha tests PASS; saved Battle/Menu Scene SHA stable; no old Config/Character or Sprite/Character dependency in this test; `git diff --check` and Change Ledger validation PASS. A passing test certifies only formal selected type0 deployment/bytes, not full Q07 runtime behavior or old-resource deletion.

Risk/rollback: hashing all type0 images costs Editor time; use per-test deduplication. The historical 42-BMP deployment assertion is superseded for current content authority but the empty-root legacy loader remains a separate owner. If this test fails, inspect formal/staged first difference without editing DAT bytes; roll back only this test under the repository's destructive-action authorization rule if needed.

Acceptance 2026-09-24: existing original Unity Editor compiled after refresh, and focused EditMode job `3bc6f024f2784e928d749118b17d4982` passed 3/3 in 46.7s: current type0 deployment, adjacent formal/staged candidate identity, and actual formal Naruto PNG production/GPU alpha. The current test has no old `Config/Character` / `Sprite/Character` / `.bmp` dependency. Active Battle Scene was not dirty, and saved Battle/Menu Scene SHA remained stable. See `artifacts/diagnostics/NTSD28-Q07-FORMAL-CHARACTER-DEPLOYMENT-001/ACCEPTANCE.md`. This does not authorize old-resource deletion or close Q07.
