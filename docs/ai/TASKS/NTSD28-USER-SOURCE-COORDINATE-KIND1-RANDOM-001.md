# NTSD28-USER-SOURCE-COORDINATE-KIND1-RANDOM-001

Status: FOCUSED_TEST_PASS / CARRIER_NOT_ACTIVE. Parent: D-024 all-entity displacement ratio audit. This is a bounded follow-up to the late OPoint birth foundation, not Q07 or a DAT migration.

Execution evidence (2026-09-24): original Editor RED `08ea61459cf14604a34e6187faeeb68f` reproduced source random omission and zero-extent integer reprojection; original Editor compile and focused GREEN `9ba815d926e04554851867dbbb794782`14/14 PASS. Existing raw random deltas are reused without additional draws or physical battle changes. Ledger746 PASS, scoped whitespace/Scene/content checks passed. No lifecycle discrepancy required Play/full SelfCheck. See Change Record for exact coverage, first differences and outstanding carrier activation gates.

Authority: paired formal playable `source/ntsd28_core/src/simulation/battle_world.cpp` kind-1 post-spawn block around lines 7723-7739 applies synchronized random deltas in X/Y/Z/action order, then copies integer position to precise position even when each amplitude is zero. Current Unity `BattleNativeOpointBirthWriter.InitializeBirth` applies the same random calls to battle physical X/Z and reprojects battle precise coordinates. It does not update the newly added independent source-rule X/Z after nonzero random X/Z, so a later source-rule reader would see pre-random history. The user requires ratio-consistent physical movement without DAT edits; the formal rule history must remain separate from scaled physical position.

Exact script scope: `Assets/NTSD/Scripts/Simulation/Ecs/Writers/BattleNativeOpointBirthWriter.cs` and `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06OpointMaterializerEditorTests.cs`. No other script, resource, Scene, camera, ProjectSettings or formal DAT edit is authorized by this package.

Implementation contract: consume each existing synchronized random draw exactly once in the same X/Y/Z/action order and use the already drawn raw X/Z deltas for initialized source-rule integer mirrors. Kind-1 source precise X/Z must be reprojected from the updated source integers, including zero extent. Keep source state absent if the parent/source birth was absent. Preserve current battle physical scaling, integer conversion, random call counts, action latch and materializer behavior. Do not infer source history from battle coordinates or scale formal local DAT fields.

Acceptance: first write a focused case using the existing in-memory synthetic kind-1 variation (no DAT file edit) that fails on current source-rule random history, then pass in both factor-1 and configured 2048×1152 views with identical formal synchronized call count, expected raw source delta and proportional battle delta. Include zero-extent and uninitialized-source controls. Run original Editor compile and narrow tests, ChangeLedger validator, diff whitespace and Scene/content boundary checks. Real Play is needed only if the focused result exposes a lifecycle discrepancy. Rollback is limited to the two declared paths, preserving all other dirty work.
