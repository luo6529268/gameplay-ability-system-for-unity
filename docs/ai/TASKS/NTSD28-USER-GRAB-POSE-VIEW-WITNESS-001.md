# NTSD28-USER-GRAB-POSE-VIEW-WITNESS-001

Status: `FOCUSED_TEST_PASS / TEST_ONLY`. Parent: `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (user D-024). This package characterizes the current production kind-3 grab pose after a prior scaled character movement. It does not alter battle rules, DAT, camera, Scene or nonbattle code.

Authority: shipped Logan playable `BattleWorld28::apply_native_relation_hit` in `source/ntsd28_core/src/simulation/battle_world.cpp` around lines 5212-5254: use attacker integer X, raw frame-local `centerx/cpoint.x`, and half of victim integer X minus target anchor; assign both precise X then truncate mirrors. Unity owner is `BattleInteractionWriter.TryApplyGrab` / `AlignGrabPair`. User D-024 scales physical battle movement in a fixed full-background camera and requires a separate source-rule coordinate history where current battle coordinates are insufficient.

Pre-edit scope: add one parameterized focused test method only to `Assets/NTSD/Scripts/Test/Editor/NTSD28B6CatchRelationExactFieldsProductionEditorTests.cs`, reusing `CreateWorld`, `CreateItr(10,20,1)` and production `CharacterMechanics.StepBattleLogic` + `InteractionWriter.TryApplyGrab`. Seed both bodies at absolute X100/140, move only victim by raw Vx48 at factor1 or 2048/1333, then grab. Formal raw target before grab is X188. With frame centers 7/9 and cpoints 3/5, raw anchor92 and half-gap48 yield formal attacker/target precise X148/140. Configured battle target integer X213 yields current battle attacker/target precise X160.5/152.5. Both domains retain the same local pose gap -8; the formal source-rule reference cannot be reconstructed by inverse-scaling the final battle pose.

Acceptance: original-project Editor compiles and runs only the two cases, asserts precise and integer positions plus local gap, and exits a logic-only World cleanly. Record whether current behavior matches the calculated battle pose; a passing characterization test is not a parity fix. No broad suite is needed. Rollback removes the one method and its required `using` only. Next carrier implementation owns actual dual-domain writes, snapshot/reset/checksum and reader switching in a separate Change.

Actual: the declared test method was added without a new `using`. Original-project Editor job `6fe9428e5d1f45ea9bd5e4049b3eb22e` ran 2/2 PASS after refresh/domain reload, including the declared positions and clean shutdown. Report: `artifacts/diagnostics/NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001/GRAB-POSE-TWO-DOMAIN-WITNESS.md`. No parity claim or Q07 release follows from this focused result.
