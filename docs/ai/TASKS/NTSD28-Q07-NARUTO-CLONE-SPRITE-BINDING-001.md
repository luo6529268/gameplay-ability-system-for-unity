# NTSD28-Q07-NARUTO-CLONE-SPRITE-BINDING-001

Status: `VERIFIED_LOGIC_TO_FORMAL_SPRITE_CATALOG_ONLY`. Parent BATCH-04/Q07 and R17/R18 visual-content return. One formal-content Naruto natural-input example proves that its spawned OID33 clone follows authored hidden-to-visible sprite catalog binding. This is not pixel/render parity.

Authority: formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; formal `resources/runtime/decoded_dat/c/nar/ncl.dat` equals staged SHA-256 `1E79078AF213896E82AC18A9E0A1D9A7026D3CC5C44386D231B67798D7FA68E3`. OID33 catalog row names `Naruto(Clone)` and `c\nar\ncl.dat`. Its frame240/241 have `pic:999`; frame242 has `pic:1`. The `file(0-139)` contract maps pic1 to `c\nar\ncl.png`, 79×79. Staged and formal VFS `ncl.png` share SHA-256 `F8673833CCC0FC18695EBD956911704C8981B65179E3EEE343AA31F9597135B0`. Existing Q07 physical L/D/J Play found OID33 but did not observe its frame/pic or catalog entry.

Exact code scope: only `Assets/NTSD/Scripts/Test/Editor/BattleComboPlayModeProbeEditor.cs`. Extend its Q07 JSON request with an optional clone-sprite audit flag and a separate result root. Keep prior request/result, legacy menu items and unrelated probes intact. In actual Battle Play, trace OID33 frame/render pic, whether `TryResolveCurrentSpriteEntry` succeeds, entry visual data/effective pic, source sheet, pixel dimensions and central binding validity. Do not modify production render, content parser, GameConfig, Scene, prefab, input or old resources.

Acceptance: unique result from the current formal GameConfig root and physical L/D/J, authored target frame285, OID33 spawn, hidden pic999 with no entry, then clone pic1 with valid visualDataId33/effectivePic1 catalog entry from `c\nar\ncl.png` and 79×79 dimensions. If the live sequence does not reach pic1 within the observation window, record a FAIL/first difference rather than inventing a pass. Exit Play, verify protected Scene hash and no overwrite; run focused compile and Change Ledger. Even a PASS proves only logic-to-formal-sprite catalog binding; Q09/Q12 still own actual drawn pixels, ordering, shadows and full skill acceptance. No computer-use.

Written: the optional `cloneSpriteAudit` Q07 request branch and separate result root are in the declared existing Editor probe. Compile and Play remain pending; earlier Q07/legacy result files are untouched.

First Play retained as `q07-naruto-clone-1.json`: observed frame241 hidden and next-tick frame242 formal pic1 binding. The test's frame240-only condition and initially missing diagnostics directory caused its FAIL/write error. Same declared probe now accepts either authored hidden frame240/241, precreates its result directory and stops observation in `finally`. Rerun pending; do not promote the first FAIL to a production defect or PASS.

Acceptance: fresh `q07-naruto-clone-2.json` PASS, physical steps ticks2/4/6, clone frame241/pic999/no entry tick11 then frame242/pic1/key(33,1)/formal ncl.png/79×79/valid central binding tick12. Editor exited Play, protected Scene hash unchanged. Full evidence and limits are in `artifacts/diagnostics/NTSD28-Q07-NARUTO-CLONE-SPRITE-BINDING-001/ACCEPTANCE.md`; Q07 visible pixel and whole-skill exits remain open.
