# NTSD28-USER-FIXED-VIEW-RUN-RATIO-001

Status: `IN_PROGRESS`. Parent: `NTSD28-UNITY-BATTLE-REALIGNMENT-001`; user-priority Naruto run-distance regression before resuming Q07.

User direction: retain the original Unity battle camera dimensions and complete-background framing. Match the formal 1333-pixel viewport's on-screen run-distance fraction in the Unity 2048-pixel-wide 16:9 Training view. The user explicitly accepts that actual battle run displacement then differs from formal NTSD logic. This is a declared user exception, not a claim of native movement parity.

User data constraint (2026-09-23): no DAT file data may be changed unless the user explicitly asks for it. Runtime speed/position application is the allowed outlet. Verify DAT paths remain untouched after implementation.

Later user decision D-024 expands the eventual ratio target to displacement of all battle entities, including weapons, items and projectiles. This existing Change remains a bounded character run/dash slice; it must not be mistaken for full-entity coverage or silently expanded into a cross-module rewrite. The follow-up inventory and implementation gates are in `docs/ai/TASKS/NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001.md`.

D-024 projection correction before further script edits: formal `RenderCamera28` is 1333×730 and `render_snapshot.cpp` projects `screen_top = position.z + position.y - center_y`; Unity `NTSDRenderSpace.GroundPixelToWorld` maps Z into screen Y, and the current full-background Training reference is 2048×1152 at 16:9. The existing character run/dash slice incorrectly applied `2048/1333` to both X and Z. Within this already declared slice, configure an independent vertical reference `1152/730` and apply it only to run/dash/heavy-run Z writers while X keeps `2048/1333`. The existing Task/Change declared paths already include GameConfig, World, Host, entity/resolvers, Native writer and focused tests; no new production file, DAT or camera change. Diagnostic Worlds default to scale 1 on both axes. Do not infer this single Training sample is universal for other maps/aspects or for the remaining entity kinds.

Measured input: formal source-model and original Unity three-tick physical-input diagnostic both give normal run 48 logical pixels and landing dash 29 logical pixels. Formal viewport width 1333; Unity existing Play camera height 1152 logical pixels and 16:9 width 2048. Target horizontal multiplier is 2048/1333 = 1.536384..., yielding about 73.7 and 44.6 pixels before integer rounding. If another Game View aspect or map is used, this fixed Training reference may not match the displayed fraction; it must not depend on render-frame camera state during a tick.

Declared implementation paths: `Assets/NTSD/Scripts/App/GameConfig.cs`, `Assets/NTSD/Config/GameConfig/GameConfig.asset`, `Assets/NTSD/Scripts/Simulation/Core/SimulationWorld.cs`, `Assets/NTSD/Scripts/Simulation/Host/SimulationTickDriver.cs`, `Assets/NTSD/Scripts/Animation/LF2Objects/LF2Entity.cs`, `Assets/NTSD/Scripts/Animation/LF2Objects/LF2CharacterActionResolver.cs`, `Assets/NTSD/Scripts/Animation/LF2Objects/LF2CharacterStateResolver.cs` and a focused test file under `Assets/NTSD/Scripts/Test/Editor/`. No Scene, DAT, source authority, camera code, input timing, UI, or nonbattle file change. Amend Task/Record before widening paths.

Amended diagnostic ownership before edit: `Assets/NTSD/Scripts/Test/Editor/BattlePhysicalMovementPlayModeProbeEditor.cs` may add a second menu variant that keeps physical D+K held through actual landing and records the following run ticks. Preserve the existing release-before-landing probe unchanged as a separate regression. This is Editor-only validation, not a new gameplay input path.

Amended focused-test ownership before edit: `Assets/NTSD/Scripts/Test/Editor/NTSD28NativeType0AirDashRedirectBuiltinsEditorTests.cs` may add one configured-world action-215 left/right velocity test beside its existing formal-scale cases. This checks the actual shared action writer with unchanged authored DAT values.

Amended production ownership before edit: the focused test exposed the current Native/ECS production outlet still writing authored run/dash speeds directly. `Assets/NTSD/Scripts/Simulation/Ecs/Writers/BattleCharacterActionWriter.cs` may apply the same world multiplier to its run, heavy-run, action-215 dash and running-jump dash X/Z assignments. Do not scale the unrelated special-ground-state depth speed.

Amended configured-run test ownership before edit: `Assets/NTSD/Scripts/Test/Editor/NTSD28NativeType0GroundBuiltinsEditorTests.cs` may add a focused configured-world normal/heavy running assertion for both horizontal directions and lane speed, using its existing fixture and unchanged authored values.

Design: record one immutable production-world horizontal run multiplier from a battle config reference width (2048 here) divided by formal view width 1333. Diagnostic Worlds default to 1. Apply it to authored running/dash/heavy-running X and lane Z velocity assignments in native shared and legacy character paths for every character; keep jump height, knockback, projectiles, walking, DAT values, and tick cadence unchanged. Account for directional dampening after the velocity assignment. The multiplier must be symmetric for left/right and must not compound when a state writes speed on successive ticks.

Risks and acceptance: collision, stage-boundary arrival, AI pursuit and run-attack reach change as consequences of real displacement; do not present formal per-tick X trace as zero-diff after enabling this exception. Compile original Editor, run narrow policy and character run/dash checks, run physical Naruto D+K held through landing in real Play, verify camera/Scene unchanged and source data hashes unchanged. Check normal and heavy run plus both directions; report any unmeasured category. The advanced-skill timing problem is separate and remains open. Roll back by reviewed inverse patch on this Change's exact paths, never by resetting the dirty tree.
