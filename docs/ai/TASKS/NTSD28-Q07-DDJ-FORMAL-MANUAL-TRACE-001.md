# NTSD28-Q07-DDJ-FORMAL-MANUAL-TRACE-001

2026-09-25 scoped exit: `FOCUSED_TEST_PASS`. The original Editor compiled and ran `manual-ddj-20260925-3` for 26 contiguous ticks; 1425/1425 listed entity fields and 767/767 input/phase fields matched the formal release replay after an explicit physical-key mapping. The old three-tick scenario also PASSed. Exact artifacts, SHA-256 values and the unresolved initial RNG difference are in `artifacts/diagnostics/NTSD28-Q07-DDJ-FORMAL-MANUAL-TRACE-001/COMPARISON-20260925.md`. Borrower counts, same-world natural Play, pixels and Q07 aggregate exit remain unverified. The older `IN_PROGRESS` lines below are pre-exit history.

2026-09-25 -2 runner PASS only proves 26 consecutive diagnostic captures and current Logan/project-mode identity. The DDJ action did not occur because the scenario used `K/S/J`: observed raw input at ticks2/4/6 is jump/down/attack; the previously verified physical route uses `L/S/K`. Keep the -2 artifacts, create a separate -3 scenario with L/S/K and rerun. Do not change the shared input map or call -2 an alignment success.

2026-09-25 first original-Editor request `manual-ddj-20260925-1` returned archived FAIL at allocation seal: diagnostic `GameDataManager` had the project-mode identity, but `CharacterAnimtorManager` had no published candidate because the initial roster contained only characters. The production same-identity guard is correct. Next exact diagnostic edit: create/set the same Q07 visual candidate in `UnityCurrentDatScope` even without an initially requested SpecialAttack; preserve old routes, compile and rerun unique -2. No production/DAT/Scene change.

Status: `IN_PROGRESS`. Parent: `NTSD28-UNITY-BATTLE-REALIGNMENT-001 / BATCH-04 / Q07`.

Authority: formal root `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, current playable DDJ source path and preserved 26-tick `formal_release_playback_action110_trace.jsonl`. The natural original-Editor `ddj-state-20260925-5` Play proves skill milestones but has a different tick-0 world and misses observed ticks 1/3. Its physical-key route is retained as a separate witness.

Exact scope: extend only `Assets/NTSD/Scripts/Test/Editor/NTSD28UnityRawCaptureEditor.cs` with a separately identified 26-tick Q07 DDJ scenario contract, using its existing Logan `UnityCurrentDatScope`, Manual `FrameInputSet`, one-step Driver and raw/domain/input-RNG output. For this new scenario only, capture the project's `ProjectBattleModeConfig` snapshot rather than excluded original mode DAT, require the staged Logan catalog SHA and formal EXE identity, and allow logic-only child materialization. Keep every existing three-tick scenario contract and default legacy path unchanged. Add the immutable scenario fixture under `artifacts/diagnostics/NTSD28-Q07-DDJ-FORMAL-MANUAL-TRACE-001/`; run the already-open original Editor request path and write unique outputs in the same artifact directory. No production code, saved Scene, DAT values, image, Build Settings or nonbattle behavior may change.

Acceptance: original Editor compiles without errors; a unique request emits 26 completed tick rows from staged formal content and project mode Asset, with tick-0 scenario/source identity and current catalog fingerprint recorded. Compare all 26 common fields against the preserved formal release trace, classify the first difference and approved map/mode/D-024 coordinate exceptions without hiding it, and check result status, shutdown/borrowers if exposed, unchanged Scene/GameConfig hashes, `Tools/Validate-ChangeLedger.ps1` and `git diff --check`. One representative old three-tick scenario should still pass if the validation branch touches it. A trace may FAIL; preserve its artifact and fix only evidence-backed diagnostic defects. Do not report whole-world or pixel parity from this test alone.

Rollback: remove only the new Q07 scenario branch and fixture, subject to repository deletion rules, without reverting shared user changes. Prior DDJ Play and release artifacts remain intact.
