# NTSD28-Q07-LEE-CHILD-COMPOSED-WORLD-CAMERA-001

Status: VERIFIED / SCOPED_UNITY_COMPOSED_WORLD_CAMERA_PIXEL. Parent: BATCH-04/Q07 and D-024, both open.

Authority and prior evidence: root formal NTSD2.8-Logan.exe SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; paired playable Lee ordinary J→L/OID204 path. The original Editor's saved Battle Scene Play already proved natural five-child birth, formal pic28 binding and isolated central-camera pixels under `NTSD28-Q07-LEE-CHILD-CAMERA-PIXEL-001`. The prior isolated readback forced a black clear and cullingMask 0, so it did not test the project's retained background layers.

Declared scope before script edit: extend only `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07LeeJlBattlePlayProbeEditor.cs` with a distinct opt-in composed world-camera capture mode and unique artifact root. Retain the existing default and isolated capture behavior. Reuse the ordinary J→L, complete production Driver, published first-child command ROI, camera restoration and ordered shutdown. Save tick11/tick12 PNGs with the saved world camera's original culling mask, clear flags, color, HDR and MSAA; change only its temporary targetTexture. No production code, DAT/image, Scene, Prefab, ProjectSettings or nonbattle change. Use the existing original Editor only; no computer-use, second Editor or overwrite of prior reports.

Acceptance: original Editor compile with zero errors; unique saved Battle Scene Play report proves five natural OID204 commands and at least one changed pixel in their projected ROI in the composed world-camera captures. Independently decode the two PNGs and identify whether background is present outside the ROI; report possible overlapping actor/background contributions instead of attributing every changed pixel to a child. Require camera state restored, ordered shutdown/borrowers0, Editor exit Play, Console zero errors and unchanged saved Menu/Battle Scene SHA. Run Ledger validator and `git diff --check`. This is a world-camera composition witness, not final Game view/HUD or formal EXE same-camera GPU parity. Rollback is only the new opt-in diagnostic branch after evidence review.

Result: original Editor compile0/Console errors0; unique `lee-child-composed-01` saved-Battle Play PASS. Tick11→12 retained-background world-camera ROI had 600 changed pixels, 574 at exactly the pixels changed by the prior isolated central capture. Independent decode confirmed background outside ROI at 515004/515036 nonblack pixels. Five OID204 commands, camera restoration, ordered shutdown/borrowers0, Editor exit Play and stable Scene SHAs passed. See `artifacts/diagnostics/NTSD28-Q07-LEE-CHILD-COMPOSED-WORLD-CAMERA-001/COMPOSED-CAMERA-ANALYSIS.md` for hashes, calculation and limits. Q07/EXE A/B remain open.
