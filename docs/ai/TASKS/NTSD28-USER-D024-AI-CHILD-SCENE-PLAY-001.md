# NTSD28-USER-D024-AI-CHILD-SCENE-PLAY-001

Status: `RUNTIME_PENDING`. Probe script compiled in the original Editor, but first request was rejected before Play because the active Menu Scene is dirty. See Change Record. D-024 non-perceptual verification before Q07 resumes.

Authority: formal root NTSD2.8-Logan EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, its playable `NativeAi28` target selection and late OPoint spawn call chain, and user-approved full-background physical displacement ratio without DAT edits.

Pre-change evidence: original-project EditMode formal-content controlled Driver human12/AI3 test 2/2 proves source-carrier continuity, target selection and physical/source X ratio. Extending AI to tick4 attempted a late OPoint, but the isolated replay fixture has no Renderer pool and failed at `LF2ObjectPool.Get`. Existing real Play pooled Renderer test directly exercises a formal late OPoint birth, not the AI decision through full Driver. Natural Menu-to-Battle AI child birth remains unproven.

Declared script path: `Assets/NTSD/Scripts/Test/Editor/NTSD28D024AiChildScenePlayProbeEditor.cs` only. Create a request-driven original-Editor Play probe that starts from the saved Menu scene, uses current formal `GameConfig` root, creates one human and one AI battle participant through `AppManager.InitializeBattleAsync`, steps the production Driver with the real pool, records target/source/physical child state and ordered shutdown, then exits Play. Before entering Play, reject a dirty Scene. No DAT, Scene, Prefab, menu code, production battle code or camera changes.

Acceptance: exact request/result lifecycle; confirm formal content identity and scene boundary; AI target selection in full Driver; if a child spawns, prove source carrier, raw source-local birth, scaled physical output, Renderer and no pool borrower leaks after shutdown. A no-child run is an evidence gap, not a parity pass. Verify original Editor compile, a fresh result, Scene disk SHA unchanged, focused ledger validation and diff check. Do not call direct late-OPoint Play proof equivalent to natural AI chain. If the existing runtime cannot reach this with no production code changes, preserve failure and define the next bounded work rather than manufacturing a special gameplay rule.

Rollback: remove only the new probe and its `.meta` after reviewing provenance; retain user work and all existing Play probes.

2026-09-24 diagnostic continuation before test-only script edit: original Editor request `d024-ai-child-play-4` passed Menu prewarm and AppManager birth, then completed 60 full Driver ticks with OID2 human/OID7 AI but observed `aiTargetSlot=-1`, no child and zero missing source carriers. Ordered shutdown returned to Menu with zero pool borrowers; both Scene disk hashes remained unchanged. This is a focused runtime failure/evidence gap, not proof of a production AI defect. Next edit stays within the declared probe script and adds read-only participant team/position/type and per-tick AI target diagnostics; rerun once to distinguish fixture conditions from a runtime fault. No production, Scene, DAT or camera edits.

Run `d024-ai-child-play-6` isolated the fixture window: both participants had HP500, teams1/2, and full Driver alternated input phases 1/0, but the probe stopped at tick60. Production AppManager initializes each participant with `HitStun=75`; AI nearest candidate handling excludes a hit-stopped distant opponent from ground selection and air override requires X<250/Z<40, while this scene starts X706/Z118 apart. Before the next declared test-only edit, extend the bounded observation window past initial hit-stop and record its decay. This does not authorize production behavior or DAT changes.

Correction from `d024-ai-child-play-7`: HitStop reached 0 by tick76, yet the target remained `-1` through tick180, so initial stun explains only the first interval. No child was observed; shutdown and zero-borrower checks passed. The next read-only diagnostic in this same probe records the AI-consumed relation teams, current data types/states and control flags at/after the eligibility transition. The cause remains unknown; do not mark production AI defective or the child witness passed.

Run `d024-ai-child-play-8` confirmed after tick76: human RelationTeam1/HP500/type0/state1/HitStop0; AI RelationTeam2/type0/AiControlled true; target remained `-1` through tick180. The next test-only edit enables the existing per-tick AI diagnostic recorder and samples its `AiCount` and nearest-query calls at the same checkpoints. It does not add a gameplay branch or override target choice.

Run `d024-ai-child-play-9` showed `AiCount=1` on sampled ticks, while the legacy `FindNearestGround` phase call counter remained 0. Since current production uses `DataOrientedCanonical`, the next test-only readout must sample its indexed-canonical nearest-search and commit phase counts; the old phase counter cannot be interpreted as a skipped query. Keep result pending and do not alter production AI.

Run `d024-ai-child-play-10` confirmed the indexed-canonical nearest-search phase count is 1 at ticks 76/90/120/180, while the target remains -1. The sampled `IndexedCanonicalCommitApply` call count is 0, but source inspection shows that phase uses Begin/End without RecordPhaseCall, so its call count cannot establish whether commit happened. Stop repeating the 180-tick Play probe. Next bounded work should reproduce the same candidate rows and input state in a focused two-profile comparison, and use `AiDecisionIndexedCanonicalCommittedCountForDiagnostics`/fallback counters or an equivalent existing witness to identify the first difference. Natural child Play/EXE parity remains pending.

2026-09-24 authority reconciliation before another test-only edit: the probe omitted `match.gameMode`, so production `SimulationTickDriver` assigns BattleGameModeId=1. Formal playable `NativeAi28::select_ordinary_target` calls `native_opposing_group`, whose mode-1 rule rejects team1 as a target of team2; Unity indexed kernel's native context also uses BattleMode and the same team gate. This fully explains target -1 with the probe's team1/team2 roster, even after HitStop reaches zero. The apparent indexed-versus-Legacy mismatch would compare different mode operands and is not a demonstrated production defect. Within the already declared probe script only, explicitly configure battle mode 0 for this two-team adversarial fixture and record the effective mode; run one focused original-Editor Play request. This is a fixture correction, not a gameplay or DAT edit. Child spawn may still need an independently proven trigger.

Run `d024-ai-child-play-11` applied battle mode 0 and the AI selected human slot 0 at tick75; 180 full Driver ticks had no observed owned child, while source carriers, ordered shutdown, pool return and Scene hashes held. The team-gate fixture issue is resolved, but natural child birth remains unproven. Formal Lee OID7 DAT contains OPoint frames; the sampled tick180 frame464 is a punch frame without OPoint. Next bounded diagnostic edit stays in the declared test script: extend the observation ceiling to 600 ticks and record AI frame transitions/any new entity birth so a missed transient child can be distinguished from no OPoint action. No production rules, DAT or Scene edit.

Run `d024-ai-child-play-12` completed 600 original-Editor production Driver ticks in battle mode0. AI selected human slot0; frame history includes 61 and 486, but no persistent child with owner1. The formal Lee `lee.dat` OPoints on these frames target OID814 actions700/701, and the formal OID814 `c/lee/a/lee.dat` defines those actions as `pic:999 wait:0 next:1000` sound-only objects; immediate recycling means their lack of an extant child is expected. Three newly observed OID120 objects had owner -1 and are unrelated random weapon spawns. Do not classify this as an AI/OPoint production failure or child-birth acceptance. Ordered shutdown PASS, pool borrowers0, Menu/Battle disk SHA unchanged. Replace the fixture with a formal reachable non-sound child action before another Play claim; production logic remains untouched. **2026-09-25 raw-result correction:** the saved JSON reports `missingSourceCount=231` and `missingSourceSlots=51:120`, so the prior claim of source-carrier misses0 for this run is false. Later controlled run `-14` reports zero misses in its narrower window; it does not cover the random OID120 births.

Candidate concrete non-sound actions from the same formal Lee DAT: frame164 spawns OID204 action20 (`a/cha/cha.dat` pic28/wait2), and frame22 spawns OID706 action73 (`a/pof/pof2.dat` pic4/wait1). These were not proven reachable by the 600-tick AI seed; a future fixture must first establish an actual trigger and distinguish controlled frame injection from natural AI selection. Original Editor ended non-Play in clean `NTSD_Battle`; both cameras are serialized enabled and both Scene hashes match pre-run. `read_console` returned zero errors, scoped diff check exit0, Change Ledger validator PASSED (793 Records). No broad test suite was run for this test-only probe.

2026-09-24 next bounded test-only change, before script edit: keep the existing natural 600-tick path intact, add an explicit request flag for a separate controlled Lee frame22 trigger after battle mode0 has selected the human target. Formal Lee frame22 has wait2 and an OPoint to non-sound OID706/action73 (`a/pof/pof2.dat` pic4/wait1). The probe must report whether the trigger was applied and whether a materialized AI-owned child retained source coordinates, physical position and Renderer after the full production Driver tick. A controlled trigger proves the downstream birth path only; it does not close natural AI action selection or formal EXE A/B. Scope is the already declared Editor probe script plus this Task/Record/evidence. No production script, DAT, Scene, camera, Prefab or nonbattle edit. Validate original Editor compile, one fresh Play result, ordered shutdown, Scene disk hash, scoped diff and Change Ledger. Rollback is removal of only this diagnostic probe after provenance review.

Controlled run `d024-ai-child-play-13` reached the trigger at tick80, retained frame22 after that tick, and materialized owned OID706 at slot50/tick82 after a full production Driver step. Its source X/Z were initialized (1167/401), physical X/Z were 1082/408, missing source carriers0, shutdown complete and borrowers0. The probe failed only its unconditional `child.Renderer != null` assertion. Current production `SimulationTickDriver` enables logic-only materialization in CentralOnly mode with a ready runtime catalog; `LF2ObjectPointFactory` then creates the entity through `LogicEntityFactory`, intentionally leaving `Renderer` null. Before the next test-only edit, record the active materialization mode and require the correct mode-specific Renderer state. Do not change production presentation mode or infer a visible-pixel pass from this logic birth. Saved Menu/Battle Scene hashes were unchanged after run13.

Controlled rerun `d024-ai-child-play-14` after the mode-aware assertion compiled with zero Console errors and PASS: mode0 selected the human, test-only Lee frame22 entered at tick80, OID706 AI-owned child appeared at slot50/tick82, source X/Z 1167/401, physical X/Z 1082/408, source initialized, missing source carriers0. CentralOnly uses logic-only materialization, so child Renderer absent is expected. Ordered shutdown complete, Menu returned, pool borrowers0. Both Scene disk SHAs remained `785F828...81E13` and `2EE465D8...B77A`; Editor restored to clean Battle. This closes only the controlled full-Driver non-sound birth/source-carrier diagnostic. Natural AI selection, child presentation pixels and formal EXE A/B remain pending, so the Change Record stays `RUNTIME_PENDING` and D-024/Q07/total alignment remain open.
