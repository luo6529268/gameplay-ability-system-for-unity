# NTSD28-Q09-OID434-CAMERA-PIXEL-001

Status: `VERIFIED_SCOPED_CONTROLLED_GPU_PIXEL` (camera acquisition/draw and source-sampled action396 pixel attribution; ordinary presented frame and formal EXE same-state pixels pending). Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001` / BATCH-05 Q09; depends on `NTSD28-Q09-OID434-CENTRAL-COMMAND-001`.

Starting evidence: original Editor physical-device tail reached OID434/action396 pic36 at tick54 and published exactly one matching Entity command in a frozen CentralOnly submission. It did not prove the world Camera acquired the plan or rendered pixels. Existing Battle Scene central pixel probes use editor-only `TryAcquireSubmissionForSelfCheck`, a temporary camera RenderTexture with culling mask 0 and solid clear, `Camera.Render()` and `ReadPixels()`, restoring camera/RT state in finally.

Exact script scope: add one explicit request mode to `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07Oid434NaturalBindingProbeEditor.cs` only. Reuse the same production tick/command path, pause the logic tick at action396, verify the original world Camera accepts the current submission, render it to a temporary isolated RT, save one create-new PNG and JSON, and count non-clear pixels inside the target command projection. To support target attribution, also count pixels in the target projected bounds that lie outside every other command's conservative projected bounds; if there are none, report attribution inconclusive rather than claim a target pixel. Restore camera fields, active RT, input and driver pause; exit Play. No production renderer/input, DAT, PNG, Scene, Prefab or nonbattle edit.

Exit: original Editor import and generated Editor C# build 0 errors; one focused Play yields accepted camera lease, actual submission draw count and a readback image with bounded target attribution or a precise negative/inconclusive stage. Verify Editor exit, Console error0, scene/config SHA and Change Ledger/diff. PASS is limited to controlled `Camera.Render()` GPU readback in Unity, not ordinary on-screen frame timing, formal EXE pixel parity, whole Q09 or R17. Preserve FAIL. Rollback limited to this opt-in diagnostic extension under protected-file rules.

2026-09-25 first-result correction before next code edit: `oid434-camera-pixel-20260925-1` preserves FAIL but proves original world Camera enabled, accepted the tick54/generation52 submission, controlled Camera.Render increased submitted draws 427→436 (last draw9), and target projected bounds contained 1563 non-clear pixels. Conservative exclusive area had 878 pixels but none non-clear; source `ras.png` pic36 has only a small central opaque motif, so the exclusive-area test cannot attribute the overlapping blue pixels and must not be read as missing rendering. The PNG was visually inspected. Next diagnostic refinement records target projection and compares opaque source-colored samples at pixels not covered by later commands, while preserving first result and creating new request/result. This is diagnostic analysis only, no production pixel policy change.

2026-09-25 second-result correction: the refinement recorded target UV/projection and all competing command bounds in a new original-Editor Play result. Camera lease/draw still pass; source opaque projection analysis found 296 target samples and none outside later-command bounds. Because later command rectangles are conservative, source alpha/actual framebuffer ownership is unresolved. Both diagnostic JSONs retain their raw FAIL. Do not infer a gameplay bug or Q09 completion. The next bounded exit requires a target-distinguishable pixel or an actual later-alpha/visibility analysis with formal same-state comparison; ordinary presented frame and EXE A/B remain separate. Evidence in `artifacts/diagnostics/NTSD28-Q09-OID434-CAMERA-PIXEL-001/PROGRESS-20260925.md`.

Next bounded observer revision authorized by this Task before script edit: record frozen later-Entity VisualDataId/EffectivePic, source path and pixel rectangle from the bound catalog, plus command UV/flip. Use unique V3 request and immutable new result, then compare source alpha against the target projected source samples. If this cannot distinguish target pixels, retain `RUNTIME_PENDING` and report the exact missing evidence. No production code, DAT, images, Scene or Prefab edit.

V3 exit: original Editor emitted the extra frozen metadata; the PNG bytes exactly match V2. Source-alpha/screenshot analysis found 151 exact target RGB matches among 213 target samples outside later sampled opacity/Shadow rectangle. This verifies the scoped controlled GPU pixel exit while retaining all three raw diagnostic FAIL statuses and not claiming normal presented-frame or formal EXE parity. See `PIXEL-ALPHA-20260925.md`. Q09/R17 and the parent goal remain open.
