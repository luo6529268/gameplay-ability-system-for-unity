# NTSD28-USER-HITFA5-TARGET-VELOCITY-001

Status `IN_PROGRESS / EQUAL_START_FOCUSED_PASS / TARGET_HISTORY_PENDING`. Parent D-024 all-entity motion audit; Q07 remains paused.

2026-09-24 next control: the dynamic one-move Unity integer gap is 224 while its formal-history gap is 199. A separately spawned target at raw initial gap 224 has the same Unity integer gap but formal gap 224. The source integer rule requires velocity 3 for the moved history and 4 for the static history. Add a single configured static-gap 224 control to the declared focused test file and verify with original Editor. This tests whether any memoryless mapping of current integer gap alone could satisfy both; no production change is authorized by this diagnostic alone.

Control exit: original Editor focused job `d248af5583144fa4822db084fa43cee3` 11/11 PASS after explicit current-integer-gap assertions. Both static-gap224 and moved-history-gap224 are reached in the registered World and produce current Unity Vx4; the playable source rule requires respectively4 and3. The diagnostic establishes that the producer needs source-coordinate history or an equivalent two-domain contract. Production remains unchanged; complete Driver/Play, formal EXE view, snapshot/reset/checksum and the other D-024 position writers remain open.

The next implementation boundary is written in `artifacts/diagnostics/NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001/COORDINATE-HISTORY-CONTRACT-DESIGN.md`. It requires an exact writer ownership table and a bounded cross-module Task/Change before any new runtime state is introduced; the current diagnostic does not authorize a local behavior5-only multiplier.

2026-09-24 follow-up: equal-start birth/one-step remains 8/8 GREEN, but that does not establish a target that moved earlier through the D-024 scaled position outlet. Static source arithmetic predicts a 3-versus-4 child velocity first difference after one formal-48px target move from an initial 151px gap, and 4-versus-5 after two; see `TARGET-HISTORY-VELOCITY-FIRST-DIFFERENCE.md` in the D-024 diagnostics folder. Before any further production change, extend the declared Editor test script to construct the target history with `CharacterMechanics.StepBattleLogic`, birth the indexed OID219 child and record formal/Unity velocity and screen fraction. Retain original equal-start controls. A blanket inverse factor is invalid at birth with no target movement; the final fix needs an explicit coordinate-history contract.

Focused diagnostic exit: original Editor job `92b7728e66144755aafa43f9b4e8deb0` 10/10 PASS, including two registered-World target-history characterization cases (one move: formal/current child Vx 3/4; two moves: 4/5). The passing diagnostic proves the current first difference, not parity. No additional production change was made. General coordinate-history implementation, full Driver/Play, formal EXE result and attachment/bounds effects remain open; Q07 remains paused.

Authority: formal indexed OID219 `w/e.dat` frame51 has `hit_Fa: 5`; paired playable `NativeAi28::step_non_character_hit_fa` behavior5 spawns one OID219 child per living same-group character and writes `motion.x = double((target.position.x - child.position.x) / 50)`. Both operands are integer positions, so division truncates toward zero before converting to double. Unity `LF2Entity.RunHitFa5FrameLogic` currently divides by `50.0`, leaving fractions. The formal source is in the playable build closure described by `source/README_SOURCE.md`.

Declared scripts: `Assets/NTSD/Scripts/Animation/LF2Objects/LF2Entity.cs` only behavior5 child directVx formula; `Assets/NTSD/Scripts/Test/Editor/NTSD28FixedViewRunRatioEditorTests.cs` actual registered World OID219 child birth and one non-character motion step. Preserve target scan, slot/action/owner, raw DAT, no user camera change, and no extra inverse view scaling of this velocity. Test source-equivalent +151/-151 integer gaps under default/configured view: birth velocity ±3, and following X screen fraction `abs(3*Sx)/2048 = 3/1333`. This is a non-perceptual formal integer arithmetic correction plus a D-024 double-scaling check, not a character-specific rule.

Acceptance: test-first original Editor RED, exact GREEN, adjacent existing ratio tests, Ledger/diff. Full Battle Scene Play and formal EXE visible certificate remain open. The separate hit_Fa6 producer is unindexed in current formal DAT and will be classified separately.

Rollback: reviewed inverse of these exact script changes, preserving all other current work.

Exit evidence (2026-09-24): original Editor RED job `f7a183006eaa465fbda93743ce395277` failed all four new cases at ±3.02 versus formal ±3. Production fix changes only the behavior5 child velocity arithmetic to integer `/ 50`, preserving raw inputs and shared final X integration. After source-DAT-loaded test strengthening, original Editor job `467b1d07c3044bd6a596bca3f8cb7a8d` passed the full focused class 8/8. It loaded formal indexed OID219 frame51 from staged `w/e.dat`, created an actual World child and stepped production non-character mechanics once; source/staged DAT SHA-256 matched. Configured view movement occupied `3/1333` of width without producer inverse scaling. Dynamic target histories, full Driver tick, Battle Scene Play and formal EXE screen evidence remain unverified. Hit_Fa6 has no current indexed DAT producer and remains separately classified.
