# NTSD28-USER-SOURCE-COORDINATE-JSON-CHECKSUM-001

Status: `FOCUSED_TEST_PASS / DIRECT_EXTENDED_SELFCHECK_PASS / CARRIER_NOT_ACTIVE`. Parent D-024. Independent review of the versioned source-rule state package found that `BattleParitySnapshotModule.CaptureExtendedChecksumSnapshot` and `CaptureLockstepChecksumSnapshot` used `ProjectExtendedRuntimeSlots`/`ProjectEntityRuntime` without the nine source-rule carrier fields. The 64-bit `CaptureRuntimeChecksum64` is a separate checksum path. This package has now added source-only sensitivity to the JSON projection used by `InProcessBattleKernelHost`; gameplay consumers remain unconnected.

Exact script scope: `Assets/NTSD/Scripts/Simulation/Lockstep/Checksum/BattleParitySnapshot.cs`, `Assets/NTSD/Scripts/Test/Editor/BattleLockstepChecksumEditorTests.cs`, and the existing extended-checksum schema assertion in `Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs`. This Task must not edit storage, DAT, Scene, camera, ProjectSettings, nonbattle, or frozen authority trace code.

Implementation contract: add all source-rule precise/int X/Z, initialized marker and four directional flags to the Extended and Lockstep canonical JSON slot projections, including active entity, empty raw slot, and the **separate occupied raw-slot payload** that snapshot/restore retains. Bump `BattleExtendedChecksumSnapshot.SchemaId` and `BattleLockstepChecksumSnapshot.SchemaId` from v3 to v4 and update only directly dependent self-check assertions. Keep the frozen `BattleParityFrameSnapshot.SchemaId` at `ntsd-battle-trace-v3`; `ProjectEntityRuntime` is shared, so explicitly gate source-rule projection to Extended/Lockstep rather than silently adding fields to frozen trace. Preserve presentation hit-record inclusion policy and stable deterministic ordering; do not change battle logic or source coordinate initialization.

Acceptance: focused original Editor test-first RED showing source-only differences are invisible to both JSON checksums, then GREEN for each of the nine fields in active entity, empty raw and occupied raw payload; frozen parity frame JSON/hash unchanged for the same test state. Run focused existing extended/lockstep checksum tests and directly affected SelfCheck method, then full SelfCheck only if required by the schema assertion or a cross-path failure. `Tools/Validate-ChangeLedger.ps1`, `git diff --check`, and Scene/content boundary check. This closes the JSON projection gap only; `CARRIER_NOT_ACTIVE` remains until all birth/motion/collision/stage/attachment histories and OID219/fusion reader tests are complete. Rollback only the three declared files and v4 identifiers, preserving other dirty work.
