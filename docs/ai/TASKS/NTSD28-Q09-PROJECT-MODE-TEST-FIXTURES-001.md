# NTSD28-Q09-PROJECT-MODE-TEST-FIXTURES-001

Status: FOCUSED_TEST_PASS. Parent: Q09 knockout feed and icon publication; Q07 native-KO-clock consumer verification.

Trigger and authority: the original Editor Q09 row test fails in SetUp because it reads `Assets/NTSD/Content/LoganRuntime/decoded_dat/data/mode/ntsd.dat`, which the user excluded from Unity production. The existing `ProjectBattleModeConfig.asset` is the approved mode replacement; `LoganModeKnockoutFeedInput.FromProjectSnapshot` and `LoganVisualContentCandidate.Capture(source, snapshot)` already implement its production consumption. Two more Q09 Editor tests still read/copy original mode DAT. No excluded DAT will be restored.

Scope: update only the three Q09 Editor test files `NTSD28Q09KnockoutFeedRowSnapshotEditorTests.cs`, `NTSD28Q09KilltextModeInputEditorTests.cs`, and `NTSD28Q09KillIconPublicationEditorTests.cs`. Load the existing project mode Asset and capture its immutable snapshot for normal row/input/icon assertions. The excluded-victim variant must use a transient in-memory config and release it; it must never mutate the saved Asset. The icon fixture copies only selected PNGs, passing the project snapshot through the existing candidate API. Preserve separate synthetic parser grammar tests. Do not edit production, mode Asset, formal DAT, Scene, nonbattle logic or other Q09 parallel dirty work.

Focused-run correction: the original Editor compiled with 0 errors and selected 9/9 tests; 8 passed and the icon publication case failed before its icon assertions because its inherited WORDS fixture already includes `resource.dat` but lacks the matching `system.dat` required by the existing SPARK candidate. Within the same icon test file, copy the formal `system.dat` and `SPARK.png` into this transient fixture so the whole candidate can be captured. These files are existing, non-excluded battle inputs; do not restore either excluded mode DAT.

Acceptance: pre-register Task/Change/Ledger/STATE/handoff/alignment before script edits. Verify source has no project mode DAT path/copy in these three files. Original Editor compile 0 errors and only the affected Q09 tests with nonzero selected count and actual results; check the 30-tick KO row boundary, all seven icon aliases and failed-publication retention. Menu/Battle/GameConfig/ProjectBattleModeConfig disk hashes unchanged, `git diff --check`, Change Ledger validator. If test behavior reveals a production first difference, report it separately rather than weakening expectations.

Risk/rollback: the existing row/image expectations were written for old DAT paths; the Asset uses normalized `/` paths and may intentionally differ. Snapshot fields and selected icon bytes must be checked before adjusting assertions. Rollback only exact test-file hunks after reviewing this package; preserve pre-existing work and all excluded-resource decisions.

Result: original-project Editor compiled the changed test scripts with zero Console errors. The first 9-case run exposed the transient SPARK fixture omission; after the scoped correction the icon test passed 1/1, then the complete affected set passed 9/9 (row 5, mode input 3, icon 1). Source search found no excluded mode DAT paths in the three tests. Menu/Battle/GameConfig/ProjectBattleModeConfig SHA-256 stayed unchanged. This closes only the test-fixture migration; it does not close Q09 presentation or Q07 cross-runtime pixels.
