# NTSD28-Q07-FORMAL-DDJ-PROBE-001

Status: `VERIFIED / ORIGINAL_EDITOR_NATURAL_PLAY_PASS`; same-input formal EXE parity and Q07 aggregate remain open. Parent: BATCH-04/Q07; this Task is a diagnostic probe correction and formal Naruto DDJ chain witness, not a production battle-rule change.

Authority: the root formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, corresponding playable `InputRouter28::route_combo_fields`/`apply_action`, the formal indexed `c/nar/nar.dat`, and current project exception boundaries. Formal frame110 requests `hit_Dj:271`; frame271 `state:1150272`, `mp:350` redirects the action to 272 at HP500 while retaining the 271 cost. Frame272 is zero-wait and its next is 495; frame495 emits OID518/action708. The old SelfCheck's OID205/frame272 assertion is historical and detached from `RunAll`.

Original-Editor RED: running the existing physical L/S/K `BattleComboPlayModeProbeEditor` request in the saved Battle Scene yielded `FAIL` because it waited for requested frame271. Its trace actually recorded combo steps at tick626/628, frame495 at tick630, then frames273/497/274 and object count 4→21. The request was consumed, original Play exited, Menu/Battle Scene hashes remained unchanged. Raw result and SHA are at `artifacts/diagnostics/NTSD28-Q07-FORMAL-DDJ-PROBE-001/original-editor-legacy-probe-red-20260925.json` and the Change Record.

Exact code scope after pre-change Record: `Assets/NTSD/Scripts/Test/Editor/BattleComboPlayModeProbeEditor.cs` only. Correct the DownJump probe so it distinguishes requested DAT action from the native resolved action and does not require a zero-wait intermediate frame to survive an entire Driver tick; add the minimum fields to capture the resolved action and OID518 child count. Preserve ForwardAttack, Sasuke, clone and physical InputSystem routes. Do not change production input, battle simulation, DAT, Scene, asset, project settings, UI or nonbattle flow. No computer-use or second Unity project.

Exit: original Editor compiles with zero errors; the same physical L/S/K path in the original saved Battle Scene records the formal source fingerprint, requested271/resolved272, source PP cost350, current full-tick frame sequence and OID518 birth, with an honest terminal result and no test-only false failure. Compare the same input/tick against formal playable/EXE when a matching LFR/trace is available; until then, mark EXE parity pending, and do not claim Q07 or the old six-clone chain is complete. Confirm Editor exits Play, Scene SHA stable, and Change Ledger validator passes. Rollback only this diagnostic script's hunk under repository approval rules; retain all user files and the RED artifact.

Exit evidence: final original-Editor `formal-ddj-play-3.json` PASS, SHA-256 `E15F20B5EE708E89147E2934AB8DFCB19A668BDB9631825BEE735952BFB63615`. Physical L/S/K steps tick2/4/6; requested271, runtime resolved action272, completed frame495, PP500→150 and first OID518 at tick16. The first two corrected-probe preflight FAIL artifacts are retained: the sole issue was a stale hardcoded semantic fingerprint, while the runtime published identity/root and exact Naruto authored frames matched. Editor exited Play, both saved Scene hashes stayed fixed, ledger validator and diff check passed. This closes only this probe correction and natural Unity witness; no formal EXE trace was run for this input.
