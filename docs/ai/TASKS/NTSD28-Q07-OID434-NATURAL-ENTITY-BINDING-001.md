# NTSD28-Q07-OID434-NATURAL-ENTITY-BINDING-001

Status: `RUNTIME_PENDING / AFTER254_BRANCH_RULED_OUT`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001` / BATCH-04 Q07, with later Q09 presentation follow-up. The diagnostic was completed; action396 entity binding remains pending on a reachable continuation.

Authority and starting evidence: formal root EXE SHA `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable frame/render path, formal `c/nar/a/ras.dat` frame396 `pic:36`, formal/staged `ras.png` same SHA `21A69B52A561CA4773FFC8FDBFE0E5B23E43B6B0F966EAF76E13888C8E23FD1A`. Existing controlled native and Unity raw traces have OID434/slot52/action396 at tick29, and separate original-Editor formal-root Play has published catalog `(434,36)` with valid central binding. The saved Battle Scene physical-input `NaturalProbe` after254 route already runs through the relevant window but does not record OID434 entity binding.

Exact new-code scope: one opt-in request-driven Editor observer in `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07Oid434NaturalBindingProbeEditor.cs` and `.meta`. It enters the original saved Battle Scene, waits for live formal Naruto, calls the existing `NTSD28UserRasenganPhysicalPlayProbeEditor.RunNaturalAfter254FromMenu()` once, observes actual production World entities after each controlled tick, and captures any OID434/action396 with `GetRenderPicIndex()` and `TryResolveCurrentSpriteEntry()` plus source sheet, key, 48×48 crop and central binding. It must not alter the existing natural probe, production input/Driver, DAT, PNG, Scene, menu or nonbattle code; no extra singleton/Scene object. Keep a unique create-new request/result and allow the original natural probe to finish before ending Play. Preserve any FAIL result.

Exit: generated Editor build includes the new script with 0 errors, original Editor imports with Console error0, one focused unique Play yields either a matching natural OID434 binding or an explicit first missing condition. Compare with published `(434,36)` and exact formal image SHA, then verify Editor exits Play, clean Scene, Battle/Menu/GameConfig hashes and Change Ledger/diff. A PASS covers only natural entity-to-catalog binding; it is not a renderer-command, GPU-pixel, human-keyboard, whole-skill or Q07 aggregate certificate. Rollback limited to new Editor diagnostic/request/result subject to protected-file rules.

2026-09-25 diagnostic correction: the first after254 run produced a valid natural-input PASS but did not observe OID434/action396. Its actor trace takes `254 -> 0` at tick41, whereas the controlled replay that contained action396 followed a different authored continuation. The observer's `firstTickGap=52` was counted after the natural probe had restored LocalFreeRun, so that field is not evidence of an in-probe gap. Preserve the FAIL result, refine the observer to report any OID434 frame and stop gap accounting when the natural result is written, and run one corrected scoped diagnostic. Do not claim action396 natural reachability from this branch.
