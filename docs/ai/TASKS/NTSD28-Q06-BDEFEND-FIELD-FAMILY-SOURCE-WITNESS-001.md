> VERIFIED / SOURCE_MODEL_ONLY；256/1280/两遍一致，分支148/96/6/6。下一BDEFEND-FIELD-FAMILY-UNITY-001，源详细边界见同ID artifact REPORT。

# Bdefend命中与恢复原见证

IN_PROGRESS / SOURCE_ONLY，准确单CPP见同ID Record。服务UNARMORED-BDEFEND-WRITER-AUDIT-001；该字段同时由无护甲覆写、减伤分支累积、护甲激活读取和C25h恢复，不能只修原full-driver的一条45。

原字段+0x0B8和Runtime.Bdefend绑定已确认；旧HitStateCount在C25h无authority，兼容API保持。新source结果后声明Unity字段家族的准确writer/reader/Shadow范围，先测试再改。
