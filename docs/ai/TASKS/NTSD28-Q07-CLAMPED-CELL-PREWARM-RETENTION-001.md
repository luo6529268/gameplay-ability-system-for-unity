# NTSD28-Q07-CLAMPED-CELL-PREWARM-RETENTION-001

Status: VERIFIED_SCOPED_RETENTION_BOUND. Parent BATCH-04/Q07 and `NTSD28-Q07-NATIVE-CLAMPED-CELL-PUBLICATION-001` remain open.

Authority and problem: formal DAT/PNG bytes, paired playable point/CLAMP semantics, and the current Unity generic derived-cell adapter remain fixed. A fresh read-only sheet scan found 31 sheets with 1,212 out-of-image declared cells and 48,533,904 RGBA bytes (46.286 MiB) of per-cell data before dedup. In `CharacterAnimtorManager.ProcessAndCreateSpritesForCandidateAsync`, the per-sheet dictionary currently retains every cell array across `await uploadSemaphore.WaitAsync()`; `cpuSemaphore` is released before that await, so its concurrency limit does not bound the queue's retained cell arrays. The actual Unity allocation peak has not been measured; 46.286 MiB is a source-data retention capacity, not an observed peak.

Exact write scope: only `Assets/NTSD/Scripts/Animation/Manager/CharacterAnimtorManager.cs` in the existing formal-PNG clamped-cell branch; this Task/Change/Ledger/STATE/handoff/alignment and diagnostics. Store each out-of-bounds cell's existing dimensions/RGBA hash before the upload await, then reconstruct pixels only when the serialized upload encounters a hash absent from the existing content-dedup dictionary. Do not alter the normal cell path, hash function, texture/Sprite/atlas source identity, publication order, gameplay pass, DAT/PNG, Scene, UI, Menu, audio or project settings. No per-OID exception.

Acceptance: source/static model demonstrates no per-cell Color32 array retained across upload await; original Editor compile zero error; the existing focused native clamp/atlas tests pass; one original Battle Scene formal-root catalog Play repeats 1,212 derived entries, 36 unique sources/textures, zero invalid bindings; one controlled OID32/frame95 camera Play retains the actual white sprite and post-exit resource count zero. Scene hashes and dirty state stable, Change Ledger/diff checks pass. Actual process high-water remains pending unless separately measured; this package only bounds the newly added queued-cell retention by construction. Preserve the current dirty worktree; rollback only this exact code delta under repository approval rules.
