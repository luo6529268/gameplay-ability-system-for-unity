# NTSD28-USER-SOURCE-COORDINATE-WEAPON-FRAGMENTS-001

Status: FOCUSED_TEST_PASS / CARRIER_NOT_ACTIVE / LEDGER_VALIDATED_AFTER_DOC_RECOVERY. Parent D-024. This batch covers the active built-in and DAT `<weapon_piece>` fragment births in `BattleNativeWeaponPieceWriter.Materialize` and its private `Spawn` task builder, not every weapon movement path.

Authority: paired formal playable `BattleWorld28::materialize_weapon_piece_fragments` (`battle_world.cpp` around 3173-3523) uses source integer X/Z plus the already drawn raw random X/Z: built-in OID999 fragments use source X+randomX, source Z; DAT-table fragments use source X+randomX, source Z+randomZ. `spawn_at` sets matching precise/integer values. Current Unity physical battle task applies approved view scaling to these offsets, but lacks an independent source-rule child position. User forbids editing formal DAT values.

Exact script scope: `Assets/NTSD/Scripts/Simulation/Ecs/Writers/BattleNativeWeaponPieceWriter.cs` and `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06NativeWeaponPieceEditorTests.cs`. No other script, DAT, image, Scene, camera or ProjectSettings edit belongs to this package.

Implementation: reuse the existing raw dx/dz random values and parent source integer X/Z to pass explicit source-rule precise and initial integer X/Z into the pooled OPoint task for both built-in and DAT fragment call sites when parent source is initialized. Child source remains absent otherwise. Preserve existing physical battle x/z, independent battle integer rounding, Y, velocity, action, slot, team, RNG order/count, admission and task recycling. Do not infer raw history by dividing scaled battle coordinates or activate source-rule gameplay consumers.

Acceptance: test-first RED/GREEN using the existing formal synthetic witness cases for built-in row0 and DAT-table row93; both factor1 and configured 2048×1152 views, initialized and absent-source controls. Assert source child precise/int, physical battle precise/int, formal RNG state and source parent unchanged. Run original Editor compile and narrow cases, ledger validator, diff check and Scene/content boundary check. No full suite unless a new failure demands it. Rollback only the declared two scripts, preserving unrelated dirty work.

Execution 2026-09-24: RED 0b6f55ec08564b2a81d7e20ddff752e4: eight cases executed, four absent controls pass, four initialized source cases fail at child source marker. GREEN 8be060d749f6417f8080cadbc67ae041: 12/12 PASS (new eight and existing ratio four), original Editor fresh compilation; Console errors zero. Child source precise/int, physical rounding, exact RNG calls/final state and parent coordinate preservation pass. Diff/Scene/content checks pass. Global ledger validator FAILED: unrelated BATTLE-CENTRAL-EDITOR-FOOT-MARKER-PREVIEW-001 and BATTLE-CENTRAL-EDITOR-PREVIEW-001 absent from STATE. STATE and Handoff were observed to contain extensive NUL bytes before this package updated either; left untouched for root recovery. No full SelfCheck or Play; carrier inactive.

2026-09-24 correction: the NUL corruption was caused by the root agent's preceding byte-prepend operation on STATE.md and handoff, not by an external actor. Corrupt bytes were backed up, both documents were recovered from intact 03:18 Git object snapshots, subsequent package state was reconstructed from Task/Change Records, and `Tools/Validate-ChangeLedger.ps1` returned exit 0 (`Temp/NTSD28-D024-RECOVERED-DOCS-ledger.log`). The earlier failed-validator observation remains recorded as a historical event; it is no longer the current gate.
