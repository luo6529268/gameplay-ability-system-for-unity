# NTSD28-USER-SOURCE-COORDINATE-FRAME-MOTION-001

Current status 2026-09-24: `FOCUSED_TEST_PASS / CARRIER_NOT_ACTIVE / PLAY_PENDING`. Original-project Editor exact job `1abe66cd561d4338a08c539a9263b414` ran 14/14 targeted frame/platform cases, and adjacent full-tick/replay job `19be14efbec74ea08df688f4840d5407` ran 3/3. Historical `CODE_WRITTEN / COMPILE_PENDING` line below predates these results. Source physics/teleport/bounds/flags and formal EXE/Play remain open.

Status: `CODE_WRITTEN / COMPILE_PENDING / CARRIER_NOT_ACTIVE`. Parent: `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (D-024). This is one source-rule coordinate writer package; source-rule gameplay readers remain inactive and Q07 remains paused.

Authority: root formal `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, with declared playable source in the actual build closure. `SimulationTickDriver28::step` calls `BattleWorld28::apply_frame_motion` before teleport and physics. In `battle_world.cpp:1695-1792`, linked platform X/Z first reads the passenger's current integer mirror and applies unscaled raw linked-frame `dvx/dvz`, then direct frame `dx/dz` reads the *updated* integer mirror, applies facing and `DelayTimer134` 0.25 scaling, and rounds each precise result to an integer mirror. Neither stage changes velocity except the existing direct-frame zeroing; Y/collision reference is separate.

Unity pre-change: `LF2Entity.ApplyNativeFrameMotionForWorldPass` calls `ApplyLinkedPlatformMotion` and `ApplyNativeFrameMotionTail` in the formal order. Both already apply the user-approved D-024 view factor to actual battle X/Z and update battle integer mirrors. Neither updates the existing versioned `SourceRuleX/Z/Int` carrier. A parent moved by these paths therefore gives its later source-domain child/AI/fusion readers a stale birth coordinate. `RoundPlatformCoordinate` uses the current midpoint-to-even policy and is the existing formal-adapter mirror owner.

Declared script scope: `Assets/NTSD/Scripts/Animation/LF2Objects/LF2Entity.cs` two methods only; `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06FrameMotionTailEditorTests.cs` targeted added cases. On initialized source positions, apply *unscaled* formal raw X/Z displacements from source integer mirrors with the same immediate rounding and sequence as the physical writer. Leave uninitialized source carriers untouched; do not derive source from battle, draw RNG, edit DAT, alter camera/Scene/ProjectSettings/nonbattle code, or touch Y/floor. Do not switch OID219/fusion/ordinary revival readers yet: source physics, teleport, bounds, relation and kind-14 flag history remain incomplete.

Acceptance: factor1 and configured-view cases for direct `dx/dz`, facing, delay 0.25, linked-platform X/Z and compound platform→direct sequence, initialized/uninitialized carrier and unchanged battle output/velocity. Use the original Editor only after existing broad job `2ed78c0b98214a5ba14ec92250d3b9f8` is terminal and C: space is restored; compile plus focused NUnit for the new methods, existing adjacent frame-motion tests, ledger validator, diff and Scene/DAT guards. Formal EXE same-input and Battle Scene Play remain separate unverified levels.

Rollback: reviewed patch of only the source-rule writes and added focused tests in these two files. The generated Editor.log, all user work and unrelated dirty files are outside this package.
