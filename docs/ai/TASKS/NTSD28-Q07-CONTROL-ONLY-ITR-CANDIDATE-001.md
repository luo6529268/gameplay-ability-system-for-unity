# NTSD28-Q07-CONTROL-ONLY-ITR-CANDIDATE-001

2026-09-25 adjacent regression correction: original Editor exact filter `NTSD.Test.RoleAwareCollisionShadowSelfCheckTests.CollisionRoleZeroItrFastPath_DegenerateAuthoredItrUsesConservativeIndexAndMatchesOriginalCollector` selected and passed 1/1 (job `97c1acddbe6841d6bba3cda642bd9e2c`). The earlier 0/0 filters used the wrong namespace; they are not regression evidence. Raw response SHA-256 `2FB3C90D3B07DC96D4F4105D2F267765456C52CDC02B123E9530472F6435FC6D` is in `artifacts/diagnostics/NTSD28-Q07-CONTROL-ONLY-ITR-CANDIDATE-001/original-editor-adjacent-rolezero-20260925.json`. This closes the named adjacent legacy test only. The Q03 capture test writes an existing `unity.tsv` artifact and was not rerun; natural Play and formal EXE parity remain open. Status stays `RUNTIME_PENDING`.

Status: `RUNTIME_PENDING / FOCUSED_EDITOR_PASS`. Parent: BATCH-04 / Q07. This is a battle-candidate behavior correction, separate from the SelfCheck content-source migration in `NTSD28-Q07-SELFCHECK-FORMAL-GEOMETRY-001`.

Adjacent fixture follow-up before script edit: `BattleRuntimeSelfCheck` has complete hand-authored ITRs whose `hasGeometry` default is false. The new production gate could hide their geometry. This Task now includes a RED/PASS check of the actual zero-dimension and negative-height synthetic self-check methods and, if RED confirms the flag omission, minimal flag additions only in their fixture constructors. The formal-content SelfCheck source migration remains a separate Task. Scope and rollback are in the expanded Change Record.

Correction after the actual Editor run: job `a1b01f3572424268812e3f13e95a1070` passed 5/5 including both synthetic checks, so the proposed fixture edit is canceled; `BattleRuntimeSelfCheck.cs` stays untouched. The artifact filename contains `red` because it was selected before execution, but the job result is PASS. This verifies the legacy synthetic route remains valid; it does not close formal content-source migration.

2026-09-25 post-fix focused result: original Editor refreshed and ran job `f5440074a23143be87e09261bff198fd`, exactly 3/3 PASS for the two formal authored-frame cases across both collector profiles and complete-geometry control; JSON SHA-256 `6B00793F44B9D60A1EA17A9AEBBFC2A297F2077BEDCAD1685C90CC993C7F124F`. An attempted adjacent legacy test filter selected 0 tests, so adjacent regression remains unverified. Scene files remain unchanged at Menu/Battle SHA `785F828C...81E13` / `2EE465D8...77A`; natural Play and formal EXE comparison remain open.

2026-09-25 original Editor first RED: focused job `f6d099cbd511474693b3f89b7be88350` finished three tests, with the complete-geometry control PASS and Puppet5/KonanAngel ForceBruteForce candidates 1 versus native 0 (two expected failures). `original-editor-red-first-20260925.json` SHA-256 `33237CDA9ECC929D602E00D44B952376C2FCC4776E51D09D310D0D453CFCED48`. Role-aware did not run because the first assertion in each loop failed; revise the test to collect both profile counts before asserting, then retain a second RED. Production code remains untouched and no direct damage is claimed. Current Task state is `CODE_WRITTEN / FIRST_RED_CAPTURED`.

2026-09-25 second RED and bounded code: the revised original-Editor focused job `e1cbe90eed304a44966022ac051edeae` captured one candidate in **each** collector profile for both authored frames (Puppet5 ITR ordinal1, KonanAngel ordinal0); its zero expectation failed twice while the valid-geometry control passed. JSON SHA-256 `5130B26649EEDB8536EEBDEF6685FEAEED25B551A7B012E7EB98AC0DE179348F`. The predeclared production predicate now reads `itr.hasGeometry`; compile, focused PASS, Q03 adjacency and Scene safety are pending. Candidate admission is proved in the controlled Editor arrangement, direct damage/natural encounter remains unproved.

Authority: the formal root EXE SHA-256 is `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; corresponding playable/core `CollisionGeometry28::decode` marks incomplete kind-100100 ITR as `control_only`, and `HitCandidateCollector28` skips candidate creation while retaining authored ITR ordinal. The unchanged native `hit_candidates_tests` target built/ran PASS. Formal/staged DAT for OID854 Puppet5 and OID54 KonanAngel are pairwise SHA-identical. Current Unity converter carries `hasGeometry=false`, while `BruteForceSceneQuery.IsReleaseItrGeometry` checks only non-null. A compiled-assembly check confirmed this static admission gap; no original Editor candidate row or damage event has yet been proved. Evidence: `artifacts/diagnostics/NTSD28-Q07-OLD-ASSET-REFERENCE-REFRESH-001/CONTROL-ONLY-ITR-CANDIDATE-AUDIT-20260925.md`.

Exact proposed code scope: `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07ControlOnlyItrCandidateEditorTests.cs` for an authored-data RED/PASS witness, and only `Assets/NTSD/Scripts/Animation/Character/BruteForceSceneQuery.cs::IsReleaseItrGeometry` for the minimal shared geometry-presence gate if RED confirms candidate admission. New test `.meta` is included. No DAT token/value, Scene, Prefab, Content image, Menu, HUD, framework, physics, unrelated battle pass or generated/third-party edit.

Test before production: use the staged formal SHA-locked `c/chi/pup5.dat` frame407 and `c/kon/ang.dat` frame11 through `Lf2DatParserV2.ParseLoganContent` and `Lf2DatConverter.ConvertLoganFrameData`, then actual `SimulationWorld.CaptureCollisionFrameSnapshotsAll` and `CollectCollisionCandidatesAll`. Create opposing groups, right-facing equal-Z entities at the source-derived coordinate pairs documented in the audit. Run `ForceBruteForce` and `ForceRoleAware` with a separate world per case. Assert the authored ordinal and `hasGeometry=false`, exact candidate row/count and that the complete kind-8 entry is not accidentally removed. Add a complete zero-width kind-0 control case to preserve authored geometry semantics. Expected native control-only count is 0; if current Unity test does not produce an extra candidate, stop and investigate the actual earliest gate rather than forcing the proposed predicate edit.

Acceptance: retain initial RED output; after a justified minimal fix, original Editor compiles with 0 errors and only the focused methods plus the adjacent Q03 geometry witness pass. Verify candidate storage/sequence and both collector profiles, rather than using a predicate-only test to claim runtime alignment. Recheck formal/staged DAT SHA, Menu/Battle saved Scene hashes and scoped diff; run `Tools/Validate-ChangeLedger.ps1`. This is a collector exit only; natural encounter/visible formal EXE parity and Q07 aggregate remain open.

Rollback: restore only this Task's precise test and predicate hunk under repository approval rules, preserving all other dirty/user files and the formal DAT bytes. Do not use destructive Git commands without approval.
