# NTSD28-Q07-MENU-SCENE-CALLBACK-PLAY-001

Status: `RUNTIME_FIRST_DIFFERENCE / BUILD_SCENE_CLOSURE_MISSING`. Parent BATCH-04/Q07. Purpose: verify that the existing `NTSD_Menu.unity` callback chain reaches an additive battle with the serialized formal Logan content and exits cleanly. This uses actual Scene component callbacks; it does not claim physical pointer/keyboard, visual UX, or all-mode parity.

Authority and baseline: formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; production GameConfig `BattleContentRuntimeRoot: Assets/NTSD/Content/LoganRuntime`; `NTSD_Menu.unity` GUID binding to that asset; B11 prior prewarm/Player publications. Current Menu Scene SHA-256 `6CF124A17F692325CED5774C3C10C3324AA047439BC6C3EB08856188A78AB4B1`, Battle Scene SHA-256 `BCD1047BF912C6A4A8BC9F3A76EAF3FA954211AD064E0402B1C01BF3BA0E9FB6`. Preserve all dirty user work.

Exact code scope: new `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07MenuSceneCallbackPlayProbeEditor.cs` and its Unity `.meta` only. Request/result under `Temp/NTSD28_Q07_MenuSceneCallback.request.json` and `artifacts/diagnostics/NTSD28-Q07-MENU-SCENE-CALLBACK-PLAY-001/`. Existing UI/App/Scene/prefab/asset and nonbattle scripts are read-only. The Editor test must refuse an unsaved active Scene, unique-output collision or wrong starting Scene, open the existing Menu Scene in-memory, enter Play, use the actual Scene `MainMenuController` button callback, loading controller, mode option, role slot, CMC and settings option callback; no direct `InitializeBattleAsync` invocation. It must restore the prior Battle Scene after Play. The request is marked false, never deleted; previous result files remain untouched.

Acceptance: report callback phases and selected Naruto OID2; verify formal content identity and three published owners, real `NTSD_Battle` additive load, `AppManager.State=BattleRunning`, World actor and chosen MatchConfig; unload through `AppManager.UnloadBattle`, confirm ordered stopped/zero World/borrowers and Menu return, then restore Editor scene and compare both Scene file hashes. If a first difference appears, preserve FAIL report and do not modify UI/Scene to force a pass. Narrow compile and this one Play path only. No computer-use.

Risk/rollback: Editor Scene switching may affect unsaved user state, hence preflight dirty guard and saved Scene asset hashes. UI callback invocation is not a substitute for a physical input or rendered UX test; such checks stay with Q12/visual return. An incomplete Play run must leave request/result evidence and report the Editor/Scene state; never reset or clean user files.

Written: the declared Editor test script and `.meta` are present. It uses existing Scene component callbacks and no production UI or battle script. Compile and actual Play remain pending.

Current result: `artifacts/diagnostics/NTSD28-Q07-MENU-SCENE-CALLBACK-PLAY-001/RESULT.md` records two actual Play attempts and a fast negative preflight. The second attempt passed formal prewarm, VS, Naruto role, CMC and Fight callbacks, then `SceneManager.LoadSceneAsync("NTSD_Battle", Additive)` failed because `ProjectSettings/EditorBuildSettings.asset` has `m_Scenes: []`. No battle or ordered-exit acceptance follows. The probe now reports this dependency before opening Menu. Resume after an authorized, explicit production Scene closure is established; then rerun the same path. Do not edit Menu UI/App code to bypass this.
