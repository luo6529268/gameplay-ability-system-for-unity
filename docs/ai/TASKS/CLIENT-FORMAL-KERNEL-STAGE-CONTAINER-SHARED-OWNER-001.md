# Task Contract — CLIENT-FORMAL-KERNEL-STAGE-CONTAINER-SHARED-OWNER-001

> Status: `FOCUSED_TEST_PASS / SHARED_STAGE_CONTAINER_OWNER_READY / GOVERNANCE_CLOSED / CLIENT_INTEGRATION_REQUIRED / USER_STANDING_AUTHORIZED / UNITY_COMPILE_0 / PACKAGE_8_8 / STAGE_RELATED_11_11 / S0_LOCKSTEP_24_24 / SELFCHECK_PASS / SERVER_DUAL_CONFIGURATION_PASS / FORMAL_MARKER_FALSE / S0_NOT_VERIFIED`

The exact scope and acceptance are owned by the Server same-ID Task/Change.
Client action is limited to removing the old immutable source/meta after the
same source/GUID exists in Server Core, updating the existing stage-container
seam test's assembly-owner assertion from `Assembly-CSharp` to
`NTSD.Battle.Kernel`, and running the declared regressions. Runtime behavior
remains frozen.

Close evidence is owned by the Server same-ID Task/Change: Unity package
`8/8`, stage-related `11/11`, S0/lockstep `24/24`, fresh SelfCheck, direct and
locked .NET 0.8.0 consumers, and Server Debug/Release all passed. The former
Client source path is absent and the source/GUID is single-owned by Server Core.
