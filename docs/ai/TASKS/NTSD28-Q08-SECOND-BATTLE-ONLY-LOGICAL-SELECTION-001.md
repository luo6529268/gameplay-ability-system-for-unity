# NTSD28-Q08-SECOND-BATTLE-ONLY-LOGICAL-SELECTION-001

Status: `RUNTIME_PENDING / FORMAL_CONTENT_SCENE_SCRIPTED_SECOND_RESULT_PASS`. Parent BATCH-04/Q08. Formal authority: root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, playable `GameSession28::step` second result and `SECOND-CYCLE-SOURCE-WITNESS.md` (transition2 at timer350, following selection transition1, same World). Unity temporary-host target RED `UNITY-SECOND-BATTLE-PROXY-RED.xml`: same World/frozen tick passed, transition expected1 observed2. After the production change, the same proxy passed 1/1 and adjacent ordinary selection passed 3/3; exact evidence is in `SECOND-BATTLE-LOGICAL-SELECTION-ACCEPTANCE-PENDING.md`. Two earlier formal-content Scene attempts OOM while loading images; later targeted Scene acceptance is recorded in the correction below, while natural battle acceptance remains pending.

Exact governed script paths: `Assets/NTSD/Scripts/Simulation/Host/SimulationTickDriver.cs`, `Assets/NTSD/Scripts/Test/BattleTestBootstrap.cs`, `Assets/NTSD/Scripts/Test/Editor/NTSD28Q08SecondBattleOnlyResultSelectionRedPlayModeTests.cs`. No AppManager, nonbattle Menu/selection UI, Scene, Build Settings, DAT/image, GAS or J: authority edit. Preserve existing first-rematch and ordinary routing changes and dirty worktree.

Add an explicit second-result consumer to the registered direct owner. It must accept state2 only after the one successful first rematch, keep the same World and entity state, advance the logical transition to1 once and let existing tick-admission freeze combat. It must not create a second World or synthesize selection UI. Unregistered ordinary Menu+Battle results still use the existing AppManager route. Run only the targeted no-image temporary-host test to convert RED→PASS, adjacent ordinary result class and first-rematch focused logic as resources allow. The formal-content Battle Scene second-cycle test remains pending while the repeated OOM condition persists; no broad success claim from the proxy. Record compile/test/Scene hashes, validator and diff check. Rollback only declared changes after reviewed diff under repository rules.

Later evidence correction (2026-09-22): the subsequent atlas-budget source-binding fix removed the prior OOM for this targeted path. The same formal-content Battle Scene `SecondResultEntersOrdinaryUpperSelectionInSameWorld` test passed 1/1, process exit 0, in `UNITY-SECOND-BATTLE-SOURCE-BUDGET.xml` (SHA-256 `E70E58EAA8D64150B314F843E8320F97D3CAD5CB3F5317A9A552B692D7692B77`). Its two result boundaries are scripted; natural KO, original-project Editor validation and formal EXE visible parity remain pending. This correction supersedes earlier OOM-pending statements only for this scripted test.
