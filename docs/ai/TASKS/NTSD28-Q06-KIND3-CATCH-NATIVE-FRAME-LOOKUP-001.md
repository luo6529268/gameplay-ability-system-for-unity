> 当前VERIFIED / KIND3_PAIR_ENTRY_ONLY，证据见同名Record。后继CPoint/throw/pickup不在已关闭范围。

# NTSD28-Q06-KIND3-CATCH-NATIVE-FRAME-LOOKUP-001

IN_PROGRESS / SOURCE_FIRST。精确范围、原caller和出口见同名Record。源/Unity读取准入及成对写入闭合后回NATIVE-FRAME-RUNTIME-READER-MIGRATION，不整体替换共享helper或重做已验C25。
