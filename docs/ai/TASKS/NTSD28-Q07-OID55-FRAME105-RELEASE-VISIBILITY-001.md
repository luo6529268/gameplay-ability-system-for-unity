# NTSD28-Q07-OID55-FRAME105-RELEASE-VISIBILITY-001

Status: `VERIFIED_SCOPED_SOURCE_AND_RELEASE_TRACE / PIXEL_PENDING`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001` / BATCH-04 Q07. Trigger: the all-frame static census found `c/saso/pup.dat` frame105/pic44 whose source rectangle is wholly beyond formal `pup.png` height; authored frame104 has `next:105`.

Scope: add only `Tools/NTSD28Q07Diagnostics/pup_frame105_lfr_probe.cpp`, a diagnostic using the paired playable/core source build closure, formal read-only `decoded_dat`/VFS, OID55 type0 at explicit action103 and a distant OID7 opponent. Use no input for a bounded set of ticks. Record source tick/action/base pic/slot0 sprite command presence and an LFR; replay that LFR through the root formal EXE with both formal resource roots and `--lfr-slot0-action 103`, save create-new report/trace, and compare action/pic/render sprite-count where the schemas permit. Check the formal EXE hash before and after. This is a controlled starting-frame witness, not proof that a menu-selected Sasori naturally enters action103.

Exit: source helper compile 0 errors, formal EXE playback exit0/PASS, exact first difference or match for recorded fields, explicit sprite visibility at frame105 from source snapshot, preserved raw artifacts and SHA, no formal resource or Unity production/Scene/DAT/image edit. If runtime cannot reach frame105 under this starting condition, record the first divergence and do not claim a visual defect. Unity parity is a later separate exit. Rollback is removal of the newly added diagnostic C++ source only under repository protected-file rules; existing artifacts remain evidence.

Result: controlled source/release trace reached action105 ticks5–7 and compared 36/36 fields without difference. The paired source emitted one actor sprite command per such tick; root EXE trace exposes only aggregate sprite count, not target pixels. Formal texture clamp samples a fully transparent edge row for this file, so no visual defect is claimed. Full report and SHA under the Task-named artifact directory; natural input and Unity GPU remain separate.
