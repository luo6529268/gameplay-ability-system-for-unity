# NTSD28-Q09-FORMAL-PLATFORM-SHADOW-CAMERA-WITNESS-001

Status: `RUNTIME_PENDING / SCOPED_CAMERA_CAPTURE_COMPLETE / PIXEL_OWNERSHIP_UNPROVEN`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001`, BATCH-05/Q09. Entry: `NTSD28-Q09-FORMAL-PLATFORM-DRIVER-PLAY-001` proved formal OID56 frame130 links OID2 through one full Driver tick, writes `RenderShadowOffset10C=-5`, and publishes one target Central Shadow command. This Task tested the remaining Unity camera-pixel seam; Q09 and the parent stay open. Result: `artifacts/diagnostics/NTSD28-Q09-FORMAL-PLATFORM-SHADOW-CAMERA-WITNESS-001/ACCEPTANCE-20260926.md`.

Authority: formal root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable `battle_world.cpp` operation30 and `render_snapshot.cpp` shadow position Z+10C, plus formal indexed `c/hid/rea.dat`. Preserve user-owned fixed camera/background and motion-ratio exceptions. The probe cannot substitute a root EXE same-condition graphics comparison.

Exact code scope: extend only `Assets/NTSD/Scripts/Test/Editor/NTSD28Q09FormalPlatformDriverPlayProbeEditor.cs`. Add an optional request flag, a baseline camera capture before fixtures, a post-tick capture after the real CentralOnly submission, projected target Shadow bounds, and conservative pixel ownership counts. The existing request path and default request behavior must remain valid. Do not modify production scripts, DAT tokens, PNG, Scene, Prefab, ProjectSettings, mode, input, other tests, shutdown order or nonbattle modules.

Acceptance: in the original saved Battle Scene and original Editor, compile with zero C# errors; execute one unique opt-in Play request. The report must retain full-tick carrier/command and cleanup checks, two PNG captures, the camera state before/after, candidate pixel count and changed pixels inside the target Shadow command but outside every other captured command's inflated projection and white in baseline. If no uniquely attributable pixels exist, state `PIXEL_OWNERSHIP_UNPROVEN` rather than pixel PASS. Inspect PNGs and Console; restore the camera, fixtures, pause and Scene hash; run `Tools/Validate-ChangeLedger.ps1` and `git diff --check`. Do not run the entire suite for this diagnostic-only change.

Risk: source/target bodies or other commands may cover the shadow, so valid logic can yield zero exclusive pixels. A forced frame and placement proves only the controlled case. Temporary RenderTexture/camera settings and fixtures must be restored in `finally`. Rollback is a reviewed exact reversal of this test-only extension and its addenda while preserving unrelated dirty work; deletion of existing files requires separate authorization.
