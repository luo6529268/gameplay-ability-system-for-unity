# NTSD28-USER-WPOINT-POSE-VIEW-WITNESS-001

Status: `FOCUSED_TEST_PASS / TEST_ONLY`. Parent: `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (D-024). Original-project Editor exact job `bdb426058e5f448da3a79d711c11d9f4` ran two cases, 2/2 PASS. At factor1 holder XInt348/held XInt359, and at configured view holder XInt373/held XInt384; both preserve the source-local X gap11 and held ZInt251. This is the non-`LF2WeaponBase` WPoint path only. It does not rewrite DAT-local `centerx/wpoint` values, scale pose anchors, or change production code.

Authority: shipped Logan playable `BattleWorld28::settle_held_refill_objects` in `source/ntsd28_core/src/simulation/battle_world.cpp` derives the held object's integer X/Z from the holder integer position and both frames' local WPoint geometry; `SimulationTickDriver28::step` calls it twice. Unity's active `SimulationQueryAndLinkModule.HeldObjectProcessAll` routes the fixture's non-weapon-component child to `BattleHeldObjectWriter.SyncHeldFrameAndPosition`, which uses the same holder integer + local anchor shape. The user's D-024 exception scales actual battle travel under the fixed full-background camera, not local sprite attachment geometry.

Declared script path: only `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06HeldNativeFrameBindingEditorTests.cs`, adding one parameterized test. Reuse its existing `source/first.jsonl` row index 1 and private registered World construction, without modifying fixture bytes. The row's source before holder/held X are300/450, and the source after-held result at static holder X300 is held X311/Z251. Configure factor1 `1333×730` and approved view `2048×1152` separately, step the holder once through production `CharacterMechanics.StepBattleLogic` from raw Vx48, then call the production `HeldObjectProcessAll`. Assert the exact expected holder movement, integer mirror and held X/Z positions, including the raw local holder-held X gap11 under both views. Do not infer source-rule reference position from the scaled held battle position.

Expected side effects: test-only; no DAT, Scene, camera, ProjectSettings, production script or nonbattle change. Acceptance: original project's existing Editor compiles and the exact two cases pass. Record if the fixture instead exposes a different writer or formula; do not change production to force the assertion. This proves a registered-World WPoint pose transaction at two views, not sprite contact, formal EXE output, full Driver/Play, every WPoint kind, or CPoint acceptance. Rollback removes the one test method and its test-only imports.
