# NTSD28-Q08-PHYSICAL-ATTACK-REAL-MENU-KO-001

Status: `VERIFIED_SCOPED`. Parent `BATCH-04 / Q08`; Q07 remains the earliest open Q and is not bypassed or closed by this independent result-chain witness. Original-Editor physical J→authored frame513→attributed KO→timer350→production selection return passed; exact evidence and limits in `artifacts/diagnostics/NTSD28-Q08-PHYSICAL-ATTACK-REAL-MENU-KO-001/ACCEPTANCE-20260925.md`.

Authority and input: formal `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable tick/input/collision/result path, and formal `c/nar/nar.dat` frame 60/62 (frame 62 has authored kind-0 injury20 ITR). Unity P1 `NTSDInputConfig.inputactions` binds `Attack` to `<Keyboard>/j` through `CharacterInputModule`. Previous controlled full-Driver collision and production return are `NTSD28-Q08-COLLISION-TO-ORDINARY-REAL-MENU-WITNESS-001`.

Gap: the original-Scene KO→timer→selection witness uses a synthetic attacker and enlarged ITR. It does not show a real participant's authored attack entered through the physical InputSystem route.

Exact ownership: extend only `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07MenuSceneCallbackPlayProbeEditor.cs` with a separate opt-in request/result route. Reuse actual Menu callbacks to select two opposing Naruto participants and formal publication. In live Battle pause LocalFreeRun, retain both real characters, place the defender at a controlled adjacent source position and reduce only its Play-time HP so one authored punch can be lethal. Send J through the existing InputSystem P1 action, step full Driver ticks, record `LastAppliedFrameInput`, actual attacker frame, defender HP and native KO event. Do not create an attacker, change DAT/ITR/frame, assign result state or call a test damage writer. Then observe result timer 0→1→350/transition2 and production selection return. Stop with an exact FAIL if the authored attack misses, input is not captured, or World identity changes; do not fall back to synthetic collision.

Scope and risk: this is a synthetic device event with controlled victim position/HP, not an unassisted player match or formal EXE visible equivalence. InputSystem events must be released even on failure. Preserve Q07 and both earlier Q08 request routes, Menu/Scene/GameConfig serialized bytes, DAT, images, production code, framework and nonbattle behavior. Use original Editor only, no computer-use or second Unity project.

Acceptance: Task/Change/Ledger before script edit; original Editor zero C# errors, unique request JSON, physical J in frame input, authored frame60/62 or exact source-equivalent punch branch, actual victim KO with native event, timer 0 on lethal tick then 1/80/101/350 and command2, production return to selection and borrowers0, restored scene and unchanged protected hashes, `git diff --check` and Change Ledger validator. A failure narrows the next first difference; it does not close Q08. No entire suite rerun for this diagnostic-only package.

Rollback: remove only this opt-in Editor diagnostic hunk under repository approval rules; preserve all pre-existing dirty work and previous JSON results.
