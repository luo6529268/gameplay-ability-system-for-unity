> 武器修正后完整984四组各846条/108例差异，before0、Shadow额外0；124个无护甲武器已通过。本Task108例仍未处理，不把相同错误的Shadow预测当原版对齐。

# 非角色 reduced 命中完整事务

READY_AFTER_UNARMORED_WEAPON_AND_TYPE5_COVERAGE。新PREARMOR-FEEDBACK源984中，90个type1 active和18个普通defense成功向量对应Unity完整入口仍落旧typed unarmored backend；当前共108例，不能因Shadow预测与旧backend相同就认为已对齐。

源入口battle_world.cpp resolve_confirmed_reduced_hit及type1/defense两个上层调用；已证type1 active不运行unarmored release，reduced在rest拒绝前不保留defense_decision的返回行为影响普通wrapper fallback。新的BattleNativeOrdinaryHitPrelude已正确选择入口，后继damage backend须接对该分支而非再次按类型忽略route。

准确Record前完整核验HP/HPBound/MP、armor资源与credit/统计、type6跳过资源伤害块、weapon durability、Bdefend、rest、motion/动作/朝向、声音和最后spark的完整顺序。复用现有reduced resolver和runtime写入，不仅取消ApplyAlternateDamage的character类型guard；该函数当前仍是角色行为，不能直接假定对所有types正确。

先984向量108例，再按源补必要type/状态/资源边界、实际两factory与Shadow、SelfCheck、Play/关闭/本地回放。剩余全984与BDEFEND父记录未关闭，不修改shared Kernel/schema/Server/非战斗/Scene/资源，禁止computer-use。

2026-09-14 下一批只读复核：断点为prelude已选reduced route但非角色dispatch丢失route，仍走typed unarmored。准确候选路径为BattleHitCandidateSequenceRunner.TryConsumeCaptured、BattleDamageWriter.TryApplyCurrentDatTargetHit及新非角色reduced事务、BattleEcsHitExecutionPlan的prelude observation与writer投影。复用既有Route.Kind/Armor/EffectiveInjury/ArmorMatch/ArmorActivation，传候选瞬时上下文，不加实体持久bool/schema。不得在prelude写入后重新Resolve猜route，也不能以RuntimeArmorHp118==-1代替broken-fallback pointer。

原108主要injury1/decrease50，reduced整数伤害多为0；它们清零不足以验完整reduced。生产前需新source扩展非零正负injury、MP/armor HP临界/不足/破裂、type6跳过全部资源块、type1/2/4/6durability、Y相对reference三侧、current/Prev2分离、state1002随机0xF3和Vz/-1.5、state2000、负link parent hold复制（不取负）、selected-route spark。非角色reduced零声音、无input status/join/mimic及matched早返，current7/70/75和阈值max(ratio,30)决定112，ground110→111。源resolve_confirmed_reduced_hit7022及type1入口7401。先新Source Record再实现见证，三生产路径仍未授权具体脚本改动，须独立Record。

root当前接线审计：SequenceRunner的prelude局部值贯穿writer observation及dispatch，consumer.BeforeDispatch仍先执行；现有NativePreludeObservation只传FeedbackOnly，Entry在ObserveNativePrelude仅记NativePreludeObserved/NativeFeedbackOnly。Shadow需要把原Route作为候选Entry值在prelude观察时保存，CanProjectLegacyWriterEffect与ProjectWriterEffect消费同一值，并在Entry构造/重置清空。不得重算route。生产能否在SequenceRunner直接分流需先审计四种consumer.Dispatch副作用，不能仅凭TryApplyCurrentDatTargetHit类型分发推断；该审计已单独交只读reviewer。新source前置Record已建NTSD28-Q06-NONCHARACTER-REDUCED-SOURCE-WITNESS-001，两Tools由worker独占，root尚未改本批生产。

独立consumer审计完成：本批nativeOrdinary+resolved kind0+Damage+UsesReducedHit+DAT type1..6可在runner原dispatch表达式中分流新writer，保留BeforeDispatch及全部后置combo/观察/原abort返回。不得提前return，不得新writer失败后fallback unarmored。四consumer的kind0没有本批额外副作用；SpecialAttack OID201释放只对type0/unarmored，因此不适用。不得调用旧RecordKind0Hit，它实际是unarmored spark包装；新尾部仅Append一次，defense的selectedArmor=null，ReducedType1Armor才route.Armor，reduced=true/unarmored=false。精确三生产路径足够，无需改consumer/Hit方法/接口。

root源horizontal复核新增差异：native reduced ground死亡稳定非2000为±1（旧Unity±3），dvx/2.0保留小数（旧int截断）；native没有旧effect22/23按相对X反向的例外。新source见证需包含奇数±5、fall80/vx0/dvx0、22/23两侧，已发给worker；不能直接复用旧AlternateGround/Air helper。state2000后置坐标应按native整数，不直接用现doubleX helper。
