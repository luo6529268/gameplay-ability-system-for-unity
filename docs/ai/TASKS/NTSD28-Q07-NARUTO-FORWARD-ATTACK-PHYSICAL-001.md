# NTSD28-Q07-NARUTO-FORWARD-ATTACK-PHYSICAL-001

Status: `VERIFIED_FORMAL_PHYSICAL_INPUT_TO_SPAWN_ONLY`. Parent BATCH-04/Q07, content-triggered R06/R18 representative. This package tests one natural formal-content action chain through actual Unity physical Input System events; it does not represent all characters or visual parity.

Authority: formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; `source/README_SOURCE.md` and playable `build.ps1` include `ntsd28_core/src/simulation/input_routing.cpp`; formal `resources/runtime/decoded_dat/c/nar/nar.dat` SHA-256 `6BE721524C8CCA0E293BEB8D6BF1DFEE306CCB948181BDAA94545EDB29418ED9`, identical to staged copy. Its standing frames 0-3 advertise `hit_Fa: 285`; frame 285 continues to 545 and the same Fa branch has frame 286 with `opoint kind:1 action:240 oid:33`. The existing Unity `BattleComboPlayModeProbeEditor` already queues physical defend/forward/attack through Input System and observes canonical combo/frame fields, but only has legacy fixed result files and destructive request/result cleanup.

Exact script scope: `Assets/NTSD/Scripts/Test/Editor/BattleComboPlayModeProbeEditor.cs` only. Add a Q07 JSON request whose `requested` flag is reset, never delete the request or any result; unique `runId` yields a new Q07 artifact JSON. Reuse its physical L+facing-forward+J path, but before input require production formal GameConfig root/fingerprint and Naruto OID2, then record frame progression, first appearance/peak of OID33 and current formal source key. Keep old menu items/legacy request behavior unchanged. No production input/combat/render script, Scene, prefab, asset or nonbattle code edit.

Acceptance: existing Editor remains on protected `NTSD_Battle.unity` with GameConfig formal root; unique Play request enters Play and queues the actual Input System keys. Terminal report must identify formal content and Naruto, observed combo steps, authored frame 285 and subsequent OID33 creation without injected entities. Capture a bounded trace/ticks and report failure honestly if the chain does not reach the target or OID. Exit Play and confirm protected Scene SHA, GameConfig root and ChangeLedger. No all-character suite rerun. This checks an authored natural input/spawn sequence, not collision/hit, full skill lifetime, original EXE pixels or all Q07/R18 cases. If a first difference appears, retain the failed report and use exact current Logan caller/data evidence before any production correction.

Risk/rollback: Editor physical-event timing and active facing/MP can influence the route; the probe must record those preconditions, not silently edit scene or actor state. Test-only change can be corrected within a new recorded result; preserve prior probes and outputs. The current scene's user HUDBg modification, old assets and Unity/GAS/nonbattle functions remain untouched. No computer-use.

Acceptance: `artifacts/diagnostics/NTSD28-Q07-NARUTO-FORWARD-ATTACK-PHYSICAL-001/ACCEPTANCE.md` and `q07-naruto-fa-1.json` record actual Play PASS. The declared target frame and OID33 were observed; the Editor is now idle outside Play, the request is marked false, and the protected Scene SHA is unchanged. Scope remains one Naruto natural-input-to-spawn example; Q07/R18 final acceptance is open.
