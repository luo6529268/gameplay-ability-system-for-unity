# NTSD28-Q10-BGM-DAT-CONTENT-ENTRY-001

Status: `DELIVERED_CONTENT_ONLY / PLAYBACK_OPEN`. Parent: BATCH-05/Q10, Q07 non-excluded formal DAT content closure and R17. Authority: root NTSD2.8-Logan EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, formal `resources/runtime/decoded_dat/data/bgm.dat` SHA-256 `ADA6B4E9C8F5C6FBD9A9A85F5E6188CD1CEE5E85139CD9367915EF786B64CAEA`, and paired playable `GameSession28::initialize`/`resolve_native_bgm_selection` in `game_session.cpp`. User D-023 selects formal non-excluded DAT; background, both mode DAT classes and unrelated audio assets remain outside this file addition.

Exact write scope: add only `Assets/NTSD/Content/LoganRuntime/decoded_dat/data/bgm.dat` as an exact byte copy and its Unity-generated `.meta`; this Task/report and alignment/handoff/STATE may change. Do not edit any DAT value, WMA/WAV, production script, Unity/GAS framework, Scene, ProjectSettings, Menu, nonbattle function or old Config resource. The destination is currently absent, so this is an addition rather than an overwrite or deletion.

Source/caller facts: the formal DAT lists eight named `bgm\\*.wma` files; playable `game_session.cpp` loads it into `native_bgm_virtual_paths_` and uses the selected entry for battle BGM, including random selection when requested. Current Unity C# source search found only a native-random BGM pre-draw constant/test and no `bgm.dat` consumer. Thus staging this DAT establishes content availability only, not audio playback or parity. Q10 must separately decide runtime event, supported media, project audio exception and same-condition validation; D-023 does not authorize copying all WMA/WAV.

Exit: before/after path and SHA check, exact one-file copy, Unity import/meta check in the original Editor if available, no modified DAT/Scene/nonbattle path, and a Q07/Q10/R17 status note preserving the playback gap. Rollback is removal of this newly added file/meta only under repository approval rules. No script Change Record is required because this Task edits no script.

Exit evidence: `artifacts/diagnostics/NTSD28-Q10-BGM-DAT-CONTENT-ENTRY-001/ACCEPTANCE-20260925.md`. Formal and staged bytes both 454 and SHA-256 `ADA6B4E9C8F5C6FBD9A9A85F5E6188CD1CEE5E85139CD9367915EF786B64CAEA`; original Editor imported a DefaultImporter meta with GUID `12d46eaf5869cb243a05f5178581d5b2`, console query returned zero errors. Staged non-excluded DAT count rose 337 -> 338; all eight referenced WMA remain absent from staged VFS, and no Unity BGM DAT consumer was found. Q10 playback is explicitly open.
