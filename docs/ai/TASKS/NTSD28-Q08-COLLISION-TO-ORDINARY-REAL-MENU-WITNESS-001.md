# NTSD28-Q08-COLLISION-TO-ORDINARY-REAL-MENU-WITNESS-001

2026-09-25 scoped exit: `VERIFIED_SCOPED`. Corrected original-Editor request `q08-collision-real-menu-result-20260925-2` passed the two-team real Menu entry, controlled full-tick collision KO at tick90, result timer 0 then 1/80/101/350, ordinary transition2 and production return to active selection with old Driver destroyed and zero pool borrowers. No result-state field was injected. The first request's diagnostic mode-ID failure is retained. Menu/Battle/GameConfig SHA values stayed unchanged. Details and limits: `artifacts/diagnostics/NTSD28-Q08-COLLISION-TO-ORDINARY-REAL-MENU-WITNESS-001/ACCEPTANCE-20260925.md`. Parent Q08 remains open for natural attack, formal EXE visible comparison and Player.

2026-09-25 first Play precondition correction: unique request `q08-collision-real-menu-result-20260925-1` reached real Menu→BattleRunning with formal publication and World4, then stopped before any fixture/collision because the diagnostic incorrectly required `BattleGameModeId==0`. Project VS uses local game-mode ID 0 with battle mode ID 1 (`SimulationTickDriver.ApplyMatchConfig` defaults the latter to 1; `GameConfig.asset` has the same pair). The first result is retained as FAIL; no KO/timer claim follows from it. Editor exited Play, restored Battle Scene, and Menu/Battle/GameConfig disk SHA values remained stable. The next request must assert the actual local0/battle1 pair without changing project mode content or result rules.

Status: `IN_PROGRESS`. Parent `BATCH-04 / Q08` and `NTSD28-Q08-ORDINARY-RESULT-SELECTION-HOST-001`.

Authority: formal root `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; paired playable `GameSession28::step` classifies living groups before combat, consumes a lethal collision later in the tick, starts result timer on the following tick, and emits ordinary transition 2 at timer 350. Existing formal source witness and original Unity full-tick lethal test are recorded under `NTSD28-Q08-RESULT-GROUP-CARRIER-AUDIT-001` and `NTSD28-Q08-COMBAT-LETHAL-PRECOMBAT-TIMING-001`.

Current gap: original-Editor real Menu→Battle result return has passed only with direct result-state injection. The Menu probe currently chooses one human and CMC0; CMC confirmation alone does not add an opponent to `MatchConfig`. One living group cannot witness a two-group lethal transition.

Exact code ownership: extend only `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07MenuSceneCallbackPlayProbeEditor.cs` with an independent opt-in request/result. Use existing real Menu callbacks to confirm a second slot on the opposing team, enter the saved additive Battle Scene, then pause the live Driver and construct one controlled lethal kind-0 collision using the existing `NTSD28Q08CombatLethalPrecombatTimingEditorTests.CreateCombatant` fixture. Do not write any `BattleResultsRuntimeState` field. Record both groups before collision, victim HP and timer after the lethal tick, timer on the following tick, milestone timers, native transition 2 at 350, and the existing production return to selection with ordered shutdown, destroyed old Driver and zero pool borrowers. The probe must identify its controlled fixture as such; it is not a physical player-attack or whole-world formal EXE parity test.

Protected scope: preserve the Q07 request, prior Q08 injected-result request, Menu/Scene/GameConfig serialized bytes, production battle logic, DAT values, images, UI/menu behavior, GAS and approved presentation exceptions. Use only the already running original Editor, no computer-use or second Unity project. A failure must still consume its request, exit Play and restore the saved Battle Scene. Do not overwrite an existing request/result file.

Acceptance: Task/Change/Ledger before script edit; original Editor compilation with zero C# errors; one unique Play request and exact JSON; observed collision-caused KO at tick N with native result timer 0, following tick timer 1, phase milestones and timer 350/transition 2, production selection return and pool borrower 0; Scene/GameConfig hashes stable; Change Ledger and diff checks. If fixture/roster preconditions fail, report the exact first failure and keep Q08 open rather than injecting result state as a fallback. This scoped witness still leaves natural physical attack, full formal EXE-visible comparison and Q08 aggregate open.

Rollback: remove only this opt-in Editor diagnostic hunk under repository approval rules, preserving other dirty work and all previous request/results.
