> 当前状态 VERIFIED / SCOPED_STATE18_NATIVE_PARTICLE_TRANSACTION；最终证据见同名Record末节。以下保留形成过程，剩余父任务继续。

# C25L state18/19粒子生成事务

READY_UNITY_RED_AND_EXACT_RECORD。父NATIVE-FRAME-TRANSACTION-INTEGRATION最终组合核验定位F08缺口；原C25L owner正确，不重做placement。前置C25L-STATE18-SPAWN-SOURCE-WITNESS已1550/12759、重复输出及mixed OPoint→particles→fragments→lifecycle通过，先读其REPORT/JSON，不重跑旧见证作为实施替代。

先对应实际caller和准确Record，再改LF2Entity.C25L与必要新writer/测试。同步RNG callsites、0/7/1选择及positive delay seam、catalog/slot先于tuple；源整数与精确位置分离、generic vitals500/owner-1/team0/right、action mirrors/sound latch；借用现有native broken-weapon generic初始化和两个factory的resetWorld能力，是否复用nativeWeaponPieceSpawn旗标须在Record声明它覆盖两种原generic出生，不复制整套birth/reset。

native state18扫描entity(slot)==null，不能机械套builtin/DAT的slot_available_for_spawn。先对应Unity融合/保留槽与失败候选，正确消费tuple并停止，不能为测试放宽全局registry。task要显式targetWorld，立即生成或已声明有序flush须确保每粒最低槽推进和后续OPoint/fragment一致；若由queued改immediate，审查Module对返回true的FlushQueuedObjectPointTasks及计数，避免无任务extra flush/跨owner队列冲刷。保留state13/200 shared旧tail，不擅自删除其helper。

测试先RED；775出生+full driver适用向量、seed2等确实产生单粒的样例、types0..6/声明999/pending/空槽/同tick高低slot、两frame后端及两factory完整birth/RNG。原NTSD28C25LState18ParticleOwnerEditorTests旧world.Rng断言随当前规则调整并记录，保留owner/mirror真实保证。完整SelfCheck/真实正式OID999与隔离fixture/Scene恢复/关闭全0，不能仅将原RUNTIME_PENDING状态文字变绿。当前dynamic global-delay生产仍B8/Q08，不新增未快照字段或虚构已支持。

无正式资源迁移、Scene/HUDBg x30保持，不改非战斗/Unity/GAS/关闭十一阶段/schema。新脚本先准确Change Ledger登记。完成该具体出口后自动回父Frame最终联合验证，reader其余live caller/display出生/post-display/Q07仍未完；post缺失是已有Task，不重新发明或形成循环等待。
