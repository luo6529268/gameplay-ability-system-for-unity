# NTSD28-Q07-SASUKE-NATURAL-STATE15-MOTION-001

Status: `VERIFIED_SCOPED`. Parent: BATCH-04/Q07, R18, and `NTSD28-Q07-STATE15-POSTPHYSICS-VELOCITY-001`. Authority: formal root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable frame motion/world tick, formal `c/sasu/a/chi.dat`, and `artifacts/diagnostics/NTSD28-Q07-SASUKE-FORMAL-MANUAL-TRACE-001/COMPARISON-20260925.md` (controlled tick24-26 action12-14 Vx0 after the generic state15 fix).

Current gap: the existing original Battle Scene physical L/D/J Sasuke probe confirms PP, frame264, four OID440 births and formal pic0 binding, but records child velocity only at birth. It cannot witness the repaired state15 post-physics velocity during natural scene play. The existing `ObservationTailTicks=18` ends near the relevant frame and Editor Update may skip a logic tick; absence of a sampled frame is a diagnostic coverage limit, not automatically a production defect.

Exact script scope: only `Assets/NTSD/Scripts/Test/Editor/BattleComboPlayModeProbeEditor.cs`. Use a dedicated opt-in Sasuke motion request path with the existing JSON schema, leaving prior request files unchanged; for this opt-in capture OID440 action12-14 child stable ID/slot/frame/position/velocity on observed full Driver ticks, and require four distinct OID440 action12 observations with Vx0 before reporting this scoped PASS. Preserve the existing natural physical input, identity gates, picture assertion and request/result ownership. No production, DAT, image, Scene, GameConfig, Menu or nonbattle edit. Do not change generic tail length for other probes.

Request-file protection: the existing `Temp/NTSD28_Q07_SasukeNeedle.request.json` is a retained prior run (`requested:false`) and must not be overwritten. Add a separate motion-request path and result root, dispatching it through the same Sasuke probe while leaving the old request route functional. The one new request/result is diagnostic output, not project configuration.

Acceptance 2026-09-25: the dedicated original-Editor physical L/D/J Battle Scene Play returned PASS. Four distinct OID440 stable IDs each had Vx0 at action12 and again at actions13–14; PP500→400, frame264 and formal chi.png pic0 binding were observed. All 12 target motion rows were present despite two other missed Editor samples. Editor exited Play; Battle/Menu Scene and GameConfig disk SHA remained unchanged. The prior request file remained untouched. Evidence: `artifacts/diagnostics/NTSD28-Q07-SASUKE-NATURAL-STATE15-MOTION-001/ACCEPTANCE-20260925.md` and its unique JSON. This closes only the natural Unity scene motion witness; formal EXE natural same-world, full combat/presentation and Q07 aggregate remain open.

Acceptance: pre-change Task/Record/Ledger registration; original Editor compiles; one unique physical L/D/J Battle Scene Play result with sampled action12 motion for all four children, explicit PASS or diagnostic coverage failure; exit Play; Scene/GameConfig disk hashes and zero compile errors; Ledger and diff checks. The result is a natural-motion subpath witness, not a full same-world formal EXE trace, collision/lifetime/pixel/audio certificate or Q07 closure. Rollback only the opt-in diagnostic hunk under repository approval rules.
