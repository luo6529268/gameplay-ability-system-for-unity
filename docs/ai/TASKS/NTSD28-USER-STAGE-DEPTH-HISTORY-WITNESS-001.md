# NTSD28-USER-STAGE-DEPTH-HISTORY-WITNESS-001

Status: `FOCUSED_TEST_PASS / TEST_ONLY`. Parent: `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (D-024). Original-project Editor final strengthened job `ddce615b36c54e6b902682f627431db7` ran exactly the declared two cases, 2/2 PASS, asserting the view factor and an independent numeric pre-bound Z. Factor1 raw Z300+40 ended at340 inside far350; configured `1152/730` physical Z traveled to `363.123287671...` before the independent default StageZ pass clamped it to350. This confirms the source-rule reference must keep its own raw Z and bound outcome instead of copying the approved physical clamp. It does not change stage values, DAT, camera, Scene, production runtime, or the Unity/GAS framework.

Authority: the shipped Logan EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable `BattleWorld28::clamp_type0_stage_depth` in `source/ntsd28_core/src/simulation/battle_world.cpp`, and the user-approved D-024 full-background physical displacement. Formal code clamps precise Z to the absolute near/far stage bounds and refreshes its integer mirror. Unity production `NTSDBattleTickSystem.ClampCharacterZToStageBounds` calls `SimulationWorld.ClampCharacterZToStageBoundsAll`, whose default `BattleEcsCharacterStageZPass.ExecuteDataOriented` writes Z/ZInt independently of the later preframe bounds pass.

Declared code path: only `Assets/NTSD/Scripts/Test/Editor/BattleEcsCharacterStageZPassEditorTests.cs`, one parameterized test using its existing registered-character World helper. The test will configure formal/default view `1333×730` or approved full view `2048×1152`, absolute Z bounds 180..350, initial precise Z300 and raw Vz40. It will use production `CharacterMechanics.StepBattleLogic` with the World's view factors, then the production `ClampCharacterZToStageBoundsAll`. Factor1 should end at Z340 (inside the formal bound); the configured view should travel beyond350 and clamp battle Z to350. Assertions cover pre/post precise Z, ZInt, and default StageZ mode. The test character is controlled and has no directional block.

Expected side effect: only a new EditMode characterization test and its records. The factor1 result is a source-rule model expectation based on paired playable arithmetic, not a captured formal EXE tick. No rule is changed. Acceptance requires the original project's existing Editor to compile and run only the two cases, with recorded exact results. If a fixture assumption fails, report the actual first difference and revise the fixture without altering production semantics. This is not full Driver/Play, formal EXE, or non-character depth acceptance.

Rollback: remove the one test method and any single test-only import added with it. The future carrier implementation requires its own bounded Task/Change covering all active writers, source-rule reference precise/int, snapshot/reset/checksum and OID219/fusion consumers.
