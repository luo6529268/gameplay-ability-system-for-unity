# NTSD28-Q07-REDUCED-HIT-HALF-DVX-PRECISION-001

Status: `FOCUSED_TEST_PASS / SCOPED_RAW_PARITY`. Parent: `NTSD28-UNITY-BATTLE-REALIGNMENT-001 / BATCH-04 Q07`. This is the independent first-difference package after the scoped MP resource fix; it does not change D-024's user-approved physical displacement ratio.

Formal root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033` and paired playable `source/ntsd28_core/src/simulation/hit_response.cpp`, `HitResponseResolver28::accumulate_ordinary_horizontal`, use `facing_sign * (double(dvx) / 2.0)` for ordinary ground reduced hit. Unity `BattleDamageWriter.ApplyAlternateGroundKnockback` currently uses `int halfDvx = itr.dvx / 2` before adding to `KnockbackVx`. The strict X550 OID87 26-tick post-resource trace shows formal target motion X8.4 at tick19 and precise X558.4 at tick20; Unity motion X8 and X558. The two selected-armor hits in the four-hit group have odd horizontal `dvx`; their 0.5 fractions are lost before the two ordinary hits. X1200 no-hit remains a control.

Declared script paths before edit: `Assets/NTSD/Scripts/Simulation/Ecs/Writers/BattleDamageWriter.cs` and `Assets/NTSD/Scripts/Test/Editor/NTSD28B5UnarmoredHpConsumptionProductionEditorTests.cs`. Add a direct shared reduced-ground odd-`dvx` test first and record its RED result; then change only the local half-`dvx` calculation to floating-point. Do not special-case OID87/Sasuke, modify DAT numeric values, alter ratio scaling, scene/camera, config assets, input or nonbattle framework. No new runtime manager, pool or shutdown phase.

Acceptance: original Editor compile zero errors; focused RED→GREEN; rerun existing strict X550 and X1200 raw scenarios into new files, preserving both prior sets; compare all 21 mapped fields, especially target motion X at tick19 and position at tick20 onward. Separate formal rule coordinates from user-approved physical ratio if the scenario uses non-unit scaling; report that mapping explicitly. Run Change Ledger validator, `git diff --check`, protected scene/config SHA and recovered-doc NUL checks. Natural physical Battle Play and full Q07 remain open until separately witnessed.

Rollback: review and reverse only this package's precision expression and focused test hunk; keep all previous user and Q07 changes and diagnostic traces.

2026-09-26 scoped exit: original Editor valid RED expected3.6/actual3.1 after accounting for pending-X initial0.1, then GREEN2/2, compilation zero errors. Existing strict root-EXE/Unity same-seed X550 and X1200 scenarios both PASS after the shared floating-point correction: 1428/1428 and 2100/2100 mapped fields respectively. Pre/post Unity changes only X550 target motion ticks19–26 and position ticks20–26; explicit v2 domain and input/RNG rows are unchanged. Natural Play, physical ratio layer and broader Q07 remain open. See `artifacts/diagnostics/NTSD28-Q07-SASUKE-ARMOR-TARGET-FIRST-DIFF-001/MOTION-FIX-ACCEPTANCE-20260926.md`.
