# NTSD28-Q07-SERIALIZED-MENU-CALLER-001

Status: `VERIFIED_SERIALIZED_MENU_CALLER_ONLY`. Parent `BATCH-04 / Q07`; dependency `NTSD28-Q07-PRODUCTION-CONTENT-ROOT-SWITCH-001`. Purpose: verify that the menu content-prewarm caller still works when the shared serialized GameConfig now selects formal Logan DAT/images. This is a narrow regression guard for the existing Menu Scene GUID reference, not a new menu feature.

Exact code scope: only `Assets/NTSD/Scripts/Test/Editor/NTSD28B11SourceCallerPlayProbeEditor.cs`, specifically the test Request and `PrepareRequest`. Add an explicit `useSerializedRoot` request switch. When set, require the persistent GameConfig asset's root equals `Assets/NTSD/Content/LoganRuntime`, and copy that value into the existing isolated Play clone request; do not invent or override another value. Preserve the existing explicit-root test path and producer behavior. No production script, GameConfig asset, Scene, prefab, nonbattle UI or old resource edit.

Acceptance: one unique `mode=menu`, `formalStaged=true`, `useSerializedRoot=true` request through the existing Play harness. It must assert actual `LoadingPrewarmController.PrewarmOnceAsync`, cache rehydration, formal fingerprint and manager/data/UI key agreement, Running World, ordered shutdown, zero old/new owner resources and pool borrowers, two-frame Stopped. Confirm the test read the exact persistent GameConfig asset path/root and after Play the serialized field/hash and protected Scene hash are unchanged; run ChangeLedger validator. Existing Play harness is allowed to clone the asset for isolation but must not silently substitute a different root. This does not prove visual interaction in the real Menu Scene or full nonbattle workflow; record that limit and retain Q07 return if needed.

Risk/rollback: test-only addition could disturb earlier explicit-root requests; keep the default switch false and limit changes to the preparation branch. A failed menu result must be investigated rather than hidden by restoring old content or editing menu UI. Rollback is a narrow correction to this test script or the single GameConfig root with a new recorded outcome; never reset unrelated work.

Acceptance result: unique `q07-serialized-menu-1` Play report PASS with `serializedRootSelected=true`, `rootFromAsset` formal path, `menuReady=true`, cache hit1, formal source key and three owner keys agreeing, World4, both previous/current owner resource survivors0, borrowers0, ordered shutdown and two-frame Stopped. Editor returned idle/not playing, GameConfig and Scene hashes held. See Q07 `SERIALIZED-MENU-CALLER-ACCEPTANCE.md`. Actual Menu Scene UI interaction remains outside this caller-only result.
