# NTSD28-Q07-SASUKE-NATURAL-PROBE-IDENTITY-001

2026-09-25 scoped exit `VERIFIED`: the diagnostic-only current-mode fingerprint correction compiled and the second unique original-Editor physical L/D/J Play PASSed after the preserved pre-input FAIL. Exact results, hashes and what this probe cannot show are in `artifacts/diagnostics/NTSD28-Q07-SASUKE-FORMAL-MANUAL-TRACE-001/NATURAL-PLAY-POSTFIX-20260925.md`. Q07 and the production state15 rule's natural tick24 velocity remain open.

Status: `IN_PROGRESS`; parent `BATCH-04/Q07`, adjacent to `NTSD28-Q07-STATE15-POSTPHYSICS-VELOCITY-001`.

Trigger: unique original-Editor saved Battle Scene Play request `q07-sasuke-state15-postfix-20260925` produced a preserved `FAIL` before pressing any key: the diagnostic preflight requires the historical `FormalFingerprint` although current production publishes the approved project mode Asset semantic fingerprint `27CAE01489909C46A5145A5867988CCD8C10E20FEF165D847B7AC7A6DE2DE02D`. The same compiled raw-capture diagnostic confirmed this current published identity. The preflight also requires formal root, OID11 and authored `hit_Fa=261`; those terms have not yet been separately measured by this failed result.

Scope: change only `Assets/NTSD/Scripts/Test/Editor/BattleComboPlayModeProbeEditor.cs` so Sasuke Q07 preflight expects the current project-mode formal fingerprint, as DDJ already does. Keep all other root, OID, authored-frame and visual-key checks; do not mutate their expected values or alter production, DAT, Scene, Menu or GameConfig. Improve the failure message to expose each observed preflight operand so another failure can be classified without guessing. Use a new run ID and output file; preserve the first failed JSON.

Acceptance: original Editor recompiles and a unique natural physical L/D/J Play request obtains a nonzero result; record actual tick/key/child/binding outcome whether PASS or FAIL, exit to Edit Mode, Scene/GameConfig hashes and scoped `git diff --check`/Ledger. A passing old probe only proves its prior milestone assertions, not full state15 velocity after tick24 unless the result records it. Do not claim natural lifetime parity without that evidence.

Rollback: restore only the preflight diagnostic hunk under repository approval rules, preserving prior artifacts and user work.
