# NTSD28-USER-HITFA7-NONCHAR-SELF-CHECK-CORRECTION-001

Status: `FOCUSED_TEST_PASS / FULL_SELFCHECK_PASS / TEST_ONLY`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001`. The second full SelfCheck after FL-05 raw-slot correction reached `FL-02/HITFA-7` and failed because its near-full fixture expected a child OID707 in the sole remaining slot; source OID707/type Other stayed active in slot50 and no child appeared. Formal playable `NativeAi28::step_non_character_hit_fa` behavior7 (`native_ai.cpp`) performs target chase and Y/tail updates without a clone spawn in that branch. The self-check fixture explicitly constructs type Other (not character), while `LF2Entity.RunNonCharacterHitFa7FrameLogic` routes type Other to the nonclone branch. A separate original-project official OID875/type3 target-present test requires object count2 and no extra clone. This is a stale test expectation for the noncharacter branch, not authority to add production spawn.

Exact script scope: only the existing `CheckFrameLifecycleNearFullPublication` method in `Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs`. In its `hitFa==7` case, replace the expected-child assertion with an exact no-child/no-RNG/source-retained/last-slot-still-free assertion and return before spawn-specific checks. Retain hitFa5/11/13 publication assertions and the character-specific `RunHitFa7FrameLogic` code unchanged. Do not edit DAT, camera, Scene, images, nonbattle files or source authority.

Acceptance: original Editor recompiles, existing official OID875 target-present and empty-slot focused tests remain green, and one full SelfCheck request is run to verify FL-02/HITFA-7 no longer blocks; any later failure must be recorded separately, not suppressed. `git diff --check`, Ledger validator, Scene SHA and Content/Config/ProjectSettings status are checked. Rollback reverses only this `hitFa==7` self-check branch correction, preserving other dirty work. Q07 remains open because D-024 carrier, OID219/fusion and other gates remain.
