# NTSD28-Q07-OID32-UNITY-RECT-WITNESS-001

Status: `FOCUSED_TEST_PASS / RECT_BUILDER_DIFFERENCE_CONFIRMED / PUBLICATION_AND_PIXEL_PENDING`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001` / BATCH-04 Q07; follows `NTSD28-Q07-OID32-FRAME95-RELEASE-VISIBILITY-001`.

Result: original Editor imported the new test/meta and selected the one named test: 1/1 PASS. Actual formal staged OID32 source sheet yielded pic54 rect present, pic64 rect missing in the production indexed-rect builder. Two earlier 0-selected jobs are retained as failed selection evidence, not counted as test passes. Exact job, hashes and limits: `artifacts/diagnostics/NTSD28-Q07-OID32-FRAME95-RELEASE-VISIBILITY-001/UNITY-RECT-ACCEPTANCE-20260925.md`.

Trigger: formal source/root EXE controlled action95 keeps pic64 and a sprite command for OID32 at ticks0–8. Static Unity `BuildIndexedSpriteRects` appears to omit its wholly outside-image rectangle. The prior attempt to call `execute_code` read-only in the original Editor failed in the MCP in-memory compiler before executing code because the Windows mono command line was too long. Use the established EditMode test channel instead; retain that failed transport observation, do not treat it as a Unity result.

Scope: add only `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07Oid32BoundarySpriteEditorTests.cs` and its Unity-generated `.meta`. One diagnostic EditMode test reads the current staged formal Logan catalog via the project mode Asset, resolves OID32's actual `SpriteFileInfo`, loads its source PNG dimensions, then calls the existing production `BuildIndexedSpriteRects`. It records the exact source sheet, dimensions, neighboring in-bounds pic and pic64 result. It must not create/publish a battle World, mutate assets or Scene, alter production code/DAT/PNG, or run all cases. Use the original Editor PID/project only.

Exit: current Editor compile 0 errors and one named EditMode result, with scene hashes unchanged; clearly distinguish actual current rect-builder behavior from full publication or GPU pixel evidence. If the formal catalog does not contain OID32 or the test setup fails, record that first difference rather than manufacturing a sprite. This is a temporary diagnostic observation and must be revised or retired if a later generic parity fix changes rect behavior. Rollback is removal of the newly added test and meta only under protected-worktree rules; keep result evidence.
