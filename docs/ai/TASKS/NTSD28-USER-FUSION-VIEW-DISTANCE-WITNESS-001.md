# NTSD28-USER-FUSION-VIEW-DISTANCE-WITNESS-001

Status: `FOCUSED_TEST_PASS / FUSION_PARITY_DEFECT_CONFIRMED / TEST_ONLY`. Parent: `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (D-024). The user requested a non-perceptual check of remaining raw-pixel versus fixed-view-ratio battle differences before Q07 resumes. Original Editor job `42504562540d48f29a0f42433c24eced` passed only the two declared cases: factor1 gap40 merges, configured factor gap50 rejects. This is a successful defect witness, not a fusion fix. Evidence: `artifacts/diagnostics/NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001/FUSION-VIEW-DISTANCE-FIRST-DIFFERENCE.md`.

Authority: shipped Logan playable `source/ntsd28_core/src/simulation/battle_world.cpp` `BattleWorld28::advance_native_fusions` checks integer X distance `<50` and Z distance `<8`, then writes integer midpoint. The source's formal fusion input is `resources/runtime/decoded_dat/data/fusion.dat`; Unity's active owner is `BattleOid5152RuntimeModule.TryMerge` via `SimulationWorld.Oid5152FusionScanAll`.

Before this test package, the Unity scan reads scaled current battle `GetRuntimeXInt` and `GetRenderZInt` against unchanged raw thresholds. A static constructed example predicts formal gap40 but Unity gap50 after a 20-pixel raw X move from an absolute birth gap20 at Sx=2048/1333. This is not yet a measured runtime failure.

Declared test-only scope: add one focused method to `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06FusionRecordTransactionEditorTests.cs`, reusing its exact formal fusion bytes and prepared World. Keep the original valid source row's OIDs, actions, HP, team, Z and slot semantics; set the primary to a starting absolute X and advance it by the same raw 20-pixel source step represented by a scaled D-024 battle step. Run the production fusion scan in factor1 and configured factor World. Assert the documented source-rule expected merge and capture whether the current configured World merges. Preserve any first-difference characterization rather than falsely writing a green parity test.

Acceptance: original Editor compiles and runs only this focused test; result records initial/current integer positions, view factor, merge result, source-rule expectation and shutdown cleanliness. No production, DAT, Scene, camera or nonbattle edit. A failed expectation test may be retained as a defect witness if its behavior is stable and reasoned. Rollback is removal of this one test method only, preserving all existing dirty work. Subsequent general-coordinate implementation requires a separate Task/Change and must not be inferred from this witness alone.
