# NTSD28-USER-CPOINT-HELD-POSE-VIEW-WITNESS-001

Status: `FOCUSED_TEST_PASS / TEST_ONLY`. Parent `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (D-024). Original-project Editor exact job `b1e0ecd2071448b580d1b62e66b45f0a` completed the two declared cases 2/2 PASS, 0 failed/skipped. Factor1 catcher/caught XInt348/373 and configured 373/398 retain raw local gap25; caught YInt4/ZInt249. This closes one held-pose writer witness, not CPoint throw or dual-coordinate carrier.

Authority: shipped Logan playable `BattleWorld28::settle_catch_relations` (`source/ntsd28_core/src/simulation/battle_world.cpp`) builds a caught object's integer X/Y/Z from catcher integer position and both raw frame-local CPoints, then mirrors precise position. Unity active `LF2Entity.RunWeaponSyncHeldStep10` -> `BattleCpointWriter.SyncHeldCpoint` -> `SyncHeldPosition` uses catcher integer position and local geometry. D-024 scales physical travel under the fixed full-background view, not local pose offsets.

Declared script path: only `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06CpointSettlementRemainderEditorTests.cs`, adding one parameterized test. Reuse unchanged formal source fixture `artifacts/diagnostics/NTSD28-Q06-CPOINT-SETTLEMENT-REMAINDER-FRAME-BINDING-001/source/first.jsonl` row1 (`mode=0`, static catcher X300, caught after X325/Y4/Z249). Build its registered World using this test's existing `Definition`, `RestoreBefore`, logic-only materialization and shutdown patterns; do not modify fixture/DAT. Move catcher by raw Vx48 through production `CharacterMechanics.StepBattleLogic` at `1333×730` or `2048×1152`, synchronize catcher integer position, then call production `RunWeaponSyncHeldStep10` on the catcher. Expected catcher/caught integer X348/373 or 373/398, local gap25, caught Y4/Z249. If the writer deviates, retain RED and diagnose before changing production.

Expected side effects: test-only; no production, DAT, character image, Scene, camera, ProjectSettings or nonbattle change. Acceptance: original-project Editor compiles and both named focused cases pass. This proves one CPoint held-pose transaction after scaled catcher motion; it does not prove throw, nonzero cpoint.z, sprite contact, full Driver/Play, formal EXE capture or dual-coordinate carrier. Rollback removes the one method.
