# Q05 HolderCopy完整载体清理

状态 FOCUSED_TEST_PASS / SCOPED_PLAY_PASS / JOINT_SCHEMA_PENDING。前置WeaponState本包完整SelfCheck/Play出口后选择；五类退休字段最后一类，父Q05仍未完成。不要重新执行GrabbedBy/TrackerFlag/ReleaseTick/WeaponState/Mass/Oscillate已关子职责。

先读取CURRENT-AUTHORITY、Q03 JOINT-FIELD-MATRIX和OPOINT-AND-HELD-DEPTH-CONTRACT、已VERIFIED的NTSD28-B6-LEGACY-HOLDERCOPY-MULTIPLEXED-SLOT-OWNER-AUDIT-001、NTSD28-B6-LEGACY-HOLDERCOPY-RESIDUAL-RETIREMENT-PRODUCTION-001及B5相关writer退休Record。前包holder-copy-next-all-references.txt（case-insensitive子串）是候选线索，需按当前源码完整闭合，不能只检索全字而漏holderCopySlot/TargetHolderCopySlot。

范围候选：runtime HolderCopySlotIndex/default99/copy/reset；LF2Entity HolderCopySlot包装与各类型初始化默认；OPointCreateTask.holderCopySlot/default-1/reset及所有factory/initializer/materializer/respawn调用；ECS Links.HolderCopySlot存储、capture/clear/compare/fingerprint；checksum/parity实际存在项；BattleEcsHitExecutionPlan TargetHolderCopySlot诊断与mismatch bit33。不要为了删一项重排其他mask bit；空洞是否保留须写准确合同，不能擅自改变相邻诊断位或发布schema。

先按实际partial/任务/对象池、两条OPoint factory live调用链与原版关系字段收敛准确Record/写入路径。HolderStableId、LinkState、TargetSlotIndex、OwnerSlotIndex、SpawnerSlotIndex、ObjectAiExcludedGroupSourceSlot2F8、TrackerParent及有效target/holder查询必须保留；不能用其中任一字段重造HolderCopy别名或零值兼容壳。若确有真实行为消费者遗漏，则先依据当前原版确认和独立记录，不能直接删除行为。

先absence与真实关系/任务pool reset/canonical snapshot/ECS/诊断回归RED，再脚本；旧SelfCheck/test中的人工99/23/-1 sentinel、throw/catch/stage/release/OID5152/type3/kind9/OPoint copy与JSON报告需全部分类迁移。旧默认因类型不同而不同属于退休载体事实，不把它强行统一成另一个假规则。保留实际HP/帧/速度/RNG/关系/cleanup和bit位置断言；保留旧失败工件，不伪造已删字段值。

新/现有测试覆盖copy/reset、两条OPoint materializer与任务复用、hit-plan优化/legacy路径、raw/claimed snapshot restore、checksum、真实pick/throw/current OPoint定向Play与零残留/完整SelfCheck。改变诊断记录布局时登记真实payload影响，不能只凭字段删除检索声称完成。

当前12/20/23/1/1仍Q05未发布窗口；这五类清理后必须继续步骤3 semantic/raw identity及双OPoint owner空队列capture/restore guard（不得Flush造对象），步骤4一次统一13/21/24/2/2，步骤5旧版本拒绝/新snapshot回放/Play。父Q05出口未满足前不发布baseline/Q07；R13仅按实际删除子条件PARTIAL_RETURN，R15仍待identity/version。

保持Unity/GAS/非战斗、33ms/3ms、十一阶段、Scene/InputActions/Gen/Plugins/外部Server包、stage.dat USER_HOLD及例外。禁止computer-use，仅桥接/日志/结果/进程；现有Foot18缺失与用户新图、Scene旧SHA保护。预变更字节/SHA、准确Task/Record先于脚本；回滚须明确批准，仅精确差量，不清未知文件。

限定出口：36脚本已清，863经859+旧预期纠正26+1闭合；完整SelfCheck、真实pickup/replacement/current OPoint前后有效见证一致。下一唯一NTSD28-Q05-CONTENT-IDENTITY-AND-CAPTURE-BOUNDARY-001，父步骤3/4/5仍待；详见同ID REPORT。
