# NTSD28-USER-STATE9996-CHILD-RATIO-001

Status: `FOCUSED_TEST_PASS / RUNTIME_PENDING`. Parent `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (D-024). This is a bounded correction to the **shared state9996 clone birth writer**, not an OID217/218 or character-specific exception. Original Editor stricter ratio job `f15b213dffd9458a8d5fd73e3606f0d1` found expected RED at configured child precise X327 versus target325.39084771192796. After the writer correction job `68803d1fe0df40749ff8512f17231d4b` passed 2/2; strengthened all-five-child rerun `f1dc8c0b632a43d4a36c33380a865e53` passed 2/2. Default formal source-row immediate and following-tick neighbors passed 2/2 in job `eae331fdc81a48958c91e511d9da16be`. These are focused EditMode exits; Battle Scene Play and general source-rule history remain open.

Authority: shipped Logan playable `BattleWorld28::advance_native_special_clones` near `battle_world.cpp:2928-3010` draws raw random X, then births each child at source integer X+randomX, Y+randomY, Z+1. The Unity active producer is `BattleLateEntityLifecycleModule.SpawnState9996Children`, which creates all five OID217/218 tasks and currently writes unscaled integer X/Z offsets. Original Editor job `f2fbd17c1a1548b480ce8166db78f62f` measured configured view parent/child XInt330/327, relative -3/2048 instead of formal -3/1333; default factor1 is correct. Evidence: `artifacts/diagnostics/NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001/STATE9996-CHILD-VIEW-FIRST-DIFFERENCE.md`.

Declared code paths: `Assets/NTSD/Scripts/Simulation/Passes/LateLifecycle/BattleLateEntityLifecycleModule.cs` and `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06State9996DirectSpawnEditorTests.cs` only. Before production change, strengthen the focused test to expect a scaled **precise** X offset and Z+1 offset at the configured view, while keeping source random draw and factor1 exact parity. This should RED against current code. Then transform only final child-relative birth X and projected Z offsets using the World axis factors, retaining raw random values, draw order, Y, launch velocities, OIDs/actions, owner and materializer. Write direct precise X/Z and integer mirrors by the same cast-after-scaling convention as late OPoint. Do not multiply parent absolute coordinates or DAT values. The source-rule raw coordinate/history remains a separately open cross-module D-024 gate; this package does not pretend to close OID219/fusion semantics.

Acceptance: original Editor targeted RED then focused GREEN for factor1/configured birth, all five source-random tuples and immediate Q06 source parity at factor1, plus the relevant following-tick or renderer birth neighbor if touched. Confirm no new random calls, no residual pool borrowers, no DAT/Scene/camera/nonbattle changes. Run `Tools/Validate-ChangeLedger.ps1` and diff check. Rollback: inverse only this package's two exact script edits, preserving pre-existing dirty work. Full Battle Scene Play, formal EXE visual equivalence and non-16:9 views remain separately open.
