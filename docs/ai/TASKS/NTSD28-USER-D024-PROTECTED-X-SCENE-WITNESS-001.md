# NTSD28-USER-D024-PROTECTED-X-SCENE-WITNESS-001

Status: `VERIFIED` (controlled original Battle Scene witness only). Parent: D-024/BATCH-04/Q07 and `NTSD28-USER-D024-PROTECTED-OBJECT-X-CLAMP-001`. Authority: root formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable `BattleWorld28::settle_ordinary_stage_bounds`, formal `data/data.txt` OID122/123 type6 and their `w/3.dat`/`w/8.dat` frames. Current Unity code's focused clamp is `FOCUSED_TEST_PASS`; original Battle Scene full Driver reachability is now verified in a controlled witness. Natural item and formal EXE same-condition acceptance remain open.

Exact script write scope: new `Assets/NTSD/Scripts/Test/Editor/NTSD28D024ProtectedXSceneWitnessEditor.cs` plus Unity-generated `.meta` only. Task/Record/Ledger/STATE/handoff/alignment and new diagnostic result documents may change. Do not edit production, existing D-025 probe, DAT, image, Scene, Prefab, camera, GameConfig, Menu or other nonbattle code. Reuse the original running Editor; do not use computer-use or a second Unity project.

Diagnostic design: a one-shot request from a clean saved `NTSD_Battle` opens Play, waits for the existing battle World and formal catalog, pauses the host, registers only two test type6 OID122/123 objects at free transient slots with positive participant class, source and physical X just beyond opposite 100-pixel margins, stable Z, zero speed and formal frame binding. Run exactly one production `SimulationTickDriver.StepOneTick` with presentation disabled. Record stage width, before/after physical/source X and integer mirrors, slot survival, frame/action and any first lifecycle difference. Do not bypass the Driver or force the clamp method in this Play witness. If the formal frame's terminal behavior recycles an object before the bound pass, report a lifecycle/fixture limit rather than a clamp pass. Finally unregister surviving test objects, restore pause, leave Play, verify pool/world counts and saved Scene hashes.

Exit: original Editor compile; one unique-result Play with exact request ID; either observed full-tick protected-clamp values or an explicit first lifecycle boundary; ordered cleanup and disk hash protection. Any production defect needs a separate Task/Change and formal same-condition check. This is controlled registration, not natural item drop/consumption or formal EXE equivalence. Rollback only the new diagnostic script/meta and this package's docs through the repository's approval rules.

Exit evidence: `artifacts/diagnostics/NTSD28-USER-D024-PROTECTED-X-SCENE-WITNESS-001/ACCEPTANCE-20260925.md`; original Editor Play `d024-protected-x-play-1` PASS, tick0->1, OID122/123 both survive and settle at physical/source X 100/1940, zero object/slot/renderer borrower leaks, both Scene disk hashes unchanged. Natural item path and formal EXE same-condition evidence remain parent gates.
