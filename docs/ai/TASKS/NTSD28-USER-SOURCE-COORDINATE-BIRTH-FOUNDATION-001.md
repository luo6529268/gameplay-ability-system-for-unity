# NTSD28-USER-SOURCE-COORDINATE-BIRTH-FOUNDATION-001

2026-09-24 real Play pooled-Renderer exit: corrected source-birth probe returned PASS in the original Battle Scene. The isolated child used `LF2ObjectPointFactory` with nonnull Renderer, source X/Z and integers `(-42,126)`, battle X/Z and integers `(312,259)` in the configured 2048×1152 view. Live tick stayed 5, checksum `1226078659506514255` and borrowers `2` stayed unchanged. The first failed result (test expectation omitted formal kind1 integer reprojection) is retained separately; production was not altered for that correction. Original Editor narrow controls 3/3, ledger validator 745 PASS, Scene SHA unchanged. Evidence: `artifacts/diagnostics/NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001/source-coordinate-renderer-play-pass.json` and `source-coordinate-renderer-play-first-fail.json`. This closes only the declared first birth-writer package; carrier remains inactive and other birth/motion readers remain open.

2026-09-24 pre-test-correction Play first difference: root's real Play probe reached pooled Renderer birth, then helper battle-X assertion expected 312.29107276819207 but observed 312.0. Scene tick remained 5, checksum remained 1226078659506514255, and borrowers stayed 2. Formal playable `battle_world.cpp:7723-7739` and Unity `BattleNativeOpointBirthWriter.InitializeBirth` both reproject kind1 precise X/Z from integer coordinates before adding the random delta, including zero centerx/centerz. The first-row fixture is kind1 with zero random extents. Amend only helper expected battle X/Z to this post-kind1 integer reprojection and explicitly assert battle XInt/ZInt; preserve the fixture and production code. Root will rerun Play after narrow compile/controls; no source history completion claim.

2026-09-24 execution handoff: independent Play helper/probe compiled in original Editor; existing focused controls `fa424933c93e4d7c9be7109ef8d14226` 4/4 PASS, ledger 745 PASS. Root may write `run` to `Temp/NTSD28_SourceCoordinateRendererBirthPlay.request` in the original initialized Play session and inspect `Temp/NTSD28_SourceCoordinateRendererBirthPlay.result.json`. The helper itself verifies source precise/int, battle X/Z and nonnull Renderer; the branch verifies scene tick/checksum and pool borrowers. It does not dispatch Q05. This is preparation evidence only; `POOLED_RENDERER_PLAY_PENDING / CARRIER_NOT_ACTIVE` remains.

2026-09-24 pre-script P2 Play acceptance amendment: add `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06OpointMaterializerPlayProbe.cs` as the tenth exact script path for a standalone source-birth request/result branch. The prior new EditMode Renderer test failed at the pool's uninitialized `_availableObjects` (`LF2ObjectPool.Get:167`, job `14620dbf63e04227bfcd68e892360a0e`); do not simulate `Awake`. Convert that test into a Play-only internal static helper, assert real pooled Renderer and formal source precise/int plus configured battle coordinates, return observations, and guard live scene checksum/tick and borrower counts around isolated-world cleanup. Do not write the legacy Q05 follow-up request from this new branch. Original Editor compile and existing narrow EditMode controls precede root's Play execution. `POOLED_RENDERER_PLAY_PENDING / CARRIER_NOT_ACTIVE` remains; no production or resource edits. Rollback encompasses only the new helper/probe branch in addition to the earlier declared package changes.

Status: `FOCUSED_TEST_PASS / CARRIER_NOT_ACTIVE`. Parent D-024 `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001`. The previous two packages made source-rule X/Z state replayable and checksum-visible but did not initialize it in gameplay. This bounded first birth-writer increment now includes explicit source-rule coordinates in `OPointCreateTask`, both World/component materializers, formal late OPoint parent-relative birth, and raw-absolute stage birth including its post-materialization placement. It does not cover all births and does not enable OID219/fusion readers. Original Editor focused 22/22 PASS; details and limitations are in the Change Record.

Authority: formal playable `ObjectSpawning28` late OPoint takes parent integer X/Z and unscaled frame `opoint.x-centerx` / `opoint.z+1` before precise/int sync; Unity already scales the *battle* relative offset in `BattleLogicObjectPointRuntime.ConfigureLateOpointPosition` and `LF2ObjectPointFactory.ConfigureLateOpointPosition`. Stage spawnX/Z is a raw absolute coordinate in the current Unity stage task/second placement; this batch propagates it to the source-rule carrier without altering how it is selected. Formal stage selection algorithm itself is not changed or certified here. The user forbids DAT data changes and wants generic entity handling, fixed camera retained, nonbattle untouched.

Exact script scope: `Assets/NTSD/Scripts/Animation/LF2Tasks/OPointCreateTask.cs`; `Assets/NTSD/Scripts/Simulation/Runtime/BattleLogicEntityFactory.cs`; `Assets/NTSD/Scripts/Animation/LF2Objects/LF2Entity.cs`; `Assets/NTSD/Scripts/Simulation/Runtime/BattleLogicObjectPointRuntime.cs`; `Assets/NTSD/Scripts/Animation/Character/LF2ObjectPointFactory.cs`; `Assets/NTSD/Scripts/Simulation/Stage/StageSpawnTaskConfigurator.cs`; `Assets/NTSD/Scripts/Simulation/Stage/SimulationStageWaveModule.cs`; focused `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06OpointMaterializerEditorTests.cs` and `Assets/NTSD/Scripts/Test/Editor/StageSpawnRestAlignmentEditorTests.cs`. Do not edit other scripts under this ID.

Task contract: the task carries explicit source-rule precise X/Z plus optional independent initial source-rule integer X/Z and their presence flags; `Clear()` resets every field, including pooled reuse. `PrepareFinalRuntimePosition` applies the common default Z+1 once in **both** domains only when source-rule position was explicitly supplied and the common offset is not already skipped; it must not infer source-rule position by dividing a scaled battle position or silently treat all absent source fields as raw absolute. `LF2Entity.ApplyInitialRuntimePosition` installs source-rule values before its explicit-integer early return, preserving intentional precise/int divergence. Both materializers use that common method. Late OPoint producers compute source birth from initialized parent source integer X/Z plus raw local offset, sharing the existing battle task and RNG; when parent source is uninitialized, child source stays uninitialized rather than fabricating history. Stage `Configure` explicitly marks raw absolute source X/Z; the two subsequent `entity.SetPos` stage calls also set and integer-sync source to the same raw absolute X/Z, including cold fallback. No gameplay consumer reads these source fields yet. Do not modify battle X/Z, velocity, DAT-local geometry, random draws, stage algorithm, Scene or camera.

Acceptance: focused test-first RED then original-project Editor compile and GREEN for explicit source task and pool Clear, precise/int divergent initialization, default Z+1 once, logic/component materializer parity, late OPoint parent battle/reference divergence in factor1 and configured view including negative direction, and stage task/post-placement/cold path raw absolute equality. Preserve existing focused OPoint/stage controls. Run ledger validator, `git diff --check`, Scene/content boundary check. Full SelfCheck only if a cross-pass failure emerges. Remain `CARRIER_NOT_ACTIVE`; later birth batches must cover kind1 random, state18, state9996, weapon fragments, hit_Fa/N30/respawn and reachable special effects before source-rule consumers activate. Rollback only the exact nine paths above, preserving all other dirty work; no destructive Git command.

2026-09-24 pre-follow-up focused acceptance correction: first 22/22 test run invoked both late producers, but `CreateWorld(row)` sets logic-only materialization in both cases. The `component=true` branch therefore did not reach pooled Renderer/component initialization. Before finalizing this package, add an explicit non-logic-only focused case in the already declared `NTSD28Q06OpointMaterializerEditorTests.cs` path: use the existing renderer-birth setup/cleanup pattern, assert the spawned child has a real Renderer, and verify source-rule precise/int birth after the component route. If the original Editor lacks a Renderer/resource prerequisite, report that exact blocker and retain only the narrower logic-only claim. Do not edit production or broaden scope for this test.
