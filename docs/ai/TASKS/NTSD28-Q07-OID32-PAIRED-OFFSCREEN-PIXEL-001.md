# NTSD28-Q07-OID32-PAIRED-OFFSCREEN-PIXEL-001

Status: `VERIFIED_SCOPED_PAIRED_SOURCE_GPU`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001` / BATCH-04 Q07 / Q09-R17 presentation follow-up.

Trigger: root formal EXE controlled LFR action95/pic64 emits the same action/pic/aggregate sprite trace as paired source; original Unity Battle Scene publishes OID32 authored pic0 but omits pic64. The paired playable D3D renderer has a console-only WARP offscreen target and PNG readback. Formal root EXE itself has no demonstrated screenshot flag, so paired-source GPU output must be labeled separately.

Scope: add only `Tools/NTSD28Q07Diagnostics/hunter_frame95_offscreen_probe.cpp`. Build it from the paired playable/core source closure plus `d3d11_renderer.cpp` with the same D3D/WIC libraries as formal `build.ps1 -Target offscreen_gate`. Use read-only formal runtime root and the same seed/slots/positions/background as the prior OID32 LFR. At tick0 create separate action95 and action0 control sessions, inspect slot0 sprite geometry, render each via D3D11 WARP offscreen, write create-new PNGs and geometry text under this Task's artifact directory. Do not change formal source, root EXE, DAT/PNG, Unity production, Scene, Prefab or nonbattle code; do not use computer-use or capture an interactive screen.

Exit: compile exit0, both offscreen render/readback calls exit0, source sprite geometry confirms pic64 `(320,480,79,79)`, both PNGs have correct viewport dimensions, and offline pixel analysis identifies actual differences in the projected slot0 region with transparent/opaque behavior separated from background and HUD. If renderer cannot initialize or pixel evidence is ambiguous, retain artifacts and report limits. This is paired-source GPU evidence, not root formal EXE GPU evidence or natural OID32 reachability. Rollback of the new diagnostic C++ source only follows protected-worktree rules; preserve evidence.

Result: the paired-source diagnostic compiled and ran with exit 0. Both 1333×730 PNGs were read back; action95's exact 79×79 actor region is 6,241/6,241 white pixels. Against action0, the RGB difference is exactly that region (6,241 changed inside, 0 outside). See `artifacts/diagnostics/NTSD28-Q07-OID32-PAIRED-OFFSCREEN-PIXEL-001/REPORT-20260925.md`, geometry CSV and pixel JSON. Root release EXE direct GPU, Unity same-state entity pixels and natural action95 reachability remain open; no production fix is authorized by this diagnostic alone.
