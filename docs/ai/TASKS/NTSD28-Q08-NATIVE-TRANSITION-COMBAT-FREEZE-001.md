# NTSD28-Q08-NATIVE-TRANSITION-COMBAT-FREEZE-001

Status: `FOCUSED_TEST_PASS / ISOLATED_SELFCHECK_PASS / HOST_PENDING`. Parent BATCH-04/Q08. Formal authority: `source/ntsd28_playable/src/game_session.cpp` `GameSession28::step` returns after the precombat BattleFlow transition command and before `tick_driver_.step`; `battle_flow.cpp` keeps ordinary command2 and distinct mode2/3/4 commands28/128/202. Double-run formal source fixtures and Unity mode4 full-tick RED are in `artifacts/diagnostics/NTSD28-Q08-RESULT-TRANSITION-HOST-AUDIT-001/REPORT.md`.

Exact script scope: `Assets/NTSD/Scripts/Simulation/Core/NTSDBattleTickSystem.cs` and `Assets/NTSD/Scripts/Test/Editor/NTSD28Q08BattleFlowRedProbeEditorTests.cs`. Preserve the prior Q08 result-page input isolation and held-continue changes. Let the native precombat classifier run first, then on a nonzero transition command skip the entire combat pass chain, world-clock, result UI writer and combat ECS shadow refresh for this and later old-world ticks. Return a successful host-step completion to the existing worker path. Keep snapshot/checksum and lifecycle data intact; no Scene, DAT, image, nonbattle, GAS or menu change. Add ordinary command2→1 and frozen old-Results-write focused cases, while reusing the archived mode4 349→350→following RED. Do not consume transitions or invent a route for 28/128/202 here; a separate host Task must route them before Q08 exit.

Validation: isolated Unity focused Q08 transition cases, adjacent result-page input and held-continue tests, one stable full SelfCheck, Change Ledger, diff and Scene hash. `SimulationTickDriver` currently applies `FrameInputSet` and increments its host tick before entering this core method; this task does not claim full host/input-carrier freeze. Original Editor/Player real battle, same-seed replay, host input/transition routing and Q08 exit remain pending. Rollback only this change's core/test hunks under repository approval rules, preserving all other user work.

Observed: additional isolated Unity RED0/2, target postchange transition3/3, combined Q08/Results input17/17, fresh full SelfCheck PASS. Exact evidence and still-open host boundary are in `artifacts/diagnostics/NTSD28-Q08-RESULT-TRANSITION-HOST-AUDIT-001/TRANSITION-COMBAT-FREEZE-ACCEPTANCE.md`.

2026-09-25 original-project focused follow-up: the existing original Unity Editor (PID 11944) ran exactly the three core transition tests—mode4 350/following freeze, ordinary command2→1 without old-world advance, and no old Results settings write after transition. Job `0a164411360a4854a799026c81256e40` finished 3/3 passed, 0 failed/skipped. Full result: `artifacts/diagnostics/NTSD28-Q08-RESULT-TRANSITION-HOST-AUDIT-001/original-editor-transition-core-three-tests-20260925.json`, SHA-256 `E1059BAD07B014A4659432D152234D0B710AE7DE0E6084804B24FD4978DDA134`. Menu/Battle Scene disk SHA-256 remained `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13` / `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`. The core scope now has original-project EditMode evidence, but natural KO, host command consumption, Player and formal EXE-visible parity remain open; parent Q08 status is unchanged.
