# NTSD28-USER-SOURCE-COORDINATE-PARTICIPANT-BIRTH-001

Status: RUNTIME_PENDING / FOCUSED_TEST_PASS / CARRIER_NOT_ACTIVE. Parent D-024 all-entity displacement ratio audit. This batch only covers the active `AppManager.SetupBattleCharacters` initial participant placement in the current battle host.

2026-09-24 execution: minimal production placement-sync seam extracted without behavior changes, original Editor RED8748c0dd failed2/2 on absent source initialization; adding raw absolute source precise/int initialization yielded GREEN41651899 2/2. Both view configurations and repeated negative/positive placement, physical values, velocities/HitStun and RNG preservation passed. Compile/domain reload and ledger747/whitespace/Scene/resource checks passed. See Record for full job IDs. True menu-to-Battle Play remains required; no complete host-route parity claim.

Authority: paired playable `GameSession28` builds participant `SpawnRequest28` from selected absolute stage X/Z, then `BattleWorld28::spawn_at` initializes integer and precise positions from those absolute values. Current Unity `AppManager.SetupBattleCharacters` writes the already chosen absolute `spawnX/spawnZ` to `lf2.PS`, syncs battle integer position and refreshes the snapshot, but does not initialize the independent source-rule X/Z. This leaves normal menu-to-battle participants without a source history for later raw-distance rules. This package does not change stage random selection or claim complete initial-setup parity.

Exact script scope: `Assets/NTSD/Scripts/App/AppManager.cs` and `Assets/NTSD/Scripts/Test/Editor/NTSD28AppParticipantSourceBirthEditorTests.cs` (new focused test and Unity-generated `.meta`). Do not edit other scripts, DAT, character images, Scene, camera or ProjectSettings. Source-rule gameplay consumers remain disabled.

Implementation: keep all existing AppManager RNG draws, physical `PS` position, velocity, hit stun and snapshot order. At the existing integer-sync point, initialize source precise X/Z from the same chosen raw absolute spawn values and source integers by the existing integer policy. A small internal placement-sync seam may be extracted solely for an executable focused test; do not move placement into a new service or change Unity framework ownership. Reused participant instances must not inherit stale source values; existing pool reset remains authoritative.

Acceptance: test-first RED on an initialized participant placement with raw absolute X/Z, GREEN after production change, including factor1/configured-view independence and an existing source value overwritten by a second placement. Run original Editor compile and narrow test, ledger validator, diff check, Scene/resource scope check. A real menu-to-Battle Play follow-up is required before declaring host-route runtime parity, but a focused helper test may close this bounded writer package as `RUNTIME_PENDING`. Rollback only these two scoped scripts and generated test metadata.
