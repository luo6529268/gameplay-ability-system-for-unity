# NTSD28-Q08-RESULT-STAGE-COUNT-AUTHORITY-AUDIT-001

Status: VERIFIED_SOURCE_BOUNDARY_ONLY. Parent: BATCH-05 / Q08, with Q07 formal-content dependency. No script, Scene, asset, authority-file or build modification.

Question: does the formal Logan result settings stage action consume the 24 available background records, and is Unity `RuntimeStageCount=0` a verified battle-rule difference under the same mode/input? The Q07 static reference audit identified the zero count but had not traced the corresponding native result settings path.

Method and exit: trace the formal EXE-matched playable source from result timer through mode-specific transition, post-roster stage menu and its background IDs; trace Unity's exact `RuntimeStageCount` producer/consumer and existing test scope. Record which observed facts support an implementation and which require a same-mode formal EXE witness. Preserve the no-computer-use restriction and do not use a source test or synthetic Unity SelfCheck as an EXE behavior certificate.

Result: `artifacts/diagnostics/NTSD28-Q08-RESULT-STAGE-COUNT-AUTHORITY-AUDIT-001/REPORT.md`. The 24 IDs are confirmed for the native post-roster menu. The inspected playable result flow does not expose mode-4 result-settings stage action after transition state 202, so it cannot decide whether the Unity mode-4 result-settings count should be 24. Q08's count issue is `AUTHORITY_MATCH_PENDING`, not a proved first difference. Other Q08 behavior with direct authority witnesses can proceed independently.
