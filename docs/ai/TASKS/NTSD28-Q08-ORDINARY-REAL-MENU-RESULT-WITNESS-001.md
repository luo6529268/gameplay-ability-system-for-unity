# NTSD28-Q08-ORDINARY-REAL-MENU-RESULT-WITNESS-001

Status: `VERIFIED_SCOPED`. Parent BATCH-04/Q08 and `NTSD28-Q08-ORDINARY-RESULT-SELECTION-HOST-001`. Authority: formal root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable `GameSession28::step()` precombat transition branch (game_session.cpp 2683, 2720–2749, 3228–3251), and source result transition witnesses linked by the parent Task. P-19/G-08 keep the project's result/selection visuals as an approved exception while preserving battle-to-selection behavior.

Gap: the parent ordinary result host is covered by temporary-scene Play 3/3 and existing host EditMode checks, but its real original-project Menu callback → additive Battle → ordinary result → existing selection route has not been witnessed after Build Settings were set Menu first, Battle second. The existing `NTSD28Q07MenuSceneCallbackPlayProbeEditor` already exercises the real Menu callback chain and then manually unloads Battle; it can be extended opt-in to issue a canonical ordinary transition after the live Battle is ready, leaving its prior diagnostic route unchanged. This is a controlled result boundary, not a natural KO or a formal GUI recording.

Exact script scope: `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07MenuSceneCallbackPlayProbeEditor.cs` only. Add a separate new request and result path for an opt-in ordinary-result witness. Preserve the old Q07 request/result, its empty-root branch, callback sequence, and cleanup. In the opt-in branch set canonical native result phase3/timer350/transition2 on the real World after formal publication checks; wait for the production driver's Update dispatch, ordered Battle unload and `MenuSelectCharacter`; assert Battle Scene gone, existing selection view active, old driver destroyed, and pool borrowers zero. Do not edit production, DAT, images, Menu or Battle Scene, Build Settings, GameConfig, GAS or other nonbattle code. Any failure must still exit Play and restore the original saved Battle Scene.

Acceptance: create Task/Change/Ledger before script edit; original Editor compile, one unique Menu callback Play witness, exact result JSON, Scene/GameConfig SHA before/after, no old request overwrite, no C# compiler error, Change Ledger and diff checks. Success closes only this real-host controlled route; natural combat result, formal EXE GUI, mode2/3/4 routing, Player and Q08 aggregate remain open. Rollback is only the opt-in diagnostic hunk, subject to repository approval rules.

Result 2026-09-25: one original-Editor real Scene request PASS with formal prewarm, BattleRunning World2, canonical ordinary transition2, existing character selection active, old Driver destroyed, zero pool borrowers, original Battle Scene restored and all three protected disk SHA values unchanged. Raw JSON and limitations are in `artifacts/diagnostics/NTSD28-Q08-ORDINARY-REAL-MENU-RESULT-WITNESS-001/ACCEPTANCE-20260925.md`. Natural KO, Player and Q08 parent remain open.
