# NTSD28-Q07-WINDOWS-PLAYER-RUNTIME-001

Status: `VERIFIED_PLAYER_CONTENT_BOOTSTRAP_ONLY`. Parent BATCH-04/Q07. Prerequisites: staged formal content identity/full Editor publication/real App-menu Play, Windows Mono Player compile and 1,343-file sidecar build-output verification. The production GameConfig remains empty-root; this package verifies the built Player before any production asset switch.

Exact script scope: add `Assets/NTSD/Scripts/Test/NTSD28Q07WindowsPlayerRuntimeProbe.cs` and generated `.meta`. The probe is compiled only into a Development Player (not Editor or release), runs only with the explicit `-ntsd-q07-player-probe` argument, selects the bundled `Assets/NTSD/Content/LoganRuntime` root in memory before scene Start, then writes a unique JSON report beside the built EXE. It must assert formal content fingerprint, manager/data/UI publication key agreement, representative formal object IDs, a Running World with objects, ordered shutdown, no pool borrowers and zero tracked published resource survivors after unload. It must then exit the Player with a meaningful status. No GameConfig asset, Scene, prefab, production loader, nonbattle code, old resource or existing build output edit. If pre-scene source selection cannot be reached through loaded GameConfig, diagnose first and amend the exact Task/Record before touching another file.

Acceptance: new unique Windows Mono Development build includes the probe and exact 1,343 sidecar files; run that built Player with the flag using a hidden window and a unique report/log path; obtain a terminal PASS with the specified assertions, inspect exit code and log, confirm GameConfig asset/Scene hashes and ChangeLedger. A timeout or missing report is not a PASS; inspect the live process before stopping or retrying. Do not use computer-use. After success, production root switch and natural character skill/presentation returns still need separate Q07 work.

Risk/rollback: actual Player content loading may reveal missing raw dependencies or timing/lifecycle differences. Probe is inert outside explicit Development Player flag, but must not silently fall back to old content. Generated build output is kept intact; no cleanup or overwrite of prior runs. Remove the test script only under repository deletion approval rules.

Acceptance result (2026-09-22): unique Windows Mono Development build `q07-player-runtime-build-1` succeeded with 0 errors and 1,343 sidecar files. A hidden-window run from a separate fresh copy `q07-player-runtime-run-3` returned exit code 0 and terminal `PASS` JSON. Formal root, fingerprint `FD18D668...B008147`, three publication keys, World object count 4, 29,400 tracked owned resources, ordered shutdown through `RuntimeMapCleared`, two-frame Stopped, 0 pool borrowers and 0 resource survivors were observed. GameConfig asset remains empty-root and Scene SHA `BCD1047B...0E9FB6` unchanged. The Player log also reports 13 missing `NTSD/Sound/SFX_*` audio directories: audio packaging/playback is outside this DAT/image bootstrap result and must remain a Q10/portability return. Natural skills, visual parity, production root switch, other platforms and full Q07 exit are not covered.
