# Task Contract — NTSD28-B5-REMAINING-EXIT-AUDIT-011

> 状态：`VERIFIED / GOVERNANCE_ONLY / NATIVE_COMBO_RUNTIME_ROUTED / B5_EXIT_NOT_READY`
> 依赖：`NTSD28-B5-REDUCED-STATE2000-AWAY-DAMPING-PRODUCTION-001 / VERIFIED`

## 目标

在reduced state2000 attacker tail闭合后，继续从Authority reduced-hit余下尾部、unarmored与non-character
damage continuation、命中记录和命中后生命周期边界逐语句只读复核，唯一定位下一项production-reachable
B5首差；若已无B5行为差异，则形成B5退出门证据。

## 边界

- 只读当前正式EXE对应playable closure、Unity production/HitPlan、冻结内容catalog与既有B5记录。
- 不改C#、Scene、Prefab、Config、资源、ProjectSettings或Authority目录。
- 已验证的state2000 damping、system-table terminal、first-BDY、hit-group、special latch、kind4/kind8、
  armor/defense、rest/resource/damage/type3规则族不得无新证据重开。
- cpoint/抓持归B6，普通spawn/despawn归B7，纯音频/火花presentation归B10，内容替换归H。

## 验收

- 闭合state2000之后的reduced tail与相邻unarmored/non-character continuation顺序。
- 为候选差异确认Authority位置、Unity实际owner、正式内容可达性及既有Change覆盖。
- 唯一选出下一实现/owner-audit包，或给出B5 exit-ready清单；同步Ledger/STATE/handoff/总表。

## 回滚

仅移除本次治理记录；没有行为、内容或Scene回滚。

## 结果

下一首差为selected-mode native combo runtime：正式`data/mode/ntsd.dat`提供`bound=1/facing=1/respond=50/
caughtact=1`；每个applied ordinary type0 hit在hit consumer内生产count/last-tick，state9 settlement可追加生产，
并在C25后`CoreComboExpire`按inclusive边界清零。Unity缺专用entity/world carrier、producer和expiry；现有
input combo与`ComboCountAtk/Vic`均不是该字段，且`CoreComboExpire`只有顺序描述无执行。已路由独立owner audit。
