# NTSD28-USER-SOURCE-FUSION-DISTANCE-001

Status 2026-09-24: `FOCUSED_TEST_PASS / PLAY_PENDING`; original Editor X two-view2/2, adjacent formal records4/4, Z threshold1/1 PASS. D-024, Q07 paused.

Authority: formal root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; playable `BattleWorld28::advance_native_fusions` around `battle_world.cpp:2741-2870`, whose integer X/Z proximity `<50/<8`, order and integer midpoint use source `Position28`, while split copies the entire primary position to the restored partner. Existing formal fusion.dat row0 two-view original-Editor witness proved source X gap40 merges but physical X gap50 rejects under approved full-background travel.

Unity pre-change: `BattleOid5152RuntimeModule.TryMerge` tests physical integer X/Z, order and midpoint only; `TrySplit` copies physical position only. Initialized source-rule carriers are ignored. The test currently characterizes rather than fixes the confirmed first difference.

Declared script scope: `Assets/NTSD/Scripts/Simulation/Passes/Oid5152/BattleOid5152RuntimeModule.cs` and `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06FusionRecordTransactionEditorTests.cs`. When both participant source histories are initialized, evaluate formal proximity/order on source integers; retain physical midpoint for approved visual coordinates and write an independent source integer midpoint to primary precise/int X/Z. On split, copy primary source carrier to partner with the formal position copy. If source history is incomplete, preserve current physical fallback without guessing. No DAT/Scene/ProjectSettings/nonbattle changes or threshold rescaling.

Acceptance: row0 gap40/50 two-view merge and independent source/physical midpoint, split source position copy, existing record and rejection checks, original Editor focused NUnit. Full Driver/Play/EXE and all fusion records remain separate gates.

Rollback: review and reverse only declared script hunks, retaining other work.

Result: `TryMerge` now checks native `<50/<8` and slot order against source integer X/Z when both histories are initialized, retaining physical fallback when incomplete. It keeps the approved physical midpoint and separately writes the integer source midpoint into source precise/int X/Z. `TrySplit` copies primary initialized source position to the restored partner with the physical position copy. Existing formal row0 two-view test now merges at source X gap40 even when physical gap50, asserts physical midpoint320/325 versus source320, then verifies split copy. A new Z case merges at source gap7 despite physical gap8 and asserts physical/source depth midpoint254/253 through split. Original Editor jobs `14f7a1c593c44e0ba42e95056423f8e6` 2/2, `de2021edfa3f435697f1e9c6f46055b2` adjacent four records4/4 and `804c1b04d9324929bd26a61f3d8fd376` depth1/1 passed. Full Driver/Play/EXE and other source readers remain open.
