# NTSD28-Q07-SELFCHECK-FORMAL-GEOMETRY-001

Status: `VERIFIED_SCOPED / ORIGINAL_EDITOR_FOCUSED_AND_PLAY_PASS`. Parent: BATCH-04 / Q07. Purpose: remove `BattleRuntimeSelfCheck.CheckDeployableResolvedGeometryRisks` as a hidden dependency on the pre-migration `Assets/NTSD/Config/data.txt` and old DAT while preserving meaningful formal-content geometry coverage.

2026-09-25 scoped Play exit: original Editor imported the opt-in probe after a targeted local bridge `refresh_unity` script compilation request. In real Menu→Battle Play, the formal publication keys matched, `BattleRunning` and World2 were present, and the no-argument geometry SelfCheck passed with R-HC-01/02 counts. The existing ordered unload returned to Menu, stopped the Driver and left zero pool borrowers. The Editor restored the saved clean Battle Scene, Console errors0, and Battle/Menu/GameConfig disk hashes stayed unchanged. Raw result SHA-256 `6B569FFB5C03539DE0491FEC4CE9717C0FF19F0CAA55CCB4072042F4CF4AC391`; exact report `artifacts/diagnostics/NTSD28-Q07-SELFCHECK-FORMAL-GEOMETRY-001/PLAY-ACCEPTANCE-20260925.md`. This closes only this geometry SelfCheck source/Play subpackage, not Q07 content/skill/visual/EXE exit or old asset deletion.

2026-09-25 Play exit extension, before probe-script edit: use the existing original-Editor `NTSD28Q07MenuSceneCallbackPlayProbeEditor` Menu→Battle request path with one opt-in `geometrySelfCheckAudit` flag. Only after the formal-root publication keys and live Battle World are verified, invoke the no-argument private `BattleRuntimeSelfCheck.CheckDeployableResolvedGeometryRisks` by exact reflection signature. Record invocation success, run the probe's existing ordered unload/return and scene restoration, and preserve its default behavior when the flag is absent. This tests the production configured-root entry in Play; it does not expand the geometry method, change battle rules, DAT, scene, Menu logic or other resources. Acceptance: original Editor compile 0 errors, one unique opt-in request PASS with `BattleRunning`, source-key match, geometry invocation, Stopped and zero borrowers; saved Battle/Menu/GameConfig SHA unchanged and focused aggregate/ledger checks. On failure keep the raw report and diagnose before altering production. Rollback only the probe opt-in hunk under repository approval rules.

2026-09-25 dependency update: the subsequent `NTSD28-Q07-SELFCHECK-LEGACY-DAT-FIXTURES-001` retired the other audited old Config DAT callers from `RunAll`; complete original-Editor `BattleRuntimeSelfCheck` re-ran to `PASS` after an EditMode-only configured-root fallback. This satisfies the earlier aggregate Editor dependency, but does not supply production Play evidence for the no-argument geometry entry. This Task remains `RUNTIME_PENDING`; formal DDJ skill-chain and Q07 aggregate gates stay open. See the subsequent Task/Record for the exact failure and pass artifacts.

2026-09-25 latest: after one compile error and one EditMode GameConfig precondition failure were retained, the actual original Editor formal/staged measurement passed 1/1 (`c1a70998299c40cda86d32f7d6a319c2`) and the source-injected common geometry SelfCheck implementation plus measurement passed 2/2 (`ecb21a677ae84970a953407559eb3f7b`). The production no-argument entry now selects the configured Logan root and project mode Asset; scoped old Config references in this method are zero. No DAT/Scene/ProjectSettings diff; Ledger 798 Records/4 code files PASS. The no-argument configured entry still needs Play evidence, and other legacy fixture readers block the aggregate SelfCheck exit.

Authority and evidence: D-023 selects the formal NTSD 2.8-Logan DAT and character-related images, excluding original background and both mode DAT families. The current caller analysis is `artifacts/diagnostics/NTSD28-Q07-OLD-ASSET-REFERENCE-REFRESH-001/EMPTY-ROOT-RETIREMENT-OWNER-MATRIX-20260925.md`; `FORMAL-RAW-GEOMETRY-INDEX-20260925.json` records native source-model authored counts and SHA-256. This diagnostic is not a Unity runtime projection or authority for a new game rule.

Current defect: `RunAll` invokes `CheckDeployableResolvedGeometryRisks`, which calls `ParseCharacterFrameConfigs(dataManager)`. That method checks and reads old `Config/data.txt` and resolves DAT paths against old Config. `GameDataManager.LoadDataFile` returns early if an object list is already present, so a formal publication can leave formal definitions paired with old DAT resolution. The check also requires exactly five old x39/y-555/w21/h-999 inverted bdy entries and no other nonpositive itr shape. Formal indexed authored data has 0 positive-width/negative-height bdy and 109 nonpositive itr entries; Unity converter/cache counts must be observed before updating assertions.

Pre-change scope: read the complete formal `LoganObjectCatalog` through the existing `BattleContentSource.ForLoganRuntime` and `ProjectBattleModeConfig` path; use `CharacterAnimtorManager.BuildCharacterFrameConfigsFromCatalog` and `LF2FrameCache` to measure the check's **actual resolved-frame** domain, including duplicate frame IDs, 0..856 accessor range and `HasGeometry`. Capture explicit counts, exemplar paths/OIDs and parser failures for both formal source and staged copy. Identify which R-HC-01/02 assertions describe battle-rule behavior and which describe obsolete content values. Write a focused Editor test before changing the SelfCheck method.

Candidate implementation boundary, contingent on the measurement: change only the SelfCheck source selection and content-specific assertions in `Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs` and a focused test in `Assets/NTSD/Scripts/Test/Editor/`. Preserve `CheckZeroDimensionCollisionGeometry` and `CheckNegativeHeightCollisionGeometry` synthetic rule cases. Do not silently skip malformed formal geometry, weaken a rule invariant to a tautology, edit DAT tokens, add original background/mode DAT, or alter production battle logic. No code is authorized by this Task alone; create a precise Change Record, Ledger/STATE/handoff entry and accurate method list before any script edit.

Exit: original Editor compiles with zero errors; focused formal-source geometry test passes with exact measured counts/paths, staged copy matches formal source, and full `BattleRuntimeSelfCheck` executes without reading old Config DAT. Verify its other existing checks remain green, then use a direct old-reference scan and Scene SHA check. This closes only the SelfCheck retirement dependency; Menu empty-root, Editor previews, historical fixture tests and old asset deletion remain separate Q07 gates.

2026-09-25 exit correction: the preceding full-SelfCheck wording is an **aggregate dependency**, not an exit attainable by this geometry-only Task. A current `RunAll` callsite audit found three other checks using `LoadProductionDatWrapper` to decrypt old Config DAT, including an old OID205 six-clone assertion absent from the formal index. This Task's local exit is formal geometry source selection, measured assertions and focused test only. Full SelfCheck without old Config additionally depends on `NTSD28-Q07-SELFCHECK-LEGACY-DAT-FIXTURES-001` and a fresh aggregate run. Detailed evidence: `artifacts/diagnostics/NTSD28-Q07-OLD-ASSET-REFERENCE-REFRESH-001/SELFCHECK-LEGACY-DAT-CALLER-AUDIT-20260925.md`. No script edit under this correction.

2026-09-25 measurement-route check: a read-only PowerShell reflection call against the current `Library/ScriptAssemblies/Assembly-CSharp.dll` constructed `BattleContentSource.ForLoganRuntime` for the staged root and `LoganObjectCatalog.Read(source, null)` returned 330 entries. This null-snapshot call is a diagnostic path, not the configured production mode-Asset route. The next reflection call to `CharacterAnimtorManager.BuildCharacterFrameConfigsFromCatalog` failed for all 330 objects outside Unity because frame conversion logs via `UnityEngine.Debug.Log`, whose `Internal_Log` ECall cannot run in the external .NET process. First failure was OID901 and the exception's stack pointed to `Lf2DatConverter.ConvertFrameDataCore` line 174. Thus the catalog count is observed, but **no Unity converted geometry count was obtained**. The required measurement remains a focused test in the original Editor after governance recovery; the offline source-model counts above stay predictions.

2026-09-25 measurement correction: the preceding external ECall failure was resolved **only in the isolated diagnostic process** by disabling its Unity `Debug.unityLogger` before conversion. The current Unity `Assembly-CSharp.dll` then converted all 330 formal and all 330 staged objects with the same default project-mode snapshot, and `LF2FrameCache` returned matching counts: 55,348 authored/unique in-range frames, 282,810 accessor visits, 19,461 itr, 86,383 bdy, 27 kind-0 zero-width/height-79 geometry lines, 82 kind-100100 no-geometry control records, zero nonpositive bdy and zero out-of-range frames on both roots. Full method/result/limit report: `artifacts/diagnostics/NTSD28-Q07-OLD-ASSET-REFERENCE-REFRESH-001/UNITY-ASSEMBLY-FORMAL-GEOMETRY-TWO-ROOTS-20260925.md`; paired JSON alongside it. This supersedes the earlier statement that no converted count was obtained; it remains **offline Unity-assembly evidence**, not original Editor focused test or full SelfCheck. The old five-inverted-bdy and no-other-nonpositive-itr content assertions are now disproven for the selected formal source; the synthetic collision-rule assertions stay in scope.

Focused Editor test design after the damaged progress docs are recovered: use the actual `ProjectBattleModeConfig.asset` snapshot and the formal `BattleContentSource.ForLoganRuntime` production converter/cache path, then compare staged and formal roots. Assert 330 configurations, no parser failures/out-of-range frames, 19,461 itr and 86,383 bdy, and classify all 109 nonpositive itr by `(kind,w,h,hasGeometry)` into the measured 27 valid zero-width kind-0 lines and 82 missing-geometry kind-100100 control records. Assert zero nonpositive bdy. Keep a separate synthetic collision overlap test for zero-width and raw inverted bodies. Include a focused kind-100100 dispatch/candidate check against formal `CollisionGeometry28` control-only semantics before replacing the old content assertion; do not merely filter the 82 out to make a count green. This is a proposed test contract, not a passed Editor result or authorization to edit scripts before Change registration.

2026-09-25 candidate-path audit: the 82 control records are a distinct production gate, not just a SelfCheck count. Native `CollisionGeometry28` marks incomplete kind-100100 ITR as `control_only` and its candidate collector skips it. Unity conversion preserves `hasGeometry=false`, but `BruteForceSceneQuery.IsReleaseItrGeometry` currently tests only non-null; broadphase and pair collection can therefore admit the record. Even a zero-size ITR may pass the current strict overlap formula when its point lies inside a body. The **static admission difference is confirmed**; a specific runtime false-hit is still unproven. Details and the exact original-Editor/native witness contract: `artifacts/diagnostics/NTSD28-Q07-OLD-ASSET-REFERENCE-REFRESH-001/CONTROL-ONLY-ITR-CANDIDATE-AUDIT-20260925.md`. Preserve the 27 complete zero-width kind-0 ITRs and test both collector profiles before any production correction.

2026-09-25 compiled-assembly witness: the current `Assembly-CSharp.dll` SHA `5EF4A473DF1C99FCDF929E01349C3008C5CBA8C0508BB538E8AA0FDDDE266184` returned `true` from the production `IsReleaseItrGeometry` for a `kind=100100,hasGeometry=false` record and `true` from production `Overlap` for a zero-size point strictly inside a target body. This is stronger than source reading but does not prove a final candidate or hit; the focused original-Editor and native same-state gate remains. No script, DAT or Scene edit was made.

2026-09-25 downstream correction: the current compiled disposition resolver returns `Unsupported` for kind-100100, and the candidate consumer exits before damage. The identified gap is candidate admission/storage (20-entry attacker row) and possible ordering/capacity/observation effects, **not a proved direct damage event**. The original-Editor gate should assert both candidate count and any store/consumer side effects; see the linked audit report. Preserve this distinction when later updating the alignment total.

2026-09-25 native focused baseline: the unchanged formal `ntsd28_core/scripts/build.ps1 -Target hit_candidates_tests` built successfully, then `hit_candidates_tests.exe` exited 0 with `hit_candidates_tests: PASS`. Its control-only fixture preserves the authored ITR index while skipping candidate creation; neighboring capacity checks also passed. Exact source/executable hashes and limits are appended to `CONTROL-ONLY-ITR-CANDIDATE-AUDIT-20260925.md`. This is native source-test evidence only; Unity original-Editor candidate collection and formal EXE-visible parity remain open. No Unity script, DAT or Scene edit occurred.

2026-09-25 actual-corpus correction: all 82 incomplete kind-100100 records are last in their authored frame; 74 Puppet5 frames have a preceding complete kind-8 ITR, and eight frames in Orochimaru/Sasori/KonanAngel have only the control ITR. The proposed control-first capacity fixture is synthetic and cannot stand for an observed natural order. Run the original-Editor collector witness first on actual OID854/frame407 (kind8 ordinal0, control ordinal1) and a control-only authored frame; keep the 20-slot synthetic case as a separate stress control. Per-frame machine evidence and scope caveat are in `CONTROL-ONLY-ITR-CANDIDATE-AUDIT-20260925.md`.

2026-09-25 geometry-only fixture coordinates: the linked candidate audit now derives a concrete pair using formal Puppet5 frame407 on both entities: right-facing attacker `(400,300)` and independently grouped target `(361,270)`, equal collision Z. The control record projects to point `(361,221)` inside the target's first BDY `X[350,377],Y[206,271]`; the complete kind-8 ITR remains remote. This is a calculated test setup, not a passed collector or natural Play result. The Editor test must still verify group/state gates, both collection profiles, authored ordinal and candidate store before a production fix is justified.

2026-09-25 offline projection addendum: the SHA-recorded native parser JSONL was mapped through the current `LF2FrameCache` 0..856 and last-frame-wins source logic. The prediction for 330 formal indexed objects is 55,348 authored unique frames in that range, 86,383 bdy and 19,461 itr entries; 109 nonpositive itr split into 27 complete kind-0 zero-width/height-79 lines (OID 419/411/422) and 82 kind-100100 records lacking complete geometry fields (OID 854/20/51/54). Positive-width/negative-height bdy remains zero. The current SelfCheck's `GetFrameDataById` returns `EmptyFrame` for absent IDs, so its `resolvedFrameCount` would visit 857 slots per loaded definition (282,810 if all 330 load), not merely authored frames. Machine counts/groups are in `FORMAL-RAW-GEOMETRY-INDEX-20260925.json`. These are still **predictions**, not a Unity converter/cache run; the focused test must measure and compare them before changing assertions, including whether `hasGeometry=false` kind-100100 records should be excluded from geometry validity checks.
