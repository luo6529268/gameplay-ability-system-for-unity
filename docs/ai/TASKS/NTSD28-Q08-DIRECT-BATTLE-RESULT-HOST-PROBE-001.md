# NTSD28-Q08-DIRECT-BATTLE-RESULT-HOST-PROBE-001

Status: `VERIFIED / DIAGNOSTIC_SCOPE_ONLY`. Parent BATCH-04/Q08. Isolated real Battle Scene Direct3D Play target passed 1/1 after formal content bootstrap; command2 stayed pending and old tick frozen with bootstrap AppManager MenuMain/no Menu Scene. See `artifacts/diagnostics/NTSD28-Q08-RESULT-TRANSITION-HOST-AUDIT-001/DIRECT-BATTLE-RESULT-PLAY-ACCEPTANCE.md`. Current-authority first battle-only transition emits command2 and recreates the battle on the following host call; the separately witnessed second result enters selection. Unity's direct `NTSD_Battle` scene uses `BattleTestBootstrap`, whereas the newly implemented ordinary command2 receiver requires an actual loaded Menu scene and AppManager BattleRunning/Paused.

Exact governed path: new `Assets/NTSD/Scripts/Test/Editor/NTSD28Q08DirectBattleResultHostPlayModeTests.cs`. Test only; no production code, original Scene, GameConfig, DAT/image, nonbattle UI or GAS change. Use the isolated Unity project `I:/GitHub/Unity_GAS/ntsd-q07-isolated-validation-20260922` with its own Library and the real copied Battle Scene. No computer-use.

Test: open the actual Battle Scene in isolated Editor, enter Play through the EditMode test runner, wait for `BattleTestBootstrap` to create AppManager and resume the driver with characters, record Scene/App state and tick, seed canonical native result phase3/command2 at a completed boundary, then observe several rendered frames. Assert the old World stays frozen and command remains pending without a Menu scene; report the observed App state and resource/bootstrapping status. This is a seam probe, not a natural KO/first-rematch acceptance. Run only this targeted test, capture XML/log, original Scene SHA, diff check and Change Ledger.

Do not infer battle-only from Menu absence or implement a rematch in this diagnostic Task. The follow-on implementation requires an explicit mode owner, exact first-rematch RNG/input-phase contract, old World shutdown/new World birth, second-result route and Player validation. Rollback only this new test path under repository rules.
