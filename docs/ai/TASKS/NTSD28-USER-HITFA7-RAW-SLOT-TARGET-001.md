# NTSD28-USER-HITFA7-RAW-SLOT-TARGET-001

Status: `FOCUSED_TEST_PASS / FULL_SELFCHECK_PASS / EXE_EMPTY_SLOT_PENDING`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001`; it arose from the D-024 requested non-perceptual audit and full SelfCheck failure. Formal root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033` was rechecked this turn. The paired playable `NativeAi28::step_non_character_hit_fa` behavior7 performs two 0.7 X comparisons against a selected target slot. Its inactive-target branch notes that native retains a non-sentinel slot pointer while the standalone model releases inactive storage and therefore reports unresolved target context rather than inventing values. `BattleWorld28` has additional native inactive-slot zero-storage commentary. Unity's raw slot table retains a reset `NTSDEntityRuntime` for addressable empty slots, and the adjacent `hit_Fa=4` path already reads it.

Current Unity first difference: `LF2Entity.RunNonCharacterHitFa7FrameLogic` resolves only `FindEntityByRuntimeSlotForQuery` and returns when it is null. Full `BattleRuntimeSelfCheck` stopped at FL-05 with source X100, preassigned empty target slot10, `Vx=0` versus its existing expected `-1.4`. This expectation is source-comment/self-check evidence of raw-slot behavior; an actual formal EXE runtime probe for this empty slot has not yet been obtained. Do not claim formal EXE equivalence from a green Unity test alone.

Declared scripts: only `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07NonCharacterHitFa7EditorTests.cs` and `Assets/NTSD/Scripts/Animation/LF2Objects/LF2Entity.cs` (`RunNonCharacterHitFa7FrameLogic`). Test-first: use staged official Logan OID875 frame55 (`hit_Fa:7`) in a registered World, subject absolute X100/Y-30/Z0, preassigned slot10 empty and reset raw slot position0. Assert exact one-step Vx=-1.4, target slot unchanged, no clone, and compare with the existing active-target case. Capture RED before production. Production: when the target slot is addressable but has no active query entity, consume its raw runtime position XInt/ZInt through existing `GetRawRuntimeSlotState` rather than returning; preserve invalid/sentinel early return, two X increments, Z dead-zone, Y, clamp, facing, action and pool reset semantics. No actor-specific, DAT, camera, Scene, image or nonbattle edits.

Acceptance: original Editor compile and only the new official-content empty-slot case plus existing active-target case pass, then full SelfCheck rerun to establish whether FL-05 clears and identify the next stop if any. Verify no DAT/Scene/ProjectSettings changes, Scene hash unchanged, `git diff --check`, Ledger validator. If full SelfCheck still fails later, record exact next failure without broad repeated runs. Rollback reverses only the one new test method and the null-target raw-position fallback in `LF2Entity.cs`, preserving other dirty work. This does not implement the D-024 source-rule coordinate carrier, fix OID219/fusion, or close Q07.
