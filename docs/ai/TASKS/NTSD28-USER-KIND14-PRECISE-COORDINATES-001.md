# NTSD28-USER-KIND14-PRECISE-COORDINATES-001

2026-09-24 correction: this Task's precise-operand authority claim is superseded by `NTSD28-USER-KIND14-INTEGER-OPERAND-AUTHORITY-CORRECTION-001`. Formal playable kind-14 compares integer `Position28::x/z`; the old focused results tested the wrong expected rule. Keep them as historical test observations only.

Status: `FOCUSED_TEST_PASS / FULL_SELFCHECK_FAILS_EARLIER_FL05 / D024_HISTORY_OPEN`. Parent `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (D-024). Exact first difference: original-project Editor job `cbe5118f13804bc0ad603aa023dba18d` ran the existing candidate and production kind14 dispatch at attacker precise/int X5.5/5 versus victim0 with Vx2; it published no positive-X block, whereas shipped playable `BattleWorld28::resolve_special_relation_hit` compares precise X and would set the flag because 5.5>5. Control X6.5/int6 did set the flag. Both characterization cases 2/2 PASS; they are defect evidence, not parity.

Declared code scope: `Assets/NTSD/Scripts/Simulation/Ecs/Writers/BattleBoundaryWriter.cs` active registered writer; `Assets/NTSD/Scripts/Simulation/Ecs/Hit/BattleEcsHitExecutionPlan.cs` ShadowCompare writer projection; integer-coordinate fallbacks in `Assets/NTSD/Scripts/Animation/LF2Objects/LF2CharacterDatHitResolver.cs`, `LF2Entity.cs`, and `LF2SpecialAttack.cs`; the focused threshold test and one direct self-check invocation method in `Assets/NTSD/Scripts/Test/Editor/BattleHitExecutionPlanEditorTests.cs`; and the existing kind14 C32 assertion block in `Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs`. The latter still explicitly expects truncated XInt/ZInt at precise X5.9/Z2.9 and must be corrected to the formal precise rule. A full self-check request in this package stopped earlier at unrelated `FL-05 hit_Fa=7` before reaching C32; the extra method invokes the existing `CheckSpecialAttackHitResolveAuditContracts` directly so C32 is actually exercised. `LF2CharacterHitResolver` already compares precise bound `PS.x/z` and needs no change. No other files or rules in this package.

Change only kind14 X/Z position operands from battle integer mirrors to precise battle positions, preserving strict ±5/±2 constants, velocity/pending-impulse predicates, flag publication and consumption, candidate/pass ordering, DAT contents, D-024 physical-position scale and Unity framework. Make the characterization case assert formal expected flag at X5.5 first to obtain a production RED, then update all declared active/fallback/projection branches. Strengthen the same focused method with X positive/negative and exact-threshold controls plus Z positive/negative strict fractional cases, then rerun these cases and the existing kind14 ShadowCompare control. This corrects the independent precision defect at factor1 and applies to all kind14 entities; it does **not** implement source-rule coordinate history for D-024 scaled travel. The latter needs a separate atomic carrier package with counterfactual source-domain flags.

Acceptance: original Editor compile, exact focused X5.5/X6.5 tests GREEN with candidate/dispatch and ShadowCompare validity, existing wide-separation kind14 test GREEN, ledger validator PASS and no Scene/DAT/ProjectSettings change. If a declared branch proves unreachable, retain the source-based semantic correction without inventing further behavior; record runtime coverage limits. Rollback: reverse only the exact operand substitutions and the focused test assertion in this package, preserving all unrelated dirty work. No irreversible action.
