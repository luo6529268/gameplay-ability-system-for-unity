# NTSD28-Q08-ORDINARY-RESULT-SELECTION-HOST-001

2026-09-25 further original-Scene witness: `NTSD28-Q08-PHYSICAL-ATTACK-REAL-MENU-KO-001 / VERIFIED_SCOPED` uses real P1 physical J and the unchanged formal Naruto frame513 ITR to KO a controlled adjacent HP10 opponent. Native KO event source/credit/four-owner identify P1 slot0, and the production result timer/ordinary selection return passed in the same Menu→Battle session. This strengthens the prior synthetic-attacker result-chain proof but still leaves normal wall-clock human match, formal EXE same-input visible outcome, Player and Q08 aggregate open. See the new witness `ACCEPTANCE-20260925.md`.

2026-09-25 additional controlled witness: `NTSD28-Q08-COLLISION-TO-ORDINARY-REAL-MENU-WITNESS-001` joined two actual Menu teams in the original Editor and used a test-only full-tick collision to KO the second participant without assigning result fields. Timer 0→1→80→101→350, transition2 and production selection return all passed, with ordered unload and borrowers0. This closes the previous gap between controlled collision and original-Scene host routing, but natural player attack, formal EXE GUI and Player remain pending. Parent stays `RUNTIME_PENDING`; see the new witness `ACCEPTANCE-20260925.md`.

2026-09-25 original-Scene correction: `NTSD28-Q08-ORDINARY-REAL-MENU-RESULT-WITNESS-001` used the saved Menu callback chain and additive Battle Scene in the original Editor. After formal publication and BattleRunning World2, a canonical ordinary result2 was injected; production host returned to existing character selection, unloaded Battle, destroyed the old Driver and left zero pool borrowers. Scene/GameConfig hashes remained unchanged. This removes the earlier original-Scene routing evidence gap for a controlled transition only. Natural KO, formal EXE GUI, Player and Q08 aggregate remain pending; the parent status is still `RUNTIME_PENDING` for those wider outcomes. Evidence: `artifacts/diagnostics/NTSD28-Q08-ORDINARY-REAL-MENU-RESULT-WITNESS-001/ACCEPTANCE-20260925.md`.

Status: `RUNTIME_PENDING`. Parent BATCH-04/Q08. Temporary-scene Play 3/3, adjacent host/Q08/Results 28/28, full SelfCheck and Change Ledger PASS; original Menu-to-Battle route remains unverified with the current empty Build Settings scene list. Formal authority: `GameSession28::step()` result timer350 emits ordinary transition2, skips combat, and the next upper-state pass enters selection state1; complete-session ordinary and battle-only source witnesses are recorded in `artifacts/diagnostics/NTSD28-Q08-RESULT-TRANSITION-HOST-AUDIT-001/REPORT.md`. P-19/G-08 permit the existing Unity selection presentation while requiring the battle-to-selection request. The existing combat-core freeze and driver admission guard already preserve the old World until host ownership.

Exact governed paths: `Assets/NTSD/Scripts/Simulation/Host/SimulationTickDriver.cs`, `Assets/NTSD/Scripts/App/AppManager.cs`, and new `Assets/NTSD/Scripts/Test/Editor/NTSD28Q08OrdinaryResultSelectionHostPlayModeTests.cs`. The test creates temporary Menu/Battle scenes in PlayMode, a live AppManager, MenuUIController and SimulationTickDriver, marks a canonical ordinary result transition2, waits for the production Update handoff, then checks one ordered unload, Battle Scene gone, `MenuSelectCharacter`, existing selection view active and old driver stopped. It also checks that command202 is not routed to ordinary selection, and that ordinary command2 without a loaded Menu stays pending rather than guessing a battle-only rematch. The prechange target should fail specifically because no production transition consumer exists.

Implementation: after synchronous tick or worker publication, on the main thread, dispatch ordinary command2 at most once through a battle-specific AppManager method. Require a loaded Menu and Battle Scene; call the existing ordered shutdown/unload pipeline and show the existing Unity character selection view after unload, resetting only the selection controller's current match state if needed. Do not change MenuUIController or CharacterSelectionController code, nonbattle menu flow, Build Settings, Scene, DAT, image, GAS or mode-specific commands28/128/202. Do not infer native `battle_scene_only_loop` from Menu presence; its explicit owner is a separate task. On refusal retain the command and frozen old World.

Validation: isolated Unity target RED and postchange PlayMode, focused host/Q08 EditMode, one stable SelfCheck, ordered shutdown postconditions, Change Ledger, diff and Scene hash. A real project Menu→Battle/Player route remains gated by the current empty `EditorBuildSettings.asset` scene list; do not silently change that list. Rollback only this task's three code/test paths under repository approval rules and preserve all user work.

2026-09-25 current-state correction: the preceding empty-Build-Settings gate is historical. User-confirmed `ProjectSettings/EditorBuildSettings.asset` now has enabled `NTSD_Menu` first and `NTSD_Battle` second, with GUIDs matching both Scene `.meta` files; SHA-256 is `8D621A077642B5154BA305861CA57DFB549FF4FDE5BC58F3E284536A5982F01E`, and the file has no current Git diff. Menu/Battle Scene SHA-256 remains `785F828C4E64182BEA214E4794B198E3C82E3C42002FDADD3932A7E061B81E13` / `2EE465D83C7169A0589447F437E37CAEFF3CC6F1BA6C3AAA55B8068F2B48B77A`. The missing scene-list condition no longer blocks original-project Menu→Battle result routing validation. That validation, natural KO, Player and formal EXE comparison have **not** thereby passed; status stays `RUNTIME_PENDING`. Do not edit the already-confirmed scene order for this Task.
