# NTSD28-Q08-ATLAS-BUDGET-SOURCE-BINDING-001

Status: `RUNTIME_PENDING`. Parent BATCH-04/Q08. Formal Logan content plans 145 2048² atlas pages (2,432,696,320 bytes) against the desktop `AtlasMemoryBudgetBytes` 536,870,912. The current Auto policy rejects a texture array but still allocates all ordered pages, and the isolated full-content Scene crashes at page119 even after per-page CPU assembly. Evidence: `artifacts/diagnostics/NTSD28-Q08-RESULT-TRANSITION-HOST-AUDIT-001/ATLAS-ORDERED-STREAMING-PARTIAL.md`.

Exact script paths: `Assets/NTSD/Scripts/Animation/Rendering/BattleRenderingDevicePolicy.cs`, `Assets/NTSD/Scripts/Animation/Manager/CharacterAnimtorManager.cs`, `Assets/NTSD/Scripts/Animation/Rendering/Editor/BattleCommonAtlasBindingEditorTests.cs`. The first adds a real `SourceTexture2D` effective atlas-policy mode for explicit selection and diagnostic reporting. The manager's Auto publication selects it only when the plan's estimated atlas bytes exceed the device's declared atlas budget; it retains preexisting source bindings after validating every character and common visual path. Explicit OrderedPages/TextureArray requests and within-budget Auto retain prior behavior. No new texture/sprite copy, no DAT/image/Scene/GameConfig/GAS/J: authority/nonbattle edit; staged ownership and cleanup remain with the existing publication. Source bindings must preserve descriptor identity, pixel rect/UV, source `Texture2D` object and command order; performance segment count may differ and must be reported.

Acceptance: focused policy/source-binding tests for over-budget Auto and explicit OrderedPages, existing common atlas tests, isolated real D3D11 formal-content Scene second-result test with test XML and process memory sampling, ordinary result adjacent test if the full Scene reaches it, battle rendering acceptance/representative pixel and ordered draw evidence, compile and ledger validator. A successful logical result assertion does not by itself prove full original-game pixel parity. If real Scene still fails, report the actual boundary and leave Q08 pending. Rollback only exact declared scripts after reviewed diff under repository rules.
