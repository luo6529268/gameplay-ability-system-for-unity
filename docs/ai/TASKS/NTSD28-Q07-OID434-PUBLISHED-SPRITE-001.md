# NTSD28-Q07-OID434-PUBLISHED-SPRITE-001

Status: `VERIFIED_SCOPED_PUBLICATION`. Parent: `NTSD28-UNITY-BATTLE-REALIGNMENT-001` / BATCH-04 / Q07; Q09 pixel presentation remains separate. Original Editor result and precise limits: `artifacts/diagnostics/NTSD28-Q07-OID434-PUBLISHED-SPRITE-001/ACCEPTANCE-20260925.md`.

Authority: formal root EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable `render_snapshot.cpp` visual resolver, formal `data/data.txt` OID434/type3 → `c/nar/a/ras.dat`, frame396 `pic:36`, and `ras.png` SHA-256 `21A69B52A561CA4773FFC8FDBFE0E5B23E43B6B0F966EAF76E13888C8E23FD1A`. Preserved formal/Unity controlled replay both have OID434/slot52/action396 at tick29. `OID518-VISUAL-OWNER-CORRECTION-20260925.md` explains why the DDJ OID518 birth is not a visible-image witness.

Exact implementation scope: add one opt-in Editor diagnostic file `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07Oid434PublishedSpriteProbeEditor.cs` and its `.meta`, plus this Task/Change/Ledger and results. Use the original saved Battle Scene and original Editor only. An independent create-new request under `Temp` enters Play; after formal prewarm and a live World, read existing `CharacterAnimtorManager.SpriteCatalog.TryGet(434,36)` without mutating it. Record the selected source sheet, entry key, pixel rect/dimensions, shared texture, central binding validity and current formal publication identity. Compare with the authored `ras.dat`/staged image identity. Leave request/output collision-safe and exit Play through the existing lifecycle. Do not add any manager/Scene object, change DAT/PNG, runtime, camera, menu or nonbattle behavior.

Exit: generated Editor project and original Editor compile with zero errors; one unique original-Editor Play result confirms or refutes OID434/pic36 production publication after formal prewarm, with exact JSON and SHA. Verify Edit-mode return, Console errors, Battle/Menu/GameConfig SHA and Scene clean. Run `Tools/Validate-ChangeLedger.ps1` and `git diff --check`. A PASS closes only the Q07 catalog-publication subgate; actual OID434 dynamic entity binding at tick29, renderer command, GPU pixels, natural physical input, Q07 and Q09 remain open. On failure report the first concrete missing condition and keep the task active. Rollback is limited to this new Editor diagnostic and its own request/result, subject to protected-file rules.
