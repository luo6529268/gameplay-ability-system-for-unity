# NTSD28-Q07-EMPTY-ROOT-BATTLE-ENTRY-001

Status: `FOCUSED_TEST_PASS / DIRECT_BATTLE_PLAY_PASS / GOVERNANCE_BLOCKED`. Parent: BATCH-04 / Q07. Authority: user D-023 selects formal NTSD 2.8-Logan DAT and character images; original/background/mode exclusions and the explicit ban on editing DAT values remain. Existing formal content publication/scene paths are the target, not old Unity `Config/data.txt`.

Scope correction before any script edit: the user's replacement `AGENTS.md` places menu/loading flow outside normal battle scope and requires prior approval for large or cross-module changes. The initial proposal covered both Menu and direct Battle; that proposal is superseded here. This Task now covers only the **test-only direct Battle bootstrap** and its focused test in the same `Test` module. `LoadingPrewarmController` and its optional empty-root mode remain a separately reviewed Q07 issue, not an implementation path in this Change.

Pre-change evidence: serialized `GameConfig.asset` selects `Assets/NTSD/Content/LoganRuntime`. `LoadingPrewarmController.PrewarmOnceCoreAsync` still executes old Config DAT/sprite tasks when that root is empty; `BattleTestBootstrap.LoadCharacterDataAsync` does the same for direct Battle test startup. The exact callers, SelfCheck and historical test dependencies are recorded in `artifacts/diagnostics/NTSD28-Q07-OLD-ASSET-REFERENCE-REFRESH-001/EMPTY-ROOT-RETIREMENT-OWNER-MATRIX-20260925.md`. Formal-root publication is already implemented and verified in scoped tests. No current formal release behavior authorizes substituting obsolete Unity content when the formal root is absent.

Exact script scope: `Assets/NTSD/Scripts/Test/BattleTestBootstrap.cs` (`LoadCharacterDataAsync` source selection only) and `Assets/NTSD/Scripts/Test/Editor/NTSD28B11SourceCacheCallerEditorTests.cs` (one focused missing-root case). No Menu/loading-flow script, Scene, Prefab, GameConfig asset, DAT, image, gameplay tick, GAS, HUD, sound or nonbattle behavior edit.

Intended behavior: direct Battle test bootstrap rejects an empty formal battle-content root early with a clear configuration error, before any old `Config/data.txt`/sprite read. A nonempty root continues through its existing formal prewarm and publication. This is a test-bootstrap content-selection boundary, not a new battle rule. Menu prewarm is unchanged. Keep `GameDataManager.LoadDataFile` and old assets present for separately owned SelfCheck/Editor fixture migration; no file deletion under this Task.

Acceptance: focused pre-change RED or direct call-chain evidence; after change, one empty-root focused test shows explicit failure and untouched legacy manager state, while the existing direct formal-root test passes. Compile the original Editor with zero errors; verify clean saved Battle/Menu Scene hashes, scoped `git diff --check`, and `Tools/Validate-ChangeLedger.ps1`. Menu code remains unchanged, so its prior formal Play evidence is adjacent only. Do not run a full character matrix for this test-bootstrap branch change.

Risk: intentional change to the previously supported empty-root historical **direct Battle test bootstrap**. Historical tests, SelfCheck, Menu prewarm and Editor preview remain separately owned and must be migrated before old asset removal. A missing root now surfaces an error instead of loading obsolete battle content; it must not cause a fallback or partial publication. Rollback: review provenance, then revert only this bounded script change using the repository-approved revert process; retain unrelated user work.

2026-09-25 validation-entry amendment before further script edits: the original Editor is running but MCP bridge is not listening and Unity CLI has no Pipeline connection. Extend only the already-owned `NTSD28B11SourceCacheCallerEditorTests.cs` with a request-file Editor TestRunner wrapper for the two exact test methods `DirectCaller_EmptyRootRejectsLegacyFallback` and `DirectCaller_LoadsTheSameConfiguredSource`. Reuse the repository's existing TestRunner request/callback pattern; fixed test names, request in `Temp`, result in `Temp`, no Scene or production behavior modification. Acceptance: original Editor consumes the request, reports 2 passed/0 failed with no unrelated test class, and then remains out of Play with Scene hashes unchanged. If the bridge returns, its direct filtered test route can be used instead. This amendment is part of this direct-entry behavior's focused validation, not a new general test framework.

2026-09-25 focused result: original Editor PID 11944 eventually consumed the request. Initial 1/2 run failed only because the existing formal-root test's shared helper lacked its separate PNG-alpha suite's `Temp` fixture; the new empty-root case passed. After recreating that fixture with its documented 6×2 RGBA layout, a new request ran the two exact tests **2/2 PASS**, with Scene hashes stable and no Scene Git diff. Original Editor Tundra builds succeeded and scoped script diff check exited 0. The complete first-failure/second-pass record is `artifacts/diagnostics/NTSD28-Q07-EMPTY-ROOT-BATTLE-ENTRY-001/original-editor-focused-20260925/ACCEPTANCE.md`. Live `STATE.md` remains NUL-truncated, so global Change Ledger validation fails and this package is not yet deliverable; direct Battle Play remains pending. Earlier `CODE_WRITTEN` and "request not run" lines above are historical snapshots.

2026-09-25 Play result: direct Battle in the original clean Editor Scene reached formal content prewarm, character setup, presentation enabled, driver resumed and bootstrap complete. Controlled stop returned to idle/out of Play; Scene remained clean and both Scene hashes stayed unchanged. Missing WAV prewarm warnings are Q10-owned and did not prevent this bootstrap. The read-only live-tick reflection attempt failed before execution on a Windows command-line limit, so no tick or entity count is claimed. Evidence: `artifacts/diagnostics/NTSD28-Q07-EMPTY-ROOT-BATTLE-ENTRY-001/original-editor-direct-play-20260925/ACCEPTANCE.md`. The earlier `DIRECT_BATTLE_PLAY_PENDING` line above is superseded for this bounded nonempty-root startup only. Governance and Q07 aggregate remain open.
