# NTSD28-Q07-SELFCHECK-LEGACY-DAT-FIXTURES-001

Status: `PLANNED / READ_ONLY_CALLER_AUDIT / NO_SCRIPT_EDIT`. Parent: BATCH-04 / Q07, after or parallel to `NTSD28-Q07-SELFCHECK-FORMAL-GEOMETRY-001` measurement. Purpose: retire the remaining `BattleRuntimeSelfCheck.RunAll` calls that directly decrypt and assert pre-migration Unity Config DAT while preserving their valid parser and gameplay-rule coverage.

Evidence and authority: `artifacts/diagnostics/NTSD28-Q07-OLD-ASSET-REFERENCE-REFRESH-001/SELFCHECK-LEGACY-DAT-CALLER-AUDIT-20260925.md`. Formal NTSD 2.8-Logan EXE/playable and its nonexcluded DAT/character images remain authority; background/mode DAT exclusions, project exceptions and the ban on DAT value edits remain. Old fixtures may document historical behavior but cannot set formal expected values.

Pre-change inventory: trace `CheckSpriteFileRangeParsingContracts`, `CheckMovementDatLoadingContracts`, `CheckNarutoDdjSixCloneProductionChain` and their shared `LoadProductionDatWrapper` to each old file, expected literal, parser behavior and runtime side effect. Compare the selected formal index and producer chain before revising any assertion. In particular, formal data.txt has no OID205 and formal Naruto frame272 has no OPoint; the old six-clone chain must not be relabeled as a formal regression without a new formal caller/trace. Keep synthetic parser/default tests separate from content-specific examples.

Implementation boundary, contingent on evidence: replace or isolate only those old-resource SelfCheck fixtures and their helper in `Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs`, with focused Editor tests as needed. Do not change production gameplay, DAT values, Menu/HUD, Scene, Prefab, GAS or audio. This Task alone authorizes no script edit; create a precise Change Record and register the active ID in Ledger, STATE and handoff before editing.

Exit: focused formal-source/fixture checks pass; no reachable `RunAll` path reads old Config DAT; both this Task and the separate formal-geometry Task have passed their scoped checks; then run the full SelfCheck once in the original Editor and inspect the actual result. Old-file deletion remains separately gated by the 521-file owner matrix and user authorization. If formal playable/EXE evidence does not define a counterpart to the old six-clone chain, retire that content-specific assertion with an explicit replacement coverage decision, not a guessed OID.
