# Q09/P-04 同 Z 正式武器完整 tick 像素见证

Status: VERIFIED_NATURAL_CENTRAL_GPU_ONLY. Parent: BATCH-05/Q09 P-04. Four independent original-Scene full-tick captures under one Scene SHA support the formal same-Z CentralOnly painter order; Legacy and formal EXE same-condition pixels remain Q09/P-04 work. See `scene-471396E7-series/ANALYSIS.md` and the preserved `PROGRESS.md` failure history.

Diagnostic correction before the next script edit: the first completed four-image same-Play run captured all images but found zero valid candidates under the original clean-background oracle. Independent PNG inspection found 463 nonclear baseline pixels within the two body bounds and nonclear content in both supposed single-weapon frames; these are not clean A-only/B-only references after a full tick. Do not relax the oracle to accept favorable pixels from contaminated references. The next test-only revision changes the natural branch to four **separate Play sessions** (`baseline`, `a`, `b`, `both`) from a fresh original Scene, each with exactly one complete tick and one actual central GPU image. All four sessions spawn the same two formal weapons in the same slot order; a variant moves each selected weapon to fixture X1100 and each unselected weapon to a valid distant reference X1400. This preserves entity/slot/RNG topology while clearing the fixture overlap region. It must record the Scene SHA and starting RNG identities for cross-run comparability. The existing controlled four-stage branch stays intact. Acceptance requires same Scene SHA/tick and physical slot identities across all four sessions, baseline clear and both single-sprite pixels present in projected body overlap, composite matching the later slot at discriminating pixels with no opposing counterexample, and each run's own cleanup/Play exit. This is a diagnostic-method correction under the same P-04 test-only objective; it does not alter production or DAT.

Authority: formal NTSD2.8-Logan playable `RenderSnapshot28` orders equal-depth entity commands by descending physical slot; the current Unity CentralOnly production path must preserve that painter order after a complete tick. This test does not claim formal EXE pixel parity.

Scope: extend only `Assets/NTSD/Scripts/Test/Editor/NTSD28Q09SameZFormalSpritePixelProbeEditor.cs` with a second, separately requested original-Battle-Scene diagnostic. Keep the existing controlled entry and result unchanged. Spawn formal OID120/121 through production OPoint factory, position both at the same X/Z, advance exactly one complete battle tick via the current production driver/worker diagnostic step with presentation enabled, and capture the actual resulting central plan and GPU image before any synthetic reference publication. Pause and take baseline/A-only/B-only reference images from the same post-tick sprites. Assert equal Z, surviving handles, submitted body commands, actual tick identity, projected overlap, and an overlap-only discriminating pixel oracle. Record whether the full tick used the dedicated worker or synchronous path. Never edit DAT, production code, Scene, camera enabled state, or nonbattle files.

Acceptance: original Editor compile 0 errors; one focused original Battle Scene Play returns a real-tick plan and at least one overlap pixel matching the later physical slot with no earlier/mixed counterexample; exact source PNG identity remains; scene disk SHA and Git diff unchanged after Play; camera state and temporary World/pool/pause state restored or Play safely exited with any incomplete in-session restoration explicitly reported. Inspect the saved images independently. A failing prerequisite is a real FAIL with the first cause, not P-04 completion. Legacy renderer and formal EXE same-condition A/B remain separate exits.

Rollback: revert only this diagnostic change by an explicitly authorized Git operation if needed; retain pre-existing probe and user work. No cleanup or deletion of unrelated artifacts.

2026-09-24 four-session exit correction: the original "baseline clear" and exact-color-only acceptance clauses were too strict for a stable nonwhite underlay and filtered sprite edges. They remain above as the pre-run contract, not a retroactive assertion that they passed. Four independent original-Scene sessions under one SHA returned `PASS_CAPTURE`; their complete overlap pixel set was analyzed with both bounded RGB compositing orders, not a favorable sample. Of 82 pixels where both single-weapon images differ from underlay and each other, all fit OID120-later within one 8-bit level, 44 reject the opposite order, zero prefer the opposite, and five blended-edge pixels explain why exact color equality alone would reject the image. The named artifact `scene-471396E7-series/ANALYSIS.md` gives the formula, region, identities and raw PNG hashes. This verifies the scoped natural-tick CentralOnly GPU order; Legacy and formal EXE same-condition comparison still belong to Q09/P-04, not this test-only package.
