# NTSD28-USER-CPOINT-THROW-POSE-VIEW-WITNESS-001

Status: `FOCUSED_TEST_PASS / TEST_ONLY`. Parent `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (D-024). Original-project Editor exact job `a75fa674dcb1434d9324c5a5fa351453` completed the two declared cases 2/2 PASS, 0 failed/skipped. Factor1 catcher/caught XInt148/159 and configured 173/184 retain raw local gap11; caught YInt-24/ZInt200 and throw Vx1.5/Vy-2.25. This closes one throw-position writer witness, not dual-coordinate carrier or full Driver/Play.

Authority: shipped Logan playable `BattleWorld28::advance_catch_relations` throw branch in `source/ntsd28_core/src/simulation/battle_world.cpp` writes caught integer X/Y from catcher integer position and raw local CPoint/frame center, mirrors precise X/Y and leaves caught Z unchanged. Unity's active `LF2Entity.RunCpointAdvanceStep10` -> `BattleCpointWriter.RunKind1` -> `ApplyThrow` performs a separate write transaction. The fixed full-background D-024 exception scales physical travel, not local CPoint offsets or DAT throw velocity.

Declared script path: only `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06CpointThrowRawBindingEditorTests.cs`, adding one parameterized test. Reuse unchanged formal source fixture `artifacts/diagnostics/NTSD28-Q06-CPOINT-THROW-NATIVE-RAW-BINDING-001/source/first.jsonl` row0 (`thrown=1`, catcher X100/Y-5/Z200; caught after static throw X111/Y-24/Z200). Use existing `MakeWorld` and `ShutdownWorld`, configure `1333×730` or `2048×1152`, move catcher by raw Vx48 through production `CharacterMechanics.StepBattleLogic`, synchronize catcher integers, then call actual `RunCpointAdvanceStep10` for the catcher. Expected catcher/caught XInt148/159 or 173/184, local gap11, caught YInt-24/ZInt200. Preserve raw `throwvx/throwvy/throwvz` values and input state; if RED, diagnose before production edits.

No production, DAT, character image, Scene, camera, ProjectSettings or nonbattle edit. Acceptance: original-project Editor compiles and both named focused cases pass. This witnesses one throw-position transaction, not full Driver/Play, landing damage, formal EXE visible output or dual-coordinate carrier. Rollback removes the one method.
