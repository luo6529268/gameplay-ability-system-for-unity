> 当前已VERIFIED限定范围；最终证据见同名Record。父frame离线事务已闭合，epoch已使用World恢复缺口另留Task；下一原Native reader迁移。以下为历史计划。

> **当前唯一恢复入口（2026-09-14 03:23Z）：** C25L-STATE18-SPAWN、LATE-OPOINT-DEPTH-AND-LIVES（generic基线限定）、OPOINT-TARGET-WORLD-INITIALIZATION、NATIVE-PENDING-PRE-C25-MOTION-GUARD、STATE18-RNG-EXCEPTION-FIXTURE、OPOINT-WEAPON-HP-BIRTH 六Record均已VERIFIED各自限定职责。原24回归终态22PASS/2FAIL仅weaponHp；修复后30/30通过含两个失败chunk5。最新full1514+未受影响direct1507合计3021向量0差异，分次证据明确记录；真实Play12+84+96、最终SelfCheck03:22:37Z PASS、有序关闭全0及两帧Stopped、CS0/Scene dirtyfalse/root14/hash bcd1047b…保持。没有运行中的Unity job或未收取exec，不再等待旧5a104/ce0f等job。
> **下一唯一Task：`NTSD28-Q06-NATIVE-FRAME-FULL-DRIVER-SOURCE-WITNESS-001 / READY_SOURCE_DRIVER_JOIN`**，服务于仍IN_PROGRESS的父NATIVE-FRAME-TRANSACTION-INTEGRATION：复用2676端点和既有state18/fragment完整组合成果，补高动作/成本回退连续完整driver、sound/previous078/lifecycle及真实Play证据；不得重做已关闭载体/碎片/本六修复。先核对正式GameSession options（尤其resource mode28=1）和现有FrameCase，准确新source CPP Record后才写。上一轮宣布高动作见证时因真实weaponHp差异暂停，尚未写这个runner。
> 已纠正旧归因：原276 non-RNG=252 pending位置+24普通OPoint weaponHp；独立C17例外546=273稀疏×两mode，未修改随机掉落生产。新增源210 witness明确kind2 positive hp覆盖weapon_hp；普通OPoint effect/continuation(type0/5+parent credit gate)/defend等仍在OPoint-REMAINING-CONSUMER-AUDIT（父frame后）中，不把generic1/0/0写成最终全部生成字段已对齐。
> 父frame完成后自动返回原frame reader/input/碰撞/held/生成余项→display其余出生/post→Q07正式DAT/角色图迁移；资源尚未部署、schema15/23/26/2/2、raw47/3、所有用户例外及stage.dat USER_HOLD保持。用户HUDBg x30保留；禁止computer-use、非战斗或Unity-GAS框架改造。总目标ACTIVE / FULL_ALIGNMENT_INCOMPLETE。

以下为历史恢复检查点，状态被上面的当前游标替代：

> **当前恢复游标（2026-09-14）：** C25L-STATE18-SPAWN-SOURCE-WITNESS已VERIFIED_SOURCE_MODEL_ONLY，1550行775出生+775完整driver、12759检查，重复字节相同；18条预设pending/无定义动作motion诊断留证，frame/lifecycle错误0。原证mixed OPoint slot50→state18 slots51..57→weapon pieces，源20/70顺序；generic HP/MP500/owner-1/team0/right、精确/整数位置分离；seed2/7/12/17/22/24覆盖持续1粒。**F08 Unity仍旧Match.Rng/普通OPoint出生，尚未修。下一唯一Task `NTSD28-Q06-C25L-STATE18-SPAWN-TRANSACTION-001 / READY_UNITY_RED_AND_EXACT_RECORD`**，保留已正确C25L owner与state13/200，只闭合剩余生成事务；之后回父Frame联合/reader/display-post/Q07。本轮仅一native诊断CPP，未新增Unity/Play结论，前碎片20/12/SelfCheck/173Play成果保持；schema15/23/26/2/2、raw47/3、资源未迁移、用户Scene bcd1047b…保持，禁止computer-use/非战斗/框架改动，总目标ACTIVE。

> **当前返回父帧事务（2026-09-14）：** fragment producer和SPAWN-ADMISSION-EDGE均已VERIFIED限定职责；20/12、5152、173Play、旧Scene四例及完整SelfCheck/关闭全0，不能沿用“fragment未写/GT08 FAIL”。下一唯一工作是本父Task的 **READY_FINAL_C25_CALLER_AND_TRACE_JOIN**：从现有实际代码逐个核对完整帧事务出口，复用2676原core与新1800fragment原证据，不重做已闭合数据载体/死亡前置/type2 fixture；补OPoint→state18/previous078→weapon pieces→lifecycle组合顺序、真实高动作/成本fallback及完整driver尚缺证据。当前完整SelfCheck已绿不等于父事务全闭合。若发现新规则差异须准确子Record；不做新框架重构。之后自动返回原reader迁移的input/碰撞/held/生成余项，再display出生/post与Q07。

> 当前下一Task NTSD28-Q06-NATIVE-WEAPON-PIECE-TRANSACTION-001。生命周期三字段和Module消费已接（386/实际Play限定通过），private shadow/旧857已移除；完整SelfCheck因旧break观察断言失败且fragment未实施，父任务不关闭。以下早期“待载体/Module未改”保留历史，不重做。

> 当前FRAME-TRANSACTION实施中：core2676已逐字段通过，Module仍旧857/HitStun消费，不能做完整高位Play验收。下一必要前置 NTSD28-Q06-NATIVE-LIFECYCLE-STATE-CARRIER-001 / READY_FOR_EXACT_PRECHANGE_RECORD，闭合独立runtime_state_code/pending/code后立刻返回本事务，不能把core通过当父任务完成。

> 前置NTSD28-Q06-NATIVE-SOUND-LATCH-CARRIER-001已VERIFIED / CARRIER_ONLY，当前五版本14/22/25/2/2已验；本Task现READY_FOR_EXACT_PRECHANGE_RECORD，使用现存NativeSoundActionLatch与FrameSounds建立Unity RED并成组接线。以下“缺载体/数据前置待”是形成过程，不再重做。

# Native帧事务成组接线

READY_FOR_EXACT_PRECHANGE_RECORD。父NATIVE-FRAME-RUNTIME-READER-MIGRATION；前置SOURCE-WITNESS已完成2676向量及CALLER-MAP；SNAPSHOT-BINDING此前已20/244/SelfCheck/真实Play验证。

首先完整读取 artifacts/diagnostics/NTSD28-Q06-NATIVE-FRAME-STEP-LIFECYCLE-SOURCE-WITNESS-001/CALLER-MAP.md、REPORT.md、validation.json 和frame-step.tsv。使用当前正式source及真实production caller，不复制旧FrameTick规则。没有新脚本实施授权路径清单前不能修改生产。

实施职责必须完整包含Native current/destination绑定、wait/latch/counter、hold/link/terminal14/type3/cpoint资格、正负next/212/999、signed成本原destination/next fallback/两消费统计、post-cost终止及encoded reset。current action、action latch、previous078、collision snapshot四者不可合并。完整driver previous078/OPoint/particle/weapon piece/lifecycle先后通过实际trace验证，不能以两个端点原函数见证替代。

数据前置先核对独立sound_action_latch与帧sound列表是否有现存载体。搜索当前Runtime未找到等价字段，现有OnFrameTickFrameChangedFromWaitCounter复用action latch并只读单sound。需要新增字段时必须准确列reset/copy/ECS hash/完整checksum/base shell/snapshot restore/版本策略与有序关闭；不凭方便加无快照字段或把旧schema宣称永远不变。音频资源整体替换仍不在范围，这里只处理原frame事件时序；已有声音例外先从主表核对。

现存InputModeCostMultiplier30/InputDoubleCost19C/InputCostWaived1B4/InputHpConsumedTotal34C/InputMpConsumedTotal350/CollisionYReference复用。BattleCharacterActionWriter.AdjustNativeMpCost可提炼共用纯signed调整，不能复用正输入成本不同的HP可支付条件与fallback。Transistor现存wait/next覆盖的live writer需追踪，不把dead legacy resolver变成正式入口，不以全局每tick重绑掩盖数据问题。

最少先Unity同输入RED覆盖2676原函数适用范围，标明跳过的完整driver前置并补实际driver用例；对negativewait等超当前DAT语料但原函数明确拒绝的输入保持准确语义。legacy/optimized两路径、Authority400/Mobile1050、snapshot/replay/checksum、完整SelfCheck和真实高位动作完整tick/encoded reset/cost fallback/对象关闭零残留。不能沿用资源owner或快照暂停probe作为完整高位动作证据。

修改前按上述闭合数据和行为范围建准确Change Record；若需先独立sound数据前置，子Task/Record必须明确出口和自动返回本任务，不能丢失frame修复。范围外Unity/GAS/Scene/InputActions/资源/Gen/Plugins/Server不动，用户HUDBg x30保持，禁止computer-use。完成reader后继续display其余出生、post、Q07正式资源；总目标仍未完成。

## 声音数据前置与回访边界（2026-09-14）

当前NATIVE-SOUND-LATCH-CARRIER-001实施独立Runtime.NativeSoundActionLatch/new-reset -1/copy/checksum/parity和entity14/aggregate22/checksum25；状态以该Record最终证据为准。数据前置结束自动返回本Task，不重做已验证snapshot descriptor binding。

source声音producer在C25g head与destination/cost前两次调用，按FrameSounds声明前20项并跳空值；Q05已经有FrameSounds readonly列表。事件顺序可以先用有声明声音路径的fixture队列验证，不依赖实际WAV替换；Q10（Q07/Q08后）承担实际WAV映射/播放/通道与音量效果回访。不要让Q06事件事务等待Q10资源播放再导致Q07/Q10循环；也不能把事件断言当实际播放通过。

source sound latch另外重置点为BattleWorld28 fusion set_action（2694）及definition transition（3649）。Unity ApplyStateDataTransform/TryApplyRuntimeIdentity/TryReloadCurrentFrameData各有不同caller，不能在FrameCache.Load或所有snapshot rebinding里一律清除；restore必须忠实恢复已保存latch。后继实际融合/定义转换行为接通时要在相应业务成功分支补重置并回访，本data包不宣称这些producer已经接通。
