# NTSD28-Q07-NARUTO-CLONE-CENTRAL-PIXEL-WITNESS-001

Status: `VERIFIED_REPRESENTATIVE_CENTRAL_PIXEL_ROUTE_ONLY / TEST_ONLY`. Parent BATCH-04/Q07; R17/R18 and Q09/Q12 retain final visual parity. See same-ID `artifacts/diagnostics/.../ACCEPTANCE.md` for the fresh third Play, both preserved test first differences and precise limits.

Authority and trigger: formal `decoded_dat/c/nar/ncl.dat` OID33 frame240/241 pic999 to frame242 pic1, `vfs/c/nar/ncl.png` SHA-256 `F8673833CCC0FC18695EBD956911704C8981B65179E3EEE343AA31F9597135B0`; prior natural physical L/D/J Play `q07-naruto-clone-2.json` proves the logic/catalog boundary but no central draw pixels. Actual source/crop audit is in that package's ACCEPTANCE.md. Use the existing running Editor and same physical-input probe; no second Editor and no computer-use.

Exact write scope: new `Assets/NTSD/Scripts/Test/Editor/NTSD28Q07CloneCentralPixelProbeEditor.cs` and its `.meta`, this Task, same-ID Change Record, Ledger/STATE/handoff, and unique diagnostics under `artifacts/diagnostics/NTSD28-Q07-NARUTO-CLONE-CENTRAL-PIXEL-WITNESS-001/`. Existing combo probe and all production/Scene/content/ProjectSettings/other tests are read-only.

Behavior: a unique request JSON causes the test-only observer to wait in real `NTSD_Battle` Play for OID33 frame242/pic1, then for a current central frame containing an entity render command with `VisualDataId=33`, `EffectivePic=1`. Capture the bound world camera into a temporary RenderTexture and PNG, reporting command identity, plan tick and non-clear pixels in the projected clone bounds. Start the existing natural-input Q07 request separately; no synthetic spawn or direct frame forcing. Do not count an unavailable camera/central frame as pass.

Acceptance and limit: fresh Unity script compile; one unique request/result pair; formal root/fingerprint and physical L/D/J result retained; OID33 command in current central submission and non-clear render pixels in its projected region; no Scene dirty/hash drift or request overwrite; focused Ledger validation. If capture is blocked or the region has zero pixels, preserve the failure and investigate. A pass proves a representative central-command/pixel route, not EXE image identity, occlusion/sorting parity, shadow, hit, skill lifetime or all characters. Rollback only the new Editor script/meta and this isolated request if needed; do not reset user work.
