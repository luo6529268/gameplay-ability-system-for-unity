# NTSD28-USER-KIND14-INTEGER-OPERAND-AUTHORITY-CORRECTION-001

Current status 2026-09-24: `FOCUSED_TEST_PASS / EXE_AND_SOURCE_HISTORY_PENDING`. Original-project Editor PID10576/bridge6403 exact job `23375124701a45939a59e88556eb5e15` executed 11 filtered cases, 11/11 PASS. The historical `IN_PROGRESS` line below predates this test. Source-rule coordinate/flag history and formal EXE fractional-position observation remain open; Q07 is not released.

Status: `IN_PROGRESS / AUTHORITY_CORRECTION / UNITY_COMPILE_PENDING`.
Parent: `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001` (D-024). This correction precedes source-domain flag integration and Q07 continuation.

Authority: the root formal `NTSD2.8-Logan.exe` SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`; its declared playable source is in the actual `build.ps1 -Target playable` closure. `physics_integrator.h:7-17` defines `Position28::x/z` as `int` and `precise_x/z` as `double`. The live `BattleWorld28::resolve_special_relation_hit` kind-14 branch in `battle_world.cpp:5542-5578` compares `attacker->position.x/z` with `target->position.x/z` plus strict raw 5/2. `PhysicsIntegrator28::step` then consumes and the World clears the one-tick flags. The prior project diagnosis/implementation erroneously described these operands as precise and changed Unity kind-14 branches to `Runtime.X/Z`. No formal EXE same-input fractional-position witness is claimed; this Task corrects the declared-source first difference and records the remaining runtime evidence gap.

Pre-change Unity state: `BattleBoundaryWriter`, `BattleEcsHitExecutionPlan` projection, `LF2CharacterDatHitResolver`, `LF2Entity`, `LF2SpecialAttack`, and `LF2CharacterHitResolver` use physical precise X/Z for kind-14 threshold comparisons. The last branch was not in the prior correction. Focused `BattleHitExecutionPlanEditorTests` and C32 `BattleRuntimeSelfCheck` currently assert the erroneous precise semantics at fractional 5.5/2.5 or 5.9/2.9.

Declared script scope: `Assets/NTSD/Scripts/Simulation/Ecs/Writers/BattleBoundaryWriter.cs`; `Assets/NTSD/Scripts/Simulation/Ecs/Hit/BattleEcsHitExecutionPlan.cs`; `Assets/NTSD/Scripts/Animation/LF2Objects/LF2CharacterDatHitResolver.cs`, `LF2Entity.cs`, `LF2SpecialAttack.cs`, `LF2CharacterHitResolver.cs`; `Assets/NTSD/Scripts/Test/Editor/BattleHitExecutionPlanEditorTests.cs`; `Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs`. No DAT, image, Scene, camera, ProjectSettings, other battle rule or nonbattle change. Keep the approved D-024 physical movement scaling. Source-rule *separate* flags remain inactive until continuous source motion and all position writers are complete.

Acceptance: all six production kind-14 consumers and projection use the same integer-mirror operand, retaining strict thresholds, velocity/pending impulse predicates and pass order. Focused cases cover positive/negative X/Z below/above integer thresholds, exact threshold and ShadowCompare; C32 direct self-check stays consistent. Run original-project Editor compile and only these focused cases once the existing mistakenly broad job is terminal, then focused ledger/diff/Scene/content guard. Do not launch a second Editor or broad suite. C: is currently full and the original Editor test job `2ed78c0b98214a5ba14ec92250d3b9f8` is nonterminal/bridge-timeout, so compilation and NUnit are presently pending. User permission for generated `Editor.log` truncation has been requested but not yet received.

Rollback: reverse only this package's operand and assertion changes with a reviewed patch, preserving all unrelated work. Supersede the prior precise-operand conclusion in its historical Task/Record and diagnostics by an appended correction; do not erase its earlier test evidence.
