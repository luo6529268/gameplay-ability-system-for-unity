# NTSD28-Q07-REMAINING-SPRITE-CONSUMER-AUDIT-001

Status: `DELIVERED_SCOPED_STATIC_CLASSIFICATION / Q07_OPEN`. Parent `NTSD28-UNITY-BATTLE-REALIGNMENT-001`, BATCH-04/Q07 with R17 content return. Entry: the fresh nonexcluded content gate reports 114 formal `sprite/*` PNG paths absent from staged `LoganRuntime` and states that actual selected battle consumers and approved exceptions still need classification. Exit evidence: `artifacts/diagnostics/NTSD28-Q07-REMAINING-SPRITE-CONSUMER-AUDIT-001/REPORT-20260926.md` and the exact 114-row CSV.

Authority: root formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable source in `GameSession28`, `RenderSnapshotBuilder28` and D3D11 renderer, plus formal runtime `decoded_dat` and `vfs`. User exclusions for native backgrounds, native mode DAT, complete native menu/selection/loading, character HUD, overhead bars and result presentation take precedence. Do not treat any DAT declaration as proof that a selected battle draw consumes a PNG.

Scope: read-only, exact path census of the 114 missing formal `sprite/*` PNGs; for each path record formal SHA-256, DAT declaration owner(s), approved exception or selected-path status, and the next proof needed. Cross-check current staged files and the formal root EXE. Produce an auditable CSV and concise report, then link the Q07/R17 status to this scoped result. No C# or C++ code, DAT values, PNG bytes, importer, Scene, Prefab, ProjectSettings, old resource or nonbattle component changes; no file deletion or automatic deployment.

Acceptance: 114/114 paths assigned to a category, count conservation verified, every row points to actual DAT declaration(s), and claims of active/nonactive use supported by concrete playable call sites or explicitly marked unproved. Preserve the eight declaration-only paths as an open question if no production reader is found. Recheck that currently selected WORDS0..5 and SPARK are already staged. Report that this is a static selected-path audit, not gameplay, pixel, release GUI or Q07 closure. `git diff --check` and live document NUL checks after documentation edits. Rollback is exact reviewed removal of this new audit document/CSV and status addenda, preserving all existing user work; destructive filesystem actions require separate authorization.
