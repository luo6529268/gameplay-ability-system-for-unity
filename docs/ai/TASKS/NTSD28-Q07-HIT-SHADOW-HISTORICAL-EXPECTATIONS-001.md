# NTSD28-Q07-HIT-SHADOW-HISTORICAL-EXPECTATIONS-001

Status: `FOCUSED_TEST_PASS / TEST_ONLY`. Parent: `NTSD28-UNITY-BATTLE-REALIGNMENT-001 / BATCH-04 Q07`.

The original Editor's preceding ShadowCompare package established writer-mask-zero and plan validity, but the adjacent legacy assertions failed. Job `7843cb4886ea46c684e0694dacfdd7d8` failed 21/21, with 18 standard and three then-compiled reduced cases. Current source has two reduced cases; the job's three-case names came from a transient earlier test assembly, so use a fresh current-assembly run for the final count.

Formal paired `battle_world.cpp::resolve_unarmored_reaction` adds `fall` and promotes totals above 60 to timer 80. The standard unarmored branch writes `bdefend_accumulator = 45`; the reduced branch adds raw `itr.bdefend` when runtime armor HP is nonpositive. These source paths do not assign Unity's historical `HitStateCount`. The old test expected HitStateCount 45/15 and timer 0 in three falling cases. First correct only these source-grounded expectations in `Assets/NTSD/Scripts/Test/Editor/BattleHitExecutionPlanEditorTests.cs`, then run the current standard/reduced selector. Any newly exposed failure needs its own source and observed-value audit before further rebaselining.

No production writer, DAT, images, Scene, config, framework or nonbattle behavior is in scope. Acceptance: current original Editor compilation with zero C# errors, accurate selected test count and failures, source-backed assertions, Ledger validator and diff check. The whole Q07 exit remains open. Rollback is an exact review of this test-only hunk, retaining all earlier user work.

Before further test-code editing: after current-assembly recompilation, job `0575071ec8034d8e9fa3f5797fbce2e2` selected 20/20 and every case reported `Expected: 12 / But was: 9`, including standard test methods that contain no 12 expectation. A single exact standard case reproduced this suspicious message in job `5d45bffb360640fd8c678aa9ef7da662`. Extend only the declared test file with a temporary opt-in diagnostic test that calls one standard and one reduced method directly and logs caught exception type/message/stack; remove that temporary probe after collecting evidence. Do not alter production or automatically rebase more assertions from these messages.

The direct probe job `cbfa79f34bc046509d3519a13de938f5` captured real NUnit stacks: both cases fail at `GetHitRecordX`, not an adapter attribution error. Formal `battle_world.cpp::append_confirmed_native_spark` uses two `random.crt_next()%9` draws; Unity `BattleNativeHitSparkWriter.Append` uses `world.NativeRandom.CrtNext()` twice. The old test seeds and checks only `world.Rng` and calculates from its raw seed. Expand the same two declared test methods to initialize `world.NativeRandom`, calculate expected jitter from its post-initialization CRT state, and assert its state/call count. Keep the temporary probe until the focused selector gives a trustworthy next result, then remove it. The test-only scope and rollback remain unchanged.

Exit: original Editor job `e093dd020cbf40c4888d216c5a740790` passed the current 18+2 selected cases 20/20. The temporary probe was removed and the original Editor recompiled with zero Console errors. Detailed evidence and remaining gates: `artifacts/diagnostics/NTSD28-Q07-SASUKE-ARMOR-TARGET-FIRST-DIFF-001/HISTORICAL-EXPECTATIONS-ACCEPTANCE-20260926.md`.
