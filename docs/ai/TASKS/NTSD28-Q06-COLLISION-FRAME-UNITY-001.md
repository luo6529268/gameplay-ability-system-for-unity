> 当前IN_PROGRESS / QUALIFICATION_RUNTIME_PASS_FULL_DRIVER_DEPENDENCIES；3264端点已清零，但4完整driver失败等BDEFEND writer与Spark CRT两子项。下一UNARMORED-BDEFEND-WRITER-AUDIT-001，之后HIT-SPARK-TRANSACTION-AUDIT-001，证据见qualification artifact REPORT。

> IN_PROGRESS / READER_RUNTIME_PASS_QUALIFICATION_PENDING。三处读帧已写，336×4 raw/descriptor/catch0，各24candidate首差保留；SelfCheck05:48:49Z/Play168/关闭PASS。下一必要子Task COLLISION-CURRENT-SNAPSHOT-QUALIFICATION-001，不以已修reader关闭父包。

# Unity 碰撞帧读取闭环

READY_UNITY_RED_AND_LIVE_GATES。前置 COLLISION-FRAME-SOURCE-WITNESS-001 已有252原输入/3780断言及重复输出，不重建旧见证代替推进。

先读取原 witness REPORT 的可达性限制和 COLLISION-FRAME-NATIVE-LOOKUP-AUDIT-001/REPORT.md、caller-inventory.json。已有35调用/7文件且未发现override或非战斗直接调用。保留用户HUDBg x30，不使用computer-use。

候选生产范围为 LF2Entity.GetCollisionFrameData、BruteForceSceneQuery.GetAuthoredCurrentFrame/GetAuthoredPrev2Frame及真实候选资格、BattleHitCandidatePairSnapshotFactory.Capture。修改任何脚本前另建精确Change Record，列定实际修改符号和测试文件；本Task不是改动其余所有reader的通用授权。

实施顺序：

1. 复用原252输入，在Unity观察两实体current/snapshot/native descriptor、候选和kind1结果，先RED。分别覆盖实际collector、role-aware缓存、HitPlan actual/Shadow和CPoint/no-op，不把两条Unity路径互相一致等同原版一致。
2. 对 source current1000/snapshot0 的诊断组合，先查正式pass可达性：driver在collection前立即snapshot；catch和hit消费在快照之后可遇到换帧/换定义。不能仅为让人为夹具变绿而删除正式current准入，也不能忽略原函数已有候选。分别记录API合同与正式可达输入的出口。
3. 当前definition+snapshot原查询不等于保留Prev2D旧定义指针；可用性以Native accessor为准。共享legacy frame API保留给未迁移路径。所有null分支和caller-local fallback单独审阅，确保未偷偷改用current。
4. pair factory还有current/previous旧查询，ordinary/role-aware资格还有旧HasFrame；需要共同闭合真实调用，准确扩展Record后才能编辑。若getter改变使kind1 raw动作写入缺口显现，另建子Record并原子完成必要行为，不隐藏失败或伪造全抓取完成。
5. compile、focused、必要旧碰撞/抓取测试、SelfCheck、真实Scene双factory/模式及有序关闭、Scene哈希/dirty保持。记录旧内容/合成DAT属性；正式Q07资源仍未部署。

后置独立审计：ApplyThrow sourceNextFrame/局部throwFrameSnapshot与旧ThrowInjury==-1变身分支的当前权威依据；CPoint/raw/held/input/hit/生成剩余reader；display/post→Q07。frame body、kind3/kind2已关闭职责不重做。epoch恢复缺口、raw47/3、schema15/23/26/2/2、stage.dat USER_HOLD和用户例外保持。
