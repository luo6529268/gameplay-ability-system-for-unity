# NTSD28-Q08-ATLAS-ORDERED-PAGE-STREAMING-001

Status: `RUNTIME_PENDING / PARTIAL_MEMORY_IMPROVEMENT`. Parent BATCH-04/Q08. Formal Logan content loads 145 atlas pages. Single-page ordered assembly reduced the sampled peak and advanced the real Scene from page 88 to page 119, but still crashed before test XML. Focused 3/3 PASS and exact memory limits are in `artifacts/diagnostics/NTSD28-Q08-RESULT-TRANSITION-HOST-AUDIT-001/ATLAS-ORDERED-STREAMING-PARTIAL.md`. This remains a battle-presentation resource-lifetime change, not a battle-rule change or a relaxation of formal content.

Exact script paths: `Assets/NTSD/Scripts/Animation/Runtime/BattleAtlasResources.cs` and `Assets/NTSD/Scripts/Animation/Rendering/Editor/BattleCommonAtlasBindingEditorTests.cs`. Preserve prior Q08 opt-in allocation trace, existing atlas plan, one-pixel extrusion, array path, ordered-page texture formats, binding keys, ownership, fallback failure cleanup, GPU pixel results and all content. When capability policy disallows an array, assemble and upload one CPU page at a time instead of retaining every CPU page until publication. If an attempted array later falls back, retain the existing assembled-page fallback and its cleanup contract. No GameConfig, Scene, DAT/image, GAS, J: authority or nonbattle edit. The isolated validation clone may receive the exact two-file patch after review; its Scene must remain unchanged.

Acceptance: focused multi-page CPU pixel equivalence against the existing whole-page assembler plus existing atlas binding tests, compile, one real isolated formal-content second-result Scene test with memory sampling, and Scene SHA unchanged. Only a test XML from that real Scene can advance second-result runtime acceptance; a crash or proxy cannot. If it passes, inspect first-rematch/second-result World and transition assertions and adjacent ordinary selection. Run Change Ledger validator and `git diff --check`. Rollback exact declared script changes after reviewed diff under repository rules.
