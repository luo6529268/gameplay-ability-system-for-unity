# NTSD28-Q07-KO-FEED-PRODUCTION-ROW-WITNESS-001

Status: FOCUSED_TEST_PASS. Parent: Q07 KO event clock and Q09 knockout feed presentation.

Authority and trigger: the formal root EXE/paired playable Naruto-punch trace has an ordinary KO on completed tick 8 with `battle_time_ticks=7`. Formal `render_snapshot.cpp` draws while `snapshot.tick < event_time+30` and retains spacing inclusively; Unity `BattleKnockoutFeedRowProjection` has equivalent predicates after the shared KO clock correction. The Q09 synthetic row test now passes, but no same-initial-state production tick-to-published-row trace exists. See `artifacts/diagnostics/NTSD28-Q07-KO-FEED-ROW-CODE-BOUNDARY-001/REPORT.md`.

Scope: extend only the existing Editor replay helper `NTSD28UnityRawCaptureEditor.WithLoganScenarioForReplayTests` with an optional project-mode-Asset flag, preserving its default call behavior. Add one focused Editor test `NTSD28Q07KnockoutFeedProductionRowEditorTests.cs` and `.meta` that reuses the existing 30-tick formal Naruto scenario and its selected staged Logan runtime, appends neutral inputs through tick 38, runs the real `SimulationTickDriver` with presentation enabled, and records/compares event time and published row count at the birth and strict/inclusive 30-tick boundary. No production runtime, mode Asset, DAT, Scene, prefab, nonbattle code or scenario JSON edits.

Acceptance: compile in the original occupied Editor with zero compile errors; discover and run this exact test with nonzero selection. Require first KO tick8/time7 and one published row at tick8/36, no row but retained native record at tick37/38; fail honestly if actor or mode gates change the actual outcome. Save a 38-tick CSV and test-job JSON, validate Menu/Battle/GameConfig/ProjectBattleModeConfig disk hashes, `git diff --check` and Change Ledger. Formal source predicate derivation plus the current root-EXE event trace are code/trace authority, but this Unity test alone cannot claim formal GPU pixel equality or close Q07/Q09.

Risk and rollback: enabling project mode in a replay scope can activate resource-dependent publication and alter the test's behavior; keep the opt-in flag default false so all existing callers remain unchanged. If the test finds another first difference, preserve the failing report and split a production fix into a new package. Roll back only this optional helper parameter/call and the new test file after reviewing this package; preserve all pre-existing dirty work.

Code written: existing helper takes `useProjectMode = false` and forwards it to its already-existing content scope; old callers retain their default. The new one-case test opts in, records 38 ticks to CSV and asserts event/row boundary at ticks 7/8/36/37/38. Original Editor compile/run is pending.

First-run correction: original Editor compiled 0 errors and discovered 1/1. At tick 6 the test threw in `LF2ObjectPool.Get` because the helper had set logic-only materialization before the battle allocation seal, whereas the existing formal raw-capture path calls `BeginBattleAllocationSeal()` first and then enables logic-only materialization. The initial five-tick CSV and exact NUnit failure stack are preserved. Also, the test used the positional `StepOneTick(frame, true, false)` overload, which means `ignorePaused=true, buildPresentation=false`; it must use named `buildPresentation: true`. Correct these two test-harness issues only in the declared files; do not change production or lower assertions.

Second-run correction: the full 38-tick CSV now matches the predicted event/row boundaries, but `WithLoganScenarioForReplayTests` reports post-disposal `objects=2, slots=2, borrowers=0`. This fails the lifecycle acceptance gate even though row formulas align. Capture the existing shutdown report's status/stage/reason in the helper diagnostic, then fix only proven fixture setup/teardown; do not weaken the zero-residual gate.

Shutdown cause: the next exact run reports `shutdown=Failed`, stage `PendingObjectPointTasksDiscarded`, reason `object-pool-runtime-state-is-invalid`; `LF2ObjectPool`'s managed collections are null in the existing EditMode singleton after domain reload. Add an isolated fresh `LF2ObjectPool` test scope only for the helper's project-mode opt-in, using the established `RendererScope` test pattern: save/restores its singleton field, create a hidden GameObject, explicitly invoke `Awake` only if its runtime state is invalid, dispose after Driver orderly shutdown. Do not change `LF2ObjectPool` production or suppress the zero-residual assertion.

Final result: original Editor compiled 0 errors and the exact single test passed 1/1 (job `3a25c06fb233489f972a390006a92e70`). The final 38-tick CSV has first KO at tick8/time7, one native record and one published row through tick36, and zero rows at ticks37/38 while the record remains. The helper's zero-object/zero-slot/zero-borrower post-shutdown gate passed. All three prior failed runs and their corrections remain recorded. Four protected disk hashes are unchanged. This closes the scoped EditMode production-row witness only; Q07/Q09 formal pixel and broader exits remain open.
