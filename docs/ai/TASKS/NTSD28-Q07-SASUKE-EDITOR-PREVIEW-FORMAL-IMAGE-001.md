# NTSD28-Q07-SASUKE-EDITOR-PREVIEW-FORMAL-IMAGE-001

Status: ISOLATED_VISIBLE_PREVIEW_PASS / ORIGINAL_EDITOR_RELOAD_PENDING. Parent: BATCH-04 / Q07 indexed old-image owner migration.

Authority and prechange: D-023 selects formal Logan character images. Formal OID11 DAT maps pic0 to `vfs/c/sasu/sasu.png` (799×960, 79×79 cells, 10 rows/12 columns), staged SHA-256 `2AEB4FF667A9EE63E60EE0E866E4700C780974746D47E231CAEF1EAFCAD7C694`. The inactive Battle Scene `BattleCentralEditorPreview` stores old `sasuke_0.bmp` GUID and bottom-origin rect (0,481,79,79). Its Inspector sample and EditMode validation fallback point to the same old BMP. `BMPLoader.LoadBmpData` accepts PNG, but the preview's `OwnedSpriteCache` currently forces BMP black-key pixel processing; formal PNG must preserve straight RGBA as production `PrepareBattleSheetPixels` does. The BMP grid test is a historical decoder check and must not be silently rewritten into a fake PNG gutter assertion.

Exact write scope: `Assets/NTSD/Scripts/Animation/Rendering/BattleCentralEditorPreview.cs` (preview-only PNG alpha branch), `Assets/NTSD/Scripts/Animation/Rendering/Editor/BattleCentralEditorPreviewEditor.cs` (sample path), `Assets/NTSD/Scripts/Animation/Rendering/Editor/BattleCentralEditorPreviewEditorTests.cs` (validation fallback and focused formal PNG check), `Assets/NTSD/Scene/NTSD_Battle.unity` (only inactive preview sourceSheet GUID and rect y). Record/ledger/status docs and focused evidence are permitted. Do not change importers, formal PNG bytes, runtime entity rendering, HUD, Menu Scene, GameConfig, other Battle Scene objects, old BMP or its existing grid test in this task.

Exit: verify imported Texture2D dimensions and raw PNG first-frame geometry before Scene edit; focused EditMode PNG decoder/alpha/crop check, existing preview tests and preview validation output pass; Scene remains clean after EditMode, its exact diff contains only the two intended preview fields, HUDBg x30 remains, Menu Scene hash remains; compile and Change Ledger pass. The old BMP remains until all retained tests/readers and exact delete approval are dealt with. If the importer dims or render differ, record the failure and stop this rebind rather than altering the importer under this task.

Rollback: reverse only this task's exact preview script and Scene hunks after recording evidence, subject to repository restore/delete approval; never reset the dirty worktree. No computer-use.

Progress: dependency `NTSD28-Q07-SASUKE-PNG-EXACT-IMPORT-DIMENSIONS-001` passed after the initial 1024-width RED. The preview PNG alpha branch, Editor example and fallback path, and inactive Scene GUID/rect are written. `git diff` shows precisely two Scene field changes; HUDBg x30 is preserved. A hash-matched isolated Unity copy passed the formal PNG test 1/1, preview class 11/11, and existing visible preview validation with a rendered PNG and JSON archived in package PROGRESS. The original Editor had an external-Scene-change Reload/Ignore modal and has not been confirmed reloaded; original in-memory Scene/render acceptance remains pending. The user was asked to choose Reload manually because computer-use is prohibited. Do not mark the original-Editor preview or Q07 aggregate VERIFIED.
