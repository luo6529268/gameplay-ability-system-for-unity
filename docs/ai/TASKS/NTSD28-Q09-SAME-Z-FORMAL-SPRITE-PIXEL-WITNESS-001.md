# NTSD28-Q09-SAME-Z-FORMAL-SPRITE-PIXEL-WITNESS-001

Status: VERIFIED_CONTROLLED_CENTRAL_GPU_ONLY. Parent: BATCH-05/Q09 P-04; existing same-Z scene order witness is complete only for materialized order. This package's original-Scene controlled pixel result passed, while parent natural full-tick, Legacy and formal EXE exits remain open.

Authority: the formal NTSD2.8-Logan EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, corresponding playable `render_snapshot.cpp` final `entity_commands` order (Z ascending, equal-Z physical slot descending), and formal runtime catalog OID120 `w/4.dat`/`w/4.png` and OID121 `w/5.dat`/`w/5.png`. Their frame 0/pic 0 sprites are 48x48 with distinguishable opaque overlap; the staged PNGs were compared by SHA to the formal runtime. The user confirmed both original Battle Scene cameras default to enabled and this task must leave them so.

Bounded change: add one Editor-only Play probe `Assets/NTSD/Scripts/Test/Editor/NTSD28Q09SameZFormalSpritePixelProbeEditor.cs` and Unity-generated `.meta` if imported. In the original `NTSD_Battle` Scene and production `CentralOnly` World, safely pause at an idle worker boundary, create only two owned OID120/121 objects through the production OPoint factory, retain real frame/pic and formal sprites, align their screen bounds and equal Z, and inspect the central publication/submission. Capture baseline, A-only, B-only and overlap via the original world camera with a temporary render target and restored camera state. The same overlap pixel must distinguish both singly rendered colors and match the expected later painter in the composite. The current central plan cache keys on simulation tick; while the logic driver is paused, use distinct explicitly labeled diagnostic presentation tick numbers for these controlled reference captures, without advancing battle logic, and restore publication at the real current tick after cleanup. This proves the production drawing outlet under controlled publication, not a complete natural battle tick. Record handles/generations, physical slots, frame/pic, command order, submission, exact sampled pixels, camera state, and cleanup. A controlled `Camera.Render()` is a GPU outlet witness, not proof of the user's unmodified natural display frame.

Protection: no DAT token, existing Scene/Prefab, camera serialisation, ProjectSettings, production runtime, menu/HUD, or nonbattle changes. Never toggle `Camera.enabled`, save the Scene, or clear unknown user entities. Capture and restore the pre-probe pause state, both RNG streams, sound queue, and the probe-owned entities/pools. Check original Battle Scene disk SHA and Git diff before and after Play; stop with a specific failure if an external Scene change appears. Do not launch a second Unity project/Editor or use computer-use.

Acceptance: original Editor compile 0 errors, one focused original Battle Scene Play controlled-publication result with both formal drawable resources submitted, equal Z and greater physical slot painted first, at least one discriminating overlapping pixel matching the later painter, and object/slot/pool/pause/camera/Scene baselines restored. Run only the focused checks needed for this probe. If a prerequisite fails, report the first reason and retain P-04 as runtime pending. Even a PASS leaves natural full-tick rendering, Legacy and formal EXE same-condition A/B as independent parent exits.

Rollback: review the exact new test file and `.meta`, then remove only those files if separately authorized; leave pre-existing user work untouched. The probe's transient runtime entities and render target must clean themselves in `finally`.

Result: original Editor compile 0 errors; focused original Battle Scene Play controlled GPU 11/11 discriminating overlap pixels followed the later slot51 OID120 body (slot52 OID121 command earlier), with 0 opposing/mixed pixels; runtime objects, slots, two pools and pause restored; original Battle Scene SHA/diff unchanged after Play exit. Formal staged PNG SHA matched original runtime. An independent reviewer found and then rechecked correction of three test-only P2 faults; no remaining P0-P2 affecting this scoped result. See `artifacts/diagnostics/NTSD28-Q09-SAME-Z-FORMAL-SPRITE-PIXEL-WITNESS-001/ACCEPTANCE.md`. Do not promote the older unrestricted-pixel PASS or this controlled result to parent P-04/Q09 completion.
