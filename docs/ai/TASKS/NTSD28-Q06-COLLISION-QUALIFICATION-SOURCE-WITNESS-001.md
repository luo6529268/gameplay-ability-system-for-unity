> VERIFIED / SOURCE_MODEL_ONLY；480端点+2完整driver、3020断言、两遍一致，详见同ID Record和artifact。

# 原准入与消费证据

IN_PROGRESS / SOURCE_ONLY。准确单CPP及合同见同ID Change Record，服务COLLISION-CURRENT-SNAPSHOT-QUALIFICATION-001；parent collision仍有96次candidate首差，不能忽略。

补非零current/previous state、effect/group冻结过滤、current缺帧但snapshot有效时的后继分类；另验证完整driver里kind3换帧是否让后继攻击仍用原snapshot。完成才据实际结果声明Unity普通/cached/consumer精确修改，不调整平台current职责。
