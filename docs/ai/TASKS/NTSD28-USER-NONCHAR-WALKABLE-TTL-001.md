# NTSD28-USER-NONCHAR-WALKABLE-TTL-001

Status: VERIFIED for the scoped D-025 continuous off-walkable TTL rule. Code, original-Editor focused tests, standard BattleRuntimeSelfCheck and normal/F5-route 305-step complete Driver Play witnesses passed in two separate Battle Scene sessions. Physical keyboard F5, natural projectile travel and dedicated-worker free-run remain Q12 integration evidence, not part of this scoped claim. It supersedes only formal noncharacter immediate offstage X destruction timing; character bounds, DAT, content, Scene and nonbattle modules remain unchanged.

Authority: user's 2026-09-24 choice: non-character battle objects continuously outside the project's walkable area for 10 seconds of battle logic time are cleared. F5 changes wall-clock cadence, not the required logical tick count. Map source is the current `BattleBootstrap`/`BattleMapCatalog`/`BoundaryWallManager` loaded walkable polygon union. The formal playable stage-bounds branch is comparison evidence, not the timing authority for this exception.

Pre-change: `LF2Entity.ApplyPreFrameXBounds` immediately frees type3 beyond `[-300,width+300]` and grounded other non-type0 objects outside `[0,width]`; controlled factor1 Editor RED showed X50 ordinary/ OID122 50 and790 formal first differences. `BoundaryWallManager.IsPointWalkable` reads Unity scene objects and cannot be called by the dedicated simulation worker. No continuous off-walkable timer exists in the runtime canonical snapshot/checksum.

Declared script paths: `Assets/NTSD/Scripts/Simulation/Stage/BattleWalkableAreaSnapshot.cs` (new pure-data geometry); `Assets/NTSD/Scripts/Simulation/Stage/SimulationStageRenderModule.cs` (main-thread capture, worker-safe query, reset); `Assets/NTSD/Scripts/Simulation/Core/SimulationWorld.cs` (query bridge); `Assets/NTSD/Scripts/Animation/LF2Objects/LF2Entity.cs` (noncharacter PreFrame lifetime, no change to type0); `Assets/NTSD/Scripts/Simulation/Core/NTSDEntityRuntime.cs` (timer state/copy/reset); `Assets/NTSD/Scripts/Simulation/Lockstep/Snapshot/BattleWorldEntityRuntimeSnapshot.cs` (schema); `Assets/NTSD/Scripts/Simulation/Lockstep/Checksum/BattleLockstepChecksumModule.cs` (timer checksum/schema); `Assets/NTSD/Scripts/Test/Editor/NTSD28NonCharacterWalkableTtlEditorTests.cs` (new focused tests); `Assets/NTSD/Scripts/Test/BattleRuntimeSelfCheck.cs` (replace superseded immediate-cull assertions and verify timed world removal); `Assets/NTSD/Scripts/Test/Editor/NTSD28NonCharacterWalkablePlayProbeEditor.cs` (original Battle Scene Play-only diagnostic, auto exit/no scene save). If a conflicting existing focused test must change, amend this Task/Change code-path list before editing it.

Invariants: capture the current loaded/scene walkable union before worker submit; no Unity Component, Transform, camera or Time API on worker tick; no per-tick allocation from polygon query. Use battle physical X/Z ground point; unavailable/empty boundary does not trigger deletion. Re-entry clears consecutive timer. Elapsed 10,000 ms at exact 33 ms/tick requires 304 elapsed ticks, using current tick index and a sentinel. Reset on reuse and snapshot/restore; checksum includes timer. Keep existing pool/free entry and ordered shutdown. OID122/123 clamping is separate position behavior; do not silently remove it with the timed cull.

Acceptance: exact RED/GREEN outside threshold (303/304 elapsed ticks), re-entry/re-exit, character exclusion, type3 and weapon/other inclusion, polygon concavity/edge, unavailable boundary, DataOriented/Legacy, F5 same tick result, runtime copy/reset, snapshot/restore and checksum. Run the narrow Editor tests in the existing original Editor, related stage/bounds tests, ledger validator and diff check, then a safe target Battle Scene Play. If current Editor Scene state blocks Play, report `RUNTIME_PENDING`; do not save user Scene or open a second project.

Risk: changing noncharacter lifetime can affect spawn counts, hit candidates and cleanup timing; adding canonical state bumps snapshot/checksum schemas. Rollback only declared package files after provenance review; preserve all existing user modifications and original diagnostic RED evidence.
