# NTSD28-USER-SOURCE-COORDINATE-STATE9996-BIRTH-001

Status: FOCUSED_TEST_PASS / CARRIER_NOT_ACTIVE. Parent D-024. This bounded package covers only the five-child `state 9996` native synchronized clone birth in `BattleLateEntityLifecycleModule.SpawnState9996Children`.

Authority: paired formal playable `BattleWorld28` clone branch (`battle_world.cpp` around 2915-3020) samples `random_x`, then makes each child's absolute integer position `source_position.x + random_x`, `source_position.z + 1`; `spawn_transient` initializes matching precise values. Unity already scales the relative battle X/Z offsets for the fixed full-background camera but currently creates no source-rule child history. The local DAT and random ranges are rule data, not to be edited.

Exact script scope: `Assets/NTSD/Scripts/Simulation/Passes/LateLifecycle/BattleLateEntityLifecycleModule.cs` and `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06State9996DirectSpawnEditorTests.cs`. Do not edit other scripts, DAT, images, Scene, camera or ProjectSettings.

Implementation: on the existing task after the already drawn `relativeX`, when the spawner's source position is initialized, set explicit source precise X and Z from parent source **integer** X/Z plus raw `relativeX` and +1, and explicit initial source integers to the same values. If absent, leave child source absent. Keep all five objects, OIDs, slot admission, action/facing/velocity/random call order, battle physical scaled positions and `skipPostInitZOffset` untouched. Do not derive source values by dividing battle coordinates or activate source-rule readers.

Acceptance: original Editor test-first RED/GREEN using existing formal state9996 five-child witness, factor1 and configured 2048×1152 view, initialized and absent-source controls; assert all spawned children's source precise/int, current battle X/Z and random state, including child #5. Run only focused tests, ledger validator, diff check, Scene/resource boundary check. Real Play is required only if a lifecycle mismatch appears in the focused branch. Rollback only these two scoped script changes, preserving all other dirty work.

Execution 2026-09-24: original Editor RED a727a04aebad4f55ac86daf50826e759 executed 4 cases; absent controls passed, initialized cases failed at child source initialized false. GREEN aad133506b7a485b8d3e29566f50f935 passed 6/6: four new five-child source cases and two existing physical-ratio cases. Source parent precise/int deliberately differ; all five children, fifth OID218, exact random calls/final random state, physical ratio positions and pool cleanup pass. Fresh original Editor compilation succeeded, error console empty. Ledger749/diff/Scene/content guards PASS. No full SelfCheck, following-tick matrix or real Play run; carrier not active, Q07 remains open.
