# NTSD28-Q08-FORMAL-SOURCE-PIXEL-WITNESS-001

Status: `RUNTIME_PENDING`. Parent BATCH-04/Q08. Authority: formal Logan DAT/image content and the existing Unity battle presentation contract. Source budget fallback has passed focused binding/order tests and a real Scene result-path test, but no post-change camera pixels have been captured. This task is a test-only witness, not a change to battle rules or rendering behavior.

Exact code path: `Assets/NTSD/Scripts/Test/Editor/NTSD28Q08FormalSourcePixelPlayModeTests.cs`. Add one D3D11 Play Mode test that opens the current Battle Scene, waits for formal content publication and a valid central pixel frame, verifies Auto selected `SourceTexture2D` because the published plan exceeded its budget, captures the production world camera into a temporary RenderTexture after releasing any central submission lease, writes a PNG and small JSON report, and requires non-clear pixels. After batchmode revealed that its world-camera renderer does not invoke the registered feature, add a request-driven runner in the same exact test script for the already-open original Editor. It must first confirm the saved Battle Scene is active and not dirty, run only the named test, record a result, and avoid a second Editor on the original Library. Preserve the existing Scene and all nonbattle paths. No DAT/image deletion, GameConfig, production renderer, GAS or J: authority edits.

Acceptance: process exit and NUnit XML, formal fingerprint/source-policy evidence, captured PNG/JSON with counts, Scene SHA unchanged, Change Ledger validation. A non-clear image proves a representative post-change camera route only; command attribution and formal EXE pixel parity remain separate gates. Rollback removes only the new test file and this task's tracking entries after reviewing their diff.
