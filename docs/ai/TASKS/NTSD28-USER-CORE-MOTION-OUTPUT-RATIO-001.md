# NTSD28-USER-CORE-MOTION-OUTPUT-RATIO-001

Status: `FOCUSED_TEST_PASS / RUNTIME_PENDING`. Parent `NTSD28-USER-ALL-ENTITY-MOTION-RATIO-001`; user policy D-024. This is the first bounded implementation slice of the all-entity motion task, not its completion. Original Editor focused suites passed 4/4, 11/11, 13/13, 4/4 and 5/5 after refresh; an additional synthetic full core tick passed, as did one production-materialized type-1 weapon physics pass. These prove the declared samples, not the full driver/Play output or all entity kinds. Details and job IDs are in the Change Record.

Observed baseline: fixed Training view 2048×1152, formal default camera 1333×730. Before this slice, run/dash source writers multiplied stored X/Z velocity before shared physics and altered the later one-unit friction result; this slice removed those early multipliers. Character and non-character production physics integrates X/Z velocity through `CharacterMechanics`, but native identity-X and type-3 visual-Z displacement are direct pre-integration writes.

Implementation boundary: let run/dash/heavy-run writers again write raw formal DAT-derived velocity on all legacy/shared/Native character paths. Apply world-configured `Sx=2048/1333`, `Sz=1152/730` exactly once to X/Z *position delta* at the three shared mechanics methods, after existing collision-block decisions and before friction mutates velocity. Pass immutable scales from the owning World through the three character callers and the non-character callers. Apply the same X/Z factors to the two direct non-character motion adds in `RunSharedNonCharacterDatFrameAdvance`. Diagnostic Worlds default both factors to one. Do not scale Y yet: airborne altitude, platform collision references and landing predicates need a separate vertical coordinate contract. Do not edit DAT, camera, Scene, nonbattle code, spawn/held/teleport paths or hitbox extents in this slice.

Exact code ownership: `Assets/NTSD/Scripts/Animation/Character/CharacterMechanics.cs`; `Assets/NTSD/Scripts/Animation/LF2Objects/LF2Character.cs`, `LF2Entity.cs`, `LF2WeaponBase.cs`, `LF2CharacterActionResolver.cs`, `LF2CharacterStateResolver.cs`; `Assets/NTSD/Scripts/Simulation/Ecs/Passes/BattleEcsCharacterFrameAdvancePass.cs`, `Assets/NTSD/Scripts/Simulation/Ecs/Writers/BattleCharacterActionWriter.cs`; focused tests `Assets/NTSD/Scripts/Test/Editor/NTSD28FixedViewRunRatioEditorTests.cs`, `NTSD28NativeType0GroundBuiltinsEditorTests.cs`, `NTSD28NativeType0AirDashRedirectBuiltinsEditorTests.cs`. Existing GameConfig/World setup is an input, not edited here.

Follow-up validation ownership before test edit: `Assets/NTSD/Scripts/Test/Editor/NTSD28Q06CanonicalCharacterPhysicsTailEditorTests.cs` may add one configured-world case to its existing production `NativePhysicsAndDeadCharacterResourceNormalizeAll` fixture, running both Legacy and DataOriented modes. Assert X/Z position factors and raw post-friction velocity, using the existing synthetic physics definition; keep prior default-world cases and source expectations intact. This is a production physics-pass witness, not a full `SimulationTickDriver` or real Play witness.

Next test-only amendment: reuse that same declared fixture and configured case for one `NTSDBattleTickSystem.RunReleaseTick` attempt under the same World/profile. Record a genuine first difference if any later pass changes the final observation; do not weaken the earlier physics-pass test or claim Host/Scene Play from the core tick alone.

Non-character validation amendment before test edit: in the same declared Q06 test file, add one configured World with a synthetic type-1 weapon definition, materialize through the production logic factory, run the production non-character physics pass once, and assert X/Z ratio and raw velocity. This tests one actual non-character caller of the shared integrator. It does not cover every weapon state, OPoint spawn, other direct writers or real Scene Play; no DAT data or production code changes are authorized by this amendment.

Acceptance: one configured full mechanics tick keeps authored velocity/friction result raw while X/Z positions gain the target viewport fraction. Cover ordinary run, dash direction and one non-character integrated motion case; default scale 1 must preserve current focused behavior. Compile the original Unity Editor and run only the relevant focused suites first; broader tests only for a new first difference. Verify DAT status clean and Battle Scene SHA unchanged. The remaining Y, OPoint spawn/initial placement, held/CPoint/teleport, boundary and camera/aspect cases stay explicitly open.

Rollback: reviewed inverse patch limited to this Change's exact lines/files, preserving all concurrent worktree content. Never use reset/restore/clean.
