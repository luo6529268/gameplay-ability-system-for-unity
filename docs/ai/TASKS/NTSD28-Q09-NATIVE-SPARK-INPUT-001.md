# NTSD28-Q09-NATIVE-SPARK-INPUT-001

Status: VERIFIED. Parent: BATCH-05/Q09, R14/R17. Authority: formal root EXE SHA B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033 and playable `GameSession28::initialize_resources`, `render_snapshot.cpp` spark projection, `d3d11_renderer.cpp` spark blit. The existing read-only reachability audit is `artifacts/diagnostics/NTSD28-Q09-NATIVE-SPARK-ATLAS-REACHABILITY-001/REPORT.md`.

First difference: formal runtime uses `resource.dat` index 43 for `sprite/UI/SPARK.png`, `system.dat` spark_w=99/spark_h=79 and raw 0..99 spark IDs. The configured Unity candidate fingerprints only character file/head/small images and WORDS/KO adjuncts; the common SPARK publication still uses the old 20-cell BMP. A formal SPARK PNG or system dimension change can therefore reuse an obsolete candidate. This package adds only immutable input capture and freshness identity, leaving the subsequent publication/renderer replacement explicit and open.

Exact scripts before edit: `Assets/NTSD/Scripts/Animation/LoganVisualContentCandidate.cs` and `Assets/NTSD/Scripts/Test/Editor/NTSD28B11VisualContentCandidateEditorTests.cs`. Test fixture may create Temp files only. No DAT token, image file, Scene, Prefab, GameConfig, HUD, sound or battle logic edit. Preserve 906 file/head/small `Images` count and project mode Asset selection. Resource/system absent together in historical minimal fixtures keeps the existing optional behavior; partial native input fails closed.

Acceptance: formal candidate exposes index43 path, 99×79 dimensions and formal SHA; a changed PNG, resource selection or system dimensions invalidates the old candidate and changes visual/source-cache identity; legacy minimal candidate remains valid. Original Editor compile 0 errors, focused candidate tests pass, Scene hashes unchanged, ledger/diff checks pass. This package does not claim native SPARK publication, either render path or Q09 completion. Rollback: remove only this package's input/candidate/test diff after reviewing the exact worktree; preserve all unrelated edits.
