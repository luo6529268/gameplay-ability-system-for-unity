# NTSD28-Q07-KIND-DAT-IDENTITY-PREPARED-WORLD-001

Status: FOCUSED_TEST_PASS. Parent: BATCH-04/Q07 and R15. Original Unity project only.

Authority: formal EXE SHA-256 `B1E13AE17C86B77240B61A971AFD4C3374B645705F42B0BBCE304FD1D2819033`, paired playable `KindCatalog28` selection and `GameSession28` setup, D-023 formal `resources/runtime/decoded_dat/data/kind.dat` SHA-256 `39E30DF8D86A5FC374B26C80BE358A6503B2C3096D8681C586478D73A0900011`. Use the already tested `LoganKindCatalogInput`; formal/staged byte equality and precise authority call chain are in the Q07 kind audit and parser Record.

Pre-change: kind selection/parser exists and passes 24/24 in the original Editor, but `LoganObjectCatalog` has no kind input; current V2 identity contains object/fusion/mode only. `LoganVisualContentCandidate` does not test kind freshness, and `BattleRuntimeDataCatalog` does not hold its immutable table. Formal V2 trace vectors and fixed tests must remain explicitly historical; they cannot certify a new seven-component content identity.

Declared production paths: `Assets/NTSD/Scripts/Animation/LoganObjectCatalog.cs`, `Assets/NTSD/Scripts/Animation/LoganContentIdentity.cs`, `Assets/NTSD/Scripts/Animation/LoganVisualContentCandidate.cs` (existing Q09 dirty work must be preserved), `Assets/NTSD/Scripts/Simulation/Runtime/BattleRuntimeDataCatalog.cs`. The Editor trace emitter `Assets/NTSD/Scripts/Test/Editor/NTSD28TraceContentIdentity.cs` and parity validator `Tools/NTSD28Parity/TraceContentIdentity.cs` belong to the same version boundary. Declare exact focused test paths in the Change Record before editing them. No combat consumer, nonbattle flow, Scene, Prefab, audio, or source resource is changed by this package.

Contract: always capture selected or locked-fallback kind input for a Logan runtime catalog. With mode present, append kind raw/semantic fingerprints after the existing five components under new `NTSD28_LOGAN_BATTLE_INPUTS_V3` (seven SHA components). When mode is absent, use an explicit distinct `NTSD28_LOGAN_BATTLE_INPUTS_V3_KIND_ONLY` tag over object/fusion/kind (five SHA components); never fill missing mode with zero or a fabricated fingerprint. Preserve V1/V2 constructors and historical vectors. The decoder semantic tag may stay V3 because the battle-input tag and preimage change; catalog semantic and 64-bit projection must change. Candidate freshness must reject kind path/bytes changes before publication. World Prepare must verify every captured component including kind, publish exactly the selected immutable catalog before seal, and retain it read-only through battle. Legacy `loganCatalog == null` preparation remains supported. Trace emitter must identify both new variants and include two kind SHA fields. Parity validator must recognize them and reject cross-version comparisons; formal native V2 capture is not promoted to V3 by relabeling. Versioned native V3 capture and same-seed comparison remain a separate R15 exit.

Acceptance: independent V3 digest vector; formal and staged kind file produce the same seven-component identity; selected bytes/path/fallback transitions invalidate a candidate; prepared World identity/table match publication and cannot be replaced while sealed; V1/V2 historical fixtures still validate as their own versions; V3 trace header validates and malformed/cross-version headers reject. Run original Editor compilation and focused EditMode tests plus parity self-test, scoped diff and Change Ledger validator. A real Battle Scene smoke is needed before claiming battle publication runtime, and no kind-dependent hit/transform behavior is claimed until consumer packages.

Rollback: reverse only declared code/test changes after diff review and the file deletion approval required by AGENTS.md; preserve all existing dirty work. Versioned fixtures and source content are never cleaned as rollback.
