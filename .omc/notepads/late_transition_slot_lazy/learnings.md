2026-07-25: SpawnLateTransitionEffects now returns before effect-resource lookup and dynamic-slot scanning when neither state transition branch is eligible. For an eligible branch 2 transition, keep the count/RNG decision before any resource-dependent slot calculation so missing resources and full capacity preserve the existing RNG sequence.

Verification: git diff --check passed for LF2Entity.cs. dotnet build NTSD_Scripts.csproj --no-restore is blocked by 204 pre-existing CS2001 missing-source errors in generated project references (GAS and TopDownEngine), so no successful compiler result is available.

2026-07-25: Late opoint spawning must reserve its exact dynamic runtime slot before fetching a task or touching LF2ObjectPool. `spawner.Match?.FindFirstFreeFrameLogicRuntimeSlot() ?? -1` preserves the authority's lowest-slot selection and must run once per attempted child so each successful registration is immediately reflected in the next lookup. A focused Editor fixture can isolate LF2ObjectPool's available/active collections to distinguish a rejected spawn that performs no construction from one that allocates then recycles.

Verification: `dotnet build Assembly-CSharp-Editor.csproj --no-restore -v:minimal` completed with 0 errors (18 existing assembly-version warnings). The focused test source also compiled in a temporary explicit-reference project with 0 warnings / 0 errors. Unity Editor was already using the project, so no second Unity instance or batch EditMode test was launched.

2026-07-26: Stage B removed only the outer LateEntityUpdate runtime snapshots after StateSpecial, a non-exiting FrameExit, and PrevFrameMirror. State transforms remain immediately synchronized by `ApplyStateDataTransform`, frame groups 11/12 remain immediately synchronized inside `HandleLateFrameTickExit`, and `MirrorLatePrevFrame` remains a `Frame.Prev`-only write. The three diagnostic enum/report slots remain stable and now naturally report zero calls.

Verification: `dotnet build Assembly-CSharp.csproj --no-restore -p:BuildProjectReferences=false -v:minimal` and the matching `Assembly-CSharp-Editor.csproj` build both completed with 0 errors. `git diff --check` passed. Direct call-site inspection found exactly the six retained Late snapshot stages (Recovery, FrameTickSuppressed, FrameTick, DeathOpoint, CleanupCompleted, TailAndQueuedFlush) and none of the three removed stages in `SimulationWorld.Passes.partial.cs`. The broad `NTSD_Scripts.csproj` build remains blocked by pre-existing generated-project CS2001 missing-source references.
