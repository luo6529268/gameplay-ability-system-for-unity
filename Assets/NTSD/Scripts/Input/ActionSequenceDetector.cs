using MoreMountains.Tools;

namespace NTSD.Input
{
    /// <summary>
    /// Legacy placeholder (MonoBehaviour).
    ///
    /// 项目已迁移到纯 C# 的 `ActionSequenceDetectorModule`（由 Character 创建并由 SimulationWorld 驱动）。
    /// 保留此脚本仅用于避免旧 prefab/scene 引用断裂（Missing Script）。
    /// </summary>
    public class ActionSequenceDetector : MMMonoBehaviour
    {
    }
}

